<template>
  <tabs class="view-tabs" label="Debug inspector views">
    <tab class={overview_tab_class} onactivate={onOverview} onclick={onOverview}>
      <text class="view-tab-label">Overview</text>
    </tab>
    <tab class={modules_tab_class} onactivate={onModules} onclick={onModules}>
      <text class="view-tab-label">Modules</text>
    </tab>
    <tab class={surfaces_tab_class} onactivate={onSurfaces} onclick={onSurfaces}>
      <text class="view-tab-label">Surfaces</text>
    </tab>
    <tab class={backend_tab_class} onactivate={onBackendServices} onclick={onBackendServices}>
      <text class="view-tab-label">Backends</text>
    </tab>
    <tab class={benchmark_tab_class} onactivate={onBenchmark} onclick={onBenchmark}>
      <text class="view-tab-label">Bench</text>
    </tab>
  </tabs>
</template>

<script lang="luau">
</script>

<style>
  .view-tabs {
    width: 100%;
    gap: var(--spacing-md);
    flex-wrap: nowrap;
    border-bottom-width: 1px;
    border-bottom-style: solid;
    border-bottom-color: var(--color-outline-variant);
  }

  .view-tab {
    flex-shrink: 0;
    padding-inline: var(--spacing-xs);
    padding-block: var(--spacing-sm);
    background: transparent;
    border-width: 0;
    border-bottom-width: 2px;
    border-bottom-style: solid;
    border-bottom-color: transparent;
  }

  .view-tab-active {
    border-bottom-color: var(--color-primary);
  }

  .view-tab-label {
    font-size: var(--typography-size-xs);
    font-weight: 600;
    color: var(--color-on-surface-variant);
    text-align: center;
  }

  .view-tab-active .view-tab-label {
    font-size: var(--typography-size-xs);
    font-weight: 700;
    color: var(--color-primary);
    text-align: center;
  }
</style>
