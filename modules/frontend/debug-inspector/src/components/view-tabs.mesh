<template>
  <tabs class="view-tabs" label="Debug inspector views">
    <tab class={overview_tab_class} onactivate={onOverview} onclick={onOverview} aria-label="Overview" tooltip="Overview">
      <text class="view-tab-icon">▦</text>
    </tab>
    <tab class={elements_tab_class} onactivate={onElements} onclick={onElements} aria-label="Elements" tooltip="Elements">
      <text class="view-tab-icon">⌖</text>
    </tab>
    <tab class={modules_tab_class} onactivate={onModules} onclick={onModules} aria-label="Modules" tooltip="Modules">
      <text class="view-tab-icon">◫</text>
    </tab>
    <tab class={surfaces_tab_class} onactivate={onSurfaces} onclick={onSurfaces} aria-label="Surfaces" tooltip="Surfaces">
      <text class="view-tab-icon">▱</text>
    </tab>
    <tab class={backend_tab_class} onactivate={onBackendServices} onclick={onBackendServices} aria-label="Backends" tooltip="Backends">
      <text class="view-tab-icon">⚙</text>
    </tab>
    <tab class={benchmark_tab_class} onactivate={onBenchmark} onclick={onBenchmark} aria-label="Benchmarks" tooltip="Benchmarks">
      <text class="view-tab-icon">◷</text>
    </tab>
  </tabs>
</template>

<script lang="luau">
</script>

<style>
  .view-tabs {
    width: 100%;
    justify-content: center;
    gap: var(--spacing-xs);
    flex-wrap: nowrap;
    border-bottom-width: 1px;
    border-bottom-style: solid;
    border-bottom-color: var(--color-outline-variant);
  }

  .view-tab {
    width: 36px;
    height: 32px;
    flex-shrink: 0;
    padding: 0;
    justify-content: center;
    align-items: center;
    background: transparent;
    border-width: 0;
    border-bottom-width: 2px;
    border-bottom-style: solid;
    border-bottom-color: transparent;
  }

  .view-tab-active {
    border-bottom-color: var(--color-primary);
  }

  .view-tab-icon {
    font-size: var(--typography-size-md);
    font-weight: 600;
    color: var(--color-on-surface-variant);
    text-align: center;
  }

  .view-tab-active .view-tab-icon {
    font-size: var(--typography-size-md);
    font-weight: 700;
    color: var(--color-primary);
    text-align: center;
  }
</style>
