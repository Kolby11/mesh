<template>
  <tabs class="view-tabs" label="Debug inspector views">
    <tab class="view-tab" onactivate={onOverview} onclick={onOverview}>
      <column class="view-tab-copy">
        <text class="view-tab-label">Overview</text>
        <box class="view-tab-indicator" hidden={overview_indicator_hidden} />
      </column>
    </tab>
    <tab class="view-tab" onactivate={onModules} onclick={onModules}>
      <column class="view-tab-copy">
        <text class="view-tab-label">Modules</text>
        <box class="view-tab-indicator" hidden={modules_indicator_hidden} />
      </column>
    </tab>
    <tab class="view-tab" onactivate={onSurfaces} onclick={onSurfaces}>
      <column class="view-tab-copy">
        <text class="view-tab-label">Surfaces</text>
        <box class="view-tab-indicator" hidden={surfaces_indicator_hidden} />
      </column>
    </tab>
    <tab class="view-tab" onactivate={onBackendServices} onclick={onBackendServices}>
      <column class="view-tab-copy">
        <text class="view-tab-label">Backends</text>
        <box class="view-tab-indicator" hidden={backend_services_indicator_hidden} />
      </column>
    </tab>
    <tab class="view-tab" onactivate={onBenchmark} onclick={onBenchmark}>
      <column class="view-tab-copy">
        <text class="view-tab-label">Bench</text>
        <box class="view-tab-indicator" hidden={benchmark_indicator_hidden} />
      </column>
    </tab>
  </tabs>
</template>

<script lang="luau">
</script>

<style>
.view-tabs {
    width: 100%;
    gap: 2px;
    flex-wrap: nowrap;
}

.view-tab {
    min-width: 0;
    flex-grow: 1;
    padding-inline: var(--spacing-xs);
    padding-block: var(--spacing-xs);
    border-radius: var(--radius-md);
    background: var(--color-surface-container);
    border-width: 1px;
    border-style: solid;
    border-color: var(--color-outline-variant);
}

.view-tab-copy {
    align-items: center;
    gap: var(--spacing-xs);
}

.view-tab-label {
    font-size: var(--typography-size-xs);
    font-weight: 700;
    color: var(--color-on-surface);
    text-align: center;
}

.view-tab-indicator {
    width: 100%;
    height: 3px;
    border-radius: var(--radius-full);
    background: var(--color-primary);
}
</style>
