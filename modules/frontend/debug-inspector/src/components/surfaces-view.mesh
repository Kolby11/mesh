<template>
  <column class="surfaces-view" hidden={hidden}>
    <text class="section-title">Surfaces</text>
    <text class="section-copy">{state_copy}</text>

    <empty-state class="empty-card" hidden={empty_hidden}>
      <column class="empty-copy">
        <text class="empty-title">No recent surface activity</text>
        <text class="empty-text">Surfaces stay listed here once recent work has been recorded.</text>
      </column>
    </empty-state>

    <list class="surface-list" label="Recent surfaces">
      {#for row in rows}
        <list-item class="surface-card">
          <column class="surface-copy">
            <text class="surface-title">{row.surface_id}</text>
            <text class="surface-meta">{row.module_id}</text>
            <text class="surface-metric">{row.stage_label}</text>
            <text class="surface-metric">{row.render_label}</text>
            <text class="surface-metric">{row.redraw_label}</text>
            <text class="surface-metric">{row.paint_policy_label}</text>
            <text class="surface-metric">{row.paint_filter_label}</text>
          </column>
        </list-item>
      {/for}
    </list>
  </column>
</template>

<script lang="luau">
hidden = false
rows = {}
</script>

<style>
  .surfaces-view,
  .surface-list {
    gap: var(--spacing-md);
  }

  .section-title {
    font-size: var(--typography-size-sm);
    font-weight: 700;
    color: var(--color-on-surface);
  }

  .section-copy,
  .empty-text,
  .surface-meta,
  .surface-metric {
    font-size: var(--typography-size-xs);
    color: var(--color-on-surface-variant);
  }

  .empty-card,
  .surface-card {
    width: 100%;
    padding: var(--spacing-md);
    border-radius: var(--radius-md);
    background: var(--color-surface-container);
    border-width: 1px;
    border-style: solid;
    border-color: var(--color-outline-variant);
  }

  .empty-copy,
  .surface-copy {
    gap: var(--spacing-xs);
  }

  .empty-title,
  .surface-title {
    font-size: var(--typography-size-sm);
    font-weight: 700;
    color: var(--color-on-surface);
  }
</style>
