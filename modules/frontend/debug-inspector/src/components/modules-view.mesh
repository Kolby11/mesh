<template>
  <column class="modules-view">
    <text class="section-title">Modules</text>
    <text class="section-copy">{state_copy}</text>

    <empty-state class="empty-card" hidden={empty_hidden}>
      <column class="empty-copy">
        <text class="empty-title">No module graph loaded</text>
        <text class="empty-text">Installed module contracts appear here after the shell publishes the debug snapshot.</text>
      </column>
    </empty-state>

    <list class="module-list" label="Installed module graph">
      <list-item class="module-card" hidden={row1_hidden}>
        <column class="module-copy">
          <row class="module-head">
            <text class="module-title">{row1_module_id}</text>
            <text class="module-kind">{row1_kind_label}</text>
          </row>
          <text class="module-meta">{row1_status_label}</text>
          <text class="section-subtitle">Uses</text>
          <text class="module-metric">{row1_uses_interfaces}</text>
          <text class="module-metric">{row1_uses_resources}</text>
          <text class="section-subtitle">Provides</text>
          <text class="module-metric">{row1_provides_interfaces}</text>
          <text class="module-metric">{row1_provides_resources}</text>
          <text class="section-subtitle">Health</text>
          <text class="module-metric">{row1_health_label}</text>
        </column>
      </list-item>

      <list-item class="module-card" hidden={row2_hidden}>
        <column class="module-copy">
          <row class="module-head">
            <text class="module-title">{row2_module_id}</text>
            <text class="module-kind">{row2_kind_label}</text>
          </row>
          <text class="module-meta">{row2_status_label}</text>
          <text class="section-subtitle">Uses</text>
          <text class="module-metric">{row2_uses_interfaces}</text>
          <text class="module-metric">{row2_uses_resources}</text>
          <text class="section-subtitle">Provides</text>
          <text class="module-metric">{row2_provides_interfaces}</text>
          <text class="module-metric">{row2_provides_resources}</text>
          <text class="section-subtitle">Health</text>
          <text class="module-metric">{row2_health_label}</text>
        </column>
      </list-item>

      <list-item class="module-card" hidden={row3_hidden}>
        <column class="module-copy">
          <row class="module-head">
            <text class="module-title">{row3_module_id}</text>
            <text class="module-kind">{row3_kind_label}</text>
          </row>
          <text class="module-meta">{row3_status_label}</text>
          <text class="section-subtitle">Uses</text>
          <text class="module-metric">{row3_uses_interfaces}</text>
          <text class="module-metric">{row3_uses_resources}</text>
          <text class="section-subtitle">Provides</text>
          <text class="module-metric">{row3_provides_interfaces}</text>
          <text class="module-metric">{row3_provides_resources}</text>
          <text class="section-subtitle">Health</text>
          <text class="module-metric">{row3_health_label}</text>
        </column>
      </list-item>

      <list-item class="module-card" hidden={row4_hidden}>
        <column class="module-copy">
          <row class="module-head">
            <text class="module-title">{row4_module_id}</text>
            <text class="module-kind">{row4_kind_label}</text>
          </row>
          <text class="module-meta">{row4_status_label}</text>
          <text class="section-subtitle">Uses</text>
          <text class="module-metric">{row4_uses_interfaces}</text>
          <text class="module-metric">{row4_uses_resources}</text>
          <text class="section-subtitle">Provides</text>
          <text class="module-metric">{row4_provides_interfaces}</text>
          <text class="module-metric">{row4_provides_resources}</text>
          <text class="section-subtitle">Health</text>
          <text class="module-metric">{row4_health_label}</text>
        </column>
      </list-item>
    </list>
  </column>
</template>

<script lang="luau">
</script>

<style>
.modules-view,
.module-list {
    gap: token(spacing.md);
}

.section-title,
.section-subtitle,
.empty-title,
.module-title {
    font-size: token(typography.size.sm);
    font-weight: 700;
    color: token(color.on-surface);
}

.section-copy,
.empty-text,
.module-meta,
.module-metric {
    font-size: token(typography.size.xs);
    color: token(color.on-surface-variant);
}

.empty-card,
.module-card {
    width: 100%;
    padding: token(spacing.md);
    border-radius: token(radius.md);
    background: token(color.surface-container);
    border-width: 1px;
    border-style: solid;
    border-color: token(color.outline-variant);
}

.empty-copy,
.module-copy {
    gap: token(spacing.xs);
}

.module-head {
    width: 100%;
    align-items: center;
    justify-content: space-between;
    gap: token(spacing.sm);
}

.module-kind {
    padding-inline: token(spacing.sm);
    padding-block: token(spacing.xs);
    border-radius: token(radius.full);
    background: token(color.secondary-container);
    color: token(color.on-secondary-container);
    font-size: token(typography.size.xs);
    font-weight: 700;
}
</style>
