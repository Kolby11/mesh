<template>
  <box class="blur-test-shell">
    <column class="blur-test-stack">
      <box class="band band-1"><text class="band-label">RED - 5% fill</text></box>
      <box class="band band-2"><text class="band-label">GREEN - 15% fill</text></box>
      <box class="band band-3"><text class="band-label">BLUE - 30% fill</text></box>
      <box class="band band-4"><text class="band-label">AMBER - 50% fill</text></box>
      <box class="band band-5"><text class="band-label">SOLID - 100% fill</text></box>
    </column>
  </box>
</template>

<script lang="luau">
</script>

<style>
.blur-test-shell {
    width: 640px;
    height: 560px;
    padding: 0;
    background: transparent;
}

.blur-test-stack {
    width: 100%;
    height: 100%;
    gap: 14px;
}

.band {
    width: 100%;
    height: 96px;
    justify-content: center;
    align-items: center;
    border-radius: 16px;
}

/* Keep the fills deliberately faint. With compositor blur enabled, fine text
behind a band is expected to disappear, but large light/dark window areas and
boundaries must still affect the band. The progression then adds increasingly
more tint. The fifth band is the fully opaque control. */
.band-1 { background: rgba(224,  49,  49, 0.05); }
.band-2 { background: rgba( 47, 158,  68, 0.15); }
.band-3 { background: rgba( 51, 110, 245, 0.30); }
.band-4 { background: rgba(240, 170,   0, 0.50); }
.band-5 { background: rgb(  20,  22,  30); }

.band-label {
    font-size: 20px;
    font-weight: 700;
    color: #ffffff;
}
</style>
