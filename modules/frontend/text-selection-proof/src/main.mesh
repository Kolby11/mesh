<template>
  <box class="proof-shell">
    <column class="proof-card">
      <text class="proof-eyebrow">Selectable text proof</text>
      <text class="proof-copy" selectable="true">
        Shell status copy remains passive until selection exists, and this first release keeps selection inside one wrapped text block.
      </text>
      <text class="proof-note">
        The surrounding frame stays non-selectable so the proof surface does not imply broader shell-wide copy behavior.
      </text>
    </column>
  </box>
</template>

<style>
.proof-shell {
    width: 100%;
    height: 100%;
    padding: token(spacing.md);
    background: transparent;
}

.proof-card {
    width: 100%;
    height: 100%;
    gap: token(spacing.sm);
    padding: token(spacing.md);
    border-radius: token(radius.lg);
    background: token(color.surface-container);
    color: token(color.on-surface);
    border-width: 1px;
    border-style: solid;
    border-color: token(color.outline-variant);
}

.proof-eyebrow {
    font-size: token(typography.size.xs);
    font-weight: 700;
    color: token(color.on-surface-variant);
}

.proof-copy {
    font-size: token(typography.size.md);
    line-height: token(typography.line-height.normal);
    color: token(color.on-surface);
}

.proof-note {
    font-size: token(typography.size.sm);
    line-height: token(typography.line-height.normal);
    color: token(color.on-surface-variant);
}
</style>
