<template>
  <text class="meta-label">{t('nav.audio')}</text>
</template>

<script lang="luau">
-- Standalone presentational label using the built-in translation helper.
</script>

<style>
.meta-label {
    color: token(color.on-surface-variant);
    font-size: token(typography.size.xs);
    font-weight: 600;
    text-transform: uppercase;
    text-wrap: none;
    transition: color token(animation.duration.short) token(animation.curves.bezier.standard);
}

@container (max-width: 720px) {
    .meta-label {
        width: content;
    }
}

@container (max-width: 560px) {
    .meta-label {
        display: none;
    }
}
</style>
