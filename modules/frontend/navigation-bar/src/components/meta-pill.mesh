<template>
  <box class="meta-pill">
    <text class="meta-pill-text">live</text>
  </box>
</template>

<script lang="luau">
-- Standalone presentational pill using the built-in translation helper.
</script>

<style>
.meta-pill {
    width: content;
    flex-shrink: 0;
    text-wrap: none;
    justify-content: center;
    align-items: center;
    padding-block: token(spacing.xs);
    padding-inline: token(spacing.sm);
    border-radius: token(radius.full);
    background: token(color.tertiary-container);
    transition: background-color token(animation.duration.short) token(animation.curves.bezier.standard),
                transform token(animation.duration.short) token(animation.curves.bezier.standard);
}

.meta-pill-text {
    color: token(color.on-tertiary-container);
    font-size: token(typography.size.sm);
    font-weight: 700;
    text-transform: uppercase;
}

.meta-pill:hover {
    background: token(color.secondary-container);
    transform: scale(1.02);
}

.meta-pill-text:hover {
    color: token(color.on-secondary-container);
}

@container (max-width: 560px) {
    .meta-pill {
        display: none;
    }
}
</style>
