---
phase: 47-taffy-layout-adapter-integration
plan: 03
title: Layout Parity Shipped Surface Gates And Docs
type: execute
wave: 3
depends_on: [47-01, 47-02]
files_modified:
  - crates/core/ui/elements/src/layout.rs
  - crates/core/shell/src/shell/component/tests/integration/real_surfaces.rs
  - crates/core/shell/src/shell/component/tests/interaction/navigation.rs
  - crates/core/shell/src/shell/component/tests/invalidation/profiling.rs
  - docs/renderer-migration.md
  - docs/renderer-ownership.md
  - docs/frontend/renderer-contract.md
autonomous: true
requirements: [LAYT-01, LAYT-02, LAYT-03]
requirements_addressed: [LAYT-01, LAYT-02, LAYT-03]
user_setup: []
must_haves:
  truths:
    - "D-07: Stable NodeId, runtime keys, retained dirty categories, and retained render-object geometry synchronization are hard gates."
    - "D-08: Existing shipped navigation/audio behavior is the acceptance target."
    - "D-09: Layout diagnostics distinguish Taffy mapping gaps from paint, text, presentation, or shell lifecycle problems."
    - "D-10: Parity tests cover rows, columns, stacks, fixed sizes, gaps, padding, absolute positioning, and container-width cases."
    - "D-11: Shipped navigation and audio fixtures are canonical parity cases."
    - "D-12: Tests compare resulting LayoutRect geometry and retained identity effects where possible."
    - "D-13: Audio popover transition-delay todo remains deferred to v1.10 and is not Phase 47 scope."
  artifacts:
    - path: "crates/core/ui/elements/src/layout.rs"
      provides: "Roadmap-required layout parity coverage"
      contains: "phase47_taffy"
    - path: "crates/core/shell/src/shell/component/tests/integration/real_surfaces.rs"
      provides: "Shipped navigation/audio Taffy layout regression proof"
      contains: "phase47"
    - path: "docs/frontend/renderer-contract.md"
      provides: "Author-facing layout replacement note"
      contains: "Taffy-backed layout"
  key_links:
    - "LAYT-02 required cases -> mesh-core-elements layout tests"
    - "LAYT-01 shipped surfaces -> shell phase47 tests"
    - "LAYT-03 unsupported cases -> diagnostics docs and no hidden fallback"
---

<objective>
Prove the Taffy replacement with required layout parity tests, shipped navigation/audio regression coverage, retained geometry/profiling checks, and documentation that reflects the new authoritative layout path.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/47-taffy-layout-adapter-integration/47-CONTEXT.md
@.planning/phases/47-taffy-layout-adapter-integration/47-RESEARCH.md
@.planning/phases/47-taffy-layout-adapter-integration/47-VALIDATION.md
@.planning/phases/47-taffy-layout-adapter-integration/47-PATTERNS.md
@crates/core/ui/elements/src/layout.rs
@crates/core/shell/src/shell/component/tests/integration/real_surfaces.rs
@crates/core/shell/src/shell/component/tests/interaction/navigation.rs
@crates/core/shell/src/shell/component/tests/invalidation/profiling.rs
@modules/frontend/navigation-bar/src/main.mesh
@modules/frontend/navigation-bar/src/components/volume-button.mesh
@docs/renderer-migration.md
@docs/renderer-ownership.md
@docs/frontend/renderer-contract.md
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| T-47-03-01: synthetic layout tests pass but shipped navigation/audio geometry regresses | high | Add `phase47` shell tests around real navigation/audio surfaces and run existing Phase 44 navigation proof tests. |
| T-47-03-02: parity coverage misses one of the required LAYT-02 cases | high | Add tests whose names or assertions explicitly cover row, column, stack, fixed size, gap, padding, absolute positioning, and container-width behavior. |
| T-47-03-03: docs still tell maintainers that current layout remains authoritative | medium | Update migration, ownership, and renderer contract docs to state Taffy-backed layout is authoritative for in-scope semantics after Phase 47. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add roadmap-required Taffy layout parity tests</name>
  <files>crates/core/ui/elements/src/layout.rs</files>
  <read_first>crates/core/ui/elements/src/layout.rs, .planning/phases/47-taffy-layout-adapter-integration/47-CONTEXT.md, .planning/phases/47-taffy-layout-adapter-integration/47-VALIDATION.md</read_first>
  <action>Add or update focused tests in `crates/core/ui/elements/src/layout.rs` with names containing `phase47_taffy`. The tests must verify all required LAYT-02 cases with concrete `LayoutRect` assertions: row layout places two 100px children at x=0 and x=100; column layout with `gap = 10.0` places the second child at y=60; stack/container behavior preserves overlapping or zero-gap children according to existing MESH semantics; fixed-size children retain exact width/height; padding of 10 moves first child to x=10 and y=10; absolute child with right=10 and bottom=10 in a 300x200 parent lands at x=210 and y=150 for an 80x40 child; percent/container-width child with width 50% in a 300px parent computes width 150. Each assertion must use exact values or a tolerance of 0.5 or less.</action>
  <verify>env XDG_CACHE_HOME=/tmp/codex-nix-cache nix develop -c cargo test -p mesh-core-elements phase47_taffy</verify>
  <acceptance_criteria>
    - `crates/core/ui/elements/src/layout.rs` contains `phase47_taffy`.
    - `crates/core/ui/elements/src/layout.rs` contains `x=210` or `210.0`.
    - `crates/core/ui/elements/src/layout.rs` contains `width 150` or `150.0`.
    - `crates/core/ui/elements/src/layout.rs` contains `gap = 10.0`.
    - `env XDG_CACHE_HOME=/tmp/codex-nix-cache nix develop -c cargo test -p mesh-core-elements phase47_taffy` exits 0.
  </acceptance_criteria>
  <done>LAYT-02 required layout cases are covered by focused Taffy replacement tests.</done>
</task>

<task type="auto">
  <name>Task 2: Add shipped navigation/audio Phase 47 regression tests</name>
  <files>crates/core/shell/src/shell/component/tests/integration/real_surfaces.rs, crates/core/shell/src/shell/component/tests/interaction/navigation.rs, crates/core/shell/src/shell/component/tests/invalidation/profiling.rs</files>
  <read_first>crates/core/shell/src/shell/component/tests/integration/real_surfaces.rs, crates/core/shell/src/shell/component/tests/interaction/navigation.rs, crates/core/shell/src/shell/component/tests/invalidation/profiling.rs, modules/frontend/navigation-bar/src/main.mesh, modules/frontend/navigation-bar/src/components/volume-button.mesh, .planning/phases/47-taffy-layout-adapter-integration/47-CONTEXT.md</read_first>
  <action>Add Phase 47 shipped-surface tests with names containing `phase47`. Reuse `real_frontend_module_component("@mesh/navigation-bar", audio_network_catalog())` and existing audio popover helpers. Verify the navigation root and audio surface render with non-zero width and height after Taffy layout, the volume button or audio control hit region remains inside its parent layout rectangle, and focused proof or profiling payloads still contain layout/damage evidence after a repaint. Do not assert transition timing or animation delay; that todo remains deferred to v1.10.</action>
  <verify>env XDG_CACHE_HOME=/tmp/codex-nix-cache nix develop -c cargo test -p mesh-core-shell phase47</verify>
  <acceptance_criteria>
    - At least one file under `crates/core/shell/src/shell/component/tests` contains `phase47`.
    - A Phase 47 test contains `@mesh/navigation-bar`.
    - A Phase 47 test references `@mesh/audio-popover` or audio controls.
    - Phase 47 tests do not contain `transition delay`.
    - `env XDG_CACHE_HOME=/tmp/codex-nix-cache nix develop -c cargo test -p mesh-core-shell phase47` exits 0.
  </acceptance_criteria>
  <done>Shipped navigation/audio surfaces have focused Phase 47 Taffy layout regression coverage.</done>
</task>

<task type="auto">
  <name>Task 3: Update author and migration docs, then run final gates</name>
  <files>docs/renderer-migration.md, docs/renderer-ownership.md, docs/frontend/renderer-contract.md</files>
  <read_first>docs/renderer-migration.md, docs/renderer-ownership.md, docs/frontend/renderer-contract.md, .planning/phases/47-taffy-layout-adapter-integration/47-CONTEXT.md, .planning/phases/47-taffy-layout-adapter-integration/47-VALIDATION.md</read_first>
  <action>Update `docs/frontend/renderer-contract.md` to state that after Phase 47, in-scope `.mesh` layout semantics are computed by Taffy-backed layout while author-facing `.mesh` syntax, service proxies, shell surface lifecycle, and presentation remain stable. Update `docs/renderer-migration.md` Phase 47 record with final gate commands and note that audio popover transition delay remains deferred to v1.10. Update `docs/renderer-ownership.md` so Taffy layout is no longer described as merely proof evidence for in-scope layout semantics. Run and record outcomes for: `env XDG_CACHE_HOME=/tmp/codex-nix-cache nix develop -c cargo test -p mesh-core-elements layout`; `env XDG_CACHE_HOME=/tmp/codex-nix-cache nix develop -c cargo test -p mesh-core-shell phase47`; `env XDG_CACHE_HOME=/tmp/codex-nix-cache nix develop -c cargo test -p mesh-core-shell phase44_navigation`; `env XDG_CACHE_HOME=/tmp/codex-nix-cache nix develop -c cargo test -p mesh-core-render proof`; and `env XDG_CACHE_HOME=/tmp/codex-nix-cache nix develop -c cargo check -p mesh-core-shell`. If a command cannot run because of sandbox, disk, Nix cache, or native dependency limits, record the exact blocker in the plan summary.</action>
  <verify>rg -n "Taffy-backed layout|Phase 47|audio popover transition delay remains deferred|phase47" docs/renderer-migration.md docs/renderer-ownership.md docs/frontend/renderer-contract.md</verify>
  <acceptance_criteria>
    - `docs/frontend/renderer-contract.md` contains `Taffy-backed layout`.
    - `docs/renderer-migration.md` contains `audio popover transition delay remains deferred`.
    - `docs/renderer-migration.md` contains `cargo test -p mesh-core-elements layout`.
    - `docs/renderer-migration.md` contains `cargo test -p mesh-core-shell phase47`.
    - `docs/renderer-ownership.md` no longer describes Taffy as only proof evidence for in-scope Phase 47 layout semantics.
    - Final command outcomes or exact blockers are recorded in `47-03-SUMMARY.md` during execution.
  </acceptance_criteria>
  <done>Docs and final gates reflect Taffy-backed layout as the in-scope authoritative path.</done>
</task>

</tasks>

<verification>
- Run `env XDG_CACHE_HOME=/tmp/codex-nix-cache nix develop -c cargo test -p mesh-core-elements phase47_taffy`.
- Run `env XDG_CACHE_HOME=/tmp/codex-nix-cache nix develop -c cargo test -p mesh-core-elements layout`.
- Run `env XDG_CACHE_HOME=/tmp/codex-nix-cache nix develop -c cargo test -p mesh-core-shell phase47`.
- Run `env XDG_CACHE_HOME=/tmp/codex-nix-cache nix develop -c cargo test -p mesh-core-shell phase44_navigation`.
- Run `env XDG_CACHE_HOME=/tmp/codex-nix-cache nix develop -c cargo test -p mesh-core-render proof`.
- Run `env XDG_CACHE_HOME=/tmp/codex-nix-cache nix develop -c cargo check -p mesh-core-shell`.
</verification>

<success_criteria>
- LAYT-02 parity cases are tested with concrete geometry assertions.
- Shipped navigation/audio surfaces are covered by Phase 47 tests.
- Retained identity, dirty categories, profiling/damage evidence, and render-object geometry sync remain preserved or explicitly tested.
- Docs state Taffy-backed layout is authoritative for in-scope layout semantics after Phase 47.
- Audio popover transition delay remains deferred to v1.10.
</success_criteria>

