---
phase: 87-layout-and-display-elements
plan: 01
title: Metadata And Diagnostics For Layout Display Primitives
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/core/ui/elements/src/element.rs
  - crates/core/ui/elements/src/lib.rs
autonomous: true
requirements: [ELEMLAYOUT-01, ELEMLAYOUT-02, ELEMLAYOUT-04, ELEMLAYOUT-05, ELEMDISPLAY-01, ELEMDISPLAY-02, ELEMDISPLAY-03]
requirements_addressed: [ELEMLAYOUT-01, ELEMLAYOUT-02, ELEMLAYOUT-04, ELEMLAYOUT-05, ELEMDISPLAY-01, ELEMDISPLAY-02, ELEMDISPLAY-03]
user_setup: []
must_haves:
  truths:
    - "D-87-01: Use mesh-core-elements as the source of truth for Phase 87 metadata and diagnostics."
    - "D-87-02: Keep progress as the only implemented progress-like display primitive in this phase; meter remains taxonomy/future behavior."
    - "D-87-03: Keep grid scope conservative and diagnostic-backed."
  artifacts:
    - path: "crates/core/ui/elements/src/element.rs"
      provides: "Phase 87 attributes, diagnostics, accessibility defaults, and tests"
      contains: "phase87_layout_display_contract"
---

<objective>
Extend the element contract with Phase 87-specific attributes, roles, style hooks, and validation rules for layout, structure, display, progress, and tooltip semantics.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Add Phase 87 attribute groups and style hooks</name>
  <files>crates/core/ui/elements/src/element.rs</files>
  <read_first>crates/core/ui/elements/src/element.rs, .planning/phases/87-layout-and-display-elements/87-CONTEXT.md</read_first>
  <action>Add static attribute definitions for layout alignment/spacing/sizing/overflow/scroll, grid tracks and placement, stack layer metadata, structure labels, display labels, image/avatar/icon metadata, shortcut keys, tooltip ownership, and progress min/max/value/indeterminate metadata. Attach those attributes to the relevant `ElementContractDef` entries without broadening unrelated future controls. Add stable style hook names for layout/display tags.</action>
  <verify>nix develop -c cargo test -p mesh-core-elements phase87_layout_display_contract</verify>
  <acceptance_criteria>
    - `grid` exposes track/gap/placement metadata.
    - `scroll-area` exposes overflow/scroll metadata.
    - `section`, `header`, `footer`, `group`, and `form-row` expose structure metadata.
    - `badge`, `avatar`, `shortcut`, `tooltip`, and `progress` expose display metadata.
    - `meter` remains present in taxonomy but does not receive duplicate runtime behavior metadata beyond common docs-compatible metadata.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add invalid attribute/value diagnostics</name>
  <files>crates/core/ui/elements/src/element.rs</files>
  <read_first>crates/core/ui/elements/src/element.rs</read_first>
  <action>Extend validation helpers so `grid` reports unsupported complex grid template values, placement attributes are accepted only where supported, `progress` validates determinate/indeterminate and min/max/value combinations, `tooltip` validates ownership/linkage attributes, and structure tags reject invalid structural attributes. Keep existing unsupported attribute/event diagnostics compatible.</action>
  <verify>nix develop -c cargo test -p mesh-core-elements phase87_layout_display_diagnostics</verify>
  <acceptance_criteria>
    - Invalid grid syntax returns `ElementDiagnosticKind::InvalidAttributeValue`.
    - Invalid progress ranges return actionable diagnostics.
    - Unsupported structure/display attributes remain reported through existing diagnostic paths.
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `nix develop -c cargo test -p mesh-core-elements phase87_layout_display_contract`
- `nix develop -c cargo test -p mesh-core-elements phase87_layout_display_diagnostics`
</verification>
