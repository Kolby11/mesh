---
phase: 93-host-api-retargeting
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/core/runtime/scripting/src/pool.rs
  - crates/core/runtime/scripting/src/context/runtime.rs
autonomous: true
requirements:
  - ISO-02
  - ISO-04

must_haves:
  truths:
    - "LuaVmPool::new() captures a baseline_globals snapshot of stdlib key names before any component host API is installed"
    - "baseline_globals() returns an Arc<HashSet<String>> containing only the stdlib keys (e.g. 'string', 'table', 'math', 'require') that exist after sandbox(true)"
    - "ScriptContext::install_host_api accepts a &mlua::Table parameter and installs all API keys into that table rather than into self.lua.globals()"
    - "The single call site in load_script_with_interface_imports passes &self.lua.globals() so existing behavior is unchanged"
    - "All existing scripting context tests pass without modification"
  artifacts:
    - path: "crates/core/runtime/scripting/src/pool.rs"
      provides: "LuaVmPool with baseline_globals field and accessor"
      contains: "baseline_globals: Arc<HashSet<String>>"
    - path: "crates/core/runtime/scripting/src/context/runtime.rs"
      provides: "ScriptContext::install_host_api with &Table target parameter"
      contains: "fn install_host_api(&mut self, target: &mlua::Table)"
  key_links:
    - from: "LuaVmPool::new()"
      to: "baseline_globals field"
      via: "temporary Lua::new() + sandbox(true) + globals walk"
      pattern: "baseline_globals.*Arc::new"
    - from: "load_script_with_interface_imports"
      to: "install_host_api"
      via: "self.lua.globals() passed as target"
      pattern: "install_host_api.*lua\\.globals"
---

<objective>
Add `baseline_globals: Arc<HashSet<String>>` to `LuaVmPool` (ISO-04) and refactor `ScriptContext::install_host_api` to accept a `&mlua::Table` parameter (ISO-02 foundation). Both are purely mechanical changes with no observable behavior difference — the call site continues to pass `&self.lua.globals()`.

Purpose: Establishes the two structural prerequisites that Phase 94 needs: (1) a pre-host-API stdlib snapshot so `sync_state_from_lua` can distinguish stdlib from user globals, and (2) an API installer that targets a caller-supplied table so per-component `_ENV` tables can be populated in Phase 94.

Output: Modified `pool.rs` and `context/runtime.rs`; all existing tests green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/93-host-api-retargeting/93-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add baseline_globals to LuaVmPool (ISO-04)</name>
  <files>crates/core/runtime/scripting/src/pool.rs</files>

  <read_first>
    Read crates/core/runtime/scripting/src/pool.rs in full — the complete file is 167 lines.
    Key structures to note:
    - `LuaVmPool` struct (lines 5-9): currently has `slots: Vec<Option<Lua>>` and `floor: usize`
    - `LuaVmPool::new(floor)` (lines 24-32): creates sandboxed floor VMs via `Lua::new()` + `lua.sandbox(true)?`
    - The private helper `new_sandboxed_lua()` (lines 17-21)
    - Existing imports: `use mlua::Lua;` — needs `HashSet` and `Arc` added
  </read_first>

  <behavior>
    - Test: `LuaVmPool::new(4).baseline_globals()` returns a non-empty set containing "string"
    - Test: The baseline set does NOT contain "self", "module", or "mesh" (those are host API keys installed later)
    - Test: `baseline_globals()` returns an `Arc` — two calls return equal pointers (same Arc clone)
  </behavior>

  <action>
    1. Add to imports at top of file: `use std::collections::HashSet; use std::sync::Arc;`
    2. Add field to `LuaVmPool` struct: `baseline_globals: Arc<HashSet<String>>`
    3. In `LuaVmPool::new()`, after creating the floor VMs loop and before returning `Ok(Self {...})`:
       - Create a temporary VM: `let baseline_vm = Lua::new(); baseline_vm.sandbox(true)?;`
       - Walk its globals: `let set: HashSet<String> = baseline_vm.globals().pairs::<String, mlua::Value>().filter_map(|r| r.ok().map(|(k, _)| k)).collect();`
       - Drop `baseline_vm` (goes out of scope naturally)
       - Store: `let baseline_globals = Arc::new(set);`
    4. Include `baseline_globals` in the `Ok(Self { slots, floor, baseline_globals })` return
    5. Add public accessor: `pub fn baseline_globals(&self) -> Arc<HashSet<String>> { Arc::clone(&self.baseline_globals) }`
    6. The `#[allow(dead_code)]` on `floor` stays unchanged.
    7. Add three tests in the existing `#[cfg(test)]` block:
       - `baseline_globals_is_non_empty`: create pool, assert `!pool.baseline_globals().is_empty()`
       - `baseline_globals_contains_stdlib`: assert `pool.baseline_globals().contains("string")`
       - `baseline_globals_excludes_host_api_keys`: assert none of ["self", "module", "mesh"] are in the baseline set
  </action>

  <verify>
    <automated>cargo test -p mesh-core-scripting pool -- --nocapture 2>&1 | grep -E "^test|FAILED|ok$"</automated>
  </verify>

  <acceptance_criteria>
    - `cargo test -p mesh-core-scripting pool` exits 0 with all pool tests passing including the three new ones
    - `grep "baseline_globals" crates/core/runtime/scripting/src/pool.rs | wc -l` returns at least 5 (field decl, Arc::new, accessor fn, and test references)
    - `cargo build -p mesh-core-scripting 2>&1 | grep error` returns empty
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Refactor ScriptContext::install_host_api to accept &Table (ISO-02 foundation)</name>
  <files>crates/core/runtime/scripting/src/context/runtime.rs</files>

  <read_first>
    Read crates/core/runtime/scripting/src/context/runtime.rs at these ranges before touching any code:
    - Lines 1-16: imports — confirm `Table` is already imported from mlua
    - Lines 140-178: `load_script_with_interface_imports` — the single call site at line 160 (`self.install_host_api()?;`)
    - Lines 563-928: the full `install_host_api` function body

    Key observations:
    - Line 563: `fn install_host_api(&mut self) -> Result<(), ScriptError>`
    - Line 564: `let globals = self.lua.globals();` — this binding is used for ALL top-level `.set(...)` calls
    - Lines 638-643: `mesh_ui_api.set("request_redraw", ...)` closure body calls `lua.globals().set(...)` — DO NOT change this
    - Lines 646-657: `mesh_locale.set("current", ...)` closure body calls `lua.globals().get(...)` — DO NOT change this
    - Lines 835, 836-841, 926: the final `globals.set("mesh", ...)`, `globals.set("__mesh_request_redraw", ...)`, `globals.set("__mesh_locale_current", ...)`, `globals.set("require", ...)` calls — these MUST switch to `target.set(...)`
    - `Table` is already imported at line 12 (`use mlua::{..., Table, ...}`)
  </read_first>

  <action>
    Perform a pure signature + variable rename refactor. No logic changes.

    1. Change the function signature at line 563 from:
       `fn install_host_api(&mut self) -> Result<(), ScriptError>`
       to:
       `fn install_host_api(&mut self, target: &mlua::Table) -> Result<(), ScriptError>`

    2. Change line 564 from:
       `let globals = self.lua.globals();`
       to:
       `let globals = target;`
       This single rename makes every subsequent `globals.set(...)` call automatically use `target`. Do not change any other lines that use `globals` — they all become `target.set(...)` through this rename.

    3. Update the call site at line 160 from:
       `self.install_host_api()?;`
       to:
       `self.install_host_api(&self.lua.globals())?;`
       
       Note: Rust's borrow checker requires splitting the borrow. Write it as:
       ```
       let globals = self.lua.globals();
       self.install_host_api(&globals)?;
       ```

    4. Confirm the following lines inside closure bodies are NOT changed (they read from `lua.globals()` at call time, not at install time):
       - The `request_redraw` closure body: `lua.globals().set("__mesh_request_redraw", true)?;`
       - The `mesh_locale.current` closure body: `lua.globals().get::<Option<String>>("__mesh_locale_current")`

    That's the complete change. No other lines need modification.
  </action>

  <verify>
    <automated>cargo test -p mesh-core-scripting 2>&1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - `cargo test -p mesh-core-scripting` exits 0 with all tests passing
    - `grep -n "fn install_host_api" crates/core/runtime/scripting/src/context/runtime.rs` shows `target: &mlua::Table` in the signature
    - `grep -n "install_host_api" crates/core/runtime/scripting/src/context/runtime.rs` shows exactly 2 hits: definition and call site
    - `cargo build -p mesh-core-scripting 2>&1 | grep error` returns empty
    - The closure bodies at the `request_redraw` and `mesh_locale.current` functions still reference `lua.globals()` (not `target`)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pool init → baseline capture | Temporary VM is created and discarded; its globals are read-only stdlib keys |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-93-01 | Information Disclosure | baseline_globals | accept | Contains only stdlib key names (strings like "string", "table"); no secret data |
| T-93-SC | Tampering | npm/pip/cargo installs | accept | No new package dependencies in this plan |
</threat_model>

<verification>
After both tasks complete:

```bash
cargo test -p mesh-core-scripting 2>&1 | grep -E "^test result|FAILED"
```

Expected: `test result: ok. N passed; 0 failed`

```bash
grep -n "fn install_host_api" crates/core/runtime/scripting/src/context/runtime.rs
grep -n "baseline_globals" crates/core/runtime/scripting/src/pool.rs | wc -l
```
</verification>

<success_criteria>
- `LuaVmPool::baseline_globals()` returns a non-empty `Arc<HashSet<String>>` containing stdlib keys and excluding host API keys ("self", "module", "mesh")
- `ScriptContext::install_host_api(&Table)` compiles; its call site passes `&self.lua.globals()`; closure bodies reading `lua.globals()` are untouched
- `cargo test -p mesh-core-scripting` passes with zero failures
</success_criteria>

<output>
Create `.planning/phases/93-host-api-retargeting/93-01-SUMMARY.md` when done.
</output>
