# Phase 63 Patterns

- Keep debug snapshot additions structured and serializable through `mesh.debug`.
- Let component-level runtime data flow through `ShellComponent` trait hooks instead of downcasting shell components.
- Document author contracts next to the existing module/settings docs rather than creating a separate keybind guide.
