# Phase 62 Patterns

- Keep shell runtime behavior deterministic and observable.
- Prefer non-fatal component diagnostics for author mistakes that can be surfaced without interrupting interaction.
- Add focused interaction tests next to existing keybind coverage in `navigation.rs`.
