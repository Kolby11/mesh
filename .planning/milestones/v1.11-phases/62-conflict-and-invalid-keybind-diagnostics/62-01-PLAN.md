# Phase 62 Plan 01: Keybind Diagnostics

## Goal

Emit actionable, non-fatal diagnostics for invalid or conflicting focused-surface keybinds while preserving safe dispatch behavior.

## Tasks

1. Add keybind diagnostic recording on `FrontendSurfaceComponent`.
2. Harden shortcut resolution with stable declaration order, invalid trigger diagnostics, unresolved override diagnostics, duplicate effective binding diagnostics, and unsafe override rejection.
3. Add focused tests for malformed declarations, duplicate bindings, unresolved overrides, unsafe overrides, and missing subscribers.

## Verification

- Run focused keybind diagnostic tests.
- Run the full navigation interaction test module.
