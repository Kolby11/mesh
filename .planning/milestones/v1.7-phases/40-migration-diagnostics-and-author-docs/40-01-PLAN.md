---
phase: 40-migration-diagnostics-and-author-docs
plan: 01
title: Migration Diagnostic Contract
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/core/extension/module/src/package/error.rs
  - crates/core/extension/module/src/package/installed_graph.rs
  - crates/core/extension/module/src/package/tests.rs
  - docs/module-system.md
  - docs/module-vocabulary.md
autonomous: true
requirements: [MIGR-01]
requirements_addressed: [MIGR-01]
user_setup: []
must_haves:
  truths:
    - "Old manifest names are replacement debt or internal-only migration inputs, not public aliases."
    - "Diagnostics distinguish blocking load errors from migration warnings."
    - "Migration guidance uses concrete replacement/removal wording."
  artifacts:
    - path: "crates/core/extension/module/src/package/error.rs"
      provides: "Structured manifest diagnostic severity and suggested action fields"
      contains: "ModuleManifestDiagnosticSeverity"
    - path: "crates/core/extension/module/src/package/installed_graph.rs"
      provides: "Legacy manifest source detection and diagnostics"
      contains: "replace package.json with module.json"
---

<objective>
Make the manifest migration diagnostic contract explicit and testable so old manifest files produce author-facing replacement/removal guidance while unsupported or ambiguous inputs remain blocking errors.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-migration-diagnostics-and-author-docs/40-RESEARCH.md
@.planning/phases/40-migration-diagnostics-and-author-docs/40-VALIDATION.md
@.planning/phases/40-migration-diagnostics-and-author-docs/40-PATTERNS.md
@.planning/phases/38-canonical-manifest-normalization/38-CONTEXT.md
@.planning/phases/39-contribution-and-interface-extension-index/39-04-SUMMARY.md
@crates/core/extension/module/src/package/error.rs
@crates/core/extension/module/src/package/installed_graph.rs
@crates/core/extension/module/src/package/tests.rs
@docs/module-system.md
@docs/module-vocabulary.md
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| T-40-01-01: legacy manifest paths are treated as public compatibility aliases | high | Keep diagnostics wording to `replace` and `remove`; add tests and docs checks that old names are migration inputs only. |
| T-40-01-02: unsupported or ambiguous manifests become warnings and load anyway | high | Assert `plugin.json` and multiple manifest files return `ModuleManifestError::Diagnostic` with `Error` severity. |
| T-40-01-03: author docs hide the difference between load errors and migration warnings | medium | Add a visible diagnostics table to the module docs with severity, trigger, and action. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add severity-focused manifest diagnostic regression tests</name>
  <files>crates/core/extension/module/src/package/tests.rs, crates/core/extension/module/src/package/error.rs, crates/core/extension/module/src/package/installed_graph.rs</files>
  <read_first>crates/core/extension/module/src/package/tests.rs, crates/core/extension/module/src/package/error.rs, crates/core/extension/module/src/package/installed_graph.rs, .planning/phases/40-migration-diagnostics-and-author-docs/40-PATTERNS.md</read_first>
  <action>Add or update package tests that pattern-match `ModuleManifestError::Diagnostic` and `LoadedModuleManifest.diagnostics` instead of only checking display strings. Assert these exact cases: `plugin.json` returns `ModuleManifestDiagnosticSeverity::Error` with suggested action `remove plugin.json or replace it with module.json`; `module.json` plus `package.json` returns `ModuleManifestDiagnosticSeverity::Error` with message containing `ambiguous module manifest files found`; legacy `package.json` loads with a `Warning` diagnostic whose suggested action is `replace package.json with module.json`; legacy old-shape `module.json` loads with a `Warning` diagnostic whose suggested action is `replace legacy module.json fields with name/version/mesh`; legacy `mesh.toml` loads with a `Warning` diagnostic whose suggested action is `replace mesh.toml with module.json`.</action>
  <verify>cargo test -p mesh-core-module package::tests diagnostic</verify>
  <acceptance_criteria>
    - `crates/core/extension/module/src/package/tests.rs` contains `ModuleManifestDiagnosticSeverity::Error`.
    - `crates/core/extension/module/src/package/tests.rs` contains `ModuleManifestDiagnosticSeverity::Warning`.
    - `crates/core/extension/module/src/package/tests.rs` contains `remove plugin.json or replace it with module.json`.
    - `crates/core/extension/module/src/package/tests.rs` contains `replace package.json with module.json`.
    - `crates/core/extension/module/src/package/tests.rs` contains `replace legacy module.json fields with name/version/mesh`.
    - `crates/core/extension/module/src/package/tests.rs` contains `replace mesh.toml with module.json`.
    - `cargo test -p mesh-core-module package::tests diagnostic` exits 0.
  </acceptance_criteria>
  <done>Manifest diagnostic severity and replacement/removal guidance are regression-tested.</done>
</task>

<task type="auto">
  <name>Task 2: Document the migration diagnostics contract</name>
  <files>docs/module-system.md, docs/module-vocabulary.md</files>
  <read_first>docs/module-system.md, docs/module-vocabulary.md, crates/core/extension/module/src/package/installed_graph.rs, .planning/phases/37-concept-inventory-and-vocabulary-lock/37-03-SUMMARY.md</read_first>
  <action>In `docs/module-system.md`, add a `## Migration Diagnostics` section near the manifest shape documentation. Include a table with exact columns `Input`, `Severity`, `Author action`, and `Runtime behavior`. Include rows for `package.json` -> `warning` -> `replace package.json with module.json`, legacy `module.json` with `id/type/api_version` -> `warning` -> `replace legacy module.json fields with name/version/mesh`, `mesh.toml` -> `warning` -> `replace mesh.toml with module.json`, `plugin.json` -> `error` -> `remove plugin.json or replace it with module.json`, and multiple manifest files -> `error` -> `keep canonical module.json and remove the old manifest file`. In `docs/module-vocabulary.md`, update the Phase 40 handoff or runtime inventory note to point to the same replacement/removal diagnostics contract.</action>
  <verify>rg -n "Migration Diagnostics|replace package.json with module.json|replace legacy module.json fields with name/version/mesh|replace mesh.toml with module.json|remove plugin.json or replace it with module.json|keep canonical module.json and remove the old manifest file" docs/module-system.md docs/module-vocabulary.md</verify>
  <acceptance_criteria>
    - `docs/module-system.md` contains `## Migration Diagnostics`.
    - `docs/module-system.md` contains table header `| Input | Severity | Author action | Runtime behavior |`.
    - `docs/module-system.md` contains `replace package.json with module.json`.
    - `docs/module-system.md` contains `remove plugin.json or replace it with module.json`.
    - `docs/module-system.md` contains `keep canonical module.json and remove the old manifest file`.
    - `docs/module-vocabulary.md` contains `replacement/removal diagnostics contract`.
  </acceptance_criteria>
  <done>Authors can see which legacy inputs warn, which inputs fail, and what exact replacement action to take.</done>
</task>

<task type="auto">
  <name>Task 3: Prove old manifest names are not documented as aliases</name>
  <files>docs/module-system.md, docs/module-vocabulary.md, crates/core/extension/module/src/package/tests.rs</files>
  <read_first>docs/module-system.md, docs/module-vocabulary.md, crates/core/extension/module/src/package/tests.rs</read_first>
  <action>Add grep-verifiable wording to docs and tests that old manifest names are `internal migration input` or `replacement debt`. Do not add public `alias`, `synonym`, or `compatible name` wording for `package.json`, old `module.json`, `mesh.toml`, or `plugin.json`. If existing nearby wording says old manifest names are aliases or synonyms, replace it with `internal migration input` or `replacement debt`.</action>
  <verify>rg -n "internal migration input|replacement debt" docs/module-system.md docs/module-vocabulary.md && ! rg -n "package.json.*alias|plugin.json.*alias|mesh.toml.*alias|package.json.*synonym|compatible name" docs/module-system.md docs/module-vocabulary.md crates/core/extension/module/src/package/tests.rs</verify>
  <acceptance_criteria>
    - `docs/module-system.md` contains `internal migration input`.
    - `docs/module-vocabulary.md` contains `replacement debt`.
    - `rg -n "package.json.*alias|plugin.json.*alias|mesh.toml.*alias|package.json.*synonym|compatible name" docs/module-system.md docs/module-vocabulary.md crates/core/extension/module/src/package/tests.rs` exits 1.
    - `cargo test -p mesh-core-module package::tests` exits 0.
  </acceptance_criteria>
  <done>Old manifest names are documented as migration debt, not aliases.</done>
</task>

</tasks>

<verification>
- Run `cargo test -p mesh-core-module package::tests`.
- Run `rg -n "Migration Diagnostics|replace package.json with module.json|remove plugin.json" docs/module-system.md docs/module-vocabulary.md`.
- Run `rg -n "package.json.*alias|plugin.json.*alias|mesh.toml.*alias|package.json.*synonym|compatible name" docs/module-system.md docs/module-vocabulary.md crates/core/extension/module/src/package/tests.rs` and confirm it exits 1.
</verification>

<success_criteria>
- Loader diagnostics have regression tests for warning versus error severity.
- Author docs distinguish migration warnings from blocking load errors.
- Old manifest names are replacement/removal guidance only, not compatibility aliases.
</success_criteria>

