---
phase: 39-contribution-and-interface-extension-index
plan: 04
title: Shell Graph Integration And Diagnostics Proof
type: execute
wave: 4
depends_on: [39-03]
files_modified:
  - crates/core/extension/module/src/package/installed_graph.rs
  - crates/core/extension/service/src/interface.rs
  - crates/core/shell/src/shell/discovery.rs
  - crates/core/shell/src/shell/backend/candidates.rs
  - crates/core/shell/src/shell/backend/spawn.rs
  - crates/core/shell/src/shell/tests.rs
  - docs/module-system.md
autonomous: true
requirements: [EXT-01, EXT-02, EXT-03, EXT-04]
requirements_addressed: [EXT-01, EXT-02, EXT-03, EXT-04]
user_setup: []
must_haves:
  truths:
    - "D-39-03: Provider declarations, interface declarations, and frontend dependencies are separate graph concepts."
    - "D-39-04: Host capabilities are explicit module permissions, never inferred from provider identity."
    - "D-39-06: Installed graph contribution indexes use strict typed registries."
    - "D-39-09: Shell integration consumes graph metadata with legacy fallback when graph loading fails."
  artifacts:
    - path: "crates/core/shell/src/shell/discovery.rs"
      provides: "Installed graph frontend/interface startup bridge"
      contains: "load_installed_module_graph"
    - path: "crates/core/shell/src/shell/backend/candidates.rs"
      provides: "Graph-driven backend candidate construction"
      contains: "backend_launch_candidates_from_graph"
---

<objective>
Wire the source-rich installed graph into shell startup and diagnostics so frontend resources, interface contracts, backend providers, and settings metadata are inspectable through one graph path with legacy fallback preserved.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-contribution-and-interface-extension-index/39-RESEARCH.md
@.planning/phases/39-contribution-and-interface-extension-index/39-VALIDATION.md
@.planning/phases/39-contribution-and-interface-extension-index/39-PATTERNS.md
@docs/module-system.md
@crates/core/extension/module/src/package/installed_graph.rs
@crates/core/extension/service/src/interface.rs
@crates/core/shell/src/shell/discovery.rs
@crates/core/shell/src/shell/backend/candidates.rs
@crates/core/shell/src/shell/backend/spawn.rs
@crates/core/shell/src/shell/tests.rs
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| T-39-04-01: shell continues to use parallel manifest scans that disagree with installed graph metadata | high | Register graph-derived interface/provider data where the graph loads and retain legacy scan fallback only on graph load failure. |
| T-39-04-02: resource/settings diagnostics become fatal and block unrelated modules | medium | Emit typed non-fatal diagnostics for missing optional resources/settings metadata unless the graph itself is invalid. |
| T-39-04-03: implementation adds service-specific Rust branches for current bundled modules | high | Use generic interface/resource ids in tests and inspect touched shell code for concrete service branches. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Register interface contracts and providers from graph metadata</name>
  <files>crates/core/extension/module/src/package/installed_graph.rs, crates/core/extension/service/src/interface.rs, crates/core/shell/src/shell/discovery.rs, crates/core/shell/src/shell/tests.rs</files>
  <read_first>crates/core/shell/src/shell/discovery.rs, crates/core/extension/service/src/interface.rs, crates/core/extension/module/src/package/installed_graph.rs, crates/core/shell/src/shell/tests.rs</read_first>
  <action>Add a shell-side bridge that loads `config/module.json` once where practical, registers enabled interface contracts/providers from installed graph typed records into `InterfaceRegistry`, and preserves the existing legacy manifest scan fallback when the installed graph cannot load. Keep registration generic by interface id, version, base module, provider module, provider name, and priority.</action>
  <verify>cargo test -p mesh-core-shell shell::tests interface</verify>
  <acceptance_criteria>
    - Shell interface registry receives graph-derived provider metadata for enabled backend providers.
    - Graph load failure still falls back to legacy discovery behavior.
    - Disabled providers do not register.
    - Interface/provider registration tests pass with a non-audio interface id.
    - `cargo test -p mesh-core-shell shell::tests interface` exits 0.
  </acceptance_criteria>
  <done>Shell interface registration uses graph metadata when available.</done>
</task>

<task type="auto">
  <name>Task 2: Route backend launch and frontend filtering through graph typed records</name>
  <files>crates/core/shell/src/shell/discovery.rs, crates/core/shell/src/shell/backend/candidates.rs, crates/core/shell/src/shell/backend/spawn.rs, crates/core/shell/src/shell/tests.rs</files>
  <read_first>crates/core/shell/src/shell/discovery.rs, crates/core/shell/src/shell/backend/candidates.rs, crates/core/shell/src/shell/backend/spawn.rs, .planning/phases/39-contribution-and-interface-extension-index/39-PATTERNS.md</read_first>
  <action>Update shell backend candidate construction and enabled frontend filtering to consume the typed installed graph records added in Plans 02 and 03 instead of re-deriving provider/interface lists from raw manifests where avoidable. Keep module source loading for script contents and compiled frontend assets unchanged. Preserve legacy fallback when `config/module.json` cannot be loaded.</action>
  <verify>cargo test -p mesh-core-shell shell::tests backend</verify>
  <acceptance_criteria>
    - Backend launch candidate interface/provider selection uses graph provider records.
    - Enabled frontend filtering still includes the builtin debug inspector and enabled frontend modules only.
    - Existing backend spawn/candidate tests still pass.
    - No service-specific Rust branch is added for bundled audio/network providers.
    - `cargo test -p mesh-core-shell shell::tests backend` exits 0.
  </acceptance_criteria>
  <done>Shell runtime paths consume graph records while preserving legacy fallbacks.</done>
</task>

<task type="auto">
  <name>Task 3: Surface resource/settings compatibility diagnostics</name>
  <files>crates/core/extension/module/src/package/installed_graph.rs, crates/core/shell/src/shell/tests.rs, docs/module-system.md</files>
  <read_first>crates/core/extension/module/src/package/installed_graph.rs, docs/icon-system.md, .planning/todos/pending/2026-05-15-define-module-install-requirement-resolution.md</read_first>
  <action>Add graph-level diagnostics or status getters for missing typed resource/settings compatibility where the graph has enough data: frontend required icon/font/i18n/theme packs without enabled providers, missing required semantic icon names when icon requirement metadata and icon pack metadata are both present, and settings schema namespace collisions. Keep diagnostics non-fatal unless they indicate an invalid manifest shape. Add minimal docs wording only if needed to name the diagnostic behavior.</action>
  <verify>cargo test -p mesh-core-module package::tests contribution_index && cargo test -p mesh-core-shell shell::tests</verify>
  <acceptance_criteria>
    - Missing resource pack requirements produce typed graph diagnostics.
    - Settings namespace collisions are detected or explicitly left as non-fatal diagnostics with tests.
    - Diagnostics include module id and contribution/source id when available.
    - Missing optional resource data does not crash graph loading.
    - Relevant package and shell tests pass.
  </acceptance_criteria>
  <done>Resource and settings compatibility gaps are visible through graph diagnostics.</done>
</task>

<task type="auto">
  <name>Task 4: Run end-to-end manifest-driven extension proof</name>
  <files>crates/core/extension/module/src/package/tests.rs, crates/core/shell/src/shell/tests.rs, docs/module-system.md</files>
  <read_first>crates/core/extension/module/src/package/tests.rs, crates/core/shell/src/shell/tests.rs, .planning/phases/39-contribution-and-interface-extension-index/39-VALIDATION.md</read_first>
  <action>Add or update end-to-end tests proving a new interface/provider/library/resource setup can be added through manifests, contracts, libraries, providers, and installed graph entries without adding service-specific Rust branches. Update `docs/module-system.md` minimally to point authors at the typed contribution/index model if execution changed public author behavior.</action>
  <verify>cargo test -p mesh-core-module package::tests && cargo test -p mesh-core-shell shell::tests</verify>
  <acceptance_criteria>
    - Tests cover manifest-driven interface declaration, provider declaration, library/resource contribution, frontend requirement, and selected active provider behavior.
    - Touched shell Rust files do not add concrete service-specific branches for the proof.
    - Public docs mention typed contribution indexes only if the public author contract changed.
    - `cargo test -p mesh-core-module package::tests` exits 0.
    - `cargo test -p mesh-core-shell shell::tests` exits 0.
  </acceptance_criteria>
  <done>Phase 39 behavior is proven end to end through manifests and graph metadata.</done>
</task>

</tasks>

<verification>
- Run `cargo test -p mesh-core-module package::tests`.
- Run `cargo test -p mesh-core-service interface::tests`.
- Run `cargo test -p mesh-core-shell shell::tests`.
- Run `cargo test --workspace` before verification if runtime is acceptable.
</verification>

<success_criteria>
- Shell startup consumes installed graph metadata for interface/provider and contribution inspection where available.
- Legacy fallback remains intact when graph loading fails.
- Resource/settings compatibility issues are visible as typed diagnostics.
- Tests prove new extension behavior routes through manifests, contracts, libraries, providers, and installed graph records without service-specific Rust branches.
</success_criteria>
