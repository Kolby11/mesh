---
phase: 39-contribution-and-interface-extension-index
plan: 03
title: Source Rich Typed Contribution Registries
type: execute
wave: 3
depends_on: [39-02]
files_modified:
  - crates/core/extension/module/src/package/installed_graph.rs
  - crates/core/extension/module/src/package/module_manifest.rs
  - crates/core/extension/module/src/package/tests.rs
autonomous: true
requirements: [EXT-03, EXT-04]
requirements_addressed: [EXT-03, EXT-04]
user_setup: []
must_haves:
  truths:
    - "D-39-06: Installed graph contribution indexes use strict typed registries."
    - "D-39-07: Contribution records are source-rich and use scoped ids."
    - "D-39-08: Runtime contribution indexes include enabled modules only; disabled modules remain catalog metadata."
  artifacts:
    - path: "crates/core/extension/module/src/package/installed_graph.rs"
      provides: "ModuleContributionIndex and typed contribution getters"
      contains: "ModuleContributionIndex"
---

<objective>
Expand the installed graph contribution index into typed, source-rich registries for frontend entrypoints, slots where available, libraries, settings, keybinds, resources, interfaces, and providers.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-contribution-and-interface-extension-index/39-DISCUSS-CHECKPOINT.json
@.planning/phases/39-contribution-and-interface-extension-index/39-RESEARCH.md
@.planning/phases/39-contribution-and-interface-extension-index/39-VALIDATION.md
@.planning/phases/39-contribution-and-interface-extension-index/39-PATTERNS.md
@docs/icon-system.md
@docs/theming/icons.md
@crates/core/extension/module/src/package/installed_graph.rs
@crates/core/extension/module/src/package/module_manifest.rs
@crates/core/extension/module/src/package/tests.rs
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| T-39-03-01: contribution records lose their origin, making diagnostics and tooling ambiguous | high | Add reusable source metadata to typed contribution records and assert manifest path/source/module id in tests. |
| T-39-03-02: contribution ids collide across modules | high | Add scoped ids using `<module-id>:<local-id>` for module-local contributions and test duplicate local ids across modules. |
| T-39-03-03: disabled modules accidentally affect runtime resources, settings, or keybinds | high | Keep indexing gated by `entry.enabled` and add tests for disabled installed modules. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add common source metadata and scoped ids</name>
  <files>crates/core/extension/module/src/package/installed_graph.rs, crates/core/extension/module/src/package/tests.rs</files>
  <read_first>crates/core/extension/module/src/package/installed_graph.rs, .planning/phases/39-contribution-and-interface-extension-index/39-RESEARCH.md</read_first>
  <action>Add a reusable contribution source struct in `installed_graph.rs` containing module id, module kind, module root graph path, manifest path, manifest source, local id, and scoped id. Retain `LoadedModuleManifest.path` and `LoadedModuleManifest.source` on `InstalledModuleNode` so contribution records can reference the manifest that produced them. Update existing contribution structs to embed or expose the source metadata while preserving existing public fields where possible.</action>
  <verify>cargo test -p mesh-core-module package::tests contribution_index</verify>
  <acceptance_criteria>
    - Existing contribution getters still compile.
    - Contribution records expose a scoped id using `<module-id>:<local-id>`.
    - Contribution records expose manifest path and manifest source.
    - Duplicate local contribution ids in different modules do not collide because scoped ids differ.
    - `cargo test -p mesh-core-module package::tests contribution_index` exits 0.
  </acceptance_criteria>
  <done>Contribution records can identify their source and stable scoped id.</done>
</task>

<task type="auto">
  <name>Task 2: Index missing typed contribution families</name>
  <files>crates/core/extension/module/src/package/installed_graph.rs, crates/core/extension/module/src/package/module_manifest.rs, crates/core/extension/module/src/package/tests.rs</files>
  <read_first>crates/core/extension/module/src/package/installed_graph.rs, crates/core/extension/module/src/package/module_manifest.rs, docs/module-vocabulary.md, docs/icon-system.md</read_first>
  <action>Extend `ModuleContributionIndex` and graph getters to cover frontend entrypoints, settings schemas, keybind actions, theme/icon/font/i18n resources, icon pack metadata or icon requirement metadata where available, library exports, interface declarations, and backend provider declarations. Keep each family as a typed registry. If slot definitions/contributions are only available in legacy normalized manifest data, index them only through an explicit typed bridge with tests; otherwise document the intentionally deferred canonical slot schema in a package test name or comment.</action>
  <verify>cargo test -p mesh-core-module package::tests contribution_index</verify>
  <acceptance_criteria>
    - Graph getters exist for frontend entrypoint contributions.
    - Graph getters exist for keybind action contributions.
    - Graph getters exist for resource requirements/contributions needed by icons, fonts, i18n, and themes.
    - Graph getters expose interface and provider contributions as typed records.
    - Libraries and settings remain typed and source-rich.
    - `cargo test -p mesh-core-module package::tests contribution_index` exits 0.
  </acceptance_criteria>
  <done>The installed graph exposes typed registries for Phase 39 contribution families.</done>
</task>

<task type="auto">
  <name>Task 3: Enforce enabled-runtime versus disabled-catalog behavior</name>
  <files>crates/core/extension/module/src/package/installed_graph.rs, crates/core/extension/module/src/package/tests.rs</files>
  <read_first>crates/core/extension/module/src/package/installed_graph.rs, crates/core/extension/module/src/package/tests.rs, .planning/phases/39-contribution-and-interface-extension-index/39-DISCUSS-CHECKPOINT.json</read_first>
  <action>Add tests proving disabled installed modules remain available through `module(id)` or equivalent catalog/node access but do not appear in runtime contribution getters, backend provider getters, frontend requirement getters, interface declaration getters, or active layout resolution. Keep this behavior data-driven by the root graph `enabled` flag.</action>
  <verify>cargo test -p mesh-core-module package::tests disabled</verify>
  <acceptance_criteria>
    - Disabled modules remain present in installed graph node metadata.
    - Disabled module contributions do not appear in runtime contribution getters.
    - Disabled backend providers are not candidates for active provider validation.
    - Disabled frontend modules do not create frontend requirement records.
    - `cargo test -p mesh-core-module package::tests disabled` exits 0.
  </acceptance_criteria>
  <done>Runtime indexes and catalog metadata follow the checkpoint decision.</done>
</task>

</tasks>

<verification>
- Run `cargo test -p mesh-core-module package::tests contribution_index`.
- Run `cargo test -p mesh-core-module package::tests disabled`.
- Run `cargo test -p mesh-core-module package::tests installed_module_graph`.
</verification>

<success_criteria>
- Typed installed graph contribution indexing covers frontend entrypoints, slots where supported, libraries, settings, keybinds, resources, interfaces, and providers.
- Contribution records are source-rich and have stable scoped ids.
- Disabled modules are visible as installed metadata but absent from runtime contribution registries.
- New extension behavior routes through manifest/index data rather than hard-coded service branches.
</success_criteria>
