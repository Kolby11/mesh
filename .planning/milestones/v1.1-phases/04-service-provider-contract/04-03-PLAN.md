---
phase: 04-service-provider-contract
plan: 03
type: execute
wave: 3
depends_on: ["04-02"]
files_modified:
  - crates/core/runtime/scripting/src/context.rs
  - crates/core/runtime/scripting/src/host_api.rs
  - crates/core/shell/src/shell/service.rs
  - crates/core/shell/src/shell/component.rs
autonomous: true
requirements: [BSVC-03, BSVC-04, BSVC-05]
requirements_addressed: [BSVC-03, BSVC-04, BSVC-05]
user_setup: []
must_haves:
  truths:
    - "D-06: normal consumers import service/interface ids such as require(\"@mesh/audio\")."
    - "D-07: direct provider imports may exist for explicit provider-specific use, but ordinary code uses interface imports."
    - "D-08: imported service modules expose reactive state as module.state."
    - "D-09: require design must not block future Lua package manager support."
    - "D-10: service proxy methods dispatch generically to backend Luau handlers."
    - "D-11: command methods return small caller-visible result tables."
    - "D-12: state updates remain the source of truth after commands."
    - "D-13: require(\"@mesh/audio\").state means active audio provider latest state."
  artifacts:
    - path: "crates/core/runtime/scripting/src/context.rs"
      provides: "Frontend Luau require/import proxy and service method publication"
      contains: "create_service_proxy"
    - path: "crates/core/shell/src/shell/service.rs"
      provides: "Service update application and script event to request conversion"
      contains: "script_events_to_requests"
---

<objective>
Expose the active provider as an interface module with `module.state`, keep compatibility direct field reads, and make proxy command methods return concrete dispatch result tables while preserving generic command routing.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-service-provider-contract/04-CONTEXT.md
@.planning/phases/04-service-provider-contract/04-RESEARCH.md
@.planning/phases/04-service-provider-contract/04-VALIDATION.md
@.planning/phases/04-service-provider-contract/04-PATTERNS.md

@crates/core/runtime/scripting/src/context.rs
@crates/core/runtime/scripting/src/host_api.rs
@crates/core/shell/src/shell/service.rs
@crates/core/shell/src/shell/component.rs
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| T-04-09: `module.state` breaks existing direct field reads | medium | Keep `audio.percent` as a compatibility alias and test both paths. |
| T-04-10: proxy command calls return nil even when dispatch is denied | medium | Return `{ ok = false, error = "..." }` for denied or unsupported command dispatch. |
| T-04-11: future Lua package imports are blocked by hard-coded `@mesh/*` parsing | medium | Keep unsupported non-MESH requires rejected as today; isolate MESH interface parsing in helper functions. |
| T-04-12: command conversion bypasses capability checks | high | Keep `service.<name>.control` checks in proxy and `script_events_to_requests()`. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add module.state to interface proxy imports</name>
  <files>crates/core/runtime/scripting/src/context.rs, crates/core/runtime/scripting/src/host_api.rs</files>
  <read_first>crates/core/runtime/scripting/src/context.rs, crates/core/runtime/scripting/src/host_api.rs, .planning/phases/04-service-provider-contract/04-CONTEXT.md</read_first>
  <action>Extend `create_service_proxy()` so the returned proxy table exposes a `state` field. Reading `audio.state` must return a Lua table backed by the latest `__mesh_svc_audio` payload. `audio.state.percent` must read the same JSON value currently available through `audio.percent`. Keep direct `audio.percent` reads as a compatibility alias. Update host API comments in `host_api.rs` so the documented shape includes `require("@mesh/<service>").state`. Add tests named `interface_proxy_exposes_state_table`, `interface_proxy_state_reads_latest_payload`, and `interface_proxy_direct_field_read_remains_compatibility_alias`.</action>
  <verify>nix develop -c cargo test -p mesh-core-scripting interface_proxy_state</verify>
  <acceptance_criteria>
    - `crates/core/runtime/scripting/src/context.rs` contains test `interface_proxy_exposes_state_table`.
    - `crates/core/runtime/scripting/src/context.rs` contains test `interface_proxy_state_reads_latest_payload`.
    - `crates/core/runtime/scripting/src/context.rs` contains test `interface_proxy_direct_field_read_remains_compatibility_alias`.
    - `crates/core/runtime/scripting/src/host_api.rs` contains `require("@mesh/<service>").state`.
    - `nix develop -c cargo test -p mesh-core-scripting interface_proxy_state` exits 0.
  </acceptance_criteria>
  <done>Frontend scripts can read active provider state through `module.state`.</done>
</task>

<task type="auto">
  <name>Task 2: Make proxy command methods return dispatch result tables</name>
  <files>crates/core/runtime/scripting/src/context.rs, crates/core/shell/src/shell/service.rs</files>
  <read_first>crates/core/runtime/scripting/src/context.rs, crates/core/shell/src/shell/service.rs, .planning/phases/04-service-provider-contract/04-RESEARCH.md</read_first>
  <action>Change contract method closures in `create_service_proxy()` so accepted command publication returns a Lua result table with exact keys `{ ok = true, queued = true }`. If the caller lacks `service.<name>.control`, return `{ ok = false, error = "capability denied" }` instead of publishing an event. If the requested key is not a contract method, preserve state-field read behavior. Update `script_events_to_requests()` so it continues to enforce `service.<name>.control` and produces `CoreRequest::PublishDiagnostics` for denied publications. Add tests named `interface_proxy_method_returns_queued_result`, `read_only_interface_proxy_returns_capability_denied_result`, and `script_events_to_requests_denies_uncontrolled_service_command`.</action>
  <verify>nix develop -c cargo test -p mesh-core-scripting interface_proxy_method && nix develop -c cargo test -p mesh-core-shell script_events_to_requests_denies_uncontrolled_service_command</verify>
  <acceptance_criteria>
    - `crates/core/runtime/scripting/src/context.rs` contains `queued`.
    - `crates/core/runtime/scripting/src/context.rs` contains test `interface_proxy_method_returns_queued_result`.
    - `crates/core/runtime/scripting/src/context.rs` contains test `read_only_interface_proxy_returns_capability_denied_result`.
    - `crates/core/shell/src/shell/service.rs` contains test or shell test reference `script_events_to_requests_denies_uncontrolled_service_command`.
    - Accepted command publication still pushes a `PublishedEvent` with channel `mesh.audio.set_volume`.
  </acceptance_criteria>
  <done>Proxy commands give immediate caller-visible dispatch results without blocking async shell routing.</done>
</task>

<task type="auto">
  <name>Task 3: Propagate module.state updates through component repaint paths</name>
  <files>crates/core/shell/src/shell/component.rs, crates/core/shell/src/shell/service.rs</files>
  <read_first>crates/core/shell/src/shell/component.rs, crates/core/shell/src/shell/service.rs, crates/core/runtime/scripting/src/context.rs</read_first>
  <action>Add or update component tests proving a frontend component that reads `local audio = require("@mesh/audio"); volumeLevel = audio.state.percent` repaints when `ServiceEvent::Updated { service: "mesh.audio", ... }` arrives. Keep `apply_service_update()` setting public state by service name for compatibility, but ensure `apply_service_payload()` feeds the proxy `state` table. Add tests named `frontend_proxy_state_update_reaches_render_state` and `frontend_proxy_state_read_tracks_repaint_invalidation`.</action>
  <verify>nix develop -c cargo test -p mesh-core-shell frontend_proxy_state</verify>
  <acceptance_criteria>
    - `crates/core/shell/src/shell/component.rs` contains test `frontend_proxy_state_update_reaches_render_state`.
    - `crates/core/shell/src/shell/component.rs` contains test `frontend_proxy_state_read_tracks_repaint_invalidation`.
    - `frontend_proxy_state_update_reaches_render_state` script contains `audio.state.percent`.
    - `nix develop -c cargo test -p mesh-core-shell frontend_proxy_state` exits 0.
  </acceptance_criteria>
  <done>`module.state` participates in existing frontend reactive repaint behavior.</done>
</task>

</tasks>

<verification>
- Run `nix develop -c cargo test -p mesh-core-scripting interface_proxy_state`.
- Run `nix develop -c cargo test -p mesh-core-scripting interface_proxy_method`.
- Run `nix develop -c cargo test -p mesh-core-shell frontend_proxy_state`.
- Run `grep -n "require(\"@mesh/<service>\").state" crates/core/runtime/scripting/src/host_api.rs`.
</verification>

<success_criteria>
- BSVC-03 is covered by consumer access to latest interface state.
- BSVC-04 is covered by generic proxy command publication.
- BSVC-05 is covered by immediate command dispatch result tables.
- D-06 through D-13 are visible in this plan's `must_haves.truths`.
</success_criteria>

<output>
After completion, create `.planning/phases/04-service-provider-contract/04-03-SUMMARY.md`
</output>
