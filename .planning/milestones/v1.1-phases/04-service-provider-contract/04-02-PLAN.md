---
phase: 04-service-provider-contract
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - crates/core/shell/src/shell/mod.rs
  - crates/core/shell/src/shell/types.rs
autonomous: true
requirements: [BSVC-01, BSVC-03, BSVC-05]
requirements_addressed: [BSVC-01, BSVC-03, BSVC-05]
user_setup: []
must_haves:
  truths:
    - "D-01: backend module identity comes from the module package id."
    - "D-05: provider identity is runtime metadata, not public state."
    - "D-13: shell stores latest state per interface for normal consumers."
    - "D-14: provider identity is tracked beside latest state as metadata."
    - "D-15: provider swaps replace interface-level state for the active provider."
    - "D-16: provider declarations and command names are validated; state shape mismatches warn."
    - "D-17: validation catches bad wiring and unknown commands without moving Phase 5 diagnostics into Phase 4."
    - "D-18: warning text and validation layering are planner discretion, but core must stay generic."
  artifacts:
    - path: "crates/core/shell/src/shell/mod.rs"
      provides: "Backend candidate identity, latest service state cache, and lifecycle bridge"
      contains: "latest_service_events"
    - path: "crates/core/shell/src/shell/types.rs"
      provides: "Service event and command event types shared by shell components"
      contains: "ServiceEvent"
---

<objective>
Store latest public state per canonical interface while tracking provider identity as metadata, and add warning-level contract validation that uses interface metadata without service-specific Rust branches.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-service-provider-contract/04-CONTEXT.md
@.planning/phases/04-service-provider-contract/04-RESEARCH.md
@.planning/phases/04-service-provider-contract/04-VALIDATION.md
@.planning/phases/04-service-provider-contract/04-PATTERNS.md

@crates/core/shell/src/shell/mod.rs
@crates/core/shell/src/shell/types.rs
@crates/core/extension/service/src/contract.rs
@crates/core/extension/service/src/interface.rs
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| T-04-05: provider id leaks into public service state | medium | Store provider id in shell metadata and assert `payload.source_plugin` is not injected by shell. |
| T-04-06: latest state keyed by provider breaks `require("@mesh/audio")` after provider swaps | high | Key consumer state by canonical interface and replace it on active provider updates. |
| T-04-07: contract validation grows service-specific branches | high | Validate by iterating `InterfaceContract` fields and methods only. |
| T-04-08: unknown command requests silently disappear | medium | Return or publish a generic failure result and diagnostic for unsupported commands. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Introduce latest interface state records with provider metadata</name>
  <files>crates/core/shell/src/shell/mod.rs, crates/core/shell/src/shell/types.rs</files>
  <read_first>crates/core/shell/src/shell/mod.rs, crates/core/shell/src/shell/types.rs, .planning/phases/04-service-provider-contract/04-CONTEXT.md</read_first>
  <action>Replace or wrap `latest_service_events: HashMap<String, ServiceEvent>` with a latest-state record keyed by canonical interface string. The record must contain exact conceptual fields `interface`, `provider_id`, and `state` where `state` is the public JSON payload. Keep `ServiceEvent::Updated` carrying `service`, `source_plugin`, and `payload` to components unless a smaller compatible rename is required. Add a helper named `record_latest_service_state` or equivalent and use it from `broadcast_service_event()`. Add tests named `latest_service_state_is_keyed_by_interface` and `latest_service_state_tracks_provider_metadata_separately`.</action>
  <verify>nix develop -c cargo test -p mesh-core-shell latest_service_state</verify>
  <acceptance_criteria>
    - `crates/core/shell/src/shell/mod.rs` contains `latest_service_state` or `LatestServiceState`.
    - `crates/core/shell/src/shell/mod.rs` contains test `latest_service_state_is_keyed_by_interface`.
    - `crates/core/shell/src/shell/mod.rs` contains test `latest_service_state_tracks_provider_metadata_separately`.
    - The public payload in the test does not contain a shell-injected `source_plugin` key.
    - `nix develop -c cargo test -p mesh-core-shell latest_service_state` exits 0.
  </acceptance_criteria>
  <done>Shell has per-interface latest state with provider identity as metadata.</done>
</task>

<task type="auto">
  <name>Task 2: Preserve provider swap semantics for interface state</name>
  <files>crates/core/shell/src/shell/mod.rs</files>
  <read_first>crates/core/shell/src/shell/mod.rs, .planning/phases/02-backend-lifecycle-foundation/02-CONTEXT.md, .planning/phases/04-service-provider-contract/04-CONTEXT.md</read_first>
  <action>Update backend runtime replacement and service update handling so an update for `mesh.audio` from a newly active provider replaces the latest state record for `mesh.audio`. When `stop_backend_runtime(interface)` replaces or stops a provider, preserve deterministic cleanup and do not start fallback providers. Add test `provider_swap_replaces_interface_latest_state` using two providers such as `@mesh/pipewire-audio` and `@mesh/pulseaudio-audio`, and assert latest `mesh.audio` metadata changes provider id while the public state remains keyed by `mesh.audio`.</action>
  <verify>nix develop -c cargo test -p mesh-core-shell provider_swap_replaces_interface_latest_state</verify>
  <acceptance_criteria>
    - `crates/core/shell/src/shell/mod.rs` contains test `provider_swap_replaces_interface_latest_state`.
    - The test asserts provider id changes from `@mesh/pipewire-audio` to `@mesh/pulseaudio-audio`.
    - The test asserts the latest state key remains `mesh.audio`.
    - `grep -n "fallback_provider(" crates/core/shell/src/shell/mod.rs` shows no new fallback use in service contract handling.
  </acceptance_criteria>
  <done>Provider swaps update interface state without hidden fallback behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Add warning-level interface contract validation</name>
  <files>crates/core/shell/src/shell/mod.rs, crates/core/shell/src/shell/types.rs</files>
  <read_first>crates/core/shell/src/shell/mod.rs, crates/core/shell/src/shell/types.rs, crates/core/extension/service/src/contract.rs, crates/core/extension/service/src/interface.rs</read_first>
  <action>Add generic validation helpers that use `InterfaceRegistry::resolve(interface, None)` and `InterfaceContract` data. Validate that backend provider declarations resolve to a contract/provider pair before launch and that command requests name a method listed in the contract when a contract exists. For state updates, compare payload object keys against `contract.state_fields`; missing required state fields or wrong coarse JSON type should emit warning-level diagnostics/tracing but must not stop the provider. Use status or message strings containing exactly `service_contract_warning` for state shape warnings and `unsupported_service_command` for unknown commands. Add tests `state_shape_mismatch_records_service_contract_warning` and `unknown_service_command_returns_failure_result`.</action>
  <verify>nix develop -c cargo test -p mesh-core-shell service_contract</verify>
  <acceptance_criteria>
    - `crates/core/shell/src/shell/mod.rs` contains string `service_contract_warning`.
    - `crates/core/shell/src/shell/mod.rs` contains string `unsupported_service_command`.
    - `crates/core/shell/src/shell/mod.rs` contains test `state_shape_mismatch_records_service_contract_warning`.
    - `crates/core/shell/src/shell/mod.rs` contains test `unknown_service_command_returns_failure_result`.
    - `grep -n "mesh.audio\\|mesh.network\\|mesh.power" crates/core/shell/src/shell/mod.rs` shows only test fixture data or generic interface strings, not service-specific branches.
  </acceptance_criteria>
  <done>Interface contracts validate state and commands generically at warning level.</done>
</task>

</tasks>

<verification>
- Run `nix develop -c cargo test -p mesh-core-shell latest_service_state`.
- Run `nix develop -c cargo test -p mesh-core-shell provider_swap_replaces_interface_latest_state`.
- Run `nix develop -c cargo test -p mesh-core-shell service_contract`.
- Run `grep -n "wpctl\\|pactl\\|nmcli\\|upower" crates/core/shell/src/shell/mod.rs crates/core/shell/src/shell/types.rs` and confirm no service-specific command behavior was added.
</verification>

<success_criteria>
- BSVC-01 is covered by provider/interface declaration validation.
- BSVC-03 is covered by latest interface state storage.
- BSVC-05 is covered by unsupported command failure visibility.
- D-01, D-05, and D-13 through D-18 are visible in this plan's `must_haves.truths`.
</success_criteria>

<output>
After completion, create `.planning/phases/04-service-provider-contract/04-02-SUMMARY.md`
</output>
