---
phase: 01-plugin-package-manifest-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/core/extension/plugin/src/package.rs
  - crates/core/extension/plugin/src/manifest.rs
  - crates/core/extension/plugin/src/lib.rs
autonomous: true
requirements: [PINST-01, PINST-03, PINST-06]
requirements_addressed: [PINST-01, PINST-03, PINST-06]
user_setup: []
must_haves:
  truths:
    - "D-01: Phase 1 implements module installation/package structure before backend lifecycle internals."
    - "D-02: the central shell-owned manifest filename is locked as ~/.mesh/package.json."
    - "D-04: the shell-owned package manifest is installed state, not a replacement for each module package manifest."
    - "D-05: the package graph normalizes from ~/.mesh/package.json plus each referenced module manifest."
    - "D-07: user-facing naming changes from plugins to modules."
    - "D-08: existing Rust internals may keep Plugin* names during compatibility transition, but new schema/docs use modules."
    - "D-09: installed modules live under ~/.mesh/modules/."
    - "D-10: each installed module has its own package.json; plugin.json remains a legacy alias if needed."
    - "D-20: general user-owned shell files live under ~/.mesh, not ~/.config/mesh."
    - "D-24: modules record Git origin metadata, but Phase 1 does not implement download."
    - "D-25: Phase 1 builds local installed-module/package graph only."
    - "D-27: module rename is strategic; Phase 1 establishes compatibility boundaries if full rename is too broad."
  artifacts:
    - path: "crates/core/extension/plugin/src/package.rs"
      provides: "Raw root package schema, module package schema, path helpers, and compatibility loader"
      contains: "RootPackageManifest"
    - path: "crates/core/extension/plugin/src/manifest.rs"
      provides: "Compatibility support for module package.json loading"
      contains: "package.json"
    - path: "crates/core/extension/plugin/src/lib.rs"
      provides: "Public package/module graph exports"
      contains: "pub mod package"
---

<objective>
Create the module package foundation: `~/.mesh` path helpers, raw `~/.mesh/package.json` installed-state schema, module-level `package.json` schema, and a compatibility loader that prefers module `package.json` while preserving existing `plugin.json` support.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-plugin-package-manifest-foundation/01-CONTEXT.md
@.planning/phases/01-plugin-package-manifest-foundation/01-RESEARCH.md
@.planning/phases/01-plugin-package-manifest-foundation/01-VALIDATION.md
@.planning/phases/01-plugin-package-manifest-foundation/01-PATTERNS.md

@crates/core/extension/plugin/src/manifest.rs
@crates/core/extension/plugin/src/lib.rs
@crates/core/extension/plugin/Cargo.toml
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| T-01-01: malformed root package JSON silently creates an empty graph | high | Parse through typed serde structs and return `PackageManifestError`; tests assert malformed JSON fails. |
| T-01-02: `MESH_HOME` override points to a relative or empty path and causes confusing file resolution | high | Path helper rejects empty and relative `MESH_HOME`; tests assert rejection. |
| T-01-03: package.json and plugin.json resolution order is ambiguous | medium | Loader always prefers `package.json` when both exist and falls back to `plugin.json` only as a legacy alias. |
| T-01-04: Git origin metadata turns into downloader behavior | medium | Schema stores repository/origin metadata only; no clone, fetch, marketplace, signature, or download command is implemented. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add `~/.mesh` path helpers and raw root manifest schema</name>
  <files>crates/core/extension/plugin/src/package.rs, crates/core/extension/plugin/src/lib.rs</files>
  <read_first>crates/core/extension/plugin/src/manifest.rs, crates/core/extension/plugin/src/lib.rs, crates/core/foundation/config/src/lib.rs, .planning/phases/01-plugin-package-manifest-foundation/01-RESEARCH.md</read_first>
  <action>Create `crates/core/extension/plugin/src/package.rs`. Add public functions `mesh_home() -> Result<PathBuf, PackageManifestError>`, `root_package_manifest_path() -> Result<PathBuf, PackageManifestError>`, `settings_path() -> Result<PathBuf, PackageManifestError>`, `modules_dir() -> Result<PathBuf, PackageManifestError>`, and `themes_dir() -> Result<PathBuf, PackageManifestError>`. `mesh_home()` must use non-empty absolute `MESH_HOME` when set, otherwise default to `$HOME/.mesh`. Add raw serde structs named exactly `RootPackageManifest`, `InstalledModuleEntry`, `RootLayoutSelection`, and `RootThemeSelection`. `RootPackageManifest` must support top-level JSON keys `schemaVersion`, `modulesDir`, `modules`, `providers`, `layout`, and `theme`. Expose `pub mod package;` from `crates/core/extension/plugin/src/lib.rs` and public re-exports for `RootPackageManifest` and `PackageManifestError`.</action>
  <verify>nix develop -c cargo test -p mesh-core-plugin module_package_paths module_root_manifest</verify>
  <acceptance_criteria>
    - `crates/core/extension/plugin/src/package.rs` contains `pub fn mesh_home`.
    - `crates/core/extension/plugin/src/package.rs` contains `pub fn root_package_manifest_path`.
    - `crates/core/extension/plugin/src/package.rs` contains `pub struct RootPackageManifest`.
    - `crates/core/extension/plugin/src/package.rs` contains `pub struct InstalledModuleEntry`.
    - `crates/core/extension/plugin/src/lib.rs` contains `pub mod package;`.
    - Test `module_package_paths_default_to_dot_mesh` asserts `root_package_manifest_path()` ends with `.mesh/package.json` when `MESH_HOME` is unset.
    - Test `module_package_paths_reject_relative_mesh_home` asserts `MESH_HOME=relative/path` returns `PackageManifestError`.
    - Test `module_root_manifest_parses_minimal_package_json` parses JSON containing `"schemaVersion": 1`, `"modulesDir": "modules"`, `"modules": {}`, `"providers": {}`, `"layout": {"entrypoint": "@mesh/panel:main"}`, and `"theme": {"active": "@mesh/default-theme", "mode": "dark"}`.
  </acceptance_criteria>
  <done>`~/.mesh` paths and the raw root installed-state package schema exist and are publicly exported.</done>
</task>

<task type="auto">
  <name>Task 2: Add module-level package.json envelope and Git origin metadata</name>
  <files>crates/core/extension/plugin/src/package.rs</files>
  <read_first>crates/core/extension/plugin/src/package.rs, crates/core/extension/plugin/src/manifest.rs, packages/plugins/backend/core/pipewire-audio/plugin.json, .planning/phases/01-plugin-package-manifest-foundation/01-CONTEXT.md</read_first>
  <action>In `package.rs`, add serde structs named exactly `ModulePackageManifest`, `MeshModuleSection`, `ModuleKind`, `ModuleRepository`, `MeshEntrypoints`, `MeshDependencies`, `MeshProvidesDeclaration`, and `MeshContributes`. The top-level module manifest must parse package-compatible fields `name`, `version`, `description`, `license`, and `repository`, with MESH-specific fields nested under `mesh`. `MeshModuleSection` must include `apiVersion`, `kind`, `entrypoints`, `dependencies`, `provides`, `contributes`, and optional `experimental`. `ModuleKind` must include `frontend`, `backend`, `theme`, `icon-pack`, `font-pack`, `language-pack`, and `interface` serde values. `ModuleRepository` must support `{ "type": "git", "url": "git+https://example.invalid/module.git" }` and validate that `type: "git"` has a non-empty `url`.</action>
  <verify>nix develop -c cargo test -p mesh-core-plugin module_package_manifest</verify>
  <acceptance_criteria>
    - `crates/core/extension/plugin/src/package.rs` contains `pub struct ModulePackageManifest`.
    - `crates/core/extension/plugin/src/package.rs` contains `pub enum ModuleKind`.
    - `crates/core/extension/plugin/src/package.rs` contains `pub struct MeshContributes`.
    - Test `module_package_manifest_parses_backend_package_json` parses `"name": "@mesh/pipewire-audio"`, `"mesh": {"kind": "backend"}`, `"entrypoints": {"main": "src/main.luau"}`, and a Git repository URL.
    - Test `module_package_manifest_rejects_empty_git_origin_url` asserts `{"repository":{"type":"git","url":""}}` returns an error.
    - `grep -R "git clone\\|git fetch\\|download\\|marketplace" crates/core/extension/plugin/src/package.rs` has no implementation logic for remote installation.
  </acceptance_criteria>
  <done>Every module kind can use one common package envelope with Git origin metadata but no downloader behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Prefer package.json while preserving plugin.json compatibility</name>
  <files>crates/core/extension/plugin/src/package.rs, crates/core/extension/plugin/src/manifest.rs</files>
  <read_first>crates/core/extension/plugin/src/package.rs, crates/core/extension/plugin/src/manifest.rs, packages/plugins/frontend/core/panel/plugin.json</read_first>
  <action>Add `pub fn load_module_manifest(module_dir: &Path) -> Result<LoadedModuleManifest, PackageManifestError>` in `package.rs`. The loader must check `module_dir/package.json` first, then `module_dir/plugin.json` as a legacy alias. Add `LoadedModuleManifest` with fields `manifest`, `path`, and `source`, where `source` can distinguish `PackageJson` from `LegacyPluginJson`. If using existing `manifest.rs` parsing for legacy manifests, convert enough data into `ModulePackageManifest` to preserve `name`, `version`, `kind`, entrypoint, dependencies, provides, settings, i18n, theme, assets, icon requirements, and repository where available. If adding direct `package.json` support to `manifest.rs` is cleaner, keep `plugin.json` behavior unchanged and add tests proving precedence.</action>
  <verify>nix develop -c cargo test -p mesh-core-plugin module_manifest_loader</verify>
  <acceptance_criteria>
    - `crates/core/extension/plugin/src/package.rs` contains `load_module_manifest`.
    - `crates/core/extension/plugin/src/package.rs` contains `LoadedModuleManifest`.
    - Test `module_manifest_loader_prefers_package_json_over_plugin_json` creates both files in a temp module directory and asserts the loaded source is `PackageJson`.
    - Test `module_manifest_loader_accepts_legacy_plugin_json` loads a directory with only `plugin.json` and asserts the source is `LegacyPluginJson`.
    - Test `module_manifest_loader_preserves_legacy_panel_entrypoint` asserts legacy `packages/plugins/frontend/core/panel/plugin.json` maps to module name `@mesh/panel` and entrypoint `src/main.mesh`.
  </acceptance_criteria>
  <done>Module package loading uses the new naming convention without breaking existing plugin fixtures.</done>
</task>

</tasks>

<verification>
- Run `nix develop -c cargo test -p mesh-core-plugin module_package`.
- Run `grep -R "RootPackageManifest\\|ModulePackageManifest\\|ModuleKind\\|load_module_manifest\\|mesh_home" -n crates/core/extension/plugin/src`.
- Run `grep -R "git clone\\|git fetch\\|marketplace\\|signature\\|download" -n crates/core/extension/plugin/src/package.rs` and confirm Phase 1 has no remote install behavior.
</verification>

<success_criteria>
- PINST-01 is covered by `RootPackageManifest` and `~/.mesh/package.json` path helpers.
- PINST-03 is prepared through `ModuleKind::Backend` plus provider declarations in the module envelope.
- PINST-06 is preserved by metadata-only Git origin handling and static no-download checks.
- D-01, D-02, D-04, D-05, D-07, D-08, D-09, D-10, D-20, D-24, D-25, and D-27 are visible in this plan's must_haves.
</success_criteria>

<output>
After completion, create `.planning/phases/01-plugin-package-manifest-foundation/01-01-SUMMARY.md`
</output>
