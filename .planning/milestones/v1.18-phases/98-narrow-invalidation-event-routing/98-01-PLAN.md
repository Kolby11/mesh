---
phase: 98-narrow-invalidation-event-routing
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/core/shell/src/shell/component.rs
  - crates/core/shell/src/shell/component/rendering.rs
  - crates/core/shell/src/shell/component/shell_component.rs
  - crates/core/foundation/debug/src/lib.rs
  - crates/core/shell/src/shell/component/tests/invalidation.rs
  - crates/core/shell/src/shell/component/tests/invalidation/narrow_script.rs
autonomous: true
requirements: [INV-01, INV-03]

must_haves:
  truths:
    - "After invalidate_script_state_narrow() is called, dirty_types contains SCRIPT_NARROW (bit 1<<9) and does NOT contain TREE_REBUILD bits (SCRIPT or TEXT)"
    - "ComponentDirtyFlags::requires_tree_rebuild() returns false when only SCRIPT_NARROW is set"
    - "After build_tree() the narrow_script_update() entry point computes a per-node diff via RetainedNodeSnapshot.diff_flags() against the prior retained tree snapshot"
    - "When any per-node diff has RetainedNodeDirtyFlags::CHILDREN or INSERTED set, narrow_script_update() returns None (structural fallback to TREE_REBUILD)"
    - "When affected_node_count * 2 > tree.node_count(), narrow_script_update() returns None before mutating any dirty set (threshold fallback)"
    - "When narrow path is taken, every ancestor NodeId of each changed leaf is added to the dirty NodeId set"
    - "paint() routes the SCRIPT_NARROW path before the existing use_retained_style_path branch, falling through to build_tree() when narrow_script_update() returns None"
    - "finalize_tree() called from the narrow path uses trigger_kind=\"rebuild\" so NodeServiceFieldDependencies stays fresh"
  artifacts:
    - path: "crates/core/shell/src/shell/component.rs"
      provides: "SCRIPT_NARROW = 1 << 9 bit; invalidate_script_state_narrow() method; ComponentInvalidationCounts.script_narrow population in to_debug_counts()"
      contains: "SCRIPT_NARROW"
    - path: "crates/core/shell/src/shell/component/rendering.rs"
      provides: "narrow_script_update(theme, width, height) -> Option<WidgetNode> with tree diff + threshold guard + ancestor expansion"
      contains: "narrow_script_update"
    - path: "crates/core/shell/src/shell/component/shell_component.rs"
      provides: "paint() routing branch for SCRIPT_NARROW that calls narrow_script_update() before retained_style_path"
      contains: "SCRIPT_NARROW"
    - path: "crates/core/foundation/debug/src/lib.rs"
      provides: "narrow_path: bool and affected_node_count: u64 on ProfilingInvalidationSnapshot; script_narrow: u64 on ComponentInvalidationCounts"
      contains: "narrow_path"
    - path: "crates/core/shell/src/shell/component/tests/invalidation/narrow_script.rs"
      provides: "Unit tests: script_narrow_flag_does_not_trigger_tree_rebuild, narrow_path_dirties_ancestor_chain, structural_change_falls_back_to_tree_rebuild, threshold_fallback_exceeds_half, threshold_narrow_below_half"
      contains: "fn script_narrow"
    - path: "crates/core/shell/src/shell/component/tests/invalidation.rs"
      provides: "mod narrow_script; declaration"
      contains: "mod narrow_script"
  key_links:
    - from: "ComponentDirtyFlags::SCRIPT_NARROW"
      to: "paint() narrow branch -> narrow_script_update()"
      via: "take_dirty_for_paint() returns flags; paint() checks flags.contains(SCRIPT_NARROW)"
      pattern: "SCRIPT_NARROW"
    - from: "narrow_script_update()"
      to: "RetainedNodeSnapshot.diff_flags()"
      via: "per-node comparison against previous retained tree snapshot"
      pattern: "diff_flags"
    - from: "narrow_script_update()"
      to: "WidgetNode::node_count()"
      via: "threshold guard: affected_count * 2 > total"
      pattern: "node_count"
---

<objective>
Add the `SCRIPT_NARROW` dirty flag bit to `ComponentDirtyFlags`, an
`invalidate_script_state_narrow()` method, and a `narrow_script_update()`
entry point in `rendering.rs` that performs a post-`build_tree()` per-node
diff using existing `RetainedNodeSnapshot.diff_flags()` machinery. The narrow
path expands changed leaf NodeIds to include their ancestor chains, applies
the >50% threshold guard before any mutation, and falls back to TREE_REBUILD
on any structural change (CHILDREN | INSERTED). Wire `paint()` to route
`SCRIPT_NARROW` before the existing retained-style path. Extend
`ProfilingInvalidationSnapshot` with `narrow_path: bool` and
`affected_node_count: u64`. Wave 0 stub tests are added in a new
`narrow_script.rs` test module covering INV-01 (leaf-only routing, ancestor
expansion, structural fallback) and INV-03 (threshold fallback above/below
50%).

Purpose: Satisfies INV-01 (leaf-only script changes bypass TREE_REBUILD with
ancestor chain dirtying) and INV-03 (>50% threshold fallback).

Output: Modified `component.rs` (new bit + method + counts wiring), modified
`rendering.rs` (`narrow_script_update`), modified `shell_component.rs`
(paint routing), modified `debug/src/lib.rs` (snapshot fields), new
`tests/invalidation/narrow_script.rs`, updated `tests/invalidation.rs` mod
file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/98-narrow-invalidation-event-routing/98-CONTEXT.md
@.planning/phases/98-narrow-invalidation-event-routing/98-RESEARCH.md
@.planning/phases/98-narrow-invalidation-event-routing/98-PATTERNS.md
@.planning/phases/97-service-field-dependency-tracking/97-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave 0 test stubs for SCRIPT_NARROW routing and threshold</name>
  <read_first>
    crates/core/shell/src/shell/component/tests/invalidation.rs,
    crates/core/shell/src/shell/component/tests/invalidation/basic.rs,
    crates/core/shell/src/shell/component/tests/common.rs,
    crates/core/shell/src/shell/component.rs
  </read_first>
  <action>
    Create `crates/core/shell/src/shell/component/tests/invalidation/narrow_script.rs` with five `#[test]` function stubs whose bodies are `unimplemented!("INV-01 narrow routing")` / `unimplemented!("INV-03 threshold")` for now:
    - `script_narrow_flag_does_not_trigger_tree_rebuild` — INV-01 routing assertion (will call `invalidate_script_state_narrow()` and assert `take_dirty_for_paint()` returns `requires_tree_rebuild == false` and `flags.contains(ComponentDirtyFlags::SCRIPT_NARROW)`).
    - `narrow_path_dirties_ancestor_chain` — INV-01 ancestor expansion (will check that after a narrow update, the dirty NodeId set includes ancestor IDs of changed leaves).
    - `structural_change_falls_back_to_tree_rebuild` — INV-01 structural fallback (will assert `narrow_script_update()` returns `None` when a CHILDREN/INSERTED diff is present).
    - `threshold_fallback_exceeds_half` — INV-03: when affected_count * 2 > node_count, `narrow_script_update()` returns `None`.
    - `threshold_narrow_below_half` — INV-03: when affected_count * 2 <= node_count, `narrow_script_update()` returns `Some(tree)`.
    Mirror imports and helpers from `tests/invalidation/basic.rs` (`test_frontend_component`, `ComponentDirtyFlags`). Register the new module in `crates/core/shell/src/shell/component/tests/invalidation.rs` by adding `mod narrow_script;` next to the existing `mod basic;` / `mod profiling;` lines. Each stub body MUST compile under `cargo check` even if the test asserts panic at runtime (use `#[ignore]` until later tasks implement the routing). Use `#[test] #[ignore = "wave 0 stub, populated by tasks 2-4"]` so `cargo test` does not fail the build.
  </action>
  <verify>
    <automated>cargo check -p mesh-core-shell 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File `crates/core/shell/src/shell/component/tests/invalidation/narrow_script.rs` exists.
    - `grep -c '^fn script_narrow_flag_does_not_trigger_tree_rebuild\|^fn narrow_path_dirties_ancestor_chain\|^fn structural_change_falls_back_to_tree_rebuild\|^fn threshold_fallback_exceeds_half\|^fn threshold_narrow_below_half' crates/core/shell/src/shell/component/tests/invalidation/narrow_script.rs` returns 5.
    - `grep -c 'mod narrow_script;' crates/core/shell/src/shell/component/tests/invalidation.rs` returns 1.
    - `cargo check -p mesh-core-shell` exits 0.
    - Every stub has the `#[ignore = "wave 0 stub, populated by tasks 2-4"]` attribute so `cargo test` does not fail.
  </acceptance_criteria>
  <done>Stub file compiles, registered in invalidation module, five test names match the required identifiers, all ignored until later tasks.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add SCRIPT_NARROW bit + invalidate_script_state_narrow() + debug snapshot fields</name>
  <read_first>
    crates/core/shell/src/shell/component.rs,
    crates/core/foundation/debug/src/lib.rs,
    .planning/phases/98-narrow-invalidation-event-routing/98-PATTERNS.md
  </read_first>
  <behavior>
    - `ComponentDirtyFlags::SCRIPT_NARROW` is defined as `1 << 9`.
    - `ComponentDirtyFlags::TREE_REBUILD` constant does NOT include `SCRIPT_NARROW`.
    - `ComponentDirtyFlags::requires_tree_rebuild()` returns false when only SCRIPT_NARROW is set; still returns true when SCRIPT or TEXT is set.
    - `invalidate_script_state_narrow()` sets `surface_pixels_invalid = true` and invalidates with `ComponentDirtyFlags::SCRIPT_NARROW` (not TREE_REBUILD).
    - `ProfilingInvalidationSnapshot` has public fields `narrow_path: bool` and `affected_node_count: u64` (defaulting to `false` / `0`).
    - `ComponentInvalidationCounts` has public field `script_narrow: u64`.
    - `to_debug_counts()` (or equivalent counts-builder in `component.rs`) increments `script_narrow` when `flags.contains(SCRIPT_NARROW)`.
    - Then un-`#[ignore]` and populate `script_narrow_flag_does_not_trigger_tree_rebuild` in `narrow_script.rs` to assert this routing.
  </behavior>
  <action>
    In `crates/core/shell/src/shell/component.rs`, inside the existing `bitflags!` block for `ComponentDirtyFlags` (around line 67-80), add `const SCRIPT_NARROW = 1 << 9;` after `const SURFACE_CONFIG = 1 << 8;`. Do NOT add SCRIPT_NARROW to the `TREE_REBUILD` compound constant defined at line 83. The `requires_tree_rebuild()` predicate at line 118 stays `self.intersects(Self::SCRIPT | Self::TEXT)` — SCRIPT_NARROW must NOT be added. Add a new method `pub(super) fn invalidate_script_state_narrow(&mut self)` next to `invalidate_script_state()` (line 503) that sets `self.surface_pixels_invalid = true;` then calls `self.invalidate(ComponentDirtyFlags::SCRIPT_NARROW);`. In `crates/core/foundation/debug/src/lib.rs`, extend struct `ProfilingInvalidationSnapshot` (around line 173) with `pub narrow_path: bool,` and `pub affected_node_count: u64,` (both default-initialized via `#[derive(Default)]`). Extend `ComponentInvalidationCounts` (around line 184) with `pub script_narrow: u64,`. In `component.rs`, locate the existing `to_debug_counts()` (or equivalent function around lines 122-134 that maps `ComponentDirtyFlags` -> `ComponentInvalidationCounts`) and add a branch that sets `script_narrow = 1` when `self.contains(ComponentDirtyFlags::SCRIPT_NARROW)`. Then in `narrow_script.rs`, remove `#[ignore]` from `script_narrow_flag_does_not_trigger_tree_rebuild` and populate its body: build a `test_frontend_component`, clear dirty state, call `invalidate_script_state_narrow()`, assert `take_dirty_for_paint()` returns `requires_tree_rebuild == false` and `flags.contains(ComponentDirtyFlags::SCRIPT_NARROW)`. Use the structure of `typed_invalidations_distinguish_restyle_from_script_rebuild` (basic.rs line 36) as the template.
  </action>
  <verify>
    <automated>cargo test -p mesh-core-shell shell::component::tests::invalidation::narrow_script::script_narrow_flag_does_not_trigger_tree_rebuild 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'SCRIPT_NARROW = 1 << 9' crates/core/shell/src/shell/component.rs` returns 1.
    - `grep 'pub(super) const TREE_REBUILD' crates/core/shell/src/shell/component.rs` does NOT match `SCRIPT_NARROW` on the same logical statement (verify with: `awk '/pub\\(super\\) const TREE_REBUILD/,/;/' crates/core/shell/src/shell/component.rs | grep -c SCRIPT_NARROW` returns 0).
    - `grep -c 'fn invalidate_script_state_narrow' crates/core/shell/src/shell/component.rs` returns 1.
    - `grep -c 'pub narrow_path: bool' crates/core/foundation/debug/src/lib.rs` returns 1.
    - `grep -c 'pub affected_node_count: u64' crates/core/foundation/debug/src/lib.rs` returns 1.
    - `grep -c 'pub script_narrow: u64' crates/core/foundation/debug/src/lib.rs` returns 1.
    - Test `script_narrow_flag_does_not_trigger_tree_rebuild` passes.
  </acceptance_criteria>
  <done>SCRIPT_NARROW bit defined, narrow invalidator method exists, debug snapshot extended, first narrow_script.rs unit test green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement narrow_script_update() with tree diff, ancestor expansion, threshold guard, structural fallback</name>
  <read_first>
    crates/core/shell/src/shell/component/rendering.rs,
    crates/core/shell/src/shell/component/runtime_tree.rs,
    crates/core/ui/elements/src/tree.rs,
    .planning/phases/98-narrow-invalidation-event-routing/98-RESEARCH.md,
    .planning/phases/98-narrow-invalidation-event-routing/98-PATTERNS.md
  </read_first>
  <behavior>
    - `narrow_script_update(&mut self, theme, width, height) -> Option<WidgetNode>` exists on the same impl block as `restyle_retained_tree()` and `build_tree()`.
    - The method calls `self.build_tree(theme, width, height)` to produce a fresh tree (so `render_hooks_pending` runs, `node_service_field_deps` rebuilds inside `finalize_tree(trigger_kind="rebuild")`).
    - Then walks the prior retained tree node snapshots and diffs each surviving NodeId via `RetainedNodeSnapshot.diff_flags()` against the corresponding new snapshot.
    - If ANY per-node diff contains `RetainedNodeDirtyFlags::CHILDREN | RetainedNodeDirtyFlags::INSERTED`, return `None` (structural fallback).
    - Collects `changed_leaf_ids: HashSet<NodeId>` from diffs that are non-empty but non-structural (ATTRIBUTES | STYLE | LAYOUT | STATE on leaf nodes).
    - Computes `total = tree.node_count()`. If `total == 0 || changed_leaf_ids.len() * 2 > total`, return `None` (threshold fallback, INV-03) BEFORE mutating any dirty set.
    - Otherwise, expands `changed_leaf_ids` to include each ancestor NodeId by walking the parent chain in the retained tree, populates `self.dirty_node_ids` (or equivalent dirty set used by retained_tree.update) with the expansion, records profiling stage with `trigger_kind=Some("narrow")`, and returns `Some(tree)`.
    - Profiling: increments `affected_node_count` on the current `ProfilingInvalidationSnapshot` builder by the expanded set size, sets `narrow_path = true`.
    - Then un-`#[ignore]` and populate `narrow_path_dirties_ancestor_chain`, `structural_change_falls_back_to_tree_rebuild`, `threshold_fallback_exceeds_half`, and `threshold_narrow_below_half` in `narrow_script.rs`.
  </behavior>
  <action>
    In `crates/core/shell/src/shell/component/rendering.rs`, after the existing `restyle_retained_tree()` method (lines 156-167), add a new method `pub(super) fn narrow_script_update(&mut self, theme: &Theme, width: u32, height: u32) -> Option<WidgetNode>`. Implementation steps in order: (1) call `let tree = self.build_tree(theme, width, height);` — this triggers the standard render_hooks + finalize_tree(trigger_kind="rebuild") path so `node_service_field_deps` is fresh. (2) Obtain the prior retained tree node snapshots via the existing `self.retained_tree` access pattern used in `restyle_retained_tree` and `finalize_tree`. If no prior retained tree exists (first render), return `None` so the caller falls through to the standard rebuild. (3) Build the new node snapshot map (or reuse the one finalize_tree just produced — pick the access path that matches how `finalize_tree` already stores diff results around line 200-220). (4) Walk the intersection of (old NodeIds, new NodeIds): for each surviving NodeId compute `old.diff_flags(&new) -> (RetainedNodeDirtyFlags, u32)`. If any returned flags `.intersects(RetainedNodeDirtyFlags::CHILDREN | RetainedNodeDirtyFlags::INSERTED)`, return `None`. Otherwise, if the flags are non-empty, push the NodeId into `changed_leaf_ids: HashSet<NodeId>`. (5) Compute `let total = tree.node_count();`. If `total == 0 || changed_leaf_ids.len() * 2 > total`, return `None` (INV-03 threshold guard). MUST happen before any mutation to `self.dirty_node_ids` or equivalent dirty set. (6) Build expanded set: for each id in `changed_leaf_ids`, walk parent chain using the retained tree's parent map (or `WidgetNode::find` parent navigation) and insert each ancestor NodeId into `expanded: HashSet<NodeId>`. Store the result into the existing per-component dirty NodeId set that `retained_tree.update_with_dirty_nodes` reads. (7) Call `self.record_profiling_stage(ProfilingStage::NarrowScriptUpdate /* or closest existing stage */, started, Some("narrow"));` — if `NarrowScriptUpdate` does not exist, use the nearest existing stage variant and document. Update the current ProfilingInvalidationSnapshot in-progress builder so `narrow_path = true` and `affected_node_count = expanded.len() as u64`. (8) Return `Some(tree)`. Then in `narrow_script.rs`: un-ignore the four remaining tests and populate them following the structure of `phase18_script_and_text_invalidations_take_full_rebuild_paint_path` (basic.rs line 103). For threshold tests, construct a small synthetic retained tree fixture (or use `test_frontend_component` with a known node count) and inject changed_leaf_ids cardinality such that the ratio is `>0.5` vs `<=0.5`.
  </action>
  <verify>
    <automated>cargo test -p mesh-core-shell shell::component::tests::invalidation::narrow_script 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'fn narrow_script_update' crates/core/shell/src/shell/component/rendering.rs` returns 1.
    - `grep -c 'CHILDREN \\| RetainedNodeDirtyFlags::INSERTED\\|INSERTED \\| RetainedNodeDirtyFlags::CHILDREN\\|intersects(.*CHILDREN' crates/core/shell/src/shell/component/rendering.rs` returns at least 1 (structural fallback check present).
    - `grep -c 'node_count()' crates/core/shell/src/shell/component/rendering.rs` returns at least 1 in the narrow path.
    - Source assertion: in `narrow_script_update`, the threshold check (`changed_leaf_ids.len() * 2 > total` or equivalent `> total / 2` comparison) appears BEFORE any insertion into `self.dirty_node_ids` (verify by manual inspection — Task 3 acceptance includes this code-order requirement).
    - Tests `narrow_path_dirties_ancestor_chain`, `structural_change_falls_back_to_tree_rebuild`, `threshold_fallback_exceeds_half`, `threshold_narrow_below_half` all pass.
    - No `#[ignore]` attributes remain on any test in `narrow_script.rs`.
  </acceptance_criteria>
  <done>narrow_script_update() implemented with tree diff, structural fallback, threshold guard, ancestor expansion; all five narrow_script.rs tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Wire SCRIPT_NARROW branch into paint() routing</name>
  <read_first>
    crates/core/shell/src/shell/component/shell_component.rs,
    crates/core/shell/src/shell/component.rs,
    crates/core/shell/src/shell/component/rendering.rs
  </read_first>
  <behavior>
    - `paint()` in `shell_component.rs` checks `dirty_types.contains(ComponentDirtyFlags::SCRIPT_NARROW) && !requires_tree_rebuild` BEFORE the existing `use_retained_style_path` branch.
    - On SCRIPT_NARROW branch: call `self.narrow_script_update(theme, content_width, content_height)`; on `Some(tree)` use it; on `None` fall through to `self.build_tree(theme, content_width, content_height)`.
    - The existing retained_style_path and full rebuild branches remain unchanged in behavior when SCRIPT_NARROW is not set.
    - A regression test (in `narrow_script.rs`) verifies that after `invalidate_script_state_narrow()` followed by `paint()`, `take_invalidation_snapshot().narrow_path == true`.
  </behavior>
  <action>
    In `crates/core/shell/src/shell/component/shell_component.rs`, locate the `paint()` method tree-selection block (around lines 325-332 where `let mut tree = if use_retained_style_path { ... } else { self.build_tree(...) };` appears). Replace with a three-branch select:
    Branch order: (1) `if dirty_types.contains(ComponentDirtyFlags::SCRIPT_NARROW) && !requires_tree_rebuild` → call `self.narrow_script_update(theme, content_width, content_height)`; on `Some(t)` bind `t`; on `None` call `self.build_tree(theme, content_width, content_height)`. (2) `else if use_retained_style_path` → existing `restyle_retained_tree` branch unchanged. (3) `else` → existing `self.build_tree(...)` unchanged. The decision must consult both `requires_tree_rebuild` and `dirty_types` returned by `take_dirty_for_paint()` (already in scope). Do NOT modify `take_dirty_for_paint()` return shape — SCRIPT_NARROW is detected via `flags.contains()`. In `narrow_script.rs`, add a sixth test (do NOT add it to must_haves list above — it is an integration check): `fn paint_with_script_narrow_records_narrow_path_in_snapshot` that calls `set_profiling_enabled(true)`, `invalidate_script_state_narrow()`, `paint(...)`, then `take_invalidation_snapshot()` and asserts `snapshot.narrow_path == true`.
  </action>
  <verify>
    <automated>cargo test -p mesh-core-shell shell::component::tests::invalidation 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - In `crates/core/shell/src/shell/component/shell_component.rs`, the `paint()` method's tree-selection block contains a reference to `SCRIPT_NARROW` and to `narrow_script_update`: both `grep -c 'SCRIPT_NARROW' crates/core/shell/src/shell/component/shell_component.rs` and `grep -c 'narrow_script_update' crates/core/shell/src/shell/component/shell_component.rs` return at least 1.
    - The narrow branch appears textually before the `use_retained_style_path` branch in the source (verify ordering by line numbers).
    - Behavior: `cargo test -p mesh-core-shell shell::component::tests::invalidation::narrow_script` passes (all 6 tests).
    - Behavior: existing tests in `tests/invalidation/basic.rs` (including `phase18_script_and_text_invalidations_take_full_rebuild_paint_path`) still pass — `cargo test -p mesh-core-shell shell::component::tests::invalidation::basic` exits 0.
  </acceptance_criteria>
  <done>paint() routes SCRIPT_NARROW first, falls back to build_tree on None, integration test confirms snapshot.narrow_path = true; existing invalidation tests unaffected.</done>
</task>

</tasks>

<verification>
  <automated>cargo test -p mesh-core-shell shell::component::tests::invalidation 2>&1 | tail -20</automated>
  <automated>cargo check --workspace 2>&1 | tail -10</automated>
</verification>

<success_criteria>
- `ComponentDirtyFlags::SCRIPT_NARROW = 1 << 9` exists; `TREE_REBUILD` constant does NOT include it; `requires_tree_rebuild()` returns false when only SCRIPT_NARROW is set.
- `invalidate_script_state_narrow()` exists on the same impl block as `invalidate_script_state()`.
- `narrow_script_update()` exists on `FrontendSurfaceComponent` and returns `Option<WidgetNode>`.
- Structural change detection (CHILDREN | INSERTED) forces `None` return.
- Threshold guard (affected * 2 > total) returns `None` before any dirty-set mutation.
- Ancestor expansion populates dirty NodeId set with parent chain of each changed leaf.
- `paint()` routes SCRIPT_NARROW branch before `use_retained_style_path`.
- `ProfilingInvalidationSnapshot.narrow_path` and `affected_node_count` fields exist; populated on narrow path.
- All five unit tests in `tests/invalidation/narrow_script.rs` plus the paint integration test pass.
- `cargo check --workspace` exits 0.
</success_criteria>

<output>
Create `.planning/phases/98-narrow-invalidation-event-routing/98-01-SUMMARY.md` when done.
</output>
