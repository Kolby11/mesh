# Requirements Archive: MESH v1.18 Performance: Smart Invalidation

**Archived:** 2026-06-09
**Status:** Complete ✅ — All 13 requirements validated and shipped

---

## v1 Requirements — All Complete ✅

### Selector Dependency Tracking (Phase 96)

- [x] **SEL-01**: `StyleRuleIndex` builds per-rule dependency masks recording which pseudo-class/state bits each rule depends on.
- [x] **SEL-02**: A reverse index maps state-bit changes to affected rule indices for O(1) lookup instead of full rule iteration.
- [x] **SEL-03**: `:hover`, `:focus`, and `:active` pseudo-class transitions restyle only nodes whose matched rules' dependency sets intersect the changed state.
- [x] **SEL-04**: Inherited style values (color, font-family, font-size, font-weight, line-height) propagate correctly to children of restyled nodes.
- [x] **SEL-05**: Existing shipped-surface regression tests pass with selector-narrow restyle enabled (navigation bar, audio popover).

### Per-Node Service Dependency Tracking (Phase 97)

- [x] **SRV-01**: During render, the template evaluator records per-node service field reads ((service, field) pairs per NodeId).
- [x] **SRV-02**: A bidirectional `NodeServiceFieldDependencies` index supports both "which nodes read field X" and "which fields does node Y read" queries.
- [x] **SRV-03**: Per-node field tracking overhead is below 1% of total render pass time on shipped surfaces.

### Narrow Invalidation & Event Routing (Phase 98)

- [x] **INV-01**: Simple text/value script state changes dirty only the affected leaf nodes plus their layout ancestor chain, not `TREE_REBUILD`.
- [x] **INV-02**: Service events fan out only to components whose tracked field sets intersect the changed fields.
- [x] **INV-03**: `TREE_REBUILD` fallback activates when >50% of nodes are affected, preserving correctness for bulk changes.
- [x] **INV-04**: Profiling payloads show reduced dirty-node counts and retained-tree churn across canonical benchmarks (hover, open/close, slider, traversal, backend-update).
- [x] **INV-05**: Pixel-identical output on all benchmark scenarios (equivalence testing against pre-invalidation baseline rendering).

---

## Traceability

| Requirement | Phase | Outcome |
|-------------|-------|---------|
| SEL-01 | Phase 96 | Validated — `state_to_rules` index + `rules_for_state_bit()` shipped |
| SEL-02 | Phase 96 | Validated — reverse index maps state-bit→rule in O(1) |
| SEL-03 | Phase 96 | Validated — targeted interaction restyle in `finalize_tree()` |
| SEL-04 | Phase 96 | Validated — inherited style propagation verified |
| SEL-05 | Phase 96 | Validated — navigation bar + audio popover regression tests pass |
| SRV-01 | Phase 97 | Validated — `service_field_reads` on `WidgetNode` + `TrackingVariableStore` |
| SRV-02 | Phase 97 | Validated — bidirectional `NodeServiceFieldDependencies` index |
| SRV-03 | Phase 97 | Validated — <1% overhead benchmark confirmed |
| INV-01 | Phase 98 | Validated — `SCRIPT_NARROW` flag + `narrow_script_update()` with ancestor expansion |
| INV-02 | Phase 98 | Validated — `json_field_diff` + `nodes_reading_field()` intersection check |
| INV-03 | Phase 98 | Validated — >50% threshold guard before narrow mutation |
| INV-04 | Phase 98 | Validated — profiling benchmark shows `narrow_path` + `affected_node_count` |
| INV-05 | Phase 98 | Validated — FNV-1a pixel equivalence on backend-update scenario |

**Coverage:** 13/13 requirements validated and shipped (100%)
