---
phase: 08-practical-css-coverage
plan: 03
type: execute
wave: 3
depends_on: [08-02]
files_modified:
  - crates/core/ui/elements/src/layout.rs
  - crates/core/ui/render/src/surface/painter.rs
  - crates/core/ui/elements/src/style.rs
autonomous: true
requirements: [CSS-01, CSS-02]
requirements_addressed: [CSS-01, CSS-02]
user_setup: []
must_haves:
  truths:
    - "D-01: the supported subset covers box model, sizing, flex layout, typography, borders/radius, overflow, display/visibility-like behavior, opacity/color/background, positioning/inset, selector state hooks, transition declarations, and animation declarations needed by later phases."
    - "D-04: ComputedStyle fields should be extended only when a property has a real renderer/layout consumer or is required by a later v1.2 phase."
    - "D-09: layout and paint consume concrete token/variable-resolved values."
    - "D-13: computed style and value resolution stay in mesh-core-elements; paint/layout consumption stays in mesh-core-render."
  artifacts:
    - path: "crates/core/ui/elements/src/layout.rs"
      provides: "Flex, absolute positioning, overflow-aware layout, and computed style consumption"
      contains: "LayoutEngine"
    - path: "crates/core/ui/render/src/surface/painter.rs"
      provides: "Background, border, text, overflow clipping, scrollbar, and z-index paint consumption"
      contains: "FrontendRenderEngine"
---

<objective>
Harden the layout and paint consumers for the expanded practical CSS subset so supported `ComputedStyle` fields produce visible shell UI behavior and unsupported fields do not leak into renderer scope.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-practical-css-coverage/08-CONTEXT.md
@.planning/phases/08-practical-css-coverage/08-RESEARCH.md
@.planning/phases/08-practical-css-coverage/08-VALIDATION.md
@.planning/phases/08-practical-css-coverage/08-PATTERNS.md

@crates/core/ui/elements/src/layout.rs
@crates/core/ui/render/src/surface/painter.rs
@crates/core/ui/elements/src/style.rs
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| T-08-05: shorthand fields resolve but layout or paint ignores them | high | Add consumer-level tests for absolute inset, flex basis/grow, border, overflow clipping, and z-index order. |
| T-08-06: display/visibility behavior removes nodes from layout but still paints or hit-tests them | medium | Keep `display: none` zero-layout and no-paint behavior tested; map `visibility` only if supported by concrete behavior. |
| T-08-07: renderer gains fields with no consumer and creates false author promises | medium | Require every new non-metadata field to have layout or paint assertions. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add layout regression tests for practical CSS consumers</name>
  <files>crates/core/ui/elements/src/layout.rs</files>
  <read_first>crates/core/ui/elements/src/layout.rs, crates/core/ui/elements/src/style.rs, .planning/phases/08-practical-css-coverage/08-PATTERNS.md</read_first>
  <action>Add focused tests in `crates/core/ui/elements/src/layout.rs` that prove Phase 8 computed fields affect layout. Add exact tests named `absolute_position_uses_inset_edges`, `flex_triple_basis_affects_main_axis_size`, `display_none_excludes_node_from_layout`, and `overflow_container_preserves_natural_main_axis_size`. Use direct `WidgetNode` construction and `LayoutEngine::compute` or `compute_with_measurer`; do not involve parser or renderer crates. If existing tests already cover a behavior, extend them only enough to assert the exact Phase 8 field path.</action>
  <verify>nix develop -c cargo test -p mesh-core-elements layout</verify>
  <acceptance_criteria>
    - `crates/core/ui/elements/src/layout.rs` contains test `absolute_position_uses_inset_edges`.
    - `crates/core/ui/elements/src/layout.rs` contains test `flex_triple_basis_affects_main_axis_size`.
    - `crates/core/ui/elements/src/layout.rs` contains test `display_none_excludes_node_from_layout`.
    - `crates/core/ui/elements/src/layout.rs` contains test `overflow_container_preserves_natural_main_axis_size`.
    - `nix develop -c cargo test -p mesh-core-elements layout` exits 0.
  </acceptance_criteria>
  <done>Expanded computed layout fields have concrete layout proof.</done>
</task>

<task type="auto">
  <name>Task 2: Add paint regression tests for visual CSS consumers</name>
  <files>crates/core/ui/render/src/surface/painter.rs</files>
  <read_first>crates/core/ui/render/src/surface/painter.rs, crates/core/ui/elements/src/style.rs, .planning/phases/08-practical-css-coverage/08-RESEARCH.md</read_first>
  <action>Add or extend tests in `crates/core/ui/render/src/surface/painter.rs` for visible Phase 8 style consumers. Add exact tests named `painter_draws_border_from_computed_edges`, `painter_clips_children_when_overflow_hidden`, and `painter_orders_children_by_z_index`. Use `PixelBuffer` assertions with fixed node sizes and colors. Do not add transform/filter/box-shadow rendering. If render tests live in a sibling module, place the tests in the existing render test location but keep these exact test names.</action>
  <verify>nix develop -c cargo test -p mesh-core-render painter_</verify>
  <acceptance_criteria>
    - `crates/core/ui/render/src/surface/painter.rs` or an adjacent render test module contains test `painter_draws_border_from_computed_edges`.
    - `crates/core/ui/render/src/surface/painter.rs` or an adjacent render test module contains test `painter_clips_children_when_overflow_hidden`.
    - `crates/core/ui/render/src/surface/painter.rs` or an adjacent render test module contains test `painter_orders_children_by_z_index`.
    - `grep -n "transform\\|filter\\|box-shadow" crates/core/ui/render/src/surface/painter.rs` shows no new Phase 8 transform/filter/box-shadow implementation.
    - `nix develop -c cargo test -p mesh-core-render painter_` exits 0.
  </acceptance_criteria>
  <done>Expanded visual fields have concrete paint proof without adding out-of-scope browser effects.</done>
</task>

<task type="auto">
  <name>Task 3: Add parser-to-layout proof fixture for shell CSS subset</name>
  <files>crates/core/ui/elements/src/style.rs, crates/core/ui/elements/src/layout.rs</files>
  <read_first>crates/core/ui/elements/src/style.rs, crates/core/ui/elements/src/layout.rs, crates/core/ui/component/src/parser.rs, .planning/phases/08-practical-css-coverage/08-CONTEXT.md</read_first>
  <action>Add an integration-style unit test in the lowest practical crate that can parse a `.mesh` style snippet and resolve it into layout-facing computed fields. Use exact test name `shell_card_css_subset_resolves_for_layout`. The snippet must include `--pad: token(spacing.md)`, `padding: var(--pad)`, `margin: 4px 8px`, `border: 1px solid token(color.outline)`, `display: flex`, `flex-direction: column`, `gap: 6px`, `position: relative`, and `overflow: hidden`. Assert computed padding, margin, border width/color alpha, direction, gap, position, and overflow fields.</action>
  <verify>nix develop -c cargo test -p mesh-core-elements shell_card_css_subset_resolves_for_layout</verify>
  <acceptance_criteria>
    - A test named `shell_card_css_subset_resolves_for_layout` exists.
    - The test source contains `--pad: token(spacing.md)`.
    - The test source contains `border: 1px solid token(color.outline)`.
    - The test asserts `style.overflow_x` and `style.overflow_y`.
    - `nix develop -c cargo test -p mesh-core-elements shell_card_css_subset_resolves_for_layout` exits 0.
  </acceptance_criteria>
  <done>A representative shell CSS fixture proves parser and resolver output feeds layout-facing fields.</done>
</task>

</tasks>

<verification>
- Run `nix develop -c cargo test -p mesh-core-elements layout`.
- Run `nix develop -c cargo test -p mesh-core-render painter_`.
- Run `nix develop -c cargo test -p mesh-core-elements shell_card_css_subset_resolves_for_layout`.
- Read `crates/core/ui/elements/src/style.rs` and confirm any non-animation `ComputedStyle` additions have layout or paint consumers.
</verification>

<success_criteria>
- CSS-01 is covered by concrete layout/paint consumers for the practical shell subset.
- CSS-02 is covered by shorthand-resolved values being consumed by layout and paint.
- D-01, D-04, D-09, and D-13 are visible in `must_haves.truths` and enforced by tests.
</success_criteria>

<output>
After completion, create `.planning/phases/08-practical-css-coverage/08-03-SUMMARY.md`
</output>

