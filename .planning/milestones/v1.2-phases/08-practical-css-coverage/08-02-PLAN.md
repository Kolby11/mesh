---
phase: 08-practical-css-coverage
plan: 02
type: execute
wave: 2
depends_on: [08-01]
files_modified:
  - crates/core/ui/elements/src/style.rs
autonomous: true
requirements: [CSS-02, CSS-04]
requirements_addressed: [CSS-02, CSS-04]
user_setup: []
must_haves:
  truths:
    - "D-04: shorthand support lowers into existing ComputedStyle fields rather than preserving a browser CSS object model."
    - "D-05: Phase 8 prioritizes margin, padding, border, border-width, border-color, border-radius, overflow, flex, inset, font, transition, and animation declarations."
    - "D-06: complex browser shorthand behavior should be limited to a useful shell subset and unsupported forms documented instead of accepted surprisingly."
    - "D-07: existing token(...) authoring remains first-class across supported properties."
    - "D-08: CSS custom properties are supported enough for local component variables and var(...) resolution in supported declarations; full browser cascade inheritance is not required."
    - "D-09: theme-token and variable resolution happen before layout or paint consume computed style."
  artifacts:
    - path: "crates/core/ui/elements/src/style.rs"
      provides: "Shorthand expansion, token resolution, local CSS variable resolution, and computed style writes"
      contains: "apply_declaration"
---

<objective>
Expand shorthand and value resolution so common shell CSS lowers into concrete `ComputedStyle` fields, with `token(...)` and local `var(...)` resolution completed before layout or paint.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-practical-css-coverage/08-CONTEXT.md
@.planning/phases/08-practical-css-coverage/08-RESEARCH.md
@.planning/phases/08-practical-css-coverage/08-VALIDATION.md
@.planning/phases/08-practical-css-coverage/08-PATTERNS.md

@crates/core/ui/elements/src/style.rs
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| T-08-03: shorthand parsing accepts browser forms that MESH cannot render predictably | high | Implement explicit practical parsers with tests for accepted and ignored forms. |
| T-08-04: CSS variables resolve after layout/paint and produce empty or stale concrete fields | high | Resolve local variables inside `StyleResolver` before writing any `ComputedStyle` field. |
| T-08-05: token(...) support regresses while adding var(...) support | medium | Keep token tests and add token-through-variable tests. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Expand edge, corner, border, overflow, flex, inset, and font shorthands</name>
  <files>crates/core/ui/elements/src/style.rs</files>
  <read_first>crates/core/ui/elements/src/style.rs, .planning/phases/08-practical-css-coverage/08-RESEARCH.md, .planning/phases/08-practical-css-coverage/08-PATTERNS.md</read_first>
  <action>Update `apply_declaration` and helper parsers in `crates/core/ui/elements/src/style.rs` so practical shorthand forms lower into existing fields. Implement CSS 1/2/3/4-value expansion for `padding`, `margin`, `border-width`, `border-color`, `border-radius`, and `inset`. Implement `overflow: <x> <y>` so one value sets both axes and two values set `overflow_x` then `overflow_y`. Implement `border: 1px solid #ffffff` so width becomes all border widths and color becomes border color; recognize `border: none` as zero width and transparent color. Implement `flex: 1 0 auto` and `flex: 0 1 12px` in addition to existing `none`, `auto`, and bare number forms. Implement `font` shorthand support for `italic 600 16px/1.4 Inter` by extracting `font_style`, `font_weight`, `font_size`, `line_height`, and `font_family` when present. Do not add a browser CSS object model.</action>
  <verify>nix develop -c cargo test -p mesh-core-elements shorthand</verify>
  <acceptance_criteria>
    - `crates/core/ui/elements/src/style.rs` contains test `padding_margin_four_value_shorthands_expand_to_edges`.
    - `crates/core/ui/elements/src/style.rs` contains test `border_shorthand_sets_width_and_color`.
    - `crates/core/ui/elements/src/style.rs` contains test `overflow_two_value_shorthand_sets_axes`.
    - `crates/core/ui/elements/src/style.rs` contains test `flex_triple_shorthand_sets_grow_shrink_basis`.
    - `crates/core/ui/elements/src/style.rs` contains test `font_shorthand_sets_text_fields`.
    - `nix develop -c cargo test -p mesh-core-elements shorthand` exits 0.
  </acceptance_criteria>
  <done>Common shell CSS shorthands produce concrete computed fields.</done>
</task>

<task type="auto">
  <name>Task 2: Add local custom property and var() resolution</name>
  <files>crates/core/ui/elements/src/style.rs</files>
  <read_first>crates/core/ui/elements/src/style.rs, crates/core/ui/component/src/style.rs, .planning/phases/08-practical-css-coverage/08-CONTEXT.md</read_first>
  <action>Extend `StyleResolver` so declarations whose property starts with `--` are stored as local CSS custom properties during rule application and `StyleValue::Var("--name")` resolves to that stored value before a supported property writes to `ComputedStyle`. Keep the scope intentionally local to the node/rules being resolved; do not implement full browser inheritance. Support token values inside variables by resolving `--surface: token(color.surface)` before a declaration such as `background: var(--surface)`. If fallback syntax `var(--missing, #fff)` is not represented by the current parser, document the unsupported fallback in code comments and return a diagnostic instead of silently using an empty string.</action>
  <verify>nix develop -c cargo test -p mesh-core-elements css_variable</verify>
  <acceptance_criteria>
    - `crates/core/ui/elements/src/style.rs` no longer contains the exact string `var({name}) not yet resolved`.
    - `crates/core/ui/elements/src/style.rs` contains test `css_variable_resolves_local_literal_value`.
    - `crates/core/ui/elements/src/style.rs` contains test `css_variable_resolves_token_value_before_computed_style`.
    - `crates/core/ui/elements/src/style.rs` contains test `missing_css_variable_produces_style_diagnostic`.
    - `nix develop -c cargo test -p mesh-core-elements css_variable` exits 0.
  </acceptance_criteria>
  <done>Supported declarations can consume local CSS variables and theme tokens predictably.</done>
</task>

<task type="auto">
  <name>Task 3: Preserve existing token and state selector behavior after resolver changes</name>
  <files>crates/core/ui/elements/src/style.rs</files>
  <read_first>crates/core/ui/elements/src/style.rs, .planning/phases/08-practical-css-coverage/08-VALIDATION.md</read_first>
  <action>Add regression coverage that existing `token(...)`, pseudo-state matching, and container query matching still work after custom property and shorthand changes. Keep the existing `resolve_node_style` public method behavior unchanged for callers that do not need diagnostics. Add exact tests named `token_resolution_still_works_after_variable_support`, `pseudo_state_rules_still_apply_after_variable_support`, and `container_query_rules_still_apply_after_variable_support`.</action>
  <verify>nix develop -c cargo test -p mesh-core-elements variable_support</verify>
  <acceptance_criteria>
    - `crates/core/ui/elements/src/style.rs` contains test `token_resolution_still_works_after_variable_support`.
    - `crates/core/ui/elements/src/style.rs` contains test `pseudo_state_rules_still_apply_after_variable_support`.
    - `crates/core/ui/elements/src/style.rs` contains test `container_query_rules_still_apply_after_variable_support`.
    - Existing test `resolve_theme_token` still passes.
    - `nix develop -c cargo test -p mesh-core-elements variable_support` exits 0.
  </acceptance_criteria>
  <done>New value resolution does not regress existing token, state selector, or container query behavior.</done>
</task>

</tasks>

<verification>
- Run `nix develop -c cargo test -p mesh-core-elements shorthand`.
- Run `nix develop -c cargo test -p mesh-core-elements css_variable`.
- Run `nix develop -c cargo test -p mesh-core-elements variable_support`.
- Read `crates/core/ui/elements/src/style.rs` and confirm shorthand helpers write existing `ComputedStyle` fields rather than adding a browser CSS object model.
</verification>

<success_criteria>
- CSS-02 is covered by common longhand and shorthand resolution into `ComputedStyle`.
- CSS-04 is covered by `token(...)` and local `var(...)` resolution in supported declarations.
- D-04, D-05, D-06, D-07, D-08, and D-09 appear in `must_haves.truths` and are enforced by tests.
</success_criteria>

<output>
After completion, create `.planning/phases/08-practical-css-coverage/08-02-SUMMARY.md`
</output>

