---
phase: 03-frontend-reactivity-and-events
plan: 02
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - crates/core/shell/src/shell/layout.rs
  - crates/core/shell/src/shell/component.rs
  - crates/core/foundation/diagnostics/src/lib.rs
  - crates/core/ui/render/src/render.rs
autonomous: true
requirements:
  - FRONT-03
  - FRONT-04
  - FRONT-05
user_setup: []
must_haves:
  truths:
    - "D-04: `on_change` fires continuously on every slider drag event with no built-in throttle."
    - "D-05: `on_change` passes a typed first argument: slider number, toggle/checkbox boolean, text input string."
    - "D-06: existing bespoke audio slider handling in `component.rs` remains alongside generic `on_change`."
    - "D-07 D-08: `on_click`, `on_change`, `on_release`, and `on_focus` are standardized across interactive elements, with layout elements supporting `on_click` only."
    - "D-09 D-10 D-11: handler errors are logged, visible in diagnostics, deduplicated, and do not interrupt rendering."
  artifacts:
    - path: "crates/core/shell/src/shell/layout.rs"
      provides: "Generic event handler lookup by normalized event name"
      contains: "find_click_handler"
    - path: "crates/core/shell/src/shell/component.rs"
      provides: "Input event routing, typed handler arguments, and handler failure handling"
      contains: "handle_input"
    - path: "crates/core/foundation/diagnostics/src/lib.rs"
      provides: "Visible diagnostics collector for handler failures"
      contains: "DiagnosticsCollector"
    - path: "crates/core/ui/render/src/render.rs"
      provides: "Normalized handler names and render tests for event attributes"
      contains: "normalize_event_handler_name"
  key_links:
    - from: "crates/core/ui/render/src/render.rs"
      to: "crates/core/shell/src/shell/layout.rs"
      via: "normalized `onchange` / `onrelease` / `onfocus` attributes become generic handler lookup keys"
      pattern: "event_handlers"
    - from: "crates/core/shell/src/shell/component.rs"
      to: "crates/core/foundation/diagnostics/src/lib.rs"
      via: "handler errors are converted into deduplicated visible diagnostics"
      pattern: "ComponentError::Script"
---

<objective>
Add standardized shell event dispatch for click, change, release, and focus handlers, including typed event arguments and visible non-fatal handler diagnostics.

Purpose: Frontend authors need element events to fire predictably with current state and clear failures, while existing shell-owned slider behavior keeps working.
Output: Generic event lookup/routing, typed arguments for interactive controls, diagnostics for handler errors, and event dispatch tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-frontend-reactivity-and-events/03-CONTEXT.md
@.planning/phases/03-frontend-reactivity-and-events/03-RESEARCH.md
@.planning/phases/03-frontend-reactivity-and-events/03-PATTERNS.md
@.planning/phases/03-frontend-reactivity-and-events/03-UI-SPEC.md
@.planning/phases/03-frontend-reactivity-and-events/03-VALIDATION.md
@.planning/phases/03-frontend-reactivity-and-events/03-01-SUMMARY.md

@crates/core/shell/src/shell/layout.rs
@crates/core/shell/src/shell/component.rs
@crates/core/foundation/diagnostics/src/lib.rs
@crates/core/ui/render/src/render.rs
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| Generic event routing breaks existing `onclick` behavior and existing surface controls | high | Generalize `find_click_handler()` to `find_event_handler(...)` while keeping `find_click_handler()` as a wrapper or updating call sites with click regression tests. |
| Slider drag fires duplicate bespoke audio commands and generic `on_change` calls with inconsistent values | medium | Keep `mesh-action="audio-volume"` code path intact, but route generic `change` handlers with the same computed slider value and test both paths without replacing the bespoke path. |
| Handler exceptions crash the surface or leave failures invisible | high | Catch `ComponentError::Script` inside `call_namespaced_handler(...)`, emit `tracing::warn!`, record a diagnostics entry, return `Ok(Vec::new())`, and keep `last_tree` unchanged. |
| Continuous slider failures flood diagnostics | medium | Deduplicate diagnostics by component id, handler name, and error message. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Generalize event handler lookup while preserving click behavior</name>
  <files>crates/core/shell/src/shell/layout.rs, crates/core/ui/render/src/render.rs</files>
  <read_first>crates/core/shell/src/shell/layout.rs, crates/core/ui/render/src/render.rs, crates/core/ui/component/src/parser/markup.rs, .planning/phases/03-frontend-reactivity-and-events/03-PATTERNS.md</read_first>
  <action>In `crates/core/shell/src/shell/layout.rs`, add `find_event_handler(tree: &WidgetNode, key: &str, event_name: &str) -> Option<String>` that returns `node.event_handlers.get(event_name).cloned()`. Keep `find_click_handler(...)` as a wrapper around `find_event_handler(..., "click")` or update all call sites to use the generic function. In `crates/core/ui/render/src/render.rs`, add or extend tests proving `onclick`, `onchange`, `onrelease`, and `onfocus` normalize to `click`, `change`, `release`, and `focus` in `WidgetNode.event_handlers`.</action>
  <verify>cargo test -p mesh-core-render && cargo test -p mesh-core-shell</verify>
  <acceptance_criteria>
    - `crates/core/shell/src/shell/layout.rs` contains `find_event_handler`.
    - Existing click routing still resolves handlers stored under the normalized `click` key.
    - Render tests assert normalized event handler keys include `change`, `release`, and `focus`.
  </acceptance_criteria>
  <done>Event lookup supports the Phase 3 event set without regressing `on_click`.</done>
</task>

<task type="auto">
  <name>Task 2: Route typed change, release, and focus handlers for interactive elements</name>
  <files>crates/core/shell/src/shell/component.rs, crates/core/shell/src/shell/layout.rs</files>
  <read_first>crates/core/shell/src/shell/component.rs, crates/core/shell/src/shell/layout.rs, crates/core/ui/elements/src/element.rs, .planning/phases/03-frontend-reactivity-and-events/03-CONTEXT.md</read_first>
  <action>Update `FrontendSurfaceComponent::handle_input(...)` so `button`, `input`, `slider`, `switch`, and `checkbox` can route `change`, `release`, and `focus` handlers where applicable. For sliders, reuse `update_slider_from_position(...)` to compute the numeric value and call `on_change(value)` continuously on pointer down and pointer move while dragging; call `on_release(value)` once on pointer release if declared. For text inputs, call `on_change(value)` after character insertion and Backspace with the current string. For switch/checkbox controls, toggle a stored boolean value on click or key activation and call `on_change(value)` with a boolean. When focus changes to a focusable node, call `on_focus()` or `on_focus(event)` according to the existing handler argument convention chosen in code; the accepted contract is that focus fires when the node becomes focused. Preserve non-interactive layout elements as `on_click` only.</action>
  <verify>cargo test -p mesh-core-shell</verify>
  <acceptance_criteria>
    - `crates/core/shell/src/shell/component.rs` calls `find_event_handler(..., "change")`, `find_event_handler(..., "release")`, and `find_event_handler(..., "focus")` or equivalent.
    - A shell test proves slider `on_change(value)` receives a JSON/Lua number and fires on pointer move while dragging.
    - A shell test proves input `on_change(value)` receives the current string after text entry.
    - A shell test proves switch or checkbox `on_change(value)` receives a boolean.
    - A shell test proves `on_release` fires once on slider pointer release.
    - A shell test proves click behavior still passes the existing click event object with `current_target.position`.
  </acceptance_criteria>
  <done>FRONT-03 and FRONT-04 handlers run with current state and typed arguments.</done>
</task>

<task type="auto">
  <name>Task 3: Report handler failures through logs and deduplicated diagnostics without crashing render</name>
  <files>crates/core/shell/src/shell/component.rs, crates/core/foundation/diagnostics/src/lib.rs, crates/core/shell/src/shell/types.rs</files>
  <read_first>crates/core/shell/src/shell/component.rs, crates/core/foundation/diagnostics/src/lib.rs, crates/core/shell/src/shell/types.rs, .planning/phases/03-frontend-reactivity-and-events/03-CONTEXT.md</read_first>
  <action>Add a handler-error reporting path in `crates/core/shell/src/shell/component.rs` around `call_namespaced_handler(...)`: when `runtime.script_ctx.call_handler(&handler_name, args)` returns an error, build a message containing the component id, handler name, and script error string; emit it with `tracing::warn!`; record it in `DiagnosticsCollector` or the existing shell diagnostics handle as a visible error; deduplicate repeated identical handler errors by `(component_id, handler_name, error_message)` so slider drags do not flood diagnostics. Return `Ok(Vec::new())` after reporting the diagnostic so the component keeps the last successfully rendered tree instead of propagating a fatal `ComponentError::Script`. Add tests proving duplicate identical failures produce one diagnostics entry and that render state remains available after a handler failure.</action>
  <verify>cargo test -p mesh-core-shell && cargo test -p mesh-core-diagnostics</verify>
  <acceptance_criteria>
    - `crates/core/shell/src/shell/component.rs` contains a `tracing::warn!` path for handler failure reporting that includes handler name and component id.
    - `crates/core/foundation/diagnostics/src/lib.rs` or existing diagnostics plumbing supports recording handler errors visibly.
    - A test proves two identical failing handler invocations produce one diagnostics entry or one error-count increment under the chosen dedupe policy.
    - A test proves `call_namespaced_handler(...)` does not return a fatal component error for script handler failure after reporting diagnostics.
  </acceptance_criteria>
  <done>FRONT-05 failures become visible diagnostics without interrupting rendering.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cargo test -p mesh-core-render` passes.
- [ ] `cargo test -p mesh-core-shell` passes.
- [ ] `cargo test -p mesh-core-diagnostics` passes if diagnostics crate tests are added or changed.
- [ ] `rg -n "find_event_handler|\"change\"|\"release\"|\"focus\"|tracing::warn!|DiagnosticsCollector" crates/core/shell/src/shell crates/core/foundation/diagnostics/src/lib.rs crates/core/ui/render/src/render.rs` shows the new dispatch and diagnostics paths.
- [ ] `rg -n "on_change|on_release|on_focus|handler failure|dedup" crates/core/shell/src/shell/component.rs` shows targeted tests or assertions.
</verification>

<success_criteria>
- `on_click` keeps existing payload behavior.
- `on_change` receives typed number/string/boolean values for supported interactive controls.
- `on_release` and `on_focus` route through the same handler dispatch system.
- Handler failures are visible through logs and diagnostics and do not crash rendering.
- Existing bespoke audio slider behavior remains available.
</success_criteria>

<output>
After completion, create `.planning/phases/03-frontend-reactivity-and-events/03-02-SUMMARY.md`
</output>
