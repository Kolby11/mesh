---
phase: 05-icon-rendering-reliability
plan: 02
type: tdd
wave: 1
depends_on:
  - 05-01
files_modified:
  - crates/core/ui/render/src/surface/icon.rs
  - crates/core/ui/render/src/surface/painter.rs
  - crates/core/ui/icon/src/lib.rs
autonomous: true
requirements: [ICON-02, ICON-03, ICON-04]
requirements_addressed: [ICON-02, ICON-03, ICON-04]
user_setup: []
must_haves:
  truths:
    - "D-09: unresolved icons render a neutral placeholder glyph, emit diagnostics through Plan 03, and keep layout stable."
    - "D-11: fallback rendering uses a tiny built-in vector path owned by core, never another icon lookup."
    - "D-13: monochrome icons inherit surrounding color by default, like text."
    - "D-14: multicolor assets can opt out of inherited tinting and preserve original colors."
    - "D-15: CSS/layout box width and height control rendered size; `size` is only a lookup hint."
    - "D-16: Phase 5 supports practical monochrome SVG rasterization and alpha-mask tinting; full complex SVG fidelity is out of scope."
    - "D-17: raster icons use alpha-mask tint unless marked multicolor."
    - "D-19: proof coverage includes SVG rendering, raster rendering, and missing-icon fallback."
  artifacts:
    - path: "crates/core/ui/render/src/surface/icon.rs"
      provides: "SVG/raster/multicolor/fallback pixel drawing tests and implementation"
      contains: "draw_missing_icon_fallback"
    - path: "crates/core/ui/render/src/surface/painter.rs"
      provides: "Layout-box icon paint integration"
      contains: "render_icon_node"
  key_links:
    - from: "crates/core/ui/icon/src/lib.rs"
      to: "crates/core/ui/render/src/surface/icon.rs"
      via: "IconResolution carries found/missing/multicolor data into pixel painting"
      pattern: "IconResolution"
---

<objective>
Make the render pipeline paint resolved SVG icons, raster icons, multicolor assets, and built-in fallback icons correctly at layout dimensions.

Purpose: Phase 5 requires icon reliability at the pixel layer after semantic resolution succeeds or misses.
Output: `mesh-core-render` has automated proof for SVG tinting, raster tinting/resizing, multicolor preservation, and layout-stable missing fallback drawing.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-icon-rendering-reliability/05-CONTEXT.md
@.planning/phases/05-icon-rendering-reliability/05-RESEARCH.md
@.planning/phases/05-icon-rendering-reliability/05-UI-SPEC.md
@.planning/phases/05-icon-rendering-reliability/05-VALIDATION.md
@.planning/phases/05-icon-rendering-reliability/05-PATTERNS.md
@.planning/phases/05-icon-rendering-reliability/05-01-PLAN.md

@crates/core/ui/render/src/surface/icon.rs
@crates/core/ui/render/src/surface/painter.rs
@crates/core/ui/icon/src/lib.rs
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| T-05-02-01: SVG decode failure panics or leaves undefined pixels | high | Keep SVG parsing fallible and route misses/decode failures to a non-crashing fallback path. |
| T-05-02-02: Raster decode failure panics or breaks layout | high | Keep raster decode fallible, paint fallback into the same box, and never resize layout based on asset dimensions. |
| T-05-02-03: Multicolor logos/flags are incorrectly tinted | medium | Carry `multicolor` metadata into draw functions and preserve original RGBA when true. |
| T-05-02-04: Missing fallback depends on configured icon packs | high | Draw fallback using `PixelBuffer` primitives in `icon.rs`; do not call `resolve_icon()` for fallback. |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add render tests for SVG, raster, multicolor, and fallback pixels</name>
  <files>crates/core/ui/render/src/surface/icon.rs</files>
  <read_first>crates/core/ui/render/src/surface/icon.rs, crates/core/ui/render/src/surface/painter.rs, .planning/phases/05-icon-rendering-reliability/05-VALIDATION.md</read_first>
  <action>Add tests in `crates/core/ui/render/src/surface/icon.rs` named `svg_icon_rasterizes_and_tints`, `raster_icon_decodes_resizes_and_tints`, `multicolor_raster_preserves_source_colors`, and `missing_icon_paints_builtin_fallback`. Use `tempfile` fixtures: a minimal monochrome SVG with non-transparent pixels, a tiny PNG with alpha, and a red/green PNG for multicolor. Each test must create a `PixelBuffer`, draw into a destination box whose dimensions differ from the source asset, and assert non-transparent pixels exist inside the destination box. The SVG/raster tint tests must assert painted RGB equals the requested tint for non-transparent pixels. The multicolor test must assert at least one red and one green source color survive. The fallback test must assert drawing the built-in fallback into a 18x18 box produces non-transparent pixels and no pixels outside the box.</action>
  <verify>nix develop -c cargo test -p mesh-core-render icon</verify>
  <acceptance_criteria>
    - The four named tests exist in `crates/core/ui/render/src/surface/icon.rs`.
    - SVG and raster tint tests assert inherited tint RGB, not only non-empty output.
    - Multicolor test asserts original red and green pixels survive.
    - Fallback test asserts the destination box stays bounded.
  </acceptance_criteria>
  <done>Render fidelity expectations are locked by pixel-level tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement multicolor-aware draw functions and fallback drawing</name>
  <files>crates/core/ui/render/src/surface/icon.rs, crates/core/ui/icon/src/lib.rs</files>
  <read_first>crates/core/ui/render/src/surface/icon.rs, crates/core/ui/icon/src/lib.rs, .planning/phases/05-icon-rendering-reliability/05-PATTERNS.md</read_first>
  <action>Update `draw_icon_from_path(...)` or add a new overload so callers can pass `multicolor: bool`. When `multicolor == false`, keep the existing alpha-mask tint contract: output RGB is the inherited tint and output alpha is the source alpha. When `multicolor == true`, preserve source RGB and alpha while blending into the buffer. Implement `draw_missing_icon_fallback(buffer, dest_x, dest_y, dest_w, dest_h, tint)` using only `PixelBuffer::blend_pixel`, `fill_rect`, or local primitives; draw a neutral outline/question-mark or warning-style glyph that fits inside `dest_w`/`dest_h` and uses the inherited tint alpha. Keep decode and SVG parse failures non-panicking.</action>
  <verify>nix develop -c cargo test -p mesh-core-render icon</verify>
  <acceptance_criteria>
    - `crates/core/ui/render/src/surface/icon.rs` contains `draw_missing_icon_fallback`.
    - The draw path has an explicit `multicolor` boolean or equivalent typed metadata.
    - Decode/parsing errors return without panic and can route to fallback when the caller has a missing result.
    - Targeted `mesh-core-render` icon tests exit 0.
  </acceptance_criteria>
  <done>Render code can paint monochrome, multicolor, and missing fallback icons correctly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Connect IconResolution to named icon drawing</name>
  <files>crates/core/ui/render/src/surface/icon.rs, crates/core/ui/render/src/surface/painter.rs, crates/core/ui/icon/src/lib.rs</files>
  <read_first>crates/core/ui/render/src/surface/icon.rs, crates/core/ui/render/src/surface/painter.rs, crates/core/ui/icon/src/lib.rs, .planning/phases/05-icon-rendering-reliability/05-01-PLAN.md</read_first>
  <action>Update `draw_named_icon(...)` so it consumes `mesh_core_icon::IconResolution` from Plan 01 instead of only `Option<PathBuf>`. For `Found { path, multicolor, .. }`, draw the path with the multicolor-aware path function. For `Missing { .. }`, draw the built-in fallback into exactly `dest_x`, `dest_y`, `dest_w`, `dest_h`. In `render_icon_node()`, preserve the current behavior where `w`/`h` are computed from layout and `size` comes from the `size` attribute or `w.max(h)`. Do not use `size` as the painted dimensions. Add or update a test that renders a `WidgetNode` icon with layout `30x28` and `size="18"` and asserts painted pixels are bounded by `30x28`, not forced to `18x18`.</action>
  <verify>nix develop -c cargo test -p mesh-core-render icon</verify>
  <acceptance_criteria>
    - `draw_named_icon` handles `IconResolution::Found` and `IconResolution::Missing`.
    - Missing named icons produce fallback pixels instead of returning a transparent box.
    - A layout-size test proves `size="18"` does not force an 18x18 paint box when layout is `30x28`.
    - `nix develop -c cargo test -p mesh-core-render icon` exits 0.
  </acceptance_criteria>
  <done>Named-icon drawing uses semantic resolution results and preserves the layout size contract.</done>
</task>

</tasks>

<verification>
- Run `nix develop -c cargo test -p mesh-core-render icon`.
- Run `nix develop -c cargo test -p mesh-core-icon -p mesh-core-render`.
- Run `grep -R "draw_missing_icon_fallback\\|multicolor\\|IconResolution::Missing\\|size = node" -n crates/core/ui/render/src/surface crates/core/ui/icon/src`.
</verification>

<success_criteria>
- ICON-02 SVG icons rasterize and tint correctly.
- ICON-03 raster icons decode, resize to layout dimensions, and tint correctly.
- ICON-04 missing icons paint a non-crashing built-in fallback.
- Multicolor assets can opt out of inherited tinting.
</success_criteria>

<output>
After completion, create `.planning/phases/05-icon-rendering-reliability/05-02-SUMMARY.md`
</output>
