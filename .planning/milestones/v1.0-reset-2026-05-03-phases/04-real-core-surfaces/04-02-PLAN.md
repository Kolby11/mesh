---
phase: 04-real-core-surfaces
plan: 02
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - packages/plugins/frontend/core/quick-settings/src/main.mesh
  - packages/plugins/frontend/core/quick-settings/src/components/audio-section.mesh
  - packages/plugins/frontend/core/quick-settings/src/components/wifi-section.mesh
  - packages/plugins/frontend/core/quick-settings/src/components/wifi-item.mesh
  - crates/core/shell/src/shell/component.rs
autonomous: true
requirements:
  - SURF-02
  - SURF-03
  - SURF-04
  - SURF-05
user_setup: []
must_haves:
  truths:
    - "D-04 D-05 D-06 D-07: quick settings audio provides live percent, mute, backend/source label, slider via `audio.set_volume(...)`, and mute/step controls through proxy command methods."
    - "D-08 D-09 D-10 D-11: quick settings network implements core Wi-Fi controls, including live enabled state, provider-emitted network list, Wi-Fi toggle, and guarded connect/disconnect only when safe."
    - "D-12 D-13 D-14: unavailable and permission-denied states are visible disabled UI states with concise copy, while technical details remain in diagnostics/logs."
    - "D-15 D-16: shipped surfaces do not reintroduce service callbacks and do not mutate proxy state fields directly."
    - "UI-SPEC: no raw hex colors, no new component library, short operational copy, stable control dimensions, token-only styles."
  artifacts:
    - path: "packages/plugins/frontend/core/quick-settings/src/components/audio-section.mesh"
      provides: "Audio control surface"
      contains: "onVolumeChange"
    - path: "packages/plugins/frontend/core/quick-settings/src/components/wifi-section.mesh"
      provides: "Wi-Fi state and network list surface"
      contains: "wifi_networks"
    - path: "packages/plugins/frontend/core/quick-settings/src/components/wifi-item.mesh"
      provides: "Guarded Wi-Fi row command behavior"
      contains: "onConnectWiFi"
    - path: "crates/core/shell/src/shell/component.rs"
      provides: "Bundled quick-settings integration coverage"
      contains: "quick"
  key_links:
    - from: "audio-section.mesh"
      to: "pipewire-audio/src/main.luau"
      via: "`audio.set_volume` -> `on_command_set_volume` -> emitted `audio.percent` rerender"
      pattern: "set_volume"
    - from: "wifi-section.mesh"
      to: "networkmanager-network/src/main.luau"
      via: "`network.set_wifi_enabled` and `network.networks`"
      pattern: "wifi_enabled"
---

<objective>
Make quick settings the primary real audio and Wi-Fi control surface, with visible unavailable/permission-denied states.

Purpose: Phase 4's user-facing value lives in quick settings. It must render live provider data, mutate audio/Wi-Fi through proxy commands, and degrade honestly when providers are absent or controls are denied.
Output: Updated quick-settings root, audio section, Wi-Fi section, Wi-Fi rows, and shell-facing tests for state/render/command behavior.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-real-core-surfaces/04-CONTEXT.md
@.planning/phases/04-real-core-surfaces/04-RESEARCH.md
@.planning/phases/04-real-core-surfaces/04-UI-SPEC.md
@.planning/phases/04-real-core-surfaces/04-VALIDATION.md
@.planning/phases/04-real-core-surfaces/04-PATTERNS.md
@.planning/phases/04-real-core-surfaces/04-01-SUMMARY.md

@packages/plugins/frontend/core/quick-settings/src/main.mesh
@packages/plugins/frontend/core/quick-settings/src/components/audio-section.mesh
@packages/plugins/frontend/core/quick-settings/src/components/wifi-section.mesh
@packages/plugins/frontend/core/quick-settings/src/components/wifi-item.mesh
@crates/core/shell/src/shell/component.rs
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| T-04-04: Quick settings silently hides unavailable providers | medium | Add visible copy `Audio unavailable`, `Network unavailable`, `Audio controls unavailable`, or `Network controls unavailable`. |
| T-04-05: Controls appear active when service commands cannot work | medium | Guard handlers and add disabled styling/state variables when `pcall(require)` fails, `available == false`, or permission-denied state is emitted. |
| T-04-06: Wi-Fi connect calls use unsafe identifiers | medium | Only call `network.connect(id)` when `id` is non-empty and provider data is sufficient; otherwise show `Connection details unavailable` or keep rows display-only. |
| T-04-07: UI changes violate shell design contract | low | Use token-only styles and stable dimensions from `04-UI-SPEC.md`; avoid new libraries and raw colors. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Upgrade quick-settings audio controls</name>
  <files>packages/plugins/frontend/core/quick-settings/src/components/audio-section.mesh</files>
  <read_first>packages/plugins/frontend/core/quick-settings/src/components/audio-section.mesh, packages/plugins/frontend/core/navigation-bar/src/components/volume-button.mesh, packages/plugins/backend/core/audio-interface/interface.toml, .planning/phases/04-real-core-surfaces/04-UI-SPEC.md, .planning/phases/04-real-core-surfaces/04-01-SUMMARY.md</read_first>
  <action>Update `audio-section.mesh` so it derives `audio_available`, `audio_controls_available`, `audio_label`, `audio_tooltip`, `audio_backend`, `audio_percent`, and `icon_name` from live proxy fields on every `onRender()`. Add `onchange={onVolumeChange}` to the volume slider. Add `function onVolumeChange(value)` that clamps `value` to `0..100`, updates local display state, and calls the finalized proxy command path with the normalized volume payload supported by Plan 01: `audio.set_volume("default", normalized)` or `audio:set_volume("default", normalized)` according to the project's working proxy call style. Preserve `onVolumeDown()`, `onVolumeUp()`, and `onToggleMute()` but guard each with `audio_ok`, `audio`, `audio.available ~= false`, and a local controls-available check. Use the UI-SPEC copy exactly: `Audio unavailable` and `Audio controls unavailable` for disabled states. Use token-only styles; do not add raw hex colors.</action>
  <verify>rg -n "onVolumeChange|onchange=\\{onVolumeChange\\}|audio\\.set_volume|audio:set_volume|Audio unavailable|Audio controls unavailable|audio_available" packages/plugins/frontend/core/quick-settings/src/components/audio-section.mesh</verify>
  <acceptance_criteria>
    - `audio-section.mesh` contains `function onVolumeChange(value)`.
    - The volume slider contains `onchange={onVolumeChange}` or the parser-supported equivalent.
    - `audio-section.mesh` calls `set_volume`.
    - `audio-section.mesh` contains `Audio unavailable`.
    - `audio-section.mesh` contains `Audio controls unavailable`.
    - `audio-section.mesh` contains no raw hex colors.
  </acceptance_criteria>
  <done>Quick settings audio renders live state and controls audio through proxy commands.</done>
</task>

<task type="auto">
  <name>Task 2: Upgrade quick-settings Wi-Fi state, list, and guarded connect behavior</name>
  <files>packages/plugins/frontend/core/quick-settings/src/main.mesh, packages/plugins/frontend/core/quick-settings/src/components/wifi-section.mesh, packages/plugins/frontend/core/quick-settings/src/components/wifi-item.mesh</files>
  <read_first>packages/plugins/frontend/core/quick-settings/src/main.mesh, packages/plugins/frontend/core/quick-settings/src/components/wifi-section.mesh, packages/plugins/frontend/core/quick-settings/src/components/wifi-item.mesh, packages/plugins/backend/core/network-interface/interface.toml, packages/plugins/backend/core/networkmanager-network/src/main.luau, .planning/phases/04-real-core-surfaces/04-UI-SPEC.md</read_first>
  <action>Update quick-settings Wi-Fi behavior so the root and Wi-Fi section expose visible service state. In `main.mesh`, keep `onToggleWiFi()` routed through `network.set_wifi_enabled(not enabled)` but guard unavailable providers and permission-denied state. In `wifi-section.mesh`, derive `network_available`, `wifi_enabled`, and `wifi_networks` from `network.available`, `network.wifi_enabled`, and `network.networks`. Render the UI-SPEC copy exactly: `Network unavailable`, `Network controls unavailable`, `Wi-Fi is disabled`, and `Scanning for networks` as applicable. In `wifi-item.mesh`, only call `network_iface.connect(network_id)` when `network_id` is non-empty and the service is available; otherwise show or log `Connection details unavailable` without faking success. Preserve full-width row layout and token-only styles.</action>
  <verify>rg -n "Network unavailable|Network controls unavailable|Wi-Fi is disabled|Scanning for networks|Connection details unavailable|set_wifi_enabled|network\\.connect" packages/plugins/frontend/core/quick-settings/src/main.mesh packages/plugins/frontend/core/quick-settings/src/components/wifi-section.mesh packages/plugins/frontend/core/quick-settings/src/components/wifi-item.mesh</verify>
  <acceptance_criteria>
    - Quick settings files contain `Network unavailable`.
    - Quick settings files contain `Network controls unavailable`.
    - `wifi-section.mesh` contains `Wi-Fi is disabled`.
    - `wifi-section.mesh` contains `Scanning for networks`.
    - `wifi-item.mesh` contains `Connection details unavailable`.
    - `main.mesh` still calls `network.set_wifi_enabled`.
    - No quick-settings `.mesh` file introduces raw hex colors.
  </acceptance_criteria>
  <done>Quick settings Wi-Fi shows live state, safe network rows, and visible disabled states.</done>
</task>

<task type="auto">
  <name>Task 3: Add quick-settings shell integration tests</name>
  <files>crates/core/shell/src/shell/component.rs</files>
  <read_first>crates/core/shell/src/shell/component.rs, crates/core/runtime/scripting/src/context.rs, crates/core/shell/src/shell/service.rs, packages/plugins/frontend/core/quick-settings/src/components/audio-section.mesh, packages/plugins/frontend/core/quick-settings/src/components/wifi-section.mesh, packages/plugins/frontend/core/quick-settings/src/components/wifi-item.mesh</read_first>
  <action>Add or extend tests in `crates/core/shell/src/shell/component.rs` for bundled quick-settings behavior. Cover: audio render state from seeded payload `{ available = true, percent = 42, muted = false, source_plugin = "@mesh/pipewire-audio" }`; audio slider handler publishing `CoreRequest::ServiceCommand { interface: "mesh.audio", command: "set_volume", ... }`; network toggle publishing `CoreRequest::ServiceCommand { interface: "mesh.network", command: "set_wifi_enabled", ... }`; missing audio/network service fallback copy; and guarded Wi-Fi row behavior when `network_id == ""`. Reuse existing helper patterns rather than adding a new test framework.</action>
  <verify>cargo test -p mesh-core-shell quick_settings</verify>
  <acceptance_criteria>
    - `component.rs` contains a quick-settings audio render test asserting `42%` or equivalent state.
    - `component.rs` contains a quick-settings audio slider command test asserting interface `mesh.audio` and command `set_volume`.
    - `component.rs` contains a quick-settings network toggle test asserting interface `mesh.network` and command `set_wifi_enabled`.
    - `component.rs` contains a fallback-copy test for missing audio or network service.
    - `cargo test -p mesh-core-shell quick_settings` exits 0.
  </acceptance_criteria>
  <done>Quick-settings behavior is covered by shell-facing automated tests.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cargo test -p mesh-core-shell quick_settings` passes.
- [ ] `rg -n "onVolumeChange|set_volume|Audio unavailable|Audio controls unavailable" packages/plugins/frontend/core/quick-settings/src/components/audio-section.mesh` shows audio contract.
- [ ] `rg -n "Network unavailable|Network controls unavailable|Connection details unavailable|set_wifi_enabled" packages/plugins/frontend/core/quick-settings/src` shows Wi-Fi contract.
- [ ] `rg -n "mesh\\.service\\.bind|mesh\\.service\\.on|\\.on_change\\(" packages/plugins/frontend/core/quick-settings/src` returns no callback regression.
</verification>

<success_criteria>
- Quick settings renders live audio and network state.
- Quick settings changes audio volume/mute through service proxy commands.
- Quick settings toggles/commands Wi-Fi through service proxy commands where supported.
- Unavailable and permission-denied states are visible and guarded.
- UI-SPEC token, copy, spacing, color, and registry constraints are respected.
</success_criteria>

<output>
After completion, create `.planning/phases/04-real-core-surfaces/04-02-SUMMARY.md`
</output>
