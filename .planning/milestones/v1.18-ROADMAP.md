# Roadmap Archive: v1.18 Performance: Smart Invalidation

**Archived:** 2026-06-09
**Status:** Complete ✅
**Milestone Goal:** Replace coarse "tree rebuild + full repaint" invalidation with typed dependency tracking so interaction state, service events, and script state changes only dirty the affected nodes and paint slots.

---

## Phase 96: Selector Dependency Tracking ✅

**Goal:** Interaction state changes (`:hover`, `:focus`, `:active`) restyle only affected nodes instead of rebuilding the entire widget tree.
**Completed:** 2026-06-07

**Plans (3/3):**
- [x] 96-01: Add `state_to_rules` reverse index and `rules_for_state_bit()` to `StyleRuleIndex`
- [x] 96-02: Replace `state_hash` with `ElementState` in `RetainedNodeSnapshot`, compute `changed_state_bits`
- [x] 96-03: Fix targeted restyle inheritance and wire `finalize_tree` for narrow interaction restyle

**Requirements satisfied:** SEL-01, SEL-02, SEL-03, SEL-04, SEL-05

---

## Phase 97: Service Field Dependency Tracking ✅

**Goal:** The renderer captures per-node service field reads during expression evaluation to build a bidirectional dependency index without measurable render-time overhead.
**Completed:** 2026-06-09

**Plans (3/3):**
- [x] 97-01: Add service_field_reads to WidgetNode + TrackingVariableStore in compiler
- [x] 97-02: NodeServiceFieldDependencies bidirectional index + wire into FrontendSurfaceComponent
- [x] 97-03: SRV-03 overhead benchmark and correctness smoke tests

**Requirements satisfied:** SRV-01, SRV-02, SRV-03

---

## Phase 98: Narrow Invalidation & Event Routing ✅

**Goal:** Script state changes and service events mark only the nodes that actually depend on the changed data, with a correctness-preserving fallback threshold and pixel-equivalent output across all benchmark scenarios.
**Completed:** 2026-06-09

**Plans (3/3):**
- [x] 98-01: SCRIPT_NARROW dirty flag + narrow_script_update() with tree diff, ancestor expansion, structural fallback, >50% threshold guard, paint() routing
- [x] 98-02: handle_service_event() JSON field-level diff + NodeServiceFieldDependencies intersection check (skip vs invalidate_script_state_narrow)
- [x] 98-03: FNV-1a pixel equivalence helper + five canonical benchmark profiling tests + extended PHASE_PROOF format

**Requirements satisfied:** INV-01, INV-02, INV-03, INV-04, INV-05

---

## Milestone Summary

**Key Accomplishments:**
1. Interaction state changes (`:hover`, `:focus`, `:active`) restyle only affected nodes via `StyleRuleIndex` reverse index — full tree rebuild eliminated for interaction
2. Per-node service field tracking with `NodeServiceFieldDependencies` bidirectional index — O(1) field↔node queries with <1% render-time overhead
3. `SCRIPT_NARROW` flag with tree diff, >50% threshold guard, and ancestor chain expansion — leaf-only script changes bypass TREE_REBUILD
4. Field-level service event fan-out via `json_field_diff` + `nodes_reading_field()` intersection — components skip invalidation when no template node reads changed fields
5. FNV-1a pixel-equivalence testing across all 5 canonical benchmark scenarios — narrow invalidation produces pixel-identical output to full-rebuild baseline

**Decisions:**
- Tree-diff approach for narrow invalidation (rebuild tree, diff against retained snapshots)
- `>50%` affected-nodes threshold triggers TREE_REBUILD fallback (hardcoded, no config)
- Per-node diff for service event fan-out (not component-level)
- Ancestor chain expansion for leaf changes (safe, no missed reflows)

**Tech Debt:** None — milestone delivers complete narrow invalidation.
