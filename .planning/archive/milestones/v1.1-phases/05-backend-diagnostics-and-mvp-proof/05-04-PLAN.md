---
phase: 05-backend-diagnostics-and-mvp-proof
plan: 04
type: execute
wave: 4
depends_on: ["05-02", "05-03"]
files_modified:
  - docs/plugins/backend/core/reference-media/README.md
  - docs/plugins/backend/core/README.md
  - docs/extensibility.md
  - docs/plugins/backend/core/mpris-media/README.md
autonomous: true
requirements: [BREF-03, BREF-02]
requirements_addressed: [BREF-03, BREF-02]
user_setup: []
must_haves:
  truths:
    - "D-07: the proof bar includes stronger author-facing documentation than a minimal note."
    - "D-08: proof coverage includes more than one behavioral path, including failure-path coverage."
    - "BREF-03: backend MVP behavior is documented in a short reference note sufficient for plugin authors."
  artifacts:
    - path: "docs/plugins/backend/core/reference-media/README.md"
      provides: "Reference backend plugin note showing the proven MVP author pattern"
      contains: "@mesh/reference-media"
    - path: "docs/plugins/backend/core/README.md"
      provides: "Backend core plugin architecture guide corrected to match Phase 2-5 decisions"
      contains: "require(\"@mesh/"
---

<objective>
Document the proven backend MVP author path with the new reference provider and correct backend plugin docs that still describe removed or superseded behavior.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-backend-diagnostics-and-mvp-proof/05-CONTEXT.md
@.planning/phases/05-backend-diagnostics-and-mvp-proof/05-RESEARCH.md
@.planning/phases/05-backend-diagnostics-and-mvp-proof/05-VALIDATION.md
@.planning/phases/05-backend-diagnostics-and-mvp-proof/05-PATTERNS.md

@docs/plugins/backend/core/README.md
@docs/extensibility.md
@docs/plugins/backend/core/mpris-media/README.md
@packages/plugins/backend/core/reference-media/plugin.json
@packages/plugins/backend/core/reference-media/src/main.luau
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| T-05-10: docs keep advertising `mesh.exec_shell`, `mesh.service.emit`, or hidden provider fallback as the author path | high | Rewrite backend core docs to say exported top-level `state`, strict structured `mesh.exec`, and explicit active-provider selection with no hidden fallback. |
| T-05-11: the reference note is too shallow to help plugin authors reproduce the proven pattern | medium | Include manifest fields, top-level `state`, `init()`, config, logging, poll interval, command handlers, and test commands explicitly in the note. |
| T-05-12: placeholder media docs are mistaken for the reference path | medium | Update `mpris-media` docs to mark it as a placeholder and point authors at `reference-media` for the MVP pattern. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Write the reference backend MVP note</name>
  <files>docs/plugins/backend/core/reference-media/README.md</files>
  <read_first>packages/plugins/backend/core/reference-media/plugin.json, packages/plugins/backend/core/reference-media/src/main.luau, docs/plugins/backend/core/pipewire-audio/README.md, docs/plugins/backend/core/upower-power/README.md</read_first>
  <action>Create `docs/plugins/backend/core/reference-media/README.md` describing the fresh provider as the Phase 5 MVP proof plugin. Include exact sections for plugin id, implemented interface, why it is deterministic, how it uses `mesh.config()`, how it exports top-level `state`, how `init()` sets the poll interval, what `on_command_play/pause/next/previous` return, and which automated tests prove the contract. Include the exact verify commands `nix develop -c cargo test -p mesh-core-scripting reference_media` and `nix develop -c cargo test -p mesh-core-backend reference_media`.</action>
  <verify>grep -n "@mesh/reference-media\\|mesh.config()\\|state\\|on_command_play\\|reference_media" docs/plugins/backend/core/reference-media/README.md</verify>
  <acceptance_criteria>
    - `docs/plugins/backend/core/reference-media/README.md` contains `@mesh/reference-media`.
    - `docs/plugins/backend/core/reference-media/README.md` contains `mesh.config()`.
    - `docs/plugins/backend/core/reference-media/README.md` contains `state =`.
    - `docs/plugins/backend/core/reference-media/README.md` contains `on_command_play`.
    - `docs/plugins/backend/core/reference-media/README.md` contains `nix develop -c cargo test -p mesh-core-backend reference_media`.
  </acceptance_criteria>
  <done>Plugin authors have a concrete short note that matches the proven Phase 5 reference provider.</done>
</task>

<task type="auto">
  <name>Task 2: Correct backend core architecture docs to match the locked MVP contract</name>
  <files>docs/plugins/backend/core/README.md, docs/extensibility.md</files>
  <read_first>docs/plugins/backend/core/README.md, docs/extensibility.md, .planning/phases/02-backend-lifecycle-foundation/02-CONTEXT.md, .planning/phases/03-backend-host-api-contract/03-CONTEXT.md, .planning/phases/04-service-provider-contract/04-CONTEXT.md</read_first>
  <action>Update backend docs so they match the implemented architecture. Replace any text that presents `mesh.exec_shell(...)` as the public backend API with strict structured `mesh.exec(program, args)`. Replace `mesh.service.emit(data)` as the preferred author path with top-level exported `state`. Remove or rewrite any text that says the core tries the next provider automatically after init failure; the docs must describe explicit active-provider selection with visible failure status instead. Update examples to use `require("@mesh/<interface>").state` where they discuss frontend consumption.</action>
  <verify>! grep -R -n "mesh.exec_shell\\|next candidate is tried\\|mesh.service.emit(data)" docs/plugins/backend/core/README.md docs/extensibility.md</verify>
  <acceptance_criteria>
    - `grep -R -n "mesh.exec_shell" docs/plugins/backend/core/README.md docs/extensibility.md` returns no matches.
    - `grep -R -n "next candidate is tried" docs/plugins/backend/core/README.md docs/extensibility.md` returns no matches.
    - `docs/plugins/backend/core/README.md` contains `state` in the backend script ergonomics section.
    - `docs/plugins/backend/core/README.md` contains `require("@mesh/` and `.state`.
  </acceptance_criteria>
  <done>Backend docs no longer teach superseded lifecycle and host API behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Mark placeholder media docs and core index toward the reference path</name>
  <files>docs/plugins/backend/core/mpris-media/README.md, docs/plugins/backend/core/README.md</files>
  <read_first>docs/plugins/backend/core/mpris-media/README.md, docs/plugins/backend/core/README.md, docs/plugins/backend/core/reference-media/README.md</read_first>
  <action>Update `docs/plugins/backend/core/mpris-media/README.md` to state clearly that `mpris-media` remains a placeholder integration target and is not the Phase 5 MVP authoring reference. Update the core backend plugin index so `reference-media` appears in the provider list and points authors to it as the recommended proof/pattern plugin. Keep `mpris-media` documented as a future real integration rather than deleting it.</action>
  <verify>grep -R -n "reference-media\\|placeholder" docs/plugins/backend/core/README.md docs/plugins/backend/core/mpris-media/README.md</verify>
  <acceptance_criteria>
    - `docs/plugins/backend/core/mpris-media/README.md` contains `placeholder`.
    - `docs/plugins/backend/core/mpris-media/README.md` contains `reference-media`.
    - `docs/plugins/backend/core/README.md` contains `reference-media` in the core backends list or equivalent provider index.
  </acceptance_criteria>
  <done>The docs distinguish the MVP reference provider from future or placeholder integrations.</done>
</task>

</tasks>

<verification>
- Run `! grep -R -n "mesh.exec_shell\\|next candidate is tried\\|mesh.service.emit(data)" docs/plugins/backend/core/README.md docs/extensibility.md`.
- Run `grep -R -n "@mesh/reference-media\\|reference-media" docs/plugins/backend/core packages/plugins/backend/core/reference-media`.
- Read `docs/plugins/backend/core/reference-media/README.md` and confirm it names the exact provider files, command handlers, and verify commands proven in Plan 03.
</verification>

<success_criteria>
- Plugin authors can follow one short, accurate reference note to build a backend provider against the proven MVP contract.
- Backend docs stop teaching removed APIs and hidden fallback behavior.
- Placeholder media docs clearly redirect authors to the new reference provider for the MVP pattern.
</success_criteria>

<output>
After completion, create `.planning/phases/05-backend-diagnostics-and-mvp-proof/05-04-SUMMARY.md`
</output>
