---
phase: 05-backend-diagnostics-and-mvp-proof
plan: 03
type: execute
wave: 3
depends_on: ["05-01", "05-02"]
files_modified:
  - packages/plugins/backend/core/reference-media/plugin.json
  - packages/plugins/backend/core/reference-media/src/main.luau
  - crates/core/runtime/scripting/src/backend.rs
  - crates/core/runtime/backend/src/lib.rs
autonomous: true
requirements: [BREF-01, BREF-02, BDIAG-04]
requirements_addressed: [BREF-01, BREF-02, BDIAG-04]
user_setup: []
must_haves:
  truths:
    - "D-01: Phase 5 uses a brand-new backend provider as the proof target."
    - "D-02: the fresh reference plugin stays intentionally simple; proof breadth comes from tests, diagnostics, and docs."
    - "D-07: the proof bar is broad, not a minimal smoke test."
    - "BREF-01: the reference backend plugin must exercise config, logging, polling, exported top-level state snapshots, and command handling."
    - "BREF-02: automated tests must prove the reference provider exports state snapshots and handles at least one command through the public contract."
  artifacts:
    - path: "packages/plugins/backend/core/reference-media/plugin.json"
      provides: "Fresh backend provider manifest implementing mesh.media as a deterministic MVP reference"
      contains: "\"id\": \"@mesh/reference-media\""
    - path: "packages/plugins/backend/core/reference-media/src/main.luau"
      provides: "Reference media backend script using config, logging, polling, exported state, and command handlers"
      contains: "state ="
---

<objective>
Add a fresh deterministic `@mesh/reference-media` provider that demonstrates the backend MVP contract cleanly, and prove its exported-state snapshot and command handling paths with automated tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-backend-diagnostics-and-mvp-proof/05-CONTEXT.md
@.planning/phases/05-backend-diagnostics-and-mvp-proof/05-RESEARCH.md
@.planning/phases/05-backend-diagnostics-and-mvp-proof/05-VALIDATION.md
@.planning/phases/05-backend-diagnostics-and-mvp-proof/05-PATTERNS.md

@packages/plugins/backend/core/media-interface/interface.toml
@packages/plugins/backend/core/mpris-media/plugin.json
@packages/plugins/backend/core/mpris-media/src/main.luau
@packages/plugins/backend/core/pipewire-audio/src/main.luau
@packages/plugins/backend/core/upower-power/src/main.luau
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| T-05-07: the proof plugin quietly reuses an existing placeholder path instead of being genuinely fresh | medium | Create a new directory and manifest id `@mesh/reference-media`; do not edit `mpris-media` into the proof target. |
| T-05-08: the reference provider proves only happy-path exported-state snapshots and not command handling | high | Add automated tests that issue at least one public command and assert both result data and updated exported state. |
| T-05-09: the proof depends on host-specific binaries or desktop services and becomes flaky | high | Keep provider behavior deterministic and in-memory, driven by config and scripted state mutations only. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold the fresh reference media provider</name>
  <files>packages/plugins/backend/core/reference-media/plugin.json, packages/plugins/backend/core/reference-media/src/main.luau</files>
  <read_first>packages/plugins/backend/core/media-interface/interface.toml, packages/plugins/backend/core/mpris-media/plugin.json, packages/plugins/backend/core/pipewire-audio/src/main.luau, packages/plugins/backend/core/upower-power/src/main.luau</read_first>
  <action>Create a new backend plugin directory `packages/plugins/backend/core/reference-media/` with `plugin.json` and `src/main.luau`. Use manifest id exactly `@mesh/reference-media`, implement interface `mesh.media` version `1.0`, and set `base_plugin` to `@mesh/media-interface`. In `src/main.luau`, define top-level `state` with exact base fields `available`, `title`, `artist`, `album`, and `state`. `init()` must call `mesh.log.info(...)` and `mesh.service.set_poll_interval(...)`. The script must read config from `mesh.config()` to seed at least one track/title field, maintain deterministic in-memory state, and implement `on_command_play`, `on_command_pause`, `on_command_next`, and `on_command_previous` with `{ ok = true }` or `{ ok = false, error = "..." }` result tables.</action>
  <verify>grep -n "\"id\": \"@mesh/reference-media\"" packages/plugins/backend/core/reference-media/plugin.json && grep -n "function on_command_play" packages/plugins/backend/core/reference-media/src/main.luau</verify>
  <acceptance_criteria>
    - `packages/plugins/backend/core/reference-media/plugin.json` contains `"id": "@mesh/reference-media"`.
    - `packages/plugins/backend/core/reference-media/plugin.json` contains `"interface": "mesh.media"`.
    - `packages/plugins/backend/core/reference-media/src/main.luau` contains `state = {`.
    - `packages/plugins/backend/core/reference-media/src/main.luau` contains `mesh.config()`.
    - `packages/plugins/backend/core/reference-media/src/main.luau` contains `mesh.log.info`.
    - `packages/plugins/backend/core/reference-media/src/main.luau` contains `mesh.service.set_poll_interval`.
    - `packages/plugins/backend/core/reference-media/src/main.luau` contains `function on_command_play`.
  </acceptance_criteria>
  <done>A fresh provider exists specifically to demonstrate the backend MVP contract.</done>
</task>

<task type="auto">
  <name>Task 2: Add automated proof for exported-state snapshots, polling, and commands</name>
  <files>crates/core/runtime/scripting/src/backend.rs, crates/core/runtime/backend/src/lib.rs</files>
  <read_first>crates/core/runtime/scripting/src/backend.rs, crates/core/runtime/backend/src/lib.rs, packages/plugins/backend/core/reference-media/src/main.luau</read_first>
  <action>Add focused tests that load the new provider script and prove the full reference path. In `mesh-core-scripting`, add tests named `reference_media_provider_reads_config_and_exports_state` and `reference_media_provider_command_updates_state`. In `mesh-core-backend`, add tests named `reference_media_backend_emits_initial_state` and `reference_media_backend_command_returns_result_and_updated_state`. At least one backend test must dispatch a public media command such as `play` or `next`, assert the returned command result has `ok = true`, and assert the updated backend state snapshot reflects the new playback status or track selection.</action>
  <verify>nix develop -c cargo test -p mesh-core-scripting reference_media && nix develop -c cargo test -p mesh-core-backend reference_media</verify>
  <acceptance_criteria>
    - `crates/core/runtime/scripting/src/backend.rs` contains test `reference_media_provider_reads_config_and_exports_state`.
    - `crates/core/runtime/scripting/src/backend.rs` contains test `reference_media_provider_command_updates_state`.
    - `crates/core/runtime/backend/src/lib.rs` contains test `reference_media_backend_emits_initial_state`.
    - `crates/core/runtime/backend/src/lib.rs` contains test `reference_media_backend_command_returns_result_and_updated_state`.
    - `nix develop -c cargo test -p mesh-core-scripting reference_media` exits 0.
    - `nix develop -c cargo test -p mesh-core-backend reference_media` exits 0.
  </acceptance_criteria>
  <done>The fresh provider is proven through the public backend runtime contract, not only by source inspection.</done>
</task>

<task type="auto">
  <name>Task 3: Prove provider logs and failure visibility stay plugin-scoped</name>
  <files>packages/plugins/backend/core/reference-media/src/main.luau, crates/core/runtime/backend/src/lib.rs</files>
  <read_first>packages/plugins/backend/core/reference-media/src/main.luau, crates/core/runtime/backend/src/lib.rs, .planning/phases/05-backend-diagnostics-and-mvp-proof/05-CONTEXT.md</read_first>
  <action>Make at least one reference-media command or poll path log a provider-scoped message and add a focused negative-path test that proves a command failure remains attributable to `@mesh/reference-media`. The failure path may be an invalid command payload or track index. Use exact test names `reference_media_invalid_command_returns_plugin_scoped_failure` and `reference_media_logs_include_plugin_identity` if logging capture already exists; otherwise add the closest existing capture pattern and assert provider id appears in the failure or diagnostics data. Keep the implementation deterministic and in-memory.</action>
  <verify>nix develop -c cargo test -p mesh-core-backend reference_media_invalid_command_returns_plugin_scoped_failure</verify>
  <acceptance_criteria>
    - `packages/plugins/backend/core/reference-media/src/main.luau` contains at least one `mesh.log.` call in a command or poll path.
    - `crates/core/runtime/backend/src/lib.rs` contains test `reference_media_invalid_command_returns_plugin_scoped_failure`.
    - The failure-path test asserts provider id `@mesh/reference-media`.
    - `nix develop -c cargo test -p mesh-core-backend reference_media_invalid_command_returns_plugin_scoped_failure` exits 0.
  </acceptance_criteria>
  <done>The proof plugin exercises diagnostics and logging with explicit provider identity, not just happy-path exported-state snapshots.</done>
</task>

</tasks>

<verification>
- Run `nix develop -c cargo test -p mesh-core-scripting reference_media`.
- Run `nix develop -c cargo test -p mesh-core-backend reference_media`.
- Read `packages/plugins/backend/core/reference-media/src/main.luau` and confirm it uses `mesh.config()`, `mesh.log`, `mesh.service.set_poll_interval`, top-level `state`, and public `on_command_*` handlers.
</verification>

<success_criteria>
- A brand-new backend provider, not a retrofit, proves config, logging, polling, exported top-level state snapshots, and command handling.
- Automated tests show at least one public media command returns result data and updates exported state snapshots.
- Reference-provider failures remain attributable to `@mesh/reference-media`.
</success_criteria>

<output>
After completion, create `.planning/phases/05-backend-diagnostics-and-mvp-proof/05-03-SUMMARY.md`
</output>
