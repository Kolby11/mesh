---
phase: 02-backend-lifecycle-foundation
plan: 04
type: execute
wave: 2
depends_on: ["02-01", "02-02", "02-03"]
files_modified:
  - crates/core/shell/src/shell/mod.rs
  - crates/core/foundation/diagnostics/src/lib.rs
  - crates/core/foundation/debug/src/lib.rs
autonomous: true
requirements: [BPLUG-01, BPLUG-02, BPLUG-03, BPLUG-04, BPLUG-05]
requirements_addressed: [BPLUG-01, BPLUG-02, BPLUG-03, BPLUG-04, BPLUG-05]
user_setup: []
must_haves:
  truths:
    - "D-19: provider startup and lifecycle failures are visible through diagnostics and runtime status."
    - "D-20: runtime status is lifecycle-stage-specific."
    - "D-21: repeated lifecycle diagnostics are deduplicated by provider and lifecycle stage."
    - "D-02: no active provider selected is a visible status."
    - "D-15: repeated poll failures stop the runtime and mark the provider failed."
    - "D-17: stopped or replaced backends leave no stale poll loops or command receivers."
  artifacts:
    - path: "crates/core/shell/src/shell/mod.rs"
      provides: "Lifecycle status updates and debug snapshot integration"
      contains: "build_debug_snapshot"
    - path: "crates/core/foundation/diagnostics/src/lib.rs"
      provides: "Deduplicated lifecycle diagnostics"
      contains: "Diagnostics"
    - path: "crates/core/foundation/debug/src/lib.rs"
      provides: "Debug snapshot structs shown by shell status/debug tools"
      contains: "DebugSnapshot"
---

<objective>
Expose backend lifecycle status and deduplicated diagnostics so invalid manifests, missing entrypoints, no active provider, init failure, poll failure, stopped runtimes, and cleanup outcomes are visible and testable.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-backend-lifecycle-foundation/02-CONTEXT.md
@.planning/phases/02-backend-lifecycle-foundation/02-RESEARCH.md
@.planning/phases/02-backend-lifecycle-foundation/02-VALIDATION.md
@.planning/phases/02-backend-lifecycle-foundation/02-PATTERNS.md

@crates/core/shell/src/shell/mod.rs
@crates/core/foundation/diagnostics/src/lib.rs
@crates/core/foundation/debug/src/lib.rs
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| T-02-13: lifecycle failures are only logs and invisible to users/tests | high | Add runtime status data and include it in debug/status snapshots. |
| T-02-14: repeated poll failures spam diagnostics every tick | high | Add dedupe keyed by provider, stage, and message with count/timestamp updates. |
| T-02-15: no active provider looks like successful startup | medium | Record `no_active_provider` status for interfaces with requirements/providers but no explicit active selection. |
| T-02-16: status names drift from the phase contract | medium | Tests assert exact status strings `invalid_manifest`, `missing_entrypoint`, `init_failed`, `poll_failed`, `stopped`, and `no_active_provider`. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add backend lifecycle status model and exact status strings</name>
  <files>crates/core/shell/src/shell/mod.rs</files>
  <read_first>crates/core/shell/src/shell/mod.rs, crates/core/runtime/backend/src/lib.rs, .planning/phases/02-backend-lifecycle-foundation/02-CONTEXT.md</read_first>
  <action>Add a shell lifecycle status type named `BackendRuntimeStatus` or equivalent with exact serialized/display status strings `no_active_provider`, `unmet_backend_requirement`, `invalid_manifest`, `missing_entrypoint`, `missing_binary`, `init_failed`, `running`, `poll_failed`, `failed`, and `stopped`. Store statuses by interface/provider in shell state, updating them during graph validation, runtime start, init failure, poll failure, stop, and replacement cleanup.</action>
  <verify>nix develop -c cargo test -p mesh-core-shell backend_lifecycle</verify>
  <acceptance_criteria>
    - `crates/core/shell/src/shell/mod.rs` contains `BackendRuntimeStatus`.
    - `crates/core/shell/src/shell/mod.rs` contains string `init_failed`.
    - `crates/core/shell/src/shell/mod.rs` contains string `poll_failed`.
    - `crates/core/shell/src/shell/mod.rs` contains string `stopped`.
    - Test `backend_lifecycle_status_names_match_phase_contract` asserts all exact status strings listed in the action.
  </acceptance_criteria>
  <done>Backend lifecycle state is explicit and uses the phase contract vocabulary.</done>
</task>

<task type="auto">
  <name>Task 2: Add deduplicated lifecycle diagnostics by provider and stage</name>
  <files>crates/core/foundation/diagnostics/src/lib.rs, crates/core/shell/src/shell/mod.rs</files>
  <read_first>crates/core/foundation/diagnostics/src/lib.rs, crates/core/shell/src/shell/mod.rs, .planning/phases/02-backend-lifecycle-foundation/02-RESEARCH.md</read_first>
  <action>Extend diagnostics with a method named `record_lifecycle_error(provider_id, stage, message)` or equivalent. It must deduplicate by `(provider_id, stage, message)`, increment `error_count` only when a new lifecycle failure key is first seen, and update health to `HealthStatus::Error` with a message containing the provider id and stage. Wire shell lifecycle failures into this method for statuses `invalid_manifest`, `missing_entrypoint`, `missing_binary`, `init_failed`, and `poll_failed`.</action>
  <verify>nix develop -c cargo test -p mesh-core-diagnostics lifecycle && nix develop -c cargo test -p mesh-core-shell backend_lifecycle</verify>
  <acceptance_criteria>
    - `crates/core/foundation/diagnostics/src/lib.rs` contains `record_lifecycle_error`.
    - Test `lifecycle_errors_are_deduplicated_by_provider_stage_and_message` records the same provider/stage/message twice and asserts `error_count() == 1`.
    - The same test records a different stage and asserts `error_count() == 2`.
    - `crates/core/shell/src/shell/mod.rs` calls `record_lifecycle_error` for `init_failed`.
    - `crates/core/shell/src/shell/mod.rs` calls `record_lifecycle_error` for `poll_failed`.
  </acceptance_criteria>
  <done>Repeated lifecycle failures remain visible without diagnostic spam.</done>
</task>

<task type="auto">
  <name>Task 3: Include backend runtime status in debug/status snapshots</name>
  <files>crates/core/foundation/debug/src/lib.rs, crates/core/shell/src/shell/mod.rs</files>
  <read_first>crates/core/foundation/debug/src/lib.rs, crates/core/shell/src/shell/mod.rs, crates/core/foundation/diagnostics/src/lib.rs</read_first>
  <action>Add backend runtime status entries to `DebugSnapshot` using a struct with exact fields `interface`, `provider_id`, `status`, and `message`. Update `Shell::build_debug_snapshot()` to include current backend lifecycle statuses sorted by interface then provider id. Preserve existing plugin, interface, health, and active surface snapshot fields.</action>
  <verify>nix develop -c cargo test -p mesh-core-shell backend_lifecycle</verify>
  <acceptance_criteria>
    - `crates/core/foundation/debug/src/lib.rs` contains `BackendRuntimeEntry`.
    - `BackendRuntimeEntry` contains `interface`.
    - `BackendRuntimeEntry` contains `provider_id`.
    - `BackendRuntimeEntry` contains `status`.
    - `BackendRuntimeEntry` contains `message`.
    - Test `debug_snapshot_includes_backend_lifecycle_status` asserts a `mesh.audio` `@mesh/pipewire-audio` `running` or failure status appears in the snapshot.
  </acceptance_criteria>
  <done>Debug/status output can show provider lifecycle state without reading logs.</done>
</task>

</tasks>

<verification>
- Run `nix develop -c cargo test -p mesh-core-diagnostics lifecycle`.
- Run `nix develop -c cargo test -p mesh-core-shell backend_lifecycle`.
- Run the full phase validation command from `02-VALIDATION.md`.
- Run `grep -n "invalid_manifest\\|missing_entrypoint\\|init_failed\\|poll_failed\\|stopped\\|no_active_provider" crates/core/shell/src/shell/mod.rs`.
</verification>

<success_criteria>
- BPLUG-01 through BPLUG-05 all have visible lifecycle status and diagnostics coverage.
- D-19, D-20, and D-21 are implemented through status naming, debug snapshots, and deduped diagnostics.
- No status or diagnostic path introduces provider fallback, automatic restart, or service-specific Rust behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/02-backend-lifecycle-foundation/02-04-SUMMARY.md`
</output>
