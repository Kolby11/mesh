---
phase: 04-service-provider-contract
plan: 04
type: execute
wave: 4
depends_on: ["04-01", "04-02", "04-03"]
files_modified:
  - packages/plugins/backend/core/pipewire-audio/src/main.luau
  - packages/plugins/backend/core/pulseaudio-audio/src/main.luau
  - packages/plugins/backend/core/networkmanager-network/src/main.luau
  - packages/plugins/backend/core/upower-power/src/main.luau
  - packages/plugins/backend/core/shell-theme/src/main.luau
  - crates/core/runtime/scripting/src/backend.rs
  - crates/core/runtime/backend/src/lib.rs
autonomous: true
requirements: [BSVC-01, BSVC-02, BSVC-03, BSVC-04, BSVC-05]
requirements_addressed: [BSVC-01, BSVC-02, BSVC-03, BSVC-04, BSVC-05]
user_setup: []
must_haves:
  truths:
    - "D-01: bundled backend module identity must align with package/plugin manifest id."
    - "D-02: bundled providers use top-level exported state as public service state."
    - "D-03: runtime snapshots bundled provider state after poll and command callbacks."
    - "D-04: mesh.service.emit is compatibility behavior, not the preferred provider pattern."
    - "D-05: bundled providers do not inject source_plugin into public state."
    - "D-09: Lua package manager support is deferred; provider migration must not add package manager behavior."
    - "D-11: command handlers return result tables where command outcomes matter."
    - "D-12: command handlers update state as the current service data source."
    - "D-16: bundled provider state shape mismatches warn during Phase 4 instead of stopping providers."
  artifacts:
    - path: "packages/plugins/backend/core/pipewire-audio/src/main.luau"
      provides: "PipeWire provider using exported state and command result returns"
      contains: "state ="
    - path: "packages/plugins/backend/core/networkmanager-network/src/main.luau"
      provides: "Network provider using exported state and command result returns"
      contains: "state ="
---

<objective>
Migrate bundled backend providers and compatibility tests to the new exported `state` contract, remove provider-authored `source_plugin` public state fields, and prove command handlers can return result tables while still updating state.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-service-provider-contract/04-CONTEXT.md
@.planning/phases/04-service-provider-contract/04-RESEARCH.md
@.planning/phases/04-service-provider-contract/04-VALIDATION.md
@.planning/phases/04-service-provider-contract/04-PATTERNS.md

@packages/plugins/backend/core/pipewire-audio/src/main.luau
@packages/plugins/backend/core/pulseaudio-audio/src/main.luau
@packages/plugins/backend/core/networkmanager-network/src/main.luau
@packages/plugins/backend/core/upower-power/src/main.luau
@packages/plugins/backend/core/shell-theme/src/main.luau
@crates/core/runtime/scripting/src/backend.rs
@crates/core/runtime/backend/src/lib.rs
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| T-04-13: provider migration drops required state fields | high | Run bundled script/runtime tests and contract warning tests for audio, network, power, and theme. |
| T-04-14: source_plugin remains in public provider state | medium | Grep bundled providers for `source_plugin` in emitted/exported state and remove it. |
| T-04-15: command handlers return results but stop refreshing state | medium | Require each mutating handler to update `state` or call its shared state refresh helper. |
| T-04-16: migration introduces Lua package manager scope creep | low | Do not add dependency resolution or package-manager imports in this phase. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Migrate audio providers to exported state</name>
  <files>packages/plugins/backend/core/pipewire-audio/src/main.luau, packages/plugins/backend/core/pulseaudio-audio/src/main.luau</files>
  <read_first>packages/plugins/backend/core/pipewire-audio/src/main.luau, packages/plugins/backend/core/pulseaudio-audio/src/main.luau, packages/plugins/backend/core/audio-interface/interface.toml, .planning/phases/04-service-provider-contract/04-CONTEXT.md</read_first>
  <action>Add a top-level `state = { available = false, percent = 0, muted = false }` table to both audio providers. Replace `mesh.service.emit(state_table)` calls with assignments to the top-level `state` variable or field updates on it. Remove `source_plugin = PLUGIN_ID` from public state tables. Keep helper functions such as `read_state()` and `emit_state()` only if they now update `state`. Update mutating command handlers (`volume_up`, `volume_down`, `set_volume`, `set_muted`, `toggle_mute`, `play_sound`) so handlers return `{ ok = true }` on accepted command execution and `{ ok = false, error = "invalid sound path" }` for invalid sound path. Preserve legacy percent payload compatibility and normalized volume payload handling from STATE.md.</action>
  <verify>nix develop -c cargo test -p mesh-core-scripting bundled_backend_scripts_expose_required_host_api_surface</verify>
  <acceptance_criteria>
    - `packages/plugins/backend/core/pipewire-audio/src/main.luau` contains `state =`.
    - `packages/plugins/backend/core/pulseaudio-audio/src/main.luau` contains `state =`.
    - `packages/plugins/backend/core/pipewire-audio/src/main.luau` does not contain `source_plugin`.
    - `packages/plugins/backend/core/pulseaudio-audio/src/main.luau` does not contain `source_plugin`.
    - `packages/plugins/backend/core/pipewire-audio/src/main.luau` contains `return { ok = true`.
    - `packages/plugins/backend/core/pulseaudio-audio/src/main.luau` contains `return { ok = true`.
  </acceptance_criteria>
  <done>Audio providers use exported state and result-returning command handlers.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate network, power, and theme providers to exported state</name>
  <files>packages/plugins/backend/core/networkmanager-network/src/main.luau, packages/plugins/backend/core/upower-power/src/main.luau, packages/plugins/backend/core/shell-theme/src/main.luau</files>
  <read_first>packages/plugins/backend/core/networkmanager-network/src/main.luau, packages/plugins/backend/core/upower-power/src/main.luau, packages/plugins/backend/core/shell-theme/src/main.luau, packages/plugins/backend/core/network-interface/interface.toml, packages/plugins/backend/core/power-interface/interface.toml</read_first>
  <action>Add top-level `state` tables to NetworkManager, UPower, and shell-theme providers. Network initial state must include `available`, `wifi_enabled`, `connections`, `devices`, and `networks`. Power initial state must include `available`, `level`, `charging`, `time_remaining_minutes`, and `time_to_full_minutes`. Shell theme state must preserve existing fields `current` and `is_dark`. Replace `mesh.service.emit(...)` and `mesh.service.emit_unavailable()` with top-level `state` assignments or updates. Remove provider-authored `source_plugin` public fields. Return `{ ok = true }` or `{ ok = false, error = "..." }` from network command handlers where command execution has an outcome.</action>
  <verify>nix develop -c cargo test -p mesh-core-scripting bundled_backend_scripts_expose_required_host_api_surface</verify>
  <acceptance_criteria>
    - `packages/plugins/backend/core/networkmanager-network/src/main.luau` contains `state =`.
    - `packages/plugins/backend/core/upower-power/src/main.luau` contains `state =`.
    - `packages/plugins/backend/core/shell-theme/src/main.luau` contains `state =`.
    - `grep -R "source_plugin" packages/plugins/backend/core/*/src/main.luau` returns no matches.
    - `packages/plugins/backend/core/networkmanager-network/src/main.luau` contains `return { ok =`.
  </acceptance_criteria>
  <done>Non-audio bundled providers use exported state and no provider-authored source identity fields.</done>
</task>

<task type="auto">
  <name>Task 3: Update bundled runtime tests for exported state and command results</name>
  <files>crates/core/runtime/scripting/src/backend.rs, crates/core/runtime/backend/src/lib.rs</files>
  <read_first>crates/core/runtime/scripting/src/backend.rs, crates/core/runtime/backend/src/lib.rs, packages/plugins/backend/core/pipewire-audio/src/main.luau, packages/plugins/backend/core/networkmanager-network/src/main.luau</read_first>
  <action>Update bundled backend script tests so they load and initialize migrated providers without relying on `mesh.service.emit(...)` as the public pattern. Add tests named `bundled_audio_provider_exports_state`, `bundled_network_provider_exports_state`, and `bundled_command_handler_returns_result_table`. Keep compatibility tests for `mesh.service.emit(...)` in a focused test named `mesh_service_emit_remains_compatibility_state_setter`. Ensure command result event tests cover at least one provider command handler that returns `{ ok = false, error = "..." }`.</action>
  <verify>nix develop -c cargo test -p mesh-core-scripting bundled_ && nix develop -c cargo test -p mesh-core-backend command_result</verify>
  <acceptance_criteria>
    - `crates/core/runtime/scripting/src/backend.rs` contains `bundled_audio_provider_exports_state`.
    - `crates/core/runtime/scripting/src/backend.rs` contains `bundled_network_provider_exports_state`.
    - `crates/core/runtime/scripting/src/backend.rs` contains `mesh_service_emit_remains_compatibility_state_setter`.
    - `crates/core/runtime/backend/src/lib.rs` contains `bundled_command_handler_returns_result_table` or an equivalent provider command result test.
    - `nix develop -c cargo test -p mesh-core-scripting bundled_` exits 0.
  </acceptance_criteria>
  <done>Bundled providers prove the exported state contract while compatibility remains tested.</done>
</task>

</tasks>

<verification>
- Run `nix develop -c cargo test -p mesh-core-scripting bundled_`.
- Run `nix develop -c cargo test -p mesh-core-backend command_result`.
- Run `grep -R "source_plugin" packages/plugins/backend/core/*/src/main.luau` and confirm it returns no matches.
- Run `grep -R "mesh.service.emit" packages/plugins/backend/core/*/src/main.luau` and confirm remaining matches are either removed or explicitly documented compatibility-only cases outside migrated providers.
</verification>

<success_criteria>
- BSVC-01 is covered by bundled provider package/interface metadata staying intact.
- BSVC-02 is covered by bundled exported state and compatibility tests.
- BSVC-03 is covered by runtime state snapshots reaching latest state storage.
- BSVC-04 is covered by bundled command handlers still using generic `on_command_*`.
- BSVC-05 is covered by bundled command result tests.
- D-01 through D-05, D-09, D-11, D-12, and D-16 are visible in this plan's `must_haves.truths`.
</success_criteria>

<output>
After completion, create `.planning/phases/04-service-provider-contract/04-04-SUMMARY.md`
</output>
