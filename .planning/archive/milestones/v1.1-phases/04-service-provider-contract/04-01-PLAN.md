---
phase: 04-service-provider-contract
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/core/runtime/scripting/src/backend.rs
  - crates/core/runtime/backend/src/lib.rs
autonomous: true
requirements: [BSVC-02, BSVC-05]
requirements_addressed: [BSVC-02, BSVC-05]
user_setup: []
must_haves:
  truths:
    - "D-02: backend Luau modules expose service state through top-level exported state."
    - "D-03: runtime snapshots exported state after init, poll, and command callbacks."
    - "D-04: mesh.service.emit(payload) is compatibility behavior, not the preferred MVP state API."
    - "D-10: service proxy methods dispatch generically to backend Luau command handlers."
    - "D-11: command methods return a small caller-visible result table and may update reactive state."
    - "D-12: state is the source of truth for current service data after commands."
  artifacts:
    - path: "crates/core/runtime/scripting/src/backend.rs"
      provides: "Backend Luau exported state snapshot and command result extraction"
      contains: "BackendScriptContext"
    - path: "crates/core/runtime/backend/src/lib.rs"
      provides: "Async backend runtime loop and shell-facing backend events"
      contains: "BackendServiceEvent"
---

<objective>
Teach the backend runtime to treat top-level exported Luau `state` as the primary service state, preserve `mesh.service.emit(...)` as a compatibility setter, and surface command handler results without service-specific Rust logic.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-service-provider-contract/04-CONTEXT.md
@.planning/phases/04-service-provider-contract/04-RESEARCH.md
@.planning/phases/04-service-provider-contract/04-VALIDATION.md
@.planning/phases/04-service-provider-contract/04-PATTERNS.md

@crates/core/runtime/scripting/src/backend.rs
@crates/core/runtime/backend/src/lib.rs
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| T-04-01: non-JSON Luau state crashes backend callbacks | high | Convert exported `state` with `LuaSerdeExt`; return warning/failure diagnostics instead of panicking. |
| T-04-02: compatibility `mesh.service.emit` and exported `state` produce conflicting updates | medium | Define one merge rule in tests: exported `state` is primary, and `emit` writes through the same pending state path. |
| T-04-03: command handler failure hides from callers | high | Convert handler return or error into a JSON-compatible result table with `ok` and `error`. |
| T-04-04: command result plumbing introduces service-specific branches | high | Keep command/result types generic: command string, JSON payload, JSON result. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Snapshot top-level backend state after lifecycle callbacks</name>
  <files>crates/core/runtime/scripting/src/backend.rs</files>
  <read_first>crates/core/runtime/scripting/src/backend.rs, .planning/phases/04-service-provider-contract/04-CONTEXT.md, .planning/phases/04-service-provider-contract/04-RESEARCH.md</read_first>
  <action>Add exported-state support to `BackendScriptContext`. Add a method named `take_service_state_snapshot` or equivalent that reads the Lua global named exactly `state`, converts it to `serde_json::Value`, and returns `Ok(None)` when `state` is nil. Call this helper after `call_init()`, after `run_poll()`, and after `run_command()`. Update `BackendRuntime` so `mesh.service.emit(value)` stores the same JSON value in the pending state path used by exported `state`. Keep `mesh.service.payload()` unchanged. Add tests named `backend_state_snapshot_reads_top_level_state`, `backend_state_snapshot_updates_after_poll`, and `backend_state_snapshot_updates_after_command`.</action>
  <verify>nix develop -c cargo test -p mesh-core-scripting backend_state</verify>
  <acceptance_criteria>
    - `crates/core/runtime/scripting/src/backend.rs` contains `state` lookup logic in `take_service_state_snapshot` or equivalent.
    - `crates/core/runtime/scripting/src/backend.rs` contains test `backend_state_snapshot_reads_top_level_state`.
    - `crates/core/runtime/scripting/src/backend.rs` contains test `backend_state_snapshot_updates_after_poll`.
    - `crates/core/runtime/scripting/src/backend.rs` contains test `backend_state_snapshot_updates_after_command`.
    - `nix develop -c cargo test -p mesh-core-scripting backend_state` exits 0.
  </acceptance_criteria>
  <done>Backend scripts can publish public service state by assigning top-level `state`.</done>
</task>

<task type="auto">
  <name>Task 2: Emit state snapshots through the async backend runtime</name>
  <files>crates/core/runtime/backend/src/lib.rs</files>
  <read_first>crates/core/runtime/backend/src/lib.rs, crates/core/runtime/scripting/src/backend.rs, .planning/phases/04-service-provider-contract/04-PATTERNS.md</read_first>
  <action>Update `spawn_backend_service()` so it publishes exported-state snapshots after successful `init()`, after each `on_poll()`, and after each successful command handler. Keep duplicate-payload suppression by comparing latest JSON state. Preserve `BackendServiceUpdate { service, source_plugin, payload }` or replace it with an equivalent generic type that still contains `service`, `source_plugin`, and `payload`. Add tests named `spawn_backend_service_emits_initial_exported_state`, `spawn_backend_service_emits_changed_exported_state_after_poll`, and `spawn_backend_service_emits_changed_exported_state_after_command`.</action>
  <verify>nix develop -c cargo test -p mesh-core-backend exported_state</verify>
  <acceptance_criteria>
    - `crates/core/runtime/backend/src/lib.rs` contains test `spawn_backend_service_emits_initial_exported_state`.
    - `crates/core/runtime/backend/src/lib.rs` contains test `spawn_backend_service_emits_changed_exported_state_after_poll`.
    - `crates/core/runtime/backend/src/lib.rs` contains test `spawn_backend_service_emits_changed_exported_state_after_command`.
    - Existing tests `spawn_backend_service_passes_settings_into_backend_context` and `backend_command_dispatches_set_volume_normalized_payload` still pass.
    - `nix develop -c cargo test -p mesh-core-backend exported_state` exits 0.
  </acceptance_criteria>
  <done>The async backend loop publishes exported state snapshots generically.</done>
</task>

<task type="auto">
  <name>Task 3: Add generic command result extraction and runtime events</name>
  <files>crates/core/runtime/scripting/src/backend.rs, crates/core/runtime/backend/src/lib.rs</files>
  <read_first>crates/core/runtime/scripting/src/backend.rs, crates/core/runtime/backend/src/lib.rs, .planning/phases/04-service-provider-contract/04-CONTEXT.md</read_first>
  <action>Change backend command execution so `on_command_*` handlers may return a Luau table result. If a command handler returns nil and does not error, create JSON result `{ "ok": true }`. If a command handler throws, create JSON result `{ "ok": false, "error": "<error string>" }` and keep the existing command failure lifecycle visibility. Add a generic `BackendServiceEvent::CommandResult` or equivalent event containing service/interface, source_plugin, command, and result. Do not include service-specific fields. Add tests named `backend_command_handler_return_table_becomes_result`, `backend_command_handler_nil_defaults_to_ok_result`, and `backend_command_handler_error_becomes_failed_result`.</action>
  <verify>nix develop -c cargo test -p mesh-core-scripting command_result && nix develop -c cargo test -p mesh-core-backend command_result</verify>
  <acceptance_criteria>
    - `crates/core/runtime/scripting/src/backend.rs` contains `backend_command_handler_return_table_becomes_result`.
    - `crates/core/runtime/scripting/src/backend.rs` contains `backend_command_handler_nil_defaults_to_ok_result`.
    - `crates/core/runtime/backend/src/lib.rs` contains `BackendServiceEvent::CommandResult` or an equivalent command result event.
    - `crates/core/runtime/backend/src/lib.rs` contains test `backend_command_handler_error_becomes_failed_result` or equivalent runtime-level command error result test.
    - `grep -n "wpctl\\|pactl\\|nmcli\\|upower" crates/core/runtime/backend/src/lib.rs crates/core/runtime/scripting/src/backend.rs` returns no service-specific command handling.
  </acceptance_criteria>
  <done>Backend command completion has generic result data plus state update propagation.</done>
</task>

</tasks>

<verification>
- Run `nix develop -c cargo test -p mesh-core-scripting backend_state`.
- Run `nix develop -c cargo test -p mesh-core-scripting command_result`.
- Run `nix develop -c cargo test -p mesh-core-backend exported_state`.
- Run `nix develop -c cargo test -p mesh-core-backend command_result`.
- Run `grep -n "wpctl\\|pactl\\|nmcli\\|upower" crates/core/runtime/backend/src/lib.rs crates/core/runtime/scripting/src/backend.rs` and confirm no service-specific command behavior was added.
</verification>

<success_criteria>
- BSVC-02 is covered by exported `state` snapshots and `mesh.service.emit(...)` compatibility.
- BSVC-05 is covered by generic command result data for success and failure.
- D-02, D-03, D-04, D-10, D-11, and D-12 are visible in this plan's `must_haves.truths`.
</success_criteria>

<output>
After completion, create `.planning/phases/04-service-provider-contract/04-01-SUMMARY.md`
</output>
