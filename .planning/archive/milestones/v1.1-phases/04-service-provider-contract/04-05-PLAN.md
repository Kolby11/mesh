---
phase: 04-service-provider-contract
plan: 05
type: execute
gap_closure: true
wave: 5
depends_on: ["04-01", "04-02", "04-03", "04-04"]
files_modified:
  - crates/core/shell/src/shell/mod.rs
autonomous: true
requirements: [BSVC-03]
requirements_addressed: [BSVC-03]
user_setup: []
must_haves:
  truths:
    - "D-13: the shell stores latest state per interface for normal consumers."
    - "D-14: provider identity is tracked as metadata beside latest interface state, not injected into public state."
    - "D-15: provider swaps and restarts must preserve the interface-level state mapping for the active provider."
    - "Theme fallback and recovery must keep `mesh.theme.current` aligned with the shell's resolved active theme id."
  artifacts:
    - path: "crates/core/shell/src/shell/mod.rs"
      provides: "Resolved shell-theme backend settings, synchronized fallback recovery, and regression coverage for `mesh.theme` latest-state correctness"
      contains: "apply_shell_runtime_settings"
---

<objective>
Close the remaining BSVC-03 gap by making shell-theme fallback startup, backend restart, and file-watch recovery keep `mesh.theme` latest state aligned with the shell's resolved active theme id.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-service-provider-contract/04-CONTEXT.md
@.planning/phases/04-service-provider-contract/04-RESEARCH.md
@.planning/phases/04-service-provider-contract/04-VALIDATION.md
@.planning/phases/04-service-provider-contract/04-PATTERNS.md
@.planning/phases/04-service-provider-contract/04-REVIEW.md
@.planning/phases/04-service-provider-contract/04-VERIFICATION.md

@crates/core/shell/src/shell/mod.rs
@packages/plugins/backend/core/shell-theme/src/main.luau
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| T-04-17: shell-theme backend restart republishes an invalid configured theme id after fallback | high | Seed backend `current_theme` from `self.theme.active().id` and add a regression that starts or respawns the provider after fallback. |
| T-04-18: theme file recovery changes the shell theme without updating `mesh.theme` latest state | high | Make file-watch reload return pending core requests and call `sync_theme_service_state()` whenever the resolved active theme id changes. |
| T-04-19: fallback fix only covers the happy path and regresses direct theme reload behavior | medium | Keep existing reload tests green and add assertions for `latest_service_state["mesh.theme"]`, component events, and resolved `is_dark` values. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Seed shell-theme backend settings from the resolved active theme</name>
  <files>crates/core/shell/src/shell/mod.rs</files>
  <read_first>crates/core/shell/src/shell/mod.rs, packages/plugins/backend/core/shell-theme/src/main.luau, .planning/phases/04-service-provider-contract/04-REVIEW.md, .planning/phases/04-service-provider-contract/04-VERIFICATION.md</read_first>
  <action>Update `apply_shell_runtime_settings(&self, candidate: &mut BackendLaunchCandidate)` so the `mesh.theme` backend receives `current_theme` from `self.theme.active().id.clone()` instead of `self.settings.theme.active.clone()`. Keep the existing `candidate.interface != "mesh.theme"` early return. Preserve the current object-update behavior for both existing `candidate.settings` objects and empty settings values. Add or rename the focused test near `shell_theme_backend_candidate_receives_active_theme_setting` so it proves a shell with `settings.theme.active = "missing-theme"` and a resolved fallback `self.theme.active().id = "mesh-default-dark"` writes `"mesh-default-dark"` into `candidate.settings["current_theme"]`.</action>
  <verify>nix develop -c cargo test -p mesh-core-shell shell_theme_backend_candidate_receives</verify>
  <acceptance_criteria>
    - `crates/core/shell/src/shell/mod.rs` contains `self.theme.active().id.clone()` inside `apply_shell_runtime_settings`.
    - `crates/core/shell/src/shell/mod.rs` does not contain `self.settings.theme.active.clone()` inside `apply_shell_runtime_settings`.
    - `crates/core/shell/src/shell/mod.rs` contains a test named `shell_theme_backend_candidate_receives_resolved_active_theme_setting` or an equivalent fallback-specific candidate-setting test.
    - `nix develop -c cargo test -p mesh-core-shell shell_theme_backend_candidate_receives` exits 0.
  </acceptance_criteria>
  <done>Shell-theme backend startup and restart read the shell's resolved active theme id, not the stale configured value.</done>
</task>

<task type="auto">
  <name>Task 2: Synchronize mesh.theme state when file-watch reload changes the active theme</name>
  <files>crates/core/shell/src/shell/mod.rs</files>
  <read_first>crates/core/shell/src/shell/mod.rs, .planning/phases/04-service-provider-contract/04-REVIEW.md, .planning/phases/04-service-provider-contract/04-VERIFICATION.md</read_first>
  <action>Change `reload_theme_if_changed(&mut self)` to return `Result<VecDeque<CoreRequest>, ShellRunError>` instead of `Result<(), ShellRunError>`. Capture `old_theme_id = self.theme.active().id.clone()` before loading the file, keep the existing metadata short-circuit behavior, then after `self.theme.replace_active(theme)` and `self.mark_components_theme_changed()?`, compare the new active theme id to `old_theme_id`. When the id changed, return `self.sync_theme_service_state(&new_theme_id)` so `latest_service_state["mesh.theme"]`, the backend `set-current` command, and component events stay in sync; otherwise return `Ok(VecDeque::new())`. Update the shell run loop call site from `self.reload_theme_if_changed()?;` to `pending.extend(self.reload_theme_if_changed()?);` so the queued requests are drained in the same frame.</action>
  <verify>nix develop -c cargo test -p mesh-core-shell settings_theme_reload</verify>
  <acceptance_criteria>
    - `crates/core/shell/src/shell/mod.rs` contains `fn reload_theme_if_changed(&mut self) -> Result<VecDeque<CoreRequest>, ShellRunError>`.
    - `crates/core/shell/src/shell/mod.rs` contains `pending.extend(self.reload_theme_if_changed()?);`.
    - `crates/core/shell/src/shell/mod.rs` contains `self.sync_theme_service_state(&new_theme_id)`.
    - `nix develop -c cargo test -p mesh-core-shell settings_theme_reload` exits 0.
  </acceptance_criteria>
  <done>Theme file recovery updates shell state, service state, and pending core requests together.</done>
</task>

<task type="auto">
  <name>Task 3: Add regressions for fallback restart and missing-theme file recovery</name>
  <files>crates/core/shell/src/shell/mod.rs</files>
  <read_first>crates/core/shell/src/shell/mod.rs, packages/plugins/backend/core/shell-theme/src/main.luau, .planning/phases/04-service-provider-contract/04-REVIEW.md, .planning/phases/04-service-provider-contract/04-VERIFICATION.md</read_first>
  <action>Add focused shell tests that cover both BSVC-03 regressions. First, add a restart or cold-start regression that begins with `settings.theme.active = "missing-theme"`, resolves the shell fallback theme to `mesh-default-dark`, applies shell runtime settings to a `mesh.theme` backend candidate, and asserts `candidate.settings["current_theme"]` plus the resulting `latest_service_state["mesh.theme"].state["current"]` remain `mesh-default-dark` after backend start or replacement. Second, add a file-watch recovery regression that starts with a missing configured theme file, asserts fallback `mesh.theme.current == "mesh-default-dark"`, then creates the requested theme file, forces `reload_theme_if_changed()`, and asserts `self.theme.active().id`, `latest_service_state["mesh.theme"].state["current"]`, and the recorded component event payload all match the recovered theme id. Keep `is_dark` assertions alongside `current` so the provider payload shape remains complete.</action>
  <verify>nix develop -c cargo test -p mesh-core-shell theme</verify>
  <acceptance_criteria>
    - `crates/core/shell/src/shell/mod.rs` contains a fallback restart or cold-start regression for `mesh.theme`.
    - `crates/core/shell/src/shell/mod.rs` contains a file-watch recovery regression that asserts both `self.theme.active().id` and `latest_service_state.get("mesh.theme")`.
    - `crates/core/shell/src/shell/mod.rs` contains `seen_events.lock().unwrap()` assertions in the new fallback recovery test.
    - `nix develop -c cargo test -p mesh-core-shell theme` exits 0.
  </acceptance_criteria>
  <done>Phase 4 has durable proof that `mesh.theme` latest state survives fallback startup, backend restart, and later theme-file recovery.</done>
</task>

</tasks>

<verification>
- Run `nix develop -c cargo test -p mesh-core-shell shell_theme_backend_candidate_receives`.
- Run `nix develop -c cargo test -p mesh-core-shell settings_theme_reload`.
- Run `nix develop -c cargo test -p mesh-core-shell theme`.
- Read `crates/core/shell/src/shell/mod.rs` and confirm `apply_shell_runtime_settings` uses the resolved active theme id and `reload_theme_if_changed` returns queued requests through `sync_theme_service_state`.
</verification>

<success_criteria>
- BSVC-03 no longer has a theme fallback exception path that can desynchronize `latest_service_state["mesh.theme"]` from the shell's resolved active theme.
- `apply_shell_runtime_settings` seeds `current_theme` from the resolved shell theme id during backend startup and restart.
- File-watch recovery updates `self.theme.active().id`, `mesh.theme.current`, and emitted component payloads in the same reload path.
- D-13, D-14, and D-15 are explicitly enforced by this plan's `must_haves.truths`.
</success_criteria>

<output>
After completion, create `.planning/phases/04-service-provider-contract/04-05-SUMMARY.md`
</output>
