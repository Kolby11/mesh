---
phase: 03-backend-host-api-contract
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - packages/plugins/backend/core/pipewire-audio/src/main.luau
  - packages/plugins/backend/core/pulseaudio-audio/src/main.luau
  - packages/plugins/backend/core/networkmanager-network/src/main.luau
  - packages/plugins/backend/core/upower-power/src/main.luau
autonomous: true
requirements: [BHOST-01, BHOST-02]
requirements_addressed: [BHOST-01, BHOST-02]
user_setup: []
must_haves:
  truths:
    - "D-01: bundled providers must call mesh.exec(program, args) with structured args."
    - "D-05: mesh.exec_shell is not part of the MVP backend host API."
    - "D-06: shell-style parsing and pipelines move into Luau logic using structured mesh.exec calls."
    - "D-07: BHOST-02 is addressed by removing/migrating exec_shell from the MVP API."
    - "Rust core must stay generic and must not add service-specific command parsing for audio, network, or power."
  artifacts:
    - path: "packages/plugins/backend/core/pipewire-audio/src/main.luau"
      provides: "PipeWire provider using structured wpctl commands"
      contains: "mesh.exec"
    - path: "packages/plugins/backend/core/pulseaudio-audio/src/main.luau"
      provides: "PulseAudio provider using structured pactl commands"
      contains: "mesh.exec"
    - path: "packages/plugins/backend/core/networkmanager-network/src/main.luau"
      provides: "NetworkManager provider using structured nmcli and bluetoothctl commands"
      contains: "mesh.exec"
    - path: "packages/plugins/backend/core/upower-power/src/main.luau"
      provides: "UPower provider parsing upower output in Luau"
      contains: "mesh.exec"
---

<objective>
Migrate bundled backend providers off `mesh.exec_shell` so all shipped providers use the strict structured `mesh.exec(program, args)` host API while keeping service-specific parsing in Luau.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-backend-host-api-contract/03-CONTEXT.md
@.planning/phases/03-backend-host-api-contract/03-RESEARCH.md
@.planning/phases/03-backend-host-api-contract/03-VALIDATION.md
@.planning/phases/03-backend-host-api-contract/03-PATTERNS.md

@packages/plugins/backend/core/pipewire-audio/src/main.luau
@packages/plugins/backend/core/pulseaudio-audio/src/main.luau
@packages/plugins/backend/core/networkmanager-network/src/main.luau
@packages/plugins/backend/core/upower-power/src/main.luau
@crates/core/runtime/scripting/src/backend.rs
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| T-03-05: provider migration reintroduces shell interpolation through a compatibility helper | high | Static grep must find no `mesh.exec_shell` calls in bundled provider scripts. |
| T-03-06: parsing moves from Luau into Rust service-specific branches | high | Modify only provider scripts and scripting contract tests; do not add shell/core service branches. |
| T-03-07: shell pipeline rewrite changes emitted service payload shape | medium | Keep existing parser helpers and run bundled backend script host API tests. |
| T-03-08: dynamic values are concatenated into program strings | medium | Every command invocation must pass dynamic values in the args table. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Migrate audio providers to structured exec</name>
  <files>packages/plugins/backend/core/pipewire-audio/src/main.luau, packages/plugins/backend/core/pulseaudio-audio/src/main.luau</files>
  <read_first>packages/plugins/backend/core/pipewire-audio/src/main.luau, packages/plugins/backend/core/pulseaudio-audio/src/main.luau, .planning/phases/03-backend-host-api-contract/03-CONTEXT.md, .planning/phases/03-backend-host-api-contract/03-PATTERNS.md</read_first>
  <action>Replace all `mesh.exec_shell` calls in PipeWire and PulseAudio providers with structured `mesh.exec(program, args)` calls. For PipeWire, call `mesh.exec("wpctl", {"status"})` and parse sink ids in Luau, call `mesh.exec("wpctl", {"get-volume", sink_id})`, and split chained updates into separate structured calls such as `mesh.exec("wpctl", {"set-volume", sink_id, "5%+"})` followed by `mesh.exec("wpctl", {"set-mute", sink_id, "0"})`. For PulseAudio, call `mesh.exec("pactl", {"get-sink-volume", "@DEFAULT_SINK@"})` and `mesh.exec("pactl", {"get-sink-mute", "@DEFAULT_SINK@"})` separately, and replace command handlers with structured `pactl` calls. Keep existing payload fields `available`, `percent`, `muted`, and `source_plugin` unchanged.</action>
  <verify>nix develop -c cargo test -p mesh-core-scripting backend</verify>
  <acceptance_criteria>
    - `packages/plugins/backend/core/pipewire-audio/src/main.luau` does not contain `exec_shell`.
    - `packages/plugins/backend/core/pulseaudio-audio/src/main.luau` does not contain `exec_shell`.
    - `packages/plugins/backend/core/pipewire-audio/src/main.luau` contains `mesh.exec("wpctl"` or `mesh.exec('wpctl'`.
    - `packages/plugins/backend/core/pulseaudio-audio/src/main.luau` contains `mesh.exec("pactl"` or `mesh.exec('pactl'`.
    - No Rust file under `crates/core/shell` is modified by this task.
  </acceptance_criteria>
  <done>Audio providers use the structured process API without shell strings.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate network and power providers to structured exec</name>
  <files>packages/plugins/backend/core/networkmanager-network/src/main.luau, packages/plugins/backend/core/upower-power/src/main.luau</files>
  <read_first>packages/plugins/backend/core/networkmanager-network/src/main.luau, packages/plugins/backend/core/upower-power/src/main.luau, .planning/phases/03-backend-host-api-contract/03-RESEARCH.md</read_first>
  <action>Replace all `mesh.exec_shell` calls in the NetworkManager and UPower providers. For NetworkManager, use structured commands such as `mesh.exec("nmcli", {"-t", "-f", "UUID,NAME,TYPE,DEVICE,STATE,SSID,SIGNAL", "connection", "show"})`, `mesh.exec("nmcli", {"-t", "-f", "DEVICE,TYPE,STATE", "device"})`, `mesh.exec("nmcli", {"radio", "wifi"})`, `mesh.exec("nmcli", {"device", "wifi", "rescan"})`, `mesh.exec("nmcli", {"-t", "-f", "SSID,SIGNAL,BSSID", "device", "wifi", "list"})`, and `mesh.exec("bluetoothctl", {"devices", "Connected"})`. For `set_wifi_enabled`, pass `{"radio", "wifi", "on"}` or `{"radio", "wifi", "off"}` as args. For UPower, call `mesh.exec("upower", {"-i", "/org/freedesktop/UPower/devices/DisplayDevice"})` and parse `present`, `state`, `percentage`, `time to empty`, `time to full`, `energy-rate`, `energy`, and `energy-full` in Luau before emitting the same JSON-compatible fields currently produced by awk.</action>
  <verify>nix develop -c cargo test -p mesh-core-scripting backend</verify>
  <acceptance_criteria>
    - `packages/plugins/backend/core/networkmanager-network/src/main.luau` does not contain `exec_shell`.
    - `packages/plugins/backend/core/upower-power/src/main.luau` does not contain `exec_shell`.
    - `packages/plugins/backend/core/networkmanager-network/src/main.luau` contains `mesh.exec('nmcli'` or `mesh.exec("nmcli"`.
    - `packages/plugins/backend/core/networkmanager-network/src/main.luau` contains `mesh.exec('bluetoothctl'` or `mesh.exec("bluetoothctl"`.
    - `packages/plugins/backend/core/upower-power/src/main.luau` contains `mesh.exec('upower'` or `mesh.exec("upower"`.
    - `packages/plugins/backend/core/upower-power/src/main.luau` contains `time_remaining_minutes`.
  </acceptance_criteria>
  <done>Network and power providers use structured process APIs and keep parsing in Luau.</done>
</task>

<task type="auto">
  <name>Task 3: Add static and bundled-provider verification for exec_shell removal</name>
  <files>packages/plugins/backend/core/pipewire-audio/src/main.luau, packages/plugins/backend/core/pulseaudio-audio/src/main.luau, packages/plugins/backend/core/networkmanager-network/src/main.luau, packages/plugins/backend/core/upower-power/src/main.luau</files>
  <read_first>crates/core/runtime/scripting/src/backend.rs, packages/plugins/backend/core/pipewire-audio/src/main.luau, packages/plugins/backend/core/pulseaudio-audio/src/main.luau, packages/plugins/backend/core/networkmanager-network/src/main.luau, packages/plugins/backend/core/upower-power/src/main.luau</read_first>
  <action>Run the bundled backend script tests after provider migration and add any missing bundled script fixture coverage needed so provider scripts load and initialize without `mesh.exec_shell`. Do not add a Rust shim named `exec_shell`. Verify with a static grep that no provider under `packages/plugins/backend/core/*/src/main.luau` calls `mesh.exec_shell`.</action>
  <verify>nix develop -c cargo test -p mesh-core-scripting bundled_backend_scripts_expose_required_host_api_surface</verify>
  <acceptance_criteria>
    - `grep -R "mesh.exec_shell" packages/plugins/backend/core/*/src/main.luau` returns no matches.
    - `nix develop -c cargo test -p mesh-core-scripting bundled_backend_scripts_expose_required_host_api_surface` exits 0.
    - `nix develop -c cargo test -p mesh-core-scripting backend` exits 0.
  </acceptance_criteria>
  <done>Bundled provider scripts prove the public API no longer requires shell execution.</done>
</task>

</tasks>

<verification>
- Run `nix develop -c cargo test -p mesh-core-scripting backend`.
- Run `grep -R "mesh.exec_shell" packages/plugins/backend/core/*/src/main.luau` and confirm it returns no matches.
- Run `grep -R "wpctl\\|pactl\\|nmcli\\|upower" crates/core/shell crates/core/runtime -n` and confirm no service-specific command behavior was added to Rust core.
</verification>

<success_criteria>
- BHOST-01 is covered by all bundled providers using structured `mesh.exec`.
- BHOST-02 is covered by eliminating provider dependency on public `mesh.exec_shell`.
- D-05 through D-07 are visible in this plan's `must_haves.truths`.
- No service-specific Rust command behavior is introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/03-backend-host-api-contract/03-02-SUMMARY.md`
</output>
