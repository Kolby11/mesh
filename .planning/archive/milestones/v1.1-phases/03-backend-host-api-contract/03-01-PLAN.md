---
phase: 03-backend-host-api-contract
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/core/runtime/scripting/src/backend.rs
  - crates/core/runtime/scripting/src/host_api.rs
autonomous: true
requirements: [BHOST-01, BHOST-02]
requirements_addressed: [BHOST-01, BHOST-02]
user_setup: []
must_haves:
  truths:
    - "D-01: mesh.exec is strict structured-only: mesh.exec(program, args)."
    - "D-02: legacy single-string mesh.exec command splitting must be removed or rejected."
    - "D-03: mesh.exec returns result tables for process failures; scripts branch on result.success."
    - "D-04: spawn failures return success=false, stdout='', stderr with the error, and code=nil."
    - "D-05: mesh.exec_shell is not part of the MVP backend host API."
    - "D-07: BHOST-02 is addressed by context-approved removal/migration of exec_shell, not preservation as a public API."
  artifacts:
    - path: "crates/core/runtime/scripting/src/backend.rs"
      provides: "Backend Luau host API registration and exec result table contract"
      contains: "install_host_api"
    - path: "crates/core/runtime/scripting/src/host_api.rs"
      provides: "Shared host API comments that must not advertise exec_shell"
      contains: "mesh.exec"
---

<objective>
Lock the backend command execution API so `mesh.exec(program, args)` is the only public process primitive, process outcomes are stable result tables, and `mesh.exec_shell` is removed from the backend MVP surface.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-backend-host-api-contract/03-CONTEXT.md
@.planning/phases/03-backend-host-api-contract/03-RESEARCH.md
@.planning/phases/03-backend-host-api-contract/03-VALIDATION.md
@.planning/phases/03-backend-host-api-contract/03-PATTERNS.md

@crates/core/runtime/scripting/src/backend.rs
@crates/core/runtime/scripting/src/host_api.rs
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| T-03-01: string command splitting allows dynamic values to become unintended argv tokens | high | Remove/reject the legacy `mesh.exec("program args")` path and require structured args. |
| T-03-02: process failures throw and kill backend callbacks instead of returning controllable data | medium | Keep spawn and exit failures as result tables with `success=false`. |
| T-03-03: `mesh.exec_shell` remains available after docs stop mentioning it | high | Remove backend host registration and add tests asserting it is absent. |
| T-03-04: comments/tests continue advertising removed APIs | medium | Update backend host API comments and host API tests in the same plan. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Make mesh.exec require structured program and args</name>
  <files>crates/core/runtime/scripting/src/backend.rs</files>
  <read_first>crates/core/runtime/scripting/src/backend.rs, .planning/phases/03-backend-host-api-contract/03-CONTEXT.md, .planning/phases/03-backend-host-api-contract/03-PATTERNS.md</read_first>
  <action>Change the backend `mesh.exec` registration in `BackendScriptContext::install_host_api()` so the public callable requires two arguments: `program: String` and `args: Vec<String>` or an equivalent required Luau array/table argument. Remove the `Option<Vec<String>>` path that permits `mesh.exec("program arg1 arg2")`. Update `run_exec` so it no longer splits `program` with `split_whitespace`; it must call `StdCommand::new(program).args(args).output()` only. Preserve `ExecOutcome` and `exec_outcome_to_lua()` as the single result table conversion path with keys `success`, `stdout`, `stderr`, and `code`.</action>
  <verify>nix develop -c cargo test -p mesh-core-scripting backend</verify>
  <acceptance_criteria>
    - `crates/core/runtime/scripting/src/backend.rs` does not contain `split_whitespace`.
    - `crates/core/runtime/scripting/src/backend.rs` contains `StdCommand::new(program).args(args)`.
    - `crates/core/runtime/scripting/src/backend.rs` contains table keys `success`, `stdout`, `stderr`, and `code`.
    - Test `exec_accepts_program_and_args` passes.
    - A new test named `exec_rejects_single_string_command_form` exists.
  </acceptance_criteria>
  <done>`mesh.exec` cannot silently parse shell-like command strings.</done>
</task>

<task type="auto">
  <name>Task 2: Preserve structured process failure result tables</name>
  <files>crates/core/runtime/scripting/src/backend.rs</files>
  <read_first>crates/core/runtime/scripting/src/backend.rs, .planning/phases/03-backend-host-api-contract/03-CONTEXT.md</read_first>
  <action>Add or update backend host API tests proving process failures do not throw. Add a test named `exec_missing_program_returns_failure_table` using `mesh.exec("__mesh_missing_command_for_test__", {})` and assert the emitted payload has `success == false`, `stdout == ""`, a non-empty `stderr`, and `code == nil`/missing. Add a test named `exec_nonzero_exit_returns_failure_table` using `mesh.exec("sh", {"-c", "printf err >&2; exit 7"})` and assert `success == false`, `stderr == "err"`, and `code == 7`. Keep process failure behavior separate from malformed Lua API usage.</action>
  <verify>nix develop -c cargo test -p mesh-core-scripting exec_</verify>
  <acceptance_criteria>
    - `crates/core/runtime/scripting/src/backend.rs` contains `exec_missing_program_returns_failure_table`.
    - `crates/core/runtime/scripting/src/backend.rs` contains `exec_nonzero_exit_returns_failure_table`.
    - `exec_missing_program_returns_failure_table` asserts `success` is false.
    - `exec_nonzero_exit_returns_failure_table` asserts `code` is `7`.
    - `nix develop -c cargo test -p mesh-core-scripting exec_` exits 0.
  </acceptance_criteria>
  <done>Backend scripts can handle missing programs and non-zero exits through result.success.</done>
</task>

<task type="auto">
  <name>Task 3: Remove mesh.exec_shell from the backend public API surface</name>
  <files>crates/core/runtime/scripting/src/backend.rs, crates/core/runtime/scripting/src/host_api.rs</files>
  <read_first>crates/core/runtime/scripting/src/backend.rs, crates/core/runtime/scripting/src/host_api.rs, .planning/phases/03-backend-host-api-contract/03-CONTEXT.md</read_first>
  <action>Remove `mesh.set("exec_shell", ...)` from backend host API registration and remove the `run_exec_shell` helper. Update backend host API comments in `backend.rs` and `host_api.rs` so they document `mesh.exec(program, args)`, `mesh.config()`, `mesh.log`, and `mesh.service.set_poll_interval(ms)` but do not advertise `mesh.exec_shell`. Update `bundled_backend_scripts_expose_required_host_api_surface` to assert `mesh.get::<Function>("exec_shell").is_err()` and keep `mesh.exec` required. Update any inline backend tests that use `mesh.exec_shell` to use `mesh.exec`, for example replace `mesh.exec_shell("printf 'hello'")` with `mesh.exec("printf", {"hello"})`.</action>
  <verify>nix develop -c cargo test -p mesh-core-scripting backend</verify>
  <acceptance_criteria>
    - `crates/core/runtime/scripting/src/backend.rs` does not contain `mesh.set("exec_shell"`.
    - `crates/core/runtime/scripting/src/backend.rs` does not contain `fn run_exec_shell`.
    - `crates/core/runtime/scripting/src/host_api.rs` does not advertise `mesh.exec_shell`.
    - `bundled_backend_scripts_expose_required_host_api_surface` asserts `mesh.get::<Function>("exec_shell").is_err()`.
    - `nix develop -c cargo test -p mesh-core-scripting backend` exits 0.
  </acceptance_criteria>
  <done>`mesh.exec_shell` is no longer exposed as a backend MVP host API.</done>
</task>

</tasks>

<verification>
- Run `nix develop -c cargo test -p mesh-core-scripting backend`.
- Run `grep -n "split_whitespace\\|exec_shell" crates/core/runtime/scripting/src/backend.rs crates/core/runtime/scripting/src/host_api.rs` and confirm no implementation or public API references remain; an absence assertion in test text is acceptable only if deliberate.
</verification>

<success_criteria>
- BHOST-01 is covered by strict `mesh.exec(program, args)` and process result table tests.
- BHOST-02 is covered by context-approved removal of public `mesh.exec_shell` and tests proving it is absent.
- D-01 through D-07 are visible in this plan's `must_haves.truths`.
</success_criteria>

<output>
After completion, create `.planning/phases/03-backend-host-api-contract/03-01-SUMMARY.md`
</output>
