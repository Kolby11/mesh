---
phase: 06-milestone-planning-reconciliation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements: []
requirements_addressed: []
user_setup: []
must_haves:
  truths:
    - "D-01: passed verification reports and summary frontmatter are the source of truth for milestone reconciliation."
    - "D-03: the 11 stale unchecked requirements must be aligned with existing verification evidence."
    - "D-05: planning docs must stop advertising public mesh.exec_shell support after the accepted Phase 03 override."
    - "D-07: STATE.md must not claim milestone completion before audit and archival finish."
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Reconciled requirement checklist and traceability status for the shipped v1.1 scope"
      contains: "BSVC-01"
    - path: ".planning/ROADMAP.md"
      provides: "Corrected milestone narrative for the Phase 03 structured-exec override"
      contains: "mesh.exec("
    - path: ".planning/STATE.md"
      provides: "Pre-archive milestone state that no longer claims completion prematurely"
      contains: "status:"
---

<objective>
Reconcile the v1.1 planning documents with the already verified backend MVP implementation so milestone closeout can proceed without contradictory requirements, roadmap, or state narratives.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/v1.1-MILESTONE-AUDIT.md
@.planning/phases/03-backend-host-api-contract/03-VERIFICATION.md
@.planning/phases/04-service-provider-contract/04-VERIFICATION.md
@.planning/phases/05-backend-diagnostics-and-mvp-proof/05-VERIFICATION.md
@.planning/phases/06-milestone-planning-reconciliation/06-CONTEXT.md
@.planning/phases/06-milestone-planning-reconciliation/06-RESEARCH.md
@.planning/phases/06-milestone-planning-reconciliation/06-VALIDATION.md
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| T-06-01: planning files continue to contradict verified milestone evidence | high | Use the milestone audit plus phase verification reports as the required source set for every document edit. |
| T-06-02: Phase 03 wording is "corrected" by hiding the accepted override rather than documenting it accurately | medium | Preserve the override explicitly and restate the shipped contract as structured `mesh.exec(program, args)` with no public `mesh.exec_shell`. |
| T-06-03: state cleanup overreaches and implies archive completion before closeout actually happens | medium | Limit `STATE.md` changes to current workflow position and pre-archive status wording only. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Reconcile shipped requirement status in REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>.planning/v1.1-MILESTONE-AUDIT.md, .planning/phases/04-service-provider-contract/04-VERIFICATION.md, .planning/phases/05-backend-diagnostics-and-mvp-proof/05-VERIFICATION.md</read_first>
  <action>Update `REQUIREMENTS.md` so the stale unchecked shipped requirements identified by the milestone audit are marked complete. Reconcile both the checklist section and the traceability table for `BSVC-01`, `BSVC-02`, `BSVC-04`, `BSVC-05`, `BDIAG-01`, `BDIAG-02`, `BDIAG-03`, `BDIAG-04`, `BREF-01`, `BREF-02`, and `BREF-03` using the passed verification reports as the source of truth.</action>
  <verify>grep -n "BSVC-01\\|BSVC-02\\|BSVC-04\\|BSVC-05\\|BDIAG-01\\|BDIAG-02\\|BDIAG-03\\|BDIAG-04\\|BREF-01\\|BREF-02\\|BREF-03" .planning/REQUIREMENTS.md</verify>
  <acceptance_criteria>
    - `.planning/REQUIREMENTS.md` marks `BSVC-01`, `BSVC-02`, `BSVC-04`, and `BSVC-05` as complete in the checklist section.
    - `.planning/REQUIREMENTS.md` marks `BDIAG-01` through `BDIAG-04` as complete in the checklist section.
    - `.planning/REQUIREMENTS.md` marks `BREF-01` through `BREF-03` as complete in the checklist section.
    - The traceability table status for all 11 reconciled requirements is `Complete`.
  </acceptance_criteria>
  <done>`REQUIREMENTS.md` no longer contradicts the passed phase verification reports for the shipped v1.1 scope.</done>
</task>

<task type="auto">
  <name>Task 2: Reconcile the Phase 03 structured-exec override in roadmap and requirements</name>
  <files>.planning/ROADMAP.md, .planning/REQUIREMENTS.md</files>
  <read_first>.planning/phases/03-backend-host-api-contract/03-VERIFICATION.md, .planning/v1.1-MILESTONE-AUDIT.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md</read_first>
  <action>Update `ROADMAP.md` and `REQUIREMENTS.md` so they no longer describe public `mesh.exec_shell` support as the MVP backend host API. Preserve the accepted Phase 03 override explicitly by restating `BHOST-02` in terms of the shipped structured `mesh.exec(program, args)` contract and the removal of public shell-style execution from the MVP surface.</action>
  <verify>grep -n "mesh.exec_shell" .planning/ROADMAP.md .planning/REQUIREMENTS.md || true</verify>
  <acceptance_criteria>
    - `grep -n "mesh.exec_shell" .planning/ROADMAP.md .planning/REQUIREMENTS.md` returns no stale product-contract wording that advertises it as the shipped MVP API.
    - `.planning/ROADMAP.md` Phase 3 success criteria match the accepted structured-exec MVP contract.
    - `.planning/REQUIREMENTS.md` `BHOST-02` wording and traceability notes reflect the accepted override accurately.
  </acceptance_criteria>
  <done>The roadmap and requirements now describe the actual shipped Phase 03 contract rather than the superseded pre-override wording.</done>
</task>

<task type="auto">
  <name>Task 3: Normalize pre-archive milestone status in STATE.md</name>
  <files>.planning/STATE.md</files>
  <read_first>.planning/STATE.md, .planning/v1.1-MILESTONE-AUDIT.md, .planning/ROADMAP.md</read_first>
  <action>Update `STATE.md` so it no longer claims `milestone_complete` before archival is actually done. Keep the current milestone context and completed-phase history, but rewrite the top-level status and current-position wording to describe the milestone as audited and awaiting archive closeout or equivalent pre-archive wording.</action>
  <verify>grep -n "milestone_complete" .planning/STATE.md || true</verify>
  <acceptance_criteria>
    - `.planning/STATE.md` frontmatter no longer sets `status: milestone_complete`.
    - `.planning/STATE.md` current-position text no longer says milestone complete before archival.
    - `.planning/STATE.md` still preserves the v1.1 milestone reference, completed phase counts, and current closeout context.
  </acceptance_criteria>
  <done>`STATE.md` accurately reflects that v1.1 is audited and pending archival rather than already fully closed.</done>
</task>

</tasks>

<verification>
- Run `grep -n "BSVC-01\\|BSVC-02\\|BSVC-04\\|BSVC-05\\|BDIAG-01\\|BDIAG-02\\|BDIAG-03\\|BDIAG-04\\|BREF-01\\|BREF-02\\|BREF-03" .planning/REQUIREMENTS.md`.
- Run `grep -n "mesh.exec_shell" .planning/ROADMAP.md .planning/REQUIREMENTS.md || true`.
- Run `grep -n "milestone_complete" .planning/STATE.md || true`.
- Read `.planning/ROADMAP.md`, `.planning/REQUIREMENTS.md`, and `.planning/STATE.md` together and confirm they tell one consistent pre-archive v1.1 story.
</verification>

<success_criteria>
- `REQUIREMENTS.md` reflects the verified shipped status of the stale Phase 04 and Phase 05 requirements.
- `ROADMAP.md` and `REQUIREMENTS.md` describe the accepted structured-exec Phase 03 outcome accurately.
- `STATE.md` no longer claims milestone completion before archive closeout.
</success_criteria>

<output>
After completion, create `.planning/phases/06-milestone-planning-reconciliation/06-01-SUMMARY.md`
</output>
