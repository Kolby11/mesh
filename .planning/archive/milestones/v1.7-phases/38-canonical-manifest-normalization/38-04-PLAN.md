---
phase: 38-canonical-manifest-normalization
plan: 04
title: Documentation Contract And End-To-End Verification
type: execute
wave: 3
depends_on: [38-01, 38-02, 38-03]
files_modified:
  - docs/module-system.md
  - docs/module-vocabulary.md
  - .planning/REQUIREMENTS.md
  - crates/core/extension/module/src/package/tests.rs
  - crates/core/extension/module/src/manifest/tests.rs
autonomous: true
requirements: [MAN-01, MAN-02, MAN-03]
requirements_addressed: [MAN-01, MAN-02, MAN-03]
user_setup: []
must_haves:
  truths:
    - "D-14: diagnostics distinguish MESH module manifests from OS package names and resource lookup aliases."
    - "D-12: diagnostics must use replace/remove wording, not alias, synonym, or compatible name."
    - "Folded todo: preserve declared requirements, contributions, capabilities, providers, resources, and settings data so Phase 39 can index and validate them."
    - "Deferred scope: installer UX, marketplace/distribution, provider conflict resolution, resource cascade resolution, and keybind dispatch are out of Phase 38."
    - "D-01: canonical author-facing module manifest file is module.json."
    - "D-05: old package.json is an internal migration loader, not a public alias."
  artifacts:
    - path: "docs/module-system.md"
      provides: "Canonical manifest schema documentation"
      contains: "module.json"
    - path: "crates/core/extension/module/src/package/tests.rs"
      provides: "End-to-end behavior tests for Phase 38"
      contains: "replace package.json with module.json"
---

<objective>
Update the canonical author-facing manifest contract and add final end-to-end tests proving Phase 38 covers schema documentation, migration diagnostics, provider preservation, and keybind preservation without expanding into Phase 39/40 work.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-canonical-manifest-normalization/38-CONTEXT.md
@.planning/phases/38-canonical-manifest-normalization/38-RESEARCH.md
@.planning/phases/38-canonical-manifest-normalization/38-VALIDATION.md
@.planning/phases/38-canonical-manifest-normalization/38-PATTERNS.md
@docs/module-system.md
@docs/module-vocabulary.md
@.planning/REQUIREMENTS.md
@crates/core/extension/module/src/package/tests.rs
@crates/core/extension/module/src/manifest/tests.rs
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| T-38-04-01: docs keep stale `package.json plus mesh` requirement language | high | Update MAN-01 and module-system docs to name canonical `module.json` plus `mesh`. |
| T-38-04-02: diagnostics accidentally say old names are aliases | high | Add grep-verifiable docs/tests asserting `replace` and `remove` wording and no `alias` wording for old manifest names. |
| T-38-04-03: final tests prove only compile success, not behavior preservation | high | Add focused tests for active provider, layout, keybind, legacy migration warnings, and ambiguity diagnostics. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Document canonical manifest schema and fix stale MAN-01 wording</name>
  <files>docs/module-system.md, docs/module-vocabulary.md, .planning/REQUIREMENTS.md</files>
  <read_first>docs/module-system.md, docs/module-vocabulary.md, .planning/REQUIREMENTS.md, .planning/phases/38-canonical-manifest-normalization/38-CONTEXT.md</read_first>
  <action>Update `docs/module-system.md` so the manifest section says `module.json` is the canonical author-facing manifest and describes the exact target shape: top-level `name`, `version`, optional `description`, `license`, `repository`, and `mesh`; inside `mesh`, document `apiVersion`, `kind`, `capabilities`, `dependencies`, `entrypoints`, `implements`, `interface`, `contributes`, `keybinds`, `i18n`, `icons`, and `iconPack` or the current canonical field name used by the code. State that `package.json`, legacy `module.json` with `id/type/api_version`, and `mesh.toml` are internal migration inputs only. In `.planning/REQUIREMENTS.md`, rewrite MAN-01 to use `module.json plus mesh` instead of `package.json plus mesh`. In `docs/module-vocabulary.md`, update the Phase 38 inventory row status if implementation completed any listed rename.</action>
  <verify>rg -n "module.json plus `mesh`|top-level `name`|apiVersion|implements|internal migration inputs only|MAN-01.*module.json" docs/module-system.md docs/module-vocabulary.md .planning/REQUIREMENTS.md</verify>
  <acceptance_criteria>
    - `.planning/REQUIREMENTS.md` MAN-01 contains `module.json`.
    - `.planning/REQUIREMENTS.md` MAN-01 does not contain `canonical package.json`.
    - `docs/module-system.md` contains `top-level \`name\``.
    - `docs/module-system.md` contains `apiVersion`.
    - `docs/module-system.md` contains `implements`.
    - `docs/module-system.md` contains `internal migration inputs only`.
    - `docs/module-system.md` does not contain `package.json plus \`mesh\``.
  </acceptance_criteria>
  <done>Author docs and requirements name the canonical module.json schema.</done>
</task>

<task type="auto">
  <name>Task 2: Add final behavior and diagnostic regression tests</name>
  <files>crates/core/extension/module/src/package/tests.rs, crates/core/extension/module/src/manifest/tests.rs</files>
  <read_first>crates/core/extension/module/src/package/tests.rs, crates/core/extension/module/src/manifest/tests.rs, crates/core/extension/module/src/package/installed_graph.rs, crates/core/extension/module/src/manifest/load.rs, config/module.json, modules/frontend/navigation-bar/module.json, modules/backend/pipewire-audio/module.json, modules/backend/pulseaudio-audio/module.json</read_first>
  <action>Add or update tests so Phase 38 has direct behavioral proof. In package tests, assert loading `config/module.json` returns two frontend modules, two backend providers for `mesh.audio`, active provider `@mesh/pipewire-audio`, and layout entrypoint `@mesh/navigation-bar:main`. Assert duplicate `module.json` plus `package.json` returns an error containing `ambiguous module manifest`. Assert `plugin.json` returns an error containing `remove plugin.json`. Assert legacy package migration diagnostics contain `replace package.json with module.json`. In manifest tests, assert canonical `module.json` preserves keybind `mute`, `localizedTriggers` when present, and canonical backend `implements` for `mesh.audio`. Do not add tests that call old names aliases.</action>
  <verify>cargo test -p mesh-core-module package::tests && cargo test -p mesh-core-module manifest::tests</verify>
  <acceptance_criteria>
    - `crates/core/extension/module/src/package/tests.rs` contains `ambiguous module manifest`.
    - `crates/core/extension/module/src/package/tests.rs` contains `remove plugin.json`.
    - `crates/core/extension/module/src/package/tests.rs` contains `replace package.json with module.json`.
    - `crates/core/extension/module/src/package/tests.rs` contains `@mesh/pipewire-audio`.
    - `crates/core/extension/module/src/manifest/tests.rs` contains `localizedTriggers`.
    - `crates/core/extension/module/src/manifest/tests.rs` contains `implements`.
    - `cargo test -p mesh-core-module package::tests` exits 0.
    - `cargo test -p mesh-core-module manifest::tests` exits 0.
  </acceptance_criteria>
  <done>Regression tests prove canonical schema, migration diagnostics, active providers, and keybind preservation.</done>
</task>

</tasks>

<verification>
- Run `cargo fmt --all -- --check`.
- Run `cargo test -p mesh-core-module package::tests`.
- Run `cargo test -p mesh-core-module manifest::tests`.
- Run `cargo test -p mesh-core-shell shell::tests`.
- Run `cargo test --workspace`.
- Run `rg -n "alias|synonym" docs/module-system.md crates/core/extension/module/src/package/tests.rs crates/core/extension/module/src/manifest/tests.rs` and verify there are no references that describe old manifest names as public aliases or synonyms.
</verification>

<success_criteria>
- MAN-01 and docs name canonical `module.json`.
- Final tests cover canonical parsing, legacy migration warnings, ambiguity errors, provider preservation, and keybind preservation.
- Diagnostics use `replace` or `remove` wording for old manifest names.
- Full workspace tests pass before phase verification.
</success_criteria>
