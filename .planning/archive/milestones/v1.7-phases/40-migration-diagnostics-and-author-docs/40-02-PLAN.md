---
phase: 40-migration-diagnostics-and-author-docs
plan: 02
title: Author Documentation Migration Sweep
type: execute
wave: 2
depends_on: [40-01]
files_modified:
  - docs/installation.md
  - docs/font-system.md
  - docs/theming/themes.md
  - docs/theming/locales.md
  - docs/settings/README.md
  - docs/llm-context.md
  - docs/modules/frontend/core/README.md
  - docs/modules/frontend/examples/README.md
  - docs/frontend/html-css-transition.md
requirements: [MIGR-01]
requirements_addressed: [MIGR-01]
autonomous: true
user_setup: []
must_haves:
  truths:
    - "Canonical author examples use module.json with top-level name/version and mesh.apiVersion/mesh.kind."
    - "package.json, old module.json, plugin.json, and mesh.toml are not new-author targets."
    - "OS package-manager names and resource lookup aliases are not MESH module vocabulary aliases."
  artifacts:
    - path: "docs/installation.md"
      provides: "Canonical installation and manifest author docs"
      contains: "module.json"
    - path: "docs/llm-context.md"
      provides: "AI-facing context aligned with the current module model"
      contains: "canonical author-facing manifest"
---

<objective>
Update stale author-facing documentation so module authors see canonical `module.json` examples and clear migration guidance instead of old `package.json`, `mesh.toml`, `id/type/api_version`, or `provides_slots` examples as the target model.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-migration-diagnostics-and-author-docs/40-RESEARCH.md
@.planning/phases/40-migration-diagnostics-and-author-docs/40-VALIDATION.md
@.planning/phases/40-migration-diagnostics-and-author-docs/40-PATTERNS.md
@docs/module-system.md
@docs/module-vocabulary.md
@docs/installation.md
@docs/font-system.md
@docs/theming/themes.md
@docs/theming/locales.md
@docs/settings/README.md
@docs/llm-context.md
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| T-40-02-01: docs keep teaching top-level `id/type/api_version` as the new manifest shape | high | Rewrite examples to top-level `name`, `version`, and nested `mesh.apiVersion`/`mesh.kind`. |
| T-40-02-02: docs sweep corrupts package-manager or resource lookup terminology | medium | Preserve OS package names, binary package suggestions, and icon/font alias mechanics that are not MESH manifest names. |
| T-40-02-03: LLM context remains stale and misguides future agents | high | Update `docs/llm-context.md` to identify `module.json` as the canonical author-facing manifest and old docs as migration history. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite installation docs around canonical module.json</name>
  <files>docs/installation.md</files>
  <read_first>docs/installation.md, docs/module-system.md, docs/module-vocabulary.md, modules/frontend/navigation-bar/module.json, modules/backend/pipewire-audio/module.json</read_first>
  <action>Rewrite the opening manifest section of `docs/installation.md` so it says this document covers the manifest format `module.json`. Replace the `## package.json` heading with `## module.json`. Replace the large frontend example that uses top-level `id`, `type`, `api_version`, `provides_slots`, `slot_contributions`, top-level `settings`, top-level `i18n`, and top-level `assets` with a canonical example using top-level `name`, `version`, and `mesh` containing `apiVersion`, `kind`, `capabilities`, `dependencies`, `entrypoints`, `contributes.layout`, `contributes.settings`, `contributes.i18n`, and `surfaceLayout`. Keep the backend example shape that already uses `name` and `mesh`, but ensure it uses `mesh.apiVersion` and `mesh.implements`. Replace the contract example that uses `id` and `type` with a canonical interface module example using `name`, `version`, `mesh.kind = "interface"`, `mesh.interface`, and `mesh.contributes.settings`. Replace the old migration note about `package.json` replacing `mesh.toml` with wording that `package.json`, legacy `module.json` fields, and `mesh.toml` are internal migration inputs that warn and should be replaced with canonical `module.json`.</action>
  <verify>rg -n "## `module.json`|mesh.apiVersion|mesh.kind|mesh.implements|mesh.interface|internal migration inputs" docs/installation.md && ! rg -n "## `package.json`|top-level `id`|api_version|provides_slots|slot_contributions" docs/installation.md</verify>
  <acceptance_criteria>
    - `docs/installation.md` contains `## \`module.json\``.
    - `docs/installation.md` contains `mesh.apiVersion`.
    - `docs/installation.md` contains `mesh.kind`.
    - `docs/installation.md` contains `mesh.implements`.
    - `docs/installation.md` contains `mesh.interface`.
    - `docs/installation.md` contains `internal migration inputs`.
    - `rg -n "## \`package.json\`|top-level \`id\`|api_version|provides_slots|slot_contributions" docs/installation.md` exits 1.
  </acceptance_criteria>
  <done>Installation docs teach the canonical module manifest shape.</done>
</task>

<task type="auto">
  <name>Task 2: Update resource, theme, locale, and settings docs</name>
  <files>docs/font-system.md, docs/theming/themes.md, docs/theming/locales.md, docs/settings/README.md</files>
  <read_first>docs/font-system.md, docs/theming/themes.md, docs/theming/locales.md, docs/settings/README.md, docs/module-system.md, modules/icon-packs/default/module.json, modules/icon-packs/material-symbols/module.json, modules/frontend/navigation-bar/module.json</read_first>
  <action>In `docs/font-system.md`, replace `Pack manifest (package.json with mesh.kind = "font-pack")` with `Pack manifest (module.json with mesh.kind = "font-pack")` and keep the JSON example canonical under `name/version/mesh`. In `docs/theming/themes.md`, replace `mesh.kind = "theme" in package.json` with `mesh.kind = "theme" in module.json`. In `docs/theming/locales.md`, replace the module-bundled translations example tree from `package.json` to `module.json`, and replace the language-pack `mesh.toml` example with a canonical `module.json` example using `mesh.kind = "language-pack"` and `mesh.contributes.i18n`. In `docs/settings/README.md`, replace contract package examples that still show `mesh.toml` with `module.json` plus `interface.toml` and `settings.schema.json`; keep shell settings file names unchanged.</action>
  <verify>rg -n "module.json|mesh.kind|mesh.contributes|settings.schema.json" docs/font-system.md docs/theming/themes.md docs/theming/locales.md docs/settings/README.md && ! rg -n "package.json with `mesh.kind`|package.json and ships|mesh.toml" docs/font-system.md docs/theming/themes.md docs/theming/locales.md docs/settings/README.md</verify>
  <acceptance_criteria>
    - `docs/font-system.md` contains `module.json with \`mesh.kind = "font-pack"\``.
    - `docs/theming/themes.md` contains `module.json`.
    - `docs/theming/locales.md` contains `mesh.kind`.
    - `docs/theming/locales.md` contains `language-pack`.
    - `docs/settings/README.md` contains `module.json`.
    - `rg -n "package.json with \`mesh.kind\`|package.json and ships|mesh.toml" docs/font-system.md docs/theming/themes.md docs/theming/locales.md docs/settings/README.md` exits 1.
  </acceptance_criteria>
  <done>Resource, theme, locale, and settings docs use canonical module manifests.</done>
</task>

<task type="auto">
  <name>Task 3: Refresh frontend examples and LLM context for future authors</name>
  <files>docs/llm-context.md, docs/modules/frontend/core/README.md, docs/modules/frontend/examples/README.md, docs/frontend/html-css-transition.md</files>
  <read_first>docs/llm-context.md, docs/modules/frontend/core/README.md, docs/modules/frontend/examples/README.md, docs/frontend/html-css-transition.md, docs/modules/README.md, docs/module-system.md</read_first>
  <action>Update the AI-facing and frontend author docs so new modules are described as using `module.json`. In `docs/llm-context.md`, replace stale statements that modules have `package.json`, that frontend anatomy is `package.json`, and that surface layout defaults live in `package.json` with `module.json` wording; add exact phrase `canonical author-facing manifest` near the first manifest description. In `docs/modules/frontend/core/README.md`, replace `"type": "surface"` or `"widget"` in `package.json` guidance with `mesh.kind` in `module.json`. In `docs/modules/frontend/examples/README.md`, replace slot-props-in-`package.json` wording with canonical `mesh.contributes.layout` or the current module-system terminology. In `docs/frontend/html-css-transition.md`, replace `package.json.exports.component.tag` guidance with the canonical module contribution field or, if exports are not yet canonical, mark it as legacy migration wording and point authors to `module.json`.</action>
  <verify>rg -n "canonical author-facing manifest|module.json|mesh.kind|mesh.contributes" docs/llm-context.md docs/modules/frontend/core/README.md docs/modules/frontend/examples/README.md docs/frontend/html-css-transition.md && ! rg -n "frontend module anatomy \\(`package.json`\\)|Surface layout defaults live in `package.json`|package.json.exports.component.tag|slot props in `package.json`" docs/llm-context.md docs/modules/frontend/core/README.md docs/modules/frontend/examples/README.md docs/frontend/html-css-transition.md</verify>
  <acceptance_criteria>
    - `docs/llm-context.md` contains `canonical author-facing manifest`.
    - `docs/llm-context.md` contains `module.json`.
    - `docs/modules/frontend/core/README.md` contains `mesh.kind`.
    - `docs/modules/frontend/examples/README.md` contains `mesh.contributes`.
    - `docs/frontend/html-css-transition.md` contains `module.json`.
    - `rg -n "frontend module anatomy \(\`package.json\`\)|Surface layout defaults live in \`package.json\`|package.json.exports.component.tag|slot props in \`package.json\`" docs/llm-context.md docs/modules/frontend/core/README.md docs/modules/frontend/examples/README.md docs/frontend/html-css-transition.md` exits 1.
  </acceptance_criteria>
  <done>Frontend author docs and LLM context no longer teach stale package manifest examples.</done>
</task>

</tasks>

<verification>
- Run all task-level grep checks.
- Run `rg -n "package.json|mesh.toml|plugin.json|id/type/api_version|settings.keyboard.shortcuts" docs/installation.md docs/font-system.md docs/theming/themes.md docs/theming/locales.md docs/settings/README.md docs/llm-context.md docs/module-system.md docs/module-vocabulary.md` and manually confirm remaining hits are explicit migration guidance, shell settings keys, OS package-manager references, or historical context.
- Run `cargo test -p mesh-core-module package::tests` if docs wording changed examples copied from tested fixtures.
</verification>

<success_criteria>
- Author docs use canonical `module.json` and `mesh` examples.
- Remaining old manifest references are explicit replacement/removal migration guidance only.
- LLM context no longer pushes future agents toward stale `package.json` authoring.
</success_criteria>

