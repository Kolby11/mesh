# Requirements Archive: MESH v1.20 Compositor Integration

**Archived:** 2026-06-18
**Milestone:** v1.20 — Compositor Integration
**Status:** All 12 requirements satisfied ✅

---

## v1.20 Requirements (All Satisfied)

### Per-Region Damage

- [x] **DMGE-01**: Shell passes a `Vec<DamageRect>` from the retained renderer through the present path instead of a single unioned rect
- [x] **DMGE-02**: Presentation calls `wl_surface::damage_buffer` once per dirty rect (capped at 16) per frame commit
- [x] **DMGE-03**: Debug/profiling exposes damage rect count per frame alongside existing damage metrics

### HiDPI / Fractional Scale

- [x] **HDPI-01**: `SurfaceEntry` stores a `scale_factor: f64` updated from `wl_output::scale` and `wp_fractional_scale_v1` events
- [x] **HDPI-02**: `PixelBuffer` is allocated at `ceil(logical × scale_factor)` physical pixels; layout remains in logical CSS pixels
- [x] **HDPI-03**: `wp_viewporter` sets destination size to logical dimensions for correct compositing at non-integer scale ratios
- [x] **HDPI-04**: Scale factor changes trigger a surface resize and full redraw without visual glitches or stale pixels
- [x] **HDPI-05**: Integer `wl_output::scale` path is used as fallback when `wp_fractional_scale_v1` is unavailable

### Compositor Blur Offload

- [x] **BLUR-01**: Shell binds `org_kde_kwin_blur` as an optional global at startup; surfaces proceed without blur on non-KDE compositors
- [x] **BLUR-02**: For surfaces with `backdrop-filter: blur(...)` nodes, shell sends `kde_blur.set_region` + `kde_blur.commit` before `wl_surface.commit` using logical pixel coordinates
- [x] **BLUR-03**: CPU software blur is not implemented as a fallback; unsupported compositors render a flat background
- [x] **BLUR-04**: Blur region commits are skipped cleanly when no backdrop-filter nodes exist in the display list

---

## Traceability

| REQ-ID | Phase | Status | Notes |
|--------|-------|--------|-------|
| DMGE-01 | Phase 101 | ✅ Complete | Vec<DamageRect> threaded through 9 integration points |
| DMGE-02 | Phase 101 | ✅ Complete | protocol_damage_rects helper with 16-rect cap |
| DMGE-03 | Phase 101/103.1 | ✅ Complete | Path confirmed correct via effective_damage.damage_rect_count() in Phase 103.1 |
| HDPI-01 | Phase 102 | ✅ Complete | scale: f32 on SurfaceEntry, both integer and fractional handlers |
| HDPI-02 | Phase 102 | ✅ Complete | ceil(logical × scale) physical buffer allocation |
| HDPI-03 | Phase 102 | ✅ Complete | wp_viewporter set_destination for non-integer scales |
| HDPI-04 | Phase 102 | ✅ Complete | needs_full_redraw flag + render path full-damage emission |
| HDPI-05 | Phase 102 | ✅ Complete | wl_output::scale integer fallback; both protocols bound as Option |
| BLUR-01 | Phase 103 | ✅ Complete | OrgKdeKwinBlurManager bound as optional global |
| BLUR-02 | Phase 103 | ✅ Complete | set_region + commit before wl_surface.commit; CR-02 coord fix in Phase 103.1 |
| BLUR-03 | Phase 103 | ✅ Complete | apply_backdrop_filter and push_backdrop_filter_command are no-ops |
| BLUR-04 | Phase 103/103.1 | ✅ Complete | CR-01 fix: blur_committed guard clears region on backdrop-filter removal |

---

## Future Requirements (Carried Forward)

- `ext_background_effect_v1` blur protocol support — deferred until compositor adoption is confirmed
- Per-surface damage rect count telemetry in the production monitoring path — debug only for v1.20
- Widget-level opaque rect analysis (OPAQUE-02) — deferred from v1.19

---

*Archived 2026-06-18 as part of v1.20 milestone completion*
