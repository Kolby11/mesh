---
phase: 08-practical-css-coverage
plan: 04
type: execute
wave: 4
depends_on: [08-02]
files_modified:
  - crates/core/ui/elements/src/style.rs
  - crates/core/ui/component/src/parser/styles.rs
autonomous: true
requirements: [CSS-01, CSS-02, CSS-03]
requirements_addressed: [CSS-01, CSS-02, CSS-03]
user_setup: []
must_haves:
  truths:
    - "D-01: transition declarations and animation declarations needed by later phases are inside the Phase 8 practical subset."
    - "D-02: custom keyframe animation scheduling stays out of Phase 8."
    - "D-05: transition and animation declarations are priority shorthand coverage."
    - "D-06: complex browser shorthand behavior should be documented or diagnosed instead of accepted surprisingly."
    - "D-11: unsupported constructs that would change cascade or animation semantics remain errors rather than silently changing output."
  artifacts:
    - path: "crates/core/ui/elements/src/style.rs"
      provides: "Transition and animation declaration metadata in ComputedStyle"
      contains: "TransitionStyle"
    - path: "crates/core/ui/component/src/parser/styles.rs"
      provides: "Rejected @keyframes boundary until Phase 12"
      contains: "@keyframes"
---

<objective>
Extend transition parsing and add scheduler-free animation declaration metadata so later Phase 12 can build motion on a stable CSS declaration contract without Phase 8 implementing keyframes or frame scheduling.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-practical-css-coverage/08-CONTEXT.md
@.planning/phases/08-practical-css-coverage/08-RESEARCH.md
@.planning/phases/08-practical-css-coverage/08-VALIDATION.md
@.planning/phases/08-practical-css-coverage/08-PATTERNS.md

@crates/core/ui/elements/src/style.rs
@crates/core/ui/component/src/parser/styles.rs
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| T-08-06: accepting animation declarations implies unsupported animation playback | high | Store metadata only and add tests/docs stating Phase 12 owns scheduling and interpolation. |
| T-08-07: @keyframes is silently accepted but ignored | high | Keep @keyframes rejected with exact parser error until Phase 12. |
| T-08-08: comma-separated transition parsing loses supported properties | medium | Add tests for multiple transition items and the supported visual property set. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Harden transition declaration parsing for supported visual properties</name>
  <files>crates/core/ui/elements/src/style.rs</files>
  <read_first>crates/core/ui/elements/src/style.rs, .planning/phases/08-practical-css-coverage/08-RESEARCH.md</read_first>
  <action>Update transition parsing in `crates/core/ui/elements/src/style.rs` so `transition`, `transition-property`, `transition-duration`, `transition-delay`, and `transition-timing-function` handle comma-separated declaration lists predictably for the supported visual property set: `all`, `opacity`, `background`, `background-color`, `color`, `border-color`, and `border-radius`. Preserve `TransitionStyle` as scheduler metadata only. Add exact tests named `transition_shorthand_parses_comma_separated_items` and `transition_property_supports_phase_8_visual_properties`.</action>
  <verify>nix develop -c cargo test -p mesh-core-elements transition_</verify>
  <acceptance_criteria>
    - `crates/core/ui/elements/src/style.rs` contains test `transition_shorthand_parses_comma_separated_items`.
    - `crates/core/ui/elements/src/style.rs` contains test `transition_property_supports_phase_8_visual_properties`.
    - `crates/core/ui/elements/src/style.rs` contains `border_color` in transition property parsing or test expectations.
    - `TransitionStyle` remains present in `crates/core/ui/elements/src/style.rs`.
    - `nix develop -c cargo test -p mesh-core-elements transition_` exits 0.
  </acceptance_criteria>
  <done>Transition declarations cover the Phase 8 visual subset without implementing scheduling.</done>
</task>

<task type="auto">
  <name>Task 2: Add animation declaration metadata without keyframe scheduling</name>
  <files>crates/core/ui/elements/src/style.rs</files>
  <read_first>crates/core/ui/elements/src/style.rs, .planning/phases/08-practical-css-coverage/08-CONTEXT.md</read_first>
  <action>Add `AnimationStyle` or equivalent metadata to `ComputedStyle` for Phase 12 handoff. Include fields for `name`, `duration_ms`, `delay_ms`, `easing`, `iteration_count`, `direction`, `fill_mode`, and `play_state`, using simple enums where useful. Apply `animation-name`, `animation-duration`, `animation-delay`, `animation-timing-function`, `animation-iteration-count`, `animation-direction`, `animation-fill-mode`, `animation-play-state`, and a practical `animation` shorthand. Do not add a frame scheduler, interpolation engine, dirty-surface loop, or `@keyframes` model in this plan. Add exact tests named `animation_longhands_store_metadata_only` and `animation_shorthand_stores_metadata_only`.</action>
  <verify>nix develop -c cargo test -p mesh-core-elements animation_</verify>
  <acceptance_criteria>
    - `crates/core/ui/elements/src/style.rs` contains `AnimationStyle`.
    - `crates/core/ui/elements/src/style.rs` contains `pub animation`.
    - `crates/core/ui/elements/src/style.rs` contains test `animation_longhands_store_metadata_only`.
    - `crates/core/ui/elements/src/style.rs` contains test `animation_shorthand_stores_metadata_only`.
    - `grep -n "request_animation_frame\\|schedule_animation\\|interpolate_keyframe" crates/core/ui/elements/src/style.rs crates/core/ui/render/src/surface/painter.rs` returns no new Phase 8 animation scheduler implementation.
    - `nix develop -c cargo test -p mesh-core-elements animation_` exits 0.
  </acceptance_criteria>
  <done>Animation declarations are accepted as metadata for Phase 12 without scheduling behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Keep @keyframes explicitly unsupported until Phase 12</name>
  <files>crates/core/ui/component/src/parser/styles.rs, crates/core/ui/component/src/parser.rs</files>
  <read_first>crates/core/ui/component/src/parser/styles.rs, crates/core/ui/component/src/parser.rs, .planning/ROADMAP.md</read_first>
  <action>Confirm or add tests proving `@keyframes` remains unsupported in Phase 8 even though `animation-*` declarations are accepted. If Plan 01 already added `unsupported_keyframes_rule_reports_at_rule_name`, extend that test to include a comment or assertion that Phase 12 owns custom keyframe scheduling. Keep `LightningCssRule::Keyframes(_) => \"@keyframes\"` mapping. Do not lower keyframe bodies into `StyleBlock`.</action>
  <verify>nix develop -c cargo test -p mesh-core-component unsupported_keyframes_rule_reports_at_rule_name</verify>
  <acceptance_criteria>
    - `crates/core/ui/component/src/parser/styles.rs` still contains `LightningCssRule::Keyframes`.
    - A test named `unsupported_keyframes_rule_reports_at_rule_name` exists.
    - The test or adjacent comment contains `Phase 12`.
    - No file under `crates/core/ui/component/src` contains a new `Keyframe` style AST type.
    - `nix develop -c cargo test -p mesh-core-component unsupported_keyframes_rule_reports_at_rule_name` exits 0.
  </acceptance_criteria>
  <done>Animation declaration support does not accidentally accept unsupported keyframe scheduling scope.</done>
</task>

</tasks>

<verification>
- Run `nix develop -c cargo test -p mesh-core-elements transition_`.
- Run `nix develop -c cargo test -p mesh-core-elements animation_`.
- Run `nix develop -c cargo test -p mesh-core-component unsupported_keyframes_rule_reports_at_rule_name`.
- Run `grep -n "request_animation_frame\\|schedule_animation\\|interpolate_keyframe" crates/core/ui/elements/src/style.rs crates/core/ui/render/src/surface/painter.rs` and confirm no scheduler was added.
</verification>

<success_criteria>
- CSS-01 and CSS-02 include transition and animation declaration coverage.
- CSS-03 includes explicit unsupported `@keyframes` behavior until Phase 12.
- D-01, D-02, D-05, D-06, and D-11 are visible in `must_haves.truths` and enforced by tests.
</success_criteria>

<output>
After completion, create `.planning/phases/08-practical-css-coverage/08-04-SUMMARY.md`
</output>

