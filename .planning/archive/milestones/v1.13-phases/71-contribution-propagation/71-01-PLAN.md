---
phase: 71-contribution-propagation
plan: 01
title: Installed Graph Localized Text Propagation
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/core/extension/module/src/package/module_manifest.rs
  - crates/core/extension/module/src/package/installed_graph.rs
  - crates/core/extension/module/src/package/tests.rs
autonomous: true
requirements: [MGRAPH-01, MGRAPH-02, MGRAPH-03, MGRAPH-04]
requirements_addressed: [MGRAPH-01, MGRAPH-02, MGRAPH-03, MGRAPH-04]
user_setup: []
must_haves:
  truths:
    - "MGRAPH-01: Installed-graph keybind contribution records preserve LocalizedText labels, descriptions, and categories without flattening them to plain strings."
    - "MGRAPH-02: Installed-graph layout contribution labels preserve LocalizedText metadata for later shell resolution."
    - "MGRAPH-03: Settings schema descriptions that use localized-text-shaped JSON objects remain unchanged in graph settings records."
    - "MGRAPH-04: Existing graph consumers that need literal strings receive deterministic fallback text through explicit helper methods until locale-aware resolution is available."
    - "Phase 71 must not perform active-locale lookup or translate LocalizedText; Phase 72 owns runtime text resolution."
  artifacts:
    - path: "crates/core/extension/module/src/package/module_manifest.rs"
      provides: "Canonical module.json contribution manifest model"
      contains: "pub struct LayoutContribution"
    - path: "crates/core/extension/module/src/package/installed_graph.rs"
      provides: "Installed graph contribution records and indexing"
      contains: "pub struct ContributedKeybindAction"
    - path: "crates/core/extension/module/src/package/tests.rs"
      provides: "Package and contribution graph regression tests"
      contains: "contribution_index_exposes_frontend_keybind_resource_interface_and_provider_records"
---

<objective>
Complete Phase 71 by preserving localized manifest text metadata through installed-graph contribution records for keybinds and layout labels, while proving settings schema descriptions remain intact and compatibility consumers can still obtain fallback strings.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/71-contribution-propagation/71-CONTEXT.md
@.planning/phases/71-contribution-propagation/71-RESEARCH.md
@.planning/phases/71-contribution-propagation/71-VALIDATION.md
@.planning/phases/70-localized-text-manifest-model/70-01-SUMMARY.md
@crates/core/extension/module/src/manifest/model.rs
@crates/core/extension/module/src/package/module_manifest.rs
@crates/core/extension/module/src/package/installed_graph.rs
@crates/core/extension/module/src/package/tests.rs
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| T-71-01: graph indexing keeps flattening LocalizedText to fallback strings, making Phase 72 unable to resolve active-locale labels | high | Change contribution record fields to retain `LocalizedText` and add tests that assert `LocalizedText::Translation` survives graph indexing. |
| T-71-02: existing consumers break because `ContributedKeybindAction.label` and layout labels stop being `Option<String>` | high | Add explicit fallback helper methods returning `Option<&str>` and update in-repo direct string assertions to use those helpers. |
| T-71-03: layout contribution raw strings stop parsing after migrating to LocalizedText | high | Rely on Phase 70 `LocalizedText` raw-string deserialization and add raw and translated layout parsing/graph tests. |
| T-71-04: settings schema localization scope expands into a schema redesign | medium | Keep settings schema as `serde_json::Value` and only add preservation tests for localized-description objects already representable in JSON. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Preserve keybind LocalizedText in installed graph records</name>
  <files>crates/core/extension/module/src/package/installed_graph.rs, crates/core/extension/module/src/package/tests.rs</files>
  <read_first>crates/core/extension/module/src/manifest/model.rs, crates/core/extension/module/src/package/installed_graph.rs, crates/core/extension/module/src/package/tests.rs, .planning/phases/71-contribution-propagation/71-RESEARCH.md</read_first>
  <action>Change `ContributedKeybindAction.label`, `description`, and `category` in `crates/core/extension/module/src/package/installed_graph.rs` from `Option<String>` to `Option<manifest::LocalizedText>`. In `ModuleContributionIndex::index_module`, assign `action.label.clone()`, `action.description.clone()`, and `action.category.clone()` instead of mapping through `fallback_text()`. Add inherent methods on `ContributedKeybindAction` named `label_text(&self) -> Option<&str>`, `description_text(&self) -> Option<&str>`, and `category_text(&self) -> Option<&str>` that call `LocalizedText::fallback_text()` on the optional rich values. Update existing package tests that need string comparison to call these helper methods. Add a test named `contribution_index_preserves_keybind_localized_text` that constructs a frontend keybind with `LocalizedText::Translation { key: "keybind.mute.label".into(), fallback: "Mute".into() }` plus translated description and category, builds an `InstalledModuleGraph`, then asserts the graph keybind record still contains `LocalizedText::Translation` for all three fields and `label_text() == Some("Mute")`.</action>
  <verify>cargo test -p mesh-core-module contribution_index_preserves_keybind_localized_text -- --nocapture</verify>
  <acceptance_criteria>
    - `crates/core/extension/module/src/package/installed_graph.rs` contains `pub label: Option<manifest::LocalizedText>`.
    - `crates/core/extension/module/src/package/installed_graph.rs` contains `pub fn label_text(&self) -> Option<&str>`.
    - `crates/core/extension/module/src/package/installed_graph.rs` contains `action.label.clone()`.
    - `crates/core/extension/module/src/package/tests.rs` contains `contribution_index_preserves_keybind_localized_text`.
    - `crates/core/extension/module/src/package/tests.rs` contains `LocalizedText::Translation`.
    - `cargo test -p mesh-core-module contribution_index_preserves_keybind_localized_text -- --nocapture` exits 0.
  </acceptance_criteria>
  <done>Keybind contribution records retain rich localized text and expose deterministic fallback helpers.</done>
</task>

<task type="auto">
  <name>Task 2: Preserve layout LocalizedText in manifest and graph records</name>
  <files>crates/core/extension/module/src/package/module_manifest.rs, crates/core/extension/module/src/package/installed_graph.rs, crates/core/extension/module/src/package/tests.rs</files>
  <read_first>crates/core/extension/module/src/manifest/model.rs, crates/core/extension/module/src/package/module_manifest.rs, crates/core/extension/module/src/package/installed_graph.rs, crates/core/extension/module/src/package/tests.rs, .planning/phases/71-contribution-propagation/71-CONTEXT.md</read_first>
  <action>Change `LayoutContribution.label` in `crates/core/extension/module/src/package/module_manifest.rs` from `Option<String>` to `Option<manifest::LocalizedText>`. Update `ModuleManifest::from_legacy_manifest()` so legacy `package.name` values become `package.name.clone().map(manifest::LocalizedText::Literal)` when creating a layout contribution. Change `ContributedLayout.label` in `crates/core/extension/module/src/package/installed_graph.rs` from `Option<String>` to `Option<manifest::LocalizedText>`, keep indexing as `contribution.label.clone()`, and add `ContributedLayout::label_text(&self) -> Option<&str>`. Add `contribution_index_preserves_layout_localized_text`, using a frontend module with `mesh.contributes.layout[0].label = Some(LocalizedText::Translation { key: "layout.main.label".into(), fallback: "Main shell".into() })`, and assert both the rich value and `label_text() == Some("Main shell")`. Add or update a parsing assertion that a canonical layout label object `{ "t": "layout.main.label", "fallback": "Main shell" }` deserializes into `LocalizedText::Translation`.</action>
  <verify>cargo test -p mesh-core-module contribution_index_preserves_layout_localized_text -- --nocapture</verify>
  <acceptance_criteria>
    - `crates/core/extension/module/src/package/module_manifest.rs` contains `pub label: Option<manifest::LocalizedText>`.
    - `crates/core/extension/module/src/package/module_manifest.rs` contains `manifest::LocalizedText::Literal`.
    - `crates/core/extension/module/src/package/installed_graph.rs` contains `pub fn label_text(&self) -> Option<&str>`.
    - `crates/core/extension/module/src/package/tests.rs` contains `contribution_index_preserves_layout_localized_text`.
    - `cargo test -p mesh-core-module contribution_index_preserves_layout_localized_text -- --nocapture` exits 0.
  </acceptance_criteria>
  <done>Layout contribution labels retain localized metadata and keep a fallback text path.</done>
</task>

<task type="auto">
  <name>Task 3: Prove settings schema localized descriptions survive graph propagation</name>
  <files>crates/core/extension/module/src/package/tests.rs</files>
  <read_first>crates/core/extension/module/src/package/module_manifest.rs, crates/core/extension/module/src/package/installed_graph.rs, crates/core/extension/module/src/package/tests.rs, .planning/phases/71-contribution-propagation/71-RESEARCH.md</read_first>
  <action>Add a test named `contribution_index_preserves_settings_schema_localized_descriptions` in `crates/core/extension/module/src/package/tests.rs`. The test must construct a frontend module with `MeshContributes.settings = Some(SettingsContribution { namespace: "@mesh/example-widget".into(), schema: serde_json::json!({ "type": "object", "properties": { "mode": { "type": "string", "description": { "t": "settings.mode.description", "fallback": "Theme mode" } } } }) })`, build an `InstalledModuleGraph`, and assert that `graph.settings_schemas()[0].schema["properties"]["mode"]["description"]["t"] == "settings.mode.description"` and `["fallback"] == "Theme mode"`. Do not add a new typed settings schema model in this phase.</action>
  <verify>cargo test -p mesh-core-module contribution_index_preserves_settings_schema_localized_descriptions -- --nocapture</verify>
  <acceptance_criteria>
    - `crates/core/extension/module/src/package/tests.rs` contains `contribution_index_preserves_settings_schema_localized_descriptions`.
    - `crates/core/extension/module/src/package/tests.rs` contains `settings.mode.description`.
    - `crates/core/extension/module/src/package/tests.rs` contains `Theme mode`.
    - `cargo test -p mesh-core-module contribution_index_preserves_settings_schema_localized_descriptions -- --nocapture` exits 0.
  </acceptance_criteria>
  <done>Settings schema localized-description objects are explicitly protected by graph regression coverage.</done>
</task>

<task type="auto">
  <name>Task 4: Run package safety verification and update phase artifacts</name>
  <files>.planning/phases/71-contribution-propagation/71-VALIDATION.md, .planning/phases/71-contribution-propagation/71-01-SUMMARY.md, .planning/STATE.md, .planning/ROADMAP.md</files>
  <read_first>.planning/phases/71-contribution-propagation/71-01-PLAN.md, .planning/phases/71-contribution-propagation/71-VALIDATION.md, .planning/STATE.md, .planning/ROADMAP.md</read_first>
  <action>Run focused tests for Tasks 1-3, then run `cargo test -p mesh-core-module package -- --nocapture`, `cargo fmt`, and `git diff --check`. Mark the validation rows in `71-VALIDATION.md` according to the command outcomes. Write `71-01-SUMMARY.md` using the project summary style with frontmatter status `complete`, one-line summary, changed files, tests run, and any follow-up notes. Update `.planning/STATE.md` and `.planning/ROADMAP.md` to record Phase 71 completion only if verification passes.</action>
  <verify>cargo test -p mesh-core-module package -- --nocapture</verify>
  <acceptance_criteria>
    - `.planning/phases/71-contribution-propagation/71-01-SUMMARY.md` exists.
    - `.planning/phases/71-contribution-propagation/71-VALIDATION.md` no longer contains `pending` for T-71 rows after successful verification.
    - `.planning/STATE.md` contains `Phase 71 completed`.
    - `.planning/ROADMAP.md` marks Phase 71 complete.
    - `cargo test -p mesh-core-module package -- --nocapture` exits 0.
    - `cargo fmt` exits 0.
    - `git diff --check` exits 0.
  </acceptance_criteria>
  <done>Phase 71 implementation is verified, documented, and marked complete.</done>
</task>

</tasks>

<verification>
- Run `cargo test -p mesh-core-module contribution_index_preserves_keybind_localized_text -- --nocapture`.
- Run `cargo test -p mesh-core-module contribution_index_preserves_layout_localized_text -- --nocapture`.
- Run `cargo test -p mesh-core-module contribution_index_preserves_settings_schema_localized_descriptions -- --nocapture`.
- Run `cargo test -p mesh-core-module package -- --nocapture`.
- Run `cargo fmt`.
- Run `git diff --check`.
</verification>

<success_criteria>
- Keybind contribution labels, descriptions, and categories retain `LocalizedText` metadata.
- Layout contribution labels retain `LocalizedText` metadata.
- Settings schema localized description objects survive graph propagation unchanged.
- Existing fallback consumers have deterministic helper methods.
- MGRAPH-01, MGRAPH-02, MGRAPH-03, and MGRAPH-04 are all covered.
</success_criteria>
