---
phase: 72-runtime-text-resolution
plan: 01
title: Shell Runtime Localized Manifest Text Resolution
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/core/foundation/debug/src/lib.rs
  - crates/core/shell/src/shell/component/runtime.rs
  - crates/core/shell/src/shell/component/input/keyboard.rs
  - crates/core/shell/src/shell/runtime/debug.rs
  - crates/core/shell/src/shell/component/tests/interaction/navigation.rs
  - crates/core/shell/src/shell/tests.rs
autonomous: true
requirements: [MRES-01, MRES-02, MRES-03, MRES-04]
requirements_addressed: [MRES-01, MRES-02, MRES-03, MRES-04]
user_setup: []
must_haves:
  truths:
    - "MRES-01: Shell runtime metadata resolves LocalizedText::Translation through active locale, fallback locale, then required fallback text."
    - "MRES-02: Debug keybind metadata exposes resolved user-facing text and source key information."
    - "MRES-03: Accessibility/debug shortcut metadata receives resolved labels/descriptions rather than raw translation keys."
    - "MRES-04: Missing translation keys produce non-fatal diagnostics with module id, field path, key, and fallback."
    - "Phase 72 must not migrate bundled manifests or author docs; Phase 73 owns shipped proof and documentation."
  artifacts:
    - path: "crates/core/shell/src/shell/component/runtime.rs"
      provides: "Script-facing module descriptor metadata"
      contains: "module_descriptor_from_manifest"
    - path: "crates/core/shell/src/shell/component/input/keyboard.rs"
      provides: "Resolved keybind and debug keybind metadata"
      contains: "debug_surface_keybinds"
    - path: "crates/core/foundation/debug/src/lib.rs"
      provides: "Debug snapshot data model"
      contains: "DebugKeybindEntry"
---

<objective>
Complete Phase 72 by resolving localized manifest keybind text at shell runtime descriptor and debug metadata boundaries, with fallback text and missing-key diagnostics.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/72-runtime-text-resolution/72-CONTEXT.md
@.planning/phases/72-runtime-text-resolution/72-RESEARCH.md
@.planning/phases/72-runtime-text-resolution/72-VALIDATION.md
@.planning/phases/71-contribution-propagation/71-01-SUMMARY.md
@crates/core/shell/src/shell/component/runtime.rs
@crates/core/shell/src/shell/component/input/keyboard.rs
@crates/core/foundation/debug/src/lib.rs
@crates/core/shell/src/shell/runtime/debug.rs
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| T-72-01: runtime metadata still exposes raw translation keys | high | Add descriptor and debug metadata tests asserting resolved strings. |
| T-72-02: missing translations break component creation | high | Always return `fallback_text()` and record diagnostics non-fatally. |
| T-72-03: source key details are lost during resolution | medium | Add additive `*_key` and `*_fallback` metadata fields. |
| T-72-04: debug metadata schema expansion breaks shell debug JSON/tests | medium | Update `DebugKeybindEntry`, JSON serialization, and shell debug tests together. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Resolve LocalizedText in script-facing keybind descriptors</name>
  <files>crates/core/shell/src/shell/component/runtime.rs, crates/core/shell/src/shell/component/tests/interaction/navigation.rs</files>
  <read_first>crates/core/shell/src/shell/component/runtime.rs, crates/core/foundation/locale/src/lib.rs, crates/core/extension/module/src/manifest/model.rs, crates/core/shell/src/shell/component/tests/interaction/navigation.rs, .planning/phases/72-runtime-text-resolution/72-RESEARCH.md</read_first>
  <action>Replace the pure `module_descriptor_from_manifest(manifest)` helper with a `FrontendSurfaceComponent` helper that resolves each keybind `LocalizedText` field through `self.locale`. For every keybind descriptor, set `label`, `description`, and `category` to resolved strings. For translation values, add additive metadata fields named `label_key`, `label_fallback`, `description_key`, `description_fallback`, `category_key`, and `category_fallback` when applicable. If `LocaleEngine::translate(key)` returns `None`, use `LocalizedText::fallback_text()` and record a degraded diagnostic containing the module id, field path, key, and fallback. Add tests named `manifest_descriptor_resolves_keybind_localized_text` and `manifest_descriptor_missing_translation_uses_fallback_and_diagnostic`.</action>
  <verify>cargo test -p mesh-core-shell manifest_descriptor_resolves_keybind_localized_text -- --nocapture && cargo test -p mesh-core-shell manifest_descriptor_missing_translation_uses_fallback_and_diagnostic -- --nocapture</verify>
  <acceptance_criteria>
    - `crates/core/shell/src/shell/component/runtime.rs` contains `label_key`.
    - `crates/core/shell/src/shell/component/runtime.rs` contains `missing localized manifest text`.
    - `crates/core/shell/src/shell/component/tests/interaction/navigation.rs` contains `manifest_descriptor_resolves_keybind_localized_text`.
    - `crates/core/shell/src/shell/component/tests/interaction/navigation.rs` contains `manifest_descriptor_missing_translation_uses_fallback_and_diagnostic`.
    - `cargo test -p mesh-core-shell manifest_descriptor_resolves_keybind_localized_text -- --nocapture` exits 0.
  </acceptance_criteria>
  <done>Script-facing keybind manifest metadata exposes resolved text plus source metadata.</done>
</task>

<task type="auto">
  <name>Task 2: Add resolved manifest text to debug keybind metadata</name>
  <files>crates/core/foundation/debug/src/lib.rs, crates/core/shell/src/shell/component/input/keyboard.rs, crates/core/shell/src/shell/runtime/debug.rs, crates/core/shell/src/shell/component/tests/interaction/navigation.rs, crates/core/shell/src/shell/tests.rs</files>
  <read_first>crates/core/foundation/debug/src/lib.rs, crates/core/shell/src/shell/component/input/keyboard.rs, crates/core/shell/src/shell/runtime/debug.rs, crates/core/shell/src/shell/component/tests/interaction/navigation.rs, crates/core/shell/src/shell/tests.rs</read_first>
  <action>Add optional fields to `DebugKeybindEntry`: `label`, `description`, `category`, `label_key`, `description_key`, and `category_key`. Populate them in `FrontendSurfaceComponent::debug_surface_keybinds()` by resolving the matching manifest action text through the same runtime helper used for descriptors. Update `keybind_entry_json()` to serialize the new fields. Update the shell debug test fixture to include `None` for the new fields. Add or update a test named `keybind_debug_metadata_includes_resolved_manifest_text` that sets a localized keybind label, loads translations, and asserts debug metadata has `label == Some("Stlmit")` and `label_key == Some("keybind.mute.label")` while keeping `accessibility_shortcut == "Control+m"`.</action>
  <verify>cargo test -p mesh-core-shell keybind_debug_metadata_includes_resolved_manifest_text -- --nocapture</verify>
  <acceptance_criteria>
    - `crates/core/foundation/debug/src/lib.rs` contains `pub label: Option<String>`.
    - `crates/core/shell/src/shell/runtime/debug.rs` contains `"label_key": entry.label_key`.
    - `crates/core/shell/src/shell/component/input/keyboard.rs` contains `label_key`.
    - `crates/core/shell/src/shell/component/tests/interaction/navigation.rs` contains `keybind_debug_metadata_includes_resolved_manifest_text`.
    - `cargo test -p mesh-core-shell keybind_debug_metadata_includes_resolved_manifest_text -- --nocapture` exits 0.
  </acceptance_criteria>
  <done>Debug keybind metadata includes resolved manifest text and source translation keys.</done>
</task>

<task type="auto">
  <name>Task 3: Verify shell runtime resolution safety</name>
  <files>.planning/phases/72-runtime-text-resolution/72-VALIDATION.md, .planning/phases/72-runtime-text-resolution/72-01-SUMMARY.md, .planning/phases/72-runtime-text-resolution/72-VERIFICATION.md, .planning/STATE.md, .planning/ROADMAP.md</files>
  <read_first>.planning/phases/72-runtime-text-resolution/72-01-PLAN.md, .planning/phases/72-runtime-text-resolution/72-VALIDATION.md, .planning/STATE.md, .planning/ROADMAP.md</read_first>
  <action>Run the focused descriptor/debug tests, then run `cargo check -p mesh-core-shell`, `cargo fmt`, and `git diff --check`. Mark `72-VALIDATION.md` rows passed only after commands pass. Write `72-01-SUMMARY.md` and `72-VERIFICATION.md` with status `passed`, and update STATE/ROADMAP/REQUIREMENTS to record Phase 72 completion.</action>
  <verify>cargo check -p mesh-core-shell</verify>
  <acceptance_criteria>
    - `.planning/phases/72-runtime-text-resolution/72-01-SUMMARY.md` exists.
    - `.planning/phases/72-runtime-text-resolution/72-VERIFICATION.md` contains `status: passed`.
    - `.planning/ROADMAP.md` marks Phase 72 complete.
    - `cargo check -p mesh-core-shell` exits 0.
    - `git diff --check` exits 0.
  </acceptance_criteria>
  <done>Runtime text resolution is verified and phase artifacts are complete.</done>
</task>

</tasks>

<verification>
- Run `cargo test -p mesh-core-shell manifest_descriptor_resolves_keybind_localized_text -- --nocapture`.
- Run `cargo test -p mesh-core-shell manifest_descriptor_missing_translation_uses_fallback_and_diagnostic -- --nocapture`.
- Run `cargo test -p mesh-core-shell keybind_debug_metadata_includes_resolved_manifest_text -- --nocapture`.
- Run `cargo check -p mesh-core-shell`.
- Run `cargo fmt`.
- Run `git diff --check`.
</verification>

<success_criteria>
- Runtime keybind descriptors expose resolved manifest text.
- Missing translation keys fall back and produce non-fatal diagnostics.
- Debug keybind metadata includes resolved text and source key information.
- Existing shortcut resolution and accessibility shortcut strings remain unchanged.
- MRES-01, MRES-02, MRES-03, and MRES-04 are covered.
</success_criteria>
