---
phase: 70-localized-text-manifest-model
plan: 01
title: Localized Text Manifest Model
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/core/extension/module/src/manifest/model.rs
  - crates/core/extension/module/src/manifest/tests.rs
  - crates/core/extension/module/src/package/installed_graph.rs
  - crates/core/extension/module/src/package/module_manifest.rs
  - crates/core/extension/module/src/package/tests.rs
  - crates/core/extension/module/src/lib.rs
autonomous: true
requirements: [MI18N-01, MI18N-02, MI18N-03, MI18N-04]
requirements_addressed: [MI18N-01, MI18N-02, MI18N-03, MI18N-04]
user_setup: []
must_haves:
  truths:
    - "MI18N-01: Raw JSON strings in localized-capable manifest fields are literal text, not implicit translation keys."
    - "MI18N-01: Localized manifest text uses the structured object shape { \"t\": \"...\", \"fallback\": \"...\" }."
    - "MI18N-02: Localized text declarations with empty translation keys or empty fallback strings must fail manifest validation."
    - "MI18N-03: Existing raw-string keybind label, description, and category manifests must continue to parse."
    - "MI18N-04: Dotted raw strings such as keybind.mute.label in localized-capable keybind fields must load but emit warning diagnostics with a concrete migration suggestion."
    - "Phase 70 must not implement shell locale resolution, installed-graph rich propagation, bundled manifest migration, or author docs; those belong to phases 71-73."
  artifacts:
    - path: "crates/core/extension/module/src/manifest/model.rs"
      provides: "Normalized runtime manifest types and keybind validation"
      contains: "pub struct KeybindAction"
    - path: "crates/core/extension/module/src/package/module_manifest.rs"
      provides: "Canonical module.json model and validation entry point"
      contains: "pub struct MeshModuleSection"
    - path: "crates/core/extension/module/src/package/installed_graph.rs"
      provides: "Canonical loader diagnostics and installed graph compatibility path"
      contains: "LoadedModuleManifest"
    - path: "crates/core/extension/module/src/manifest/tests.rs"
      provides: "Runtime manifest parser tests"
      contains: "parses_module_json_keybind_display_keys"
    - path: "crates/core/extension/module/src/package/tests.rs"
      provides: "Canonical module.json loader and diagnostic tests"
      contains: "ModuleManifestDiagnosticSeverity"
---

<objective>
Complete Phase 70 by adding the reusable localized text manifest model and diagnostics foundation so `module.json` authors can explicitly distinguish literal text from localized catalog lookup while existing raw-string manifests remain compatible.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/70-localized-text-manifest-model/70-RESEARCH.md
@.planning/phases/70-localized-text-manifest-model/70-VALIDATION.md
@.planning/spikes/004-manifest-localized-text-contract/README.md
@crates/core/extension/module/src/manifest/model.rs
@crates/core/extension/module/src/manifest/tests.rs
@crates/core/extension/module/src/package/module_manifest.rs
@crates/core/extension/module/src/package/installed_graph.rs
@crates/core/extension/module/src/package/tests.rs
@crates/core/extension/module/src/lib.rs
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| T-70-01: malformed localized text objects enter the manifest model and later render as empty labels | high | Validate non-empty `t` and `fallback` values and add tests for both failures. |
| T-70-02: existing raw-string manifests break when keybind fields switch to localized text | high | Deserialize raw strings as `LocalizedText::Literal`, keep fallback helpers, and preserve the existing keybind display metadata test. |
| T-70-03: raw dotted translation keys keep looking localized but are never resolved | medium | Emit non-fatal `ModuleManifestDiagnostic` warnings for suspicious raw dotted keybind text fields. |
| T-70-04: Phase 70 overreaches into runtime locale resolution | medium | Keep Phase 70 scoped to schema, parser, compatibility helpers, diagnostics, and tests; leave resolution to Phase 72. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add LocalizedText schema and focused parser tests</name>
  <files>crates/core/extension/module/src/manifest/model.rs, crates/core/extension/module/src/manifest/tests.rs, crates/core/extension/module/src/lib.rs</files>
  <read_first>crates/core/extension/module/src/manifest/model.rs, crates/core/extension/module/src/manifest/tests.rs, crates/core/extension/module/src/lib.rs, .planning/phases/70-localized-text-manifest-model/70-RESEARCH.md, .planning/spikes/004-manifest-localized-text-contract/README.md</read_first>
  <action>Add a public `LocalizedText` type to `crates/core/extension/module/src/manifest/model.rs` with two variants: `Literal(String)` for raw JSON strings and `Translation { key: String, fallback: String }` for JSON objects shaped as `{ "t": "...", "fallback": "..." }`. Implement custom `Deserialize` so strings become `Literal` and objects with keys `t` and `fallback` become `Translation`. Derive or implement `Debug`, `Clone`, `PartialEq`, `Eq`, and `Serialize`. Add methods named `fallback_text(&self) -> &str`, `translation_key(&self) -> Option<&str>`, `validate(&self, field_path: &str) -> Result<(), String>`, and `is_suspicious_raw_i18n_key(&self) -> bool`. The suspicious-key heuristic must return true only for `Literal` values that contain `.` and contain no whitespace. Re-export `LocalizedText` from `crates/core/extension/module/src/lib.rs`. Add tests in `crates/core/extension/module/src/manifest/tests.rs` whose names start with `manifest_localized_text_`: `manifest_localized_text_raw_string_is_literal`, `manifest_localized_text_object_is_translation`, `manifest_localized_text_empty_key_is_rejected`, and `manifest_localized_text_empty_fallback_is_rejected`.</action>
  <verify>cargo test -p mesh-core-module manifest_localized_text -- --nocapture</verify>
  <acceptance_criteria>
    - `crates/core/extension/module/src/manifest/model.rs` contains `pub enum LocalizedText`.
    - `crates/core/extension/module/src/manifest/model.rs` contains `Translation {`.
    - `crates/core/extension/module/src/manifest/model.rs` contains `fallback_text(&self) -> &str`.
    - `crates/core/extension/module/src/manifest/model.rs` contains `is_suspicious_raw_i18n_key(&self) -> bool`.
    - `crates/core/extension/module/src/lib.rs` contains `LocalizedText`.
    - `crates/core/extension/module/src/manifest/tests.rs` contains `manifest_localized_text_raw_string_is_literal`.
    - `crates/core/extension/module/src/manifest/tests.rs` contains `manifest_localized_text_object_is_translation`.
    - `cargo test -p mesh-core-module manifest_localized_text -- --nocapture` exits 0.
  </acceptance_criteria>
  <done>The reusable localized text schema parses literal and translated manifest text and rejects empty localized declarations.</done>
</task>

<task type="auto">
  <name>Task 2: Apply LocalizedText to keybind display fields without breaking raw strings</name>
  <files>crates/core/extension/module/src/manifest/model.rs, crates/core/extension/module/src/manifest/tests.rs, crates/core/extension/module/src/package/module_manifest.rs, crates/core/extension/module/src/package/installed_graph.rs, crates/core/extension/module/src/package/tests.rs</files>
  <read_first>crates/core/extension/module/src/manifest/model.rs, crates/core/extension/module/src/manifest/tests.rs, crates/core/extension/module/src/package/module_manifest.rs, crates/core/extension/module/src/package/installed_graph.rs, crates/core/extension/module/src/package/tests.rs, .planning/phases/70-localized-text-manifest-model/70-RESEARCH.md</read_first>
  <action>Change `KeybindAction.label`, `KeybindAction.description`, and `KeybindAction.category` in `crates/core/extension/module/src/manifest/model.rs` from `Option<String>` to `Option<LocalizedText>`. Update `KeybindAction::validate` so it calls `LocalizedText::validate` with exact field paths `mesh.keybinds.{action_id}.label`, `mesh.keybinds.{action_id}.description`, and `mesh.keybinds.{action_id}.category`. Keep raw string manifests compatible by making existing raw values deserialize as `LocalizedText::Literal`. Add a helper method on `KeybindAction` or `LocalizedText` so compatibility consumers can get fallback text as `String`; update `crates/core/extension/module/src/package/installed_graph.rs` so `ContributedKeybindAction.label`, `description`, and `category` still receive `Option<String>` fallback text for Phase 70. Update tests that construct `KeybindAction` manually in `crates/core/extension/module/src/package/tests.rs` and shell-facing fixtures only as needed by wrapping literals with `LocalizedText::Literal(...)` or by using the fallback helper. Update `parses_module_json_keybind_display_keys` to assert that the raw label is `LocalizedText::Literal(\"keybind.mute.label\".into())`, and add a new test asserting an object label parses as `LocalizedText::Translation { key: \"keybind.mute.label\".into(), fallback: \"Mute\".into() }`.</action>
  <verify>cargo test -p mesh-core-module parses_module_json_keybind_display_keys -- --nocapture && cargo test -p mesh-core-module manifest_localized_text -- --nocapture</verify>
  <acceptance_criteria>
    - `crates/core/extension/module/src/manifest/model.rs` contains `pub label: Option<LocalizedText>`.
    - `crates/core/extension/module/src/manifest/model.rs` contains `pub description: Option<LocalizedText>`.
    - `crates/core/extension/module/src/manifest/model.rs` contains `pub category: Option<LocalizedText>`.
    - `crates/core/extension/module/src/manifest/tests.rs` contains `LocalizedText::Literal`.
    - `crates/core/extension/module/src/manifest/tests.rs` contains `LocalizedText::Translation`.
    - `crates/core/extension/module/src/package/installed_graph.rs` still contains `pub label: Option<String>` for `ContributedKeybindAction`.
    - `cargo test -p mesh-core-module parses_module_json_keybind_display_keys -- --nocapture` exits 0.
    - `cargo test -p mesh-core-module manifest_localized_text -- --nocapture` exits 0.
  </acceptance_criteria>
  <done>Keybind display fields accept localized text objects while raw-string keybind manifests and existing string consumers keep working.</done>
</task>

<task type="auto">
  <name>Task 3: Emit canonical loader diagnostics for suspicious raw i18n keys</name>
  <files>crates/core/extension/module/src/manifest/model.rs, crates/core/extension/module/src/package/installed_graph.rs, crates/core/extension/module/src/package/tests.rs</files>
  <read_first>crates/core/extension/module/src/manifest/model.rs, crates/core/extension/module/src/package/error.rs, crates/core/extension/module/src/package/installed_graph.rs, crates/core/extension/module/src/package/tests.rs, modules/frontend/navigation-bar/module.json, .planning/phases/70-localized-text-manifest-model/70-RESEARCH.md</read_first>
  <action>Add a diagnostics collection path for suspicious raw keybind text. Implement a method named `localized_text_diagnostics` or `keybind_text_diagnostics` on `KeybindsSection` or `MeshModuleSection` that returns warning records for each `LocalizedText::Literal` in `mesh.keybinds.<action_id>.label`, `.description`, and `.category` where `is_suspicious_raw_i18n_key()` is true. In `crates/core/extension/module/src/package/installed_graph.rs`, when loading canonical `module.json` in `load_module_manifest`, append these warnings to `LoadedModuleManifest.diagnostics`. Each warning must use `ModuleManifestDiagnostic::warning`, set `module_id` to the loaded module name, set `field_path` to the exact field path such as `mesh.keybinds.mute.label`, set `message` to include `looks like an i18n key`, and set `suggested_action` to include `{ \"t\": \"keybind.mute.label\", \"fallback\": \"...\" }`. Add package tests named `module_manifest_loader_warns_for_raw_dotted_keybind_label` and `module_manifest_loader_does_not_warn_for_literal_keybind_label`. The first test should parse a canonical module with `label: \"keybind.mute.label\"` and assert one warning with field path `mesh.keybinds.mute.label`; the second should use `label: \"Mute\"` and assert diagnostics are empty.</action>
  <verify>cargo test -p mesh-core-module module_manifest_loader_warns_for_raw_dotted_keybind_label -- --nocapture && cargo test -p mesh-core-module module_manifest_loader_does_not_warn_for_literal_keybind_label -- --nocapture</verify>
  <acceptance_criteria>
    - `crates/core/extension/module/src/package/installed_graph.rs` contains `looks like an i18n key`.
    - `crates/core/extension/module/src/package/installed_graph.rs` contains `mesh.keybinds`.
    - `crates/core/extension/module/src/package/tests.rs` contains `module_manifest_loader_warns_for_raw_dotted_keybind_label`.
    - `crates/core/extension/module/src/package/tests.rs` contains `module_manifest_loader_does_not_warn_for_literal_keybind_label`.
    - `crates/core/extension/module/src/package/tests.rs` contains `mesh.keybinds.mute.label`.
    - `cargo test -p mesh-core-module module_manifest_loader_warns_for_raw_dotted_keybind_label -- --nocapture` exits 0.
    - `cargo test -p mesh-core-module module_manifest_loader_does_not_warn_for_literal_keybind_label -- --nocapture` exits 0.
  </acceptance_criteria>
  <done>Canonical module loading reports non-fatal migration diagnostics for raw dotted keybind text while literal labels remain quiet.</done>
</task>

</tasks>

<verification>
- Run `cargo test -p mesh-core-module manifest_localized_text -- --nocapture`.
- Run `cargo test -p mesh-core-module parses_module_json_keybind_display_keys -- --nocapture`.
- Run `cargo test -p mesh-core-module module_manifest_loader_warns_for_raw_dotted_keybind_label -- --nocapture`.
- Run `cargo test -p mesh-core-module module_manifest_loader_does_not_warn_for_literal_keybind_label -- --nocapture`.
- Run `cargo test -p mesh-core-module manifest -- --nocapture`.
- Run `cargo fmt`.
- Run `git diff --check`.
</verification>

<success_criteria>
- `LocalizedText` exists as a reusable public manifest model.
- Raw strings in keybind text fields parse as literal text.
- `{ "t": "...", "fallback": "..." }` parses as localized text.
- Empty localized text keys or fallbacks fail validation.
- Existing raw-string keybind manifests still parse.
- Canonical module loading emits non-fatal diagnostics for suspicious raw dotted keybind text fields.
- MI18N-01, MI18N-02, MI18N-03, and MI18N-04 are all covered by the plan.
</success_criteria>
