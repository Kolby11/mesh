---
phase: 98-narrow-invalidation-event-routing
plan: "03"
type: execute
wave: 3
depends_on: ["98-01", "98-02"]
files_modified:
  - crates/core/shell/src/shell/component/tests/invalidation/profiling.rs
  - crates/core/shell/src/shell/component/rendering.rs
autonomous: true
requirements: [INV-04, INV-05]

must_haves:
  truths:
    - "A `fnv_hash_buffer(&PixelBuffer) -> u64` helper exists in profiling.rs using inline FNV-1a with OFFSET=14695981039346656037 and PRIME=1099511628211"
    - "Five pixel equivalence tests exist (hover, open/close, slider, traversal, backend-update); each renders the same scenario twice — once with narrow-invalidation enabled, once forced to TREE_REBUILD — and asserts equal FNV hashes"
    - "Five profiling benchmark tests show the narrow-invalidation render path produces snapshot.narrow_path == true AND snapshot.affected_node_count < total_node_count for at least one canonical scenario (backend-update)"
    - "Existing PHASE31_PROOF / PHASE26_BASELINE logging lines include narrow_path and affected_node_count in their format strings"
    - "Profiling assertions document reduced retained-tree churn: snapshot.component.script_narrow > 0 on backend-update scenario; snapshot.full_rebuild == false on narrow-eligible scenarios"
  artifacts:
    - path: "crates/core/shell/src/shell/component/tests/invalidation/profiling.rs"
      provides: "fnv_hash_buffer helper, five pixel equivalence tests, five benchmark profiling tests, extended PHASE31_PROOF format with narrow_path + affected_node_count"
      contains: "fnv_hash_buffer"
    - path: "crates/core/shell/src/shell/component/rendering.rs"
      provides: "(if needed) ensure ProfilingInvalidationSnapshot population sets narrow_path and affected_node_count on the narrow path — fix any gap discovered when running the profiling benchmarks"
      contains: "narrow_path"
  key_links:
    - from: "phase98_pixel_equivalence_{scenario} tests"
      to: "fnv_hash_buffer(PixelBuffer)"
      via: "hash baseline vs narrow render outputs and assert_eq!"
      pattern: "fnv_hash_buffer"
    - from: "phase98_narrow_invalidation_reduces_churn"
      to: "ProfilingInvalidationSnapshot.narrow_path + affected_node_count"
      via: "take_invalidation_snapshot() after backend-update render"
      pattern: "snapshot.narrow_path"
---

<objective>
Add the FNV-1a pixel-equivalence helper, the five canonical pixel
equivalence tests (hover, open/close, slider, traversal, backend-update), and
the five profiling benchmark assertions that prove reduced dirty-node churn
under the narrow invalidation paths added by Plans 01 and 02. Extend the
existing `PHASE31_PROOF` / `PHASE26_BASELINE` logging lines to include
`narrow_path` and `affected_node_count` so profiling output reflects the new
Phase 98 fields. If the narrow path implemented in Plan 01 fails to populate
`narrow_path` or `affected_node_count` on the snapshot for any benchmark
scenario, fix the gap in `rendering.rs`.

Purpose: Satisfies INV-04 (profiling payloads show reduced dirty-node counts
across canonical benchmarks) and INV-05 (pixel-identical output via FNV hash
equivalence on all five benchmark scenarios).

Output: Extended `tests/invalidation/profiling.rs` with FNV helper, five
pixel-equivalence tests, five profiling benchmarks, updated proof-log format
lines. Optional rendering.rs fix if a profiling field-population gap is
discovered.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/98-narrow-invalidation-event-routing/98-CONTEXT.md
@.planning/phases/98-narrow-invalidation-event-routing/98-RESEARCH.md
@.planning/phases/98-narrow-invalidation-event-routing/98-PATTERNS.md
@.planning/phases/98-narrow-invalidation-event-routing/98-01-SUMMARY.md
@.planning/phases/98-narrow-invalidation-event-routing/98-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: FNV-1a pixel hash helper + five pixel equivalence tests (INV-05)</name>
  <read_first>
    crates/core/shell/src/shell/component/tests/invalidation/profiling.rs,
    crates/core/shell/src/shell/component/tests/common.rs,
    crates/core/shell/src/shell/component/runtime_tree.rs
  </read_first>
  <behavior>
    - A free `fn fnv_hash_buffer(buffer: &PixelBuffer) -> u64` lives in `profiling.rs` (or its companion `mod tests::invalidation::common`) using inline FNV-1a with constants OFFSET=14695981039346656037 and PRIME=1099511628211 over `&buffer.data` bytes — matches the existing `RuntimeTreeHasher` pattern in `runtime_tree.rs:224`.
    - Five `#[test]` functions exist:
        - `phase98_pixel_equivalence_hover`
        - `phase98_pixel_equivalence_open_close`
        - `phase98_pixel_equivalence_slider`
        - `phase98_pixel_equivalence_traversal`
        - `phase98_pixel_equivalence_backend_update`
    - Each test renders the same scenario twice into two `PixelBuffer`s: (a) baseline path with the narrow invalidation paths force-disabled (e.g., by calling `invalidate_script_state()` instead of the narrow variant, or by emitting a payload diff with no template-node intersection that nonetheless triggers Lua-side `needs_rebuild`); (b) narrow path enabled (use the normal `handle_service_event` flow that Plan 02 wired). Hash both buffers via `fnv_hash_buffer()` and `assert_eq!(baseline_hash, narrow_hash, "pixel equivalence: {scenario}")`.
    - Tests use `real_frontend_module_component("@mesh/navigation-bar", audio_network_catalog())` from `common.rs` for backend-update scenarios; for hover/open-close/slider/traversal, choose a shipped module that exercises the relevant interaction (`@mesh/navigation-bar` for hover/traversal, `@mesh/audio-popover` for open/close/slider — adjust based on the actual shipped fixtures available in `common.rs`).
  </behavior>
  <action>
    In `crates/core/shell/src/shell/component/tests/invalidation/profiling.rs`: (1) Add `fn fnv_hash_buffer(buffer: &PixelBuffer) -> u64` as a private free function near the top of the file, with constants `OFFSET = 14695981039346656037u64` and `PRIME = 1099511628211u64`, iterating `&buffer.data` with the FNV-1a inner loop (`hash ^= u64::from(*byte); hash = hash.wrapping_mul(PRIME);`). Use `std::collections::DefaultHasher` is FORBIDDEN — its seed is process-randomized. (2) Add the five `phase98_pixel_equivalence_*` tests. For each scenario, structure the test as: (a) construct two identical fresh components (`baseline_component`, `narrow_component`) via `real_frontend_module_component(...)`; (b) drive each through the same scenario sequence (mock pointer enter for hover, surface activate for open_close, multiple payload increments for slider, focus traversal for traversal, audio.percent payload update for backend-update); (c) for `baseline_component`, before each step call `baseline_component.invalidate_script_state()` directly to force the TREE_REBUILD path (bypassing the narrow detection); for `narrow_component`, use the normal `handle_service_event` / interaction flow; (d) call `paint(&theme, W, H, &mut buffer)` on each, hash with `fnv_hash_buffer`, and `assert_eq!(baseline_hash, narrow_hash, "scenario={}", "{scenario_name}")`. Use the existing mount/paint pattern from `phase26_real_surface_baseline_emits_canonical_proof_measurements` (around lines 72-95) as the template. For scenarios where no shipped surface exists for that interaction, scope the test to the closest analog and document it in a `// NOTE:` comment.
  </action>
  <verify>
    <automated>cargo test -p mesh-core-shell shell::component::tests::invalidation::profiling::phase98_pixel_equivalence -- --nocapture 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'fn fnv_hash_buffer' crates/core/shell/src/shell/component/tests/invalidation/profiling.rs` returns 1.
    - `grep -c '14695981039346656037' crates/core/shell/src/shell/component/tests/invalidation/profiling.rs` returns 1.
    - `grep -c '1099511628211' crates/core/shell/src/shell/component/tests/invalidation/profiling.rs` returns 1.
    - `grep -c 'DefaultHasher' crates/core/shell/src/shell/component/tests/invalidation/profiling.rs` returns 0.
    - `grep -c '^fn phase98_pixel_equivalence_hover\|^fn phase98_pixel_equivalence_open_close\|^fn phase98_pixel_equivalence_slider\|^fn phase98_pixel_equivalence_traversal\|^fn phase98_pixel_equivalence_backend_update' crates/core/shell/src/shell/component/tests/invalidation/profiling.rs` returns 5.
    - All five `phase98_pixel_equivalence_*` tests pass.
  </acceptance_criteria>
  <done>FNV-1a helper added; five pixel equivalence tests pass with assert_eq! on baseline vs narrow buffer hashes across all five canonical scenarios.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Profiling benchmark assertions + extended proof-log format (INV-04)</name>
  <read_first>
    crates/core/shell/src/shell/component/tests/invalidation/profiling.rs,
    crates/core/foundation/debug/src/lib.rs,
    crates/core/shell/src/shell/component/rendering.rs
  </read_first>
  <behavior>
    - Five `#[test]` benchmark functions exist (one per canonical scenario from INV-04), or a single `phase98_narrow_invalidation_reduces_churn` test that exercises all five and emits a `PHASE98_PROOF` log line per scenario.
    - Each benchmark renders its scenario via the normal (narrow-enabled) path, calls `take_invalidation_snapshot()`, and asserts:
        - For backend-update (audio.percent change): `snapshot.narrow_path == true` AND `snapshot.full_rebuild == false` AND `snapshot.component.script_narrow > 0` AND `snapshot.affected_node_count > 0` AND `snapshot.affected_node_count < (total node count of the surface tree)`.
        - For hover scenario: assertion that the existing retained-style path still runs (`snapshot.retained_path == true`) — narrow path is service-event-driven, hover uses the Plan 96 selector path. Document this in a comment.
        - For open/close, slider, traversal: at minimum, assert the surface produced a non-zero paint snapshot and document which dirty path was exercised.
    - Existing `log_phase31_proof()` (or equivalent helper around lines 46-70 in profiling.rs) is extended to include `narrow_path={} affected_node_count={}` in the eprintln format string, populated from `snapshot.narrow_path` and `snapshot.affected_node_count`.
    - If the benchmark reveals that the narrow path does not actually populate `snapshot.narrow_path` or `snapshot.affected_node_count` correctly, fix the population in `rendering.rs` (Plan 01 implementation gap). Acceptable to modify rendering.rs in this plan ONLY for the snapshot field population — not for narrow path logic itself.
  </behavior>
  <action>
    In `crates/core/shell/src/shell/component/tests/invalidation/profiling.rs`: (1) Add (or extend the existing) `log_phase31_proof()` or write a new `log_phase98_proof()` helper that includes `narrow_path={} affected_node_count={}` alongside the existing `retained={} full_rebuild={}` format fields. Use snapshot fields populated by Plan 01. (2) Add the test function `phase98_narrow_invalidation_reduces_churn` that mounts `real_frontend_module_component("@mesh/navigation-bar", audio_network_catalog())`, enables profiling, performs an audio.percent service event via `handle_service_event`, paints, then asserts on the snapshot: `assert!(snapshot.narrow_path, "narrow_path must be true after audio.percent narrow update");` `assert!(!snapshot.full_rebuild, "full_rebuild must be false on narrow path");` `assert!(snapshot.component.script_narrow > 0, "script_narrow count must be incremented");` `assert!(snapshot.affected_node_count > 0 && snapshot.affected_node_count < /* tree node count e.g. fetched from compiled surface */, "affected_node_count must be > 0 and < total");`. (3) Add four scenario-specific log helpers or extend the proof helper with a `scenario: &str` parameter so each benchmark emits a comparable line. (4) Update `log_phase31_proof` callers (in existing tests like `phase26_real_surface_baseline_emits_canonical_proof_measurements`) so the new format string compiles — pass `snapshot.narrow_path` and `snapshot.affected_node_count` to the formatter. (5) If after running the test, `snapshot.narrow_path` is `false` on the backend-update path, diagnose by inspecting `rendering.rs::narrow_script_update()` and ensure the function sets `self.profiling_invalidation_snapshot_in_progress.narrow_path = true` and `.affected_node_count = expanded.len() as u64` (use the exact field path used by existing snapshot population — match the pattern of how `retained_path` and `full_rebuild` are set in `rendering.rs` / `shell_component.rs`). Modify `rendering.rs` only as needed to make the assertions pass.
  </action>
  <verify>
    <automated>cargo test -p mesh-core-shell shell::component::tests::invalidation::profiling -- --nocapture 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'fn phase98_narrow_invalidation_reduces_churn' crates/core/shell/src/shell/component/tests/invalidation/profiling.rs` returns 1.
    - `grep -c 'narrow_path=' crates/core/shell/src/shell/component/tests/invalidation/profiling.rs` returns at least 1 (proof log format includes the field).
    - `grep -c 'affected_node_count' crates/core/shell/src/shell/component/tests/invalidation/profiling.rs` returns at least 2 (assertion + log line).
    - Test `phase98_narrow_invalidation_reduces_churn` passes.
    - All five `phase98_pixel_equivalence_*` tests from Task 1 still pass.
    - Existing tests in `tests/invalidation/profiling.rs` (e.g., `phase26_real_surface_baseline_emits_canonical_proof_measurements`) still pass — `cargo test -p mesh-core-shell shell::component::tests::invalidation::profiling` exits 0 in full.
    - Full workspace check still compiles: `cargo check --workspace` exits 0.
  </acceptance_criteria>
  <done>Profiling benchmark asserts narrow_path == true and affected_node_count > 0 on backend-update; proof log format extended with new fields; all existing profiling tests still pass; full invalidation suite green.</done>
</task>

</tasks>

<verification>
  <automated>cargo test -p mesh-core-shell shell::component::tests::invalidation -- --nocapture 2>&1 | tail -40</automated>
  <automated>cargo test --workspace 2>&1 | tail -10</automated>
</verification>

<success_criteria>
- FNV-1a `fnv_hash_buffer` helper present with documented constants matching `RuntimeTreeHasher`.
- Five `phase98_pixel_equivalence_*` tests pass with `assert_eq!(baseline_hash, narrow_hash)` per scenario.
- `phase98_narrow_invalidation_reduces_churn` test passes with narrow_path == true and affected_node_count > 0 on the backend-update scenario.
- Proof log format includes `narrow_path={} affected_node_count={}`.
- All five canonical scenarios (hover, open/close, slider, traversal, backend-update) produce identical pixel output between baseline (full rebuild) and narrow paths.
- `cargo test --workspace` passes.
- `ProfilingInvalidationSnapshot.narrow_path` and `.affected_node_count` are populated by the narrow path.
</success_criteria>

<output>
Create `.planning/phases/98-narrow-invalidation-event-routing/98-03-SUMMARY.md` when done.
</output>
