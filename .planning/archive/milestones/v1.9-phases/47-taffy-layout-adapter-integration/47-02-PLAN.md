---
phase: 47-taffy-layout-adapter-integration
plan: 02
title: Taffy Backed LayoutEngine Replacement
type: execute
wave: 2
depends_on: [47-01]
files_modified:
  - crates/core/ui/elements/src/layout.rs
  - crates/core/ui/elements/src/style/types.rs
autonomous: true
requirements: [LAYT-01, LAYT-03]
requirements_addressed: [LAYT-01, LAYT-03]
user_setup: []
must_haves:
  truths:
    - "D-01: Replace relevant current layout engine code with Taffy for in-scope layout behavior."
    - "D-02: Phase 47 is a production replacement for layout."
    - "D-04: Unsupported Taffy cases are diagnostics/blockers, not silent old-engine fallbacks."
    - "D-07: Stable NodeId, runtime keys, retained dirty categories, and retained render-object geometry synchronization are hard gates."
    - "D-09: Layout diagnostics distinguish Taffy mapping gaps from paint, text, presentation, and shell lifecycle problems."
  artifacts:
    - path: "crates/core/ui/elements/src/layout.rs"
      provides: "Taffy-backed LayoutEngine implementation"
      contains: "compute_taffy_layout"
    - path: "crates/core/ui/elements/src/layout.rs"
      provides: "Taffy style conversion for MESH computed styles"
      contains: "taffy_style_for_node"
  key_links:
    - "WidgetNode tree -> Taffy tree -> WidgetNode.layout writeback"
    - "TextMeasurer -> Taffy measure closure"
    - "Taffy diagnostics -> tracing warnings, no old-engine fallback"
---

<objective>
Replace the current custom `LayoutEngine` computation with a Taffy-backed implementation that preserves the existing public layout API, MESH `NodeId` identity, text measurement injection, and retained geometry writeback.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/47-taffy-layout-adapter-integration/47-CONTEXT.md
@.planning/phases/47-taffy-layout-adapter-integration/47-RESEARCH.md
@.planning/phases/47-taffy-layout-adapter-integration/47-VALIDATION.md
@.planning/phases/47-taffy-layout-adapter-integration/47-PATTERNS.md
@crates/core/ui/elements/src/layout.rs
@crates/core/ui/elements/src/tree.rs
@crates/core/ui/elements/src/style/types.rs
@crates/core/shell/src/shell/component/rendering.rs
@crates/core/frontend/render/src/render_object.rs
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| T-47-02-01: Taffy node handles replace MESH `NodeId` and break retained identity | high | Maintain an internal map from MESH `NodeId` to transient Taffy nodes and write results back to the existing `WidgetNode.layout` fields. |
| T-47-02-02: text measurement semantics regress because Taffy receives no measure closure | high | Route text leaves through `compute_layout_with_measure` or equivalent Taffy measurement and call the existing injected `TextMeasurer`. |
| T-47-02-03: unsupported mappings silently call the old `layout_node` implementation | high | Delete or bypass old custom layout recursion from production paths; record diagnostics through `TaffyLayoutReport` instead of old-engine fallback. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Build Taffy style conversion and tree mapping</name>
  <files>crates/core/ui/elements/src/layout.rs</files>
  <read_first>crates/core/ui/elements/src/layout.rs, crates/core/ui/elements/src/tree.rs, crates/core/ui/elements/src/style/types.rs, .planning/phases/47-taffy-layout-adapter-integration/47-RESEARCH.md, /home/kolby/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/taffy-0.10.1/examples/measure.rs</read_first>
  <action>Add Taffy conversion helpers in `crates/core/ui/elements/src/layout.rs`: `fn taffy_dimension(dimension: Dimension) -> taffy::style::Dimension`, `fn taffy_length_percentage(value: Option<f32>) -> taffy::style::LengthPercentageAuto`, `fn taffy_style_for_node(node: &WidgetNode, report: &mut TaffyLayoutReport) -> taffy::style::Style`, and `fn build_taffy_tree(node: &WidgetNode, tree: &mut taffy::TaffyTree<NodeId>, report: &mut TaffyLayoutReport) -> Result<taffy::prelude::NodeId, taffy::TaffyError>`. Map MESH `Dimension::Px` to Taffy length, `Dimension::Percent` to percent, `Dimension::Auto` to auto, and `Dimension::Content` to auto plus a diagnostic reason containing `content dimension mapped through Taffy measurement`. Map `FlexDirection::Row` and `FlexDirection::Column`, `flex_grow`, `flex_shrink`, `flex_basis`, `gap`, `padding`, `margin`, `align_items`, `align_self`, `justify_content`, and absolute positioning insets. Preserve `Display::None` by assigning zero layout during writeback rather than creating a visible Taffy flow node.</action>
  <verify>env XDG_CACHE_HOME=/tmp/codex-nix-cache nix develop -c cargo test -p mesh-core-elements taffy_layout</verify>
  <acceptance_criteria>
    - `crates/core/ui/elements/src/layout.rs` contains `fn taffy_style_for_node`.
    - `crates/core/ui/elements/src/layout.rs` contains `fn build_taffy_tree`.
    - `crates/core/ui/elements/src/layout.rs` contains `content dimension mapped through Taffy measurement`.
    - `crates/core/ui/elements/src/layout.rs` contains `taffy::TaffyTree<NodeId>`.
    - `crates/core/ui/elements/src/layout.rs` contains mappings for `FlexDirection::Row` and `FlexDirection::Column`.
    - `env XDG_CACHE_HOME=/tmp/codex-nix-cache nix develop -c cargo test -p mesh-core-elements taffy_layout` exits 0.
  </acceptance_criteria>
  <done>MESH computed styles can be converted into a Taffy tree keyed by MESH node identity.</done>
</task>

<task type="auto">
  <name>Task 2: Replace LayoutEngine compute path with Taffy writeback</name>
  <files>crates/core/ui/elements/src/layout.rs</files>
  <read_first>crates/core/ui/elements/src/layout.rs, crates/core/shell/src/shell/component/rendering.rs, crates/core/frontend/render/src/render_object.rs, .planning/phases/47-taffy-layout-adapter-integration/47-CONTEXT.md</read_first>
  <action>Add `pub fn compute_taffy_layout(root: &mut WidgetNode, available_width: f32, available_height: f32, measurer: Option<&dyn TextMeasurer>) -> TaffyLayoutReport` to `LayoutEngine`. Make `compute`, `compute_with_measurer`, and `compute_with_intrinsic_cache_and_measurer` call this Taffy-backed function. Keep the existing public function signatures intact; `compute_with_intrinsic_cache_and_measurer` should accept the `IntrinsicLayoutCache` argument for API stability, but Taffy becomes the production geometry source. Implement Taffy result writeback so every visible `WidgetNode.layout` receives `x`, `y`, `width`, and `height` from Taffy. For diagnostics in returned reports, emit `tracing::warn!(target: "mesh::layout", ...)` from the public compute methods. Remove production calls to the old recursive `layout_node` path; if helper code remains temporarily for tests, it must not be invoked by `LayoutEngine::compute*`.</action>
  <verify>env XDG_CACHE_HOME=/tmp/codex-nix-cache nix develop -c cargo test -p mesh-core-elements simple_row_layout column_with_gap padding_insets_children</verify>
  <acceptance_criteria>
    - `crates/core/ui/elements/src/layout.rs` contains `pub fn compute_taffy_layout`.
    - `LayoutEngine::compute(` calls `compute_taffy_layout`.
    - `LayoutEngine::compute_with_measurer(` calls `compute_taffy_layout`.
    - `LayoutEngine::compute_with_intrinsic_cache_and_measurer(` calls `compute_taffy_layout`.
    - `crates/core/ui/elements/src/layout.rs` contains `target: "mesh::layout"`.
    - `rg -n "layout_node\\(" crates/core/ui/elements/src/layout.rs` shows no call from `LayoutEngine::compute`, `LayoutEngine::compute_with_measurer`, or `LayoutEngine::compute_with_intrinsic_cache_and_measurer`.
    - `env XDG_CACHE_HOME=/tmp/codex-nix-cache nix develop -c cargo test -p mesh-core-elements simple_row_layout column_with_gap padding_insets_children` exits 0.
  </acceptance_criteria>
  <done>`LayoutEngine` public entrypoints now use Taffy as the production geometry source.</done>
</task>

<task type="auto">
  <name>Task 3: Preserve text measurement and retained geometry behavior</name>
  <files>crates/core/ui/elements/src/layout.rs</files>
  <read_first>crates/core/ui/elements/src/layout.rs, crates/core/ui/elements/src/tree.rs, crates/core/frontend/render/src/render_object.rs, /home/kolby/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/taffy-0.10.1/examples/measure.rs</read_first>
  <action>Wire text leaf measurement into Taffy using the existing `TextMeasurer` trait. For nodes where `node.tag == "text"` and no children exist, Taffy measurement must read `attributes["content"]`, `font_family`, `font_size`, `font_weight`, and `line_height`, and pass an optional max width when Taffy provides a definite available width. Add or update tests so a `CountingMeasurer` proves text measurement is called during Taffy layout, changing text content changes measured width or height, and the same `WidgetNode.id` remains unchanged before and after layout. Add a retained geometry test that updates a child layout through Taffy and then verifies `RenderObjectTree` reports geometry dirtiness using the existing render-object API if that API is accessible from current test modules; otherwise add a `LayoutRect` writeback test in `mesh-core-elements` and leave render-object proof to Plan 03 shell/render tests.</action>
  <verify>env XDG_CACHE_HOME=/tmp/codex-nix-cache nix develop -c cargo test -p mesh-core-elements intrinsic_cache_reuses_probe_layouts_across_passes</verify>
  <acceptance_criteria>
    - `crates/core/ui/elements/src/layout.rs` contains `node.tag == "text"`.
    - `crates/core/ui/elements/src/layout.rs` contains `measure_text(`.
    - `crates/core/ui/elements/src/layout.rs` contains `CountingMeasurer`.
    - `crates/core/ui/elements/src/layout.rs` contains an assertion that a node id before layout equals the same node id after layout.
    - `env XDG_CACHE_HOME=/tmp/codex-nix-cache nix develop -c cargo test -p mesh-core-elements intrinsic_cache_reuses_probe_layouts_across_passes` exits 0, or the test is renamed to reflect Taffy measurement while preserving a focused `intrinsic` test filter that exits 0.
  </acceptance_criteria>
  <done>Taffy replacement preserves text measurement injection and MESH node identity during layout writeback.</done>
</task>

</tasks>

<verification>
- Run `env XDG_CACHE_HOME=/tmp/codex-nix-cache nix develop -c cargo test -p mesh-core-elements taffy_layout`.
- Run `env XDG_CACHE_HOME=/tmp/codex-nix-cache nix develop -c cargo test -p mesh-core-elements layout`.
- Run `env XDG_CACHE_HOME=/tmp/codex-nix-cache nix develop -c cargo check -p mesh-core-shell`.
- Run `rg -n "pub fn compute_taffy_layout|fn taffy_style_for_node|target: \"mesh::layout\"" crates/core/ui/elements/src/layout.rs`.
</verification>

<success_criteria>
- `LayoutEngine` production entrypoints use Taffy for geometry.
- Text leaf measurement still goes through `TextMeasurer`.
- MESH `NodeId` identity is preserved.
- Unsupported Taffy mapping gaps are diagnostics/blockers, not old-engine fallbacks.
</success_criteria>

