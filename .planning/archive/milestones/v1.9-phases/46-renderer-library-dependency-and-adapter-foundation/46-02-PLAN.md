---
phase: 46-renderer-library-dependency-and-adapter-foundation
plan: 02
title: Renderer Library Adapter Status Seam
type: execute
wave: 2
depends_on: [46-01]
files_modified:
  - crates/core/frontend/render/src/library_adapters.rs
  - crates/core/frontend/render/src/lib.rs
autonomous: true
requirements: [LIBS-02]
requirements_addressed: [LIBS-02]
user_setup: []
must_haves:
  truths:
    - "D-05: Current renderer behavior remains default for build and runtime."
    - "D-06: Explicit Cargo features control dependency fan-out."
    - "D-07: Feature flags are paired with adapter-level rollback once behavior exists."
    - "D-08: Phase 46 code stays in or below crates/core/frontend/render."
    - "D-09: FocusedProofSnapshot and crate-facing conversion modules are adapter-owned migration evidence, not public API."
    - "D-10: Phase 46 defines adapter seam and dependency gates only; real library behavior belongs to Phases 47-50."
    - "D-14: Audio popover transition-delay work is not part of Phase 46."
    - "D-15: Module install requirement-resolution work is not part of Phase 46."
  artifacts:
    - path: "crates/core/frontend/render/src/library_adapters.rs"
      provides: "Internal feature-status and rollback seam for renderer libraries"
      contains: "CURRENT_RENDERER_AUTHORITY"
    - path: "crates/core/frontend/render/src/lib.rs"
      provides: "Render crate module/export registration"
      contains: "pub mod library_adapters;"
  key_links:
    - "renderer feature flags -> renderer_library_statuses()"
    - "renderer_library_rollback_authority() -> current MESH software renderer authority"
---

<objective>
Create a small internal adapter-status seam in `mesh-core-render` so later phases can discover enabled renderer-library paths and a fixed rollback authority without changing layout, text, paint, accessibility, shell, presentation, or author-facing behavior.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-renderer-library-dependency-and-adapter-foundation/46-CONTEXT.md
@.planning/phases/46-renderer-library-dependency-and-adapter-foundation/46-RESEARCH.md
@.planning/phases/46-renderer-library-dependency-and-adapter-foundation/46-VALIDATION.md
@.planning/phases/46-renderer-library-dependency-and-adapter-foundation/46-PATTERNS.md
@crates/core/frontend/render/src/lib.rs
@crates/core/frontend/render/src/proof.rs
@crates/core/frontend/render/src/display_list.rs
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| T-46-02-01: status seam becomes behavior switch and changes current rendering | high | Limit the new module to status records, feature detection, and rollback authority strings; do not call it from painter/layout/text execution paths. |
| T-46-02-02: feature status is untestable under enabled and disabled builds | medium | Add tests that compare status booleans to `cfg!(feature = "...")` for every feature. |
| T-46-02-03: adapter seam leaks into `.mesh` author contract | medium | Keep module in `mesh-core-render`, expose only internal Rust helpers, and document public non-effect in Plan 03. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add internal renderer library status module</name>
  <files>crates/core/frontend/render/src/library_adapters.rs</files>
  <read_first>crates/core/frontend/render/src/proof.rs, crates/core/frontend/render/src/display_list.rs, crates/core/frontend/render/src/lib.rs, crates/core/frontend/render/Cargo.toml, .planning/phases/46-renderer-library-dependency-and-adapter-foundation/46-CONTEXT.md</read_first>
  <action>Create `crates/core/frontend/render/src/library_adapters.rs`. Define `pub const CURRENT_RENDERER_AUTHORITY: &str = "mesh-software-renderer";`, `pub const RENDERER_LIBRARY_STATUS_COUNT: usize = 5;`, and a `#[derive(Debug, Clone, Copy, PartialEq, Eq)] pub struct RendererLibraryStatus { pub id: &'static str, pub feature: &'static str, pub role: &'static str, pub enabled: bool, pub default_authority: &'static str }`. Add `pub fn renderer_library_statuses() -> [RendererLibraryStatus; RENDERER_LIBRARY_STATUS_COUNT]` returning exactly these records: id `taffy`, feature `renderer-taffy`, role `layout`; id `parley`, feature `renderer-parley`, role `text`; id `accesskit`, feature `renderer-accesskit`, role `accessibility`; id `anyrender`, feature `renderer-anyrender`, role `paint-experimental`; id `vello_encoding`, feature `renderer-vello-encoding`, role `paint-encoding-experimental`. Each record's `enabled` value must use `cfg!(feature = "...")`. Each record's `default_authority` must be `CURRENT_RENDERER_AUTHORITY`. Add `pub fn renderer_library_rollback_authority() -> &'static str { CURRENT_RENDERER_AUTHORITY }`. Do not import or call painter, layout, text, AccessKit runtime, shell, or presentation code.</action>
  <verify>env XDG_CACHE_HOME=/tmp/codex-nix-cache nix develop -c cargo test -p mesh-core-render renderer_library</verify>
  <acceptance_criteria>
    - `crates/core/frontend/render/src/library_adapters.rs` contains `CURRENT_RENDERER_AUTHORITY`.
    - `crates/core/frontend/render/src/library_adapters.rs` contains `RendererLibraryStatus`.
    - `crates/core/frontend/render/src/library_adapters.rs` contains `renderer_library_statuses`.
    - `crates/core/frontend/render/src/library_adapters.rs` contains `cfg!(feature = "renderer-taffy")`.
    - `crates/core/frontend/render/src/library_adapters.rs` contains `cfg!(feature = "renderer-vello-encoding")`.
    - `crates/core/frontend/render/src/library_adapters.rs` does not contain calls to `paint_frontend_tree`, `RetainedDisplayList::`, or `FrontendSurfaceComponent`.
  </acceptance_criteria>
  <done>The render crate has an internal status seam for enabled renderer-library features and current rollback authority.</done>
</task>

<task type="auto">
  <name>Task 2: Export the internal adapter seam from mesh-core-render</name>
  <files>crates/core/frontend/render/src/lib.rs</files>
  <read_first>crates/core/frontend/render/src/lib.rs, crates/core/frontend/render/src/library_adapters.rs</read_first>
  <action>Update `crates/core/frontend/render/src/lib.rs` to add `pub mod library_adapters;`. Add a `pub use library_adapters::{renderer_library_rollback_authority, renderer_library_statuses, RendererLibraryStatus, CURRENT_RENDERER_AUTHORITY};` block near the existing `pub use proof::{...}` exports. Do not change existing proof, render object, display list, or surface exports.</action>
  <verify>env XDG_CACHE_HOME=/tmp/codex-nix-cache nix develop -c cargo check -p mesh-core-render</verify>
  <acceptance_criteria>
    - `crates/core/frontend/render/src/lib.rs` contains `pub mod library_adapters;`.
    - `crates/core/frontend/render/src/lib.rs` contains `renderer_library_statuses`.
    - `crates/core/frontend/render/src/lib.rs` still contains `pub mod proof;`.
    - `crates/core/frontend/render/src/lib.rs` still contains `build_focused_proof_snapshot`.
    - `env XDG_CACHE_HOME=/tmp/codex-nix-cache nix develop -c cargo check -p mesh-core-render` exits 0.
  </acceptance_criteria>
  <done>The render crate exports the internal adapter status seam for later v1.9 phases.</done>
</task>

<task type="auto">
  <name>Task 3: Add status and rollback tests for disabled and enabled feature builds</name>
  <files>crates/core/frontend/render/src/library_adapters.rs</files>
  <read_first>crates/core/frontend/render/src/library_adapters.rs, crates/core/frontend/render/Cargo.toml, .planning/phases/46-renderer-library-dependency-and-adapter-foundation/46-VALIDATION.md</read_first>
  <action>Add a `#[cfg(test)] mod tests` block to `library_adapters.rs`. Add a test named `renderer_library_statuses_track_feature_flags` that builds a map or iterates the returned array and asserts: there are 5 records; `taffy.enabled == cfg!(feature = "renderer-taffy")`; `parley.enabled == cfg!(feature = "renderer-parley")`; `accesskit.enabled == cfg!(feature = "renderer-accesskit")`; `anyrender.enabled == cfg!(feature = "renderer-anyrender")`; `vello_encoding.enabled == cfg!(feature = "renderer-vello-encoding")`. Add a second test named `renderer_library_rollback_authority_stays_mesh_software_renderer` that asserts `renderer_library_rollback_authority() == "mesh-software-renderer"` and every status record has `default_authority == "mesh-software-renderer"`. Keep test names containing `renderer_library` so the validation filter works.</action>
  <verify>env XDG_CACHE_HOME=/tmp/codex-nix-cache nix develop -c cargo test -p mesh-core-render renderer_library</verify>
  <acceptance_criteria>
    - `crates/core/frontend/render/src/library_adapters.rs` contains `renderer_library_statuses_track_feature_flags`.
    - `crates/core/frontend/render/src/library_adapters.rs` contains `renderer_library_rollback_authority_stays_mesh_software_renderer`.
    - `env XDG_CACHE_HOME=/tmp/codex-nix-cache nix develop -c cargo test -p mesh-core-render renderer_library` exits 0.
    - `env XDG_CACHE_HOME=/tmp/codex-nix-cache nix develop -c cargo test -p mesh-core-render --features renderer-libraries renderer_library` exits 0.
    - `env XDG_CACHE_HOME=/tmp/codex-nix-cache nix develop -c cargo test -p mesh-core-render proof` exits 0.
  </acceptance_criteria>
  <done>Adapter status and rollback tests pass with feature flags disabled and enabled, while existing focused proof tests still pass.</done>
</task>

</tasks>

<verification>
- Run `env XDG_CACHE_HOME=/tmp/codex-nix-cache nix develop -c cargo test -p mesh-core-render renderer_library`.
- Run `env XDG_CACHE_HOME=/tmp/codex-nix-cache nix develop -c cargo test -p mesh-core-render --features renderer-libraries renderer_library`.
- Run `env XDG_CACHE_HOME=/tmp/codex-nix-cache nix develop -c cargo test -p mesh-core-render proof`.
- Run `rg -n "paint_frontend_tree|RetainedDisplayList::|FrontendSurfaceComponent" crates/core/frontend/render/src/library_adapters.rs` and verify no output.
</verification>

<success_criteria>
- Later phases have a concrete internal seam to inspect enabled renderer-library features.
- The seam records the current software renderer as rollback authority.
- Default renderer behavior remains unchanged.
- Existing focused proof tests still pass.
</success_criteria>
