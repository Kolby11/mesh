---
phase: 46-renderer-library-dependency-and-adapter-foundation
plan: 03
title: Dependency Risk Record And Adoption Gates
type: execute
wave: 3
depends_on: [46-01, 46-02]
files_modified:
  - docs/renderer-migration.md
  - docs/renderer-ownership.md
  - docs/frontend/renderer-contract.md
autonomous: true
requirements: [LIBS-03, LIBS-01, LIBS-02]
requirements_addressed: [LIBS-03, LIBS-01, LIBS-02]
user_setup: []
must_haves:
  truths:
    - "D-03: Rust-incompatible latest versions must be documented rather than silently selected."
    - "D-04: Blitz, Winit, DOM/web-platform, Stylo, and broader Skia expansion stay out of Phase 46."
    - "D-09: Focused proof and crate-facing conversion modules are adapter-owned and not public API."
    - "D-11: Phase 46 updates renderer migration dependency records with Linux/Nix, dependency, native, binary/build, CI, and rollback details."
    - "D-12: Verification covers disabled default behavior and enabled dependency build path."
    - "D-13: Gate commands include metadata/tree checks, cargo check paths, focused render proof tests, Phase 44 shell tests, and workspace tests where feasible."
    - "D-14: Audio transition polish stays deferred to v1.10 animations."
    - "D-15: Module install requirement resolution stays deferred to module-system work."
  artifacts:
    - path: "docs/renderer-migration.md"
      provides: "Phase 46 dependency record and promotion gates"
      contains: "Phase 46 Renderer Library Dependency Record"
    - path: "docs/renderer-ownership.md"
      provides: "Adapter-owned status for Phase 46 feature/status seam"
      contains: "Renderer library feature scaffold"
    - path: "docs/frontend/renderer-contract.md"
      provides: "Author-facing non-effect statement for Phase 46"
      contains: "disabled-by-default renderer-library features"
  key_links:
    - "Cargo feature scaffold -> docs/renderer-migration.md dependency record"
    - "library_adapters.rs -> docs/renderer-ownership.md adapter-owned boundary"
    - "renderer contract -> no .mesh author API change"
---

<objective>
Document the actual dependency, Linux/Nix, build, CI, and rollback impact of the Phase 46 renderer-library scaffold, classify the new feature/status seam as internal adapter-owned work, and prove shipped-surface gates still pass.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-renderer-library-dependency-and-adapter-foundation/46-CONTEXT.md
@.planning/phases/46-renderer-library-dependency-and-adapter-foundation/46-RESEARCH.md
@.planning/phases/46-renderer-library-dependency-and-adapter-foundation/46-VALIDATION.md
@.planning/phases/46-renderer-library-dependency-and-adapter-foundation/46-PATTERNS.md
@docs/renderer-migration.md
@docs/renderer-ownership.md
@docs/frontend/renderer-contract.md
@Cargo.toml
@crates/core/frontend/render/Cargo.toml
@crates/core/frontend/render/src/library_adapters.rs
@flake.nix
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| T-46-03-01: dependency risk is hidden in Cargo manifests and later phases assume libraries are production-default | high | Add a Phase 46 dependency record with exact feature names, versions, Rust constraints, default-disabled status, and rollback path. |
| T-46-03-02: docs imply author-facing `.mesh` behavior changed | medium | Update renderer contract to state Phase 46 features are internal and disabled by default. |
| T-46-03-03: shipped surfaces regress while dependency checks pass | high | Run Phase 44 focused shell regressions and render proof tests after docs/manifests are complete. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add Phase 46 dependency record to renderer migration docs</name>
  <files>docs/renderer-migration.md</files>
  <read_first>docs/renderer-migration.md, Cargo.toml, crates/core/frontend/render/Cargo.toml, flake.nix, .planning/phases/46-renderer-library-dependency-and-adapter-foundation/46-RESEARCH.md</read_first>
  <action>Add a section to `docs/renderer-migration.md` titled exactly `## Phase 46 Renderer Library Dependency Record` after the existing `### Dependency Record Template` section or near the Promotion Gates section. Include a markdown table with rows for these fields and exact content: `Linux/Nix impact` says default builds need no new Nix packages and full Vello/wgpu native risk is deferred; `Root workspace dependencies` lists `taffy 0.10.1`, `parley 0.7.0`, `accesskit 0.24.0`, `anyrender 0.10.0`, and `vello_encoding 0.5.1`; `Feature flags` lists `renderer-taffy`, `renderer-parley`, `renderer-accesskit`, `renderer-anyrender`, `renderer-vello-encoding`, and `renderer-libraries`; `Native libraries` says no new default runtime native libraries, full `vello`/`wgpu` deferred to Phase 49; `Binary/build risk` says optional dependency fan-out is measured through `cargo tree -p mesh-core-render --features renderer-libraries`; `CI gates` lists the default and enabled `cargo check`, `mesh-core-render renderer_library`, `mesh-core-render proof`, `mesh-core-shell phase44`, `mesh-core-shell phase44_navigation`, and workspace test commands; `Rollback path` says leave default features empty and route later behavior through `renderer_library_rollback_authority() == "mesh-software-renderer"`. Also add a short note that latest `parley 0.9.0`, `parley 0.8.0`, `vello 0.9.0`, and `vello_encoding 0.9.0` require Rust 1.88 and are not selected for the Rust 1.85 workspace.</action>
  <verify>rg -n "## Phase 46 Renderer Library Dependency Record|renderer-taffy|renderer-parley|renderer-accesskit|renderer-anyrender|renderer-vello-encoding|renderer-libraries|Rust 1\\.88|mesh-software-renderer" docs/renderer-migration.md</verify>
  <acceptance_criteria>
    - `docs/renderer-migration.md` contains `## Phase 46 Renderer Library Dependency Record`.
    - `docs/renderer-migration.md` contains `taffy 0.10.1`.
    - `docs/renderer-migration.md` contains `parley 0.7.0`.
    - `docs/renderer-migration.md` contains `accesskit 0.24.0`.
    - `docs/renderer-migration.md` contains `anyrender 0.10.0`.
    - `docs/renderer-migration.md` contains `vello_encoding 0.5.1`.
    - `docs/renderer-migration.md` contains `Rust 1.88`.
    - `docs/renderer-migration.md` contains `mesh-software-renderer`.
  </acceptance_criteria>
  <done>The renderer migration docs have a concrete Phase 46 dependency, Nix, CI, build-risk, and rollback record.</done>
</task>

<task type="auto">
  <name>Task 2: Classify Phase 46 scaffold in ownership and author contract docs</name>
  <files>docs/renderer-ownership.md, docs/frontend/renderer-contract.md</files>
  <read_first>docs/renderer-ownership.md, docs/frontend/renderer-contract.md, crates/core/frontend/render/src/library_adapters.rs, crates/core/frontend/render/Cargo.toml, .planning/phases/46-renderer-library-dependency-and-adapter-foundation/46-CONTEXT.md</read_first>
  <action>Update `docs/renderer-ownership.md` Adapter-Owned Boundaries with a row named `Renderer library feature scaffold` whose paths include `crates/core/frontend/render/Cargo.toml` and `crates/core/frontend/render/src/library_adapters.rs`, and whose promotion condition says it may promote only when later phases preserve NodeId identity, invalidation, damage/profiling, diagnostics, theme-owned selection, AccessKit-compatible update evidence, and rollback gates. Update `docs/frontend/renderer-contract.md` under `Renderer Migration Direction` or `Diagnostics And Verification` to state that Phase 46 adds disabled-by-default renderer-library features and an internal status seam only; `.mesh` syntax, layout semantics, service proxies, shell surface lifecycle, and author APIs do not change.</action>
  <verify>rg -n "Renderer library feature scaffold|library_adapters.rs|disabled-by-default renderer-library features|\\.mesh syntax|author APIs do not change" docs/renderer-ownership.md docs/frontend/renderer-contract.md</verify>
  <acceptance_criteria>
    - `docs/renderer-ownership.md` contains `Renderer library feature scaffold`.
    - `docs/renderer-ownership.md` contains `library_adapters.rs`.
    - `docs/renderer-ownership.md` contains `crates/core/frontend/render/Cargo.toml`.
    - `docs/frontend/renderer-contract.md` contains `disabled-by-default renderer-library features`.
    - `docs/frontend/renderer-contract.md` contains `.mesh syntax`.
    - `docs/frontend/renderer-contract.md` contains `author APIs do not change`.
  </acceptance_criteria>
  <done>Ownership and author-contract docs classify Phase 46 as internal adapter scaffolding with no public author behavior change.</done>
</task>

<task type="auto">
  <name>Task 3: Run final Phase 46 adoption gates</name>
  <files>docs/renderer-migration.md, docs/renderer-ownership.md, docs/frontend/renderer-contract.md</files>
  <read_first>.planning/phases/46-renderer-library-dependency-and-adapter-foundation/46-VALIDATION.md, docs/renderer-migration.md, docs/renderer-ownership.md, docs/frontend/renderer-contract.md, Cargo.toml, crates/core/frontend/render/Cargo.toml</read_first>
  <action>Run the final Phase 46 gate commands and record outcomes in the plan summary. Required commands: `env XDG_CACHE_HOME=/tmp/codex-nix-cache nix develop -c cargo check -p mesh-core-render`; `env XDG_CACHE_HOME=/tmp/codex-nix-cache nix develop -c cargo check -p mesh-core-render --features renderer-libraries`; `env XDG_CACHE_HOME=/tmp/codex-nix-cache nix develop -c cargo test -p mesh-core-render renderer_library`; `env XDG_CACHE_HOME=/tmp/codex-nix-cache nix develop -c cargo test -p mesh-core-render proof`; `env XDG_CACHE_HOME=/tmp/codex-nix-cache nix develop -c cargo test -p mesh-core-shell phase44`; `env XDG_CACHE_HOME=/tmp/codex-nix-cache nix develop -c cargo test -p mesh-core-shell phase44_navigation`; and `env XDG_CACHE_HOME=/tmp/codex-nix-cache nix develop -c cargo test --workspace` if time/disk/native dependency conditions allow. If a command cannot run because of sandbox, Nix cache, disk, or native dependency limits, record the exact blocker and do not mark it green.</action>
  <verify>rg -n "renderer-taffy|renderer-parley|renderer-accesskit|renderer-anyrender|renderer-vello-encoding|Rust 1\\.88|rollback path|disabled-by-default renderer-library features" Cargo.toml crates/core/frontend/render/Cargo.toml docs/renderer-migration.md docs/renderer-ownership.md docs/frontend/renderer-contract.md</verify>
  <acceptance_criteria>
    - Default `mesh-core-render` check outcome is recorded in `46-03-SUMMARY.md`.
    - Enabled `renderer-libraries` check outcome is recorded in `46-03-SUMMARY.md`.
    - `mesh-core-render renderer_library` test outcome is recorded in `46-03-SUMMARY.md`.
    - `mesh-core-render proof` test outcome is recorded in `46-03-SUMMARY.md`.
    - `mesh-core-shell phase44` and `phase44_navigation` outcomes are recorded in `46-03-SUMMARY.md`.
    - Workspace test outcome or exact blocker is recorded in `46-03-SUMMARY.md`.
    - `rg -n "renderer-taffy|renderer-parley|renderer-accesskit|renderer-anyrender|renderer-vello-encoding|Rust 1\\.88|rollback path|disabled-by-default renderer-library features" Cargo.toml crates/core/frontend/render/Cargo.toml docs/renderer-migration.md docs/renderer-ownership.md docs/frontend/renderer-contract.md` finds matches.
  </acceptance_criteria>
  <done>Final adoption gates prove dependency scaffold, rollback docs, and shipped-surface regression status before Phase 46 closes.</done>
</task>

</tasks>

<verification>
- Run `rg -n "## Phase 46 Renderer Library Dependency Record|renderer-taffy|renderer-parley|renderer-accesskit|renderer-anyrender|renderer-vello-encoding|Rust 1\\.88|mesh-software-renderer" docs/renderer-migration.md`.
- Run `rg -n "Renderer library feature scaffold|library_adapters.rs|disabled-by-default renderer-library features|author APIs do not change" docs/renderer-ownership.md docs/frontend/renderer-contract.md`.
- Run `env XDG_CACHE_HOME=/tmp/codex-nix-cache nix develop -c cargo check -p mesh-core-render`.
- Run `env XDG_CACHE_HOME=/tmp/codex-nix-cache nix develop -c cargo check -p mesh-core-render --features renderer-libraries`.
- Run `env XDG_CACHE_HOME=/tmp/codex-nix-cache nix develop -c cargo test -p mesh-core-render renderer_library`.
- Run `env XDG_CACHE_HOME=/tmp/codex-nix-cache nix develop -c cargo test -p mesh-core-render proof`.
- Run `env XDG_CACHE_HOME=/tmp/codex-nix-cache nix develop -c cargo test -p mesh-core-shell phase44`.
- Run `env XDG_CACHE_HOME=/tmp/codex-nix-cache nix develop -c cargo test -p mesh-core-shell phase44_navigation`.
</verification>

<success_criteria>
- Dependency record documents Linux/Nix impact, root deps, native deps, build risk, CI gates, and rollback path.
- Ownership docs classify Phase 46 scaffold as adapter-owned.
- Author contract states no `.mesh` author API change.
- Both disabled and enabled feature paths are verified or exact blockers are recorded.
- Shipped Phase 44 focused proof/shell regressions remain green or exact blockers are recorded.
</success_criteria>
