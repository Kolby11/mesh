---
phase: 60-surface-keybind-dispatch-runtime
plan: 01
title: Focused Surface Keybind Dispatch Runtime
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/core/shell/src/shell/component/input/mod.rs
  - crates/core/shell/src/shell/component/input/keyboard.rs
  - crates/core/shell/src/shell/component/tests/interaction/navigation.rs
autonomous: true
requirements: [KDISP-01, KDISP-02, KDISP-03, KDISP-04]
requirements_addressed: [KDISP-01, KDISP-02, KDISP-03, KDISP-04]
user_setup: []
must_haves:
  truths:
    - "D-01: Reuse the existing FrontendSurfaceComponent keyboard input path and dispatch_surface_shortcut flow; do not add a global keybind dispatcher."
    - "D-02: Surface keybinds dispatch through explicit template subscribers using keybind plus onkeybind."
    - "D-03: Manifest actions must never dispatch directly to raw handler names."
    - "D-04: Shell-global shortcuts, Tab/Escape handling, and Ctrl+C selection copy remain before surface keybind dispatch."
    - "D-05: Surface keybind dispatch remains before focused keydown handlers and default widget activation for non-text-focused cases."
    - "D-06: Focused text input remains protected; a bare printable keybind must not steal normal text entry when an input owns focus."
    - "D-07: Multiple subscribers for the same action dispatch in deterministic tree order and aggregate CoreRequests."
    - "D-08: An action with no subscribers in the current tree is a no-op for Phase 60; missing-target diagnostics belong to Phase 62."
    - "D-09: The keybind event payload keeps keybind.id, trigger_kind, and resolution source."
    - "D-10: Navigation bar is the primary Phase 60 proof surface because it already declares mesh.keybinds.mute and subscribes a volume button handler."
    - "D-11: Audio popover is regression/proof only where already relevant; do not expand Phase 60 into broad audio popover redesign or keybind UI work."
    - "D-12: Preserve legacy settings fallback tests while proving manifest-owned dispatch is primary."
  artifacts:
    - path: "crates/core/shell/src/shell/component/input/mod.rs"
      provides: "Component-level keyboard precedence and surface shortcut call site"
      contains: "dispatch_surface_shortcut"
    - path: "crates/core/shell/src/shell/component/input/keyboard.rs"
      provides: "Resolved shortcuts, subscriber collection, and keybind event payload"
      contains: "KeybindSubscriber"
    - path: "crates/core/shell/src/shell/component/tests/interaction/navigation.rs"
      provides: "Focused keybind and shipped navigation regression tests"
      contains: "keyboard_shortcuts"
---

<objective>
Complete Phase 60 by making focused-surface manifest keybind dispatch safe around existing keyboard ownership rules, especially focused text input, while proving dispatch through explicit runtime subscribers and the shipped navigation-bar surface.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/60-surface-keybind-dispatch-runtime/60-CONTEXT.md
@.planning/phases/60-surface-keybind-dispatch-runtime/60-RESEARCH.md
@.planning/phases/60-surface-keybind-dispatch-runtime/60-VALIDATION.md
@.planning/phases/60-surface-keybind-dispatch-runtime/60-PATTERNS.md
@crates/core/shell/src/shell/component/input/mod.rs
@crates/core/shell/src/shell/component/input/keyboard.rs
@crates/core/shell/src/shell/component/tests/interaction/navigation.rs
@modules/frontend/navigation-bar/module.json
@modules/frontend/navigation-bar/src/components/volume-button.mesh
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| T-60-01: manifest keybind actions bypass explicit runtime subscribers | high | Keep dispatch rooted in `keybind` + `onkeybind` subscribers and add tests that manifest actions dispatch only when a subscriber exists. |
| T-60-02: bare printable surface keybinds steal focused text input | high | Add focused-input guard before `dispatch_surface_shortcut` for bare printable keys and prove focused input receives the character instead. |
| T-60-03: Phase 60 regresses keyboard ownership precedence | high | Preserve Tab/Escape and Ctrl+C selection copy ordering and run existing keyboard/navigation tests. |
| T-60-04: shipped proof relies on legacy settings-only shortcuts | medium | Strengthen navigation-bar proof to assert the shipped manifest declares `mesh.keybinds.mute` and the runtime exposes a `mute` keybind subscriber. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add focused dispatch characterization tests</name>
  <files>crates/core/shell/src/shell/component/tests/interaction/navigation.rs</files>
  <read_first>crates/core/shell/src/shell/component/tests/interaction/navigation.rs, crates/core/shell/src/shell/component/input/mod.rs, crates/core/shell/src/shell/component/input/keyboard.rs, .planning/phases/60-surface-keybind-dispatch-runtime/60-CONTEXT.md</read_first>
  <action>Add focused tests in `crates/core/shell/src/shell/component/tests/interaction/navigation.rs` covering three concrete behaviors and explicitly preserving D-02, D-03, D-07, D-08, and D-09. First, add or extend a test named `keyboard_shortcuts_manifest_keybind_dispatches_only_to_runtime_subscribers` that creates a component with manifest action id `mute`, key `m`, one node with attributes `keybind="mute"` and `onkeybind={onMuteShortcut}`, presses `m`, and asserts `mute_count == 1.0`; then remove or omit the subscriber from the runtime tree, press `m`, and assert `mute_count` remains unchanged. Second, add or extend a test named `keyboard_shortcuts_bare_printable_does_not_steal_focused_text_input` that creates an input node focused by `focused_key`, a manifest action `mute` bound to bare key `m`, and a separate keybind subscriber; press `m` and assert the input change path receives text `m` while `mute_count == 0.0`. Third, ensure an existing or new modifier test still proves `Ctrl+M` only dispatches when `KeyModifiers { ctrl: true, shift: false, alt: false }` is active.</action>
  <verify>nix develop -c cargo test -p mesh-core-shell keyboard_shortcuts</verify>
  <acceptance_criteria>
    - `crates/core/shell/src/shell/component/tests/interaction/navigation.rs` contains `keyboard_shortcuts_manifest_keybind_dispatches_only_to_runtime_subscribers`.
    - `crates/core/shell/src/shell/component/tests/interaction/navigation.rs` contains `keyboard_shortcuts_bare_printable_does_not_steal_focused_text_input`.
    - `crates/core/shell/src/shell/component/tests/interaction/navigation.rs` contains `mute_count == 0.0` in the focused text input test.
    - `crates/core/shell/src/shell/component/tests/interaction/navigation.rs` contains `KeyModifiers {`.
    - `nix develop -c cargo test -p mesh-core-shell keyboard_shortcuts` exits 0.
  </acceptance_criteria>
  <done>Focused tests prove manifest-owned keybind dispatch uses subscribers and does not steal bare printable text input.</done>
</task>

<task type="auto">
  <name>Task 2: Protect focused text input from bare printable keybinds</name>
  <files>crates/core/shell/src/shell/component/input/mod.rs, crates/core/shell/src/shell/component/input/keyboard.rs</files>
  <read_first>crates/core/shell/src/shell/component/input/mod.rs, crates/core/shell/src/shell/component/input/keyboard.rs, crates/core/shell/src/shell/component/tests/interaction/navigation.rs, .planning/phases/60-surface-keybind-dispatch-runtime/60-PATTERNS.md</read_first>
  <action>In the `ComponentInput::KeyPressed` branch in `crates/core/shell/src/shell/component/input/mod.rs`, preserve D-01, D-04, D-05, and D-06 by keeping the existing component input path, keeping Tab/Escape handling and `Ctrl+C` selection copy before surface shortcut dispatch, and keeping surface keybind dispatch before focused `keydown` and default widget activation for non-text-focused cases. Before calling `self.dispatch_surface_shortcut(&tree, &key, modifiers, &keyboard_settings)?`, determine whether the currently normalized focused key points at an input node and whether the pressed key is a bare printable key. Treat a bare printable key as a one-character key with `!modifiers.ctrl && !modifiers.alt` and with no special names such as `Tab`, `Escape`, `Backspace`, `Enter`, or arrow keys. If an input owns focus and the key is bare printable, skip surface shortcut dispatch and continue into the existing focused input path so normal text entry runs. Preserve modified shortcuts such as `Ctrl+M` and non-input surface keybind behavior. Add a small helper in `input/mod.rs` or `input/keyboard.rs` only if it keeps the branch readable; use a concrete helper name such as `should_skip_surface_shortcut_for_text_input` or `is_bare_printable_key`.</action>
  <verify>nix develop -c cargo test -p mesh-core-shell keyboard_shortcuts</verify>
  <acceptance_criteria>
    - `crates/core/shell/src/shell/component/input/mod.rs` still calls `selection_copy_payload` before `dispatch_surface_shortcut`.
    - `crates/core/shell/src/shell/component/input/mod.rs` contains `dispatch_surface_shortcut`.
    - `crates/core/shell/src/shell/component/input/mod.rs` or `crates/core/shell/src/shell/component/input/keyboard.rs` contains `is_bare_printable_key` or `should_skip_surface_shortcut_for_text_input`.
    - The focused text input test from Task 1 passes.
    - `nix develop -c cargo test -p mesh-core-shell keyboard_shortcuts` exits 0.
  </acceptance_criteria>
  <done>Surface keybind dispatch no longer steals bare printable text input while preserving modified and non-input keybind behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Strengthen shipped navigation manifest dispatch proof</name>
  <files>crates/core/shell/src/shell/component/tests/interaction/navigation.rs</files>
  <read_first>crates/core/shell/src/shell/component/tests/interaction/navigation.rs, modules/frontend/navigation-bar/module.json, modules/frontend/navigation-bar/src/components/volume-button.mesh, modules/frontend/navigation-bar/config/settings.json, crates/core/shell/src/shell/component/input/keyboard.rs</read_first>
  <action>Update the shipped navigation proof in `navigation_bar_keyboard_shortcut_and_theme_activation_work_on_real_surface` or add a focused adjacent test to preserve D-10, D-11, and D-12. Assert that the real compiled navigation component has a manifest keybind action id `mute` from `modules/frontend/navigation-bar/module.json`, that the runtime tree exposes a `KeybindSubscriber` with `keybind_id == "mute"` and handler `onMuteKeybind`, and that pressing the resolved manifest/default mute key produces the expected audio mute service command request. Keep the legacy settings fallback tests intact; do not remove `keyboard_shortcuts_surface_handler_runs_and_metadata_matches_binding` or `keyboard_shortcuts_manifest_declaration_wins_over_legacy_settings_same_id`. Do not modify `modules/frontend/audio-popover/module.json` or `modules/frontend/audio-popover/src/main.mesh` in Phase 60.</action>
  <verify>nix develop -c cargo test -p mesh-core-shell navigation_bar_keyboard_shortcut_and_theme_activation_work_on_real_surface</verify>
  <acceptance_criteria>
    - `crates/core/shell/src/shell/component/tests/interaction/navigation.rs` contains `manifest.keybinds.actions`.
    - `crates/core/shell/src/shell/component/tests/interaction/navigation.rs` contains `keybind_id == "mute"` or `keybind_id, "mute"`.
    - `crates/core/shell/src/shell/component/tests/interaction/navigation.rs` contains `onMuteKeybind`.
    - `crates/core/shell/src/shell/component/tests/interaction/navigation.rs` still contains `keyboard_shortcuts_manifest_declaration_wins_over_legacy_settings_same_id`.
    - `nix develop -c cargo test -p mesh-core-shell navigation_bar_keyboard_shortcut_and_theme_activation_work_on_real_surface` exits 0.
  </acceptance_criteria>
  <done>The real navigation surface proves manifest-owned keybind dispatch through its shipped subscriber path.</done>
</task>

</tasks>

<verification>
- Run `nix develop -c cargo test -p mesh-core-shell keyboard_shortcuts`.
- Run `nix develop -c cargo test -p mesh-core-shell navigation_bar_keyboard_shortcut_and_theme_activation_work_on_real_surface`.
- Run `nix develop -c cargo test -p mesh-core-shell shell::component::tests::interaction::navigation`.
- Run `rg -n "keyboard_shortcuts_manifest_keybind_dispatches_only_to_runtime_subscribers|keyboard_shortcuts_bare_printable_does_not_steal_focused_text_input|should_skip_surface_shortcut_for_text_input|is_bare_printable_key|onMuteKeybind" crates/core/shell/src/shell/component`.
</verification>

<success_criteria>
- Manifest-owned surface keybind actions dispatch through explicit runtime subscribers.
- Surface keybind dispatch preserves shell-global, Tab/Escape, Ctrl+C selection copy, focused text input, focus traversal, and default widget activation boundaries.
- Bare printable keybinds do not steal focused text input.
- The shipped navigation-bar surface proves manifest-owned mute keybind dispatch without relying only on legacy settings shortcuts.
- KDISP-01, KDISP-02, KDISP-03, and KDISP-04 are all covered by the plan.
</success_criteria>
