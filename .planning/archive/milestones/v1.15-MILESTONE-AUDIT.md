# v1.15 Milestone Audit: Persistent Storage System

**Date:** 2026-05-26
**Auditor:** Codex / `$gsd-autonomous`
**Status:** passed

## Verdict

v1.15 satisfies the milestone intent in the current worktree: `self.storage`
now has a shell-owned scoped storage foundation, frontend/backend Luau bindings,
lifecycle persistence, render dependency tracking, shipped UI proof, tests, and
author documentation.

The earlier pre-archive blocker was a large dirty worktree from prior runtime
work. That work has now been committed in `e3b267e`, so the milestone is ready
to archive.

## Requirement Audit

| Area | Requirements | Result |
|------|--------------|--------|
| Storage Foundation | STORECORE-01 through STORECORE-06 | Pass |
| Luau `self.storage` Binding | STOREAPI-01 through STOREAPI-06 | Pass |
| Lifecycle And Persistence | STORELIFE-01 through STORELIFE-05 | Pass |
| Rerender Integration | STORERENDER-01 through STORERENDER-04 | Pass |
| Proof, Diagnostics, And Docs | STOREPROOF-01 through STOREPROOF-06 | Pass |

## Evidence

- `crates/core/runtime/scripting/src/storage.rs` implements scoped JSON storage,
  deterministic sanitized paths, dirty tracking, temp-file plus rename
  persistence, corrupt-file recovery, and Lua storage table proxy creation.
- Frontend `ScriptContext` exposes `self.storage`, tracks render-time storage
  reads by key, marks script state dirty only for watched-key writes, flushes on
  `unmount`, and reports storage diagnostics through existing script
  diagnostics.
- Backend `BackendScriptContext` exposes `self.storage`, flushes on `stop`, and
  exposes storage diagnostics through the backend runtime drain path.
- `modules/frontend/navigation-bar/src/components/language-button.mesh` persists
  language selection through `self.storage.language`.
- `docs/module-system.md` documents storage scope, supported values, snapshots,
  deletion, lifecycle timing, diagnostics, and render dependency behavior.

## Verification Commands

```bash
nix develop -c cargo fmt --check
nix develop -c cargo test -p mesh-core-scripting storage --no-fail-fast
nix develop -c cargo test -p mesh-core-scripting storage_flushes --no-fail-fast
nix develop -c cargo test -p mesh-core-scripting persistence_failure --no-fail-fast
nix develop -c cargo test -p mesh-core-scripting storage_render_reads --no-fail-fast
nix develop -c cargo test -p mesh-core-shell navigation_language_button_publishes_locale_request_on_real_surface --no-fail-fast
```

All commands passed during the autonomous run.

## Accepted Tech Debt

- No critical milestone blockers remain.
- Follow-up product work continues in v1.16 Elements Improvements, where the
  navigation language selector should move from a custom horizontal menu to a
  native `<select>/<option>` control.

## Recommendation

Proceed with `$gsd-complete-milestone`, archive v1.15, and start v1.16
requirements from the queued Elements Improvements scope.
