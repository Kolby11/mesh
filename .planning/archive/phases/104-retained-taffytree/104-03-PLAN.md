---
phase: 104-retained-taffytree
plan: 03
type: execute
wave: 2
depends_on: ["104-01"]
files_modified:
  - crates/core/shell/src/shell/component.rs
  - crates/core/shell/src/shell/component/shell_component.rs
  - crates/core/shell/src/shell/component/rendering.rs
autonomous: true
requirements: [LAYOUT-01, LAYOUT-02]
must_haves:
  truths:
    - "FrontendSurfaceComponent owns a layout_state: PerSurfaceLayoutState field initialized in its constructor"
    - "finalize_tree calls LayoutEngine::compute_incremental with the retained layout_state and dirty-derived booleans instead of the per-frame compute_with_intrinsic_cache_and_measurer rebuild"
    - "theme_changed, locale_changed, and reload_source reset layout_state so the next frame rebuilds fresh (valid=false)"
  artifacts:
    - path: "crates/core/shell/src/shell/component.rs"
      provides: "layout_state field on FrontendSurfaceComponent + constructor init"
      contains: "layout_state"
    - path: "crates/core/shell/src/shell/component/rendering.rs"
      provides: "compute_incremental call site replacing the per-frame rebuild"
      contains: "compute_incremental"
  key_links:
    - from: "rendering.rs finalize_tree"
      to: "self.layout_state"
      via: "LayoutEngine::compute_incremental(tree, &mut self.layout_state, ...)"
      pattern: "compute_incremental"
    - from: "theme_changed / locale_changed / reload_source"
      to: "self.layout_state"
      via: "PerSurfaceLayoutState::default() reset"
      pattern: "layout_state = .*default"
---

<objective>
Wire the retained layout state into `FrontendSurfaceComponent`: add a `layout_state: PerSurfaceLayoutState` field (initialized in the constructor), replace the per-frame `LayoutEngine::compute_with_intrinsic_cache_and_measurer` call in `finalize_tree` with `LayoutEngine::compute_incremental` passing `&mut self.layout_state` and the dirty-derived booleans, and reset `layout_state` to `PerSurfaceLayoutState::default()` (valid=false) in `theme_changed`, `locale_changed`, and `reload_source`.

Purpose: Make the retained tree (LAYOUT-01) actually drive the shell's hot layout path, with the VISUAL_REPAINT / LAYOUT dirty routing (LAYOUT-02) fed from the existing `ComponentDirtyFlags`, and ensure theme/locale/source resets fall back to a fresh build for one frame.
Output: The shell paint path computes layout incrementally against a retained per-surface TaffyTree.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/104-retained-taffytree/104-CONTEXT.md
@.planning/phases/104-retained-taffytree/104-RESEARCH.md

<interfaces>
<!-- compute_incremental signature is produced by plan 104-01 (Wave 1, complete before this runs). -->

From mesh-core-elements (after plan 01, exported via lib.rs line 35):
- pub struct PerSurfaceLayoutState  (Default available; valid=false on default)
- LayoutEngine::compute_incremental(root: &mut WidgetNode, state: &mut PerSurfaceLayoutState, available_width: f32, available_height: f32, dirty_layout: bool, dirty_structural: bool, intrinsic_cache: &mut IntrinsicLayoutCache, measurer: Option<&dyn TextMeasurer>)

Current call site to replace — crates/core/shell/src/shell/component/rendering.rs lines 332-350 inside finalize_tree:
  if !reused_retained_layout {
      let layout_started = std::time::Instant::now();
      let measurer = SharedTextMeasurer;
      LayoutEngine::compute_with_intrinsic_cache_and_measurer(
          tree, width as f32, height as f32, &mut self.intrinsic_layout_cache, Some(&measurer));
      self.record_profiling_stage(ProfilingStage::Layout, layout_started, Some(trigger_kind));
  }

ComponentDirtyFlags (crates/core/shell/src/shell/component.rs lines 67-111):
- const LAYOUT = 1 << 3;
- const STYLE = 1 << 2;
- const SCRIPT = 1 << 0;
- const TEXT = 1 << 5;
- pub(super) const TREE_REBUILD: Self = Self::SCRIPT | ... | Self::TEXT  (line 88; it is the SCRIPT|TEXT-bearing composite)
- pub(super) const VISUAL_REPAINT: Self = Self::STYLE | ... (line 111)
- dirty_types: ComponentDirtyFlags is already a parameter of finalize_tree (line 204)

Derive the two booleans for compute_incremental from dirty_types:
- dirty_structural = dirty_types.intersects(ComponentDirtyFlags::SCRIPT | ComponentDirtyFlags::TEXT)
- dirty_layout = dirty_types.contains(ComponentDirtyFlags::LAYOUT)

FrontendSurfaceComponent struct — crates/core/shell/src/shell/component.rs:
- struct defined at line 275; fields include `intrinsic_layout_cache: IntrinsicLayoutCache` (364), `retained_tree`, `retained_render_objects`, `retained_display_list` (365-368)
- constructor initializers at lines 488-492 (intrinsic_layout_cache, retained_tree, retained_render_objects, retained_display_list all set to ::default())

Reset sites — crates/core/shell/src/shell/component/shell_component.rs:
- theme_changed: line 696; already resets intrinsic_layout_cache + retained_* at lines 706-709
- locale_changed: line 724; already resets the same at lines 732-735
- reload_source: line 805; resets caches/flags at lines 838-841 (does NOT currently reset intrinsic_layout_cache or retained_* — add layout_state reset here)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add layout_state field and constructor initialization</name>
  <files>crates/core/shell/src/shell/component.rs</files>
  <read_first>
    - crates/core/shell/src/shell/component.rs (struct def at line 275, field block around 364-368, constructor initializers around 488-492, the existing `use mesh_core_elements::...` import lines)
  </read_first>
  <action>
    Add a field `layout_state: PerSurfaceLayoutState,` to the `FrontendSurfaceComponent` struct, placed adjacent to `intrinsic_layout_cache` / `retained_*` fields (around lines 364-368) to match the established retained-state grouping. Import `PerSurfaceLayoutState` from `mesh_core_elements` (it is re-exported from the crate root per plan 01's lib.rs change — extend the existing `use mesh_core_elements::{...}` import that already brings in `IntrinsicLayoutCache`/`LayoutEngine`).

    In the constructor (around lines 488-492 where `intrinsic_layout_cache: IntrinsicLayoutCache::default()` etc. are set), add `layout_state: PerSurfaceLayoutState::default(),`.
  </action>
  <verify>
    <automated>cargo build --package mesh-core-shell 2>&1 | grep -q "error\[" && exit 1 || cargo build --package mesh-core-shell</automated>
  </verify>
  <acceptance_criteria>
    - component.rs contains `layout_state: PerSurfaceLayoutState` in the struct field list
    - component.rs constructor contains `layout_state: PerSurfaceLayoutState::default()`
    - `PerSurfaceLayoutState` is imported from mesh_core_elements
    - `cargo build --package mesh-core-shell` exits 0
  </acceptance_criteria>
  <done>FrontendSurfaceComponent has a layout_state field initialized to PerSurfaceLayoutState::default() and the crate builds.</done>
</task>

<task type="auto">
  <name>Task 2: Swap finalize_tree to compute_incremental and add reset sites</name>
  <files>crates/core/shell/src/shell/component/rendering.rs, crates/core/shell/src/shell/component/shell_component.rs</files>
  <read_first>
    - crates/core/shell/src/shell/component/rendering.rs (finalize_tree, lines 332-350 — the `if !reused_retained_layout` block and the profiling call)
    - crates/core/shell/src/shell/component/shell_component.rs (theme_changed 696-709, locale_changed 724-735, reload_source 805-842)
  </read_first>
  <action>
    In rendering.rs `finalize_tree`, inside the existing `if !reused_retained_layout { ... }` block (lines 332-350), replace the `LayoutEngine::compute_with_intrinsic_cache_and_measurer(tree, width as f32, height as f32, &mut self.intrinsic_layout_cache, Some(&measurer));` call with:
    `LayoutEngine::compute_incremental(tree, &mut self.layout_state, width as f32, height as f32, dirty_layout, dirty_structural, &mut self.intrinsic_layout_cache, Some(&measurer));`
    Immediately before the call, derive the two booleans from the in-scope `dirty_types`:
    - `let dirty_structural = dirty_types.intersects(ComponentDirtyFlags::SCRIPT | ComponentDirtyFlags::TEXT);`
    - `let dirty_layout = dirty_types.contains(ComponentDirtyFlags::LAYOUT);`
    Keep the surrounding `layout_started` timing and `record_profiling_stage(ProfilingStage::Layout, ...)` exactly as-is — do not add new profiling stages (that is Phase 106 scope). Keep `let measurer = SharedTextMeasurer;`.

    In shell_component.rs add `self.layout_state = PerSurfaceLayoutState::default();` to all three reset sites:
    - theme_changed (after the existing retained_* resets at lines 706-709)
    - locale_changed (after lines 732-735)
    - reload_source (alongside the cache/flag resets at lines 838-841; this site does not currently reset retained state, so add the layout_state reset there explicitly)
    Import `PerSurfaceLayoutState` from mesh_core_elements in shell_component.rs if not already in scope.
  </action>
  <verify>
    <automated>cargo test --package mesh-core-shell 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - rendering.rs finalize_tree calls `LayoutEngine::compute_incremental(` and no longer calls `compute_with_intrinsic_cache_and_measurer` inside finalize_tree
    - rendering.rs derives `dirty_structural` from `SCRIPT | TEXT` and `dirty_layout` from `LAYOUT` before the call
    - shell_component.rs contains `self.layout_state = PerSurfaceLayoutState::default();` in theme_changed, locale_changed, AND reload_source (3 occurrences)
    - `cargo build --package mesh-core-shell` exits 0
    - `cargo test --package mesh-core-shell` exits 0 (existing shell tests still pass — confirms no layout regression)
  </acceptance_criteria>
  <done>finalize_tree drives layout through compute_incremental against the retained layout_state, the three reset sites invalidate it, and the shell test suite passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dirty flags → layout routing | Internal flags from the component runtime; no external input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-104-05 | Tampering | stale Taffy styles after theme/locale change | mitigate | layout_state reset to default (valid=false) in theme_changed/locale_changed/reload_source forces a one-frame fresh build with fresh token values (RESEARCH.md Anti-Pattern: not resetting valid) |
| T-104-06 | Repudiation | layout dirty bits mis-mapped | accept | Boolean derivation mirrors the existing reused_retained_layout LAYOUT check already in finalize_tree; low risk, covered by shell test suite |
</threat_model>

<verification>
- `cargo build --package mesh-core-shell` succeeds
- `cargo test --package mesh-core-shell` passes (existing interaction/reflow tests exercise the layout path)
- `cargo test --package mesh-core-elements && cargo test --package mesh-core-shell` both green (full phase gate)
</verification>

<success_criteria>
- layout_state retained on FrontendSurfaceComponent and driven through compute_incremental (LAYOUT-01)
- dirty-derived booleans route VISUAL_REPAINT vs LAYOUT correctly into the retained path (LAYOUT-02)
- theme/locale/source resets force a fresh rebuild, preventing stale token geometry
</success_criteria>

<output>
Create `.planning/phases/104-retained-taffytree/104-03-SUMMARY.md` when done
</output>
