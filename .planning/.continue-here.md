---
context: default
phase: module-system-redesign-todo
task: null
status: in_progress
last_updated: 2026-06-19T07:12:23.928Z
---

## Current State

Working through `todo.md` — Module system redesign section. Two i18n/locale items were completed this session; the todo.md checkboxes still need to be marked `[x]` and everything committed.

<current_state>
Session implemented two todo.md items fully:

1. **"Resolve localized manifest metadata through active locale for... provider labels and resource labels"** — now done. `PathContribution.label`, `ThemeContribution.label`, `MeshProvidesDeclaration.label`, `BackendProviderNode.label` all upgraded from raw strings to `Option<LocalizedText>`. Debug snapshot now includes resolved `provides_interface_labels`, `provides_themes`, `provides_theme_labels`. `resolve_debug_manifest_text` updated to pass `module_id`.

2. **"Let modules declare supported locales and contributed catalogs once"** — now done. `LocaleEngine` has `load_module_translations(module_id, set)` and `translate_for_module(key, module_id)`. Discovery now returns `(module_id, locale, path)` triples. `load_graph_i18n_catalogs` calls `load_module_translations`. `resolve_manifest_text` uses `translate_for_module`. New graph diagnostics: `redundant_supported_locales` and `missing_default_locale_catalog`. `navigation-bar/module.json` had redundant `supportedLocales` removed.

Everything compiles and all tests pass (118 module tests, locale tests, surface-config tests).

**Immediately needed:** Mark both todo.md items as `[x]` and commit all changes.
</current_state>

<completed_work>
- `PathContribution.label`, `ThemeContribution.label`, `MeshProvidesDeclaration.label` → `Option<manifest::LocalizedText>`
- `BackendProviderNode.label` → `Option<manifest::LocalizedText>` with `label_text()` helper
- `ContributedTheme.label_text()` and `ContributedPathResource.label_text()` helpers added
- Localized text diagnostics extended to cover theme/path/implements labels (suspicious raw i18n key checks)
- `LocaleEngine::load_module_translations(module_id, set)` — scoped storage + merges into global pool
- `LocaleEngine::translate_for_module(key, module_id)` — checks module catalog first, falls back to global
- `graph_i18n_catalog_paths` returns `Vec<(String, String, PathBuf)>` — (module_id, locale, path)
- `load_graph_i18n_catalogs` uses `load_module_translations`
- `resolve_manifest_text` uses `translate_for_module` instead of `translate`
- `ModuleGraphEntry` gains `provides_interface_labels`, `provides_themes`, `provides_theme_labels`
- `debug.rs` populates new fields; `module_graph_entry_json` emits structured `provides.interfaces` and `provides.themes` with labels
- `resolve_debug_manifest_text` takes `module_id` param, uses `translate_for_module`
- `redundant_supported_locales` diagnostic when mesh.i18n.supportedLocales duplicates provides.i18n
- `missing_default_locale_catalog` diagnostic when defaultLocale has no contributed catalog
- `navigation-bar/module.json` — removed redundant `supportedLocales`; `mesh.i18n` now only declares `defaultLocale`
- Test at line 770 updated to reflect removed `supportedLocales`
- All test label `.into()` calls fixed to `LocalizedText::Literal(...)`
- `cargo fmt` applied to all changed crates
</completed_work>

<remaining_work>
**Immediate (before moving on):**
- Mark the two completed todo.md items as `[x]`
- Commit all uncommitted files

**Next todo.md items in the module system redesign:**
- "Make event channels typed and declared" — frontend `audio.VolumeChanged:on(...)` validation against interface contract events. Backend undeclared emit already covered. Frontend side remains.
- "Eliminate service-specific Rust branches where possible" — audio optimistic state and some debug/profiling paths are pragmatic exceptions; new domains should not add new ones.
- "Treat manifests as defaults and user config as overrides"
- "Support multiple instances of the same frontend module later"
- Diagnostics/settings UI full module graph view (filter, active-provider detail, native binary health, keybinds, per-module customization)
</remaining_work>

<decisions_made>
- `Option<LocalizedText>` (not `LocalizedText` directly) for all label fields — consistent with existing pattern; missing labels are `None` not empty string
- `load_module_translations` also calls `load_translations` internally so global `translate()` and template `t("key")` continue working unchanged
- `redundant_supported_locales` is advisory diagnostic, not hard error — clean migration path
- Settings schema `description` strings in `serde_json::Value` blobs deferred — schema is untyped; settings UI not yet built
</decisions_made>

<blockers>
- `smithay-client-toolkit` requires xkbcommon pkg-config for full `cargo check -p mesh-core-shell` — this is an environment issue, not a code issue. The shell lib itself (`--lib` flag) compiles cleanly.
</blockers>

## Required Reading (in order)
1. `todo.md` lines 88-96 — the two completed items and their checkboxes
2. `crates/core/foundation/locale/src/lib.rs` — new `load_module_translations` and `translate_for_module` methods
3. `crates/core/extension/module/src/package/installed_graph.rs` — `redundant_supported_locales` diagnostic, updated label types

## Infrastructure State
- No running services
- All changes uncommitted (48 modified files, most from prior sessions)

<context>
This was a continuation of the module-system redesign backlog in todo.md. The session started from where the previous agent left off (layout labels done, keybind labels done), and completed the remaining localization items: resource/provider label types, module-scoped locale engine, and the "declare once" simplification for i18n catalogs.

The overall module-system redesign in todo.md is roughly 70% complete. The remaining open items are around event channel typing, settings UI generation, multi-instance support, and diagnostics UI.
</context>

<next_action>
1. Mark lines 94-95 in todo.md as `[x]` (both i18n/locale items completed this session)
2. `git add` all modified files and commit with message: "feat(module): localized labels for resources/providers, module-scoped locale catalogs"
3. Continue with next todo.md item: "Make event channels typed and declared" (frontend VolumeChanged:on validation)
</next_action>
