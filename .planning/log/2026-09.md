# September 2026

## 2026-09-01 — Complete whole-codebase audit synthesis

`working tree` · area: codebase audit

Completed the 2026-09-01 audit artifact set. The inventory accounts for 648
in-scope non-binary files across 30 Cargo workspace packages with zero
unassigned files. Sections 01–15 reuse the completed dated reports and Section
16 reuses its completed historical planning report, per the instruction not to
repeat sections already done in `.planning`. Three fresh cross-section review
agents covered performance, ownership/redundancy, and logic/lifecycle; their
findings were reconciled into `cross-section-findings.md` and `FINAL.md`.

No production code was changed and no performance claim was made. New open
cross-section findings are linked from the shell-core backlog entries.

## 2026-09-02 — Reconcile whole-codebase audit into the backlog

`working tree` · area: backlog reconciliation

Moved the genuinely new open findings from the 2026-09-01 whole-codebase audit
into one section of `docs/BACKLOG.md`, sorted by audit section and grouped into
executable tasks. Existing backlog and older-audit findings were left in place;
duplicate scattered audit entries were consolidated. No production code or
performance claim was added.

## 2026-09-02 — Remove closed backlog entries

`working tree` · area: backlog and Markdown cleanup

Removed backlog entries whose implementation is recorded as complete in the
August log: Section 15 lifecycle and reconciliation follow-ups, Section 16
authoring/tooling work, runtime sandbox/exec/storage/reload items, surface
policy correctness, rendering correctness, and presentation lifecycle
correctness. Preserved open
activation, rendering, resource-broker, direct-SHM, rotation, and benchmark
tasks. Audit reports remain unchanged as historical evidence.

No production code or performance claim was added.

## 2026-09-02 — Resolve per-instance settings through owner validation

`working tree` · area: settings validation

`SettingsStore::namespace` now resolves `#instance` keys through the validated
projection for their base module owner. Invalid instance overrides are omitted
from runtime-facing settings while the raw values remain in `to_value()` for
repair, with diagnostics identifying the instance key and field path.

**Verified.** The new invalid-instance regression and all 53
`mesh-core-config` library tests pass. The focused shell props integration test
passes in the Nix development environment, and `nix develop -c cargo check
--workspace` passes. The full shell library suite also ran with 775 passed, 46
failed, and 131 ignored; its failures are in unrelated renderer, interaction,
debug, lifecycle, and fixture coverage.
