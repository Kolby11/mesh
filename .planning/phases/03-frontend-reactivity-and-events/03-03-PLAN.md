---
phase: 03-frontend-reactivity-and-events
plan: 03
type: execute
wave: 3
depends_on:
  - "03-01"
  - "03-02"
files_modified:
  - packages/plugins/frontend/core/navigation-bar/src/components/volume-button.mesh
  - packages/plugins/frontend/core/navigation-bar/src/main.mesh
  - crates/core/shell/src/shell/component.rs
  - docs/plugins/frontend/core/README.md
autonomous: true
requirements:
  - FRONT-01
  - FRONT-02
  - FRONT-03
  - FRONT-04
  - FRONT-05
user_setup: []
must_haves:
  truths:
    - "D-12 D-13: the end-to-end proof lives in the existing navigation-bar plugin, with an inline volume slider that uses `on_change(value)` to call the audio command path."
    - "The proof exercises typed `on_change`, reactive global updates for icon/tooltip/slider state, service command publishing, and rerender behavior in one observable component."
    - "No new plugin or separate fixture is needed for the user-visible proof."
    - "FRONT-01 through FRONT-05 are all covered by automated runtime/shell tests plus the navigation-bar proof path."
  artifacts:
    - path: "packages/plugins/frontend/core/navigation-bar/src/components/volume-button.mesh"
      provides: "Real inline volume slider proof component"
      contains: "onVolumeChange"
    - path: "crates/core/shell/src/shell/component.rs"
      provides: "End-to-end shell test coverage for event-to-state-to-render proof"
      contains: "on_change"
    - path: "docs/plugins/frontend/core/README.md"
      provides: "Frontend event/reactivity documentation update if this phase touches public guidance"
      contains: "on_change"
  key_links:
    - from: "packages/plugins/frontend/core/navigation-bar/src/components/volume-button.mesh"
      to: "packages/plugins/backend/core/audio-interface/interface.toml"
      via: "`audio.set_volume` command shape used by inline slider handler"
      pattern: "set_volume"
    - from: "crates/core/shell/src/shell/component.rs"
      to: "packages/plugins/frontend/core/navigation-bar/src/components/volume-button.mesh"
      via: "test fixture mirrors the real component's slider event-to-state-to-command flow"
      pattern: "onVolumeChange"
---

<objective>
Add the real navigation-bar inline volume-slider proof and end-to-end checks that demonstrate Phase 3's event-to-state-to-render behavior in a shipped component.

Purpose: Runtime and dispatch tests prove internals, but the phase goal also requires a minimal interactive `.mesh` component that plugin authors can inspect and run.
Output: Updated navigation-bar volume component, shell-facing proof tests, and frontend docs if needed.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-frontend-reactivity-and-events/03-CONTEXT.md
@.planning/phases/03-frontend-reactivity-and-events/03-RESEARCH.md
@.planning/phases/03-frontend-reactivity-and-events/03-PATTERNS.md
@.planning/phases/03-frontend-reactivity-and-events/03-UI-SPEC.md
@.planning/phases/03-frontend-reactivity-and-events/03-VALIDATION.md
@.planning/phases/03-frontend-reactivity-and-events/03-01-SUMMARY.md
@.planning/phases/03-frontend-reactivity-and-events/03-02-SUMMARY.md

@packages/plugins/frontend/core/navigation-bar/src/components/volume-button.mesh
@packages/plugins/frontend/core/navigation-bar/src/main.mesh
@packages/plugins/backend/core/audio-interface/interface.toml
@crates/core/shell/src/shell/component.rs
@docs/plugins/frontend/core/README.md
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| The proof component uses a synthetic fixture and does not validate a shipped surface | medium | Implement the proof inside `packages/plugins/frontend/core/navigation-bar/src/components/volume-button.mesh` or a sibling imported by it. |
| The slider calls an audio command shape that does not match the interface contract | high | Read `packages/plugins/backend/core/audio-interface/interface.toml` and use `set_volume(device_id, volume)` with concrete values such as `"default"` and a `0.0..1.0` float, unless the executed codebase has already normalized a different command payload shape. |
| The inline slider causes cramped navigation-bar layout or duplicate visible controls | medium | Keep it compact, token-based, and inside the existing volume widget group; preserve existing button click behavior and tooltip/ARIA copy. |
| The proof only changes service state, not reactive globals, so it fails to cover FRONT-01/FRONT-02 | medium | Update reactive globals such as `audio_percent`, `icon_name`, and `audio_tooltip` inside `onVolumeChange(value)` or `onRender()` and assert the next render observes the changed values. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add inline navigation-bar volume slider using generic on_change</name>
  <files>packages/plugins/frontend/core/navigation-bar/src/components/volume-button.mesh</files>
  <read_first>packages/plugins/frontend/core/navigation-bar/src/components/volume-button.mesh, packages/plugins/frontend/core/navigation-bar/src/main.mesh, packages/plugins/backend/core/audio-interface/interface.toml, .planning/phases/03-frontend-reactivity-and-events/03-UI-SPEC.md</read_first>
  <action>Update `packages/plugins/frontend/core/navigation-bar/src/components/volume-button.mesh` so the existing `button` click behavior remains intact and an inline compact `<slider>` is added near the volume icon. Add reactive globals `audio_percent = 0`, `slider_value = 0.0`, `icon_name = "audio-volume-muted"`, and `audio_tooltip = "Volume unavailable"` if they are not already present. In `onRender()`, read `audio.percent` and `audio.muted`, derive `audio_percent`, `slider_value`, `icon_name`, and `audio_tooltip`, and keep unavailable fallback copy. Add `function onVolumeChange(value)` that receives the typed slider number, clamps it to the accepted range, updates `slider_value` and `audio_percent`, updates `icon_name`/`audio_tooltip`, and calls the audio command method using the contract shape: `audio:set_volume("default", normalized)` where `normalized` is a `0.0..1.0` float. If the implementation's current proxy command convention requires dot-call instead of colon-call, use `audio.set_volume("default", normalized)` consistently with existing runtime tests. Do not remove `onVolumeClick(event)` or the existing surface toggle events.</action>
  <verify>rg -n "onVolumeChange|<slider|set_volume|audio_percent|slider_value|onVolumeClick" packages/plugins/frontend/core/navigation-bar/src/components/volume-button.mesh</verify>
  <acceptance_criteria>
    - `volume-button.mesh` contains an inline `<slider` with `onchange={onVolumeChange}` or the parser-supported equivalent.
    - `volume-button.mesh` contains `function onVolumeChange(value)`.
    - `volume-button.mesh` contains a call to `set_volume` with concrete `device_id` and `volume` arguments or the current codebase's documented equivalent.
    - Existing `function onVolumeClick(event)` remains present.
    - `audio_tooltip` remains non-empty when audio is unavailable.
  </acceptance_criteria>
  <done>The shipped navigation bar contains the Phase 3 interactive proof component.</done>
</task>

<task type="auto">
  <name>Task 2: Add end-to-end shell proof for event-to-state-to-render and service command publishing</name>
  <files>crates/core/shell/src/shell/component.rs</files>
  <read_first>crates/core/shell/src/shell/component.rs, crates/core/runtime/scripting/src/context.rs, crates/core/shell/src/shell/service.rs, packages/plugins/frontend/core/navigation-bar/src/components/volume-button.mesh, packages/plugins/backend/core/audio-interface/interface.toml</read_first>
  <action>Add or extend tests in `crates/core/shell/src/shell/component.rs` that mirror the navigation-bar inline slider behavior. The test must create a frontend script with `audio_percent`, `slider_value`, `icon_name`, `audio_tooltip`, `onRender()`, and `onVolumeChange(value)`; seed an audio proxy payload; dispatch a slider change through the shell event path; assert the Luau handler receives a number; assert reactive globals change and mark dirty; assert the next build/paint sees the updated state; and assert a published `mesh.audio.set_volume` command becomes `CoreRequest::ServiceCommand` with the expected payload. Include a failure variant where `onVolumeChange` raises an error and diagnostics records it without losing the last rendered tree.</action>
  <verify>cargo test -p mesh-core-shell</verify>
  <acceptance_criteria>
    - A shell test named `navigation_volume_slider_proves_event_state_render_flow` or equivalent exists.
    - The test asserts a slider `on_change` path publishes `mesh.audio.set_volume` or yields `CoreRequest::ServiceCommand { interface: "mesh.audio", command: "set_volume", ... }`.
    - The test asserts `audio_percent` or `slider_value` changed after the handler call.
    - The test asserts dirty state drives a rebuild/paint path before being cleared.
    - A failure-path test proves diagnostics records a thrown slider handler error and `last_tree` remains available.
  </acceptance_criteria>
  <done>The phase has an automated end-to-end proof for FRONT-01 through FRONT-05.</done>
</task>

<task type="auto">
  <name>Task 3: Update frontend event/reactivity guidance if public docs mention old behavior</name>
  <files>docs/plugins/frontend/core/README.md</files>
  <read_first>docs/plugins/frontend/core/README.md, packages/plugins/frontend/core/navigation-bar/src/components/volume-button.mesh, .planning/phases/03-frontend-reactivity-and-events/03-CONTEXT.md</read_first>
  <action>Inspect `docs/plugins/frontend/core/README.md` for frontend event/reactivity guidance. If it already documents `on_click`, `on_change`, reactive globals, and diagnostics accurately, leave it unchanged. If it omits or contradicts Phase 3 behavior, add a concise section showing a copyable `.mesh` snippet with a slider `onchange={onVolumeChange}`, a typed `value` argument, a reactive global assignment, and `pcall(require, "@mesh/audio@>=1.0")` fallback copy. Explicitly state that handler failures appear in diagnostics and do not crash rendering. Do not reintroduce service proxy callback APIs such as `audio.on_change(...)`.</action>
  <verify>rg -n "onchange|on_change|reactive global|diagnostics|audio\\.on_change|mesh\\.service\\.bind|mesh\\.service\\.on" docs/plugins/frontend/core/README.md</verify>
  <acceptance_criteria>
    - If docs are changed, `docs/plugins/frontend/core/README.md` contains a slider event example with a typed `value` argument.
    - If docs are changed, the docs mention reactive globals and diagnostics for handler failures.
    - `docs/plugins/frontend/core/README.md` does not teach `audio.on_change(...)`, `mesh.service.bind(...)`, or `mesh.service.on(...)` as the current event/reactivity model.
  </acceptance_criteria>
  <done>Public frontend guidance matches the shipped Phase 3 proof behavior when docs need an update.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cargo test -p mesh-core-shell` passes.
- [ ] `rg -n "onVolumeChange|<slider|set_volume|audio_percent|slider_value" packages/plugins/frontend/core/navigation-bar/src/components/volume-button.mesh` shows the proof component.
- [ ] `rg -n "navigation_volume_slider|event_state_render|set_volume|handler error" crates/core/shell/src/shell/component.rs` shows end-to-end tests.
- [ ] `rg -n "audio\\.on_change|mesh\\.service\\.bind|mesh\\.service\\.on" packages/plugins/frontend/core/navigation-bar/src/components/volume-button.mesh docs/plugins/frontend/core/README.md` returns no current-model regressions.
</verification>

<success_criteria>
- Navigation-bar includes a real inline volume slider proof.
- Dragging the slider routes a typed `on_change` value into Luau.
- The handler updates reactive globals and publishes an audio command.
- The next paint observes updated script state.
- Handler failures remain visible and non-fatal.
</success_criteria>

<output>
After completion, create `.planning/phases/03-frontend-reactivity-and-events/03-03-SUMMARY.md`
</output>
