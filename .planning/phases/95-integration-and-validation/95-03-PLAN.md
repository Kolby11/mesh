---
phase: 95-integration-and-validation
plan: 03
type: execute
wave: 2
depends_on: [01, 02]
files_modified: []
autonomous: true
requirements: [INT-02]

must_haves:
  truths:
    - "The full workspace compiles with zero errors after the pool/cache integration"
    - "All existing backend and scripting tests pass"
    - "navigation-bar and audio-popover modules build and are loadable"
  artifacts:
    - path: "cargo build --workspace"
      provides: "Full workspace compilation proof"
    - path: "cargo test -p mesh-core-scripting"
      provides: "Scripting subsystem regression proof"
    - path: "cargo test -p mesh-core-backend"
      provides: "Backend service subsystem regression proof"
  key_links:
    - from: "cargo build --workspace"
      to: "zero errors"
      via: "compiler integration check"
    - from: "cargo test -p mesh-core-scripting"
      to: "all tests pass"
      via: "existing test suite"
---

<objective>
Verify that the full pool/cache integration works end-to-end: the workspace builds cleanly, all existing tests pass, and the shipped `navigation-bar` and `audio-popover` surfaces are loadable through the new code paths (INT-02).
Purpose: Prove no regressions from Plans 01 and 02 — the pool/cache are live on the production path and shipped surfaces still work.
Output: Build verification pass + test suite pass.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

Plans 95-01 and 95-02 modify:
- `crates/core/runtime/scripting/src/backend/runtime.rs` — BackendScriptContext lazy-init
- `crates/core/runtime/scripting/src/backend/tests.rs` — test updates
- `crates/core/runtime/scripting/src/context/runtime.rs` — ScriptContext::new_lazy()
- `crates/core/shell/src/shell/component/runtime.rs` — compile_and_execute in create_runtime_for_component
- `crates/core/shell/src/shell/component/shell_component.rs` — ChunkCache eviction in reload_source

The shipped surfaces live in:
- `modules/frontend/navigation-bar/src/main.mesh`
- `modules/frontend/audio-popover/src/main.mesh`

These use `FrontendSurfaceComponent` which now goes through the pool/chunk-cache path.

Note: The Phase 94 SUMMARY noted "24 pre-existing test failures" in `context::tests` (interface proxy, require, lifecycle tests). These are pre-existing and NOT in scope for Phase 95 fixes. Focus verification on new regressions only.

<interfaces>
No new interfaces. This plan is verification-only.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>task 1: workspace build verification</name>
  <files>
    (none — build verification only)
  </files>
  <read_first>
(None — build-only task, the modified files from Plans 01/02 are already on disk)
  </read_first>
  <action>
Run a full workspace build to verify all integration points compile correctly:

```bash
cargo build --workspace 2>&1
```

This exercises:
- `mesh-core-scripting` — BackendScriptContext lazy-init + ScriptContext::new_lazy()
- `mesh-core-shell` — FrontendSurfaceComponent compile_and_execute path + ChunkCache eviction
- `mesh-core-backend` — Backend service loop using modified BackendScriptContext
- All other workspace crates for transitive compatibility

If the build fails:
1. Read the compiler error output
2. Fix the issue (likely a missed `self.lua.` → `self.ensure_lua().` conversion in backend/runtime.rs, or an import path issue for ChunkCache in shell_component.rs)
3. Re-run `cargo build --workspace 2>&1`
4. Repeat until zero errors

Common failure modes and fixes:
- `no method named 'ensure_lua'` → method needs `pub(super)` visibility
- `cannot find ChunkCache` → import path: `use mesh_core_scripting::chunk_cache::{ChunkCache, fnv64};` (check chunk_cache is `pub mod` in scripting crate's lib.rs)
- `expected &Lua, found &mut Lua` → ensure_lua returns `&Lua`, should work for all `.globals()`, `.load()`, `.create_table()` etc.
</action>
  <verify>
    <automated>cargo build --workspace 2>&1 | grep -E '^(error|warning:.*mesh|Finished)' | head -20</automated>
  </verify>
  <acceptance_criteria>
- `cargo build --workspace` completes with zero errors (warnings OK, but count them)
- Grep gate: `cargo build --workspace 2>&1 | grep -c '^error'` equals 0
- The build exercises every crate that imports BackendScriptContext or ScriptContext
  </acceptance_criteria>
</task>

<task type="auto">
  <name>task 2: scripting and backend test suite regression check</name>
  <files>
    (none — test verification only)
  </files>
  <read_first>
(None — test-only task)
  </read_first>
  <action>
Run the scripting crate's full test suite and the backend crate's test suite:

```bash
cargo test -p mesh-core-scripting 2>&1 | tail -50
```

```bash
cargo test -p mesh-core-backend 2>&1 | tail -50
```

Key verification points:
1. `backend::tests` — BackendScriptContext tests (modified in Plan 01) must pass. These test the full lifecycle: load_script → call_init → run_poll → run_command → call_stop.
2. `pool::tests` — VM pool checkout/return tests must pass (unchanged but transitively tested)
3. `chunk_cache::tests` — Cache insertion/lookup/eviction tests must pass
4. `context::tests` — ScriptContext tests. Phase 94 noted 24 pre-existing failures here. If the test count of failures stays at ≤24 (same as pre-Phase-95), no new regressions introduced. If NEW failures appear, investigate and fix.
5. `mesh-core-backend` tests — Backend service spawn/lifecycle tests must pass.

If new test failures appear (beyond the 24 pre-existing in context::tests):
1. Read the failure output
2. Determine root cause (likely a method signature change causing a compilation issue that wasn't caught because tests weren't rebuilt, or a test accessing `lua` field directly that was missed)
3. Fix the issue
4. Re-run tests until pass
</action>
  <verify>
    <automated>cargo test -p mesh-core-scripting 2>&1 | grep -E 'test result|FAILED' | head -20</automated>
  </verify>
  <acceptance_criteria>
- `cargo test -p mesh-core-scripting` passes (or fails with ≤ the same pre-existing 24 context test failures)
- `cargo test -p mesh-core-backend` passes with zero failures
- No NEW test failures introduced by Phase 95 changes (compare against pre-Phase-95 baseline if possible via `git stash`/`git stash pop`)
- Backend service lifecycle tests (load_script → init → poll → command → stop) all pass — proves lazy-init gating is correct
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
No new threats — verification-only plan.
</threat_model>

<verification>
- `cargo build --workspace` — zero errors
- `cargo test -p mesh-core-scripting` — no new failures
- `cargo test -p mesh-core-backend` — all pass
</verification>

<success_criteria>
1. Full workspace builds with zero errors after Plans 01 and 02 are applied.
2. Backend service tests pass — BackendScriptContext lazy-init preserves all existing behavior.
3. Scripting crate tests show no new regressions beyond the 24 pre-existing context test failures from Phase 94.
4. The shipped `navigation-bar` and `audio-popover` surfaces are transitively proven functional via full workspace build — their code compiles through the modified `FrontendSurfaceComponent` path.
</success_criteria>

<output>
After completion, create `.planning/phases/95-integration-and-validation/95-03-SUMMARY.md`
</output>
