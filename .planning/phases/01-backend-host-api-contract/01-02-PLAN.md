---
phase: 01-backend-host-api-contract
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - crates/core/runtime/backend/src/lib.rs
  - crates/core/shell/src/shell/mod.rs
  - crates/core/runtime/scripting/src/backend.rs
autonomous: true
requirements:
  - HOST-01
  - HOST-02
  - HOST-03
  - HOST-04
  - HOST-05
  - HOST-06
user_setup: []
must_haves:
  truths:
    - "D-03: Service-specific logic remains in Luau backend plugins; Rust only wires runtime and settings."
    - "D-05/D-09: Runtime-level backend failures are diagnosable and associated with plugin identity."
    - "D-06: Shell command compatibility is preserved while structured exec remains the safer path."
    - "D-11: `mesh.service.set_poll_interval(ms)` affects backend polling without shell restart."
    - "D-12: Existing `emit_json` and `emit_unavailable` behavior remains compatible."
  artifacts:
    - path: "crates/core/runtime/backend/src/lib.rs"
      provides: "Backend runtime polling, command dispatch, and update channel integration"
      contains: "spawn_backend_service"
    - path: "crates/core/shell/src/shell/mod.rs"
      provides: "Shell backend spawn plumbing with settings/config passed to backend runtime"
      contains: "spawn_backend_service"
    - path: "crates/core/runtime/scripting/src/backend.rs"
      provides: "BackendScriptContext constructor path consumed by backend runtime"
      contains: "new_with_capabilities"
  key_links:
    - from: "crates/core/shell/src/shell/mod.rs"
      to: "crates/core/runtime/backend/src/lib.rs"
      via: "spawn_backend_service arguments include plugin settings/config"
      pattern: "spawn_backend_service"
    - from: "crates/core/runtime/backend/src/lib.rs"
      to: "crates/core/runtime/scripting/src/backend.rs"
      via: "BackendScriptContext construction and polling interval reads"
      pattern: "poll_interval_ms"
---

<objective>
Wire the stabilized backend host API through the async backend runtime and shell spawn path.

Purpose: Backend service plugins should receive their settings, emit updates, and adjust polling cadence in the real runtime, not only in isolated unit tests.
Output: Runtime integration changes and tests proving settings propagation, update emission, and poll interval behavior.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-backend-host-api-contract/01-CONTEXT.md
@.planning/phases/01-backend-host-api-contract/01-RESEARCH.md
@.planning/phases/01-backend-host-api-contract/01-PATTERNS.md
@.planning/phases/01-backend-host-api-contract/01-VALIDATION.md
@.planning/phases/01-backend-host-api-contract/01-01-SUMMARY.md

@crates/core/runtime/backend/src/lib.rs
@crates/core/runtime/scripting/src/backend.rs
@crates/core/shell/src/shell/mod.rs
@packages/plugins/backend/core/networkmanager-network/src/main.luau
@packages/plugins/backend/core/upower-power/src/main.luau
@packages/plugins/backend/core/shell-theme/src/main.luau
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| A backend script lowers poll interval to zero and busy-loops | high | Continue clamping intervals with `.max(50)` or an equivalent lower bound in the runtime loop. |
| Shell settings passed to the wrong backend plugin | medium | Pass settings through the same plugin instance used to spawn the script and test settings by plugin context. |
| Backend command failures disappear in async task logs | medium | Include plugin id and handler name in warnings/errors emitted by backend runtime paths. |
| Integration changes accidentally require service logic in Rust | low | Keep audio/network/power/media logic in existing Luau scripts and add only generic runtime plumbing. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Thread plugin settings from shell spawn to BackendScriptContext</name>
  <files>crates/core/shell/src/shell/mod.rs, crates/core/runtime/backend/src/lib.rs, crates/core/runtime/scripting/src/backend.rs</files>
  <read_first>crates/core/shell/src/shell/mod.rs, crates/core/runtime/backend/src/lib.rs, crates/core/runtime/scripting/src/backend.rs, crates/core/extension/plugin/src/manifest.rs, crates/core/foundation/config/src/lib.rs</read_first>
  <action>Extend `spawn_backend_service` to accept backend plugin settings as a `serde_json::Value` or a small config struct containing `settings: serde_json::Value`. In `crates/core/shell/src/shell/mod.rs`, find the backend plugin instance settings available near the spawn site and pass that JSON value into `spawn_backend_service`. In `crates/core/runtime/backend/src/lib.rs`, construct `BackendScriptContext` with capabilities plus settings so `mesh.config()` from Plan 01 works in real backend plugins. Keep a default `{}` value for call sites or tests that do not provide settings.</action>
  <verify>cargo test -p mesh-core-backend && cargo test -p mesh-core-scripting config_returns_backend_settings</verify>
  <acceptance_criteria>
    - `crates/core/runtime/backend/src/lib.rs` has a `spawn_backend_service` parameter or config struct field for `settings`.
    - `crates/core/runtime/backend/src/lib.rs` passes settings into a `BackendScriptContext` constructor.
    - `crates/core/shell/src/shell/mod.rs` passes a plugin-specific settings JSON value into `spawn_backend_service`.
    - `cargo test -p mesh-core-scripting config_returns_backend_settings` passes after shell/runtime integration changes.
  </acceptance_criteria>
  <done>HOST-03 works through the real backend runtime path, not only isolated unit tests.</done>
</task>

<task type="auto">
  <name>Task 2: Make set_poll_interval effective while backend runtime is running</name>
  <files>crates/core/runtime/backend/src/lib.rs, crates/core/runtime/scripting/src/backend.rs</files>
  <read_first>crates/core/runtime/backend/src/lib.rs, crates/core/runtime/scripting/src/backend.rs, .planning/phases/01-backend-host-api-contract/01-RESEARCH.md</read_first>
  <action>Update `spawn_backend_service` so it observes `ctx.poll_interval_ms().max(50)` after `init()`, after `on_poll()`, and after command handlers. If the value differs from the active interval, recreate or reset the Tokio interval with the new bounded duration. Preserve payload deduplication. Add tests using Tokio time controls or broad timing bounds proving an `on_command_*` or `on_poll()` call that runs `mesh.service.set_poll_interval(100)` updates the active interval without restarting the service task.</action>
  <verify>cargo test -p mesh-core-backend</verify>
  <acceptance_criteria>
    - `crates/core/runtime/backend/src/lib.rs` contains logic comparing the active interval to `ctx.poll_interval_ms().max(50)` after runtime callbacks.
    - `crates/core/runtime/backend/src/lib.rs` recreates or resets the Tokio interval when the bounded interval changes.
    - A backend runtime test proves `set_poll_interval` after startup changes the polling cadence or active interval state.
    - The lower bound `max(50)` or an equivalent minimum remains present.
  </acceptance_criteria>
  <done>HOST-06 is implemented in the async backend loop.</done>
</task>

<task type="auto">
  <name>Task 3: Add runtime compatibility coverage for bundled backend APIs</name>
  <files>crates/core/runtime/backend/src/lib.rs, crates/core/runtime/scripting/src/backend.rs</files>
  <read_first>crates/core/runtime/backend/src/lib.rs, crates/core/runtime/scripting/src/backend.rs, packages/plugins/backend/core/pipewire-audio/src/main.luau, packages/plugins/backend/core/pulseaudio-audio/src/main.luau, packages/plugins/backend/core/networkmanager-network/src/main.luau, packages/plugins/backend/core/upower-power/src/main.luau, packages/plugins/backend/core/shell-theme/src/main.luau</read_first>
  <action>Add or update tests that load representative bundled backend scripts where practical and prove their required host APIs still exist. At minimum, keep the existing shell-theme script test passing and add assertions that the host API table supports `mesh.exec_shell`, `mesh.service.emit`, `mesh.service.emit_unavailable`, `mesh.service.set_poll_interval`, `mesh.service.payload`, `mesh.log`, and `mesh.config`. Do not require real system tools such as `wpctl`, `pactl`, `nmcli`, or `upower` to be present in tests.</action>
  <verify>cargo test -p mesh-core-scripting && cargo test -p mesh-core-backend</verify>
  <acceptance_criteria>
    - Tests do not execute `wpctl`, `pactl`, `nmcli`, `bluetoothctl`, `upower`, or `playerctl` as hard dependencies.
    - `cargo test -p mesh-core-scripting` passes.
    - `cargo test -p mesh-core-backend` passes.
    - Existing `shell_theme_backend_accepts_current_payload` or equivalent bundled-script test still passes.
  </acceptance_criteria>
  <done>HOST-01 through HOST-06 are covered against runtime integration and compatibility fixtures.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cargo test -p mesh-core-scripting` passes.
- [ ] `cargo test -p mesh-core-backend` passes.
- [ ] `rg -n "set_poll_interval|poll_interval_ms|max\\(50\\)" crates/core/runtime/backend/src/lib.rs crates/core/runtime/scripting/src/backend.rs` shows bounded dynamic interval handling.
- [ ] `rg -n "spawn_backend_service\\(" crates/core/shell/src/shell/mod.rs crates/core/runtime/backend/src/lib.rs` shows the updated settings-aware call shape.
</verification>

<success_criteria>
- HOST-06 is implemented in the running backend service loop.
- Backend settings reach `mesh.config()` through the shell spawn path.
- Backend update emission still flows through `BackendServiceUpdate`.
- Bundled backend plugin compatibility is preserved without moving service logic into Rust.
- Runtime diagnostics include enough plugin identity to debug failures.
</success_criteria>

<output>
After completion, create `.planning/phases/01-backend-host-api-contract/01-02-SUMMARY.md`
</output>
