---
phase: 01-backend-host-api-contract
plan: 03
type: execute
gap_closure: true
wave: 3
depends_on:
  - "01-01"
  - "01-02"
files_modified:
  - crates/core/runtime/scripting/src/backend.rs
  - crates/tools/lsp/src/knowledge/mesh_api.rs
  - docs/plugins/backend/core/README.md
autonomous: true
requirements:
  - HOST-05
user_setup: []
must_haves:
  truths:
    - "D-05: Host API misuse, serialization failures, and runtime-level failures are visible through explicit Luau errors or diagnostics, not silent no-ops."
    - "D-12: Existing `emit_json` and `emit_unavailable` compatibility behavior remains available for backend plugins and docs/LSP stay aligned with the real host API."
  artifacts:
    - path: "crates/core/runtime/scripting/src/backend.rs"
      provides: "Backend `mesh.service.emit_json(value?)` implementation plus regression tests"
      contains: "\"emit_json\""
    - path: "crates/tools/lsp/src/knowledge/mesh_api.rs"
      provides: "LSP-facing backend API signature for `mesh.service.emit_json`"
      contains: "service.emit_json"
    - path: "docs/plugins/backend/core/README.md"
      provides: "Plugin-author backend API contract for `mesh.service.emit_json(value?)`"
      contains: "mesh.service.emit_json(value?)"
  key_links:
    - from: "crates/core/runtime/scripting/src/backend.rs"
      to: "packages/plugins/backend/core/upower-power/src/main.luau"
      via: "compatibility for string-based `mesh.service.emit_json(result.stdout)` remains intact while nil/table forms are restored"
      pattern: "emit_json"
    - from: "crates/core/runtime/scripting/src/backend.rs"
      to: "crates/tools/lsp/src/knowledge/mesh_api.rs"
      via: "runtime behavior and editor-facing API knowledge describe the same `emit_json(value?)` contract"
      pattern: "service.emit_json"
---

<objective>
Close the Phase 01 verification gaps around `mesh.service.emit_json(...)`.

Purpose: Phase 01 only passes if backend plugins keep the documented compatibility forms for `emit_json` and malformed payloads fail visibly instead of disappearing silently.
Output: Updated backend `emit_json(value?)` host API behavior, aligned API knowledge/docs, and regression coverage for string, table, nil/current-payload, and invalid JSON inputs.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-backend-host-api-contract/01-CONTEXT.md
@.planning/phases/01-backend-host-api-contract/01-RESEARCH.md
@.planning/phases/01-backend-host-api-contract/01-REVIEW.md
@.planning/phases/01-backend-host-api-contract/01-VERIFICATION.md
@.planning/phases/01-backend-host-api-contract/01-01-SUMMARY.md
@.planning/phases/01-backend-host-api-contract/01-02-SUMMARY.md

@crates/core/runtime/scripting/src/backend.rs
@crates/tools/lsp/src/knowledge/mesh_api.rs
@docs/plugins/backend/core/README.md
@packages/plugins/backend/core/upower-power/src/main.luau
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| Restoring `emit_json(nil)` accidentally re-emits stale state from a previous handler instead of the current command payload | high | Source nil fallback from `runtime.current_payload`, which is reset per handler call, and add a regression test proving `emit_json(nil)` mirrors the active command payload. |
| Invalid JSON still fails silently after the compatibility refactor | high | Parse JSON strings with `serde_json::from_str(...).map_err(mlua::Error::external)` and add a direct host-function test that asserts invalid JSON returns an error. |
| Docs/LSP continue describing a different contract than the runtime | medium | Update `mesh_api.rs` and `docs/plugins/backend/core/README.md` in the same plan and verify exact signature strings with `rg`. |
| Table compatibility broadens behavior in a way that breaks existing string callers | low | Preserve the existing string path unchanged for `emit_json(result.stdout)` and keep the bundled `upower-power` pattern as a compatibility fixture. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Restore `mesh.service.emit_json(value?)` compatibility in the backend runtime</name>
  <files>crates/core/runtime/scripting/src/backend.rs</files>
  <read_first>crates/core/runtime/scripting/src/backend.rs, .planning/phases/01-backend-host-api-contract/01-VERIFICATION.md, packages/plugins/backend/core/upower-power/src/main.luau</read_first>
  <action>Change the backend host API registration for `mesh.service.emit_json` from `text: String` to `value: Option<LuaValue>`. Implement these exact branches: `None` or `Some(LuaValue::Nil)` must clone `runtime.current_payload`; `Some(LuaValue::String(text))` must parse `text.to_str()?.trim()` with `serde_json::from_str::<JsonValue>(...)`; any other Lua value must convert with `lua.from_value::<JsonValue>(other)`. On success set `runtime.pending_emit = Some(payload)`. On invalid JSON or incompatible Lua values, return `mlua::Error` instead of `Ok(())`. Leave `mesh.service.emit_unavailable()` behavior unchanged.</action>
  <verify>cargo test -p mesh-core-scripting emit_json</verify>
  <acceptance_criteria>
    - `crates/core/runtime/scripting/src/backend.rs` contains `create_function(move |lua, value: Option<LuaValue>|` for `"emit_json"`.
    - `crates/core/runtime/scripting/src/backend.rs` contains a `None | Some(LuaValue::Nil)` branch that uses `runtime.current_payload.clone()`.
    - `crates/core/runtime/scripting/src/backend.rs` contains `serde_json::from_str::<JsonValue>` inside the string branch and returns an `mlua::Error` on parse failure.
    - `packages/plugins/backend/core/upower-power/src/main.luau` remains unchanged and still calls `mesh.service.emit_json(result.stdout)`.
  </acceptance_criteria>
  <done>The backend runtime once again accepts string, Lua-table, and nil/current-payload `emit_json` forms without regressing the existing string-based bundled backend pattern.</done>
</task>

<task type="auto">
  <name>Task 2: Add regression tests for visible `emit_json` failures and restored fallback behavior</name>
  <files>crates/core/runtime/scripting/src/backend.rs</files>
  <read_first>crates/core/runtime/scripting/src/backend.rs, .planning/phases/01-backend-host-api-contract/01-REVIEW.md, .planning/phases/01-backend-host-api-contract/01-VERIFICATION.md</read_first>
  <action>Add focused tests in `crates/core/runtime/scripting/src/backend.rs` covering: `mesh.service.emit_json(r.stdout)` still parsing a JSON string; `mesh.service.emit_json({ available = true, percent = 65 })` emitting a Lua table directly; `mesh.service.emit_json(nil)` during a command handler emitting the current command payload; and invalid JSON producing a visible failure. For the invalid JSON case, call the registered `emit_json` host function directly from the Lua globals/service table and assert the function call returns `Err(...)` for a malformed string such as `"{not-json}"`. Also keep the no-stale-payload guarantee by asserting a failed `emit_json` call does not produce a payload.</action>
  <verify>cargo test -p mesh-core-scripting backend</verify>
  <acceptance_criteria>
    - `crates/core/runtime/scripting/src/backend.rs` contains a test named `emit_json_accepts_lua_table` or equivalent that emits `{ available = true, percent = 65 }` and asserts both fields.
    - `crates/core/runtime/scripting/src/backend.rs` contains a test named `emit_json_nil_uses_current_command_payload` or equivalent that calls `run_command(...)` and verifies the emitted payload matches the command payload.
    - `crates/core/runtime/scripting/src/backend.rs` contains a test named `emit_json_rejects_invalid_json_string` or equivalent that asserts the host function call returns `Err`.
    - `cargo test -p mesh-core-scripting backend` exits successfully after the new tests are added.
  </acceptance_criteria>
  <done>The failed verification truths now have automated coverage proving both compatibility and visible failure behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Align editor and plugin-author API contract text with the restored runtime behavior</name>
  <files>crates/tools/lsp/src/knowledge/mesh_api.rs, docs/plugins/backend/core/README.md</files>
  <read_first>crates/tools/lsp/src/knowledge/mesh_api.rs, docs/plugins/backend/core/README.md, crates/core/runtime/scripting/src/backend.rs</read_first>
  <action>Update the LSP API knowledge entry for `mesh.service.emit_json` so its signature and description reflect the restored `value?` contract rather than `json_string: string?` only. Update `docs/plugins/backend/core/README.md` so the backend ergonomics section explicitly states that `mesh.service.emit_json(value?)` accepts JSON text, a Lua table, or nil/current-payload fallback. Keep the wording consistent with the runtime behavior implemented in `backend.rs`.</action>
  <verify>rg -n "emit_json\\(value\\?\\)|Lua table|current command payload" crates/tools/lsp/src/knowledge/mesh_api.rs docs/plugins/backend/core/README.md crates/core/runtime/scripting/src/backend.rs</verify>
  <acceptance_criteria>
    - `crates/tools/lsp/src/knowledge/mesh_api.rs` no longer contains the exact string `mesh.service.emit_json(json_string: string?)`.
    - `crates/tools/lsp/src/knowledge/mesh_api.rs` contains `mesh.service.emit_json(value?)`.
    - `docs/plugins/backend/core/README.md` contains language that `emit_json(value?)` accepts JSON text, a Lua table, or current command payload fallback.
    - `rg -n "emit_json\\(value\\?\\)|current command payload" crates/tools/lsp/src/knowledge/mesh_api.rs docs/plugins/backend/core/README.md` returns matches.
  </acceptance_criteria>
  <done>The runtime, docs, and editor-facing API metadata all describe the same backend `emit_json` behavior.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cargo test -p mesh-core-scripting backend` passes.
- [ ] `rg -n "emit_json\\(value\\?\\)|current_payload|from_str::<JsonValue>" crates/core/runtime/scripting/src/backend.rs crates/tools/lsp/src/knowledge/mesh_api.rs docs/plugins/backend/core/README.md` shows the restored compatibility contract.
- [ ] `rg -n "mesh\\.service\\.emit_json\\(result\\.stdout\\)" packages/plugins/backend/core/upower-power/src/main.luau` still matches the bundled backend compatibility fixture.
</verification>

<success_criteria>
- `mesh.service.emit_json(...)` accepts string, Lua-table, and nil/current-payload compatibility forms again.
- Invalid JSON passed through `emit_json` fails visibly instead of being discarded silently.
- Existing `emit_unavailable` behavior remains untouched.
- Backend docs and LSP metadata match the runtime contract well enough for plugin authors and editor hints to stop disagreeing with the implementation.
</success_criteria>

<output>
After completion, create `.planning/phases/01-backend-host-api-contract/01-03-SUMMARY.md`
</output>
