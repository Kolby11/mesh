---
phase: 01-plugin-package-manifest-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/core/extension/plugin/src/package.rs
  - crates/core/extension/plugin/src/lib.rs
autonomous: true
requirements: [PINST-01, PINST-03, PINST-06]
requirements_addressed: [PINST-01, PINST-03, PINST-06]
user_setup: []
must_haves:
  truths:
    - "D-01: Phase 1 implements the plugin installation/package structure before backend lifecycle internals."
    - "D-02: the manifest is package.json-like and shell-owned."
    - "D-03: exact filename is planner discretion, but the manifest must be central rather than scattered per-plugin state."
    - "D-07: backend plugins declare category/service values such as audio, network, power, media, or shortcuts."
    - "D-11: Phase 1 is local installed-plugin/package graph only; no remote download, registry, signing, sandboxing, or marketplace UX."
  artifacts:
    - path: "crates/core/extension/plugin/src/package.rs"
      provides: "Typed installed-plugin package manifest parser and validator"
      contains: "InstalledPluginPackage"
    - path: "crates/core/extension/plugin/src/lib.rs"
      provides: "Public exports for package manifest types"
      contains: "InstalledPluginPackage"
---

<objective>
Add the raw installed-plugin package manifest schema and validation boundary.

Purpose: Phase 1 needs a package.json-like shell-owned file that can represent installed frontend plugins, installed backend plugins, backend categories, and local-only package graph semantics before graph resolution or shell startup consumes it.
Output: `mesh-core-plugin` exposes typed `InstalledPluginPackage` parsing and validation with tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-plugin-package-manifest-foundation/01-CONTEXT.md
@.planning/phases/01-plugin-package-manifest-foundation/01-RESEARCH.md
@.planning/phases/01-plugin-package-manifest-foundation/01-VALIDATION.md
@.planning/phases/01-plugin-package-manifest-foundation/01-PATTERNS.md

@crates/core/extension/plugin/src/manifest.rs
@crates/core/extension/plugin/src/lib.rs
@crates/core/extension/plugin/Cargo.toml
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| T-01-01: malformed package JSON silently produces an empty or permissive package graph | high | Parse through typed serde structs and return structured errors; tests must assert malformed JSON and empty required fields fail. |
| T-01-02: duplicate plugin IDs across frontend/backend sections produce ambiguous installed state | high | Validate plugin IDs are globally unique across all package sections. |
| T-01-03: backend plugin category is missing or inconsistent, preventing later provider selection | high | Require non-empty `category` on backend entries and expose it as a typed field. |
| T-01-04: Phase 1 accidentally implements remote download/signing scope | medium | Keep schema local-only; no URL, registry, signature, marketplace, or download execution behavior in this plan. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add package manifest schema and parser tests</name>
  <files>crates/core/extension/plugin/src/package.rs, crates/core/extension/plugin/src/lib.rs</files>
  <read_first>crates/core/extension/plugin/src/manifest.rs, crates/core/extension/plugin/src/lib.rs, .planning/phases/01-plugin-package-manifest-foundation/01-CONTEXT.md</read_first>
  <action>Create `crates/core/extension/plugin/src/package.rs`. Define raw schema structs named exactly `InstalledPluginPackage`, `InstalledPluginsSection`, `InstalledFrontendPlugin`, `InstalledBackendPlugin`, `FrontendBackendRequirements`, and `ProviderSelection`. Add `pub fn from_json_str(input: &str) -> Result<InstalledPluginPackage, PackageManifestError>` on `InstalledPluginPackage`. Expose the module from `crates/core/extension/plugin/src/lib.rs` with `pub mod package;` and public re-exports for `InstalledPluginPackage` and `PackageManifestError`. Add tests named `package_manifest_parses_frontend_backend_and_provider_selection` and `package_manifest_rejects_malformed_json`.</action>
  <verify>nix develop -c cargo test -p mesh-core-plugin package_manifest</verify>
  <acceptance_criteria>
    - `crates/core/extension/plugin/src/package.rs` contains `pub struct InstalledPluginPackage`.
    - `crates/core/extension/plugin/src/package.rs` contains `pub struct InstalledBackendPlugin`.
    - `crates/core/extension/plugin/src/package.rs` contains `pub struct FrontendBackendRequirements`.
    - `crates/core/extension/plugin/src/lib.rs` contains `pub mod package;`.
    - Test `package_manifest_parses_frontend_backend_and_provider_selection` parses frontend plugin `@mesh/panel`, backend plugin `@mesh/pipewire-audio`, category `audio`, and active provider `@mesh/pipewire-audio`.
    - Test `package_manifest_rejects_malformed_json` asserts `InstalledPluginPackage::from_json_str("{")` returns an error.
  </acceptance_criteria>
  <done>Raw package manifest syntax is typed, parsed, and publicly exported.</done>
</task>

<task type="auto">
  <name>Task 2: Validate local-only installed plugin package fields</name>
  <files>crates/core/extension/plugin/src/package.rs</files>
  <read_first>crates/core/extension/plugin/src/package.rs, crates/core/extension/plugin/src/manifest.rs, .planning/phases/01-plugin-package-manifest-foundation/01-VALIDATION.md</read_first>
  <action>Implement `InstalledPluginPackage::validate(&self) -> Result<(), PackageManifestError>`, and have `from_json_str` call it before returning. Validation rules: `version` must be `1`; every plugin `id` must be non-empty; plugin IDs must be unique across frontend and backend entries; every backend plugin `category` must be non-empty; every active provider entry must have a non-empty backend plugin ID; the schema must not require or interpret remote download/signing fields. Add tests named `package_manifest_rejects_duplicate_plugin_ids`, `package_manifest_rejects_backend_without_category`, and `package_manifest_rejects_unknown_schema_version`.</action>
  <verify>nix develop -c cargo test -p mesh-core-plugin package_manifest</verify>
  <acceptance_criteria>
    - `InstalledPluginPackage::from_json_str` calls `validate`.
    - Test `package_manifest_rejects_duplicate_plugin_ids` uses `@mesh/panel` in both frontend and backend entries and asserts an error.
    - Test `package_manifest_rejects_backend_without_category` asserts an empty backend `category` returns an error.
    - Test `package_manifest_rejects_unknown_schema_version` asserts `version: 2` returns an error mentioning supported version `1` or equivalent.
    - `grep -R "download\\|marketplace\\|signature" crates/core/extension/plugin/src/package.rs` has no remote-install implementation logic beyond comments or test strings that explicitly say out of scope.
  </acceptance_criteria>
  <done>Package manifest validation enforces local installed-plugin invariants.</done>
</task>

</tasks>

<verification>
- Run `nix develop -c cargo test -p mesh-core-plugin package_manifest`.
- Run `grep -R "InstalledPluginPackage\\|InstalledBackendPlugin\\|FrontendBackendRequirements\\|ProviderSelection" -n crates/core/extension/plugin/src`.
- Run `grep -R "download\\|marketplace\\|signature" -n crates/core/extension/plugin/src/package.rs` and confirm there is no remote-install behavior.
</verification>

<success_criteria>
- PINST-01 is covered by a typed central installed-plugin package manifest parser.
- PINST-03 is covered by required backend category fields.
- PINST-06 is preserved by local-only schema and validation scope.
- D-01, D-02, D-03, D-07, and D-11 are visible in this plan's must_haves.
</success_criteria>

<output>
After completion, create `.planning/phases/01-plugin-package-manifest-foundation/01-01-SUMMARY.md`
</output>
