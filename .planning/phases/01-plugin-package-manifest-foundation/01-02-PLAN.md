---
phase: 01-plugin-package-manifest-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/core/extension/plugin/src/package.rs
autonomous: true
requirements: [PINST-02, PINST-04, PINST-05]
requirements_addressed: [PINST-02, PINST-04, PINST-05]
user_setup: []
must_haves:
  truths:
    - "D-04: frontend plugins declare required backend plugins or backend service categories."
    - "D-05: enabling/installing a frontend plugin makes backend dependencies visible to the shell package graph."
    - "D-08: multiple backend plugins in the same category must be represented so users can choose which provider is active."
    - "D-09: provider activation remains hybrid: default/highest-priority provider plus explicit user override."
    - "D-10: the package graph feeds shell core settings/provider organization."
  artifacts:
    - path: "crates/core/extension/plugin/src/package.rs"
      provides: "Normalized installed-plugin graph and provider/dependency resolution"
      contains: "InstalledPluginGraph"
---

<objective>
Build the normalized installed-plugin graph from the raw package manifest.

Purpose: downstream shell lifecycle code should consume graph answers such as frontend backend requirements, backend providers by category, unresolved categories, and active provider selection instead of parsing raw JSON directly.
Output: `mesh-core-plugin` exposes `InstalledPluginGraph` with tests for frontend requirements, backend category grouping, and active provider validation.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-plugin-package-manifest-foundation/01-CONTEXT.md
@.planning/phases/01-plugin-package-manifest-foundation/01-RESEARCH.md
@.planning/phases/01-plugin-package-manifest-foundation/01-VALIDATION.md
@.planning/phases/01-plugin-package-manifest-foundation/01-PATTERNS.md

@crates/core/extension/plugin/src/package.rs
@packages/plugins/frontend/core/panel/plugin.json
@packages/plugins/frontend/core/quick-settings/plugin.json
@packages/plugins/backend/core/pipewire-audio/plugin.json
@packages/plugins/backend/core/pulseaudio-audio/plugin.json
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| T-01-05: active provider references an unknown backend plugin ID | high | Graph construction validates active provider IDs against installed backend entries. |
| T-01-06: active provider category mismatch silently selects wrong service provider | high | Graph construction validates that active provider `@id` belongs to the category key being selected. |
| T-01-07: frontend backend dependencies disappear after normalization | high | Graph exposes `requirements_for_frontend(plugin_id)` and tests exact backend categories/plugins. |
| T-01-08: multiple providers collapse to one too early | medium | Graph stores `backend_by_category` as a list and only exposes active choice separately. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add normalized graph types and frontend requirement lookup</name>
  <files>crates/core/extension/plugin/src/package.rs</files>
  <read_first>crates/core/extension/plugin/src/package.rs, .planning/phases/01-plugin-package-manifest-foundation/01-CONTEXT.md</read_first>
  <action>In `package.rs`, add `pub struct InstalledPluginGraph`, `pub struct BackendProviderNode`, `pub struct FrontendPluginNode`, and `pub struct FrontendBackendRequirementSet` or equivalent public graph/result types. Add `pub fn InstalledPluginGraph::from_package(package: InstalledPluginPackage) -> Result<Self, PackageManifestError>`. The graph must preserve frontend plugin IDs, backend plugin IDs, backend category values, and frontend `requires.backend_categories` and `requires.plugins` values. Add `pub fn requirements_for_frontend(&self, plugin_id: &str) -> Option<&FrontendBackendRequirementSet>` or equivalent. Add test `package_graph_exposes_frontend_backend_requirements` using `@mesh/quick-settings` requiring backend categories `audio`, `network`, and `power`, and plugin `@mesh/audio-interface`.</action>
  <verify>nix develop -c cargo test -p mesh-core-plugin package_graph</verify>
  <acceptance_criteria>
    - `crates/core/extension/plugin/src/package.rs` contains `pub struct InstalledPluginGraph`.
    - `crates/core/extension/plugin/src/package.rs` contains `requirements_for_frontend`.
    - Test `package_graph_exposes_frontend_backend_requirements` asserts `audio`, `network`, and `power` are present for `@mesh/quick-settings`.
    - Test `package_graph_exposes_frontend_backend_requirements` asserts `@mesh/audio-interface` is present as a required plugin dependency.
  </acceptance_criteria>
  <done>Frontend backend dependencies survive normalization into the installed plugin graph.</done>
</task>

<task type="auto">
  <name>Task 2: Group backend providers by category and validate active choices</name>
  <files>crates/core/extension/plugin/src/package.rs</files>
  <read_first>crates/core/extension/plugin/src/package.rs, packages/plugins/backend/core/pipewire-audio/plugin.json, packages/plugins/backend/core/pulseaudio-audio/plugin.json</read_first>
  <action>Extend `InstalledPluginGraph` with `pub fn backend_providers_for_category(&self, category: &str) -> &[BackendProviderNode]` or an equivalent accessor, and `pub fn active_provider(&self, category: &str) -> Option<&BackendProviderNode>` or equivalent. Graph construction must validate that every provider selection references an installed backend plugin whose `category` matches the provider selection key. Add tests named `package_graph_keeps_multiple_audio_providers`, `package_graph_returns_explicit_active_provider`, `package_graph_rejects_unknown_active_provider`, and `package_graph_rejects_active_provider_category_mismatch`. Use `@mesh/pipewire-audio` and `@mesh/pulseaudio-audio` as two backend plugins in category `audio`; active provider is `@mesh/pipewire-audio`.</action>
  <verify>nix develop -c cargo test -p mesh-core-plugin package_graph</verify>
  <acceptance_criteria>
    - Test `package_graph_keeps_multiple_audio_providers` asserts category `audio` has exactly two providers.
    - Test `package_graph_returns_explicit_active_provider` asserts category `audio` resolves to `@mesh/pipewire-audio`.
    - Test `package_graph_rejects_unknown_active_provider` asserts an active provider ID not listed under backend plugins returns an error.
    - Test `package_graph_rejects_active_provider_category_mismatch` asserts selecting a `network` backend under `audio` returns an error.
  </acceptance_criteria>
  <done>Provider category grouping and active provider override semantics are represented in the graph.</done>
</task>

<task type="auto">
  <name>Task 3: Report unresolved frontend backend categories</name>
  <files>crates/core/extension/plugin/src/package.rs</files>
  <read_first>crates/core/extension/plugin/src/package.rs, .planning/phases/01-plugin-package-manifest-foundation/01-RESEARCH.md</read_first>
  <action>Add an accessor such as `pub fn unresolved_backend_categories(&self) -> Vec<UnresolvedBackendCategory>` that compares every frontend `requires.backend_categories` entry against installed backend provider categories. Add test `package_graph_reports_unresolved_backend_category` where frontend plugin `@mesh/shortcuts-ui` requires category `shortcuts` and no backend plugin has category `shortcuts`; assert the unresolved result contains frontend plugin `@mesh/shortcuts-ui` and category `shortcuts`.</action>
  <verify>nix develop -c cargo test -p mesh-core-plugin package_graph</verify>
  <acceptance_criteria>
    - `crates/core/extension/plugin/src/package.rs` contains `unresolved_backend_categories`.
    - Test `package_graph_reports_unresolved_backend_category` asserts unresolved category `shortcuts`.
    - Test `package_graph_reports_unresolved_backend_category` asserts unresolved frontend plugin `@mesh/shortcuts-ui`.
    - `nix develop -c cargo test -p mesh-core-plugin package_graph` exits 0.
  </acceptance_criteria>
  <done>Package graph exposes dependency gaps without trying to download packages in Phase 1.</done>
</task>

</tasks>

<verification>
- Run `nix develop -c cargo test -p mesh-core-plugin package_graph`.
- Run `grep -R "InstalledPluginGraph\\|active_provider\\|unresolved_backend_categories\\|requirements_for_frontend" -n crates/core/extension/plugin/src/package.rs`.
</verification>

<success_criteria>
- PINST-02 is covered by frontend backend dependency lookup.
- PINST-04 is covered by multiple providers plus explicit active provider selection.
- PINST-05 is covered by normalized graph APIs.
- D-04, D-05, D-08, D-09, and D-10 are visible in this plan's must_haves.
</success_criteria>

<output>
After completion, create `.planning/phases/01-plugin-package-manifest-foundation/01-02-SUMMARY.md`
</output>
