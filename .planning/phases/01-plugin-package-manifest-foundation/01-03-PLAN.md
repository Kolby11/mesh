---
phase: 01-plugin-package-manifest-foundation
plan: 03
type: execute
wave: 2
depends_on: ["01-01", "01-02"]
files_modified:
  - config/plugins.json
  - crates/core/extension/plugin/src/package.rs
  - crates/core/shell/src/shell/mod.rs
autonomous: true
requirements: [PINST-01, PINST-04, PINST-05, PINST-06]
requirements_addressed: [PINST-01, PINST-04, PINST-05, PINST-06]
user_setup: []
must_haves:
  truths:
    - "D-02: the package manifest is shell-owned and package.json-like."
    - "D-06: backend-only plugins such as shortcuts must be installable directly."
    - "D-09: active provider selection is hybrid: default/highest-priority plus explicit user override."
    - "D-10: package graph feeds shell core settings/provider organization."
    - "D-12: Phase 2 backend lifecycle consumes this graph instead of relying only on implicit directory scanning and priority."
  artifacts:
    - path: "config/plugins.json"
      provides: "Repo-local sample/default installed-plugin package manifest"
      contains: "@mesh/pipewire-audio"
    - path: "crates/core/extension/plugin/src/package.rs"
      provides: "Default package manifest path/helper and loader"
      contains: "load_installed_plugin_graph"
    - path: "crates/core/shell/src/shell/mod.rs"
      provides: "Shell-facing proof that graph can be loaded and active providers read"
      contains: "plugin_package"
---

<objective>
Add the repo-local package manifest fixture and shell-facing loader/proof for active provider choices.

Purpose: Phase 1 must prove the central manifest is not just a parser; shell startup code can load the installed-plugin graph and later lifecycle phases can consume active provider choices.
Output: `config/plugins.json` exists, package graph loader helpers exist, and shell tests prove the active audio provider can be derived from the manifest without remote package behavior.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-plugin-package-manifest-foundation/01-CONTEXT.md
@.planning/phases/01-plugin-package-manifest-foundation/01-RESEARCH.md
@.planning/phases/01-plugin-package-manifest-foundation/01-VALIDATION.md
@.planning/phases/01-plugin-package-manifest-foundation/01-PATTERNS.md

@config/shell-settings.json
@packages/plugins/frontend/core/panel/plugin.json
@packages/plugins/frontend/core/quick-settings/plugin.json
@packages/plugins/backend/core/pipewire-audio/plugin.json
@packages/plugins/backend/core/pulseaudio-audio/plugin.json
@crates/core/shell/src/shell/mod.rs
@crates/core/extension/plugin/src/package.rs
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| T-01-09: missing package manifest prevents shell from starting | high | Loader returns an empty/default graph or explicit non-fatal error path consistent with existing config fallback patterns. |
| T-01-10: sample manifest references nonexistent plugin IDs or categories | medium | Fixture uses existing repo plugin IDs and tests parse/resolve it. |
| T-01-11: shell integration accidentally starts using graph for lifecycle before graph semantics are complete | medium | Phase 1 adds load/proof helpers only; lifecycle consumption waits for Phase 2. |
| T-01-12: package graph adds remote install behavior prematurely | medium | Static checks confirm no marketplace/download/signature behavior in shell/package loader. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add repo-local package manifest fixture</name>
  <files>config/plugins.json</files>
  <read_first>packages/plugins/frontend/core/panel/plugin.json, packages/plugins/frontend/core/quick-settings/plugin.json, packages/plugins/backend/core/pipewire-audio/plugin.json, packages/plugins/backend/core/pulseaudio-audio/plugin.json, .planning/phases/01-plugin-package-manifest-foundation/01-CONTEXT.md</read_first>
  <action>Create `config/plugins.json` using the Phase 1 package schema. Include frontend entries for `@mesh/panel` and `@mesh/quick-settings` with `source` values `packages/plugins/frontend/core/panel` and `packages/plugins/frontend/core/quick-settings`. Include backend entries for `@mesh/pipewire-audio` and `@mesh/pulseaudio-audio`, both with `category` set to `audio`, `provides` set to `mesh.audio`, and sources pointing at their existing directories. Include provider selection `audio.active = "@mesh/pipewire-audio"`. Include one backend-only example entry `@mesh/shell-theme` with category `theme`, provides `mesh.theme`, and source `packages/plugins/backend/core/shell-theme` so direct backend install semantics are represented.</action>
  <verify>test -f config/plugins.json && grep -n "@mesh/pipewire-audio\\|@mesh/quick-settings\\|@mesh/shell-theme\\|audio" config/plugins.json</verify>
  <acceptance_criteria>
    - `config/plugins.json` contains `"version": 1`.
    - `config/plugins.json` contains `"@mesh/panel"`.
    - `config/plugins.json` contains `"@mesh/quick-settings"`.
    - `config/plugins.json` contains `"@mesh/pipewire-audio"`.
    - `config/plugins.json` contains `"@mesh/pulseaudio-audio"`.
    - `config/plugins.json` contains `"@mesh/shell-theme"`.
    - `config/plugins.json` contains `"active": "@mesh/pipewire-audio"`.
  </acceptance_criteria>
  <done>Repo-local package manifest fixture represents frontend installs, backend alternatives, and backend-only installs.</done>
</task>

<task type="auto">
  <name>Task 2: Add default path and graph loader helpers</name>
  <files>crates/core/extension/plugin/src/package.rs</files>
  <read_first>crates/core/extension/plugin/src/package.rs, crates/core/foundation/config/src/lib.rs, config/plugins.json</read_first>
  <action>Add path/helper functions in `package.rs`: `pub fn default_plugin_package_path() -> PathBuf` and `pub fn load_installed_plugin_graph(path: &Path) -> Result<InstalledPluginGraph, PackageManifestError>` or equivalent. `default_plugin_package_path()` must first honor a non-empty `MESH_PLUGIN_PACKAGE_PATH` environment variable, then use the repo-local `config/plugins.json` when it exists, then fall back to an XDG-style path such as `$XDG_CONFIG_HOME/mesh/plugins.json` through existing path conventions or a documented fallback. `load_installed_plugin_graph` must parse `InstalledPluginPackage::from_json_str` and return `InstalledPluginGraph::from_package`. Add test `plugin_package_default_path_prefers_repo_config_when_present` or equivalent that proves the repo-local path ends with `config/plugins.json` in this workspace. Add test `load_installed_plugin_graph_reads_config_plugins_json` that loads `config/plugins.json` and asserts active provider `audio` is `@mesh/pipewire-audio`.</action>
  <verify>nix develop -c cargo test -p mesh-core-plugin plugin_package</verify>
  <acceptance_criteria>
    - `crates/core/extension/plugin/src/package.rs` contains `default_plugin_package_path`.
    - `crates/core/extension/plugin/src/package.rs` contains `load_installed_plugin_graph`.
    - Test `load_installed_plugin_graph_reads_config_plugins_json` reads `config/plugins.json`.
    - Test `load_installed_plugin_graph_reads_config_plugins_json` asserts active provider `@mesh/pipewire-audio`.
    - `nix develop -c cargo test -p mesh-core-plugin plugin_package` exits 0.
  </acceptance_criteria>
  <done>Package graph has a shell-consumable loader and default path.</done>
</task>

<task type="auto">
  <name>Task 3: Add shell-facing package graph proof</name>
  <files>crates/core/shell/src/shell/mod.rs</files>
  <read_first>crates/core/shell/src/shell/mod.rs, crates/core/extension/plugin/src/package.rs, config/plugins.json</read_first>
  <action>Add a small shell-facing helper or test-only proof in `crates/core/shell/src/shell/mod.rs` that loads the installed plugin graph through `mesh_core_plugin::package::load_installed_plugin_graph` and confirms the shell can derive active provider choices. Do not rewrite `spawn_backend_plugins()` in Phase 1. Add a focused test named `plugin_package_graph_exposes_active_audio_provider_for_shell` or equivalent. The test must load `config/plugins.json`, assert `active_provider("audio")` has plugin ID `@mesh/pipewire-audio`, assert backend category `audio` has two providers, and assert backend category `theme` includes `@mesh/shell-theme` to prove backend-only package entries are supported.</action>
  <verify>nix develop -c cargo test -p mesh-core-shell plugin_package</verify>
  <acceptance_criteria>
    - `crates/core/shell/src/shell/mod.rs` contains `plugin_package_graph_exposes_active_audio_provider_for_shell`.
    - The shell test asserts active audio provider `@mesh/pipewire-audio`.
    - The shell test asserts category `audio` has exactly two providers.
    - The shell test asserts category `theme` includes `@mesh/shell-theme`.
    - `spawn_backend_plugins()` remains present and is not rewritten to consume the graph in this phase.
    - `nix develop -c cargo test -p mesh-core-shell plugin_package` exits 0.
  </acceptance_criteria>
  <done>Shell can load the package graph and read active provider choices without changing lifecycle spawning yet.</done>
</task>

</tasks>

<verification>
- Run `nix develop -c cargo test -p mesh-core-plugin -p mesh-core-shell plugin_package`.
- Run `grep -n "@mesh/pipewire-audio\\|@mesh/pulseaudio-audio\\|@mesh/shell-theme\\|active" config/plugins.json`.
- Run `grep -R "load_installed_plugin_graph\\|default_plugin_package_path\\|plugin_package_graph_exposes_active_audio_provider_for_shell" -n crates/core/extension/plugin/src crates/core/shell/src`.
- Run `grep -R "marketplace\\|signature\\|download" -n crates/core/extension/plugin/src/package.rs crates/core/shell/src/shell/mod.rs` and confirm Phase 1 has no remote install behavior.
</verification>

<success_criteria>
- PINST-01 is covered by repo-local `config/plugins.json`.
- PINST-04 is covered by active audio provider choice and multiple audio providers.
- PINST-05 is covered by shell-facing graph load proof.
- PINST-06 is preserved by static no-remote-install checks.
- D-02, D-06, D-09, D-10, and D-12 are visible in this plan's must_haves.
</success_criteria>

<output>
After completion, create `.planning/phases/01-plugin-package-manifest-foundation/01-03-SUMMARY.md`
</output>
