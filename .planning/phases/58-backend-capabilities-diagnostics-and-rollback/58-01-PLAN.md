---
phase: 58
plan: 1
slug: painter-backend-snapshot-and-rollback-docs
status: planned
---

# Plan 58-01: Painter Backend Snapshot And Rollback Docs

<objective>
Expose backend-neutral painter capability and diagnostic snapshots, then document how debug/proof code should consume them.
</objective>

<tasks>

## Task 1: Add public backend snapshot API
- Add snapshot structs for backend id, rollback authority, capabilities, and recent diagnostics.
- Implement `FrontendRenderEngine::paint_backend_snapshot()`.
- Re-export the snapshot types from the render crate.

## Task 2: Cover capability and diagnostic behavior
- Extend painter backend tests to assert Skia capability reporting, unsupported-feature diagnostics, snapshot fields, and diagnostic clearing.

## Task 3: Document rollback/diagnostics usage
- Update render crate docs with the intended snapshot consumption path and backend-neutrality rule.

</tasks>

<verification>

```bash
cargo fmt --check
nix develop -c cargo test -p mesh-core-render painter_backend
```

</verification>
