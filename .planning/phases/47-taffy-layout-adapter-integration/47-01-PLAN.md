---
phase: 47-taffy-layout-adapter-integration
plan: 01
title: Taffy Layout Ownership And Diagnostics Foundation
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/core/ui/elements/Cargo.toml
  - crates/core/ui/elements/src/layout.rs
  - docs/renderer-migration.md
  - docs/renderer-ownership.md
autonomous: true
requirements: [LAYT-01, LAYT-03]
requirements_addressed: [LAYT-01, LAYT-03]
user_setup: []
must_haves:
  truths:
    - "D-01: Replace relevant current layout engine code with Taffy for in-scope layout behavior."
    - "D-02: Phase 47 is a production replacement for layout, not proof-only scaffolding or an opt-in runtime experiment."
    - "D-03: Phase 47 overrides Phase 46 default-authority posture for layout only; Taffy is expected to take over relevant layout code."
    - "D-04: Unsupported Taffy cases are implementation gaps to diagnose and close, not silent runtime fallbacks."
    - "D-05: LAYT-03 is reconciled through visible diagnostics or blocker records, not hidden old-layout compatibility."
    - "D-06: Taffy mapping gaps must be mapped, narrowed with documentation/tests, or recorded as blockers."
  artifacts:
    - path: "crates/core/ui/elements/Cargo.toml"
      provides: "Taffy dependency in the crate that owns layout"
      contains: "taffy = { workspace = true }"
    - path: "crates/core/ui/elements/src/layout.rs"
      provides: "Taffy diagnostic and mapping foundation"
      contains: "TaffyLayoutDiagnostic"
  key_links:
    - "mesh-core-elements layout ownership -> workspace taffy dependency"
    - "unsupported Taffy mapping -> visible diagnostic records"
---

<objective>
Move Taffy dependency ownership to the layout-owning crate and add the diagnostic/mapping foundation needed to replace the current layout engine without preserving a hidden old-engine fallback.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/47-taffy-layout-adapter-integration/47-CONTEXT.md
@.planning/phases/47-taffy-layout-adapter-integration/47-RESEARCH.md
@.planning/phases/47-taffy-layout-adapter-integration/47-VALIDATION.md
@.planning/phases/47-taffy-layout-adapter-integration/47-PATTERNS.md
@Cargo.toml
@crates/core/ui/elements/Cargo.toml
@crates/core/ui/elements/src/layout.rs
@crates/core/frontend/render/Cargo.toml
@crates/core/frontend/render/src/library_adapters.rs
@docs/renderer-migration.md
@docs/renderer-ownership.md
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| T-47-01-01: Taffy remains owned by `mesh-core-render`, forcing layout replacement through the wrong crate boundary | high | Add `taffy = { workspace = true }` to `crates/core/ui/elements/Cargo.toml` and keep layout code in `mesh-core-elements`. |
| T-47-01-02: LAYT-03 is implemented as silent old-engine fallback, contradicting the user decision | high | Add explicit diagnostic/blocker vocabulary in `layout.rs` and docs stating unsupported cases are visible gaps, not compatibility fallback. |
| T-47-01-03: docs still classify Taffy layout as only adapter-owned evidence after Phase 47 starts replacement | medium | Update renderer migration/ownership docs to distinguish layout promotion from non-layout renderer-library scaffolding. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Move Taffy dependency ownership to mesh-core-elements</name>
  <files>crates/core/ui/elements/Cargo.toml</files>
  <read_first>crates/core/ui/elements/Cargo.toml, Cargo.toml, crates/core/frontend/render/Cargo.toml, .planning/phases/47-taffy-layout-adapter-integration/47-CONTEXT.md</read_first>
  <action>Add `taffy = { workspace = true }` to `crates/core/ui/elements/Cargo.toml` under `[dependencies]`. Leave the existing root workspace dependency `taffy = { version = "0.10.1", default-features = false, features = ["std", "taffy_tree", "flexbox", "grid", "block_layout", "calc", "content_size"] }` unchanged. Leave the existing optional `mesh-core-render` `renderer-taffy` feature scaffold in place for status reporting unless the compiler proves it is now unused and tests require cleanup. Do not move `LayoutEngine` into `mesh-core-render`.</action>
  <verify>env XDG_CACHE_HOME=/tmp/codex-nix-cache nix develop -c cargo check -p mesh-core-elements</verify>
  <acceptance_criteria>
    - `crates/core/ui/elements/Cargo.toml` contains `taffy = { workspace = true }`.
    - `Cargo.toml` still contains `taffy = { version = "0.10.1"`.
    - `crates/core/ui/elements/src/layout.rs` still contains `pub struct LayoutEngine`.
    - `env XDG_CACHE_HOME=/tmp/codex-nix-cache nix develop -c cargo check -p mesh-core-elements` exits 0.
  </acceptance_criteria>
  <done>`mesh-core-elements` can use Taffy directly while retaining layout ownership.</done>
</task>

<task type="auto">
  <name>Task 2: Add Taffy diagnostic and unsupported-case vocabulary</name>
  <files>crates/core/ui/elements/src/layout.rs</files>
  <read_first>crates/core/ui/elements/src/layout.rs, crates/core/ui/elements/src/style/types.rs, .planning/phases/47-taffy-layout-adapter-integration/47-CONTEXT.md, .planning/phases/47-taffy-layout-adapter-integration/47-RESEARCH.md</read_first>
  <action>Add a `#[derive(Debug, Clone, PartialEq)] pub struct TaffyLayoutDiagnostic { pub node_id: NodeId, pub tag: String, pub reason: String }` near `LayoutRect`. Add a `#[derive(Debug, Default)] pub struct TaffyLayoutReport { pub diagnostics: Vec<TaffyLayoutDiagnostic> }` with `pub fn is_clean(&self) -> bool { self.diagnostics.is_empty() }`. Add a private helper `fn record_taffy_diagnostic(report: &mut TaffyLayoutReport, node: &WidgetNode, reason: impl Into<String>)` that stores `node.id`, `node.tag.clone()`, and the reason. Add at least one focused unit test named `taffy_diagnostic_records_node_identity_and_reason` that constructs a `WidgetNode`, records reason `unsupported layout mapping: test-only`, and asserts the diagnostic contains the original `node.id`, original tag, and exact reason. This task must not add a path that calls the old custom layout after a Taffy failure.</action>
  <verify>env XDG_CACHE_HOME=/tmp/codex-nix-cache nix develop -c cargo test -p mesh-core-elements taffy_diagnostic</verify>
  <acceptance_criteria>
    - `crates/core/ui/elements/src/layout.rs` contains `pub struct TaffyLayoutDiagnostic`.
    - `crates/core/ui/elements/src/layout.rs` contains `pub struct TaffyLayoutReport`.
    - `crates/core/ui/elements/src/layout.rs` contains `unsupported layout mapping: test-only`.
    - `crates/core/ui/elements/src/layout.rs` does not contain a helper named `fallback_to_legacy_layout`.
    - `env XDG_CACHE_HOME=/tmp/codex-nix-cache nix develop -c cargo test -p mesh-core-elements taffy_diagnostic` exits 0.
  </acceptance_criteria>
  <done>Layout replacement has visible diagnostic primitives for unsupported Taffy mappings.</done>
</task>

<task type="auto">
  <name>Task 3: Document Phase 47 layout authority promotion</name>
  <files>docs/renderer-migration.md, docs/renderer-ownership.md</files>
  <read_first>docs/renderer-migration.md, docs/renderer-ownership.md, .planning/phases/47-taffy-layout-adapter-integration/47-CONTEXT.md, .planning/phases/46-renderer-library-dependency-and-adapter-foundation/46-CONTEXT.md</read_first>
  <action>Update `docs/renderer-migration.md` with a section titled exactly `## Phase 47 Taffy Layout Replacement Record`. State that Taffy is promoted for in-scope layout computation, that `mesh-core-elements` owns the Taffy dependency because it owns `LayoutEngine`, and that unsupported cases produce diagnostics/blockers rather than silent old-engine fallback. Update `docs/renderer-ownership.md` so the `Taffy` replacement-candidate row or nearby text states Phase 47 promotes Taffy toward authoritative layout for rows, columns, stacks, fixed sizes, gaps, padding, absolute positioning, and container-width cases while MESH `NodeId`, runtime keys, dirty categories, render-object sync, diagnostics, profiling, and presentation remain authoritative MESH boundaries.</action>
  <verify>rg -n "Phase 47 Taffy Layout Replacement Record|mesh-core-elements owns the Taffy dependency|unsupported cases produce diagnostics|Phase 47 promotes Taffy" docs/renderer-migration.md docs/renderer-ownership.md</verify>
  <acceptance_criteria>
    - `docs/renderer-migration.md` contains `## Phase 47 Taffy Layout Replacement Record`.
    - `docs/renderer-migration.md` contains `mesh-core-elements owns the Taffy dependency`.
    - `docs/renderer-migration.md` contains `unsupported cases produce diagnostics`.
    - `docs/renderer-ownership.md` contains `Phase 47 promotes Taffy`.
    - `docs/renderer-ownership.md` contains `NodeId`.
    - `docs/renderer-ownership.md` contains `render-object sync`.
  </acceptance_criteria>
  <done>Renderer migration docs explicitly reflect the Phase 47 layout replacement boundary.</done>
</task>

</tasks>

<verification>
- Run `env XDG_CACHE_HOME=/tmp/codex-nix-cache nix develop -c cargo check -p mesh-core-elements`.
- Run `env XDG_CACHE_HOME=/tmp/codex-nix-cache nix develop -c cargo test -p mesh-core-elements taffy_diagnostic`.
- Run `rg -n "Phase 47 Taffy Layout Replacement Record|unsupported cases produce diagnostics|Phase 47 promotes Taffy" docs/renderer-migration.md docs/renderer-ownership.md`.
</verification>

<success_criteria>
- Taffy is available in the layout-owning crate.
- Unsupported Taffy mappings have visible diagnostic records.
- No hidden legacy layout fallback is introduced.
- Renderer migration docs distinguish Phase 47 layout promotion from Phase 46 non-layout scaffolding.
</success_criteria>

