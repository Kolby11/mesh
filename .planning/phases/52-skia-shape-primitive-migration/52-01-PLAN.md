---
phase: 52-skia-shape-primitive-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/css-coverage.md
  - crates/core/ui/elements/src/style/types.rs
  - crates/core/ui/elements/src/style.rs
autonomous: true
requirements:
  - STYLE-01
tags:
  - style-profile
  - renderer-boundary
  - docs
must_haves:
  truths:
    - "Maintainer can read one author-facing bounded MESH shell CSS profile that explicitly rejects arbitrary browser CSS."
    - "Every supported visual property category is classified as implemented, diagnostic-only, deferred, or out-of-scope."
    - "The property support matrix is executable through `supported_css_properties()` tests, not only prose."
    - "Style profile metadata remains backend-neutral and introduces no `skia_safe` dependency above the painter backend."
  artifacts:
    - path: "docs/css-coverage.md"
      provides: "Author-facing bounded style profile and support matrix"
      contains: "implemented"
    - path: "crates/core/ui/elements/src/style/types.rs"
      provides: "Style profile metadata next to supported CSS property source of truth"
      exports:
        - "supported_css_properties"
        - "is_supported_css_property"
    - path: "crates/core/ui/elements/src/style.rs"
      provides: "Executable style profile matrix tests"
      contains: "style_profile"
  key_links:
    - from: "docs/css-coverage.md"
      to: "crates/core/ui/elements/src/style/types.rs"
      via: "documented support categories mirror style profile metadata"
      pattern: "implemented|diagnostic-only|deferred|out-of-scope"
    - from: "crates/core/ui/elements/src/style.rs"
      to: "crates/core/ui/elements/src/style/types.rs"
      via: "tests call supported_css_properties/is_supported_css_property"
      pattern: "supported_css_properties\\("
---

<objective>
Create the author-facing and executable style profile contract for Phase 52.

Purpose: STYLE-01 requires MESH to lock a bounded XML/.mesh, CSS-like, theme-token style profile without implying browser CSS compatibility.
Output: Updated style profile documentation, profile metadata, and tests proving the metadata stays synchronized with the supported property list.
</objective>

<execution_context>
@/home/kolby/.codex/get-shit-done/workflows/execute-plan.md
@/home/kolby/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/52-skia-shape-primitive-migration/52-CONTEXT.md
@.planning/phases/52-skia-shape-primitive-migration/52-RESEARCH.md
@.planning/phases/52-skia-shape-primitive-migration/52-VALIDATION.md
@.planning/phases/52-skia-shape-primitive-migration/52-PATTERNS.md
@docs/css-coverage.md
@crates/core/ui/elements/src/style/types.rs
@crates/core/ui/elements/src/style.rs

<interfaces>
From `crates/core/ui/elements/src/style/types.rs`:
```rust
pub struct StyleDiagnostic {
    pub property: String,
    pub selector: Option<String>,
    pub message: String,
}

pub fn supported_css_properties() -> &'static [&'static str];
pub fn is_supported_css_property(property: &str) -> bool;
pub fn is_transition_safe_keyframe_property(property: &str) -> bool;

pub struct ComputedStyle {
    pub background_color: Color,
    pub border_color: Color,
    pub border_width: Edges,
    pub border_radius: Corners,
    pub opacity: f32,
    pub transform: Transform2D,
    pub box_shadow: BoxShadow,
    pub filter: VisualFilter,
    pub backdrop_filter: VisualFilter,
    pub transition: TransitionStyle,
    pub animation: AnimationStyle,
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 52-01-01: Add executable style profile metadata</name>
  <files>crates/core/ui/elements/src/style/types.rs, crates/core/ui/elements/src/style.rs</files>
  <read_first>
    - .planning/phases/52-skia-shape-primitive-migration/52-CONTEXT.md
    - .planning/phases/52-skia-shape-primitive-migration/52-RESEARCH.md
    - .planning/phases/52-skia-shape-primitive-migration/52-VALIDATION.md
    - crates/core/ui/elements/src/style/types.rs
    - crates/core/ui/elements/src/style.rs
  </read_first>
  <behavior>
    - Test 1: `style_profile_matrix_classifies_supported_visual_properties` proves representative properties for color, size, spacing, border, radius, opacity, transform, shadow, filter, layout, font, animation, and transition have explicit profile classifications.
    - Test 2: `style_profile_matrix_matches_supported_css_properties` proves every `supported_css_properties()` entry has a matrix/profile row unless it is a custom property prefix handled by `property.starts_with("--")`.
    - Test 3: `style_profile_marks_browser_css_out_of_scope` proves `grid-template-columns`, `float`, `white-space`, `container-type`, and `text-wrap` are not implemented supported properties.
  </behavior>
  <action>
    Add compact style profile metadata next to `SUPPORTED_CSS_PROPERTIES` in `crates/core/ui/elements/src/style/types.rs`, using backend-neutral Rust data such as string slices and enums only. Required statuses are exactly `implemented`, `diagnostic-only`, `deferred`, and `out-of-scope`. Do not add `skia_safe`, painter command types, render-object types, or a second token resolver. Add tests in `crates/core/ui/elements/src/style.rs` with names prefixed `style_profile_` so `cargo test -p mesh-core-elements style_profile -- --nocapture` selects them. Use the Phase 52 decisions: MESH shell CSS is authoritative, arbitrary browser CSS is out-of-scope, and already compiling visual categories remain represented rather than removed.
  </action>
  <verify>
    <automated>cargo test -p mesh-core-elements style_profile -- --nocapture</automated>
    <automated>cargo test -p mesh-core-elements style -- --nocapture</automated>
    <automated>rg "skia_safe" crates/core/ui/elements/src/style/types.rs crates/core/ui/elements/src/style.rs && exit 1 || exit 0</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p mesh-core-elements style_profile -- --nocapture` passes.
    - `cargo test -p mesh-core-elements style -- --nocapture` passes.
    - `rg "style_profile_.*classifies|style_profile_.*matches|style_profile_.*browser" crates/core/ui/elements/src/style.rs` finds the new tests.
    - `rg "skia_safe" crates/core/ui/elements/src/style/types.rs crates/core/ui/elements/src/style.rs && exit 1 || exit 0` exits 0.
  </acceptance_criteria>
  <done>The supported-property source of truth has executable profile classifications and STYLE-01 coverage is testable through the required Phase 52 command.</done>
</task>

<task type="auto">
  <name>Task 52-01-02: Update author-facing CSS coverage into painter style profile</name>
  <files>docs/css-coverage.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
    - .planning/phases/52-skia-shape-primitive-migration/52-CONTEXT.md
    - .planning/phases/52-skia-shape-primitive-migration/52-PATTERNS.md
    - docs/css-coverage.md
    - crates/core/ui/elements/src/style/types.rs
  </read_first>
  <action>
    Rewrite `docs/css-coverage.md` from a simple coverage list into the Phase 52 bounded painter style profile. Keep the existing direct documentation style, but add a support matrix whose status column uses exactly `implemented`, `diagnostic-only`, `deferred`, and `out-of-scope`. Cover these categories by name: color, size, spacing, border, radius, opacity, transform, shadow, filter, image, gradient, animation, transition, layout, font, selectors, tokens, and custom properties. State explicitly that MESH supports shell CSS for XML/.mesh, not arbitrary HTML/CSS/DOM/browser compatibility. State that theme tokens resolve through `mesh-core-theme` + `StyleResolver`, while CSS custom properties are local variables and are not theme tokens. State that Phase 52 does not migrate widget/control painting, Skia primitive execution, effects/layers/images/gradients, animation invalidation, damage policy, or backend observability.
  </action>
  <verify>
    <automated>rg "implemented|diagnostic-only|deferred|out-of-scope" docs/css-coverage.md</automated>
    <automated>rg "mesh-core-theme.*StyleResolver|StyleResolver.*mesh-core-theme" docs/css-coverage.md</automated>
    <automated>cargo test -p mesh-core-elements style_profile -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `docs/css-coverage.md` contains all four status strings: `implemented`, `diagnostic-only`, `deferred`, and `out-of-scope`.
    - `docs/css-coverage.md` contains `MESH shell CSS` and does not describe MESH as full browser CSS.
    - `rg "Skia primitive execution|animation invalidation|damage policy|backend observability" docs/css-coverage.md` finds the explicit phase exclusions.
    - `cargo test -p mesh-core-elements style_profile -- --nocapture` passes after the documentation update.
  </acceptance_criteria>
  <done>The author-facing style profile visibly implements STYLE-01 and mirrors the executable matrix.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.mesh` author CSS -> style profile metadata | Untrusted author-like style declarations are classified before parser/resolver behavior promises support. |
| style profile metadata -> docs/tests | Maintainer-facing support claims become executable contract and must not overpromise rendering support. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-01 | Tampering | `SUPPORTED_CSS_PROPERTIES` / profile matrix | mitigate | Add `style_profile_*` tests proving every supported property has an explicit status and browser CSS remains out-of-scope. |
| T-52-02 | Repudiation | `docs/css-coverage.md` | mitigate | Document the exact statuses and phase exclusions so maintainers can trace why unsupported browser properties are diagnostic/deferred/out-of-scope. |
| T-52-03 | Information Disclosure | style/display-list retained data | mitigate | Verify no `skia_safe` import is introduced into style profile files. |
</threat_model>

<verification>
Run the Phase 52 task gate:

```bash
cargo test -p mesh-core-elements style_profile -- --nocapture
cargo test -p mesh-core-elements style -- --nocapture
rg "skia_safe" crates/core/ui/elements/src/style/types.rs crates/core/ui/elements/src/style.rs && exit 1 || exit 0
```
</verification>

<success_criteria>
STYLE-01 is satisfied when the bounded MESH shell CSS profile is documented, profile classifications are executable, browser CSS is explicitly excluded, and no backend-specific Skia types enter style/display-list retained data.
</success_criteria>

<output>
After completion, create `.planning/phases/52-skia-shape-primitive-migration/52-01-SUMMARY.md`.
</output>
