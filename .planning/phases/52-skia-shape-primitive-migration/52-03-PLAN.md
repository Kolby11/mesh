---
phase: 52-skia-shape-primitive-migration
plan: 03
type: execute
wave: 3
depends_on:
  - 52-01
  - 52-02
files_modified:
  - crates/core/ui/elements/src/style/types.rs
  - crates/core/ui/elements/src/style/resolve.rs
  - crates/core/ui/elements/src/style.rs
autonomous: true
requirements:
  - STYLE-03
tags:
  - style-profile
  - diagnostics
  - parser
must_haves:
  truths:
    - "Unsupported browser-like properties produce actionable `StyleDiagnostic` entries instead of silent acceptance."
    - "Accepted-yet-unlowered properties such as `transform-origin` are classified and diagnosed rather than pretending painter support exists."
    - "Ambiguous shipped web-like declarations such as `container-type`, `text-wrap`, and `border-style` are diagnostic-only or out-of-scope by profile metadata."
    - "Descendant selector behavior is documented and guarded where feasible without broad selector architecture changes."
  artifacts:
    - path: "crates/core/ui/elements/src/style/types.rs"
      provides: "Diagnostic-only/deferred property classification helpers"
      contains: "diagnostic"
    - path: "crates/core/ui/elements/src/style/resolve.rs"
      provides: "Resolver diagnostics for unsupported or ambiguous web-like properties"
      contains: "StyleDiagnostic"
    - path: "crates/core/ui/elements/src/style.rs"
      provides: "STYLE-03 diagnostic tests"
      contains: "style_diagnostics"
  key_links:
    - from: "crates/core/ui/elements/src/style/resolve.rs"
      to: "crates/core/ui/elements/src/style/types.rs"
      via: "resolver checks profile classification before lowering"
      pattern: "StyleDiagnostic"
    - from: "crates/core/ui/elements/src/style.rs"
      to: "crates/core/ui/elements/src/style/resolve.rs"
      via: "tests call resolve_node_style_with_diagnostics"
      pattern: "resolve_node_style_with_diagnostics"
---

<objective>
Add Phase 52 diagnostics for unsupported and ambiguous web-like style declarations.

Purpose: STYLE-03 requires unsupported or ambiguous web-style properties to be visible diagnostics, not silently accepted with missing visual behavior.
Output: Resolver diagnostics and tests covering web-like unsupported properties, accepted-yet-unlowered declarations, and shipped compatibility cases.
</objective>

<execution_context>
@/home/kolby/.codex/get-shit-done/workflows/execute-plan.md
@/home/kolby/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/52-skia-shape-primitive-migration/52-CONTEXT.md
@.planning/phases/52-skia-shape-primitive-migration/52-RESEARCH.md
@.planning/phases/52-skia-shape-primitive-migration/52-VALIDATION.md
@.planning/phases/52-skia-shape-primitive-migration/52-PATTERNS.md
@.planning/phases/52-skia-shape-primitive-migration/52-01-SUMMARY.md
@.planning/phases/52-skia-shape-primitive-migration/52-02-SUMMARY.md
@crates/core/ui/elements/src/style/types.rs
@crates/core/ui/elements/src/style/resolve.rs
@crates/core/ui/elements/src/style.rs
@modules/frontend/navigation-bar/src/main.mesh
@modules/frontend/navigation-bar/src/components/volume-button.mesh

<interfaces>
Diagnostic pattern from `crates/core/ui/elements/src/style/resolve.rs`:
```rust
diagnostics.push(StyleDiagnostic {
    property: decl.property.clone(),
    selector,
    message: format!("unsupported CSS property '{}'", decl.property),
});
```

Profile hooks from `crates/core/ui/elements/src/style/types.rs`:
```rust
pub fn supported_css_properties() -> &'static [&'static str];
pub fn is_supported_css_property(property: &str) -> bool;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 52-03-01: Diagnose diagnostic-only and deferred profile properties</name>
  <files>crates/core/ui/elements/src/style/types.rs, crates/core/ui/elements/src/style/resolve.rs, crates/core/ui/elements/src/style.rs</files>
  <read_first>
    - .planning/phases/52-skia-shape-primitive-migration/52-CONTEXT.md
    - .planning/phases/52-skia-shape-primitive-migration/52-RESEARCH.md
    - .planning/phases/52-skia-shape-primitive-migration/52-VALIDATION.md
    - crates/core/ui/elements/src/style/types.rs
    - crates/core/ui/elements/src/style/resolve.rs
    - crates/core/ui/elements/src/style.rs
  </read_first>
  <behavior>
    - Test 1: `style_diagnostics_transform_origin_is_accepted_but_unlowered` resolves `transform-origin: center` and emits one diagnostic mentioning `accepted by parser` or `not lowered`.
    - Test 2: `style_diagnostics_browser_layout_properties_are_unsupported` emits diagnostics for `container-type: inline-size` and `text-wrap: nowrap`.
    - Test 3: `style_diagnostics_border_style_is_diagnostic_only` emits a diagnostic for `border-style: solid` unless implementation proves it is intentionally lowered into existing border behavior.
  </behavior>
  <action>
    Extend profile metadata/helpers in `crates/core/ui/elements/src/style/types.rs` so `crates/core/ui/elements/src/style/resolve.rs` can distinguish implemented properties from diagnostic-only/deferred/out-of-scope declarations. In `apply_declaration_with_diagnostics`, before calling `apply_declaration`, emit non-fatal `StyleDiagnostic` messages for diagnostic-only or deferred properties. Required property behavior: `transform-origin` must no longer be silently accepted without lowering; `container-type` and `text-wrap` must produce unsupported/browser-profile diagnostics; `border-style` must either produce a diagnostic-only message or be explicitly lowered through existing border parsing with tests proving effect. Prefer diagnostics for `border-style` because Phase 52 does not migrate border painting. Keep custom properties beginning with `--` supported as local variables.
  </action>
  <verify>
    <automated>cargo test -p mesh-core-elements style_diagnostics -- --nocapture</automated>
    <automated>cargo test -p mesh-core-elements style -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p mesh-core-elements style_diagnostics -- --nocapture` passes.
    - `rg "transform-origin|container-type|text-wrap|border-style" crates/core/ui/elements/src/style.rs` finds assertions for all four properties.
    - Diagnostic messages include the property name and either `unsupported`, `diagnostic-only`, `deferred`, or `not lowered`.
    - `rg "skia_safe" crates/core/ui/elements/src/style/types.rs crates/core/ui/elements/src/style/resolve.rs crates/core/ui/elements/src/style.rs && exit 1 || exit 0` exits 0.
  </acceptance_criteria>
  <done>STYLE-03 diagnostics cover accepted-yet-unlowered and browser-like ambiguous style properties.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 52-03-02: Guard shipped fixture diagnostics and selector-profile ambiguity</name>
  <files>crates/core/ui/elements/src/style.rs</files>
  <read_first>
    - .planning/phases/52-skia-shape-primitive-migration/52-CONTEXT.md
    - .planning/phases/52-skia-shape-primitive-migration/52-RESEARCH.md
    - crates/core/ui/elements/src/style.rs
    - crates/core/ui/elements/src/style/resolve.rs
    - crates/core/ui/component/src/parser/styles.rs
    - modules/frontend/navigation-bar/src/main.mesh
    - modules/frontend/navigation-bar/src/components/volume-button.mesh
  </read_first>
  <behavior>
    - Test 1: shipped navigation fixture diagnostics include only expected web-like property names and do not mask token failures.
    - Test 2: descendant selector behavior is either asserted as documented out-of-scope behavior or explicitly diagnostic if the current parser exposes enough selector context without architecture changes.
  </behavior>
  <action>
    Extend the shipped style tests added in Plan 02 so expected diagnostics are exact enough to prevent future silent acceptance. Include fixture coverage for `container-type`, `text-wrap`, and `border-style` where present. For descendant selector behavior, first inspect how `parse_selector` currently lowers `.nav-button:hover .nav-button-glyph`; if the parser exposes no descendant context to `StyleResolver`, document this in a test comment and assert the support matrix/docs classify descendant selectors as out-of-scope. If a conservative diagnostic can be added without rewriting selector parsing, add a test for that diagnostic; do not introduce a new selector architecture in Phase 52.
  </action>
  <verify>
    <automated>cargo test -p mesh-core-elements style_diagnostics -- --nocapture</automated>
    <automated>cargo test -p mesh-core-elements shipped_navigation_style -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p mesh-core-elements style_diagnostics -- --nocapture` passes.
    - `cargo test -p mesh-core-elements shipped_navigation_style -- --nocapture` passes.
    - `rg "descendant|container-type|text-wrap|border-style" crates/core/ui/elements/src/style.rs docs/css-coverage.md` finds coverage of descendant selector status and shipped diagnostic properties.
    - No task changes `modules/frontend/navigation-bar/**/*.mesh` to remove existing compatibility declarations.
  </acceptance_criteria>
  <done>Shipped fixture diagnostics and selector-profile ambiguity are explicit and do not hide token/style regressions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author CSS declaration -> resolver diagnostics | Unsupported browser-like declarations enter non-fatal diagnostic handling. |
| parser-accepted property -> computed style lowering | A property may parse but not lower; this must be visible. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-07 | Tampering | `StyleResolver::apply_declaration_with_diagnostics` | mitigate | Emit diagnostics for diagnostic-only/deferred/out-of-scope properties before lowering can silently no-op. |
| T-52-08 | Repudiation | shipped fixture expected diagnostics | mitigate | Assert exact property names in fixture tests so maintainers can distinguish expected compatibility diagnostics from regressions. |
| T-52-09 | Elevation of Privilege | selector parser scope | accept | No security privilege is granted by selector matching; Phase 52 documents/diagnoses descendant selectors without adding a broader selector engine. |
</threat_model>

<verification>
Run the Phase 52 diagnostics gate:

```bash
cargo test -p mesh-core-elements style_diagnostics -- --nocapture
cargo test -p mesh-core-elements shipped_navigation_style -- --nocapture
cargo test -p mesh-core-elements style -- --nocapture
```
</verification>

<success_criteria>
STYLE-03 is satisfied when unsupported, diagnostic-only, deferred, and accepted-yet-unlowered properties produce actionable diagnostics and shipped compatibility fixtures prove those diagnostics do not break token/style resolution.
</success_criteria>

<output>
After completion, create `.planning/phases/52-skia-shape-primitive-migration/52-03-SUMMARY.md`.
</output>
