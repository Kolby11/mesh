---
phase: 52-skia-shape-primitive-migration
plan: 02
type: execute
wave: 2
depends_on:
  - 52-01
files_modified:
  - crates/core/ui/elements/src/style.rs
autonomous: true
requirements:
  - STYLE-02
tags:
  - style-profile
  - tokens
  - fixtures
must_haves:
  truths:
    - "Existing theme token references resolve through `mesh-core-theme` and `StyleResolver`."
    - "CSS custom properties that already work as local variables continue to resolve without becoming theme tokens."
    - "Shipped navigation and audio style fixtures compile or resolve with expected diagnostics and no token regressions."
    - "Painter-relevant values lower into backend-neutral `ComputedStyle` data."
  artifacts:
    - path: "crates/core/ui/elements/src/style.rs"
      provides: "Token/custom-property and shipped fixture style regression tests"
      contains: "shipped_navigation_style"
  key_links:
    - from: "crates/core/ui/elements/src/style.rs"
      to: "crates/core/ui/elements/src/style/resolve.rs"
      via: "tests call StyleResolver::resolve_node_style_with_diagnostics"
      pattern: "resolve_node_style_with_diagnostics"
    - from: "crates/core/ui/elements/src/style.rs"
      to: "modules/frontend/navigation-bar/src/main.mesh"
      via: "fixture test reads shipped navigation styles"
      pattern: "navigation-bar/src/main\\.mesh"
---

<objective>
Prove token compatibility and shipped style fixture compatibility for the Phase 52 profile.

Purpose: STYLE-02 requires existing token references and shipped module styles to keep resolving through the current token/style pipeline while painter-relevant style values remain backend-neutral.
Output: Focused Rust tests for theme tokens, local custom properties, and shipped navigation/audio style fixtures.
</objective>

<execution_context>
@/home/kolby/.codex/get-shit-done/workflows/execute-plan.md
@/home/kolby/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/52-skia-shape-primitive-migration/52-CONTEXT.md
@.planning/phases/52-skia-shape-primitive-migration/52-RESEARCH.md
@.planning/phases/52-skia-shape-primitive-migration/52-VALIDATION.md
@.planning/phases/52-skia-shape-primitive-migration/52-PATTERNS.md
@.planning/phases/52-skia-shape-primitive-migration/52-01-SUMMARY.md
@crates/core/ui/elements/src/style.rs
@crates/core/ui/elements/src/style/resolve.rs
@crates/core/ui/elements/src/style/types.rs
@crates/core/ui/component/src/parser/styles.rs
@modules/frontend/navigation-bar/src/main.mesh
@modules/frontend/navigation-bar/src/components/volume-button.mesh
@modules/frontend/audio-popover/src/main.mesh

<interfaces>
From `crates/core/ui/elements/src/style/resolve.rs`:
```rust
impl<'a> StyleResolver<'a> {
    pub fn new(theme: &'a Theme) -> Self;
    pub fn resolve_node_style_with_diagnostics(
        &self,
        rules: &[StyleRule],
        tag: &str,
        classes: &[String],
        id: Option<&str>,
        context: StyleContext,
        state: ElementState,
    ) -> (ComputedStyle, Vec<StyleDiagnostic>);
}
```

From `crates/core/ui/component/src/parser/styles.rs`:
```rust
pub fn parse_style(css: &str) -> Result<StyleBlock, ParseError>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 52-02-01: Add token and custom-property compatibility tests</name>
  <files>crates/core/ui/elements/src/style.rs</files>
  <read_first>
    - .planning/phases/52-skia-shape-primitive-migration/52-CONTEXT.md
    - .planning/phases/52-skia-shape-primitive-migration/52-RESEARCH.md
    - .planning/phases/52-skia-shape-primitive-migration/52-VALIDATION.md
    - crates/core/ui/elements/src/style.rs
    - crates/core/ui/elements/src/style/resolve.rs
    - crates/core/ui/elements/src/style/types.rs
  </read_first>
  <behavior>
    - Test 1: `shipped_navigation_style_token_resolution_uses_theme_pipeline` resolves `token(color.*)`, `token(spacing.*)`, `token(radius.*)`, and animation token references through `StyleResolver`.
    - Test 2: `shipped_navigation_style_custom_properties_remain_local_variables` proves `--surface: token(...)` followed by `background: var(--surface)` resolves to the same backend-neutral `ComputedStyle.background_color`.
    - Test 3: unresolved animation token references still emit an actionable `StyleDiagnostic` containing the missing token name.
  </behavior>
  <action>
    Extend the existing `#[cfg(test)] mod tests` in `crates/core/ui/elements/src/style.rs`. Use real `mesh_core_theme::default_theme()` and `StyleResolver::new(&theme)`; do not introduce a mock token resolver. Build small `StyleRule` fixtures inline using `Declaration`, `Selector`, and `StyleValue`, then assert concrete `ComputedStyle` fields such as `background_color`, `border_radius`, `padding`, `transition`, and `animation` where supported. Preserve local CSS custom property behavior by storing `--name` declarations in the same rule before consuming `var(--name)`.
  </action>
  <verify>
    <automated>cargo test -p mesh-core-elements shipped_navigation_style -- --nocapture</automated>
    <automated>cargo test -p mesh-core-elements style -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p mesh-core-elements shipped_navigation_style -- --nocapture` passes.
    - `rg "shipped_navigation_style_.*token|shipped_navigation_style_.*custom" crates/core/ui/elements/src/style.rs` finds token and custom-property tests.
    - `rg "StyleResolver::new\\(&theme\\)" crates/core/ui/elements/src/style.rs` confirms tests use the current resolver path.
    - `rg "skia_safe" crates/core/ui/elements/src/style.rs && exit 1 || exit 0` exits 0.
  </acceptance_criteria>
  <done>Theme tokens, animation token failures, and local custom properties have STYLE-02 regression coverage through the current resolver path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 52-02-02: Add shipped navigation/audio fixture style gates</name>
  <files>crates/core/ui/elements/src/style.rs</files>
  <read_first>
    - .planning/phases/52-skia-shape-primitive-migration/52-CONTEXT.md
    - .planning/phases/52-skia-shape-primitive-migration/52-RESEARCH.md
    - .planning/phases/52-skia-shape-primitive-migration/52-VALIDATION.md
    - crates/core/ui/elements/src/style.rs
    - crates/core/ui/component/src/parser/styles.rs
    - modules/frontend/navigation-bar/src/main.mesh
    - modules/frontend/navigation-bar/src/components/volume-button.mesh
    - modules/frontend/audio-popover/src/main.mesh
  </read_first>
  <behavior>
    - Test 1: `shipped_navigation_style_fixtures_parse_without_syntax_regression` parses style blocks from navigation main and volume-button fixtures.
    - Test 2: `shipped_navigation_style_expected_diagnostics_do_not_block_tokens` asserts known web-like declarations produce expected diagnostics while critical token-backed fields still resolve.
    - Test 3: `shipped_audio_style_fixture_resolves_painter_relevant_values` parses the audio popover style fixture and resolves representative background, spacing, border/radius, opacity, transition, or font values without Skia-specific data.
  </behavior>
  <action>
    Add fixture tests in `crates/core/ui/elements/src/style.rs` that read shipped `.mesh` files with `include_str!` or a crate-stable relative path. Extract `<style>` content using existing parser helpers if exposed, or add a small test-only helper in the test module. Parse style blocks through the existing component parser path, then resolve selected rules with `StyleResolver::resolve_node_style_with_diagnostics`. Assert exact expected diagnostic property names for currently shipped web-like properties instead of requiring zero diagnostics. Required properties to account for are `container-type`, `text-wrap`, and `border-style`; if a fixture does not contain one of these in the current checkout, assert only the properties present in that fixture.
  </action>
  <verify>
    <automated>cargo test -p mesh-core-elements shipped_navigation_style -- --nocapture</automated>
    <automated>cargo test -p mesh-core-elements style -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p mesh-core-elements shipped_navigation_style -- --nocapture` passes.
    - `rg "modules/frontend/navigation-bar/src/main\\.mesh|modules/frontend/navigation-bar/src/components/volume-button\\.mesh|modules/frontend/audio-popover/src/main\\.mesh" crates/core/ui/elements/src/style.rs` finds all shipped fixture references.
    - `rg "container-type|text-wrap|border-style" crates/core/ui/elements/src/style.rs` finds expected diagnostic assertions or fixture-aware expected-property handling.
    - Tests assert at least one non-default `ComputedStyle` visual field after resolving fixture rules.
  </acceptance_criteria>
  <done>Shipped navigation/audio styles are compatibility fixtures for STYLE-02, with expected diagnostics separated from token/style regressions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| theme token strings -> `ComputedStyle` | Theme-controlled token values cross into resolved style data. |
| shipped `.mesh` styles -> parser/resolver | Existing module style text must remain compatible while diagnostics become more explicit. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-04 | Tampering | token/custom-property resolution | mitigate | Use real `mesh-core-theme` + `StyleResolver` tests so painter work cannot introduce a parallel token path. |
| T-52-05 | Repudiation | shipped fixture diagnostics | mitigate | Assert exact expected diagnostic property names while proving critical token-backed fields still resolve. |
| T-52-06 | Information Disclosure | retained style data | mitigate | Keep tests and fixtures in `ComputedStyle`/plain MESH data only; grep for `skia_safe` in touched style tests. |
</threat_model>

<verification>
Run the Phase 52 task gate:

```bash
cargo test -p mesh-core-elements shipped_navigation_style -- --nocapture
cargo test -p mesh-core-elements style -- --nocapture
```
</verification>

<success_criteria>
STYLE-02 is satisfied when token references, local custom properties, and shipped navigation/audio style fixtures resolve through the existing theme/resolver pipeline and no backend-specific type leaks into style data.
</success_criteria>

<output>
After completion, create `.planning/phases/52-skia-shape-primitive-migration/52-02-SUMMARY.md`.
</output>
