---
phase: 52-skia-shape-primitive-migration
plan: 04
type: execute
wave: 4
depends_on:
  - 52-01
  - 52-02
  - 52-03
files_modified:
  - crates/core/ui/component/src/parser.rs
  - crates/core/ui/component/src/style.rs
  - .planning/phases/52-skia-shape-primitive-migration/52-VALIDATION.md
autonomous: true
requirements:
  - STYLE-01
  - STYLE-02
  - STYLE-03
tags:
  - style-profile
  - parser
  - validation
must_haves:
  truths:
    - "Component parser keyframe expectations match the Phase 52 style profile."
    - "The targeted elements style/profile suite and component parser suite pass together."
    - "Validation metadata is updated to mark the planned Nyquist Wave 0 gaps as covered."
    - "Final proof keeps Skia types out of style/display-list retained data."
  artifacts:
    - path: "crates/core/ui/component/src/parser.rs"
      provides: "Parser tests aligned to Phase 52 keyframe profile"
      contains: "filter"
    - path: "crates/core/ui/component/src/style.rs"
      provides: "Transition-safe keyframe property source for parser expectations"
      contains: "is_transition_safe_keyframe_property"
    - path: ".planning/phases/52-skia-shape-primitive-migration/52-VALIDATION.md"
      provides: "Updated validation status after plan execution"
      contains: "nyquist_compliant: true"
  key_links:
    - from: "crates/core/ui/component/src/parser.rs"
      to: "crates/core/ui/component/src/style.rs"
      via: "parser tests use transition-safe keyframe policy"
      pattern: "filter|backdrop-filter|box-shadow"
    - from: ".planning/phases/52-skia-shape-primitive-migration/52-VALIDATION.md"
      to: "cargo test -p mesh-core-elements style -- --nocapture && cargo test -p mesh-core-component parser -- --nocapture"
      via: "final phase validation command"
      pattern: "nyquist_compliant: true"
---

<objective>
Align component parser expectations with the Phase 52 style profile and run the final shipped style gate.

Purpose: Plans 01-03 make the style profile executable in `mesh-core-elements`; this final plan ensures the component parser test expectations and validation metadata agree with that profile.
Output: Updated parser/keyframe tests where needed, updated validation status, and final automated proof commands.
</objective>

<execution_context>
@/home/kolby/.codex/get-shit-done/workflows/execute-plan.md
@/home/kolby/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/52-skia-shape-primitive-migration/52-CONTEXT.md
@.planning/phases/52-skia-shape-primitive-migration/52-RESEARCH.md
@.planning/phases/52-skia-shape-primitive-migration/52-VALIDATION.md
@.planning/phases/52-skia-shape-primitive-migration/52-PATTERNS.md
@.planning/phases/52-skia-shape-primitive-migration/52-01-SUMMARY.md
@.planning/phases/52-skia-shape-primitive-migration/52-02-SUMMARY.md
@.planning/phases/52-skia-shape-primitive-migration/52-03-SUMMARY.md
@crates/core/ui/component/src/parser.rs
@crates/core/ui/component/src/parser/styles.rs
@crates/core/ui/component/src/style.rs
@crates/core/ui/elements/src/style.rs
@crates/core/frontend/render/src/display_list.rs
@crates/core/frontend/render/src/render_object.rs

<interfaces>
From `crates/core/ui/component/src/style.rs`:
```rust
pub fn is_transition_safe_keyframe_property(property: &str) -> bool;
```

Final validation commands from `52-VALIDATION.md`:
```bash
cargo test -p mesh-core-elements style -- --nocapture
cargo test -p mesh-core-component parser -- --nocapture
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 52-04-01: Align component parser keyframe expectations</name>
  <files>crates/core/ui/component/src/parser.rs, crates/core/ui/component/src/style.rs</files>
  <read_first>
    - .planning/phases/52-skia-shape-primitive-migration/52-CONTEXT.md
    - .planning/phases/52-skia-shape-primitive-migration/52-RESEARCH.md
    - .planning/phases/52-skia-shape-primitive-migration/52-VALIDATION.md
    - crates/core/ui/component/src/parser.rs
    - crates/core/ui/component/src/parser/styles.rs
    - crates/core/ui/component/src/style.rs
  </read_first>
  <behavior>
    - Test 1: component parser tests accept keyframe metadata for `filter`, `backdrop-filter`, and `box-shadow` if `is_transition_safe_keyframe_property` classifies them as accepted by the Phase 52 matrix.
    - Test 2: unsupported keyframe properties outside the bounded profile still fail parser validation.
  </behavior>
  <action>
    Update stale component parser test expectations around `filter`, `backdrop-filter`, and `box-shadow` keyframes so they match the Phase 52 matrix and `is_transition_safe_keyframe_property`. Preserve parser rejection for unsupported keyframe declarations outside the bounded profile. Do not broaden keyframes to arbitrary browser animation properties. Change production parser code only if tests reveal the helper and parser path disagree; otherwise update tests only.
  </action>
  <verify>
    <automated>cargo test -p mesh-core-component parser -- --nocapture</automated>
    <automated>cargo test -p mesh-core-elements style -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p mesh-core-component parser -- --nocapture` passes.
    - `rg "filter|backdrop-filter|box-shadow" crates/core/ui/component/src/parser.rs crates/core/ui/component/src/style.rs` shows keyframe expectations and transition-safe policy are aligned.
    - Tests still include at least one unsupported keyframe property rejection outside the bounded profile.
  </acceptance_criteria>
  <done>Component parser tests no longer contradict the Phase 52 accepted metadata/deferred render profile for filter/shadow keyframes.</done>
</task>

<task type="auto">
  <name>Task 52-04-02: Run final style/profile gate and update validation status</name>
  <files>.planning/phases/52-skia-shape-primitive-migration/52-VALIDATION.md</files>
  <read_first>
    - .planning/phases/52-skia-shape-primitive-migration/52-VALIDATION.md
    - .planning/phases/52-skia-shape-primitive-migration/52-CONTEXT.md
    - .planning/phases/52-skia-shape-primitive-migration/52-RESEARCH.md
    - crates/core/ui/elements/src/style/types.rs
    - crates/core/ui/elements/src/style/resolve.rs
    - crates/core/ui/elements/src/style.rs
    - crates/core/ui/component/src/parser.rs
    - crates/core/frontend/render/src/display_list.rs
    - crates/core/frontend/render/src/render_object.rs
  </read_first>
  <action>
    Run the full Phase 52 validation command exactly as specified in `52-VALIDATION.md`: `cargo test -p mesh-core-elements style -- --nocapture && cargo test -p mesh-core-component parser -- --nocapture`. Also run the backend-neutrality grep gate against style/display-list retained data. If both pass, update `52-VALIDATION.md` frontmatter `nyquist_compliant` to `true`, set `wave_0_complete` to `true`, and mark the four Per-Task Verification Map rows as green using the existing table format. Do not mark green without running the commands in this task.
  </action>
  <verify>
    <automated>cargo test -p mesh-core-elements style -- --nocapture && cargo test -p mesh-core-component parser -- --nocapture</automated>
    <automated>rg "skia_safe" crates/core/ui/elements/src/style/types.rs crates/core/ui/elements/src/style/resolve.rs crates/core/ui/elements/src/style.rs crates/core/frontend/render/src/display_list.rs crates/core/frontend/render/src/render_object.rs && exit 1 || exit 0</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p mesh-core-elements style -- --nocapture && cargo test -p mesh-core-component parser -- --nocapture` passes.
    - `rg "skia_safe" crates/core/ui/elements/src/style/types.rs crates/core/ui/elements/src/style/resolve.rs crates/core/ui/elements/src/style.rs crates/core/frontend/render/src/display_list.rs crates/core/frontend/render/src/render_object.rs && exit 1 || exit 0` exits 0.
    - `.planning/phases/52-skia-shape-primitive-migration/52-VALIDATION.md` contains `nyquist_compliant: true` and `wave_0_complete: true`.
    - `.planning/phases/52-skia-shape-primitive-migration/52-VALIDATION.md` no longer marks Phase 52 Wave 0 file-exists gaps as `❌ W0`.
  </acceptance_criteria>
  <done>Final Phase 52 style/profile/parser proof is green and validation metadata records Nyquist compliance.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.mesh` keyframes -> component parser | Author animation metadata is parsed before style resolution and must remain bounded. |
| final validation metadata -> executor/checker | Plan completion evidence is recorded in validation artifacts after commands pass. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-10 | Tampering | component parser keyframe tests | mitigate | Align tests to `is_transition_safe_keyframe_property` while preserving rejection for unsupported properties. |
| T-52-11 | Repudiation | `52-VALIDATION.md` | mitigate | Update validation status only after running the exact Phase 52 full suite command and backend-neutrality grep gate. |
| T-52-12 | Information Disclosure | retained style/display-list data | mitigate | Run grep gate proving `skia_safe` is absent from style and retained display-list/render-object files. |
</threat_model>

<verification>
Run the final Phase 52 gate:

```bash
cargo test -p mesh-core-elements style -- --nocapture && cargo test -p mesh-core-component parser -- --nocapture
rg "skia_safe" crates/core/ui/elements/src/style/types.rs crates/core/ui/elements/src/style/resolve.rs crates/core/ui/elements/src/style.rs crates/core/frontend/render/src/display_list.rs crates/core/frontend/render/src/render_object.rs && exit 1 || exit 0
```
</verification>

<success_criteria>
Phase 52 is ready for verification when component parser expectations match the style profile, all STYLE-01/STYLE-02/STYLE-03 gates pass, validation metadata is updated, and style/display-list retained data remains backend-neutral.
</success_criteria>

<output>
After completion, create `.planning/phases/52-skia-shape-primitive-migration/52-04-SUMMARY.md`.
</output>
