---
phase: 53-element-and-display-list-primitive-coverage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/core/frontend/render/src/surface/painter/tests.rs
autonomous: true
requirements:
  - ELEM-01
  - ELEM-02
tags:
  - painter
  - command-classes
  - test-foundation
must_haves:
  truths:
    - "Painter primitive proof uses backend-neutral PainterCommand classes, not pixel-only assertions."
    - "Direct widget-tree and retained replay fixtures can compare command classes without moving NodeId or damage ownership into PaintBackend."
  artifacts:
    - path: "crates/core/frontend/render/src/surface/painter/tests.rs"
      provides: "Command-class helpers and first direct/retained parity fixture"
      contains: "painter_primitive"
---

<objective>
Create the Phase 53 command-class proof foundation.
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 53-01-01: Add painter command-class helpers and baseline parity fixture</name>
  <files>crates/core/frontend/render/src/surface/painter/tests.rs</files>
  <read_first>
    - .planning/phases/53-element-and-display-list-primitive-coverage/53-CONTEXT.md
    - .planning/phases/53-element-and-display-list-primitive-coverage/53-RESEARCH.md
    - crates/core/frontend/render/src/surface/painter/tests.rs
    - crates/core/frontend/render/src/surface/painter/backend.rs
    - crates/core/frontend/render/src/display_list.rs
  </read_first>
  <behavior>
    - Add a helper named `painter_command_classes` that maps `PainterCommand` values to stable strings: `draw_rect`, `draw_rounded_rect`, `draw_shadow`, `apply_filter`, `draw_text`, `draw_image`, `push_clip`, `pop_clip`, `push_layer`, `pop_layer`, `draw_path`.
    - Add a test named `painter_primitive_command_classes_record_helper_backed_rects`.
    - Add a test named `display_list_primitive_direct_and_retained_box_emit_same_command_classes`.
  </behavior>
  <action>
    Extend `crates/core/frontend/render/src/surface/painter/tests.rs`. Reuse `RecordingPaintBackend`, `FrontendRenderEngine::with_paint_backend`, `RetainedDisplayList`, and existing `node(...)` helper. The direct/retained fixture should build a single `box` node with non-transparent background and border, render the direct tree through a recording backend, build/update a retained display list for the same node, replay it through another recording backend, and assert both class vectors include the same rect/rounded-rect class sequence for the node primitive.
  </action>
  <acceptance_criteria>
    - `rg "fn painter_command_classes" crates/core/frontend/render/src/surface/painter/tests.rs` finds the helper.
    - `rg "painter_primitive_command_classes_record_helper_backed_rects" crates/core/frontend/render/src/surface/painter/tests.rs` finds the test.
    - `rg "display_list_primitive_direct_and_retained_box_emit_same_command_classes" crates/core/frontend/render/src/surface/painter/tests.rs` finds the test.
    - `cargo test -p mesh-core-render painter_primitive -- --nocapture` passes.
    - `cargo test -p mesh-core-render display_list_primitive -- --nocapture` passes.
  </acceptance_criteria>
  <done>Phase 53 has command-class helpers and a baseline direct/retained primitive parity fixture.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Mitigation Plan |
|---|---|---|---|
| T-53-01 | Tampering | command-class proof | Use real `PainterCommand` values from `RecordingPaintBackend`, not string-only fakes. |
| T-53-02 | Repudiation | direct/retained parity | Add named tests that compare direct and retained class streams. |
</threat_model>

<verification>
```bash
cargo test -p mesh-core-render painter_primitive -- --nocapture
cargo test -p mesh-core-render display_list_primitive -- --nocapture
```
</verification>

<success_criteria>
Plan 01 is complete when command-class helpers exist and a baseline direct/retained box fixture proves class parity.
</success_criteria>
