---
phase: 61-localized-resolution-and-override-safety
plan: 01
title: Deterministic Surface Keybind Resolution
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/core/shell/src/shell/component/input/keyboard.rs
  - crates/core/shell/src/shell/component/tests/interaction/navigation.rs
autonomous: true
requirements: [KRES-01, KRES-02, KRES-03, KRES-04]
requirements_addressed: [KRES-01, KRES-02, KRES-03, KRES-04]
user_setup: []
must_haves:
  truths:
    - "D-01: Effective binding precedence is user override, exact active locale, parent locale, generic manifest trigger, then no binding."
    - "D-02: Parent locale fallback happens only after exact locale lookup misses or yields no usable key."
    - "D-03: Blank localized trigger keys are not usable and fall through to the next candidate."
    - "D-04: User overrides remain keyed by surface id and stable action id through keyboard.surface_shortcuts."
    - "D-05: User overrides replace only effective trigger key data for an existing action; they do not replace action id, target, labels, category, or handler path."
    - "D-06: Override source metadata remains KeybindResolutionSource::UserOverride and event.keybind.source = user_override."
    - "D-07: Localized defaults apply to access_key actions; shortcut actions keep generic shortcut defaults unless user override exists."
    - "D-08: Existing localized shortcut tests must be updated if they conflict with KRES-03."
    - "D-09: Localized access-key trigger kind and modifiers remain reflected in resolved metadata."
    - "D-10: Canonical manifest declarations are primary; legacy settings declarations are fallback only when manifest lacks the action id."
    - "D-11: Legacy fallback actions remain plain shortcut declarations without manifest metadata."
    - "D-12: Preserve Phase 60 dispatch behavior: no empty subscriber consumption, text input stealing, or precedence regressions."
  artifacts:
    - path: "crates/core/shell/src/shell/component/input/keyboard.rs"
      provides: "Effective surface shortcut resolver and declaration precedence"
      contains: "resolve_surface_shortcut_declaration"
    - path: "crates/core/shell/src/shell/component/tests/interaction/navigation.rs"
      provides: "Focused keybind locale, override, legacy fallback, and dispatch regression tests"
      contains: "keybind_locale"
---

<objective>
Complete Phase 61 by making focused-surface keybind resolution deterministic across user overrides, locale defaults, generic manifest triggers, and legacy fallback declarations while preserving Phase 60 dispatch behavior.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/61-localized-resolution-and-override-safety/61-CONTEXT.md
@.planning/phases/61-localized-resolution-and-override-safety/61-RESEARCH.md
@.planning/phases/61-localized-resolution-and-override-safety/61-VALIDATION.md
@.planning/phases/61-localized-resolution-and-override-safety/61-PATTERNS.md
@.planning/phases/60-surface-keybind-dispatch-runtime/60-01-SUMMARY.md
@crates/core/shell/src/shell/component/input/keyboard.rs
@crates/core/shell/src/shell/component/tests/interaction/navigation.rs
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| T-61-01: user override creates an action that the manifest/settings never declared | high | Add a regression test proving an unknown override action id does not produce a resolved shortcut. |
| T-61-02: localized shortcut defaults steal generic shortcut behavior | high | Enforce localized defaults only for access-key actions and update the existing localized shortcut test. |
| T-61-03: resolver changes regress Phase 60 dispatch/input precedence | high | Run the full navigation interaction suite after resolver edits. |
| T-61-04: legacy settings become canonical declarations again | medium | Preserve manifest-over-legacy test and keep fallback only for missing action ids. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Lock resolver precedence with focused tests</name>
  <files>crates/core/shell/src/shell/component/tests/interaction/navigation.rs</files>
  <read_first>crates/core/shell/src/shell/component/tests/interaction/navigation.rs, crates/core/shell/src/shell/component/input/keyboard.rs, .planning/phases/61-localized-resolution-and-override-safety/61-CONTEXT.md</read_first>
  <action>Update `crates/core/shell/src/shell/component/tests/interaction/navigation.rs` so resolver tests explicitly cover KRES-01 through KRES-04. Keep or strengthen existing `keybind_locale_exact_locale_wins_over_parent_and_generic`, `keybind_locale_parent_locale_wins_over_generic`, `keybind_locale_user_override_wins_over_locale_and_generic`, and `keybind_locale_blank_localized_trigger_falls_back_to_generic`. Add a new test named `keybind_override_cannot_create_missing_manifest_declaration` that constructs `KeyboardSettings { surface_shortcuts: HashMap::from([("@test/reactive-surface".into(), HashMap::from([("missing".into(), SurfaceShortcutOverride { key: Some("x".into()) })]))]) }` on a component with no manifest action id `missing`, calls `resolved_surface_shortcuts`, and asserts no resolved shortcut has `keybind_id == "missing"`. Replace or update the existing localized shortcut behavior test so a `KeybindTriggerKind::Shortcut` action with generic key `a` and localized trigger key `p` still resolves to key `a` with `KeybindResolutionSource::ModuleDefault` unless a user override exists.</action>
  <verify>nix develop -c cargo test -p mesh-core-shell keybind_locale</verify>
  <acceptance_criteria>
    - `crates/core/shell/src/shell/component/tests/interaction/navigation.rs` contains `keybind_override_cannot_create_missing_manifest_declaration`.
    - `crates/core/shell/src/shell/component/tests/interaction/navigation.rs` contains `keybind_locale_shortcut_keeps_generic_trigger_without_user_override`.
    - `crates/core/shell/src/shell/component/tests/interaction/navigation.rs` contains `KeybindResolutionSource::ModuleDefault` in the localized shortcut generic fallback test.
    - `nix develop -c cargo test -p mesh-core-shell keybind_locale` exits 0.
  </acceptance_criteria>
  <done>Resolver tests prove deterministic precedence, override safety, access-key-localized scope, and legacy compatibility boundaries.</done>
</task>

<task type="auto">
  <name>Task 2: Enforce access-key-localized resolution semantics</name>
  <files>crates/core/shell/src/shell/component/input/keyboard.rs</files>
  <read_first>crates/core/shell/src/shell/component/input/keyboard.rs, crates/core/shell/src/shell/component/tests/interaction/navigation.rs, .planning/phases/61-localized-resolution-and-override-safety/61-PATTERNS.md</read_first>
  <action>Update `resolve_surface_shortcut_declaration` in `crates/core/shell/src/shell/component/input/keyboard.rs` to preserve D-01 through D-12. Keep the override branch first and keep it sourced from an existing declaration so overrides cannot create actions. After the override branch, only iterate `keybind_locale_candidates(active_locale)` when `declaration.generic_trigger.kind == mesh_core_module::KeybindTriggerKind::AccessKey`; shortcut actions must skip localized defaults and fall through to the generic trigger unless there is a user override. Keep blank localized trigger keys unusable by preserving the `if key.trim().is_empty() { return None; }` behavior inside localized trigger extraction. Keep the final generic fallback returning `KeybindResolutionSource::ModuleDefault`. Do not change `dispatch_surface_shortcut`, `keybind_subscribers`, or the Phase 60 text input guard.</action>
  <verify>nix develop -c cargo test -p mesh-core-shell keybind_locale</verify>
  <acceptance_criteria>
    - `crates/core/shell/src/shell/component/input/keyboard.rs` contains `KeybindTriggerKind::AccessKey` near the localized trigger candidate loop or a helper used by that loop.
    - `crates/core/shell/src/shell/component/input/keyboard.rs` still contains `KeybindResolutionSource::UserOverride`.
    - `crates/core/shell/src/shell/component/input/keyboard.rs` still contains `KeybindResolutionSource::ModuleDefault`.
    - `nix develop -c cargo test -p mesh-core-shell keybind_locale` exits 0.
  </acceptance_criteria>
  <done>Localized defaults are restricted to access-key actions while user overrides and generic shortcut defaults remain deterministic.</done>
</task>

<task type="auto">
  <name>Task 3: Verify legacy fallback and Phase 60 dispatch regressions</name>
  <files>crates/core/shell/src/shell/component/tests/interaction/navigation.rs</files>
  <read_first>crates/core/shell/src/shell/component/tests/interaction/navigation.rs, crates/core/shell/src/shell/component/input/keyboard.rs, .planning/phases/60-surface-keybind-dispatch-runtime/60-01-SUMMARY.md</read_first>
  <action>Run the focused regression set and make only test corrections needed to keep KRES-04 and D-12 intact. Preserve `keyboard_shortcuts_manifest_declaration_wins_over_legacy_settings_same_id`, `keyboard_shortcuts_surface_handler_runs_and_metadata_matches_binding`, `keyboard_shortcuts_manifest_keybind_dispatches_only_to_runtime_subscribers`, and `keyboard_shortcuts_bare_printable_does_not_steal_focused_text_input`. If any assertions fail because test names or expected source strings changed in Tasks 1-2, update the assertions to the new Phase 61 contract without deleting coverage.</action>
  <verify>nix develop -c cargo test -p mesh-core-shell shell::component::tests::interaction::navigation</verify>
  <acceptance_criteria>
    - `crates/core/shell/src/shell/component/tests/interaction/navigation.rs` still contains `keyboard_shortcuts_manifest_declaration_wins_over_legacy_settings_same_id`.
    - `crates/core/shell/src/shell/component/tests/interaction/navigation.rs` still contains `keyboard_shortcuts_bare_printable_does_not_steal_focused_text_input`.
    - `nix develop -c cargo test -p mesh-core-shell keyboard_shortcuts_manifest_keybind_subscriber_resolves_user_override_by_id` exits 0.
    - `nix develop -c cargo test -p mesh-core-shell keyboard_shortcuts_manifest_declaration_wins_over_legacy_settings_same_id` exits 0.
    - `nix develop -c cargo test -p mesh-core-shell shell::component::tests::interaction::navigation` exits 0.
  </acceptance_criteria>
  <done>Legacy fallback, override metadata, and Phase 60 dispatch/input regressions remain covered after resolution changes.</done>
</task>

</tasks>

<verification>
- Run `nix develop -c cargo test -p mesh-core-shell keybind_locale`.
- Run `nix develop -c cargo test -p mesh-core-shell keyboard_shortcuts_manifest_keybind_subscriber_resolves_user_override_by_id`.
- Run `nix develop -c cargo test -p mesh-core-shell keyboard_shortcuts_manifest_declaration_wins_over_legacy_settings_same_id`.
- Run `nix develop -c cargo test -p mesh-core-shell shell::component::tests::interaction::navigation`.
- Run `rg -n "keybind_override_cannot_create_missing_manifest_declaration|keybind_locale_shortcut_keeps_generic_trigger_without_user_override|KeybindTriggerKind::AccessKey|KeybindResolutionSource::UserOverride|keyboard_shortcuts_bare_printable_does_not_steal_focused_text_input" crates/core/shell/src/shell/component`.
</verification>

<success_criteria>
- KRES-01: Effective bindings resolve user override, exact locale, parent locale, generic manifest trigger, then no binding.
- KRES-02: User overrides are keyed by surface id and stable action id and cannot create declarations.
- KRES-03: Localized defaults apply only to access-key actions; shortcut actions keep generic defaults unless user override exists.
- KRES-04: Legacy settings shortcuts remain compatibility fallback only when canonical manifest actions do not declare the action.
- Phase 60 dispatch, no-subscriber, text input, and legacy fallback tests remain passing.
</success_criteria>
