---
phase: 05-icon-rendering-reliability
plan: 03
type: tdd
wave: 1
depends_on:
  - 05-01
files_modified:
  - crates/core/foundation/diagnostics/src/lib.rs
  - crates/core/extension/plugin/src/manifest.rs
  - crates/core/shell/src/shell/component.rs
  - crates/core/shell/src/shell/mod.rs
autonomous: true
requirements: [ICON-04]
requirements_addressed: [ICON-04]
user_setup: []
must_haves:
  truths:
    - "D-07: frontend plugins or components declare semantic icons and icon packs they depend on."
    - "D-08: plugin loading does not fail only because an icon is missing."
    - "D-09: unresolved icons render fallback and emit diagnostics while keeping layout stable."
    - "D-10: missing-icon diagnostics dedupe by plugin plus semantic icon name and are not emitted on every paint frame."
    - "D-12: unresolved icons mark plugin health as warning/degraded while keeping the plugin active."
    - "D-20: config-level profile switching is enough; this plan does not add a visible UI switch."
  artifacts:
    - path: "crates/core/foundation/diagnostics/src/lib.rs"
      provides: "Missing icon diagnostic dedupe and degraded health"
      contains: "record_missing_icon"
    - path: "crates/core/extension/plugin/src/manifest.rs"
      provides: "Semantic icon requirement manifest schema"
      contains: "IconRequirementsSection"
    - path: "crates/core/shell/src/shell/component.rs"
      provides: "Plugin-context missing icon validation/integration"
      contains: "missing_icon"
  key_links:
    - from: "crates/core/extension/plugin/src/manifest.rs"
      to: "crates/core/foundation/diagnostics/src/lib.rs"
      via: "declared semantic icon requirements are checked without failing plugin load"
      pattern: "icon_requirements"
---

<objective>
Add non-fatal missing-icon diagnostics and semantic icon declarations so plugin health reflects degraded icon availability without blocking load.

Purpose: Phase 5 requires missing icons to be visible, deduplicated, and non-crashing.
Output: Diagnostics, manifest parsing, and shell/component integration support one warning per `(plugin_id, semantic_name)` while leaving the plugin active.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-icon-rendering-reliability/05-CONTEXT.md
@.planning/phases/05-icon-rendering-reliability/05-RESEARCH.md
@.planning/phases/05-icon-rendering-reliability/05-UI-SPEC.md
@.planning/phases/05-icon-rendering-reliability/05-VALIDATION.md
@.planning/phases/05-icon-rendering-reliability/05-PATTERNS.md
@.planning/phases/05-icon-rendering-reliability/05-01-PLAN.md

@crates/core/foundation/diagnostics/src/lib.rs
@crates/core/extension/plugin/src/manifest.rs
@crates/core/shell/src/shell/component.rs
@crates/core/shell/src/shell/mod.rs
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| T-05-03-01: Missing icon logs every paint frame | high | Store missing icons in diagnostics state keyed by `(plugin_id, semantic_name)` and return false after the first insert. |
| T-05-03-02: Missing required icon unloads a surface | high | Treat missing icons as degraded health, not load errors; tests assert plugin/surface remains active. |
| T-05-03-03: Manifest schema conflates asset paths with semantic requirements | medium | Add `icon_requirements` or equivalent dedicated section; leave `assets.icons` as asset path metadata. |
| T-05-03-04: Diagnostics lack plugin identity | high | Record missing icon through component/shell paths that have manifest package ID or `Diagnostics::plugin_id()`. |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add diagnostics dedupe tests for missing icons</name>
  <files>crates/core/foundation/diagnostics/src/lib.rs</files>
  <read_first>crates/core/foundation/diagnostics/src/lib.rs, .planning/phases/05-icon-rendering-reliability/05-CONTEXT.md</read_first>
  <action>Add a test named `missing_icon_diagnostics_are_deduplicated_by_plugin_and_semantic_name`. The test must create `Diagnostics::new("@mesh/quick-settings")`, call `record_missing_icon("audio-volume-muted", vec!["material:nope".into()])` twice, then call `record_missing_icon("network-wireless", vec!["material:nope".into()])`. Assert the first and third calls return true, the second returns false, `error_count() == 0`, and `health()` matches `HealthStatus::Degraded(_)` with a message containing the semantic icon name. If the public helper stores tried candidates, assert the message or snapshot preserves at least one tried candidate string.</action>
  <verify>nix develop -c cargo test -p mesh-core-diagnostics missing_icon_diagnostics_are_deduplicated_by_plugin_and_semantic_name</verify>
  <acceptance_criteria>
    - The named diagnostics test exists.
    - Duplicate missing icon calls for the same plugin and semantic icon return false.
    - Different semantic icon names still record once.
    - Missing icon diagnostics set degraded health, not error health.
  </acceptance_criteria>
  <done>Missing icon diagnostic semantics are pinned before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement record_missing_icon in diagnostics</name>
  <files>crates/core/foundation/diagnostics/src/lib.rs</files>
  <read_first>crates/core/foundation/diagnostics/src/lib.rs, .planning/phases/05-icon-rendering-reliability/05-PATTERNS.md</read_first>
  <action>Add a `missing_icons: HashSet<(String, String)>` or equivalent to `DiagnosticsState`. Implement `Diagnostics::record_missing_icon(&self, semantic_name: impl Into<String>, tried: Vec<String>) -> bool`. Use `self.plugin_id.clone()` plus `semantic_name` as the dedupe key. On first insert, set `HealthStatus::Degraded(format!("missing icon '{semantic_name}' for plugin '{plugin_id}'"))` or a similarly explicit degraded message and do not increment `error_count`. On duplicate, leave health/error count unchanged and return false. Keep existing handler error behavior unchanged.</action>
  <verify>nix develop -c cargo test -p mesh-core-diagnostics</verify>
  <acceptance_criteria>
    - `DiagnosticsState` contains missing icon dedupe state.
    - `record_handler_error` tests still pass.
    - `record_missing_icon` does not increment `error_count`.
    - `nix develop -c cargo test -p mesh-core-diagnostics` exits 0.
  </acceptance_criteria>
  <done>Diagnostics can represent non-fatal icon degradation once per semantic icon.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add manifest support for semantic icon requirements</name>
  <files>crates/core/extension/plugin/src/manifest.rs</files>
  <read_first>crates/core/extension/plugin/src/manifest.rs, packages/plugins/frontend/core/panel/plugin.json, packages/plugins/frontend/core/quick-settings/plugin.json, packages/plugins/frontend/core/navigation-bar/plugin.json</read_first>
  <action>Add a dedicated normalized manifest section named `icon_requirements` with `required: Vec<String>` and `optional: Vec<String>` or an equivalent explicitly named schema. Parse it from both JSON manifests (`"icon_requirements": { "required": ["audio-volume-muted"], "optional": [] }`) and TOML manifests if TOML support can be added without broad churn. Add a test named `parses_plugin_json_icon_requirements` asserting JSON parsing preserves required semantic icon names and does not modify `assets.icons`. Do not overload `dependencies.icon_packs`; keep icon pack IDs there and semantic icon names in `icon_requirements`.</action>
  <verify>nix develop -c cargo test -p mesh-core-plugin parses_plugin_json_icon_requirements</verify>
  <acceptance_criteria>
    - `Manifest` contains `icon_requirements` or an equivalently named dedicated semantic section.
    - JSON parsing preserves `icon_requirements.required`.
    - `AssetsSection.icons` remains an asset path field and is not used for semantic requirements.
    - `nix develop -c cargo test -p mesh-core-plugin parses_plugin_json_icon_requirements` exits 0.
  </acceptance_criteria>
  <done>Plugins can declare semantic icons separately from icon pack dependencies and asset paths.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Wire missing icon validation without failing plugin load</name>
  <files>crates/core/shell/src/shell/component.rs, crates/core/shell/src/shell/mod.rs, crates/core/foundation/diagnostics/src/lib.rs, crates/core/extension/plugin/src/manifest.rs</files>
  <read_first>crates/core/shell/src/shell/component.rs, crates/core/shell/src/shell/mod.rs, crates/core/foundation/diagnostics/src/lib.rs, crates/core/extension/plugin/src/manifest.rs, .planning/phases/05-icon-rendering-reliability/05-RESEARCH.md</read_first>
  <action>Add or update shell/component integration so missing declared semantic icons can call `Diagnostics::record_missing_icon(...)` with the manifest package ID and semantic icon name, while the plugin remains loaded/active. Prefer a preflight or render-adjacent helper that has both the plugin diagnostics handle and the `IconResolution::Missing { tried, .. }` from Plan 01. Add a focused test named `missing_required_icon_degrades_plugin_without_unloading` or equivalent. The test must define a frontend surface manifest with `icon_requirements.required = ["missing-proof"]`, use an icon config/registry that cannot resolve `missing-proof`, assert the component/plugin is still active or present in the frontend catalog, assert diagnostics health is degraded, and assert duplicate validation/render calls do not increment error count or duplicate the missing icon record.</action>
  <verify>nix develop -c cargo test -p mesh-core-shell missing_required_icon_degrades_plugin_without_unloading</verify>
  <acceptance_criteria>
    - Missing required semantic icons mark plugin health degraded.
    - Missing required semantic icons do not return a plugin load error.
    - Duplicate missing icon validation/render calls dedupe through diagnostics.
    - If `mesh-core-shell` tests are blocked by host display dependencies, static checks show `record_missing_icon` is called from shell/component code and the summary records the host blocker.
  </acceptance_criteria>
  <done>Shell/plugin integration exposes missing icons as non-fatal degraded diagnostics.</done>
</task>

</tasks>

<verification>
- Run `nix develop -c cargo test -p mesh-core-diagnostics`.
- Run `nix develop -c cargo test -p mesh-core-plugin parses_plugin_json_icon_requirements`.
- Run `nix develop -c cargo test -p mesh-core-shell missing_required_icon_degrades_plugin_without_unloading`.
- Run `grep -R "record_missing_icon\\|icon_requirements\\|IconRequirementsSection\\|missing_required_icon" -n crates/core/foundation/diagnostics crates/core/extension/plugin crates/core/shell`.
</verification>

<success_criteria>
- ICON-04 has diagnostics and degraded-health behavior for missing icons.
- Missing icons are deduplicated by plugin plus semantic icon name.
- Plugin loading remains non-fatal when an icon is missing.
- Semantic icon requirements are separate from icon pack dependency IDs and asset paths.
</success_criteria>

<output>
After completion, create `.planning/phases/05-icon-rendering-reliability/05-03-SUMMARY.md`
</output>
