---
phase: 54-skia-shape-path-text-highlight-and-border-migration
plan: 03
type: execute
wave: 3
depends_on:
  - 54-01
files_modified:
  - crates/core/frontend/render/src/surface/painter/backend.rs
  - crates/core/frontend/render/src/surface/painter/tests.rs
requirements:
  - SKIA-01
---

# Plan 54-03: Path Execution

## Objective

Implement Skia `DrawPath` execution for fill and stroke painter commands.

## Tasks

### Task 54-03-01: Convert painter paths to Skia paths

- Add tests named `skia_path_fill_triangle_paints_expected_pixels` and `skia_path_stroke_line_paints_expected_pixels`.
- Convert `PainterPathElement` values into a private Skia path builder inside `backend.rs`.
- Execute fill and stroke styles through Skia canvas with the command clip applied.
- Update Skia capabilities so `paths: true` only after tests pass.

## Verification

```bash
cargo test -p mesh-core-render skia_path -- --nocapture
```
