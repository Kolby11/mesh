---
phase: 54-skia-shape-path-text-highlight-and-border-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/core/frontend/render/src/surface/painter/backend.rs
  - crates/core/frontend/render/src/surface/painter/tests.rs
requirements:
  - SKIA-01
---

# Plan 54-01: Skia Rect And Clip Shape Execution

## Objective

Make rectangular fills use Skia-backed shape execution and add focused pixel proof for command clips.

## Tasks

### Task 54-01-01: Route rect fills through Skia canvas

- Add tests named `skia_shape_rect_fill_uses_command_clip` and `skia_shape_rect_fill_respects_transparency`.
- Update `SkiaPaintBackend::fill_rect_impl` to draw through `with_skia_canvas` instead of `PixelBuffer::clear_rect`.
- Preserve clipping behavior and existing transparent/no-area behavior.

## Verification

```bash
cargo test -p mesh-core-render skia_shape -- --nocapture
```
