---
phase: 38-canonical-manifest-normalization
plan: 02
title: Canonical Module Loader And Migration Paths
type: execute
wave: 1
depends_on: [38-01]
files_modified:
  - crates/core/extension/module/src/package/installed_graph.rs
  - crates/core/extension/module/src/package/module_manifest.rs
  - crates/core/extension/module/src/package/root.rs
  - crates/core/extension/module/src/package/error.rs
  - crates/core/extension/module/src/manifest/load.rs
  - crates/core/extension/module/src/manifest/model.rs
  - crates/core/extension/module/src/package/tests.rs
  - crates/core/extension/module/src/manifest/tests.rs
autonomous: true
requirements: [MAN-01, MAN-02, MAN-03]
requirements_addressed: [MAN-01, MAN-02, MAN-03]
user_setup: []
must_haves:
  truths:
    - "D-03: old module.json with id/type/api_version is a legacy runtime manifest shape, not the Phase 38 target."
    - "D-05: loader precedence prefers canonical module.json."
    - "D-06: multiple manifest files in one module directory are a blocking ambiguity diagnostic."
    - "D-07: plugin.json is not accepted as a public or migration manifest name."
    - "D-08: mesh.toml remains only an internal legacy-normalization path when needed."
    - "D-17: runtime normalization preserves implements, legacy provides migration, localizedTriggers/localized keybind data, settings.keyboard.shortcuts migration input, and MeshModuleSection validation."
    - "D-11: migration preserves active provider mapping, enabled module graph, layout entrypoint, interfaces, providers, capabilities, dependencies, settings, resources, and v1.6 keybinds."
    - "D-13: invalid, duplicate, or ambiguous manifest data should be blocking; deprecated migration inputs should be warnings."
  artifacts:
    - path: "crates/core/extension/module/src/package/installed_graph.rs"
      provides: "Canonical/legacy module manifest source detection"
      contains: "CanonicalModuleJson"
    - path: "crates/core/extension/module/src/manifest/load.rs"
      provides: "Runtime loader order and legacy migration diagnostics"
      contains: "module.json"
---

<objective>
Make `module.json` the canonical module manifest loader while keeping old manifest forms as explicit internal migration inputs with deterministic diagnostics and no silent data loss.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-canonical-manifest-normalization/38-CONTEXT.md
@.planning/phases/38-canonical-manifest-normalization/38-RESEARCH.md
@.planning/phases/38-canonical-manifest-normalization/38-VALIDATION.md
@.planning/phases/38-canonical-manifest-normalization/38-PATTERNS.md
@crates/core/extension/module/src/package/installed_graph.rs
@crates/core/extension/module/src/package/module_manifest.rs
@crates/core/extension/module/src/manifest/load.rs
@crates/core/extension/module/src/package/tests.rs
@crates/core/extension/module/src/manifest/tests.rs
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| T-38-02-01: loader silently prefers one manifest when old and new files coexist | high | Check for duplicate manifest files before parsing and return an ambiguity error. |
| T-38-02-02: old module.json gets mistaken for canonical module.json | high | Detect canonical shape by `name` plus `mesh`; detect legacy shape by old `id`, `type`, or `api_version` keys. |
| T-38-02-03: migration loaders drop provider or keybind data | high | Add tests for legacy `provides`, canonical `implements`, and localized keybind triggers. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Change module manifest source detection to canonical module.json first</name>
  <files>crates/core/extension/module/src/package/installed_graph.rs, crates/core/extension/module/src/package/tests.rs</files>
  <read_first>crates/core/extension/module/src/package/installed_graph.rs, crates/core/extension/module/src/package/module_manifest.rs, crates/core/extension/module/src/manifest/load.rs, crates/core/extension/module/src/package/tests.rs, .planning/phases/38-canonical-manifest-normalization/38-CONTEXT.md</read_first>
  <action>Update `load_module_manifest(module_dir)` so it checks for `module.json`, `package.json`, `mesh.toml`, and `plugin.json` before parsing. If `plugin.json` exists, return `ModuleManifestError::Validation` or a dedicated error containing suggested action `remove plugin.json or replace it with module.json`. If more than one of `module.json`, `package.json`, and `mesh.toml` exists, return a blocking ambiguity diagnostic/error containing `ambiguous module manifest` and `remove the old manifest file`. If only `module.json` exists and the JSON has top-level `name` plus `mesh`, parse it as `ModuleManifest` and set source `CanonicalModuleJson`. If only `module.json` exists and it has old keys `id`, `type`, or `api_version`, parse through the legacy JSON path, convert with `ModuleManifest::from_legacy_manifest`, and attach a warning with suggested action `replace legacy module.json fields with name/version/mesh`. If only `package.json` exists, parse it as a legacy package-shaped module manifest and attach a warning with suggested action `replace package.json with module.json`.</action>
  <verify>cargo test -p mesh-core-module package::tests</verify>
  <acceptance_criteria>
    - `crates/core/extension/module/src/package/installed_graph.rs` contains `CanonicalModuleJson`.
    - `crates/core/extension/module/src/package/installed_graph.rs` contains `LegacyPackageJson`.
    - `crates/core/extension/module/src/package/installed_graph.rs` contains `LegacyModuleJson`.
    - `crates/core/extension/module/src/package/installed_graph.rs` contains `plugin.json`.
    - `crates/core/extension/module/src/package/tests.rs` contains `module_manifest_loader_rejects_ambiguous_module_and_package_json`.
    - `crates/core/extension/module/src/package/tests.rs` contains `module_manifest_loader_accepts_canonical_module_json`.
    - `crates/core/extension/module/src/package/tests.rs` contains `replace package.json with module.json`.
    - `cargo test -p mesh-core-module package::tests` exits 0.
  </acceptance_criteria>
  <done>Installed graph module loading treats canonical module.json as the target and old forms as explicit migration paths.</done>
</task>

<task type="auto">
  <name>Task 2: Align runtime manifest loader with canonical and legacy paths</name>
  <files>crates/core/extension/module/src/manifest/load.rs, crates/core/extension/module/src/manifest/model.rs, crates/core/extension/module/src/manifest/tests.rs</files>
  <read_first>crates/core/extension/module/src/manifest/load.rs, crates/core/extension/module/src/manifest/model.rs, crates/core/extension/module/src/manifest/json.rs, crates/core/extension/module/src/package/module_manifest.rs, crates/core/extension/module/src/manifest/tests.rs</read_first>
  <action>Update `manifest::load_manifest` so it uses canonical `module.json` first when the file contains `name` plus `mesh`, legacy `module.json` only when old keys are present, legacy `package.json` only as migration input, and `mesh.toml` last. Rename `ManifestSource::PackageJson` to `LegacyPackageJson`, keep a legacy source for `MeshToml`, add `CanonicalModuleJson`, and keep `LegacyModuleJson`. Update `Display` for sources so canonical source displays `module.json` and legacy package source displays `package.json (legacy migration)`. Add tests that canonical module JSON round-trips keybinds and `implements`, legacy module JSON preserves `localizedTriggers`, and package JSON migration source is not described as an alias.</action>
  <verify>cargo test -p mesh-core-module manifest::tests</verify>
  <acceptance_criteria>
    - `crates/core/extension/module/src/manifest/model.rs` contains `CanonicalModuleJson`.
    - `crates/core/extension/module/src/manifest/model.rs` contains `LegacyPackageJson`.
    - `crates/core/extension/module/src/manifest/model.rs` contains `LegacyModuleJson`.
    - `crates/core/extension/module/src/manifest/load.rs` parses canonical `module.json` with `ModuleManifest`.
    - `crates/core/extension/module/src/manifest/tests.rs` contains `canonical_module_json_keybinds_round_trip_to_runtime_manifest`.
    - `crates/core/extension/module/src/manifest/tests.rs` contains `localizedTriggers`.
    - `rg -n "alias|synonym" crates/core/extension/module/src/manifest/load.rs crates/core/extension/module/src/manifest/tests.rs` exits 1.
    - `cargo test -p mesh-core-module manifest::tests` exits 0.
  </acceptance_criteria>
  <done>Runtime manifest loading distinguishes canonical module JSON from legacy migration sources.</done>
</task>

</tasks>

<verification>
- Run `cargo test -p mesh-core-module package::tests`.
- Run `cargo test -p mesh-core-module manifest::tests`.
- Confirm ambiguity and plugin rejection tests exist and pass.
</verification>

<success_criteria>
- Canonical `module.json` is parsed as the target schema.
- Legacy `package.json`, old `module.json`, and `mesh.toml` load only through explicit migration paths.
- Duplicate manifest files and `plugin.json` produce blocking diagnostics.
- Provider and keybind data survive normalization.
</success_criteria>
