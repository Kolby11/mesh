---
phase: 38-canonical-manifest-normalization
plan: 03
title: Root Graph And Shipped Manifest Migration
type: execute
wave: 2
depends_on: [38-01, 38-02]
files_modified:
  - crates/core/extension/module/src/package/root.rs
  - crates/core/extension/module/src/package/paths.rs
  - crates/core/extension/module/src/package/installed_graph.rs
  - crates/core/extension/module/src/package/tests.rs
  - crates/core/shell/src/shell/discovery.rs
  - crates/core/shell/src/shell/backend/spawn.rs
  - crates/core/shell/src/shell/tests.rs
  - crates/core/shell/src/shell/component/tests/restyle/selection.rs
  - config/package.json
  - config/module.json
  - modules/backend/pipewire-audio/package.json
  - modules/backend/pipewire-audio/module.json
  - modules/backend/pulseaudio-audio/module.json
  - modules/frontend/navigation-bar/module.json
autonomous: true
requirements: [MAN-01, MAN-02]
requirements_addressed: [MAN-01, MAN-02]
user_setup: []
must_haves:
  truths:
    - "D-09: root installed-module graph moves from config/package.json toward config/module.json."
    - "D-10: checked-in artifacts that runtime loads are migrated to canonical paths/shapes where practical."
    - "D-11: migration preserves active provider mapping, enabled module graph, layout entrypoint, interfaces, providers, capabilities, dependencies, settings, resources, and v1.6 keybinds."
    - "D-05: package.json module manifests are internal-only migration loaders that tell authors to replace them with module.json."
    - "D-14: operating-system package names in dependency suggestions are not vocabulary aliases and should not be renamed."
  artifacts:
    - path: "config/module.json"
      provides: "Canonical root installed-module graph"
      contains: "schemaVersion"
    - path: "modules/backend/pipewire-audio/module.json"
      provides: "Canonical backend provider manifest"
      contains: "implements"
---

<objective>
Move the checked-in root graph and representative shipped module manifests to canonical `module.json` files, update shell integration to use the canonical path, and prove active providers, layout entrypoints, dependencies, and keybind data still load.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-canonical-manifest-normalization/38-CONTEXT.md
@.planning/phases/38-canonical-manifest-normalization/38-RESEARCH.md
@.planning/phases/38-canonical-manifest-normalization/38-VALIDATION.md
@.planning/phases/38-canonical-manifest-normalization/38-PATTERNS.md
@config/package.json
@modules/frontend/navigation-bar/module.json
@modules/backend/pipewire-audio/package.json
@modules/backend/pulseaudio-audio/module.json
@crates/core/shell/src/shell/discovery.rs
@crates/core/shell/src/shell/backend/spawn.rs
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| T-38-03-01: root graph migration loses active provider selection | high | Add fixture test loading `config/module.json` and asserting active provider `@mesh/pipewire-audio`. |
| T-38-03-02: shipped manifest migration drops v1.6 keybind/settings data | high | Convert navigation-bar old fields into canonical `mesh` fields and assert keybind/settings data survives. |
| T-38-03-03: dependency package-manager names are accidentally renamed | medium | Preserve binary dependency `packages` values such as `wireplumber`, `alsa-utils`, and `pulseaudio-utils`. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Move root graph default and shell integration to config/module.json</name>
  <files>crates/core/extension/module/src/package/root.rs, crates/core/extension/module/src/package/paths.rs, crates/core/extension/module/src/package/installed_graph.rs, crates/core/extension/module/src/package/tests.rs, crates/core/shell/src/shell/discovery.rs, crates/core/shell/src/shell/backend/spawn.rs, crates/core/shell/src/shell/tests.rs, crates/core/shell/src/shell/component/tests/restyle/selection.rs, config/module.json</files>
  <read_first>crates/core/extension/module/src/package/root.rs, crates/core/extension/module/src/package/paths.rs, crates/core/extension/module/src/package/installed_graph.rs, crates/core/shell/src/shell/discovery.rs, crates/core/shell/src/shell/backend/spawn.rs, config/package.json, .planning/phases/38-canonical-manifest-normalization/38-CONTEXT.md</read_first>
  <action>Create `config/module.json` by preserving the current root graph data from `config/package.json`: `schemaVersion: 1`, `modulesDir: "../modules"`, the five module entries for `@mesh/navigation-bar`, `@mesh/audio-popover`, `@mesh/text-selection-proof`, `@mesh/pipewire-audio`, and `@mesh/pulseaudio-audio`, provider mapping `"mesh.audio": "@mesh/pipewire-audio"`, and layout entrypoint `"@mesh/navigation-bar:main"`. Remove `config/package.json` only after tests use `config/module.json`. Rename `root_package_manifest_path()` to `root_module_graph_manifest_path()` and make it return `.mesh/module.json`. Update shell discovery, backend spawn, shell tests, and restyle tests to join `config/module.json`. Keep a legacy helper only if it is private and named as migration, not an alias.</action>
  <verify>cargo test -p mesh-core-module package::tests installed_module_graph_loads_repo_module_fixture</verify>
  <acceptance_criteria>
    - `config/module.json` exists.
    - `test ! -f config/package.json` exits 0 after migration is complete.
    - `crates/core/extension/module/src/package/paths.rs` contains `root_module_graph_manifest_path`.
    - `rg -n "config/package.json|root_package_manifest_path" crates/core/extension/module/src crates/core/shell/src` exits 1.
    - `crates/core/extension/module/src/package/tests.rs` contains `installed_module_graph_loads_repo_module_fixture`.
    - `cargo test -p mesh-core-module package::tests installed_module_graph_loads_repo_module_fixture` exits 0.
  </acceptance_criteria>
  <done>The root installed-module graph and shell integration use canonical module.json paths.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate shipped module manifests to canonical module.json shape</name>
  <files>modules/backend/pipewire-audio/module.json, modules/backend/pulseaudio-audio/module.json, modules/frontend/navigation-bar/module.json, crates/core/extension/module/src/package/tests.rs, crates/core/extension/module/src/manifest/tests.rs</files>
  <read_first>modules/backend/pipewire-audio/package.json, modules/backend/pulseaudio-audio/module.json, modules/frontend/navigation-bar/module.json, crates/core/extension/module/src/package/module_manifest.rs, crates/core/extension/module/src/manifest/json.rs, crates/core/extension/module/src/package/tests.rs</read_first>
  <action>Convert shipped manifests touched by the root graph to canonical schema. For `modules/backend/pipewire-audio`, rename `package.json` to `module.json` and preserve `name`, `version`, `description`, `mesh.apiVersion`, `mesh.kind`, capabilities, i18n, binary dependencies with OS package names unchanged, entrypoint `src/main.luau`, and `mesh.implements` for `mesh.audio`. For `modules/backend/pulseaudio-audio/module.json`, replace old top-level `id/type/api_version/provides` with canonical `name/version/mesh`, preserve binary dependency package names, and convert `provides` to `implements` with `provider: "pulseaudio"`, `label: "PulseAudio"`, `baseModule: "@mesh/audio-interface"`, and priority 50. For `modules/frontend/navigation-bar/module.json`, replace old top-level `id/type/api_version` with canonical `name/version/mesh`, preserve capabilities, dependencies, icon requirements through the supported canonical fields, entrypoint `src/main.mesh`, keybind `mute`, settings schema, accessibility data where currently supported, and layout contribution `main` pointing to `src/main.mesh` so the root graph layout entrypoint resolves.</action>
  <verify>cargo test -p mesh-core-module package::tests</verify>
  <acceptance_criteria>
    - `modules/backend/pipewire-audio/module.json` exists.
    - `test ! -f modules/backend/pipewire-audio/package.json` exits 0 after migration is complete.
    - `modules/backend/pulseaudio-audio/module.json` contains `"implements"`.
    - `modules/backend/pulseaudio-audio/module.json` contains `"provider": "pulseaudio"`.
    - `modules/frontend/navigation-bar/module.json` contains `"name": "@mesh/navigation-bar"`.
    - `modules/frontend/navigation-bar/module.json` contains `"keybinds"`.
    - `modules/frontend/navigation-bar/module.json` contains `"layout"`.
    - `cargo test -p mesh-core-module package::tests` exits 0.
  </acceptance_criteria>
  <done>Checked-in runtime module fixtures use canonical module.json shape and preserve provider/keybind data.</done>
</task>

</tasks>

<verification>
- Run `cargo test -p mesh-core-module package::tests`.
- Run `cargo test -p mesh-core-shell shell::tests`.
- Run `rg -n "config/package.json|package.json" crates/core/shell/src modules/backend/pipewire-audio config` and verify remaining hits are intentional legacy migration tests or documentation comments only.
</verification>

<success_criteria>
- `config/module.json` is the checked-in root graph.
- Shell startup and tests load the root graph from `config/module.json`.
- Shipped backend/frontend manifests touched by Phase 38 use canonical `module.json`.
- Active provider, layout entrypoint, dependencies, capabilities, and keybind data survive fixture loading.
</success_criteria>
