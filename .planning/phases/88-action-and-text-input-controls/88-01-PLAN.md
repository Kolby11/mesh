---
phase: 88-action-and-text-input-controls
plan: 01
title: Single Button And Input Contract Metadata
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/core/ui/elements/src/element.rs
autonomous: true
requirements: [ELEMACTION-01, ELEMACTION-02, ELEMTEXT-01, ELEMTEXT-02, ELEMTEXT-03]
requirements_addressed: [ELEMACTION-01, ELEMACTION-02, ELEMTEXT-01, ELEMTEXT-02, ELEMTEXT-03]
user_setup: []
must_haves:
  truths:
    - "D-88-01: Only button gets native action behavior in Phase 88."
    - "D-88-02: Icons inside buttons are child <icon> markup, not button attributes."
    - "D-88-03: Input variants configure one runtime input path."
---

<objective>
Extend element metadata and diagnostics for the single-button model and configured input variants.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Add action/input attributes and state hooks</name>
  <files>crates/core/ui/elements/src/element.rs</files>
  <read_first>crates/core/ui/elements/src/element.rs, .planning/phases/88-action-and-text-input-controls/88-CONTEXT.md</read_first>
  <action>Add metadata for button states (`pressed`, `busy`, `default`, `destructive`, `keybind`, command/link intent) and input attributes (`placeholder`, `type`, `multiline`, `masked`, `min`, `max`, `step`). Do not add `icon`, `name`, or `src` as button attributes. Add tests named with `phase88` asserting the single-button contract.</action>
  <verify>nix develop -c cargo test -p mesh-core-elements phase88</verify>
</task>

<task type="auto">
  <name>Task 2: Add validation diagnostics</name>
  <files>crates/core/ui/elements/src/element.rs</files>
  <read_first>crates/core/ui/elements/src/element.rs</read_first>
  <action>Add diagnostics for button icon shortcut attributes, unsupported browser form/navigation behavior, invalid numeric min/max/step/value values, and unsupported textarea multiline editing markers if needed. Keep compatibility tags accepted as taxonomy entries.</action>
  <verify>nix develop -c cargo test -p mesh-core-elements phase88</verify>
</task>

</tasks>
