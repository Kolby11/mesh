---
phase: 56-animation-and-transition-paint-integration
plan: 03
type: execute
wave: 3
depends_on:
  - 56-01
  - 56-02
autonomous: true
files_modified:
  - crates/core/shell/src/shell/component/animation.rs
  - crates/core/shell/src/shell/component/tests/interaction/animation.rs
  - crates/core/ui/component/src/style.rs
  - crates/core/ui/component/src/parser/styles.rs
  - .planning/phases/56-animation-and-transition-paint-integration/56-VALIDATION.md
requirements:
  - ANIM-01
  - ANIM-02
requirements_addressed: [ANIM-01, ANIM-02]
tags:
  - keyframes
  - diagnostics
  - repaint
must_haves:
  truths:
    - "D-08: Keyframe animations currently flow through AnimatableStyle and are conservative relayout; Phase 56 narrows only where tests prove keyframe stops touch paint-only properties."
    - "D-09: Transition/keyframe lifecycle behavior remains deterministic for stable _mesh_key nodes."
    - "D-05: Unsupported or non-interpolable animation properties are visible through diagnostics."
  artifacts:
    - path: "crates/core/shell/src/shell/component/animation.rs"
      provides: "Paint-only keyframe classification"
      contains: "keyframe_animation_paint_only_rule_uses_visual_repaint"
---

<objective>
Narrow keyframe animation dirty routing when keyframe stops are provably paint-only, while preserving diagnostics and conservative relayout for unknown/layout-affecting stops.
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 56-03-01: Classify keyframe rules before choosing repaint vs relayout</name>
  <files>crates/core/shell/src/shell/component/animation.rs crates/core/shell/src/shell/component/tests/interaction/animation.rs</files>
  <read_first>
    - .planning/phases/56-animation-and-transition-paint-integration/56-CONTEXT.md
    - .planning/phases/56-animation-and-transition-paint-integration/56-RESEARCH.md
    - crates/core/shell/src/shell/component/animation.rs
    - crates/core/shell/src/shell/component/tests/interaction/animation.rs
    - crates/core/ui/elements/src/style/types.rs
  </read_first>
  <behavior>
    - Paint-only keyframe rules use visual repaint when all stops are paint-only/layer-effect fields.
    - Layout-affecting keyframe rules still use relayout.
    - Unknown or unclassified keyframe rules still use relayout.
  </behavior>
  <action>
    Add a helper named `keyframe_rule_animation_bucket(rule: &RenderKeyframeRule) -> AnimationPropertyBucket` in `animation.rs`. The helper should compare each adjacent `AnimatableStyle` stop and classify the changed fields using the same bucket semantics as transitions. In `apply_node_keyframe_animation`, return or record the active keyframe bucket for the caller; in `apply_style_animations_with_previous`, treat active paint-only/layer-effect keyframes like visual repaint and active layout-affecting/unknown keyframes like relayout. Add tests named `keyframe_animation_paint_only_rule_uses_visual_repaint`, `keyframe_animation_layout_rule_uses_relayout`, and `keyframe_animation_unknown_rule_stays_conservative`.
  </action>
  <verify>
    nix develop -c cargo test -p mesh-core-shell keyframe_animation_paint_only -- --nocapture
    nix develop -c cargo test -p mesh-core-shell keyframe_animation_layout -- --nocapture
  </verify>
  <acceptance_criteria>
    - `rg "keyframe_rule_animation_bucket" crates/core/shell/src/shell/component/animation.rs` finds the helper.
    - `rg "keyframe_animation_paint_only_rule_uses_visual_repaint" crates/core/shell/src/shell/component/tests/interaction/animation.rs` finds the test.
    - `rg "keyframe_animation_layout_rule_uses_relayout" crates/core/shell/src/shell/component/tests/interaction/animation.rs` finds the test.
    - `rg "keyframe_animation_unknown_rule_stays_conservative" crates/core/shell/src/shell/component/tests/interaction/animation.rs` finds the test.
    - `nix develop -c cargo test -p mesh-core-shell keyframe_animation -- --nocapture` exits 0.
  </acceptance_criteria>
  <done>Paint-only keyframes can repaint without relayout, while layout/unknown keyframes remain conservative.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 56-03-02: Preserve animation diagnostics and token compatibility</name>
  <files>crates/core/ui/component/src/style.rs crates/core/ui/component/src/parser/styles.rs crates/core/shell/src/shell/component/tests/interaction/animation.rs .planning/phases/56-animation-and-transition-paint-integration/56-VALIDATION.md</files>
  <read_first>
    - .planning/phases/56-animation-and-transition-paint-integration/56-CONTEXT.md
    - crates/core/ui/component/src/style.rs
    - crates/core/ui/component/src/parser/styles.rs
    - crates/core/ui/elements/src/style.rs
    - crates/core/shell/src/shell/component/tests/interaction/animation.rs
    - .planning/phases/56-animation-and-transition-paint-integration/56-VALIDATION.md
  </read_first>
  <behavior>
    - Existing animation token diagnostics still reach component diagnostics.
    - Unsupported keyframe properties continue to fail or diagnose explicitly.
    - Current shipped navigation keyframes continue across rebuild.
  </behavior>
  <action>
    Extend existing tests rather than rewriting parser behavior. Ensure `is_transition_safe_keyframe_property` and parser tests cover `opacity`, `transform`, `background-color`, `border-color`, `border-radius`, `box-shadow`, `filter`, and `backdrop-filter` as accepted keyframe properties. Ensure unsupported properties such as `display`, `position`, and `grid-template-columns` remain rejected with a message containing `unsupported keyframe property`. Add or update shell tests so `animation_token_runtime_diagnostic_reaches_component`, `keyframe_animation_missing_name_records_diagnostic`, and `navigation_bar_keyframe_animation_continues_across_rebuild` still pass after dirty-routing changes. In `56-VALIDATION.md`, mark rows `56-03-01` and `56-03-02` green only after focused tests pass.
  </action>
  <verify>
    nix develop -c cargo test -p mesh-core-component keyframe -- --nocapture
    nix develop -c cargo test -p mesh-core-shell animation_token -- --nocapture
    nix develop -c cargo test -p mesh-core-shell navigation_bar_keyframe_animation_continues_across_rebuild -- --nocapture
  </verify>
  <acceptance_criteria>
    - `rg "unsupported keyframe property" crates/core/ui/component/src/parser/styles.rs crates/core/ui/component/src/style.rs` finds the diagnostic string or assertion.
    - `rg "animation_token_runtime_diagnostic_reaches_component" crates/core/shell/src/shell/component/tests/interaction/animation.rs` finds the test.
    - `rg "navigation_bar_keyframe_animation_continues_across_rebuild" crates/core/shell/src/shell/component/tests/interaction/animation.rs` finds the test.
    - `nix develop -c cargo test -p mesh-core-component keyframe -- --nocapture` exits 0.
    - `nix develop -c cargo test -p mesh-core-shell animation_token -- --nocapture` exits 0.
    - `rg "56-03-01.*✅ green|56-03-02.*✅ green" .planning/phases/56-animation-and-transition-paint-integration/56-VALIDATION.md` finds both validation rows marked green.
  </acceptance_criteria>
  <done>Keyframe routing changes preserve current token, parser, and shipped-navigation compatibility.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Mitigation Plan |
|---|---|---|---|
| T-56-05 | Denial of service | keyframe animation loop | Repaint-only keyframes avoid layout only when every stop is classified paint-only; unknown/layout stops remain conservative. |
| T-56-06 | Repudiation | animation diagnostics | Keep missing animation and unsupported keyframe property diagnostics asserted by focused tests. |
</threat_model>

<verification>
```bash
nix develop -c cargo test -p mesh-core-shell keyframe_animation -- --nocapture
nix develop -c cargo test -p mesh-core-component keyframe -- --nocapture
nix develop -c cargo test -p mesh-core-shell animation_token -- --nocapture
```
</verification>

<success_criteria>
Plan 03 is complete when paint-only keyframes repaint without relayout, layout/unknown keyframes remain conservative, and parser/runtime diagnostics still cover unsupported animation behavior.
</success_criteria>
