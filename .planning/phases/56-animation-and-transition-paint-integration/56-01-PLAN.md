---
phase: 56-animation-and-transition-paint-integration
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
files_modified:
  - crates/core/ui/elements/src/style/types.rs
  - crates/core/ui/elements/src/style.rs
  - crates/core/shell/src/shell/component/animation.rs
  - .planning/phases/56-animation-and-transition-paint-integration/56-VALIDATION.md
requirements:
  - ANIM-01
  - ANIM-02
requirements_addressed: [ANIM-01, ANIM-02]
tags:
  - animation
  - property-buckets
  - retained-invalidation
must_haves:
  truths:
    - "D-01: Current bounded transition/keyframe behavior represented by AnimatedVisualStyle, TransitionProperties, AnimationStyle, token resolution, and shipped .mesh declarations remains the compatibility baseline."
    - "D-02: Properties are classified by invalidation cost before behavior changes."
    - "D-03: Layout-affecting properties continue to trigger relayout."
    - "D-04: transition-property: all stays conservative when any active animated property is layout-affecting."
    - "D-05: Unsupported or non-interpolable animation properties are visible through diagnostics."
  artifacts:
    - path: "crates/core/ui/elements/src/style/types.rs"
      provides: "Animation property bucket predicates"
      contains: "AnimationPropertyBucket"
    - path: "crates/core/shell/src/shell/component/animation.rs"
      provides: "Shell-side active animation bucket detection"
      contains: "active_animation_bucket"
---

<objective>
Make Phase 56 property classification explicit and testable before changing dirty-routing behavior.
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 56-01-01: Add explicit animation property bucket predicates</name>
  <files>crates/core/ui/elements/src/style/types.rs crates/core/ui/elements/src/style.rs</files>
  <read_first>
    - .planning/phases/56-animation-and-transition-paint-integration/56-CONTEXT.md
    - .planning/phases/56-animation-and-transition-paint-integration/56-RESEARCH.md
    - .planning/phases/56-animation-and-transition-paint-integration/56-PATTERNS.md
    - crates/core/ui/elements/src/style/types.rs
    - crates/core/ui/elements/src/style.rs
  </read_first>
  <behavior>
    - Add an explicit bucket enum for transition/keyframe invalidation decisions.
    - Keep existing `TransitionProperties::affects_layout()` behavior compatible.
    - Add tests that prove paint-only, layout-affecting, layer/effect, and unsupported/none classifications.
  </behavior>
  <action>
    In `crates/core/ui/elements/src/style/types.rs`, add `pub enum AnimationPropertyBucket { None, PaintOnly, LayerEffect, LayoutAffecting }`. Add methods on `TransitionProperties`: `animation_bucket(self) -> AnimationPropertyBucket`, `has_paint_only_animation(self) -> bool`, `has_layer_effect_animation(self) -> bool`, and `has_layout_affecting_animation(self) -> bool`. Classify `opacity`, `background_color`, `border_color`, `color`, `border_radius`, `transform`, and `border_width` as `PaintOnly` unless `all` is true with layout fields. Classify `box_shadow`, `filter`, and `backdrop_filter` as `LayerEffect`. Classify `width`, `height`, `min_width`, `max_width`, `min_height`, `max_height`, `padding`, `margin`, `font_size`, `letter_spacing`, `line_height`, `gap`, and inset fields as `LayoutAffecting`. Treat `all` as `LayoutAffecting`. Keep existing `affects_layout()` returning true for all layout-affecting fields and `all`. In `crates/core/ui/elements/src/style.rs`, add tests named `animation_property_bucket_classifies_paint_only_properties`, `animation_property_bucket_classifies_layer_effect_properties`, `animation_property_bucket_classifies_layout_affecting_properties`, and `animation_property_bucket_treats_all_as_layout_affecting`.
  </action>
  <verify>
    nix develop -c cargo test -p mesh-core-elements animation_property_bucket -- --nocapture
  </verify>
  <acceptance_criteria>
    - `rg "pub enum AnimationPropertyBucket" crates/core/ui/elements/src/style/types.rs` finds the enum.
    - `rg "fn animation_bucket" crates/core/ui/elements/src/style/types.rs` finds the bucket method.
    - `rg "has_layout_affecting_animation" crates/core/ui/elements/src/style/types.rs` finds the predicate.
    - `rg "animation_property_bucket_classifies_paint_only_properties" crates/core/ui/elements/src/style.rs` finds the test.
    - `rg "animation_property_bucket_treats_all_as_layout_affecting" crates/core/ui/elements/src/style.rs` finds the test.
    - `nix develop -c cargo test -p mesh-core-elements animation_property_bucket -- --nocapture` exits 0.
  </acceptance_criteria>
  <done>Animation property buckets are explicit, documented by tests, and ready for shell dirty routing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 56-01-02: Wire shell-side active animation bucket detection</name>
  <files>crates/core/shell/src/shell/component/animation.rs .planning/phases/56-animation-and-transition-paint-integration/56-VALIDATION.md</files>
  <read_first>
    - .planning/phases/56-animation-and-transition-paint-integration/56-CONTEXT.md
    - .planning/phases/56-animation-and-transition-paint-integration/56-RESEARCH.md
    - crates/core/shell/src/shell/component/animation.rs
    - crates/core/ui/elements/src/style/types.rs
    - .planning/phases/56-animation-and-transition-paint-integration/56-VALIDATION.md
  </read_first>
  <behavior>
    - Add shell-side helpers that determine whether active transitions are paint-only, layer/effect, or layout-affecting.
    - Preserve current dirty behavior until Plan 02 changes routing.
    - Mark validation rows for Plan 01 as green only after focused tests pass.
  </behavior>
  <action>
    In `animation.rs`, add a helper named `active_transition_bucket(transition: TransitionStyle) -> AnimationPropertyBucket` that delegates to `transition.properties.animation_bucket()`. Add a focused test named `animation_property_bucket_shell_helper_preserves_transition_classification` that builds `TransitionStyle` values for `opacity`, `box-shadow`, and `width` and asserts the helper returns `PaintOnly`, `LayerEffect`, and `LayoutAffecting`. Do not change `apply_style_animations_with_previous` dirty flag behavior in this task. In `56-VALIDATION.md`, update rows `56-01-01` and `56-01-02` from pending to green only if both focused commands pass.
  </action>
  <verify>
    nix develop -c cargo test -p mesh-core-shell animation_property_bucket -- --nocapture
    nix develop -c cargo test -p mesh-core-elements animation_property_bucket -- --nocapture
  </verify>
  <acceptance_criteria>
    - `rg "active_transition_bucket" crates/core/shell/src/shell/component/animation.rs` finds the helper.
    - `rg "animation_property_bucket_shell_helper_preserves_transition_classification" crates/core/shell/src/shell/component/animation.rs` finds the test.
    - `nix develop -c cargo test -p mesh-core-shell animation_property_bucket -- --nocapture` exits 0.
    - `nix develop -c cargo test -p mesh-core-elements animation_property_bucket -- --nocapture` exits 0.
    - `rg "56-01-01.*✅ green|56-01-02.*✅ green" .planning/phases/56-animation-and-transition-paint-integration/56-VALIDATION.md` finds both validation rows marked green.
  </acceptance_criteria>
  <done>Shell animation code can observe the same bucket classification as the style model without behavior changes.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Mitigation Plan |
|---|---|---|---|
| T-56-01 | Tampering | animation dirty classification | Use explicit enum and tests so properties cannot silently move between repaint and relayout categories. |
| T-56-02 | Repudiation | unsupported animation properties | Preserve diagnostics for unsupported/non-interpolable properties and require focused tests before routing changes. |
</threat_model>

<verification>
```bash
nix develop -c cargo test -p mesh-core-elements animation_property_bucket -- --nocapture
nix develop -c cargo test -p mesh-core-shell animation_property_bucket -- --nocapture
```
</verification>

<success_criteria>
Plan 01 is complete when animation property buckets are explicit, shell code can read them, compatibility tests pass, and no dirty-routing behavior has changed before classification is proven.
</success_criteria>
