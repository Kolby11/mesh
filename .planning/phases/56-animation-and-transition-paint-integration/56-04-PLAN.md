---
phase: 56-animation-and-transition-paint-integration
plan: 04
type: execute
wave: 4
depends_on:
  - 56-02
  - 56-03
autonomous: true
files_modified:
  - crates/core/shell/src/shell/component/shell_component.rs
  - crates/core/frontend/render/src/display_list.rs
  - crates/core/frontend/render/src/render_object.rs
  - .planning/phases/56-animation-and-transition-paint-integration/56-VALIDATION.md
requirements:
  - ANIM-03
requirements_addressed: [ANIM-03]
tags:
  - damage
  - visual-bounds
  - animation
must_haves:
  truths:
    - "D-11: Animated visual bounds are computed from current animated style, not only target static style."
    - "D-12: Damage includes both previous and current animated visual bounds for moving or expanding effects."
    - "D-13: Phase 56 damage work is focused on animation correctness and repaint metrics; broad stacking/z-index/clipping policy remains Phase 57."
    - "D-15: Proof uses existing profiling/debug payloads and dirty metrics where possible."
  artifacts:
    - path: "crates/core/shell/src/shell/component/shell_component.rs"
      provides: "Animated visual damage calculations"
      contains: "animation_damage_unions_previous_and_current"
---

<objective>
Ensure animated transforms and effect overflows damage the correct previous and current visual bounds without broad Phase 57 damage-policy redesign.
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 56-04-01: Expand widget damage to animated visual bounds</name>
  <files>crates/core/shell/src/shell/component/shell_component.rs crates/core/frontend/render/src/display_list.rs</files>
  <read_first>
    - .planning/phases/56-animation-and-transition-paint-integration/56-CONTEXT.md
    - .planning/phases/56-animation-and-transition-paint-integration/56-RESEARCH.md
    - .planning/phases/56-animation-and-transition-paint-integration/56-PATTERNS.md
    - crates/core/shell/src/shell/component/shell_component.rs
    - crates/core/frontend/render/src/display_list.rs
    - crates/core/ui/elements/src/style/types.rs
  </read_first>
  <behavior>
    - Damage for dirty animated nodes includes transform translation/scale bounds where represented.
    - Damage for dirty animated nodes includes shadow and filter overflow from current computed style.
    - Existing damage policy thresholds remain unchanged.
  </behavior>
  <action>
    In `shell_component.rs`, add a helper named `visual_damage_rect_for_widget_node(node: &WidgetNode, surface: DamageRect) -> Option<DamageRect>`. Start from layout bounds, then expand using current `node.computed_style.transform`, `box_shadow`, `filter`, and `backdrop_filter`. Use existing Phase 55 display-list visual-bounds math as the reference for shadow/filter overflow, but keep the helper local to damage calculation. Change `damage_rect_for_widget_node` or its call site to use the new visual helper for dirty node ids. Add tests named `animation_damage_includes_transform_visual_bounds` and `animation_damage_includes_shadow_filter_visual_bounds`. Do not alter `select_damage_policy` thresholds.
  </action>
  <verify>
    nix develop -c cargo test -p mesh-core-shell animation_damage -- --nocapture
    nix develop -c cargo test -p mesh-core-render display_list_effect -- --nocapture
  </verify>
  <acceptance_criteria>
    - `rg "visual_damage_rect_for_widget_node" crates/core/shell/src/shell/component/shell_component.rs` finds the helper.
    - `rg "animation_damage_includes_transform_visual_bounds" crates/core/shell/src/shell/component/shell_component.rs` finds the test.
    - `rg "animation_damage_includes_shadow_filter_visual_bounds" crates/core/shell/src/shell/component/shell_component.rs` finds the test.
    - `nix develop -c cargo test -p mesh-core-shell animation_damage -- --nocapture` exits 0.
    - `nix develop -c cargo test -p mesh-core-render display_list_effect -- --nocapture` exits 0.
  </acceptance_criteria>
  <done>Dirty animated nodes damage their current visual bounds, including transform/effect overflow.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 56-04-02: Union previous and current animated damage bounds</name>
  <files>crates/core/shell/src/shell/component/shell_component.rs crates/core/frontend/render/src/render_object.rs .planning/phases/56-animation-and-transition-paint-integration/56-VALIDATION.md</files>
  <read_first>
    - .planning/phases/56-animation-and-transition-paint-integration/56-CONTEXT.md
    - crates/core/shell/src/shell/component/shell_component.rs
    - crates/core/frontend/render/src/render_object.rs
    - crates/core/shell/src/shell/component/animation.rs
    - .planning/phases/56-animation-and-transition-paint-integration/56-VALIDATION.md
  </read_first>
  <behavior>
    - Moving or expanding animated visuals damage both the previous and current visual bounds.
    - Stale pixels from transform and shadow/filter changes are cleared by a single bounded damage rect when below existing promotion thresholds.
    - Repaint metrics expose the selected damage rect.
  </behavior>
  <action>
    Preserve previous visual damage bounds for dirty animated nodes across the update boundary. Add a small map keyed by `NodeId` or reuse retained render-object previous data if available; the selected approach must avoid script/tree rebuilds for paint-only ticks. When calculating `damage_rect_for_node_ids`, union current visual bounds with previous visual bounds before clipping to the surface. Add tests named `animation_damage_unions_previous_and_current_transform_bounds` and `animation_damage_unions_previous_and_current_shadow_bounds`. In `56-VALIDATION.md`, mark rows `56-04-01` and `56-04-02` green only after focused shell/render tests pass.
  </action>
  <verify>
    nix develop -c cargo test -p mesh-core-shell animation_damage -- --nocapture
    nix develop -c cargo test -p mesh-core-render render_object_tree_marks_animated -- --nocapture
  </verify>
  <acceptance_criteria>
    - `rg "animation_damage_unions_previous_and_current_transform_bounds" crates/core/shell/src/shell/component/shell_component.rs` finds the test.
    - `rg "animation_damage_unions_previous_and_current_shadow_bounds" crates/core/shell/src/shell/component/shell_component.rs` finds the test.
    - `rg "previous.*visual.*damage|last.*visual.*damage" crates/core/shell/src/shell/component/shell_component.rs` finds persistent previous-bound state or a clearly named equivalent.
    - `nix develop -c cargo test -p mesh-core-shell animation_damage -- --nocapture` exits 0.
    - `rg "56-04-01.*✅ green|56-04-02.*✅ green" .planning/phases/56-animation-and-transition-paint-integration/56-VALIDATION.md` finds both validation rows marked green.
  </acceptance_criteria>
  <done>Animated damage clears stale pixels by unioning previous and current visual bounds.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Mitigation Plan |
|---|---|---|---|
| T-56-07 | Tampering | selective damage | Damage animated visual bounds, not layout-only bounds, to avoid stale pixels from transform/effect movement. |
| T-56-08 | Denial of service | damage policy | Preserve existing promotion thresholds and add bounded-damage tests instead of forcing full-surface repaint for every animation. |
</threat_model>

<verification>
```bash
nix develop -c cargo test -p mesh-core-shell animation_damage -- --nocapture
nix develop -c cargo test -p mesh-core-render display_list_effect -- --nocapture
nix develop -c cargo test -p mesh-core-render render_object_tree_marks_animated -- --nocapture
```
</verification>

<success_criteria>
Plan 04 is complete when animated visual bounds include transform/effect overflow, previous/current animated bounds are unioned for damage, and existing damage policy thresholds remain intact.
</success_criteria>
