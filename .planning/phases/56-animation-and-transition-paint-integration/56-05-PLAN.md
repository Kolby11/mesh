---
phase: 56-animation-and-transition-paint-integration
plan: 05
type: execute
wave: 5
depends_on:
  - 56-01
  - 56-02
  - 56-03
  - 56-04
autonomous: true
files_modified:
  - crates/core/shell/src/shell/component/tests/integration/real_surfaces.rs
  - crates/core/shell/src/shell/runtime/request.rs
  - crates/core/shell/src/shell/component/shell_component.rs
  - .planning/phases/56-animation-and-transition-paint-integration/56-VALIDATION.md
requirements:
  - ANIM-01
  - ANIM-02
  - ANIM-03
requirements_addressed: [ANIM-01, ANIM-02, ANIM-03]
tags:
  - shipped-surfaces
  - audio-popover
  - validation
must_haves:
  truths:
    - "D-16: Fold the pending Phase 31 audio popover transition-delay polish into Phase 56 as compatibility proof, not broad redesign."
    - "D-17: Recheck transition timing does not reintroduce first-click, same-hover close, or first-drag input loss."
    - "D-18: Do not fold module install requirement-resolution into Phase 56."
    - "ANIM-01, ANIM-02, and ANIM-03 all have automated proof before validation metadata is complete."
  artifacts:
    - path: "crates/core/shell/src/shell/component/tests/integration/real_surfaces.rs"
      provides: "Shipped navigation/audio animation regression proof"
      contains: "shipped_navigation_animation"
    - path: ".planning/phases/56-animation-and-transition-paint-integration/56-VALIDATION.md"
      provides: "Final validation metadata"
      contains: "nyquist_compliant: true"
---

<objective>
Prove Phase 56 on shipped navigation/audio animation paths, close validation metadata, and preserve the bounded painter ownership split.
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 56-05-01: Add shipped navigation and audio popover animation regression proof</name>
  <files>crates/core/shell/src/shell/component/tests/integration/real_surfaces.rs crates/core/shell/src/shell/runtime/request.rs crates/core/shell/src/shell/component/shell_component.rs</files>
  <read_first>
    - .planning/phases/56-animation-and-transition-paint-integration/56-CONTEXT.md
    - .planning/todos/pending/2026-05-13-phase31-audio-popover-transition-delay.md
    - crates/core/shell/src/shell/component/tests/integration/real_surfaces.rs
    - crates/core/shell/src/shell/runtime/request.rs
    - crates/core/shell/src/shell/component/shell_component.rs
    - modules/frontend/navigation-bar/src/main.mesh
    - modules/frontend/navigation-bar/src/components/volume-button.mesh
  </read_first>
  <behavior>
    - Shipped navigation keyframe/transition behavior still requests rendering while active.
    - Audio popover show/hide transition timing uses existing `hide_transition_ms` / `closing_until` behavior.
    - Same-hover close, first-click, and first-drag regressions stay covered.
  </behavior>
  <action>
    Add focused shipped-surface tests named `shipped_navigation_animation_keeps_status_pulse_repaint_only`, `shipped_navigation_audio_popover_transition_delay_stays_bounded`, and `shipped_navigation_audio_popover_transition_does_not_consume_first_input`. If implementation changes are needed, keep them bounded to existing surface transition lifecycle fields: `hide_transition_ms`, `closing_until`, `set_surface_exiting`, and delayed hide completion. Do not introduce a new public surface-transition API in this plan.
  </action>
  <verify>
    nix develop -c cargo test -p mesh-core-shell shipped_navigation_animation -- --nocapture
    nix develop -c cargo test -p mesh-core-shell shipped_navigation -- --nocapture
  </verify>
  <acceptance_criteria>
    - `rg "shipped_navigation_animation_keeps_status_pulse_repaint_only" crates/core/shell/src/shell/component/tests/integration/real_surfaces.rs` finds the test.
    - `rg "shipped_navigation_audio_popover_transition_delay_stays_bounded" crates/core/shell/src/shell/component/tests/integration/real_surfaces.rs` finds the test.
    - `rg "shipped_navigation_audio_popover_transition_does_not_consume_first_input" crates/core/shell/src/shell/component/tests/integration/real_surfaces.rs` finds the test.
    - `nix develop -c cargo test -p mesh-core-shell shipped_navigation_animation -- --nocapture` exits 0.
    - `nix develop -c cargo test -p mesh-core-shell shipped_navigation -- --nocapture` exits 0.
  </acceptance_criteria>
  <done>Shipped navigation/audio animation paths remain compatible with Phase 56 painter integration.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 56-05-02: Run final Phase 56 validation and mark metadata complete</name>
  <files>.planning/phases/56-animation-and-transition-paint-integration/56-VALIDATION.md</files>
  <read_first>
    - .planning/phases/56-animation-and-transition-paint-integration/56-VALIDATION.md
    - .planning/phases/56-animation-and-transition-paint-integration/56-CONTEXT.md
    - .planning/REQUIREMENTS.md
  </read_first>
  <behavior>
    - All focused Phase 56 suites pass.
    - Validation metadata records `status: complete`, `nyquist_compliant: true`, and `wave_0_complete: true`.
    - Backend-neutrality remains intact for renderer/style/display-list data.
  </behavior>
  <action>
    Run the final full suite exactly as listed in `56-VALIDATION.md`: `nix develop -c cargo test -p mesh-core-shell animation -- --nocapture`, `nix develop -c cargo test -p mesh-core-shell shipped_navigation -- --nocapture`, and `nix develop -c cargo test -p mesh-core-render render_object_tree_marks -- --nocapture`. Also run `rg "skia_safe" crates/core/frontend/render/src/display_list.rs crates/core/frontend/render/src/render_object.rs crates/core/ui/elements/src && exit 1 || exit 0`. If all pass, update frontmatter in `56-VALIDATION.md` to `status: complete`, `nyquist_compliant: true`, and `wave_0_complete: true`; update every Phase 56 verification row to `✅ green`; set `Approval: complete`.
  </action>
  <verify>
    nix develop -c cargo test -p mesh-core-shell animation -- --nocapture
    nix develop -c cargo test -p mesh-core-shell shipped_navigation -- --nocapture
    nix develop -c cargo test -p mesh-core-render render_object_tree_marks -- --nocapture
    rg "skia_safe" crates/core/frontend/render/src/display_list.rs crates/core/frontend/render/src/render_object.rs crates/core/ui/elements/src && exit 1 || exit 0
  </verify>
  <acceptance_criteria>
    - `rg "status: complete" .planning/phases/56-animation-and-transition-paint-integration/56-VALIDATION.md` finds complete status.
    - `rg "nyquist_compliant: true" .planning/phases/56-animation-and-transition-paint-integration/56-VALIDATION.md` finds true.
    - `rg "wave_0_complete: true" .planning/phases/56-animation-and-transition-paint-integration/56-VALIDATION.md` finds true.
    - `rg "Approval: complete" .planning/phases/56-animation-and-transition-paint-integration/56-VALIDATION.md` finds approval.
    - `nix develop -c cargo test -p mesh-core-shell animation -- --nocapture` exits 0.
    - `nix develop -c cargo test -p mesh-core-shell shipped_navigation -- --nocapture` exits 0.
    - `nix develop -c cargo test -p mesh-core-render render_object_tree_marks -- --nocapture` exits 0.
  </acceptance_criteria>
  <done>Phase 56 validation metadata is complete and all focused animation proof is green.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Mitigation Plan |
|---|---|---|---|
| T-56-09 | Denial of service | shipped animation regressions | Shipped navigation/audio tests prove animation changes do not reintroduce input loss or runaway relayout. |
| T-56-10 | Tampering | backend ownership boundary | Final backend-neutrality grep confirms Skia types do not leak into retained style/display-list/render-object data. |
</threat_model>

<verification>
```bash
nix develop -c cargo test -p mesh-core-shell animation -- --nocapture
nix develop -c cargo test -p mesh-core-shell shipped_navigation -- --nocapture
nix develop -c cargo test -p mesh-core-render render_object_tree_marks -- --nocapture
rg "skia_safe" crates/core/frontend/render/src/display_list.rs crates/core/frontend/render/src/render_object.rs crates/core/ui/elements/src && exit 1 || exit 0
```
</verification>

<success_criteria>
Plan 05 is complete when shipped navigation/audio animation proof is green, validation metadata is complete, and all Phase 56 requirements have final automated evidence.
</success_criteria>
