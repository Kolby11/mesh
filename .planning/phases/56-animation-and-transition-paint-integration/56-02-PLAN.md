---
phase: 56-animation-and-transition-paint-integration
plan: 02
type: execute
wave: 2
depends_on:
  - 56-01
autonomous: true
files_modified:
  - crates/core/shell/src/shell/component/animation.rs
  - crates/core/shell/src/shell/component/tests/interaction/animation.rs
  - crates/core/frontend/render/src/render_object.rs
  - .planning/phases/56-animation-and-transition-paint-integration/56-VALIDATION.md
requirements:
  - ANIM-02
requirements_addressed: [ANIM-02]
tags:
  - animation
  - visual-repaint
  - retained-render-objects
must_haves:
  truths:
    - "D-06: MESH remains the owner of animation state and tick scheduling; Skia receives ordinary painter commands."
    - "D-07: Paint-only animation ticks use VISUAL_REPAINT and update retained render-object/display-list signatures without rebuilding the Luau tree or forcing layout."
    - "D-09: Transition start, restart, cancellation, delay, pause, fill-mode, and keyframe lifecycle behavior remain deterministic for stable _mesh_key nodes."
    - "D-10: Direct widget-tree rendering and retained display-list replay keep command-class parity while animated styles are active."
    - "D-14: Add deterministic tests that assert paint-only animation ticks avoid layout when geometry does not change, and that layout-affecting animation ticks still trigger relayout."
  artifacts:
    - path: "crates/core/shell/src/shell/component/animation.rs"
      provides: "Paint-only transition dirty routing"
      contains: "animation_transition_dirty_uses_visual_repaint_for_paint_only_changes"
    - path: "crates/core/frontend/render/src/render_object.rs"
      provides: "Retained dirty slot proof for animated visual changes"
      contains: "render_object_tree_marks_animated"
---

<objective>
Route paint-only transition ticks through retained visual repaint while preserving relayout for geometry-changing animations.
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 56-02-01: Use visual repaint for paint-only transitions</name>
  <files>crates/core/shell/src/shell/component/animation.rs crates/core/shell/src/shell/component/tests/interaction/animation.rs</files>
  <read_first>
    - .planning/phases/56-animation-and-transition-paint-integration/56-CONTEXT.md
    - .planning/phases/56-animation-and-transition-paint-integration/56-RESEARCH.md
    - .planning/phases/56-animation-and-transition-paint-integration/56-PATTERNS.md
    - crates/core/shell/src/shell/component/animation.rs
    - crates/core/shell/src/shell/component/tests/interaction/animation.rs
    - crates/core/shell/src/shell/component.rs
  </read_first>
  <behavior>
    - Active transitions whose changed properties are paint-only or layer/effect-only use `ComponentDirtyFlags::VISUAL_REPAINT`.
    - Active transitions whose changed properties affect layout still use `ComponentDirtyFlags::STYLE_RELAYOUT`.
    - Existing transition restart/cancellation behavior remains unchanged.
  </behavior>
  <action>
    Replace the current `has_layout_affecting_animation` boolean accumulation with an active transition bucket accumulator. In `apply_style_animations_to_node`, after `apply_node_style_animation`, inspect the active `StyleAnimation` for the node. If it is unfinished, use `animation.transition.properties.animation_bucket()` to mark either paint-only/layer-effect active or layout-affecting active. In `apply_style_animations_with_previous`, choose `ComponentDirtyFlags::STYLE_RELAYOUT` only when a layout-affecting active transition exists; choose `ComponentDirtyFlags::VISUAL_REPAINT` when active transitions are paint-only/layer-effect and no layout-affecting keyframe exists. Add tests named `animation_transition_dirty_uses_visual_repaint_for_paint_only_changes` and `animation_transition_dirty_uses_relayout_for_geometry_changes`.
  </action>
  <verify>
    nix develop -c cargo test -p mesh-core-shell animation_transition_dirty -- --nocapture
  </verify>
  <acceptance_criteria>
    - `rg "animation_transition_dirty_uses_visual_repaint_for_paint_only_changes" crates/core/shell/src/shell/component/tests/interaction/animation.rs` finds the test.
    - `rg "animation_transition_dirty_uses_relayout_for_geometry_changes" crates/core/shell/src/shell/component/tests/interaction/animation.rs` finds the test.
    - `rg "ComponentDirtyFlags::VISUAL_REPAINT" crates/core/shell/src/shell/component/animation.rs` still finds visual repaint routing.
    - `rg "ComponentDirtyFlags::STYLE_RELAYOUT" crates/core/shell/src/shell/component/animation.rs` still finds relayout routing.
    - `nix develop -c cargo test -p mesh-core-shell animation_transition_dirty -- --nocapture` exits 0.
  </acceptance_criteria>
  <done>Paint-only transitions repaint without forcing full style relayout.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 56-02-02: Prove retained render-object dirty slots for animated visual updates</name>
  <files>crates/core/frontend/render/src/render_object.rs .planning/phases/56-animation-and-transition-paint-integration/56-VALIDATION.md</files>
  <read_first>
    - .planning/phases/56-animation-and-transition-paint-integration/56-CONTEXT.md
    - .planning/phases/56-animation-and-transition-paint-integration/56-PATTERNS.md
    - crates/core/frontend/render/src/render_object.rs
    - crates/core/shell/src/shell/component/animation.rs
    - .planning/phases/56-animation-and-transition-paint-integration/56-VALIDATION.md
  </read_first>
  <behavior>
    - Transform-only animated style changes dirty the render-object transform slot, not geometry.
    - Opacity-only animated style changes dirty the opacity slot, not geometry.
    - Shadow/filter/color animated style changes dirty material, not geometry.
  </behavior>
  <action>
    Add tests in `render_object.rs` named `render_object_tree_marks_animated_transform_without_geometry_dirty`, `render_object_tree_marks_animated_opacity_without_geometry_dirty`, and `render_object_tree_marks_animated_material_without_geometry_dirty`. Each test should build a stable `WidgetNode` with the same `NodeId`, update exactly one animated visual field, call `RenderObjectTree::update`, and assert the expected dirty slot count is `1` while `dirty.geometry == 0`. In `56-VALIDATION.md`, mark rows `56-02-01` and `56-02-02` green only after the shell and render focused tests pass.
  </action>
  <verify>
    nix develop -c cargo test -p mesh-core-render render_object_tree_marks_animated -- --nocapture
    nix develop -c cargo test -p mesh-core-shell animation_transition_dirty -- --nocapture
  </verify>
  <acceptance_criteria>
    - `rg "render_object_tree_marks_animated_transform_without_geometry_dirty" crates/core/frontend/render/src/render_object.rs` finds the test.
    - `rg "render_object_tree_marks_animated_opacity_without_geometry_dirty" crates/core/frontend/render/src/render_object.rs` finds the test.
    - `rg "render_object_tree_marks_animated_material_without_geometry_dirty" crates/core/frontend/render/src/render_object.rs` finds the test.
    - `nix develop -c cargo test -p mesh-core-render render_object_tree_marks_animated -- --nocapture` exits 0.
    - `nix develop -c cargo test -p mesh-core-shell animation_transition_dirty -- --nocapture` exits 0.
    - `rg "56-02-01.*✅ green|56-02-02.*✅ green" .planning/phases/56-animation-and-transition-paint-integration/56-VALIDATION.md` finds both validation rows marked green.
  </acceptance_criteria>
  <done>Retained render-object slots prove paint-only animated updates do not masquerade as geometry changes.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Mitigation Plan |
|---|---|---|---|
| T-56-03 | Denial of service | animation repaint loop | Keep paint-only ticks on `VISUAL_REPAINT` and add tests proving layout is avoided when geometry does not change. |
| T-56-04 | Tampering | retained render-object state | Assert exact dirty slots for transform, opacity, and material changes so retained paint receives the intended update category. |
</threat_model>

<verification>
```bash
nix develop -c cargo test -p mesh-core-shell animation_transition_dirty -- --nocapture
nix develop -c cargo test -p mesh-core-render render_object_tree_marks_animated -- --nocapture
```
</verification>

<success_criteria>
Plan 02 is complete when paint-only transitions route through visual repaint, geometry-changing transitions still relayout, and retained render objects report precise non-geometry dirty slots for animated visual changes.
</success_criteria>
