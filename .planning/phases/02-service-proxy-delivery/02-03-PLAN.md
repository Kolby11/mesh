---
phase: 02-service-proxy-delivery
plan: 03
type: execute
wave: 3
depends_on:
  - "02-01"
  - "02-02"
files_modified:
  - packages/plugins/frontend/core/panel/src/main.mesh
  - packages/plugins/frontend/core/quick-settings/src/main.mesh
  - packages/plugins/frontend/core/quick-settings/src/components/audio-section.mesh
  - packages/plugins/frontend/core/quick-settings/src/components/wifi-section.mesh
  - packages/plugins/frontend/core/quick-settings/src/components/bluetooth-section.mesh
  - crates/core/shell/src/shell/component.rs
  - docs/plugins/frontend/core/README.md
autonomous: true
requirements:
  - PROXY-01
  - PROXY-02
  - PROXY-03
  - PROXY-04
  - PROXY-05
  - PROXY-06
  - SURF-06
user_setup: []
must_haves:
  truths:
    - "Bundled panel and quick-settings surfaces exercise the same public proxy API external plugins are expected to use."
    - "Real frontend scripts receive live service state through proxy fields and react after every backend emission."
    - "Real frontend scripts dispatch service commands through contract methods, not shell-specific hacks."
    - "Service-proxy failures stay visible while the UI degrades gracefully."
  artifacts:
    - path: "packages/plugins/frontend/core/panel/src/main.mesh"
      provides: "Built-in panel proof of proxy reads"
      contains: "require(\"@mesh/audio@>=1.0\")"
    - path: "packages/plugins/frontend/core/quick-settings/src/main.mesh"
      provides: "Interactive quick-settings proof of proxy reads and commands"
      contains: "onToggleWiFi"
    - path: "docs/plugins/frontend/core/README.md"
      provides: "Copyable public frontend proxy guidance"
      contains: "require(\"@mesh/<service>\")"
  key_links:
    - from: "packages/plugins/frontend/core/quick-settings/src/**/*.mesh"
      to: "crates/core/runtime/scripting/src/context.rs"
      via: "public proxy field reads and `on_change` subscriptions"
      pattern: "on_change"
    - from: "packages/plugins/frontend/core/panel/src/main.mesh"
      to: "packages/plugins/backend/core/*-interface/interface.toml"
      via: "reads only documented contract state fields and commands"
      pattern: "@mesh/"
---

<objective>
Adopt the finalized service proxy contract in the built-in panel and quick-settings surfaces and prove the end-to-end bridge with real frontend code.

Purpose: Phase 2 is only complete when the same public proxy APIs are used by the shell's own shipped surfaces, not just by unit tests.
Output: Migrated bundled `.mesh` surfaces, end-to-end shell coverage, and updated public frontend guidance for all proxy requirements plus SURF-06.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-service-proxy-delivery/02-RESEARCH.md
@.planning/phases/02-service-proxy-delivery/02-PATTERNS.md
@.planning/phases/02-service-proxy-delivery/02-VALIDATION.md
@.planning/phases/02-service-proxy-delivery/02-01-SUMMARY.md
@.planning/phases/02-service-proxy-delivery/02-02-SUMMARY.md

@packages/plugins/frontend/core/panel/src/main.mesh
@packages/plugins/frontend/core/quick-settings/src/main.mesh
@packages/plugins/frontend/core/quick-settings/src/components/audio-section.mesh
@packages/plugins/frontend/core/quick-settings/src/components/wifi-section.mesh
@packages/plugins/frontend/core/quick-settings/src/components/bluetooth-section.mesh
@docs/plugins/frontend/core/README.md
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| Bundled surfaces keep relying on legacy `mesh.service.bind/on` paths so the public proxy API remains unproven | medium | Migrate panel and quick-settings examples to `require(...)`, proxy field reads, and `proxy.on_change(...)` or `on_<service>_update()` where appropriate. |
| Frontend scripts read undocumented payload keys and lock the project into accidental contracts | medium | Limit built-in reads to contract-documented state fields produced by Plan 02 and update docs in the same plan. |
| Graceful degradation regresses into blank UI when a provider or contract is missing | medium | Keep `pcall(require, ...)` fallback patterns, but pair them with the visible diagnostics path from Plan 01 and explicit unavailable labels. |
| Surface migrations introduce shell-specific branching instead of proxy commands | low | Route writes through contract methods such as `audio.volume_up()` and `network.set_wifi_enabled(...)`, not direct shell events except for shell-owned surface toggles. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Migrate bundled panel and quick-settings scripts onto the finalized proxy read/update path</name>
  <files>packages/plugins/frontend/core/panel/src/main.mesh, packages/plugins/frontend/core/quick-settings/src/main.mesh, packages/plugins/frontend/core/quick-settings/src/components/audio-section.mesh, packages/plugins/frontend/core/quick-settings/src/components/wifi-section.mesh, packages/plugins/frontend/core/quick-settings/src/components/bluetooth-section.mesh</files>
  <read_first>packages/plugins/frontend/core/panel/src/main.mesh, packages/plugins/frontend/core/quick-settings/src/main.mesh, packages/plugins/frontend/core/quick-settings/src/components/audio-section.mesh, packages/plugins/frontend/core/quick-settings/src/components/wifi-section.mesh, packages/plugins/frontend/core/quick-settings/src/components/bluetooth-section.mesh, .planning/phases/02-service-proxy-delivery/02-RESEARCH.md</read_first>
  <action>Update the built-in frontend scripts so they consistently use `require("@mesh/<service>@>=1.0")`, proxy field reads from the latest emitted payload, and either `proxy.on_change(...)` or `on_<service>_update()` for reactive refreshes. Replace remaining legacy `mesh.service.bind(...)` / `mesh.service.on(...)` usage in quick-settings where the proxy path is now sufficient. Keep `pcall(require, ...)` for graceful fallback where a provider may be missing, but preserve explicit unavailable labels and rely on the visible diagnostics path from Plan 01.</action>
  <verify>cargo test -p mesh-core-shell</verify>
  <acceptance_criteria>
    - `packages/plugins/frontend/core/quick-settings/src/main.mesh` no longer contains `mesh.service.bind(` or `mesh.service.on(` for the migrated service paths.
    - `packages/plugins/frontend/core/panel/src/main.mesh` and the quick-settings component scripts all contain `require("@mesh/` lookups for the services they consume.
    - At least one bundled script contains `on_audio_update` or another `on_<service>_update` handler, or an equivalent explicit proxy subscription chosen and documented in the action.
  </acceptance_criteria>
  <done>The shipped surfaces now prove the same proxy path external plugin authors are meant to use.</done>
</task>

<task type="auto">
  <name>Task 2: Add shell-facing integration coverage for live updates and proxy command dispatch</name>
  <files>crates/core/shell/src/shell/component.rs</files>
  <read_first>crates/core/shell/src/shell/component.rs, crates/core/shell/src/shell/service.rs, crates/core/runtime/scripting/src/context.rs, packages/plugins/frontend/core/panel/src/main.mesh, packages/plugins/frontend/core/quick-settings/src/components/audio-section.mesh</read_first>
  <action>Add or extend shell/component integration tests so a frontend runtime receiving `ServiceEvent::Updated { service = "mesh.audio", ... }` can read the updated proxy fields, run the expected update handlers, and publish a service command from a bundled-style handler. Cover at minimum one read path (for example `audio.percent`) and one write path (for example `audio.volume_up()` or `network.set_wifi_enabled(true)`) through to `CoreRequest::ServiceCommand`.</action>
  <verify>cargo test -p mesh-core-shell</verify>
  <acceptance_criteria>
    - `cargo test -p mesh-core-shell` passes.
    - A test named `frontend_proxy_update_reaches_bundled_surface_logic` or equivalent proves a service update changes frontend state through the proxy path.
    - A test named `frontend_proxy_command_becomes_service_command_request` or equivalent proves a bundled-style handler publishes `CoreRequest::ServiceCommand`.
  </acceptance_criteria>
  <done>PROXY-01 through PROXY-05 are proven in shell-facing integration coverage, not only unit tests.</done>
</task>

<task type="auto">
  <name>Task 3: Refresh public frontend guidance around the stabilized proxy contract</name>
  <files>docs/plugins/frontend/core/README.md</files>
  <read_first>docs/plugins/frontend/core/README.md, packages/plugins/frontend/core/panel/src/main.mesh, packages/plugins/frontend/core/quick-settings/src/main.mesh, packages/plugins/frontend/core/quick-settings/src/components/audio-section.mesh</read_first>
  <action>Update the public frontend plugin guide so its copyable examples match the stabilized proxy contract from this phase: `require("@mesh/<service>")`, proxy field reads, `proxy.on_change(...)`, and `on_<service>_update()` where appropriate. Use the bundled panel and quick-settings patterns as the examples of record. Explicitly note that missing providers should be handled with `pcall(...)` plus user-visible fallback UI rather than by assuming the service always exists.</action>
  <verify>rg -n "require\\(\"@mesh/|on_change|on_[a-z]+_update|pcall\\(require" docs/plugins/frontend/core/README.md</verify>
  <acceptance_criteria>
    - `docs/plugins/frontend/core/README.md` contains `require("@mesh/<service>")` guidance tied to the proxy contract.
    - `docs/plugins/frontend/core/README.md` contains at least one `on_change` or `on_<service>_update` example that matches the migrated bundled surfaces.
    - `docs/plugins/frontend/core/README.md` contains guidance for `pcall(require, ...)` fallback behavior when no provider or contract is available.
  </acceptance_criteria>
  <done>The public frontend docs reflect the actual stabilized Phase 2 proxy behavior.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cargo test -p mesh-core-shell` passes.
- [ ] `rg -n "mesh\\.service\\.bind|mesh\\.service\\.on" packages/plugins/frontend/core/panel/src/main.mesh packages/plugins/frontend/core/quick-settings/src/**/*.mesh` shows only any intentionally retained legacy usage, if any.
- [ ] `rg -n "require\\(\"@mesh/|on_change|on_[a-z]+_update" packages/plugins/frontend/core/panel/src/main.mesh packages/plugins/frontend/core/quick-settings/src/**/*.mesh docs/plugins/frontend/core/README.md` shows the finalized proxy patterns.
- [ ] Built-in surfaces still route shell-owned surface toggles through `mesh.events.publish(...)`, while service mutations go through proxy commands.
</verification>

<success_criteria>
- Bundled panel and quick-settings surfaces use the finalized service proxy contract end to end.
- Live backend emissions update real frontend UI through proxy state and update handlers.
- Bundled surfaces issue service commands through contract methods.
- Public frontend guidance matches the real built-in proxy usage and fallback behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/02-service-proxy-delivery/02-03-SUMMARY.md`
</output>
