---
phase: 02-service-proxy-delivery
plan: 03
type: execute
wave: 3
depends_on:
  - "02-01"
  - "02-02"
files_modified:
  - packages/plugins/frontend/core/panel/src/main.mesh
  - packages/plugins/frontend/core/quick-settings/src/main.mesh
  - packages/plugins/frontend/core/quick-settings/src/components/audio-section.mesh
  - packages/plugins/frontend/core/quick-settings/src/components/wifi-section.mesh
  - packages/plugins/frontend/core/quick-settings/src/components/bluetooth-section.mesh
  - crates/core/shell/src/shell/component.rs
  - docs/plugins/frontend/core/README.md
autonomous: true
requirements:
  - PROXY-01
  - PROXY-02
  - PROXY-03
  - PROXY-04
  - PROXY-05
  - PROXY-06
  - SURF-06
user_setup: []
must_haves:
  truths:
    - "D-23 D-24 D-25: migrate all bundled Phase 02 consumers, remove legacy `mesh.service.bind(...)` / `mesh.service.on(...)` / `proxy.on_change(...)` usage completely, and allow dominant-provider advanced behavior while keeping the base-interface core path functional."
    - "Bundled panel and quick-settings surfaces use the same public read-fields / command-methods proxy API that external plugin authors are expected to use."
    - "Bundled surfaces no longer depend on `mesh.service.bind(...)`, `mesh.service.on(...)`, or `proxy.on_change(...)` for their primary service update path."
    - "Advanced dominant-provider state may enrich bundled surfaces, but the core read path and primary commands still work on the shared base contract."
    - "Bundled degraded states remain visible and textual when `pcall(require, ...)` cannot acquire a provider or interface contract."
  artifacts:
    - path: "packages/plugins/frontend/core/panel/src/main.mesh"
      provides: "Read-first panel proof of direct proxy field reads"
      contains: "require(\"@mesh/network@>=1.0\")"
    - path: "packages/plugins/frontend/core/quick-settings/src/main.mesh"
      provides: "Quick-settings shell controls driven by proxy fields and command methods"
      contains: "onToggleWiFi"
    - path: "docs/plugins/frontend/core/README.md"
      provides: "Copyable frontend guidance for the stabilized proxy contract"
      contains: "require(\"@mesh/<service>\")"
    - path: "crates/core/shell/src/shell/component.rs"
      provides: "Shell-facing integration coverage for bundled proxy update flows"
      contains: "ServiceEvent::Updated"
  key_links:
    - from: "packages/plugins/frontend/core/panel/src/main.mesh"
      to: "packages/plugins/backend/core/*-interface/interface.toml"
      via: "panel derives concise labels from current proxy fields instead of read-helper methods"
      pattern: "@mesh/"
    - from: "packages/plugins/frontend/core/quick-settings/src/components/*.mesh"
      to: "crates/core/runtime/scripting/src/context.rs"
      via: "bundled components exercise the finalized rerender-driven field tracking semantics"
      pattern: "require(\"@mesh/"
    - from: "docs/plugins/frontend/core/README.md"
      to: "packages/plugins/frontend/core/quick-settings/src/main.mesh"
      via: "public docs mirror the exact bundled authoring pattern"
      pattern: "Quick Settings"
---

<objective>
Migrate the bundled panel and quick-settings surfaces to the finalized Phase 02 proxy contract and prove the end-to-end behavior with shell-facing integration coverage and updated frontend guidance.

Purpose: The runtime and contract work are only real when the shell’s shipped frontend surfaces stop depending on legacy callbacks/binds and instead read service state through rerender plus command methods on the proxy.
Output: Migrated bundled `.mesh` consumers, shell integration coverage, and public frontend docs aligned to the final contract.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-service-proxy-delivery/02-CONTEXT.md
@.planning/phases/02-service-proxy-delivery/02-RESEARCH.md
@.planning/phases/02-service-proxy-delivery/02-PATTERNS.md
@.planning/phases/02-service-proxy-delivery/02-VALIDATION.md
@.planning/phases/02-service-proxy-delivery/02-UI-SPEC.md
@.planning/phases/02-service-proxy-delivery/02-01-SUMMARY.md
@.planning/phases/02-service-proxy-delivery/02-02-SUMMARY.md

@packages/plugins/frontend/core/panel/src/main.mesh
@packages/plugins/frontend/core/quick-settings/src/main.mesh
@packages/plugins/frontend/core/quick-settings/src/components/audio-section.mesh
@packages/plugins/frontend/core/quick-settings/src/components/wifi-section.mesh
@packages/plugins/frontend/core/quick-settings/src/components/bluetooth-section.mesh
@docs/plugins/frontend/core/README.md
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| Bundled surfaces keep calling legacy read-helper or callback APIs and therefore never prove the finalized public authoring model | high | Rewrite panel and quick-settings to read current proxy fields directly, derive local presentation on rerender, and route backend mutation through named command methods only. |
| The shell UI becomes provider-locked for basic operation instead of only for advanced dominant-provider extras | medium | Keep the panel and quick-settings core user path on guaranteed base state/commands, while gating richer extras behind graceful fallback text and defaults. |
| Degraded service states render as blank or silently stale UI after migrating away from callback-driven local state | medium | Preserve `pcall(require, ...)` guards, explicit unavailable copy, and shell-facing integration tests that exercise missing-provider paths. |
| Frontend docs continue teaching `mesh.service.bind(...)`, `mesh.service.on(...)`, and `audio:on_change(...)` after the runtime and bundled surfaces have moved on | medium | Update docs to use the shipped panel/quick-settings examples and explicitly reject callback-style service update APIs as the canonical model. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite bundled panel and quick-settings surfaces to read proxy fields and invoke proxy commands directly</name>
  <files>packages/plugins/frontend/core/panel/src/main.mesh, packages/plugins/frontend/core/quick-settings/src/main.mesh, packages/plugins/frontend/core/quick-settings/src/components/audio-section.mesh, packages/plugins/frontend/core/quick-settings/src/components/wifi-section.mesh, packages/plugins/frontend/core/quick-settings/src/components/bluetooth-section.mesh</files>
  <read_first>packages/plugins/frontend/core/panel/src/main.mesh, packages/plugins/frontend/core/quick-settings/src/main.mesh, packages/plugins/frontend/core/quick-settings/src/components/audio-section.mesh, packages/plugins/frontend/core/quick-settings/src/components/wifi-section.mesh, packages/plugins/frontend/core/quick-settings/src/components/bluetooth-section.mesh, .planning/phases/02-service-proxy-delivery/02-CONTEXT.md, .planning/phases/02-service-proxy-delivery/02-UI-SPEC.md</read_first>
  <action>Update the bundled frontend scripts so they no longer use `mesh.service.bind(...)`, `mesh.service.on(...)`, or `audio.on_change(...)`. In `packages/plugins/frontend/core/panel/src/main.mesh`, replace `audio.default_output()`, `power.battery()`, and `network.active_connection()` with direct reads from emitted proxy fields such as `audio.percent`, `audio.muted`, `power.level`, `power.available`, `network.connections`, and `network.source_plugin`, then derive `volumeIcon`, `volumeLevel`, `batteryText`, and `networkStatus` locally on rerender. In `packages/plugins/frontend/core/quick-settings/src/main.mesh` and its child components, derive icon state, Wi-Fi state, Bluetooth device lists, and audio labels from direct proxy field reads and keep writes routed through named command methods such as `audio.volume_up()`, `audio.toggle_mute()`, and `network.set_wifi_enabled(...)`. Preserve `pcall(require, ...)` guards and explicit unavailable copy when a service cannot be loaded.</action>
  <verify>cargo test -p mesh-core-shell</verify>
  <acceptance_criteria>
    - `packages/plugins/frontend/core/quick-settings/src/main.mesh` no longer contains `mesh.service.bind(` or `mesh.service.on(`.
    - `packages/plugins/frontend/core/quick-settings/src/components/audio-section.mesh` no longer contains `on_change(`.
    - `packages/plugins/frontend/core/panel/src/main.mesh` no longer calls `default_output()`, `battery()`, or `active_connection()` for its primary read path.
    - All listed bundled surface scripts continue to contain `require("@mesh/` lookups and explicit unavailable/fallback copy where a service may be missing.
  </acceptance_criteria>
  <done>The shipped shell surfaces now exercise the same direct-field / named-command proxy model external authors should copy.</done>
</task>

<task type="auto">
  <name>Task 2: Add shell-facing integration tests for bundled rerender reads, degraded states, and proxy command dispatch</name>
  <files>crates/core/shell/src/shell/component.rs</files>
  <read_first>crates/core/shell/src/shell/component.rs, crates/core/runtime/scripting/src/context.rs, crates/core/shell/src/shell/service.rs, packages/plugins/frontend/core/panel/src/main.mesh, packages/plugins/frontend/core/quick-settings/src/components/audio-section.mesh, packages/plugins/frontend/core/quick-settings/src/components/wifi-section.mesh</read_first>
  <action>Add or extend tests in `crates/core/shell/src/shell/component.rs` so a bundled-style frontend runtime receiving `ServiceEvent::Updated` can read updated proxy fields on rerender, keep explicit fallback copy when `pcall(require, ...)` fails, and publish named proxy commands that become `CoreRequest::ServiceCommand`. Cover at minimum one panel read path (`audio.percent` or `network.connections`), one quick-settings write path (`audio.volume_up()` or `network.set_wifi_enabled(true)`), and one degraded lookup path where a missing provider still yields user-visible copy instead of a blank surface.</action>
  <verify>cargo test -p mesh-core-shell</verify>
  <acceptance_criteria>
    - `cargo test -p mesh-core-shell` passes.
    - A test named `frontend_proxy_update_reaches_panel_or_quick_settings_render_state` or equivalent proves rerender-visible service state without callback registration.
    - A test named `frontend_proxy_command_from_bundled_handler_becomes_service_command_request` or equivalent proves a bundled control publishes `CoreRequest::ServiceCommand`.
    - A test named `frontend_missing_service_keeps_visible_fallback_copy` or equivalent proves a failed `require(...)` path still renders non-empty explanatory UI.
  </acceptance_criteria>
  <done>Bundled surfaces prove the finalized proxy semantics in shell-facing integration tests, including degraded states.</done>
</task>

<task type="auto">
  <name>Task 3: Rewrite frontend plugin documentation to match the shipped read-fields / command-methods pattern</name>
  <files>docs/plugins/frontend/core/README.md</files>
  <read_first>docs/plugins/frontend/core/README.md, packages/plugins/frontend/core/panel/src/main.mesh, packages/plugins/frontend/core/quick-settings/src/main.mesh, packages/plugins/frontend/core/quick-settings/src/components/audio-section.mesh, .planning/phases/02-service-proxy-delivery/02-UI-SPEC.md</read_first>
  <action>Replace the current callback/bind examples in `docs/plugins/frontend/core/README.md` with copyable examples based on the migrated panel and quick-settings code. Show `require("@mesh/<service>")` or `mesh.service.use(...)` reading state fields directly on rerender, show named command methods for backend mutation, keep element event handlers on template elements only, and document `pcall(require, ...)` plus visible fallback copy as the canonical degraded-state pattern. Do not leave any example that teaches `mesh.service.bind(...)`, `mesh.service.on(...)`, or `audio:on_change(...)` as the public service-update model.</action>
  <verify>rg -n "mesh\\.service\\.bind|mesh\\.service\\.on|on_change\\(|require\\(\"@mesh/|pcall\\(require|onclick|onchange" docs/plugins/frontend/core/README.md</verify>
  <acceptance_criteria>
    - `docs/plugins/frontend/core/README.md` contains `require("@mesh/<service>")` guidance tied to direct field reads and named command methods.
    - `docs/plugins/frontend/core/README.md` no longer uses `mesh.service.bind(...)`, `mesh.service.on(...)`, or `audio:on_change(...)` in its primary examples.
    - `docs/plugins/frontend/core/README.md` includes `pcall(require, ...)` plus textual fallback guidance for missing providers or contracts.
  </acceptance_criteria>
  <done>The public frontend docs now teach the same proxy pattern used by the shipped shell surfaces.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cargo test -p mesh-core-shell` passes.
- [ ] `rg -n "mesh\\.service\\.bind|mesh\\.service\\.on|on_change\\(" packages/plugins/frontend/core/panel/src/main.mesh packages/plugins/frontend/core/quick-settings/src/main.mesh packages/plugins/frontend/core/quick-settings/src/components/*.mesh docs/plugins/frontend/core/README.md` returns no callback/bind usage in the migrated paths.
- [ ] `rg -n "require\\(\"@mesh/|pcall\\(require|onclick|onchange" packages/plugins/frontend/core/panel/src/main.mesh packages/plugins/frontend/core/quick-settings/src/main.mesh packages/plugins/frontend/core/quick-settings/src/components/*.mesh docs/plugins/frontend/core/README.md` shows the finalized proxy and element-event patterns.
- [ ] Built-in surfaces still route shell-owned surface toggles through `mesh.events.publish(...)` while service mutations go through proxy commands.
</verification>

<success_criteria>
- Bundled panel and quick-settings surfaces use direct proxy state reads plus named command methods.
- The bundled UI no longer depends on callback/bind-style service update APIs.
- Missing-provider or missing-contract paths remain visibly degraded rather than silently blank.
- Frontend docs mirror the shipped surface authoring model exactly.
</success_criteria>

<output>
After completion, create `.planning/phases/02-service-proxy-delivery/02-03-SUMMARY.md`
</output>
