---
phase: 02-service-proxy-delivery
plan: 02
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - crates/core/extension/service/src/contract.rs
  - crates/core/extension/service/src/interface.rs
  - crates/tools/lsp/src/knowledge/mesh_api.rs
  - packages/plugins/backend/core/audio-interface/interface.toml
  - packages/plugins/backend/core/network-interface/interface.toml
  - packages/plugins/backend/core/power-interface/interface.toml
  - packages/plugins/backend/core/media-interface/interface.toml
  - docs/plugins/backend/core/README.md
autonomous: true
requirements:
  - PROXY-03
  - SURF-06
user_setup: []
must_haves:
  truths:
    - "Proxy command methods come from the declared contract, not ad hoc frontend knowledge."
    - "Audio, network, power, and media contracts document their state fields, callbacks, and commands."
    - "Runtime, docs, and editor-facing API knowledge describe the same service proxy contract."
  artifacts:
    - path: "crates/core/extension/service/src/contract.rs"
      provides: "Typed contract metadata parsing"
      contains: "InterfaceContract"
    - path: "packages/plugins/backend/core/audio-interface/interface.toml"
      provides: "Audio service contract metadata"
      contains: "set_volume"
    - path: "docs/plugins/backend/core/README.md"
      provides: "Plugin-author contract overview"
      contains: "interface contracts"
  key_links:
    - from: "packages/plugins/backend/core/*-interface/interface.toml"
      to: "crates/core/extension/service/src/contract.rs"
      via: "machine-readable contract metadata for fields, callbacks, and commands"
      pattern: "interface.toml"
    - from: "crates/core/extension/service/src/contract.rs"
      to: "crates/tools/lsp/src/knowledge/mesh_api.rs"
      via: "shared contract language for editor and runtime documentation"
      pattern: "service.use"
---

<objective>
Turn the audio, network, power, and media contract packages into a reliable source of truth for proxy commands and documented state fields.

Purpose: Phase 2 only stays stable if frontend authors, runtime code, and LSP hints all describe the same service contract surface.
Output: Extended contract metadata, updated core interface TOMLs, and aligned runtime/editor/docs language for PROXY-03 and SURF-06.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-service-proxy-delivery/02-RESEARCH.md
@.planning/phases/02-service-proxy-delivery/02-PATTERNS.md
@.planning/phases/02-service-proxy-delivery/02-VALIDATION.md
@.planning/phases/02-service-proxy-delivery/02-01-SUMMARY.md

@crates/core/extension/service/src/contract.rs
@crates/core/extension/service/src/interface.rs
@crates/tools/lsp/src/knowledge/mesh_api.rs
@packages/plugins/backend/core/audio-interface/interface.toml
@packages/plugins/backend/core/network-interface/interface.toml
@packages/plugins/backend/core/power-interface/interface.toml
@packages/plugins/backend/core/media-interface/interface.toml
@docs/plugins/backend/core/README.md
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| Frontend docs and runtime contract drift again because state fields live only in prose | medium | Add machine-readable contract metadata for state fields and callbacks, then consume or mirror that metadata consistently in docs and editor knowledge. |
| Contract parser changes break existing interface TOML loading | medium | Make any new metadata optional and preserve current method/event parsing for old contracts. Add parser regression tests. |
| Proxy commands expose methods the backend never documented or vice versa | medium | Verify all callable proxy methods are declared in the contract TOMLs and keep exact command names aligned across contract, runtime, and docs. |
| Service metadata changes broaden Phase 2 into a plugin-format rewrite | low | Limit the new metadata to state fields, callbacks/update hooks, and command descriptions needed by this phase. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add optional contract metadata for emitted state fields and update callbacks</name>
  <files>crates/core/extension/service/src/contract.rs, crates/core/extension/service/src/interface.rs, packages/plugins/backend/core/audio-interface/interface.toml, packages/plugins/backend/core/network-interface/interface.toml, packages/plugins/backend/core/power-interface/interface.toml, packages/plugins/backend/core/media-interface/interface.toml</files>
  <read_first>crates/core/extension/service/src/contract.rs, crates/core/extension/service/src/interface.rs, packages/plugins/backend/core/audio-interface/interface.toml, packages/plugins/backend/core/network-interface/interface.toml, packages/plugins/backend/core/power-interface/interface.toml, packages/plugins/backend/core/media-interface/interface.toml</read_first>
  <action>Extend the contract model to load optional metadata that documents the latest emitted state fields and update callbacks for a service. Keep existing `methods`, `events`, `types`, and `capabilities` parsing untouched. Add machine-readable metadata to the four built-in contracts for audio, network, power, and media covering at minimum: the primary state fields frontend scripts read, the implicit update callback name `on_<service>_update`, and the proxy subscription callback shape represented by `proxy.on_change(...)`.</action>
  <verify>cargo test -p mesh-core-service</verify>
  <acceptance_criteria>
    - `crates/core/extension/service/src/contract.rs` contains typed fields for optional state-field and callback metadata or an equivalent parsed representation.
    - The audio, network, power, and media `interface.toml` files each contain explicit state-field metadata and an update callback entry.
    - Existing contract parser tests still pass and at least one new test proves the optional metadata loads successfully.
  </acceptance_criteria>
  <done>SURF-06 has a machine-readable contract source of truth instead of only scattered examples.</done>
</task>

<task type="auto">
  <name>Task 2: Align proxy command metadata with the callable contract surface</name>
  <files>packages/plugins/backend/core/audio-interface/interface.toml, packages/plugins/backend/core/network-interface/interface.toml, packages/plugins/backend/core/power-interface/interface.toml, packages/plugins/backend/core/media-interface/interface.toml, crates/core/extension/service/src/interface.rs</files>
  <read_first>packages/plugins/backend/core/audio-interface/interface.toml, packages/plugins/backend/core/network-interface/interface.toml, packages/plugins/backend/core/power-interface/interface.toml, packages/plugins/backend/core/media-interface/interface.toml, crates/core/runtime/scripting/src/context.rs</read_first>
  <action>Review the built-in contract TOMLs against the proxy methods that bundled frontends and backends actually use. Add or correct metadata so command names, argument names, and return types match the intended proxy API exactly. Keep provider resolution behavior in `interface.rs` unchanged except for any small typed helpers needed to expose the richer metadata cleanly.</action>
  <verify>cargo test -p mesh-core-service && cargo test -p mesh-core-scripting context</verify>
  <acceptance_criteria>
    - The audio contract still contains callable methods such as `set_volume`, `set_muted`, `volume_up`, `volume_down`, and `toggle_mute`.
    - The network, power, and media contracts expose the command methods their providers or bundled surfaces are expected to call.
    - `cargo test -p mesh-core-scripting context` passes after the contract metadata changes, proving proxy method lookup still works.
  </acceptance_criteria>
  <done>PROXY-03 is backed by a trustworthy contract surface instead of ad hoc runtime behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Update editor and plugin-author documentation to match the finalized contract metadata</name>
  <files>crates/tools/lsp/src/knowledge/mesh_api.rs, docs/plugins/backend/core/README.md</files>
  <read_first>crates/tools/lsp/src/knowledge/mesh_api.rs, docs/plugins/backend/core/README.md, packages/plugins/backend/core/audio-interface/interface.toml, packages/plugins/backend/core/network-interface/interface.toml, packages/plugins/backend/core/power-interface/interface.toml, packages/plugins/backend/core/media-interface/interface.toml</read_first>
  <action>Update the editor-facing `mesh.service.use(...)` / proxy knowledge and the backend contract overview docs so they explicitly describe the built-in contract metadata for state fields, update callbacks, and command methods. Use the same service names and callback language as the contract TOMLs. Do not leave the docs saying only that methods exist "when a contract exists"; list the concrete built-in services Phase 2 stabilizes and the fields/callbacks frontend authors can rely on.</action>
  <verify>rg -n "on_[a-z]+_update|on_change|state field|command" crates/tools/lsp/src/knowledge/mesh_api.rs docs/plugins/backend/core/README.md</verify>
  <acceptance_criteria>
    - `crates/tools/lsp/src/knowledge/mesh_api.rs` contains language that proxy fields, `on_change`, and `on_<service>_update` are part of the service proxy contract.
    - `docs/plugins/backend/core/README.md` names audio, network, power, and media and documents their contract state fields/callbacks/commands.
    - `rg -n "on_[a-z]+_update|on_change|state field|command" crates/tools/lsp/src/knowledge/mesh_api.rs docs/plugins/backend/core/README.md` returns matches.
  </acceptance_criteria>
  <done>Runtime, docs, and editor hints all describe the same service contract surface.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cargo test -p mesh-core-service` passes.
- [ ] `cargo test -p mesh-core-scripting context` passes.
- [ ] `rg -n "state|callback|on_change|on_[a-z]+_update" packages/plugins/backend/core/*-interface/interface.toml` shows contract metadata for the four core services.
- [ ] `rg -n "service\\.use|on_change|on_[a-z]+_update" crates/tools/lsp/src/knowledge/mesh_api.rs docs/plugins/backend/core/README.md` shows aligned docs/editor wording.
</verification>

<success_criteria>
- Contract metadata, not scattered examples, defines the built-in proxy method surface.
- Audio, network, power, and media contracts document state fields, callbacks, and commands.
- Runtime-facing and editor-facing API descriptions stop disagreeing with the contract packages.
</success_criteria>

<output>
After completion, create `.planning/phases/02-service-proxy-delivery/02-02-SUMMARY.md`
</output>
