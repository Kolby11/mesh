---
phase: 04-real-core-surfaces
plan: 03
type: execute
wave: 3
depends_on:
  - "04-02"
files_modified:
  - packages/plugins/frontend/core/panel/src/main.mesh
  - crates/core/shell/src/shell/component.rs
  - docs/plugins/frontend/core/README.md
autonomous: true
requirements:
  - SURF-01
  - SURF-02
  - SURF-03
  - SURF-04
  - SURF-05
user_setup:
  - "Manual runtime verification requires a Wayland/dev shell environment with at least one audio provider and NetworkManager available."
must_haves:
  truths:
    - "D-01 D-02 D-03: top panel stays a compact live status and entry surface; direct panel controls are not required."
    - "D-12 D-13 D-14: final surfaces show visible disabled states for unavailable/permission-denied provider states."
    - "D-15 D-16 D-17: final surfaces use public proxy field reads plus named commands, with service logic staying in Luau providers."
    - "Phase 4 success criteria 1-5 must be proven by automated checks where possible plus documented manual runtime verification where host services are required."
  artifacts:
    - path: "packages/plugins/frontend/core/panel/src/main.mesh"
      provides: "Compact top panel live-service proof"
      contains: "onRender"
    - path: "crates/core/shell/src/shell/component.rs"
      provides: "End-to-end shell tests across panel and quick settings"
      contains: "real_core_surfaces"
    - path: "docs/plugins/frontend/core/README.md"
      provides: "Optional docs alignment if public examples contradict Phase 4"
      contains: "require(\"@mesh/"
  key_links:
    - from: "panel/src/main.mesh"
      to: "quick-settings/src/main.mesh"
      via: "panel opens quick settings for control rather than mutating service state directly"
      pattern: "shell.toggle-quick-settings"
---

<objective>
Finalize the compact top panel proof and run end-to-end Phase 4 coverage across panel, quick settings, public API usage, and fallback states.

Purpose: After runtime compatibility and quick-settings controls are in place, Phase 4 needs a complete proof that shipped surfaces exercise the same public APIs external plugin authors will use.
Output: Compact panel proof, final integration tests/static checks, and docs alignment only if existing public guidance contradicts the implemented model.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-real-core-surfaces/04-CONTEXT.md
@.planning/phases/04-real-core-surfaces/04-RESEARCH.md
@.planning/phases/04-real-core-surfaces/04-UI-SPEC.md
@.planning/phases/04-real-core-surfaces/04-VALIDATION.md
@.planning/phases/04-real-core-surfaces/04-PATTERNS.md
@.planning/phases/04-real-core-surfaces/04-01-SUMMARY.md
@.planning/phases/04-real-core-surfaces/04-02-SUMMARY.md

@packages/plugins/frontend/core/panel/src/main.mesh
@packages/plugins/frontend/core/quick-settings/src/main.mesh
@packages/plugins/frontend/core/quick-settings/src/components/audio-section.mesh
@packages/plugins/frontend/core/quick-settings/src/components/wifi-section.mesh
@crates/core/shell/src/shell/component.rs
@docs/plugins/frontend/core/README.md
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| T-04-08: Final verification passes tests but panel no longer shows live service values | medium | Add panel-specific assertions and static checks for `audio.percent`, `network.connections`, or `power.level` reads in `onRender()`. |
| T-04-09: Panel becomes a second control surface and expands Phase 4 scope | low | Keep panel interaction limited to opening/toggling quick settings or existing surface events. |
| T-04-10: Public docs/examples still teach outdated callback APIs | medium | Inspect docs and update only if they contradict the final read-field/named-command model. |
| T-04-11: Host-service behavior cannot be fully proven in CI | medium | Record manual runtime validation instructions in the summary and verification artifacts. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Confirm compact panel live-service proof</name>
  <files>packages/plugins/frontend/core/panel/src/main.mesh</files>
  <read_first>packages/plugins/frontend/core/panel/src/main.mesh, .planning/phases/04-real-core-surfaces/04-CONTEXT.md, .planning/phases/04-real-core-surfaces/04-UI-SPEC.md, .planning/phases/04-real-core-surfaces/04-02-SUMMARY.md</read_first>
  <action>Inspect and update `panel/src/main.mesh` only as needed so it remains a compact status and entry surface. It must read real service proxy fields in `onRender()` such as `audio.percent`, `audio.muted`, `network.connections`, and `power.level`/`power.available`; derive `volumeLevel`, `volumeIcon`, `networkStatus`, and `batteryText`; and publish `shell.toggle-quick-settings` from the volume/audio indicator. Do not add direct panel volume sliders, Wi-Fi toggles, or network connect/disconnect controls. Preserve token-only styles and compact right-side indicator layout.</action>
  <verify>rg -n "audio\\.percent|audio\\.muted|network\\.connections|power\\.level|shell\\.toggle-quick-settings|set_wifi_enabled|set_volume" packages/plugins/frontend/core/panel/src/main.mesh</verify>
  <acceptance_criteria>
    - `panel/src/main.mesh` contains `audio.percent` or another real audio proxy field read.
    - `panel/src/main.mesh` contains `network.connections` or another real network proxy field read.
    - `panel/src/main.mesh` contains `power.level` or `power.available` if battery remains visible.
    - `panel/src/main.mesh` contains `shell.toggle-quick-settings`.
    - `panel/src/main.mesh` does not call `audio.set_volume` or `network.set_wifi_enabled`.
  </acceptance_criteria>
  <done>The top panel satisfies SURF-01 without expanding into a direct control surface.</done>
</task>

<task type="auto">
  <name>Task 2: Add final end-to-end surface coverage and static regressions</name>
  <files>crates/core/shell/src/shell/component.rs</files>
  <read_first>crates/core/shell/src/shell/component.rs, crates/core/shell/src/shell/service.rs, packages/plugins/frontend/core/panel/src/main.mesh, packages/plugins/frontend/core/quick-settings/src/main.mesh, packages/plugins/frontend/core/quick-settings/src/components/audio-section.mesh, packages/plugins/frontend/core/quick-settings/src/components/wifi-section.mesh</read_first>
  <action>Add or extend final Phase 4 tests in `crates/core/shell/src/shell/component.rs` with names prefixed `real_core_surfaces_`. Cover: panel render state changes when seeded audio/network/power payloads change; panel volume click publishes or maps to quick-settings toggle event; quick-settings audio command and Wi-Fi command tests from Plan 02 remain passing; missing service fallback copy remains non-empty. Add static regression assertions or test strings that reject `mesh.service.bind`, `mesh.service.on`, and proxy `.on_change(` in the panel/quick-settings fixture strings if those fixtures are embedded in tests.</action>
  <verify>cargo test -p mesh-core-shell real_core_surfaces</verify>
  <acceptance_criteria>
    - `component.rs` contains at least one test name starting with `real_core_surfaces_`.
    - A test asserts panel state changes after seeded service payloads.
    - A test asserts panel click behavior opens/toggles quick settings or publishes the expected shell event.
    - A static or runtime assertion prevents callback API regression in shipped panel/quick-settings examples.
    - `cargo test -p mesh-core-shell real_core_surfaces` exits 0.
  </acceptance_criteria>
  <done>Final automated coverage proves Phase 4 surface behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Align public frontend docs only if they contradict Phase 4</name>
  <files>docs/plugins/frontend/core/README.md</files>
  <read_first>docs/plugins/frontend/core/README.md, packages/plugins/frontend/core/panel/src/main.mesh, packages/plugins/frontend/core/quick-settings/src/components/audio-section.mesh, packages/plugins/frontend/core/quick-settings/src/components/wifi-section.mesh, .planning/phases/04-real-core-surfaces/04-CONTEXT.md</read_first>
  <action>Inspect `docs/plugins/frontend/core/README.md`. If it teaches legacy service callbacks (`mesh.service.bind`, `mesh.service.on`, or proxy `on_change`) as the current model, replace those examples with concise Phase 4-compatible examples: `pcall(require, "@mesh/audio@>=1.0")`, read fields such as `audio.percent` in `onRender()`, call named methods such as `audio.set_volume("default", 0.5)` from element handlers, and show visible fallback copy like `Audio unavailable`. If the docs already match, leave the file unchanged and note that in the summary.</action>
  <verify>rg -n "mesh\\.service\\.bind|mesh\\.service\\.on|\\.on_change\\(|require\\(\"@mesh/|pcall\\(require|Audio unavailable|set_volume" docs/plugins/frontend/core/README.md</verify>
  <acceptance_criteria>
    - If docs are changed, README contains `pcall(require, "@mesh/audio@>=1.0")` or equivalent service lookup guidance.
    - If docs are changed, README contains `audio.percent` and `audio.set_volume` or `audio:set_volume` examples.
    - README does not teach `mesh.service.bind`, `mesh.service.on`, or proxy `.on_change(` as current service update APIs.
  </acceptance_criteria>
  <done>Public frontend docs do not contradict Phase 4 shipped surface behavior.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cargo test -p mesh-core-scripting` passes.
- [ ] `cargo test -p mesh-core-backend` passes.
- [ ] `cargo test -p mesh-core-shell` passes.
- [ ] `rg -n "mesh\\.service\\.bind|mesh\\.service\\.on|\\.on_change\\(" packages/plugins/frontend/core/panel/src/main.mesh packages/plugins/frontend/core/quick-settings/src docs/plugins/frontend/core/README.md` returns no current-model regressions.
- [ ] `rg -n "Audio unavailable|Audio controls unavailable|Network unavailable|Network controls unavailable|Connection details unavailable" packages/plugins/frontend/core/quick-settings/src` shows visible disabled state copy.
- [ ] Manual runtime verification steps for real provider behavior are recorded in `04-03-SUMMARY.md`.
</verification>

<success_criteria>
- Top panel renders live service-backed data and stays compact.
- Quick settings renders live audio and network state.
- Quick settings can change audio volume/mute through service proxy commands.
- Quick settings can toggle or command network state where supported.
- Shipped surfaces exercise public proxy APIs external plugin authors can copy.
</success_criteria>

<output>
After completion, create `.planning/phases/04-real-core-surfaces/04-03-SUMMARY.md`
</output>
