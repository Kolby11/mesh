---
phase: 04-real-core-surfaces
plan: 06
type: execute
wave: 2
depends_on:
  - "04-04"
files_modified:
  - packages/plugins/frontend/core/panel/src/main.mesh
  - packages/plugins/frontend/core/quick-settings/src/main.mesh
  - docs/plugins/frontend/core/README.md
  - crates/core/shell/src/shell/component.rs
autonomous: true
gap_closure: true
requirements:
  - SURF-01
  - SURF-02
  - SURF-03
  - SURF-04
  - SURF-05
requirements_addressed:
  - SURF-01
  - SURF-02
  - SURF-03
  - SURF-04
  - SURF-05
user_setup: []
must_haves:
  truths:
    - "Panel opens quick settings through supported shell surface routing."
    - "Quick settings close uses supported shell surface routing."
    - "Frontend docs teach the same supported shell surface events and proxy APIs shipped by the surfaces."
  artifacts:
    - path: "packages/plugins/frontend/core/panel/src/main.mesh"
      provides: "Supported quick-settings toggle request"
      contains: "shell.toggle-surface"
    - path: "packages/plugins/frontend/core/quick-settings/src/main.mesh"
      provides: "Supported quick-settings hide request"
      contains: "shell.hide-surface"
    - path: "docs/plugins/frontend/core/README.md"
      provides: "Public frontend shell event examples"
      contains: "surface_id = \"@mesh/quick-settings\""
    - path: "crates/core/shell/src/shell/component.rs"
      provides: "Real core surface routing regressions"
      contains: "real_core_surfaces"
  key_links:
    - from: "packages/plugins/frontend/core/panel/src/main.mesh"
      to: "crates/core/shell/src/shell/service.rs"
      via: "`mesh.events.publish(\"shell.toggle-surface\", { surface_id = \"@mesh/quick-settings\" })` maps to `CoreRequest::ToggleSurface`"
      pattern: "shell.toggle-surface"
    - from: "packages/plugins/frontend/core/quick-settings/src/main.mesh"
      to: "crates/core/shell/src/shell/service.rs"
      via: "`mesh.events.publish(\"shell.hide-surface\", { surface_id = \"@mesh/quick-settings\" })` maps to `CoreRequest::HideSurface`"
      pattern: "shell.hide-surface"
---

<objective>
Close the shell surface event routing gap and keep public docs aligned with the shipped core surfaces.

Purpose: The panel and quick-settings surface currently publish unsupported named shell events. The shell router already supports generic surface events with `surface_id`; shipped surfaces and docs must use that finalized contract so controls open and close reliably.
Output: Updated panel/quick-settings shell events, docs examples, and routing regressions.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-real-core-surfaces/04-CONTEXT.md
@.planning/phases/04-real-core-surfaces/04-VERIFICATION.md
@.planning/phases/04-real-core-surfaces/04-REVIEW.md
@.planning/phases/04-real-core-surfaces/04-UI-SPEC.md
@.planning/phases/04-real-core-surfaces/04-PATTERNS.md
@.planning/phases/04-real-core-surfaces/04-03-SUMMARY.md
@.planning/phases/04-real-core-surfaces/04-04-SUMMARY.md

@packages/plugins/frontend/core/panel/src/main.mesh
@packages/plugins/frontend/core/quick-settings/src/main.mesh
@docs/plugins/frontend/core/README.md
@crates/core/shell/src/shell/component.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace unsupported quick-settings shell event names in shipped surfaces</name>
  <files>packages/plugins/frontend/core/panel/src/main.mesh, packages/plugins/frontend/core/quick-settings/src/main.mesh</files>
  <read_first>packages/plugins/frontend/core/panel/src/main.mesh, packages/plugins/frontend/core/quick-settings/src/main.mesh, crates/core/shell/src/shell/service.rs, .planning/phases/04-real-core-surfaces/04-VERIFICATION.md, .planning/phases/04-real-core-surfaces/04-UI-SPEC.md</read_first>
  <action>In `packages/plugins/frontend/core/panel/src/main.mesh`, change `onVolumeClick()` to publish the supported shell event exactly as `mesh.events.publish("shell.toggle-surface", { surface_id = "@mesh/quick-settings" })`. In `packages/plugins/frontend/core/quick-settings/src/main.mesh`, change `onClose()` to publish exactly `mesh.events.publish("shell.hide-surface", { surface_id = "@mesh/quick-settings" })`. Do not add direct panel audio/network controls; the panel remains a compact status and entry surface per D-01 and D-02. Do not introduce raw colors or UI layout changes.</action>
  <verify>
    <automated>rg -n "shell\\.toggle-surface|shell\\.hide-surface|surface_id = \"@mesh/quick-settings\"" packages/plugins/frontend/core/panel/src/main.mesh packages/plugins/frontend/core/quick-settings/src/main.mesh</automated>
    <automated>bash -lc 'if rg -n "shell\\.(toggle|close)-quick-settings" packages/plugins/frontend/core/panel/src/main.mesh packages/plugins/frontend/core/quick-settings/src/main.mesh; then exit 1; fi'</automated>
  </verify>
  <acceptance_criteria>
    - Panel `onVolumeClick()` publishes `shell.toggle-surface` with `surface_id = "@mesh/quick-settings"`.
    - Quick settings `onClose()` publishes `shell.hide-surface` with `surface_id = "@mesh/quick-settings"`.
    - Shipped panel and quick-settings sources contain no `shell.toggle-quick-settings` or `shell.close-quick-settings`.
    - Panel still does not call `audio.set_volume` or `network.set_wifi_enabled`.
  </acceptance_criteria>
  <done>Shipped surfaces use supported shell surface event names for quick-settings open and close.</done>
</task>

<task type="auto">
  <name>Task 2: Update frontend docs to supported shell surface events</name>
  <files>docs/plugins/frontend/core/README.md</files>
  <read_first>docs/plugins/frontend/core/README.md, packages/plugins/frontend/core/panel/src/main.mesh, packages/plugins/frontend/core/quick-settings/src/main.mesh, .planning/phases/04-real-core-surfaces/04-CONTEXT.md</read_first>
  <action>In `docs/plugins/frontend/core/README.md`, replace examples that teach `shell.toggle-quick-settings` or `shell.close-quick-settings`. The shell event section must show `mesh.events.publish("shell.toggle-surface", { surface_id = "@mesh/quick-settings" })` for opening/toggling and `mesh.events.publish("shell.hide-surface", { surface_id = "@mesh/quick-settings" })` for closing. Keep the existing distinction that service mutations use named proxy methods such as `audio.set_volume("default", normalized)` while shell UI transitions use `mesh.events.publish(...)`. Do not reintroduce callback-style service APIs.</action>
  <verify>
    <automated>rg -n "shell\\.toggle-surface|shell\\.hide-surface|surface_id = \"@mesh/quick-settings\"|audio\\.set_volume|mesh\\.service\\.bind|mesh\\.service\\.on|\\.on_change\\(" docs/plugins/frontend/core/README.md</automated>
    <automated>bash -lc 'if rg -n "shell\\.(toggle|close)-quick-settings" docs/plugins/frontend/core/README.md; then exit 1; fi'</automated>
  </verify>
  <acceptance_criteria>
    - README teaches `shell.toggle-surface` with `surface_id = "@mesh/quick-settings"`.
    - README teaches `shell.hide-surface` with `surface_id = "@mesh/quick-settings"`.
    - README contains no `shell.toggle-quick-settings` or `shell.close-quick-settings`.
    - README still teaches named service proxy methods for service mutations.
  </acceptance_criteria>
  <done>Frontend docs no longer teach unsupported quick-settings shell event names.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Repair final surface routing regressions after supported event update</name>
  <files>crates/core/shell/src/shell/component.rs</files>
  <read_first>crates/core/shell/src/shell/component.rs, crates/core/shell/src/shell/service.rs, packages/plugins/frontend/core/panel/src/main.mesh, packages/plugins/frontend/core/quick-settings/src/main.mesh, .planning/phases/04-real-core-surfaces/04-04-SUMMARY.md</read_first>
  <action>Update the `real_core_surfaces_*` tests in `component.rs` so panel click behavior expects the supported surface-routing contract instead of the unsupported named event. The panel click test should assert either the drained published event channel is `shell.toggle-surface` with payload `{ "surface_id": "@mesh/quick-settings" }`, or the routed request is `CoreRequest::ToggleSurface { surface_id: "@mesh/quick-settings" }` depending on the existing helper shape. Add or update a quick-settings close test that asserts `shell.hide-surface` with the same `surface_id` maps to `CoreRequest::HideSurface`. Keep callback-free regression assertions for `mesh.service.bind`, `mesh.service.on`, and `.on_change(` in shipped panel/quick-settings sources. Account for Plan 04's `CoreRequest::ServiceCommand` source fields when updating pattern matches.</action>
  <verify>
    <automated>cargo test -p mesh-core-shell real_core_surfaces -- --nocapture</automated>
    <automated>rg -n "real_core_surfaces|shell\\.toggle-surface|shell\\.hide-surface|ToggleSurface|HideSurface|@mesh/quick-settings|shell\\.toggle-quick-settings|shell\\.close-quick-settings" crates/core/shell/src/shell/component.rs</automated>
  </verify>
  <acceptance_criteria>
    - Final surface tests no longer assert `shell.toggle-quick-settings` or `shell.close-quick-settings`.
    - A panel click regression proves `shell.toggle-surface` targets `@mesh/quick-settings`.
    - A quick-settings close regression proves `shell.hide-surface` targets `@mesh/quick-settings`.
    - Callback-free public API regression checks remain present.
    - If `cargo test -p mesh-core-shell real_core_surfaces -- --nocapture` is blocked by missing `xkbcommon.pc`, the static `rg` checks pass and the summary records that host constraint explicitly.
  </acceptance_criteria>
  <done>Final surface regressions prove supported quick-settings surface routing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| panel script -> shell router | Panel publishes shell UI transition requests through event channels. |
| quick-settings script -> shell router | Quick settings close action publishes shell UI transition requests. |
| public docs -> plugin author code | External plugin authors copy documented event and proxy API examples. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-06-01 | Tampering | unsupported shell event names | mitigate | Replace `shell.toggle-quick-settings` and `shell.close-quick-settings` with routed `shell.toggle-surface`/`shell.hide-surface` plus `surface_id`. |
| T-04-06-02 | Denial of Service | quick-settings close/open path | mitigate | Add regressions proving panel toggle and close become `ToggleSurface`/`HideSurface` rather than unknown `ServiceCommand`s. |
| T-04-06-03 | Repudiation | frontend docs | mitigate | Update docs so public examples match shipped surface routing and named proxy command APIs. |
| T-04-06-04 | Elevation of Privilege | service command examples | transfer | Command authorization is owned by Plan 04; this plan preserves named proxy methods and does not add service command bypasses. |
</threat_model>

<must_haves>
- Panel audio indicator opens/toggles quick settings through `shell.toggle-surface` and `surface_id = "@mesh/quick-settings"`.
- Quick settings close uses `shell.hide-surface` and `surface_id = "@mesh/quick-settings"`.
- Docs contain the same shell event names as shipped surfaces.
- Unsupported named quick-settings shell events are absent from shipped surfaces, docs, and final surface tests.
</must_haves>

<verification>
- Run `cargo test -p mesh-core-shell real_core_surfaces -- --nocapture` where `xkbcommon.pc` is available.
- On hosts missing `xkbcommon.pc`, run the task static `rg` checks and record the cargo compile blocker in `04-06-SUMMARY.md`.
- Run `rg -n "shell\\.(toggle|close)-quick-settings" packages/plugins/frontend/core/panel/src/main.mesh packages/plugins/frontend/core/quick-settings/src/main.mesh docs/plugins/frontend/core/README.md crates/core/shell/src/shell/component.rs`; this command must return no matches.
</verification>

<success_criteria>
- SURF-01's compact panel entry point routes to quick settings through supported shell APIs.
- SURF-02 through SURF-05 final surface regressions and docs no longer depend on unsupported shell event names.
- Public docs and shipped surfaces exercise the same finalized scripting and shell contracts.
</success_criteria>

<output>
After completion, create `.planning/phases/04-real-core-surfaces/04-06-SUMMARY.md`
</output>
