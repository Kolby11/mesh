---
phase: 87-layout-and-display-elements
plan: 02
title: Compiler Lowering Accessibility And Runtime Proof
type: execute
wave: 2
depends_on: [87-01]
files_modified:
  - crates/core/frontend/compiler/src/tags.rs
  - crates/core/frontend/compiler/src/render.rs
  - crates/core/ui/elements/src/layout.rs
  - crates/core/shell/src/shell/component/tests/integration/real_surfaces.rs
autonomous: true
requirements: [ELEMLAYOUT-01, ELEMLAYOUT-02, ELEMLAYOUT-03, ELEMLAYOUT-04, ELEMLAYOUT-05, ELEMDISPLAY-01, ELEMDISPLAY-02, ELEMDISPLAY-03, ELEMDISPLAY-04, ELEMDISPLAY-05]
requirements_addressed: [ELEMLAYOUT-01, ELEMLAYOUT-02, ELEMLAYOUT-03, ELEMLAYOUT-04, ELEMLAYOUT-05, ELEMDISPLAY-01, ELEMDISPLAY-02, ELEMDISPLAY-03, ELEMDISPLAY-04, ELEMDISPLAY-05]
user_setup: []
must_haves:
  truths:
    - "D-87-04: Runtime tags stay conservative; source semantics are preserved as attributes/accessibility metadata."
    - "D-87-05: Scroll-area remains runtime-compatible with existing scroll behavior."
    - "D-87-06: Tooltip behavior reuses existing title/tooltip ownership."
---

<objective>
Make Phase 87 source tags produce usable runtime metadata and focused behavior while preserving existing shipped layout and tooltip behavior.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Preserve source semantics through lowering</name>
  <files>crates/core/frontend/compiler/src/tags.rs, crates/core/frontend/compiler/src/render.rs</files>
  <read_first>crates/core/frontend/compiler/src/tags.rs, crates/core/frontend/compiler/src/render.rs, crates/core/ui/component/src/template.rs</read_first>
  <action>Before lowering source tags to runtime primitives, store source tag metadata such as `data-mesh-element`, role defaults, and display/progress metadata on `WidgetNode.attributes`. Use the source tag, not only the lowered runtime tag, for diagnostics and accessibility defaults. Preserve existing lowering for current shipped tags.</action>
  <verify>nix develop -c cargo test -p mesh-core-frontend phase87_layout_display</verify>
  <acceptance_criteria>
    - A `grid` source node lowers safely but retains `data-mesh-element=grid`.
    - Structure tags retain source semantics and accessibility roles.
    - `progress` lowers safely while retaining value/range metadata and `ProgressBar` accessibility.
    - Existing shipped tag lowering tests remain passing.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add focused runtime layout/display tests</name>
  <files>crates/core/ui/elements/src/layout.rs, crates/core/frontend/compiler/src/render.rs</files>
  <read_first>crates/core/ui/elements/src/layout.rs, crates/core/ui/elements/src/style/resolve.rs, crates/core/frontend/compiler/src/render.rs</read_first>
  <action>Add tests proving stack overlay behavior, spacer/divider sizing hooks, scroll-area lowering to scroll behavior, conservative grid metadata handling, and display primitive content/accessibility metadata. Do not add broad CSS grid support.</action>
  <verify>nix develop -c cargo test -p mesh-core-elements phase87_layout_runtime && nix develop -c cargo test -p mesh-core-frontend phase87_layout_display</verify>
  <acceptance_criteria>
    - Stack children overlap as expected.
    - Scroll-area participates in existing scroll offsets.
    - Display primitives carry accessible labels/roles.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Prove tooltip compatibility</name>
  <files>crates/core/shell/src/shell/component/tests/integration/real_surfaces.rs</files>
  <read_first>crates/core/shell/src/shell/component/tests/integration/real_surfaces.rs, crates/core/shell/src/shell/component/input/mod.rs</read_first>
  <action>Add or extend a focused test that proves semantic tooltip/title ownership still works for pointer ownership and keyboard/focus-accessible metadata. Reuse the existing tooltip lookup path.</action>
  <verify>nix develop -c cargo test -p mesh-core-shell tooltip</verify>
  <acceptance_criteria>
    - Existing shipped navigation tooltip test remains passing.
    - New semantic tooltip/progress metadata does not reset existing tooltip timers or ownership behavior.
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `nix develop -c cargo test -p mesh-core-frontend phase87_layout_display`
- `nix develop -c cargo test -p mesh-core-elements phase87_layout_runtime`
- `nix develop -c cargo test -p mesh-core-shell tooltip`
</verification>
