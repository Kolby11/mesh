---
phase: 73-shipped-manifest-i18n-proof
plan: 01
title: Shipped Manifest I18n Proof
type: execute
wave: 1
depends_on: []
files_modified:
  - modules/frontend/navigation-bar/module.json
  - crates/core/extension/module/src/package/tests.rs
  - crates/core/shell/src/shell/component/tests/interaction/navigation.rs
  - docs/module-system.md
autonomous: true
requirements: [MPROOF-01, MPROOF-02, MPROOF-03, MPROOF-04]
requirements_addressed: [MPROOF-01, MPROOF-02, MPROOF-03, MPROOF-04]
---

<objective>
Complete Phase 73 by migrating the shipped navigation manifest to explicit localized text objects, proving the real shipped graph/runtime path, and documenting the author contract.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Migrate shipped navigation manifest keybind metadata</name>
  <files>modules/frontend/navigation-bar/module.json</files>
  <action>Add `mesh.i18n` metadata and `mesh.contributes.i18n` catalog contributions for English and Slovak. Convert `mesh.keybinds.mute.label`, `description`, and `category` from raw dotted strings to `{ "t": "...", "fallback": "..." }` objects.</action>
  <verify>cargo test -p mesh-core-module shipped_navigation_manifest_uses_explicit_localized_keybind_text -- --nocapture</verify>
  <done>Navigation manifest no longer uses ambiguous raw dotted keybind strings.</done>
</task>

<task type="auto">
  <name>Task 2: Add shipped-fixture graph and runtime tests</name>
  <files>crates/core/extension/module/src/package/tests.rs, crates/core/shell/src/shell/component/tests/interaction/navigation.rs</files>
  <action>Add module package tests asserting the real navigation manifest parses localized keybind fields and the installed graph preserves their translation keys. Add a shell test that loads the real navigation component, switches locale to Slovak, recreates the runtime as the production locale path does, and asserts debug keybind metadata resolves the shipped translated label and source key.</action>
  <verify>cargo test -p mesh-core-module shipped_module_graph_preserves_navigation_localized_keybind_text -- --nocapture && cargo test -p mesh-core-shell navigation_shipped_keybind_metadata_resolves_from_i18n_catalogs -- --nocapture</verify>
  <done>Real shipped modules prove parser, graph, runtime, debug, and catalog behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Update author docs and verify phase</name>
  <files>docs/module-system.md, .planning/phases/73-shipped-manifest-i18n-proof/73-VALIDATION.md, .planning/phases/73-shipped-manifest-i18n-proof/73-01-SUMMARY.md, .planning/phases/73-shipped-manifest-i18n-proof/73-VERIFICATION.md</files>
  <action>Update the keybind documentation to explain raw strings as literals, field-local localized text objects, `mesh.i18n`, and `mesh.contributes.i18n`. Run focused tests, `cargo check -p mesh-core-shell`, `cargo fmt`, and `git diff --check`, then mark validation and state complete.</action>
  <verify>cargo check -p mesh-core-shell</verify>
  <done>Docs and verification artifacts complete Phase 73.</done>
</task>

</tasks>

<verification>
- `cargo test -p mesh-core-module shipped_navigation_manifest_uses_explicit_localized_keybind_text -- --nocapture`
- `cargo test -p mesh-core-module shipped_module_graph_preserves_navigation_localized_keybind_text -- --nocapture`
- `cargo test -p mesh-core-shell navigation_shipped_keybind_metadata_resolves_from_i18n_catalogs -- --nocapture`
- `cargo check -p mesh-core-shell`
- `cargo fmt`
- `git diff --check`
</verification>
