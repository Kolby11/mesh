---
phase: 51-painter-contract-and-backend-boundary
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/core/frontend/render/src/surface/painter/backend.rs
  - crates/core/frontend/render/src/surface/painter/tests.rs
autonomous: true
requirements: [PAINT-01, PAINT-02, BACKEND-01, BACKEND-02]
tags: [renderer, painter-api, backend-contract]
must_haves:
  truths:
    - "D-01: PainterCommand covers PushClip, PopClip, PushLayer, PopLayer, DrawRect, DrawRoundedRect, DrawPath, DrawText, DrawImage, DrawShadow, and ApplyFilter"
    - "D-02: Retained display-list and render-object data do not expose Skia-only types"
    - "D-04: PaintBackend exposes backend capabilities and command execution"
    - "D-06: Skia is the authoritative initial backend; Vello is a compatibility target only in this phase"
  artifacts:
    - path: "crates/core/frontend/render/src/surface/painter/backend.rs"
      provides: "Backend-neutral painter command model and backend trait"
      contains: "enum PainterCommand"
    - path: "crates/core/frontend/render/src/surface/painter/backend.rs"
      provides: "Backend capability contract"
      contains: "struct PainterBackendCapabilities"
---

<objective>
Define the backend-neutral painter command model and extend `PaintBackend` with capability reporting and command execution. This plan establishes the contract only; broad call-site migration belongs to later phases.

Decision coverage: D-01, D-02, D-04, D-06.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/51-painter-contract-and-backend-boundary/51-CONTEXT.md
@.planning/phases/51-painter-contract-and-backend-boundary/51-RESEARCH.md
@.planning/phases/51-painter-contract-and-backend-boundary/51-PATTERNS.md
@crates/core/frontend/render/src/surface/painter/backend.rs
@crates/core/frontend/render/src/surface/painter/tests.rs
@crates/core/frontend/render/src/display_list.rs
@crates/core/frontend/render/src/render_object.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add backend-neutral painter command/value types</name>
  <files>crates/core/frontend/render/src/surface/painter/backend.rs</files>
  <action>
    Add backend-neutral painter value types near the current `PaintBackend` trait:
    `PainterCommand`, `PainterClip`, `PainterLayer`, `PainterPaint`, `PainterPaintStyle`, `PainterStroke`, `PainterPath`, `PainterImage`, `PainterFilter`, `PainterBlendMode`, `PainterBackendCapabilities`, `UnsupportedPainterFeature`, and `PainterDiagnostic`.

    The command enum must include at least `PushClip`, `PopClip`, `PushLayer`, `PopLayer`, `DrawRect`, `DrawRoundedRect`, `DrawPath`, `DrawText`, `DrawImage`, `DrawShadow`, and `ApplyFilter`.

    Keep these types free of `skia_safe` imports. Use existing renderer/style value types such as `ClipRect`, `Color`, `BoxShadow`, and `VisualFilter` where they are already backend-neutral within this crate.
  </action>
  <verify>
    <automated>cargo check -p mesh-core-render</automated>
    <automated>rg "skia_safe" crates/core/frontend/render/src/display_list.rs crates/core/frontend/render/src/render_object.rs && exit 1 || exit 0</automated>
  </verify>
  <acceptance_criteria>
    - `PainterCommand` includes every command named in PAINT-01.
    - No `skia_safe` type appears in `PainterCommand` or retained display-list/render-object structures.
    - `cargo check -p mesh-core-render` exits 0.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Extend PaintBackend with capabilities and command execution</name>
  <files>crates/core/frontend/render/src/surface/painter/backend.rs</files>
  <action>
    Add `capabilities(&self) -> PainterBackendCapabilities` and `execute_commands(&self, buffer: &mut PixelBuffer, commands: &[PainterCommand], diagnostics: &mut Vec<PainterDiagnostic>)` to `PaintBackend`.

    Implement both for `SkiaPaintBackend`. In Phase 51, `execute_commands` may route only the currently supported helper-equivalent commands and emit diagnostics for unsupported or deferred commands. Do not remove existing helper methods yet unless the compatibility wrappers remain intact.
  </action>
  <verify>
    <automated>cargo check -p mesh-core-render</automated>
  </verify>
  <acceptance_criteria>
    - `SkiaPaintBackend::capabilities` reports Skia support for currently implemented primitives.
    - Unsupported commands produce `PainterDiagnostic` entries instead of silently succeeding.
    - Existing helper methods still compile for current call sites.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Add contract tests for command coverage and Skia isolation</name>
  <files>crates/core/frontend/render/src/surface/painter/tests.rs</files>
  <action>
    Add tests that prove:
    - every PAINT-01 command variant exists and can be constructed;
    - `SkiaPaintBackend::capabilities().backend_id` or equivalent identifies Skia;
    - unsupported/deferred command execution records a diagnostic;
    - retained display-list and render-object source files do not contain `skia_safe`.
  </action>
  <verify>
    <automated>cargo test -p mesh-core-render painter_command_contract -- --nocapture</automated>
    <automated>cargo test -p mesh-core-render painter_backend_capabilities -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - Contract tests fail if a required command variant is removed.
    - Capability tests fail if unsupported commands are silently ignored.
  </acceptance_criteria>
</task>

</tasks>
