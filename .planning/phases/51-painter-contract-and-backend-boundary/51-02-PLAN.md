---
phase: 51-painter-contract-and-backend-boundary
plan: 02
type: execute
wave: 2
depends_on: [51-01]
files_modified:
  - crates/core/frontend/render/src/surface/painter/backend.rs
  - crates/core/frontend/render/src/surface/painter.rs
  - crates/core/frontend/render/src/surface/painter/tests.rs
autonomous: true
requirements: [PAINT-01, PAINT-02, BACKEND-01]
tags: [renderer, compatibility, lowering]
must_haves:
  truths:
    - "D-03: Current helper APIs lower through painter commands before backend execution while preserving both direct widget-tree painting and retained display-list replay routes"
    - "D-05: Compatibility wrappers preserve a reversible backend migration route until the Skia path is hardened"
    - "D-09: Existing direct helper APIs have a migration path to painter commands"
---

<objective>
Convert the current helper-shaped painter API into compatibility wrappers over the new command execution path. This keeps existing render traversal stable while making the command backend boundary real.

Decision coverage: D-03, D-05, D-09.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/51-painter-contract-and-backend-boundary/51-CONTEXT.md
@.planning/phases/51-painter-contract-and-backend-boundary/51-RESEARCH.md
@.planning/phases/51-painter-contract-and-backend-boundary/51-PATTERNS.md
@crates/core/frontend/render/src/surface/painter/backend.rs
@crates/core/frontend/render/src/surface/painter.rs
@crates/core/frontend/render/src/surface/painter/tree.rs
@crates/core/frontend/render/src/surface/painter/widgets.rs
@crates/core/frontend/render/src/surface/painter/text.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lower helper methods through PainterCommand</name>
  <files>crates/core/frontend/render/src/surface/painter/backend.rs</files>
  <action>
    Update existing helper methods on `PaintBackend` or their default implementations so each helper builds a small `PainterCommand` sequence and calls `execute_commands`:
    - `fill_rect` -> `DrawRect`
    - `fill_rect_with_filter` -> `ApplyFilter` or filtered `DrawRect`
    - `fill_rounded_rect` -> `DrawRoundedRect`
    - `fill_rounded_rect_with_filter` -> `ApplyFilter` or filtered `DrawRoundedRect`
    - `stroke_rounded_rect` -> stroked `DrawRoundedRect`
    - `draw_box_shadow` -> `DrawShadow`
    - `apply_backdrop_filter` -> layer/filter command sequence

    Preserve return semantics for helpers that currently return `bool`.
  </action>
  <verify>
    <automated>cargo check -p mesh-core-render</automated>
  </verify>
  <acceptance_criteria>
    - Existing render call sites compile without broad traversal changes.
    - Helper methods are compatibility adapters, not independent primitive implementations.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Record painter diagnostics on FrontendRenderEngine</name>
  <files>crates/core/frontend/render/src/surface/painter.rs</files>
  <action>
    Add a narrow diagnostics collection path for painter backend execution. It can be internal to `FrontendRenderEngine` in this phase, but must be inspectable from tests or existing proof/debug plumbing.

    Diagnostics should include backend id, unsupported feature id, and a concise message. Do not make diagnostics part of retained display-list identity or material hashes.
  </action>
  <verify>
    <automated>cargo check -p mesh-core-render</automated>
  </verify>
  <acceptance_criteria>
    - Unsupported backend behavior is observable.
    - Diagnostics do not alter retained display-list ordering or damage computation.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Add lowering tests for compatibility wrappers</name>
  <files>crates/core/frontend/render/src/surface/painter/tests.rs</files>
  <action>
    Add tests that invoke representative compatibility helpers and assert they route through the command backend path. Prefer a small recording/test backend if the existing trait shape allows it; otherwise assert through diagnostics/capability evidence exposed by the Skia backend.
  </action>
  <verify>
    <automated>cargo test -p mesh-core-render painter_helper_lowering -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - Tests prove helper calls no longer bypass the command contract.
    - Existing painter tests still pass under Nix.
  </acceptance_criteria>
</task>

</tasks>
