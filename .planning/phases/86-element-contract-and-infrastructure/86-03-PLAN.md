---
phase: 86-element-contract-and-infrastructure
plan: 03
title: Attribute Diagnostics And Author Element Model Docs
type: execute
wave: 3
depends_on: [86-01, 86-02]
files_modified:
  - crates/core/ui/elements/src/element.rs
  - crates/core/frontend/compiler/src/render.rs
  - docs/frontend/elements.md
  - docs/frontend/mesh-syntax.md
autonomous: true
requirements: [ELEMCORE-05, ELEMCORE-06]
requirements_addressed: [ELEMCORE-05, ELEMCORE-06]
user_setup: []
must_haves:
  truths:
    - "D-09: Implement generic unsupported/invalid attribute diagnostics with concrete author actions."
    - "D-10: Author docs should provide taxonomy, common attributes, state, events, diagnostics, accessibility expectations, and explicit non-compatibility with HTML/Qt/Flutter parity."
    - "D-11: Later phases add behavior against the taxonomy instead of redefining element names, state, or event semantics."
    - "D-12: Defer family-specific rendering, keyboard behavior, popups, layout algorithms, collection interaction, and shipped surface migration to Phases 87-91."
  artifacts:
    - path: "crates/core/ui/elements/src/element.rs"
      provides: "Generic attribute/event validation diagnostics"
      contains: "ElementDiagnostic"
    - path: "docs/frontend/elements.md"
      provides: "Author-facing MESH-native element model"
      contains: "MESH-native, not HTML-compatible"
  key_links:
    - "Element metadata -> diagnostics with author actions"
    - "Element docs -> roadmap requirements and later phase source of truth"
---

<objective>
Add generic metadata-backed unsupported/invalid attribute diagnostics and document the MESH-native element model for authors, including taxonomy, common state/events, accessibility expectations, and explicit non-parity with HTML/Qt/Flutter.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/86-element-contract-and-infrastructure/86-CONTEXT.md
@.planning/phases/86-element-contract-and-infrastructure/86-RESEARCH.md
@.planning/phases/86-element-contract-and-infrastructure/86-VALIDATION.md
@.planning/phases/86-element-contract-and-infrastructure/86-PATTERNS.md
@crates/core/ui/elements/src/element.rs
@crates/core/frontend/compiler/src/render.rs
@docs/frontend/mesh-syntax.md
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| T-86-03-01: Unsupported attributes are silently accepted, leaving authors without concrete action | high | Add metadata-backed diagnostics that include tag, attribute/event, reason, and action text. |
| T-86-03-02: Docs imply browser, Qt, or Flutter compatibility beyond MESH-native behavior | medium | Add explicit non-parity wording and out-of-scope examples in `docs/frontend/elements.md`. |
| T-86-03-03: Later phases drift from Phase 86 contract because docs and metadata disagree | medium | Cross-link syntax docs to the element model and verify docs list the same families as metadata. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add metadata-backed element diagnostics</name>
  <files>crates/core/ui/elements/src/element.rs</files>
  <read_first>crates/core/ui/elements/src/element.rs, crates/core/ui/elements/src/style.rs, crates/core/ui/elements/src/layout.rs, .planning/phases/86-element-contract-and-infrastructure/86-CONTEXT.md</read_first>
  <action>Add an additive `ElementDiagnostic` struct with `tag`, `name`, `kind`, `message`, and `action` fields plus an `ElementDiagnosticKind` enum for `UnsupportedAttribute`, `UnsupportedEvent`, and `InvalidAttributeValue`. Add helper functions such as `validate_element_attribute(tag, name, value)` and `validate_element_event(tag, event_name)` that use the element contract metadata and return zero or one diagnostics. Diagnostics for unsupported known-tag attributes must include an action like `Remove the attribute or use one of: ...`. Add tests named `unsupported_element_attribute_reports_author_action`, `unsupported_element_event_reports_author_action`, and `known_common_attribute_does_not_report_diagnostic`.</action>
  <verify>cargo test -p mesh-core-elements element_diagnostic</verify>
  <acceptance_criteria>
    - `crates/core/ui/elements/src/element.rs` contains `pub struct ElementDiagnostic`.
    - `crates/core/ui/elements/src/element.rs` contains `UnsupportedAttribute`.
    - `crates/core/ui/elements/src/element.rs` contains `Remove the attribute or use one of`.
    - `cargo test -p mesh-core-elements element_diagnostic` exits 0.
  </acceptance_criteria>
  <done>Known element attributes/events have generic diagnostics with concrete author actions.</done>
</task>

<task type="auto">
  <name>Task 2: Surface generic diagnostics during frontend compilation without breaking shipped modules</name>
  <files>crates/core/frontend/compiler/src/render.rs</files>
  <read_first>crates/core/frontend/compiler/src/render.rs, crates/core/ui/elements/src/element.rs, crates/core/frontend/compiler/src/compile.rs, .planning/phases/86-element-contract-and-infrastructure/86-RESEARCH.md</read_first>
  <action>Thread metadata-backed validation through the frontend compiler path at the point where element attributes and event handlers are parsed. If the current compiler API cannot carry non-fatal diagnostics to shell component diagnostics without a larger API change, add a narrow helper and tests that collect diagnostics from an `ElementNode` without changing runtime rendering behavior. Preserve shipped module compatibility by not rejecting existing unknown pass-through attributes unless the metadata says they are invalid for a known element. Add tests named `frontend_element_diagnostics_collect_unsupported_attribute` and `frontend_element_diagnostics_preserve_shipped_pass_through_attributes`.</action>
  <verify>cargo test -p mesh-core-frontend frontend_element_diagnostics</verify>
  <acceptance_criteria>
    - `crates/core/frontend/compiler/src/render.rs` contains `frontend_element_diagnostics_collect_unsupported_attribute`.
    - `crates/core/frontend/compiler/src/render.rs` contains `frontend_element_diagnostics_preserve_shipped_pass_through_attributes`.
    - Existing `parse_attributes` still returns event handlers and attributes for shipped modules.
    - `cargo test -p mesh-core-frontend frontend_element_diagnostics` exits 0.
  </acceptance_criteria>
  <done>Frontend compilation has a generic diagnostic collection path without destabilizing shipped surfaces.</done>
</task>

<task type="auto">
  <name>Task 3: Document the MESH-native element model</name>
  <files>docs/frontend/elements.md, docs/frontend/mesh-syntax.md</files>
  <read_first>docs/frontend/mesh-syntax.md, docs/frontend/renderer-contract.md, .planning/REQUIREMENTS.md, .planning/phases/86-element-contract-and-infrastructure/86-CONTEXT.md, .planning/phases/86-element-contract-and-infrastructure/86-RESEARCH.md</read_first>
  <action>Create `docs/frontend/elements.md` with sections titled exactly `## Native Element Model`, `## Element Families`, `## Common Attributes`, `## Shared State`, `## Events`, `## Style Hooks`, `## Accessibility`, `## Diagnostics`, and `## Relationship To HTML Qt And Flutter`. The relationship section must state that MESH is shell-native and uses HTML, Qt Widgets/layouts, and Flutter only as coverage references, not compatibility targets. Include the Phase 86 family list and representative tags. Update `docs/frontend/mesh-syntax.md` to link to `docs/frontend/elements.md` using the text `Native element model`.</action>
  <verify>test -f docs/frontend/elements.md && grep -q "## Native Element Model" docs/frontend/elements.md && grep -q "## Relationship To HTML Qt And Flutter" docs/frontend/elements.md && grep -q "Native element model" docs/frontend/mesh-syntax.md</verify>
  <acceptance_criteria>
    - `docs/frontend/elements.md` contains `## Native Element Model`.
    - `docs/frontend/elements.md` contains `## Element Families`.
    - `docs/frontend/elements.md` contains `HTML`.
    - `docs/frontend/elements.md` contains `Qt Widgets`.
    - `docs/frontend/elements.md` contains `Flutter`.
    - `docs/frontend/mesh-syntax.md` contains `Native element model`.
  </acceptance_criteria>
  <done>Author docs explain ELEMCORE-06 and link the element model from frontend syntax documentation.</done>
</task>

</tasks>

<verification>
- Run `cargo test -p mesh-core-elements element_diagnostic`.
- Run `cargo test -p mesh-core-frontend frontend_element_diagnostics`.
- Run `test -f docs/frontend/elements.md && grep -q "## Relationship To HTML Qt And Flutter" docs/frontend/elements.md`.
</verification>

<success_criteria>
- Unsupported/invalid element attributes produce actionable diagnostics.
- Author docs explain the MESH-native element model and non-parity boundary.
- Later phases have a stable contract source of truth for element family behavior.
</success_criteria>
