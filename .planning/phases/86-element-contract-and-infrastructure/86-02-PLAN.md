---
phase: 86-element-contract-and-infrastructure
plan: 02
title: Parser Compiler And Value Event Plumbing
type: execute
wave: 2
depends_on: [86-01]
files_modified:
  - crates/core/ui/component/src/template.rs
  - crates/core/ui/component/src/parser/markup.rs
  - crates/core/frontend/compiler/src/tags.rs
  - crates/core/frontend/compiler/src/render.rs
autonomous: true
requirements: [ELEMCORE-02, ELEMCORE-04]
requirements_addressed: [ELEMCORE-02, ELEMCORE-04]
user_setup: []
must_haves:
  truths:
    - "D-05: Put canonical runtime element metadata in mesh-core-elements, with parser/compiler consuming it rather than duplicating tag knowledge."
    - "D-06: Preserve current shipped tags and map them into the new taxonomy without changing existing module behavior in Phase 86."
    - "D-07: Route typed value/change events through shared Luau handler plumbing using element metadata for event names and payload shape."
    - "D-08: Define stable classes and pseudo-state hooks from shared state metadata while keeping Phase 86 behavior generic."
  artifacts:
    - path: "crates/core/ui/component/src/template.rs"
      provides: "Source tag classification aligned to element metadata"
      contains: "SourceTag::from_tag_name"
    - path: "crates/core/frontend/compiler/src/tags.rs"
      provides: "Safe lowering from planned source elements to existing runtime primitives"
      contains: "lower_source_tag"
    - path: "crates/core/frontend/compiler/src/render.rs"
      provides: "Shared value/change handler extraction"
      contains: "normalize_event_handler_name"
  key_links:
    - "Element contract metadata -> parser source classification"
    - "SourceTag -> UiTag lowering -> WidgetNode runtime attributes and event handlers"
---

<objective>
Make parser and compiler paths consume or mirror the Phase 86 element contract while preserving shipped behavior, then add shared value/change event plumbing for Luau handlers.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/86-element-contract-and-infrastructure/86-CONTEXT.md
@.planning/phases/86-element-contract-and-infrastructure/86-RESEARCH.md
@.planning/phases/86-element-contract-and-infrastructure/86-VALIDATION.md
@.planning/phases/86-element-contract-and-infrastructure/86-PATTERNS.md
@crates/core/ui/component/src/template.rs
@crates/core/ui/component/src/parser/markup.rs
@crates/core/frontend/compiler/src/tags.rs
@crates/core/frontend/compiler/src/render.rs
@crates/core/ui/elements/src/element.rs
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| T-86-02-01: Parser and compiler keep a divergent tag vocabulary after metadata is added | high | Add tests that representative planned tags classify and lower through the same contract-backed list. |
| T-86-02-02: New tags break existing shipped modules by changing current lowering for `row`, `box`, `button`, `text`, `icon`, `input`, `switch`, or `checkbox` | high | Preserve current lowering results and add regression assertions for existing tags. |
| T-86-02-03: Value/change events require bespoke per-control paths | medium | Add metadata-backed event names and generic handler extraction for `oninput`, `onchange`, and activation events. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Align source tag classification with the element contract</name>
  <files>crates/core/ui/component/src/template.rs, crates/core/ui/component/src/parser/markup.rs</files>
  <read_first>crates/core/ui/component/src/template.rs, crates/core/ui/component/src/parser/markup.rs, crates/core/ui/elements/src/element.rs, .planning/phases/86-element-contract-and-infrastructure/86-CONTEXT.md</read_first>
  <action>Update source tag classification so planned native tags from the Phase 86 contract are recognized as lowercase MESH primitives while PascalCase custom component handling stays unchanged. If direct dependency from `mesh-core-component` to `mesh-core-elements` would create an unwanted crate dependency, keep `SourceTag` as the local adapter but add tests and comments that identify `mesh-core-elements` as the canonical vocabulary. Extend reserved PascalCase primitive correction messages for newly recognized multi-word tags such as `SegmentedControl`, `RadioGroup`, `CommandButton`, `ScrollArea`, and `EmptyState`. Add tests named `planned_native_tags_parse_as_elements` and `reserved_pascal_primitives_report_lowercase_element_names`.</action>
  <verify>cargo test -p mesh-core-component planned_native_tags</verify>
  <acceptance_criteria>
    - `crates/core/ui/component/src/template.rs` contains `SegmentedControl`.
    - `crates/core/ui/component/src/parser/markup.rs` contains `planned_native_tags_parse_as_elements`.
    - `crates/core/ui/component/src/parser/markup.rs` contains `reserved_pascal_primitives_report_lowercase_element_names`.
    - `cargo test -p mesh-core-component planned_native_tags` exits 0.
  </acceptance_criteria>
  <done>Parser AST can represent every planned native element without treating lowercase tags as custom components.</done>
</task>

<task type="auto">
  <name>Task 2: Lower planned elements to safe runtime primitives</name>
  <files>crates/core/frontend/compiler/src/tags.rs, crates/core/frontend/compiler/src/render.rs</files>
  <read_first>crates/core/frontend/compiler/src/tags.rs, crates/core/frontend/compiler/src/render.rs, crates/core/ui/component/src/template.rs, crates/core/ui/elements/src/element.rs, .planning/phases/86-element-contract-and-infrastructure/86-RESEARCH.md</read_first>
  <action>Extend `UiTag` or `lower_source_tag` so every planned native source tag lowers to a safe existing primitive without changing current behavior for shipped tags. Use `box` for structural/container tags without behavior yet, `row`/`column` for layout-compatible aliases, `text` for display text-like tags, `button` for action controls, `input` for text/numeric controls, and current toggle/list primitives where appropriate. Preserve `UiTag::as_str()` results for existing tags. Add tests named `planned_element_tags_lower_to_safe_runtime_primitives` and `existing_shipped_tags_keep_current_lowering`.</action>
  <verify>cargo test -p mesh-core-frontend planned_element_tags</verify>
  <acceptance_criteria>
    - `crates/core/frontend/compiler/src/tags.rs` contains `planned_element_tags_lower_to_safe_runtime_primitives`.
    - `crates/core/frontend/compiler/src/tags.rs` contains `existing_shipped_tags_keep_current_lowering`.
    - `crates/core/frontend/compiler/src/tags.rs` maps `SourceTag::Select` or equivalent to a safe runtime primitive.
    - `cargo test -p mesh-core-frontend planned_element_tags` exits 0.
  </acceptance_criteria>
  <done>Compiler lowering can represent the full planned taxonomy while preserving current runtime behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Add shared value and change handler metadata plumbing</name>
  <files>crates/core/frontend/compiler/src/render.rs, crates/core/ui/elements/src/element.rs</files>
  <read_first>crates/core/frontend/compiler/src/render.rs, crates/core/ui/elements/src/element.rs, crates/core/ui/elements/src/events.rs, .planning/phases/86-element-contract-and-infrastructure/86-CONTEXT.md</read_first>
  <action>Add generic metadata and compiler support for shared value/change style events. Ensure `oninput`, `onchange`, `onselect`, `onactivate`, and `onopenchange` normalize to handler names `input`, `change`, `select`, `activate`, and `openchange` without per-control bespoke branches. Ensure two-way `bind:value` continues to resolve through the existing attribute path. Add tests named `shared_value_change_handlers_are_normalized` and `two_way_value_binding_still_resolves_attribute_value`.</action>
  <verify>cargo test -p mesh-core-frontend shared_value_change_handlers</verify>
  <acceptance_criteria>
    - `crates/core/frontend/compiler/src/render.rs` contains `shared_value_change_handlers_are_normalized`.
    - `crates/core/frontend/compiler/src/render.rs` contains `two_way_value_binding_still_resolves_attribute_value`.
    - `crates/core/frontend/compiler/src/render.rs` accepts `oninput`.
    - `crates/core/frontend/compiler/src/render.rs` accepts `onopenchange`.
    - `cargo test -p mesh-core-frontend shared_value_change_handlers` exits 0.
  </acceptance_criteria>
  <done>Shared Luau handler plumbing covers value/change events for later control families.</done>
</task>

</tasks>

<verification>
- Run `cargo test -p mesh-core-component planned_native_tags`.
- Run `cargo test -p mesh-core-frontend planned_element_tags`.
- Run `cargo test -p mesh-core-frontend shared_value_change_handlers`.
</verification>

<success_criteria>
- Parser/AST/runtime metadata can represent every planned native element.
- Existing shipped source tags retain current behavior.
- Shared value/change event handling exists without per-control event branches.
</success_criteria>
