---
phase: 86-element-contract-and-infrastructure
plan: 01
title: Element Taxonomy And Shared Contract Metadata
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/core/ui/elements/src/element.rs
  - crates/core/ui/elements/src/tree.rs
  - crates/core/ui/elements/src/accessibility.rs
autonomous: true
requirements: [ELEMCORE-01, ELEMCORE-03]
requirements_addressed: [ELEMCORE-01, ELEMCORE-03]
user_setup: []
must_haves:
  truths:
    - "D-01: Use a registry-backed ElementKind taxonomy with family metadata, common attribute metadata, state/event capabilities, accessibility defaults, and diagnostics hooks."
    - "D-02: Include all planned families in Phase 86 metadata: layout, display, action, text/numeric input, choice/menu, container, collection, and shell-specific controls."
    - "D-03: Represent shell-specific controls as first-class MESH element family entries."
    - "D-04: Shared control state covers disabled, read-only, required, focused, selected, checked, expanded, pressed, invalid, active, and value."
  artifacts:
    - path: "crates/core/ui/elements/src/element.rs"
      provides: "Canonical element taxonomy, attributes, events, accessibility defaults, and compatibility references"
      contains: "pub enum ElementFamily"
    - path: "crates/core/ui/elements/src/tree.rs"
      provides: "Shared runtime state fields carried by WidgetNode"
      contains: "pub read_only: bool"
  key_links:
    - "ElementKind taxonomy -> SourceTag/compiler compatibility adapters"
    - "Element metadata -> author docs and diagnostics"
---

<objective>
Create the canonical Phase 86 element contract in `mesh-core-elements`: all planned families, common attributes, shared state flags, event metadata, accessibility defaults, and compatibility references.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/86-element-contract-and-infrastructure/86-CONTEXT.md
@.planning/phases/86-element-contract-and-infrastructure/86-RESEARCH.md
@.planning/phases/86-element-contract-and-infrastructure/86-VALIDATION.md
@.planning/phases/86-element-contract-and-infrastructure/86-PATTERNS.md
@crates/core/ui/elements/src/element.rs
@crates/core/ui/elements/src/tree.rs
@crates/core/ui/elements/src/accessibility.rs
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| T-86-01-01: The taxonomy omits a v1.16 element family, causing later phases to redefine names and semantics | high | Add metadata coverage tests that assert every ELEMCORE family and planned tag list is present. |
| T-86-01-02: Shared state remains narrower than the requirement, forcing per-control workaround fields later | high | Extend `ElementState` and metadata state flags to include the complete required state list. |
| T-86-01-03: HTML/Qt/Flutter references are encoded as compatibility promises | medium | Store compatibility references as inspiration/source labels only, with docs and tests using non-parity wording. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add element family, attribute, state, event, and compatibility metadata types</name>
  <files>crates/core/ui/elements/src/element.rs</files>
  <read_first>crates/core/ui/elements/src/element.rs, crates/core/ui/elements/src/accessibility.rs, .planning/phases/86-element-contract-and-infrastructure/86-CONTEXT.md, .planning/phases/86-element-contract-and-infrastructure/86-RESEARCH.md</read_first>
  <action>In `crates/core/ui/elements/src/element.rs`, add typed contract structs and enums near the existing `ElementKind` definitions: `ElementFamily`, `ElementAttributeType`, `ElementAttributeDef`, `ElementStateFlag`, `ElementEventDef`, `ElementAccessibilityDef`, `ElementCompatibilityRef`, and an additive richer `ElementContractDef`. Include fields for tag, kind, family, type name, attributes, states, events, accessibility role/focusability, style hooks, and compatibility references. Keep existing `ElementFieldDef`, `ElementTypeDef`, `ELEMENT_TYPE_DEFS`, `element_type_for_tag`, and snapshot APIs source-compatible.</action>
  <verify>cargo test -p mesh-core-elements element_contract_metadata_types</verify>
  <acceptance_criteria>
    - `crates/core/ui/elements/src/element.rs` contains `pub enum ElementFamily`.
    - `crates/core/ui/elements/src/element.rs` contains `pub enum ElementStateFlag`.
    - `crates/core/ui/elements/src/element.rs` contains `pub struct ElementEventDef`.
    - `crates/core/ui/elements/src/element.rs` contains `pub struct ElementContractDef`.
    - `cargo test -p mesh-core-elements element_contract_metadata_types` exits 0.
  </acceptance_criteria>
  <done>`mesh-core-elements` has additive typed metadata surfaces for the Phase 86 element contract.</done>
</task>

<task type="auto">
  <name>Task 2: Register the full Phase 86 element taxonomy</name>
  <files>crates/core/ui/elements/src/element.rs</files>
  <read_first>crates/core/ui/elements/src/element.rs, .planning/REQUIREMENTS.md, .planning/phases/86-element-contract-and-infrastructure/86-CONTEXT.md</read_first>
  <action>Extend `ElementKind` and `ELEMENT_TYPE_DEFS` or add parallel `ELEMENT_CONTRACT_DEFS` so the metadata includes these planned source tags exactly: `box`, `row`, `column`, `grid`, `stack`, `spacer`, `divider`, `scroll-area`, `section`, `header`, `footer`, `group`, `form-row`, `text`, `icon`, `image`, `badge`, `progress`, `meter`, `tooltip`, `avatar`, `shortcut`, `button`, `icon-button`, `toggle-button`, `command-button`, `link-button`, `input`, `textarea`, `search`, `password`, `number-input`, `stepper`, `select`, `option`, `checkbox`, `switch`, `radio`, `radio-group`, `segmented-control`, `menu`, `menu-item`, `command-item`, `separator`, `preference-row`, `panel`, `popover`, `dialog`, `sheet`, `tabs`, `tab`, `accordion`, `details`, `list`, `list-item`, `table`, `cell`, `tree`, `empty-state`, `slot`, `surface`, and `widget`. Preserve existing aliases `scroll`, `scroll-view`, `label`, `slider`, `text-input`, `password-input`, `search-input`, `email-input`, and `url-input` as compatibility entries or aliases. Add a test named `element_contract_covers_v1_16_taxonomy` that asserts the required families and representative tags are present.</action>
  <verify>cargo test -p mesh-core-elements element_contract_covers_v1_16_taxonomy</verify>
  <acceptance_criteria>
    - `crates/core/ui/elements/src/element.rs` contains `grid`.
    - `crates/core/ui/elements/src/element.rs` contains `segmented-control`.
    - `crates/core/ui/elements/src/element.rs` contains `empty-state`.
    - `crates/core/ui/elements/src/element.rs` contains `element_contract_covers_v1_16_taxonomy`.
    - `cargo test -p mesh-core-elements element_contract_covers_v1_16_taxonomy` exits 0.
  </acceptance_criteria>
  <done>The canonical metadata covers all Phase 86 element families and planned tags.</done>
</task>

<task type="auto">
  <name>Task 3: Extend shared runtime state and metadata defaults</name>
  <files>crates/core/ui/elements/src/tree.rs, crates/core/ui/elements/src/element.rs</files>
  <read_first>crates/core/ui/elements/src/tree.rs, crates/core/ui/elements/src/element.rs, crates/core/ui/elements/src/accessibility.rs, .planning/phases/86-element-contract-and-infrastructure/86-CONTEXT.md</read_first>
  <action>Extend `ElementState` in `tree.rs` with the required shared fields `read_only`, `required`, `selected`, `expanded`, `pressed`, `invalid`, and `value` while preserving existing `hovered`, `active`, `focused`, `focus_visible`, `disabled`, and `checked` behavior. Keep `ElementState` copyable so existing style and runtime fingerprint paths remain compatible; actual value payloads stay in attributes and event payloads. Update `ElementStateSnapshot` in `element.rs` to serialize the expanded state. Add metadata helpers that expose common state flags for applicable contracts without forcing every element to use every state. Add tests named `element_state_snapshot_exposes_shared_control_state` and `element_contract_common_state_flags_cover_required_set`.</action>
  <verify>cargo test -p mesh-core-elements element_contract</verify>
  <acceptance_criteria>
    - `crates/core/ui/elements/src/tree.rs` contains `pub read_only: bool`.
    - `crates/core/ui/elements/src/tree.rs` contains `pub value: bool`.
    - `crates/core/ui/elements/src/element.rs` contains `element_state_snapshot_exposes_shared_control_state`.
    - `crates/core/ui/elements/src/element.rs` contains `element_contract_common_state_flags_cover_required_set`.
    - `cargo test -p mesh-core-elements element_contract` exits 0.
  </acceptance_criteria>
  <done>Runtime and metadata state cover ELEMCORE-03 without per-control state workarounds.</done>
</task>

</tasks>

<verification>
- Run `cargo test -p mesh-core-elements element_contract`.
- Run `cargo test -p mesh-core-elements element_contract`.
</verification>

<success_criteria>
- `mesh-core-elements` exposes the full v1.16 native element taxonomy.
- Shared state metadata covers every ELEMCORE-03 state.
- Existing element snapshot and type-definition APIs remain compatible.
</success_criteria>
