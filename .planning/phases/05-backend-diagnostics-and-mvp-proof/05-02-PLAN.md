---
phase: 05-backend-diagnostics-and-mvp-proof
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - crates/core/foundation/diagnostics/src/lib.rs
  - crates/core/shell/src/shell/mod.rs
  - crates/core/shell/src/shell/types.rs
autonomous: true
requirements: [BDIAG-02, BDIAG-03, BDIAG-04]
requirements_addressed: [BDIAG-02, BDIAG-03, BDIAG-04]
user_setup: []
must_haves:
  truths:
    - "D-03: when an active provider fails, public interface state must clear immediately to unavailable or error."
    - "D-04: honest runtime failure state is preferred over stale continuity."
    - "D-05: repeated backend failures deduplicate by provider plus lifecycle stage."
    - "D-06: repeated failures update count and timestamp instead of creating a new diagnostic per poll cycle."
    - "BDIAG-04: backend logs and diagnostics must include provider identity and lifecycle context."
  artifacts:
    - path: "crates/core/foundation/diagnostics/src/lib.rs"
      provides: "Provider-plus-stage lifecycle diagnostic aggregation with count/timestamp metadata"
      contains: "record_lifecycle_error"
    - path: "crates/core/shell/src/shell/mod.rs"
      provides: "Active-provider stale-state clearing and runtime-status bridge"
      contains: "handle_backend_lifecycle"
---

<objective>
Clear stale public service state when the active provider fails, and aggregate repeated backend failures as provider-plus-stage diagnostics with identity, count, and timestamp context.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-backend-diagnostics-and-mvp-proof/05-CONTEXT.md
@.planning/phases/05-backend-diagnostics-and-mvp-proof/05-RESEARCH.md
@.planning/phases/05-backend-diagnostics-and-mvp-proof/05-VALIDATION.md
@.planning/phases/05-backend-diagnostics-and-mvp-proof/05-PATTERNS.md

@crates/core/foundation/diagnostics/src/lib.rs
@crates/core/shell/src/shell/mod.rs
@crates/core/shell/src/shell/types.rs
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| T-05-04: the shell keeps stale last-known-good state after init/poll/command failure | high | Synthesize unavailable/error latest state for the active provider and assert it replaces the old payload immediately. |
| T-05-05: repeated backend failures create a new diagnostic entry every poll cycle | high | Replace exact-message `HashSet` dedup with provider-plus-stage buckets that update count and last-seen. |
| T-05-06: diagnostics lose provider identity or failure-stage detail while deduplicating | medium | Keep provider id, stage, latest message, count, and last-seen in the lifecycle record and expose them through tests/debug paths. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Bucket lifecycle diagnostics by provider and stage</name>
  <files>crates/core/foundation/diagnostics/src/lib.rs</files>
  <read_first>crates/core/foundation/diagnostics/src/lib.rs, .planning/phases/05-backend-diagnostics-and-mvp-proof/05-RESEARCH.md, .planning/phases/05-backend-diagnostics-and-mvp-proof/05-PATTERNS.md</read_first>
  <action>Replace the lifecycle-error `HashSet<(provider_id, stage, message)>` design with a keyed record map indexed by `(provider_id, stage)`. Each record must store the latest message string, an occurrence count, and a last-seen timestamp. Preserve the existing plugin health behavior so the first unique provider-plus-stage failure increments the unique error count and sets error health, while repeats only update metadata. Add tests named `lifecycle_errors_are_deduplicated_by_provider_and_stage`, `repeated_lifecycle_failures_increment_count_without_new_error`, and `lifecycle_error_record_keeps_latest_message`.</action>
  <verify>nix develop -c cargo test -p mesh-core-diagnostics lifecycle</verify>
  <acceptance_criteria>
    - `crates/core/foundation/diagnostics/src/lib.rs` contains test `lifecycle_errors_are_deduplicated_by_provider_and_stage`.
    - `crates/core/foundation/diagnostics/src/lib.rs` contains an occurrence counter field such as `count`.
    - `crates/core/foundation/diagnostics/src/lib.rs` contains a timestamp field such as `last_seen`.
    - `nix develop -c cargo test -p mesh-core-diagnostics lifecycle` exits 0.
  </acceptance_criteria>
  <done>Repeated backend failures collapse into stable provider-plus-stage diagnostic buckets.</done>
</task>

<task type="auto">
  <name>Task 2: Clear active-provider public state on failure</name>
  <files>crates/core/shell/src/shell/mod.rs, crates/core/shell/src/shell/types.rs</files>
  <read_first>crates/core/shell/src/shell/mod.rs, crates/core/shell/src/shell/types.rs, .planning/phases/05-backend-diagnostics-and-mvp-proof/05-CONTEXT.md, .planning/phases/04-service-provider-contract/04-CONTEXT.md</read_first>
  <action>Update `handle_backend_lifecycle()` and any helper it calls so when the current provider for an interface enters `init_failed`, terminal `failed`, or terminal `stopped` after an unrecoverable failure, the shell replaces `latest_service_state[interface]` with an unavailable/error payload instead of leaving the last successful state visible. Use the canonical interface key, preserve provider metadata, and keep the logic generic. Where a contract or existing payload exposes `available`, set it to `false`; otherwise keep the payload JSON-compatible without inventing service-specific fields. Add tests named `active_provider_failure_clears_latest_service_state` and `stale_provider_failure_does_not_clear_new_provider_state`.</action>
  <verify>nix develop -c cargo test -p mesh-core-shell backend_lifecycle</verify>
  <acceptance_criteria>
    - `crates/core/shell/src/shell/mod.rs` contains test `active_provider_failure_clears_latest_service_state`.
    - `crates/core/shell/src/shell/mod.rs` contains test `stale_provider_failure_does_not_clear_new_provider_state`.
    - `crates/core/shell/src/shell/mod.rs` writes through `latest_service_state` during lifecycle handling, not only during `ServiceEvent::Updated`.
    - `grep -n "mesh.audio\\|mesh.network\\|mesh.power\\|mesh.media" crates/core/shell/src/shell/mod.rs` shows only generic interface data or tests, not service-specific failure branches.
    - `nix develop -c cargo test -p mesh-core-shell backend_lifecycle` exits 0.
  </acceptance_criteria>
  <done>Consumers stop seeing stale provider state as soon as the active provider is known failing.</done>
</task>

<task type="auto">
  <name>Task 3: Expose deduped failure context through shell runtime status paths</name>
  <files>crates/core/shell/src/shell/mod.rs</files>
  <read_first>crates/core/shell/src/shell/mod.rs, crates/core/foundation/diagnostics/src/lib.rs, .planning/phases/05-backend-diagnostics-and-mvp-proof/05-RESEARCH.md</read_first>
  <action>Extend the shell's existing runtime-status/debug path so repeated backend failure context is observable in tests and diagnostics reviews. Reuse `backend_runtime_statuses` and `build_debug_snapshot()` rather than creating a second shell-only store. Ensure the shell records provider id, interface, lifecycle stage/status, and the rolled-up count/timestamp data from diagnostics or equivalent status metadata. Add tests named `backend_lifecycle_debug_snapshot_includes_failure_counts` and `backend_runtime_status_records_provider_identity_for_failures`.</action>
  <verify>nix develop -c cargo test -p mesh-core-shell backend_lifecycle_debug</verify>
  <acceptance_criteria>
    - `crates/core/shell/src/shell/mod.rs` contains test `backend_lifecycle_debug_snapshot_includes_failure_counts`.
    - `crates/core/shell/src/shell/mod.rs` contains test `backend_runtime_status_records_provider_identity_for_failures`.
    - Debug/runtime status output includes provider id and lifecycle status text.
    - `nix develop -c cargo test -p mesh-core-shell backend_lifecycle_debug` exits 0.
  </acceptance_criteria>
  <done>Backend diagnostics stay deduplicated while remaining visible in shell runtime status and debug evidence.</done>
</task>

</tasks>

<verification>
- Run `nix develop -c cargo test -p mesh-core-diagnostics lifecycle`.
- Run `nix develop -c cargo test -p mesh-core-shell backend_lifecycle`.
- Run `nix develop -c cargo test -p mesh-core-shell backend_lifecycle_debug`.
- Read `crates/core/shell/src/shell/mod.rs` and confirm active-provider failures replace stale `latest_service_state` instead of preserving the old payload.
</verification>

<success_criteria>
- Active-provider init/poll/command failure paths replace stale public state with unavailable/error state immediately.
- Repeated failures deduplicate by provider plus stage and update count/timestamp instead of creating spam.
- Diagnostics and debug output retain provider identity and lifecycle context.
</success_criteria>

<output>
After completion, create `.planning/phases/05-backend-diagnostics-and-mvp-proof/05-02-SUMMARY.md`
</output>

