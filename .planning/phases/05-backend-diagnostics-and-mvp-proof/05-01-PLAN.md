---
phase: 05-backend-diagnostics-and-mvp-proof
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/core/runtime/scripting/src/backend.rs
  - crates/core/runtime/backend/src/lib.rs
autonomous: true
requirements: [BDIAG-01, BDIAG-02]
requirements_addressed: [BDIAG-01, BDIAG-02]
user_setup: []
must_haves:
  truths:
    - "D-03: once a provider is known failing, stale last-known-good public state must not remain authoritative."
    - "D-04: failure visibility favors honest runtime state over stale continuity."
    - "BDIAG-01: invalid manifests, missing entrypoints, missing contracts, init failures, poll failures, emit failures, and command failures must produce clear diagnostics."
    - "BDIAG-02: backend Luau init, poll, emit, and command failures degrade plugin health without crashing the shell."
  artifacts:
    - path: "crates/core/runtime/scripting/src/backend.rs"
      provides: "Backend state snapshot and command result conversion with explicit failure-stage boundaries"
      contains: "BackendScriptContext"
    - path: "crates/core/runtime/backend/src/lib.rs"
      provides: "Async backend lifecycle events for load/init/poll/command/state failure paths"
      contains: "BackendServiceEvent"
---

<objective>
Make backend runtime failures explicit at the correct lifecycle stage so Phase 5 can clear stale state, degrade health, and diagnose load/init/poll/emit/command failures without introducing service-specific Rust logic.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-backend-diagnostics-and-mvp-proof/05-CONTEXT.md
@.planning/phases/05-backend-diagnostics-and-mvp-proof/05-RESEARCH.md
@.planning/phases/05-backend-diagnostics-and-mvp-proof/05-VALIDATION.md
@.planning/phases/05-backend-diagnostics-and-mvp-proof/05-PATTERNS.md

@crates/core/runtime/scripting/src/backend.rs
@crates/core/runtime/backend/src/lib.rs
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| T-05-01: exported-state snapshot failures collapse into generic runtime errors and cannot be bucketed cleanly | high | Keep failure stages explicit around snapshot conversion and event emission paths; add tests for state-snapshot failure behavior. |
| T-05-02: repeated poll or command failures crash the runtime abruptly instead of degrading first | high | Preserve existing poll failure threshold behavior and add targeted failure-path tests for degraded-then-stop sequencing. |
| T-05-03: command result plumbing loses backend failure visibility | medium | Emit command result data and lifecycle failure events together when handler execution or result conversion fails. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Tighten backend snapshot and command failure boundaries</name>
  <files>crates/core/runtime/scripting/src/backend.rs</files>
  <read_first>crates/core/runtime/scripting/src/backend.rs, .planning/phases/05-backend-diagnostics-and-mvp-proof/05-CONTEXT.md, .planning/phases/05-backend-diagnostics-and-mvp-proof/05-RESEARCH.md</read_first>
  <action>Refine `BackendScriptContext` so exported-state snapshot failures and command-result conversion failures are distinguishable from plain handler runtime failures. Keep `take_service_state_snapshot()` as the single exported-state read path, but make the returned errors preserve enough stage context for Phase 5 diagnostics work to bucket them under snapshot/emit or command-result handling instead of opaque runtime failure text. Do not add any service-name branches. Add focused tests named `backend_state_snapshot_failure_is_reported`, `backend_command_result_conversion_failure_is_reported`, and `backend_command_runtime_error_becomes_failed_result`.</action>
  <verify>nix develop -c cargo test -p mesh-core-scripting backend_command_result</verify>
  <acceptance_criteria>
    - `crates/core/runtime/scripting/src/backend.rs` contains test `backend_state_snapshot_failure_is_reported`.
    - `crates/core/runtime/scripting/src/backend.rs` contains test `backend_command_result_conversion_failure_is_reported`.
    - `crates/core/runtime/scripting/src/backend.rs` still contains `take_service_state_snapshot`.
    - `grep -n "wpctl\\|pactl\\|nmcli\\|upower" crates/core/runtime/scripting/src/backend.rs` returns no service-specific handling.
    - `nix develop -c cargo test -p mesh-core-scripting backend_command_result` exits 0.
  </acceptance_criteria>
  <done>Backend scripting errors are stage-aware enough for shell diagnostics and stale-state clearing to react correctly.</done>
</task>

<task type="auto">
  <name>Task 2: Preserve generic backend lifecycle events for every failure path</name>
  <files>crates/core/runtime/backend/src/lib.rs</files>
  <read_first>crates/core/runtime/backend/src/lib.rs, crates/core/runtime/scripting/src/backend.rs, .planning/phases/05-backend-diagnostics-and-mvp-proof/05-PATTERNS.md</read_first>
  <action>Update `spawn_backend_service()` so load failures, missing entrypoints, init failures, repeated poll failures, command handler failures, and state-snapshot/result-conversion failures all emit the correct generic lifecycle events with stable stage strings. Keep `Started`, `PollFailed`, `Failed`, `CommandResult`, and `Stopped` generic; do not add service-specific payloads. Preserve the existing `MAX_CONSECUTIVE_POLL_FAILURES` behavior so poll failures degrade visibly before the runtime stops. Add tests named `spawn_backend_service_reports_snapshot_failure_stage`, `spawn_backend_service_command_error_emits_result_and_failed_event`, and `spawn_backend_service_stops_after_three_consecutive_poll_failures` if the existing poll-failure test needs updated assertions.</action>
  <verify>nix develop -c cargo test -p mesh-core-backend backend</verify>
  <acceptance_criteria>
    - `crates/core/runtime/backend/src/lib.rs` contains test `spawn_backend_service_reports_snapshot_failure_stage`.
    - `crates/core/runtime/backend/src/lib.rs` contains test `spawn_backend_service_command_error_emits_result_and_failed_event`.
    - `crates/core/runtime/backend/src/lib.rs` contains `MAX_CONSECUTIVE_POLL_FAILURES`.
    - `crates/core/runtime/backend/src/lib.rs` still emits `BackendServiceEvent::PollFailed`, `BackendServiceEvent::Failed`, and `BackendServiceEvent::CommandResult`.
    - `nix develop -c cargo test -p mesh-core-backend backend` exits 0.
  </acceptance_criteria>
  <done>Every backend failure path reaches the shell as a clear generic lifecycle event.</done>
</task>

<task type="auto">
  <name>Task 3: Add targeted runtime proof for missing entrypoint and unsupported command diagnostics</name>
  <files>crates/core/runtime/scripting/src/backend.rs, crates/core/runtime/backend/src/lib.rs</files>
  <read_first>crates/core/runtime/scripting/src/backend.rs, crates/core/runtime/backend/src/lib.rs, .planning/REQUIREMENTS.md</read_first>
  <action>Add or tighten tests that prove two BDIAG-01 cases explicitly. First, a script without `init()` must yield `MissingEntrypoint` and produce the matching runtime failure event path. Second, an unsupported command must return a generic failed command result table while preserving shell-visible lifecycle behavior for true handler runtime failures only. Use exact test names `backend_missing_init_entrypoint_is_reported` and `backend_unsupported_command_returns_error_result`. Keep the unsupported-command behavior generic and JSON-based.</action>
  <verify>nix develop -c cargo test -p mesh-core-scripting backend_missing_init_entrypoint_is_reported && nix develop -c cargo test -p mesh-core-backend backend_unsupported_command_returns_error_result</verify>
  <acceptance_criteria>
    - `crates/core/runtime/scripting/src/backend.rs` contains test `backend_missing_init_entrypoint_is_reported`.
    - `crates/core/runtime/backend/src/lib.rs` contains test `backend_unsupported_command_returns_error_result`.
    - Unsupported command results contain `ok = false` or JSON `"ok": false`.
    - Missing entrypoint handling still uses the `MissingEntrypoint` error variant.
  </acceptance_criteria>
  <done>Phase 5 has explicit runtime proofs for missing-entrypoint and unsupported-command diagnostics.</done>
</task>

</tasks>

<verification>
- Run `nix develop -c cargo test -p mesh-core-scripting backend_command_result`.
- Run `nix develop -c cargo test -p mesh-core-backend backend`.
- Read `crates/core/runtime/backend/src/lib.rs` and confirm generic lifecycle events still carry service, source_plugin, stage, and JSON-compatible result data only.
</verification>

<success_criteria>
- Load, init, poll, command, and exported-state failure paths are distinguishable in the backend runtime.
- Repeated poll failures still degrade before shutdown instead of crashing the shell.
- Unsupported commands return generic error results without requiring service-specific Rust handling.
</success_criteria>

<output>
After completion, create `.planning/phases/05-backend-diagnostics-and-mvp-proof/05-01-SUMMARY.md`
</output>

