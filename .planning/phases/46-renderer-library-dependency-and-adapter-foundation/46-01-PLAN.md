---
phase: 46-renderer-library-dependency-and-adapter-foundation
plan: 01
title: Optional Renderer Library Cargo Features
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - crates/core/frontend/render/Cargo.toml
autonomous: true
requirements: [LIBS-01]
requirements_addressed: [LIBS-01]
user_setup: []
must_haves:
  truths:
    - "D-01: Selected renderer libraries are production Cargo manifest entries behind conservative adoption boundaries."
    - "D-02: Taffy, Parley, and AccessKit are primary foundation dependencies; AnyRender/Vello-style paint remains experimental."
    - "D-03: Dependency versions must be Rust 1.85-compatible or explicitly documented as deferred."
    - "D-04: Phase 46 must not add Blitz, Winit, DOM/web-platform, Stylo, or broader Skia expansion work."
    - "D-05: Current renderer behavior remains default for build and runtime."
    - "D-06: Explicit Cargo features control dependency fan-out."
  artifacts:
    - path: "Cargo.toml"
      provides: "Workspace candidate renderer-library version source"
      contains: "taffy = { version = \"0.10.1\""
    - path: "crates/core/frontend/render/Cargo.toml"
      provides: "Disabled-by-default render crate feature gates"
      contains: "renderer-libraries = ["
  key_links:
    - "Cargo.toml [workspace.dependencies] -> crates/core/frontend/render/Cargo.toml optional dependencies"
    - "renderer-libraries feature -> enabled dependency build verification"
---

<objective>
Add Rust 1.85-compatible renderer-library dependencies to production Cargo manifests behind disabled-by-default `mesh-core-render` features, without changing current renderer behavior or pulling candidate libraries into the default build.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-renderer-library-dependency-and-adapter-foundation/46-CONTEXT.md
@.planning/phases/46-renderer-library-dependency-and-adapter-foundation/46-RESEARCH.md
@.planning/phases/46-renderer-library-dependency-and-adapter-foundation/46-VALIDATION.md
@.planning/phases/46-renderer-library-dependency-and-adapter-foundation/46-PATTERNS.md
@Cargo.toml
@crates/core/frontend/render/Cargo.toml
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| T-46-01-01: candidate libraries become default dependencies and alter normal builds before adapter gates exist | high | Add empty `default = []` and make every candidate dependency optional behind explicit feature names. Verify default `cargo check -p mesh-core-render`. |
| T-46-01-02: latest crate versions require Rust 1.88 and break the Rust 1.85 workspace | high | Pin Rust-compatible versions from Phase 46 research: `taffy 0.10.1`, `parley 0.7.0`, `accesskit 0.24.0`, `anyrender 0.10.0`, `vello_encoding 0.5.1`. |
| T-46-01-03: full Vello pulls `wgpu` fan-out into the foundation phase | medium | Do not add full `vello`; add `vello_encoding` only under `renderer-vello-encoding` and leave full Vello backend to Phase 49. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add workspace renderer-library dependency versions</name>
  <files>Cargo.toml</files>
  <read_first>Cargo.toml, .planning/phases/46-renderer-library-dependency-and-adapter-foundation/46-RESEARCH.md</read_first>
  <action>Add these exact entries to the root `Cargo.toml` `[workspace.dependencies]` table, near the existing shared dependency entries: `taffy = { version = "0.10.1", default-features = false, features = ["std", "taffy_tree", "flexbox", "grid", "block_layout", "calc", "content_size"] }`, `parley = { version = "0.7.0", default-features = false, features = ["std"] }`, `accesskit = { version = "0.24.0", default-features = false }`, `anyrender = { version = "0.10.0", default-features = false }`, and `vello_encoding = { version = "0.5.1", default-features = false }`. Do not add `vello`, `blitz`, `winit`, `stylo`, `html5ever`, `xml5ever`, or additional Skia dependencies in this task.</action>
  <verify>rg -n "taffy = \\{ version = \"0\\.10\\.1\"|parley = \\{ version = \"0\\.7\\.0\"|accesskit = \\{ version = \"0\\.24\\.0\"|anyrender = \\{ version = \"0\\.10\\.0\"|vello_encoding = \\{ version = \"0\\.5\\.1\"" Cargo.toml</verify>
  <acceptance_criteria>
    - `Cargo.toml` contains `taffy = { version = "0.10.1"`.
    - `Cargo.toml` contains `parley = { version = "0.7.0"`.
    - `Cargo.toml` contains `accesskit = { version = "0.24.0"`.
    - `Cargo.toml` contains `anyrender = { version = "0.10.0"`.
    - `Cargo.toml` contains `vello_encoding = { version = "0.5.1"`.
    - `Cargo.toml` does not gain a new `vello =` dependency.
  </acceptance_criteria>
  <done>Workspace manifests know the selected Rust-compatible renderer-library versions.</done>
</task>

<task type="auto">
  <name>Task 2: Add disabled-by-default mesh-core-render features</name>
  <files>crates/core/frontend/render/Cargo.toml</files>
  <read_first>crates/core/frontend/render/Cargo.toml, Cargo.toml, crates/core/frontend/render/README.md, .planning/phases/46-renderer-library-dependency-and-adapter-foundation/46-CONTEXT.md</read_first>
  <action>Add a `[features]` table to `crates/core/frontend/render/Cargo.toml` before `[dependencies]` if none exists. The table must contain exactly these feature keys and dependency mappings: `default = []`, `renderer-taffy = ["dep:taffy"]`, `renderer-parley = ["dep:parley"]`, `renderer-accesskit = ["dep:accesskit"]`, `renderer-anyrender = ["dep:anyrender"]`, `renderer-vello-encoding = ["dep:vello_encoding"]`, and `renderer-libraries = ["renderer-taffy", "renderer-parley", "renderer-accesskit", "renderer-anyrender", "renderer-vello-encoding"]`. Then add optional dependency entries under `[dependencies]`: `taffy = { workspace = true, optional = true }`, `parley = { workspace = true, optional = true }`, `accesskit = { workspace = true, optional = true }`, `anyrender = { workspace = true, optional = true }`, and `vello_encoding = { workspace = true, optional = true }`. Leave existing `cosmic-text`, `fontdb`, `image`, `resvg`, `skia-safe`, `swash`, and `zeno` dependencies intact.</action>
  <verify>env XDG_CACHE_HOME=/tmp/codex-nix-cache nix develop -c cargo check -p mesh-core-render</verify>
  <acceptance_criteria>
    - `crates/core/frontend/render/Cargo.toml` contains `default = []`.
    - `crates/core/frontend/render/Cargo.toml` contains `renderer-taffy = ["dep:taffy"]`.
    - `crates/core/frontend/render/Cargo.toml` contains `renderer-parley = ["dep:parley"]`.
    - `crates/core/frontend/render/Cargo.toml` contains `renderer-accesskit = ["dep:accesskit"]`.
    - `crates/core/frontend/render/Cargo.toml` contains `renderer-anyrender = ["dep:anyrender"]`.
    - `crates/core/frontend/render/Cargo.toml` contains `renderer-vello-encoding = ["dep:vello_encoding"]`.
    - `crates/core/frontend/render/Cargo.toml` contains `renderer-libraries = ["renderer-taffy", "renderer-parley", "renderer-accesskit", "renderer-anyrender", "renderer-vello-encoding"]`.
    - `env XDG_CACHE_HOME=/tmp/codex-nix-cache nix develop -c cargo check -p mesh-core-render` exits 0.
  </acceptance_criteria>
  <done>`mesh-core-render` has optional renderer-library feature gates and the default render crate build remains unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Verify enabled dependency feature path</name>
  <files>Cargo.toml, crates/core/frontend/render/Cargo.toml</files>
  <read_first>Cargo.toml, crates/core/frontend/render/Cargo.toml, .planning/phases/46-renderer-library-dependency-and-adapter-foundation/46-VALIDATION.md</read_first>
  <action>Run the enabled feature path and dependency tree checks. If `renderer-libraries` fails because a selected crate version is incompatible with Rust 1.85 or another selected dependency, first prefer the Rust-compatible versions already listed in `46-RESEARCH.md`; if no compatible version exists, keep that crate optional but remove it from the aggregate `renderer-libraries` feature and record the reason in the Plan 03 documentation task. Do not silently drop a primary dependency without documenting it.</action>
  <verify>env XDG_CACHE_HOME=/tmp/codex-nix-cache nix develop -c cargo check -p mesh-core-render --features renderer-libraries</verify>
  <acceptance_criteria>
    - `env XDG_CACHE_HOME=/tmp/codex-nix-cache nix develop -c cargo check -p mesh-core-render --features renderer-libraries` exits 0, or the plan summary records the exact incompatible crate/version and the manifest/docs explain why it was removed from `renderer-libraries`.
    - `env XDG_CACHE_HOME=/tmp/codex-nix-cache nix develop -c cargo tree -p mesh-core-render --features renderer-libraries` runs and its output is summarized in the plan summary.
    - Default `cargo check -p mesh-core-render` still exits 0 after enabled-feature changes.
  </acceptance_criteria>
  <done>The selected optional dependency set compiles when explicitly enabled and remains absent from default behavior.</done>
</task>

</tasks>

<verification>
- Run `env XDG_CACHE_HOME=/tmp/codex-nix-cache nix develop -c cargo check -p mesh-core-render`.
- Run `env XDG_CACHE_HOME=/tmp/codex-nix-cache nix develop -c cargo check -p mesh-core-render --features renderer-libraries`.
- Run `env XDG_CACHE_HOME=/tmp/codex-nix-cache nix develop -c cargo tree -p mesh-core-render --features renderer-libraries`.
- Run `rg -n "renderer-taffy|renderer-parley|renderer-accesskit|renderer-anyrender|renderer-vello-encoding|renderer-libraries" crates/core/frontend/render/Cargo.toml`.
</verification>

<success_criteria>
- Production Cargo manifests include selected renderer-library dependency versions.
- All candidate libraries are disabled by default.
- Enabled feature path compiles or incompatible candidates are explicitly documented and excluded from the aggregate feature.
- No renderer behavior, presentation behavior, or `.mesh` author contract changes in this plan.
</success_criteria>
