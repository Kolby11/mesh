---
phase: 08-practical-css-coverage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/core/ui/elements/src/style.rs
  - crates/core/ui/component/src/parser/styles.rs
  - crates/core/ui/component/src/style.rs
autonomous: true
requirements: [CSS-01, CSS-03]
requirements_addressed: [CSS-01, CSS-03]
user_setup: []
must_haves:
  truths:
    - "D-01: common CSS means a practical shell-ui subset, not browser compatibility."
    - "D-02: CSS Grid, floats, multicolumn layout, full media queries, arbitrary web at-rules, transforms/filters, rich web text APIs, and browser edge cases stay out of Phase 8."
    - "D-03: the supported subset must be documented as an author-facing contract with explicit MESH semantic differences."
    - "D-10: unsupported properties produce clear diagnostics or structured warnings with property name and selector/source context."
    - "D-11: unsupported at-rules fail or warn consistently according to risk; malformed supported CSS and semantics-changing unsupported constructs remain errors."
    - "D-12: unknown properties must not crash the shell and must be visible to plugin authors."
  artifacts:
    - path: "crates/core/ui/elements/src/style.rs"
      provides: "Supported CSS property contract and style diagnostics emitted during computed style resolution"
      contains: "StyleResolver"
    - path: "crates/core/ui/component/src/parser/styles.rs"
      provides: "At-rule and declaration lowering errors before runtime style resolution"
      contains: "unsupported at-rule"
---

<objective>
Define the Phase 8 supported CSS property contract and replace silent unsupported-property behavior with deterministic diagnostics while preserving the practical shell CSS boundary.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-practical-css-coverage/08-CONTEXT.md
@.planning/phases/08-practical-css-coverage/08-RESEARCH.md
@.planning/phases/08-practical-css-coverage/08-VALIDATION.md
@.planning/phases/08-practical-css-coverage/08-PATTERNS.md

@crates/core/ui/elements/src/style.rs
@crates/core/ui/component/src/parser/styles.rs
@crates/core/ui/component/src/style.rs
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| T-08-01: unsupported CSS silently no-ops and authors think it is supported | high | Add structured style diagnostics for unsupported properties and assert them in tests. |
| T-08-02: property support drifts between parser, resolver, docs, and LSP | high | Introduce a resolver-side supported-property contract with tests that named Phase 8 categories are covered. |
| T-08-03: diagnostics reject harmless declarations too aggressively and block shell startup | medium | Keep unknown properties as warnings/diagnostics, while parser errors remain for malformed or semantics-changing unsupported at-rules. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add supported CSS property metadata and diagnostics types</name>
  <files>crates/core/ui/elements/src/style.rs</files>
  <read_first>crates/core/ui/elements/src/style.rs, .planning/phases/08-practical-css-coverage/08-CONTEXT.md, .planning/phases/08-practical-css-coverage/08-RESEARCH.md</read_first>
  <action>Add a small public supported-property contract in `crates/core/ui/elements/src/style.rs`. Define `pub struct StyleDiagnostic { pub property: String, pub selector: Option<String>, pub message: String }` or an equivalent testable struct. Add `pub fn supported_css_properties() -> &'static [&'static str]` that includes at least: `background`, `background-color`, `color`, `border`, `border-color`, `border-width`, `border-radius`, `display`, `visibility`, `opacity`, `overflow`, `overflow-x`, `overflow-y`, `width`, `height`, `min-width`, `max-width`, `min-height`, `max-height`, `padding`, `padding-inline`, `padding-block`, `margin`, `margin-inline`, `margin-block`, `font`, `font-family`, `font-size`, `font-weight`, `font-style`, `line-height`, `letter-spacing`, `text-align`, `text-overflow`, `direction`, `flex`, `flex-direction`, `flex-wrap`, `flex-grow`, `flex-shrink`, `flex-basis`, `justify-content`, `align-items`, `align-self`, `align-content`, `gap`, `row-gap`, `column-gap`, `position`, `z-index`, `inset`, `top`, `right`, `bottom`, `left`, `transition`, `transition-property`, `transition-duration`, `transition-delay`, `transition-timing-function`, `animation`, `animation-name`, `animation-duration`, `animation-delay`, `animation-timing-function`, `animation-iteration-count`, `animation-direction`, `animation-fill-mode`, and `animation-play-state`. Add `pub fn is_supported_css_property(property: &str) -> bool` that treats custom properties beginning with `--` as supported variable declarations.</action>
  <verify>nix develop -c cargo test -p mesh-core-elements supported_css</verify>
  <acceptance_criteria>
    - `crates/core/ui/elements/src/style.rs` contains `pub struct StyleDiagnostic`.
    - `crates/core/ui/elements/src/style.rs` contains `pub fn supported_css_properties`.
    - `crates/core/ui/elements/src/style.rs` contains `pub fn is_supported_css_property`.
    - `crates/core/ui/elements/src/style.rs` contains test `supported_css_properties_cover_phase_8_contract`.
    - `nix develop -c cargo test -p mesh-core-elements supported_css` exits 0.
  </acceptance_criteria>
  <done>The resolver has a single testable contract for the Phase 8 supported property set.</done>
</task>

<task type="auto">
  <name>Task 2: Emit deterministic diagnostics for unknown declarations</name>
  <files>crates/core/ui/elements/src/style.rs</files>
  <read_first>crates/core/ui/elements/src/style.rs, crates/core/ui/component/src/style.rs, .planning/phases/08-practical-css-coverage/08-PATTERNS.md</read_first>
  <action>Add a diagnostic-producing style resolution path without removing the existing `resolve_node_style` API. Add `pub fn resolve_node_style_with_diagnostics(...) -> (ComputedStyle, Vec<StyleDiagnostic>)` or equivalent, using the same parameters as `resolve_node_style`. It must call the same declaration application logic but collect a diagnostic when a non-custom property is not in `supported_css_properties()`. The diagnostic message must include the exact substring `unsupported CSS property` and the property name. Keep `resolve_node_style(...)` as a wrapper that returns only `ComputedStyle` so existing callers compile.</action>
  <verify>nix develop -c cargo test -p mesh-core-elements style_diagnostics</verify>
  <acceptance_criteria>
    - `crates/core/ui/elements/src/style.rs` contains `resolve_node_style_with_diagnostics`.
    - `crates/core/ui/elements/src/style.rs` contains test `unsupported_property_produces_style_diagnostic`.
    - `crates/core/ui/elements/src/style.rs` contains the string `unsupported CSS property`.
    - Existing `resolve_node_style(` call sites still compile through `nix develop -c cargo test -p mesh-core-elements style`.
    - `nix develop -c cargo test -p mesh-core-elements style_diagnostics` exits 0.
  </acceptance_criteria>
  <done>Unknown properties are visible to authors through deterministic diagnostics instead of debug-only logs.</done>
</task>

<task type="auto">
  <name>Task 3: Lock parser at-rule boundary with focused tests</name>
  <files>crates/core/ui/component/src/parser/styles.rs, crates/core/ui/component/src/parser.rs</files>
  <read_first>crates/core/ui/component/src/parser/styles.rs, crates/core/ui/component/src/parser.rs, .planning/phases/08-practical-css-coverage/08-CONTEXT.md</read_first>
  <action>Add parser tests that prove unsupported at-rules with cascade or scheduling semantics remain explicit parse errors in Phase 8. Use exact test names `unsupported_media_rule_reports_at_rule_name` and `unsupported_keyframes_rule_reports_at_rule_name`. Each test must parse a `<style>` block containing `@media` or `@keyframes`, assert `parse_component(source).unwrap_err()` or equivalent, and assert the error text includes `unsupported at-rule '@media'` or `unsupported at-rule '@keyframes'` exactly. Do not add support for CSS Grid, floats, media queries, arbitrary at-rules, transforms, or filters.</action>
  <verify>nix develop -c cargo test -p mesh-core-component unsupported_</verify>
  <acceptance_criteria>
    - `crates/core/ui/component/src/parser.rs` or `crates/core/ui/component/src/parser/styles.rs` contains test `unsupported_media_rule_reports_at_rule_name`.
    - `crates/core/ui/component/src/parser.rs` or `crates/core/ui/component/src/parser/styles.rs` contains test `unsupported_keyframes_rule_reports_at_rule_name`.
    - The tests assert `unsupported at-rule '@media'`.
    - The tests assert `unsupported at-rule '@keyframes'`.
    - `nix develop -c cargo test -p mesh-core-component unsupported_` exits 0.
  </acceptance_criteria>
  <done>Phase 8 rejects semantics-changing unsupported at-rules consistently and visibly.</done>
</task>

</tasks>

<verification>
- Run `nix develop -c cargo test -p mesh-core-elements supported_css`.
- Run `nix develop -c cargo test -p mesh-core-elements style_diagnostics`.
- Run `nix develop -c cargo test -p mesh-core-component unsupported_`.
- Read `crates/core/ui/elements/src/style.rs` and confirm unknown properties no longer only emit `tracing::debug!("unknown style property`.
</verification>

<success_criteria>
- CSS-01 is covered by a named practical property contract.
- CSS-03 is covered by deterministic unsupported-property diagnostics and parser at-rule errors.
- D-01, D-02, D-03, D-10, D-11, and D-12 appear in `must_haves.truths` and are enforced by tests.
</success_criteria>

<output>
After completion, create `.planning/phases/08-practical-css-coverage/08-01-SUMMARY.md`
</output>

