---
phase: 08-practical-css-coverage
plan: 05
type: execute
wave: 5
depends_on: [08-01, 08-02, 08-03, 08-04]
files_modified:
  - crates/tools/lsp/src/knowledge/css.rs
  - crates/tools/lsp/src/analyzer/style.rs
  - docs/css-coverage.md
  - docs/frontend/mesh-syntax.md
  - packages/plugins/frontend/core/navigation-bar/src/main.mesh
autonomous: true
requirements: [CSS-01, CSS-02, CSS-03, CSS-04]
requirements_addressed: [CSS-01, CSS-02, CSS-03, CSS-04]
user_setup: []
must_haves:
  truths:
    - "D-03: the supported subset must be documented as an author-facing contract with examples that look like normal CSS where possible and explicit notes where MESH differs."
    - "D-13: parser/lowering stays in mesh-core-component, computed style/value resolution stays in mesh-core-elements, and paint/layout consumption stays in mesh-core-render."
    - "D-14: any supported CSS expansion updates tests near the parser/resolver and author-facing documentation or LSP knowledge where styling help exists."
    - "D-15: navigation-bar is not migrated fully in Phase 8; focused fixtures or examples are allowed, full proof belongs to Phase 13."
  artifacts:
    - path: "crates/tools/lsp/src/knowledge/css.rs"
      provides: "LSP-facing CSS property completions for the supported MESH subset"
      contains: "CSS_PROPERTIES"
    - path: "docs/css-coverage.md"
      provides: "Author-facing supported and unsupported CSS contract"
      contains: "CSS Coverage in MESH"
    - path: "packages/plugins/frontend/core/navigation-bar/src/main.mesh"
      provides: "Existing core surface style examples and future Phase 13 proof target"
      contains: "token("
---

<objective>
Update LSP knowledge, author-facing CSS documentation, and focused proof examples so plugin authors can discover the Phase 8 practical CSS contract without implying full navigation-bar migration or browser compatibility.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-practical-css-coverage/08-CONTEXT.md
@.planning/phases/08-practical-css-coverage/08-RESEARCH.md
@.planning/phases/08-practical-css-coverage/08-VALIDATION.md
@.planning/phases/08-practical-css-coverage/08-PATTERNS.md

@crates/tools/lsp/src/knowledge/css.rs
@crates/tools/lsp/src/analyzer/style.rs
@docs/css-coverage.md
@docs/frontend/mesh-syntax.md
@packages/plugins/frontend/core/navigation-bar/src/main.mesh
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| T-08-07: LSP suggests unsupported CSS and creates false author expectations | high | Update `CSS_PROPERTIES` to mirror the Phase 8 supported contract and add completion tests. |
| T-08-08: docs claim stale support status and downstream agents plan against wrong behavior | medium | Rewrite `docs/css-coverage.md` statuses to match code after Plans 1-4. |
| T-08-09: navigation-bar proof expands into Phase 13 migration early | medium | Add only focused examples or comments/tests; do not restructure navigation-bar in Phase 8. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Update LSP CSS property knowledge and completion tests</name>
  <files>crates/tools/lsp/src/knowledge/css.rs, crates/tools/lsp/src/analyzer/style.rs</files>
  <read_first>crates/tools/lsp/src/knowledge/css.rs, crates/tools/lsp/src/analyzer/style.rs, crates/core/ui/elements/src/style.rs, .planning/phases/08-practical-css-coverage/08-CONTEXT.md</read_first>
  <action>Update `CSS_PROPERTIES` in `crates/tools/lsp/src/knowledge/css.rs` so it lists the Phase 8 supported properties from `supported_css_properties()`: practical box model, sizing, flex layout, typography, borders/radius, overflow, display/visibility-like behavior, opacity/color/background, positioning/inset, selector state hooks through styleable properties, transition declarations, and animation declarations. Replace the stale comment `unsupported properties are silently ignored` with wording that unsupported properties produce style diagnostics. Add tests named `css_completion_includes_phase_8_shorthands`, `css_completion_includes_animation_declarations`, and `css_completion_does_not_include_grid_or_transform` in the existing LSP test location or in a new test module in `crates/tools/lsp/src/knowledge/css.rs`.</action>
  <verify>nix develop -c cargo test -p mesh-tools-lsp css_completion</verify>
  <acceptance_criteria>
    - `crates/tools/lsp/src/knowledge/css.rs` contains `animation-name`.
    - `crates/tools/lsp/src/knowledge/css.rs` contains `border`.
    - `crates/tools/lsp/src/knowledge/css.rs` no longer contains `unsupported properties are silently ignored`.
    - `crates/tools/lsp/src/knowledge/css.rs` or an adjacent LSP test module contains test `css_completion_does_not_include_grid_or_transform`.
    - `nix develop -c cargo test -p mesh-tools-lsp css_completion` exits 0.
  </acceptance_criteria>
  <done>LSP completions match the Phase 8 authoring contract and avoid out-of-scope browser CSS.</done>
</task>

<task type="auto">
  <name>Task 2: Refresh CSS coverage and syntax docs</name>
  <files>docs/css-coverage.md, docs/frontend/mesh-syntax.md</files>
  <read_first>docs/css-coverage.md, docs/frontend/mesh-syntax.md, .planning/phases/08-practical-css-coverage/08-CONTEXT.md, .planning/phases/08-practical-css-coverage/08-RESEARCH.md</read_first>
  <action>Update `docs/css-coverage.md` so supported property tables match the Phase 8 contract after Plans 1-4. Explicitly document: MESH supports practical shell CSS rather than full browser CSS; CSS Grid, floats, multicolumn, full media queries, arbitrary at-rules, transforms/filters, and custom keyframe scheduling are out of scope; `token(...)` is first-class; local `var(...)` is supported for supported declarations; `@keyframes` remains unsupported until Phase 12; animation declarations are metadata only until Phase 12 scheduling. Update `docs/frontend/mesh-syntax.md` to link or refer to `docs/css-coverage.md` and include one concise style snippet using `--surface`, `var(--surface)`, `token(spacing.md)`, `border`, `flex`, `overflow`, and `transition`.</action>
  <verify>grep -n "practical shell CSS\\|var(--surface)\\|@keyframes\\|Phase 12" docs/css-coverage.md docs/frontend/mesh-syntax.md</verify>
  <acceptance_criteria>
    - `docs/css-coverage.md` contains `practical shell CSS`.
    - `docs/css-coverage.md` contains `CSS Grid`.
    - `docs/css-coverage.md` contains `@keyframes`.
    - `docs/css-coverage.md` contains `Phase 12`.
    - `docs/frontend/mesh-syntax.md` contains `var(--surface)`.
    - `docs/frontend/mesh-syntax.md` contains `token(spacing.md)`.
  </acceptance_criteria>
  <done>Author-facing docs describe the exact supported and unsupported Phase 8 CSS boundary.</done>
</task>

<task type="auto">
  <name>Task 3: Add focused authoring proof without navigation-bar migration</name>
  <files>packages/plugins/frontend/core/navigation-bar/src/main.mesh, docs/css-coverage.md</files>
  <read_first>packages/plugins/frontend/core/navigation-bar/src/main.mesh, docs/css-coverage.md, .planning/phases/08-practical-css-coverage/08-CONTEXT.md</read_first>
  <action>Add a focused proof example for Phase 8 styling without performing the full Phase 13 navigation-bar migration. Prefer a small `.mesh` style fixture in tests if one exists; if using `packages/plugins/frontend/core/navigation-bar/src/main.mesh`, limit edits to adding or adjusting no more than three declarations that demonstrate supported Phase 8 syntax already appropriate for the current component, such as `border: 1px solid token(color.outline)`, a local `--nav-surface` variable consumed by `background: var(--nav-surface)`, or a `transition: background-color 150ms ease-out`. Do not restructure the navigation-bar template, do not add keyboard/text-selection/animation proof behavior, and do not migrate the full surface styling.</action>
  <verify>grep -n "var(--nav-surface)\\|transition: background-color\\|border: 1px solid" packages/plugins/frontend/core/navigation-bar/src/main.mesh docs/css-coverage.md</verify>
  <acceptance_criteria>
    - `packages/plugins/frontend/core/navigation-bar/src/main.mesh` or a dedicated style proof fixture contains `transition: background-color`.
    - `packages/plugins/frontend/core/navigation-bar/src/main.mesh` or a dedicated style proof fixture contains either `var(--nav-surface)` or `border: 1px solid`.
    - `packages/plugins/frontend/core/navigation-bar/src/main.mesh` is not rewritten into a new component structure.
    - `grep -n "onkeydown\\|selectable\\|@keyframes" packages/plugins/frontend/core/navigation-bar/src/main.mesh` returns no new Phase 13 behavior from this plan.
  </acceptance_criteria>
  <done>Phase 8 has a focused real-surface styling proof while full navigation-bar proof remains Phase 13.</done>
</task>

</tasks>

<verification>
- Run `nix develop -c cargo test -p mesh-tools-lsp css_completion`.
- Run `grep -n "practical shell CSS\\|var(--surface)\\|@keyframes\\|Phase 12" docs/css-coverage.md docs/frontend/mesh-syntax.md`.
- Run `grep -n "onkeydown\\|selectable\\|@keyframes" packages/plugins/frontend/core/navigation-bar/src/main.mesh` and confirm no new Phase 13 behavior was added.
- Run the Phase 8 full validation command from `08-VALIDATION.md`.
</verification>

<success_criteria>
- CSS-01 through CSS-04 are visible in LSP/docs/proof examples.
- D-03, D-13, D-14, and D-15 are visible in `must_haves.truths` and enforced by docs/LSP/proof checks.
- Navigation-bar remains a focused Phase 8 example, not the full Phase 13 migration.
</success_criteria>

<output>
After completion, create `.planning/phases/08-practical-css-coverage/08-05-SUMMARY.md`
</output>

