---
phase: 40-migration-diagnostics-and-author-docs
plan: 03
title: Keybind Migration Continuity
type: execute
wave: 2
depends_on: [40-01]
files_modified:
  - crates/core/extension/module/src/package/installed_graph.rs
  - crates/core/extension/module/src/package/tests.rs
  - crates/core/shell/src/shell/component/input/keyboard.rs
  - crates/core/shell/src/shell/component/tests/interaction/navigation.rs
  - docs/module-system.md
  - docs/settings/README.md
autonomous: true
requirements: [MIGR-02]
requirements_addressed: [MIGR-02]
user_setup: []
must_haves:
  truths:
    - "Paused v1.6 keybind declarations remain part of canonical module manifest data."
    - "Typed installed-graph keybind contributions must expose enough data for later dispatch, conflict, and accessibility phases."
    - "settings.keyboard.surface_shortcuts are user overrides, not canonical module declarations."
  artifacts:
    - path: "crates/core/extension/module/src/package/installed_graph.rs"
      provides: "Typed keybind contribution records"
      contains: "ContributedKeybindAction"
    - path: "crates/core/shell/src/shell/component/input/keyboard.rs"
      provides: "Manifest-first shortcut resolution with legacy settings fallback"
      contains: "legacy_settings_surface_shortcut_declarations"
---

<objective>
Preserve paused v1.6 keybind declaration/resolution work inside the canonical contribution model by exposing trigger data through installed-graph keybind records and documenting how user shortcut overrides migrate around manifest declarations.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-migration-diagnostics-and-author-docs/40-RESEARCH.md
@.planning/phases/40-migration-diagnostics-and-author-docs/40-VALIDATION.md
@.planning/phases/40-migration-diagnostics-and-author-docs/40-PATTERNS.md
@.planning/phases/39-contribution-and-interface-extension-index/39-03-SUMMARY.md
@crates/core/extension/module/src/package/installed_graph.rs
@crates/core/extension/module/src/package/tests.rs
@crates/core/shell/src/shell/component/input/keyboard.rs
@crates/core/shell/src/shell/component/tests/interaction/navigation.rs
@docs/module-system.md
@docs/settings/README.md
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| T-40-03-01: typed keybind graph records drop default or localized trigger data | high | Add `trigger` and `localized_triggers` fields to `ContributedKeybindAction` and assert they round-trip from canonical manifests. |
| T-40-03-02: legacy settings shortcuts become the canonical declaration path again | high | Keep shell resolution manifest-first and document settings as user overrides/fallback only. |
| T-40-03-03: keybind changes regress v1.6 override precedence | high | Run existing navigation shortcut tests and add focused assertions if trigger graph data changes resolution assumptions. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Carry trigger data in typed keybind contribution records</name>
  <files>crates/core/extension/module/src/package/installed_graph.rs, crates/core/extension/module/src/package/tests.rs</files>
  <read_first>crates/core/extension/module/src/package/installed_graph.rs, crates/core/extension/module/src/package/tests.rs, crates/core/extension/module/src/manifest/model.rs, modules/frontend/navigation-bar/module.json</read_first>
  <action>Extend `ContributedKeybindAction` in `installed_graph.rs` with `pub trigger: manifest::KeybindTrigger` and `pub localized_triggers: HashMap<String, manifest::KeybindTrigger>` or the existing in-scope imported equivalents. In `ModuleContributionIndex::index_module`, clone `action.trigger` and `action.localized_triggers` into each keybind record. Add or update package tests to build a frontend manifest with keybind action `mute`, default trigger `{ kind: shortcut, key: m }`, and localized trigger `sk -> { kind: shortcut, key: s }`; load it through `InstalledModuleGraph`; assert `graph.contributed_keybinds()` or the existing getter returns a record whose `action_id` is `mute`, default trigger key is `m`, and localized `sk` trigger key is `s`.</action>
  <verify>cargo test -p mesh-core-module package::tests keybind</verify>
  <acceptance_criteria>
    - `crates/core/extension/module/src/package/installed_graph.rs` contains `pub trigger:`.
    - `crates/core/extension/module/src/package/installed_graph.rs` contains `pub localized_triggers:`.
    - `crates/core/extension/module/src/package/installed_graph.rs` clones `action.trigger`.
    - `crates/core/extension/module/src/package/installed_graph.rs` clones `action.localized_triggers`.
    - `crates/core/extension/module/src/package/tests.rs` contains `localized_triggers` or `localizedTriggers`.
    - `cargo test -p mesh-core-module package::tests keybind` exits 0.
  </acceptance_criteria>
  <done>Installed graph keybind contribution records preserve default and localized trigger declarations.</done>
</task>

<task type="auto">
  <name>Task 2: Protect manifest-first shortcut resolution behavior</name>
  <files>crates/core/shell/src/shell/component/input/keyboard.rs, crates/core/shell/src/shell/component/tests/interaction/navigation.rs</files>
  <read_first>crates/core/shell/src/shell/component/input/keyboard.rs, crates/core/shell/src/shell/component/tests/interaction/navigation.rs, docs/settings/README.md, .planning/STATE.md</read_first>
  <action>Inspect the existing shortcut resolution tests. If they already cover manifest defaults, localized triggers, user overrides, and legacy settings fallback, leave production code unchanged and add only missing assertions. If coverage is missing, add focused tests in `navigation.rs` proving: a manifest keybind action with id `mute` wins over a legacy settings-derived declaration with the same id; `keyboard.surface_shortcuts["@mesh/navigation-bar"].mute.key = "u"` overrides the manifest default key `m`; localized manifest triggers still resolve by active locale when no user override exists; a legacy settings declaration is still used when the manifest does not declare that action id. Do not change `legacy_settings_surface_shortcut_declarations` behavior except to fix a failing preservation test.</action>
  <verify>cargo test -p mesh-core-shell shell::component::tests::interaction::navigation</verify>
  <acceptance_criteria>
    - Navigation shortcut tests cover manifest default trigger behavior.
    - Navigation shortcut tests cover user override precedence.
    - Navigation shortcut tests cover localized trigger behavior.
    - Navigation shortcut tests cover legacy settings fallback for missing manifest ids.
    - `crates/core/shell/src/shell/component/input/keyboard.rs` still contains `legacy_settings_surface_shortcut_declarations`.
    - `cargo test -p mesh-core-shell shell::component::tests::interaction::navigation` exits 0.
  </acceptance_criteria>
  <done>Shell shortcut resolution preserves v1.6 behavior while keybind declarations remain canonical manifest data.</done>
</task>

<task type="auto">
  <name>Task 3: Document keybind migration and override boundaries</name>
  <files>docs/module-system.md, docs/settings/README.md</files>
  <read_first>docs/module-system.md, docs/settings/README.md, modules/frontend/navigation-bar/module.json, crates/core/shell/src/shell/component/input/keyboard.rs</read_first>
  <action>In `docs/module-system.md`, add a `### Keybind Contributions` subsection under the manifest/contribution documentation. State that modules declare keybind actions in `mesh.keybinds`, including `label`, `description`, `category`, `trigger`, and `localizedTriggers`, and that installed graph keybind contributions preserve action id, default trigger, and localized triggers for later dispatch/conflict phases. In `docs/settings/README.md`, update the keyboard settings section to say `keyboard.surface_shortcuts` is user override data for manifest-declared action ids. Add exact wording `Legacy settings-derived shortcut declarations are fallback input only when a manifest action id is absent.` Keep the existing `surface_shortcuts` JSON example.</action>
  <verify>rg -n "Keybind Contributions|mesh.keybinds|localizedTriggers|installed graph keybind contributions|surface_shortcuts|Legacy settings-derived shortcut declarations are fallback input only when a manifest action id is absent" docs/module-system.md docs/settings/README.md</verify>
  <acceptance_criteria>
    - `docs/module-system.md` contains `### Keybind Contributions`.
    - `docs/module-system.md` contains `mesh.keybinds`.
    - `docs/module-system.md` contains `localizedTriggers`.
    - `docs/module-system.md` contains `installed graph keybind contributions`.
    - `docs/settings/README.md` contains `surface_shortcuts`.
    - `docs/settings/README.md` contains `Legacy settings-derived shortcut declarations are fallback input only when a manifest action id is absent.`
  </acceptance_criteria>
  <done>Authors and future implementers can see how v1.6 keybind declarations migrate into the canonical contribution model.</done>
</task>

</tasks>

<verification>
- Run `cargo test -p mesh-core-module package::tests keybind`.
- Run `cargo test -p mesh-core-shell shell::component::tests::interaction::navigation`.
- Run `rg -n "Keybind Contributions|mesh.keybinds|localizedTriggers|surface_shortcuts|fallback input" docs/module-system.md docs/settings/README.md`.
</verification>

<success_criteria>
- Typed installed-graph keybind records expose default and localized trigger data.
- Shell shortcut resolution remains manifest-first with user overrides and legacy fallback preserved.
- Documentation explains the keybind migration path for later dispatch/conflict/accessibility work.
</success_criteria>

