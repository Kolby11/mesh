---
phase: 57
plan: 1
slug: retained-ordering-visual-bounds-damage-diagnostics
status: planned
---

# Plan 57-01: Retained Ordering, Visual Bounds, Damage Diagnostics

<objective>
Make retained display-list damage and profiling explicit for z-order, clipping, visual overflow, and fallback promotion decisions.
</objective>

<tasks>

## Task 1: Add retained paint diagnostics counters
- Add backend-neutral counters to display-list metrics for changed layout, changed paint, effect overflow, and fallback promotion.
- Populate them from `RenderObjectDirtySummary`, visual bounds, and reuse/fallback decisions.
- Wire the fields through `RetainedPaintSnapshot` and debug JSON serialization.

## Task 2: Strengthen display-list correctness tests
- Add tests proving z-index command ordering is stable and computed before replay.
- Add tests proving clipped descendants with effect overflow are retained only when their visual bounds intersect the clip.
- Add tests proving effect overflow contributes to damage and profiling counters.

## Task 3: Verify focused render/debug behavior
- Run focused render display-list tests.
- Run focused shell debug/profiling tests if compile scope permits.
- Record verification output in the phase summary.

</tasks>

<verification>

```bash
nix develop -c cargo test -p mesh-core-render display_list_
nix develop -c cargo test -p mesh-core-shell retained_paint
```

</verification>
