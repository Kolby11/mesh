---
phase: 03-backend-host-api-contract
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - crates/core/runtime/scripting/src/backend.rs
  - crates/core/runtime/scripting/src/host_api.rs
autonomous: true
requirements: [BHOST-03, BHOST-04]
requirements_addressed: [BHOST-03, BHOST-04]
user_setup: []
must_haves:
  truths:
    - "D-08: mesh.config() is the public config contract and returns the backend plugin's full settings table."
    - "D-09: no mesh.config.get, mesh.config.get_all, or path lookup helpers in Phase 3."
    - "D-10: mesh.log supports fixed levels debug, info, warn, and error."
    - "D-11: invalid log levels must not crash backend scripts and should be logged as warnings."
    - "D-12: both mesh.log('info', 'message') and mesh.log.info('message') styles are public."
  artifacts:
    - path: "crates/core/runtime/scripting/src/backend.rs"
      provides: "Config and log host API contract plus tests"
      contains: "mesh.config"
    - path: "crates/core/runtime/scripting/src/host_api.rs"
      provides: "Shared host API comments consistent with public backend logging/config contract"
      contains: "mesh.log"
---

<objective>
Harden the backend config and logging APIs as explicit public contracts: `mesh.config()` returns the whole settings table, `mesh.log` supports fixed levels and both call styles, and invalid levels warn without crashing backend scripts.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-backend-host-api-contract/03-CONTEXT.md
@.planning/phases/03-backend-host-api-contract/03-RESEARCH.md
@.planning/phases/03-backend-host-api-contract/03-VALIDATION.md
@.planning/phases/03-backend-host-api-contract/03-PATTERNS.md

@crates/core/runtime/scripting/src/backend.rs
@crates/core/runtime/scripting/src/host_api.rs
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| T-03-09: config helpers drift into public API before the whole-table contract stabilizes | medium | Tests and comments must expose only `mesh.config()`. |
| T-03-10: invalid log levels crash backend scripts | medium | Keep invalid levels non-fatal and logged as warnings. |
| T-03-11: logging aliases are inconsistent across call styles | low | Test callable `mesh.log(level, msg)` and named level methods together. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Lock mesh.config as whole-table API only</name>
  <files>crates/core/runtime/scripting/src/backend.rs, crates/core/runtime/scripting/src/host_api.rs</files>
  <read_first>crates/core/runtime/scripting/src/backend.rs, crates/core/runtime/scripting/src/host_api.rs, .planning/phases/03-backend-host-api-contract/03-CONTEXT.md</read_first>
  <action>Keep `mesh.config()` as a callable function returning the full plugin settings table. Do not add `mesh.config.get`, `mesh.config.get_all`, or path lookup helpers. Update host API comments in `backend.rs` and `host_api.rs` so backend config examples show only `local cfg = mesh.config()`. Strengthen `config_returns_backend_settings` so it asserts nested values from a settings object with exact fields `enabled = true`, `nested.name = "demo"`, and another nested or array value if straightforward.</action>
  <verify>nix develop -c cargo test -p mesh-core-scripting config_returns_backend_settings</verify>
  <acceptance_criteria>
    - `crates/core/runtime/scripting/src/backend.rs` contains `mesh.set(` followed by `"config"` registration.
    - `crates/core/runtime/scripting/src/backend.rs` does not contain `config.get`.
    - `crates/core/runtime/scripting/src/host_api.rs` does not contain `config.get`.
    - Test `config_returns_backend_settings` asserts `enabled` and `name` from nested settings.
    - `nix develop -c cargo test -p mesh-core-scripting config_returns_backend_settings` exits 0.
  </acceptance_criteria>
  <done>`mesh.config()` is the only public backend config API for Phase 3.</done>
</task>

<task type="auto">
  <name>Task 2: Lock mesh.log levels and call styles</name>
  <files>crates/core/runtime/scripting/src/backend.rs, crates/core/runtime/scripting/src/host_api.rs</files>
  <read_first>crates/core/runtime/scripting/src/backend.rs, crates/core/runtime/scripting/src/host_api.rs, .planning/phases/03-backend-host-api-contract/03-CONTEXT.md</read_first>
  <action>Ensure the backend host API supports exactly the public levels `debug`, `info`, `warn`, and `error` through both call styles: `mesh.log("info", "message")` and `mesh.log.info("message")`. Keep `warning` only as a compatibility alias if existing tests/scripts require it, but do not document it as one of the four public levels. Update comments to list the four public levels. Ensure named methods exist for `debug`, `info`, `warn`, and `error`.</action>
  <verify>nix develop -c cargo test -p mesh-core-scripting log_level_function_and_aliases_are_callable</verify>
  <acceptance_criteria>
    - `crates/core/runtime/scripting/src/backend.rs` contains `("debug", "debug")`.
    - `crates/core/runtime/scripting/src/backend.rs` contains `("info", "info")`.
    - `crates/core/runtime/scripting/src/backend.rs` contains `("warn", "warn")`.
    - `crates/core/runtime/scripting/src/backend.rs` contains `("error", "error")`.
    - Test `log_level_function_and_aliases_are_callable` calls `mesh.log("debug", "polling")`.
    - Test `log_level_function_and_aliases_are_callable` calls `mesh.log.debug("polling")`.
  </acceptance_criteria>
  <done>`mesh.log` exposes stable level names and both call styles.</done>
</task>

<task type="auto">
  <name>Task 3: Prove invalid log levels are non-fatal warnings</name>
  <files>crates/core/runtime/scripting/src/backend.rs</files>
  <read_first>crates/core/runtime/scripting/src/backend.rs, .planning/phases/03-backend-host-api-contract/03-CONTEXT.md</read_first>
  <action>Add a test named `invalid_log_level_is_non_fatal` using a backend script that calls `mesh.log("trace", "not public")` and then emits `{ ok = true }`. The test must assert `ctx.run_poll().unwrap().unwrap().get("ok") == true`. Keep `log_message()` behavior for unknown levels as `tracing::warn!` with the plugin id and the unknown level in the message.</action>
  <verify>nix develop -c cargo test -p mesh-core-scripting invalid_log_level_is_non_fatal</verify>
  <acceptance_criteria>
    - `crates/core/runtime/scripting/src/backend.rs` contains `invalid_log_level_is_non_fatal`.
    - `crates/core/runtime/scripting/src/backend.rs` contains `unknown log level`.
    - `crates/core/runtime/scripting/src/backend.rs` contains `tracing::warn!`.
    - `nix develop -c cargo test -p mesh-core-scripting invalid_log_level_is_non_fatal` exits 0.
  </acceptance_criteria>
  <done>Bad plugin log levels are visible but cannot crash the backend script.</done>
</task>

</tasks>

<verification>
- Run `nix develop -c cargo test -p mesh-core-scripting config_returns_backend_settings`.
- Run `nix develop -c cargo test -p mesh-core-scripting log_level_function_and_aliases_are_callable`.
- Run `nix develop -c cargo test -p mesh-core-scripting invalid_log_level_is_non_fatal`.
- Run `nix develop -c cargo test -p mesh-core-scripting backend`.
</verification>

<success_criteria>
- BHOST-03 is covered by the whole-table `mesh.config()` contract and tests.
- BHOST-04 is covered by fixed log levels, both call styles, and non-fatal invalid-level behavior.
- D-08 through D-12 are visible in this plan's `must_haves.truths`.
</success_criteria>

<output>
After completion, create `.planning/phases/03-backend-host-api-contract/03-03-SUMMARY.md`
</output>
