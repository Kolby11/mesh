---
phase: 03-backend-host-api-contract
plan: 04
type: execute
wave: 3
depends_on: ["03-03"]
files_modified:
  - crates/core/runtime/scripting/src/backend.rs
  - crates/core/runtime/backend/src/lib.rs
autonomous: true
requirements: [BHOST-05]
requirements_addressed: [BHOST-05]
user_setup: []
must_haves:
  truths:
    - "D-13: mesh.service.set_poll_interval(ms) clamps low values to 50ms."
    - "D-14: clamped interval requests emit a warning so plugin authors can see the correction."
    - "D-15: interval changes take effect after the current callback returns for init, poll, and command handlers."
  artifacts:
    - path: "crates/core/runtime/scripting/src/backend.rs"
      provides: "set_poll_interval host API clamp and warning behavior"
      contains: "set_poll_interval"
    - path: "crates/core/runtime/backend/src/lib.rs"
      provides: "Runtime interval refresh timing after callbacks"
      contains: "refresh_interval"
---

<objective>
Lock `mesh.service.set_poll_interval(ms)` as a bounded backend host API: low values clamp to 50ms with a warning, and interval changes take effect after the current callback returns.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-backend-host-api-contract/03-CONTEXT.md
@.planning/phases/03-backend-host-api-contract/03-RESEARCH.md
@.planning/phases/03-backend-host-api-contract/03-VALIDATION.md
@.planning/phases/03-backend-host-api-contract/03-PATTERNS.md

@crates/core/runtime/scripting/src/backend.rs
@crates/core/runtime/backend/src/lib.rs
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| T-03-12: plugins set near-zero poll intervals and burn CPU | high | Clamp low values to `MIN_POLL_INTERVAL_MS = 50`. |
| T-03-13: clamping silently hides plugin author mistakes | medium | Emit a warning when clamping occurs. |
| T-03-14: interval changes interrupt callbacks or take effect at inconsistent times | medium | Preserve refresh-after-callback behavior and add async runtime tests. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Clamp low poll intervals at the host API boundary with warning</name>
  <files>crates/core/runtime/scripting/src/backend.rs</files>
  <read_first>crates/core/runtime/scripting/src/backend.rs, .planning/phases/03-backend-host-api-contract/03-CONTEXT.md, .planning/phases/03-backend-host-api-contract/03-PATTERNS.md</read_first>
  <action>Add a named constant `MIN_POLL_INTERVAL_MS: u64 = 50` in `backend.rs` or another appropriate backend scripting module scope. Update the `mesh.service.set_poll_interval` host function so values below `MIN_POLL_INTERVAL_MS` store `MIN_POLL_INTERVAL_MS` instead of the requested value and call `tracing::warn!` with the plugin id, requested interval, and clamped interval. Values at or above 50ms should store unchanged. Add tests named `poll_interval_below_minimum_is_clamped` and `poll_interval_at_minimum_is_accepted`.</action>
  <verify>nix develop -c cargo test -p mesh-core-scripting poll_interval</verify>
  <acceptance_criteria>
    - `crates/core/runtime/scripting/src/backend.rs` contains `MIN_POLL_INTERVAL_MS`.
    - `crates/core/runtime/scripting/src/backend.rs` contains `tracing::warn!`.
    - `crates/core/runtime/scripting/src/backend.rs` contains `poll_interval_below_minimum_is_clamped`.
    - `poll_interval_below_minimum_is_clamped` asserts `ctx.poll_interval_ms() == 50`.
    - `nix develop -c cargo test -p mesh-core-scripting poll_interval` exits 0.
  </acceptance_criteria>
  <done>Plugin requests below 50ms are corrected and visible.</done>
</task>

<task type="auto">
  <name>Task 2: Share the minimum interval contract with the backend runtime</name>
  <files>crates/core/runtime/scripting/src/backend.rs, crates/core/runtime/backend/src/lib.rs</files>
  <read_first>crates/core/runtime/scripting/src/backend.rs, crates/core/runtime/backend/src/lib.rs</read_first>
  <action>Expose the minimum poll interval constant from scripting if needed, or duplicate it with the exact same name/value in the backend runtime if crate boundaries make sharing awkward. Update `bounded_poll_interval_ms(ctx)` so it uses `MIN_POLL_INTERVAL_MS` instead of a magic literal `50`. The function should still return `ctx.poll_interval_ms().max(MIN_POLL_INTERVAL_MS)` as a defensive runtime guard even though the host API clamps values earlier.</action>
  <verify>nix develop -c cargo test -p mesh-core-backend spawn_backend_service</verify>
  <acceptance_criteria>
    - `crates/core/runtime/backend/src/lib.rs` contains `MIN_POLL_INTERVAL_MS`.
    - `crates/core/runtime/backend/src/lib.rs` does not contain `.max(50)`.
    - `crates/core/runtime/backend/src/lib.rs` contains `.max(MIN_POLL_INTERVAL_MS)`.
    - `nix develop -c cargo test -p mesh-core-backend spawn_backend_service` exits 0.
  </acceptance_criteria>
  <done>Runtime defensive clamping uses the same named contract value.</done>
</task>

<task type="auto">
  <name>Task 3: Prove interval changes take effect after callbacks</name>
  <files>crates/core/runtime/backend/src/lib.rs</files>
  <read_first>crates/core/runtime/backend/src/lib.rs, crates/core/runtime/scripting/src/backend.rs, .planning/phases/03-backend-host-api-contract/03-CONTEXT.md</read_first>
  <action>Strengthen async backend runtime tests so interval changes made in `init()`, `on_poll()`, and command handlers take effect only after the current callback returns. Existing `spawn_backend_service_applies_runtime_poll_interval_changes` may be expanded or split. Add a command-handler case named `spawn_backend_service_applies_command_interval_change_after_handler` where `on_command_fast()` calls `mesh.service.set_poll_interval(60)` and emits a payload, then the following poll cadence uses 60ms. Keep bounded `tokio::time::timeout` waits so tests do not hang.</action>
  <verify>nix develop -c cargo test -p mesh-core-backend spawn_backend_service</verify>
  <acceptance_criteria>
    - `crates/core/runtime/backend/src/lib.rs` contains `spawn_backend_service_applies_command_interval_change_after_handler`.
    - `crates/core/runtime/backend/src/lib.rs` contains `mesh.service.set_poll_interval(60)`.
    - `crates/core/runtime/backend/src/lib.rs` contains `refresh_interval(&ctx, &mut interval_ms, &mut tick);` after command handling.
    - `nix develop -c cargo test -p mesh-core-backend spawn_backend_service` exits 0.
  </acceptance_criteria>
  <done>Interval timing is locked for init, poll, and command callbacks.</done>
</task>

</tasks>

<verification>
- Run `nix develop -c cargo test -p mesh-core-scripting poll_interval`.
- Run `nix develop -c cargo test -p mesh-core-backend spawn_backend_service`.
- Run `nix develop -c cargo test -p mesh-core-scripting backend`.
</verification>

<success_criteria>
- BHOST-05 is covered by host API clamp tests and backend runtime timing tests.
- D-13 through D-15 are visible in this plan's `must_haves.truths`.
- Poll interval changes remain post-callback, not mid-callback.
</success_criteria>

<output>
After completion, create `.planning/phases/03-backend-host-api-contract/03-04-SUMMARY.md`
</output>
