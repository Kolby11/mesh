---
phase: 55-effects-layers-shadows-blur-images-and-gradients
plan: 04
type: execute
wave: 4
depends_on:
  - 55-03
autonomous: true
files_modified:
  - crates/core/frontend/render/src/surface/painter/backend.rs
  - crates/core/frontend/render/src/display_list.rs
  - crates/core/frontend/render/src/surface/painter/tests.rs
  - crates/core/shell/src/shell/component/tests/integration/real_surfaces.rs
requirements:
  - EFFECT-01
  - EFFECT-03
  - LAYER-01
requirements_addressed: [EFFECT-01, EFFECT-03, LAYER-01]
tags:
  - diagnostics
  - visual-bounds
  - retained-rendering
must_haves:
  truths:
    - "D-09: Unsupported combinations, excessive blur, unsupported blend modes, missing assets, deferred image forms, and backend capability gaps produce concise painter diagnostics."
    - "D-10: Diagnostics are non-fatal for authoring/runtime paths unless existing parser or manifest invariants are violated."
    - "D-11: Capability checks stay backend-neutral."
    - "D-12: Verification focuses on command lowering, Skia pixels, retained replay parity, diagnostics, and clipped/out-of-bounds effect cases."
    - "D-13: Visual-bounds fixtures cover shadow/filter/image/gradient output without broad Phase 57 damage-policy redesign."
    - "D-14: Animation-specific invalidation and animated visual bounds stay out of Phase 55."
  artifacts:
    - path: "crates/core/frontend/render/src/surface/painter/backend.rs"
      provides: "Painter diagnostics with source context and blur/image/capability checks"
      contains: "MAX_EFFECT_BLUR_RADIUS"
    - path: "crates/core/frontend/render/src/display_list.rs"
      provides: "Visual clip and damage fixtures for Phase 55 effects"
      contains: "display_list_effect"
---

<objective>
Make unsupported Phase 55 effect behavior inspectable and prove retained visual bounds include supported effect output.
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 55-04-01: Add painter diagnostics for unsupported effect and asset cases</name>
  <files>crates/core/frontend/render/src/surface/painter/backend.rs crates/core/frontend/render/src/surface/painter/tests.rs</files>
  <read_first>
    - .planning/phases/55-effects-layers-shadows-blur-images-and-gradients/55-CONTEXT.md
    - .planning/REQUIREMENTS.md
    - crates/core/frontend/render/src/surface/painter/backend.rs
    - crates/core/frontend/render/src/surface/painter/tests.rs
  </read_first>
  <behavior>
    - Add `MAX_EFFECT_BLUR_RADIUS: f32 = 96.0`.
    - Add a source-context field to `PainterDiagnostic` that can carry `node_id` and `property` when available.
    - Emit diagnostics for blur radius above `MAX_EFFECT_BLUR_RADIUS`, missing image path, unsupported image source, unsupported blend mode, and backend capability mismatch.
    - Add tests named `painter_effect_diagnostic_reports_excessive_blur`, `painter_effect_diagnostic_reports_missing_image`, and `painter_effect_diagnostic_reports_unsupported_blend_mode`.
  </behavior>
  <action>
    Extend `PainterDiagnostic` with `source: Option<PainterDiagnosticSource>`. Add `PainterDiagnosticSource { node_id: Option<NodeId>, property: Option<String> }` using `mesh_core_elements::NodeId`. Where backend commands lack node context, use `None`. Add `MAX_EFFECT_BLUR_RADIUS` and validate `VisualFilter.blur_radius` before executing `PainterFilter::Blur`, `PainterFilter::Backdrop`, and `PainterPaint.filter`. Missing image paths must push an `UnsupportedPainterFeature::Image` diagnostic whose message contains exactly `missing image asset`.
  </action>
  <verify>
    cargo test -p mesh-core-render painter_effect_diagnostic -- --nocapture
  </verify>
  <acceptance_criteria>
    - `rg "MAX_EFFECT_BLUR_RADIUS" crates/core/frontend/render/src/surface/painter/backend.rs` finds the constant.
    - `rg "struct PainterDiagnosticSource" crates/core/frontend/render/src/surface/painter/backend.rs` finds the source context struct.
    - `rg "missing image asset" crates/core/frontend/render/src/surface/painter/backend.rs crates/core/frontend/render/src/surface/painter/tests.rs` finds the diagnostic string.
    - `rg "painter_effect_diagnostic_reports_excessive_blur" crates/core/frontend/render/src/surface/painter/tests.rs` finds the excessive blur test.
    - `rg "painter_effect_diagnostic_reports_missing_image" crates/core/frontend/render/src/surface/painter/tests.rs` finds the missing image test.
    - `rg "painter_effect_diagnostic_reports_unsupported_blend_mode" crates/core/frontend/render/src/surface/painter/tests.rs` finds the blend diagnostic test.
    - `cargo test -p mesh-core-render painter_effect_diagnostic -- --nocapture` passes.
  </acceptance_criteria>
  <done>Unsupported Phase 55 effect cases emit explicit non-fatal diagnostics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 55-04-02: Add visual-bounds and shipped-surface proof for effects</name>
  <files>crates/core/frontend/render/src/display_list.rs crates/core/frontend/render/src/surface/painter/tests.rs crates/core/shell/src/shell/component/tests/integration/real_surfaces.rs</files>
  <read_first>
    - .planning/phases/55-effects-layers-shadows-blur-images-and-gradients/55-CONTEXT.md
    - crates/core/frontend/render/src/display_list.rs
    - crates/core/frontend/render/src/surface/painter/tests.rs
    - crates/core/shell/src/shell/component/tests/integration/real_surfaces.rs
  </read_first>
  <behavior>
    - Add display-list tests named `display_list_effect_visual_clip_includes_shadow_and_filter_overflow`, `display_list_effect_visual_clip_includes_image_bounds`, and `display_list_effect_visual_clip_includes_gradient_bounds`.
    - Add painter pixel tests named `painter_effect_clipped_shadow_stays_inside_clip` and `painter_effect_gradient_respects_rounded_clip`.
    - Add or update a shipped navigation/audio regression test to assert no painter diagnostics are emitted for supported shipped surface styles.
  </behavior>
  <action>
    In `display_list.rs`, keep existing shadow/filter visual bound expansion and add fixtures proving image/gradient background output has at least the node layout bounds included. Do not expand image/gradient bounds outside layout unless the implemented style data includes an explicit out-of-layout mode. In `tests.rs`, add clipped shadow and rounded gradient pixel tests. In shell real-surface tests, add an assertion that shipped navigation/audio paint diagnostics do not contain `missing image asset`, `unsupported background-image`, or `excessive blur`.
  </action>
  <verify>
    cargo test -p mesh-core-render display_list_effect -- --nocapture
    cargo test -p mesh-core-render painter_effect -- --nocapture
    cargo test -p mesh-core-shell shipped_navigation -- --nocapture
  </verify>
  <acceptance_criteria>
    - `rg "display_list_effect_visual_clip_includes_shadow_and_filter_overflow" crates/core/frontend/render/src/display_list.rs` finds the overflow test.
    - `rg "display_list_effect_visual_clip_includes_image_bounds" crates/core/frontend/render/src/display_list.rs` finds the image bounds test.
    - `rg "display_list_effect_visual_clip_includes_gradient_bounds" crates/core/frontend/render/src/display_list.rs` finds the gradient bounds test.
    - `rg "painter_effect_clipped_shadow_stays_inside_clip" crates/core/frontend/render/src/surface/painter/tests.rs` finds the clipped shadow test.
    - `rg "painter_effect_gradient_respects_rounded_clip" crates/core/frontend/render/src/surface/painter/tests.rs` finds the rounded gradient test.
    - `cargo test -p mesh-core-render display_list_effect -- --nocapture` passes.
    - `cargo test -p mesh-core-render painter_effect -- --nocapture` passes.
    - `cargo test -p mesh-core-shell shipped_navigation -- --nocapture` passes or the existing closest shipped navigation/audio real-surface test command passes with the new assertion.
  </acceptance_criteria>
  <done>Supported Phase 55 effects have visual-bounds and clipped rendering proof without Phase 57 damage-policy redesign.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Mitigation Plan |
|---|---|---|---|
| T-55-07 | Repudiation | painter diagnostics | Include backend id, feature id, message, and optional source context in diagnostics. |
| T-55-08 | Denial of service | excessive blur | Bound supported blur radius with `MAX_EFFECT_BLUR_RADIUS` and diagnose larger values. |
</threat_model>

<verification>
```bash
cargo test -p mesh-core-render painter_effect_diagnostic -- --nocapture
cargo test -p mesh-core-render display_list_effect -- --nocapture
cargo test -p mesh-core-render painter_effect -- --nocapture
```
</verification>

<success_criteria>
Plan 04 is complete when unsupported effect cases diagnose, excessive blur is bounded, and visual-bounds/clipping tests cover supported effect output.
</success_criteria>
