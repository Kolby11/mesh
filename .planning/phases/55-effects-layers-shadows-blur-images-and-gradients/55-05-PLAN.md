---
phase: 55-effects-layers-shadows-blur-images-and-gradients
plan: 05
type: execute
wave: 5
depends_on:
  - 55-01
  - 55-02
  - 55-03
  - 55-04
autonomous: true
files_modified:
  - crates/core/frontend/render/src/surface/painter/backend.rs
  - crates/core/frontend/render/src/surface/painter/tests.rs
  - crates/core/frontend/render/src/display_list.rs
  - crates/core/ui/elements/src/style.rs
  - .planning/phases/55-effects-layers-shadows-blur-images-and-gradients/55-VALIDATION.md
requirements:
  - EFFECT-01
  - EFFECT-02
  - EFFECT-03
  - LAYER-01
requirements_addressed: [EFFECT-01, EFFECT-02, EFFECT-03, LAYER-01]
tags:
  - validation
  - backend-neutrality
  - shipped-proof
must_haves:
  truths:
    - "D-12: Final verification covers command lowering, Skia effect pixels, retained replay parity, diagnostics, and clipped/out-of-bounds cases."
    - "D-13: Visual-bounds fixtures are present for supported effect output."
    - "D-14: Phase 55 does not implement animation-specific invalidation or animated visual bounds."
    - "D-15: Shipped navigation/audio surfaces remain compatibility proof while synthetic fixtures cover unsupported combinations."
    - "All Phase 55 requirements EFFECT-01, EFFECT-02, EFFECT-03, and LAYER-01 are covered by automated tests."
  artifacts:
    - path: ".planning/phases/55-effects-layers-shadows-blur-images-and-gradients/55-VALIDATION.md"
      provides: "Nyquist validation completion metadata"
      contains: "nyquist_compliant: true"
---

<objective>
Run final Phase 55 verification, prove backend-neutral retained data, and mark validation complete.
</objective>

<tasks>

<task type="auto" tdd="false">
  <name>Task 55-05-01: Run full effect suite and backend-neutrality proof</name>
  <files>crates/core/frontend/render/src/surface/painter/backend.rs crates/core/frontend/render/src/surface/painter/tests.rs crates/core/frontend/render/src/display_list.rs crates/core/ui/elements/src/style.rs .planning/phases/55-effects-layers-shadows-blur-images-and-gradients/55-VALIDATION.md</files>
  <read_first>
    - .planning/phases/55-effects-layers-shadows-blur-images-and-gradients/55-CONTEXT.md
    - .planning/phases/55-effects-layers-shadows-blur-images-and-gradients/55-RESEARCH.md
    - .planning/phases/55-effects-layers-shadows-blur-images-and-gradients/55-VALIDATION.md
    - crates/core/frontend/render/src/surface/painter/backend.rs
    - crates/core/frontend/render/src/surface/painter/tests.rs
    - crates/core/frontend/render/src/display_list.rs
    - crates/core/frontend/render/src/render_object.rs
    - crates/core/ui/elements/src/style.rs
  </read_first>
  <behavior>
    - Run all Phase 55 focused renderer/style test suites.
    - Run backend-neutrality grep for `skia_safe` leakage.
    - Update `55-VALIDATION.md` frontmatter to `status: complete`, `nyquist_compliant: true`, and `wave_0_complete: true` only after tests pass.
    - Ensure any unsupported effect still has an explicit diagnostic test or deferred note.
  </behavior>
  <action>
    Execute the full commands in this plan's `<verification>` block. If any test fails, fix the failing implementation or test before updating validation metadata. After all commands pass, edit `.planning/phases/55-effects-layers-shadows-blur-images-and-gradients/55-VALIDATION.md` so the frontmatter contains exactly `status: complete`, `nyquist_compliant: true`, and `wave_0_complete: true`.
  </action>
  <verify>
    cargo test -p mesh-core-elements style_background -- --nocapture
    cargo test -p mesh-core-render painter_effect -- --nocapture
    cargo test -p mesh-core-render display_list_effect -- --nocapture
    cargo test -p mesh-core-render skia_effect_layer -- --nocapture
    cargo test -p mesh-core-render skia_effect_image_gradient -- --nocapture
    rg "skia_safe" crates/core/frontend/render/src/display_list.rs crates/core/frontend/render/src/render_object.rs crates/core/ui/elements/src && exit 1 || exit 0
  </verify>
  <acceptance_criteria>
    - `cargo test -p mesh-core-elements style_background -- --nocapture` exits 0.
    - `cargo test -p mesh-core-render painter_effect -- --nocapture` exits 0.
    - `cargo test -p mesh-core-render display_list_effect -- --nocapture` exits 0.
    - `cargo test -p mesh-core-render skia_effect -- --nocapture` exits 0 or the implemented focused commands `skia_effect_layer` and `skia_effect_image_gradient` both exit 0.
    - `rg "skia_safe" crates/core/frontend/render/src/display_list.rs crates/core/frontend/render/src/render_object.rs crates/core/ui/elements/src && exit 1 || exit 0` exits 0.
    - `rg "status: complete" .planning/phases/55-effects-layers-shadows-blur-images-and-gradients/55-VALIDATION.md` finds the completed status.
    - `rg "nyquist_compliant: true" .planning/phases/55-effects-layers-shadows-blur-images-and-gradients/55-VALIDATION.md` finds the compliant flag.
    - `rg "wave_0_complete: true" .planning/phases/55-effects-layers-shadows-blur-images-and-gradients/55-VALIDATION.md` finds the wave 0 flag.
  </acceptance_criteria>
  <done>Phase 55 has full automated proof and validation metadata is complete.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Mitigation Plan |
|---|---|---|---|
| T-55-09 | Tampering | retained backend-neutral data | Run grep proving `skia_safe` did not leak into retained display-list, render-object, or style data. |
| T-55-10 | Repudiation | phase validation | Update validation metadata only after commands pass and keep command outputs in the plan summary. |
</threat_model>

<verification>
```bash
cargo test -p mesh-core-elements style_background -- --nocapture
cargo test -p mesh-core-render painter_effect -- --nocapture
cargo test -p mesh-core-render display_list_effect -- --nocapture
cargo test -p mesh-core-render skia_effect_layer -- --nocapture
cargo test -p mesh-core-render skia_effect_image_gradient -- --nocapture
rg "skia_safe" crates/core/frontend/render/src/display_list.rs crates/core/frontend/render/src/render_object.rs crates/core/ui/elements/src && exit 1 || exit 0
```
</verification>

<success_criteria>
Plan 05 is complete when all Phase 55 focused tests and backend-neutrality grep pass, unsupported gaps remain diagnosed or deferred, and `55-VALIDATION.md` is marked complete.
</success_criteria>
