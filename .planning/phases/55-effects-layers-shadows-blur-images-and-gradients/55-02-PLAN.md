---
phase: 55-effects-layers-shadows-blur-images-and-gradients
plan: 02
type: execute
wave: 2
depends_on:
  - 55-01
autonomous: true
files_modified:
  - crates/core/frontend/render/src/surface/painter/backend.rs
  - crates/core/frontend/render/src/surface/painter/tree.rs
  - crates/core/frontend/render/src/display_list.rs
  - crates/core/frontend/render/src/surface/painter/tests.rs
requirements:
  - EFFECT-01
  - EFFECT-02
  - LAYER-01
requirements_addressed: [EFFECT-01, EFFECT-02, LAYER-01]
tags:
  - painter
  - retained-display-list
  - command-lowering
must_haves:
  truths:
    - "D-01: Skia owns supported effect execution while MESH owns layer creation, retained ordering, z-index, clipping intent, damage inputs, and command filtering."
    - "D-02: Opacity, non-default blend behavior, filter, backdrop-filter, and clip/effect combinations lower into explicit painter commands instead of helper bypasses."
    - "D-03: Layer insertion is minimal; plain background/border/shadow paths remain simple command sequences when isolation is unnecessary."
    - "D-04: Direct widget-tree and retained display-list effect lowering preserve command-class parity."
    - "D-05: Image and gradient commands stay backend-neutral."
  artifacts:
    - path: "crates/core/frontend/render/src/surface/painter/backend.rs"
      provides: "Painter command variants for linear gradient and richer image data"
      contains: "DrawLinearGradient"
    - path: "crates/core/frontend/render/src/surface/painter/tests.rs"
      provides: "Direct/retained effect command-class parity tests"
      contains: "painter_effect_lowering"
---

<objective>
Lower Phase 55 effect, image, and gradient style data into explicit backend-neutral painter command classes for both direct and retained render paths.
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 55-02-01: Add painter image and gradient command data</name>
  <files>crates/core/frontend/render/src/surface/painter/backend.rs crates/core/frontend/render/src/surface/painter/tests.rs</files>
  <read_first>
    - .planning/phases/55-effects-layers-shadows-blur-images-and-gradients/55-CONTEXT.md
    - .planning/phases/55-effects-layers-shadows-blur-images-and-gradients/55-RESEARCH.md
    - crates/core/frontend/render/src/surface/painter/backend.rs
    - crates/core/frontend/render/src/surface/painter/tests.rs
  </read_first>
  <behavior>
    - Replace or extend `PainterImage { id: String }` with backend-neutral source data that can represent a relative style image path.
    - Add `PainterLinearGradient { from: Color, to: Color }`.
    - Add `PainterCommand::DrawLinearGradient { gradient: PainterLinearGradient, rect: ClipRect, radius: f32, clip: ClipRect }`.
    - Extend the test helper `painter_command_classes` so the gradient command maps to `draw_linear_gradient`.
  </behavior>
  <action>
    In `backend.rs`, add `PainterImageSource::Path(String)` and update `PainterImage` to contain `source: PainterImageSource`. Add `PainterLinearGradient` with `from: Color` and `to: Color`. Add the `DrawLinearGradient` command variant exactly as listed in behavior. In `tests.rs`, update every existing `PainterImage { id: "img".into() }` fixture to use `PainterImage { source: PainterImageSource::Path("img".into()) }`, and update `painter_command_classes` to return `draw_linear_gradient` for the new command.
  </action>
  <verify>
    cargo test -p mesh-core-render painter_command -- --nocapture
  </verify>
  <acceptance_criteria>
    - `rg "enum PainterImageSource" crates/core/frontend/render/src/surface/painter/backend.rs` finds the enum.
    - `rg "struct PainterLinearGradient" crates/core/frontend/render/src/surface/painter/backend.rs` finds the struct.
    - `rg "DrawLinearGradient" crates/core/frontend/render/src/surface/painter/backend.rs crates/core/frontend/render/src/surface/painter/tests.rs` finds the command and test helper coverage.
    - `rg "PainterImage \\{ id:" crates/core/frontend/render/src/surface/painter/tests.rs` returns no matches.
    - `cargo test -p mesh-core-render painter_command -- --nocapture` passes.
  </acceptance_criteria>
  <done>Painter commands can represent backend-neutral image paths and linear gradients.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 55-02-02: Lower background image and gradient commands in direct and retained paths</name>
  <files>crates/core/frontend/render/src/surface/painter/tree.rs crates/core/frontend/render/src/display_list.rs crates/core/frontend/render/src/surface/painter/tests.rs</files>
  <read_first>
    - .planning/phases/55-effects-layers-shadows-blur-images-and-gradients/55-CONTEXT.md
    - .planning/phases/55-effects-layers-shadows-blur-images-and-gradients/55-PATTERNS.md
    - crates/core/frontend/render/src/surface/painter/tree.rs
    - crates/core/frontend/render/src/display_list.rs
    - crates/core/frontend/render/src/surface/painter/tests.rs
    - crates/core/ui/elements/src/style/types.rs
  </read_first>
  <behavior>
    - Add shared direct/retained lowering so `BackgroundPaint::Image` emits `PainterCommand::DrawImage`.
    - Add shared direct/retained lowering so `BackgroundPaint::LinearGradient` emits `PainterCommand::DrawLinearGradient`.
    - Add tests named `painter_effect_lowering_direct_and_retained_image_emit_same_command_classes` and `painter_effect_lowering_direct_and_retained_gradient_emit_same_command_classes`.
    - Retained display-list signatures rebuild when background image path or gradient colors change.
  </behavior>
  <action>
    In `display_list.rs`, copy `ComputedStyle.background_paint` into retained display-node style data, include it in display-list entry signatures, and add a test named `display_list_effect_rebuilds_when_background_paint_changes`. In `tree.rs`, draw background image/gradient commands in the same node slot where background color is currently drawn, before border drawing and after shadow/backdrop handling. Use the same `bounds`, `radius`, and `node_clip`/`clip` behavior as background fills. Add command-class parity tests in `tests.rs` using `RecordingPaintBackend`.
  </action>
  <verify>
    cargo test -p mesh-core-render painter_effect_lowering -- --nocapture
    cargo test -p mesh-core-render display_list_effect -- --nocapture
  </verify>
  <acceptance_criteria>
    - `rg "background_paint" crates/core/frontend/render/src/display_list.rs crates/core/frontend/render/src/surface/painter/tree.rs` finds retained data and lowering usage.
    - `rg "painter_effect_lowering_direct_and_retained_image_emit_same_command_classes" crates/core/frontend/render/src/surface/painter/tests.rs` finds the image parity test.
    - `rg "painter_effect_lowering_direct_and_retained_gradient_emit_same_command_classes" crates/core/frontend/render/src/surface/painter/tests.rs` finds the gradient parity test.
    - `rg "display_list_effect_rebuilds_when_background_paint_changes" crates/core/frontend/render/src/display_list.rs` finds the retained rebuild test.
    - `cargo test -p mesh-core-render painter_effect_lowering -- --nocapture` passes.
    - `cargo test -p mesh-core-render display_list_effect -- --nocapture` passes.
  </acceptance_criteria>
  <done>Direct and retained render paths emit equivalent image and gradient command classes.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Mitigation Plan |
|---|---|---|---|
| T-55-03 | Tampering | retained effect lowering | Compare direct and retained command classes for image and gradient nodes. |
| T-55-04 | Information disclosure | image source data | Keep image source as relative/backend-neutral path data; do not embed absolute system paths in retained style data. |
</threat_model>

<verification>
```bash
cargo test -p mesh-core-render painter_effect_lowering -- --nocapture
cargo test -p mesh-core-render display_list_effect -- --nocapture
```
</verification>

<success_criteria>
Plan 02 is complete when background image and linear-gradient style data lower into backend-neutral painter commands with direct/retained parity and retained rebuild proof.
</success_criteria>
