---
phase: 55-effects-layers-shadows-blur-images-and-gradients
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
files_modified:
  - crates/core/ui/elements/src/style/types.rs
  - crates/core/ui/elements/src/style/parse.rs
  - crates/core/ui/elements/src/style/resolve.rs
  - crates/core/ui/elements/src/style.rs
  - crates/core/frontend/render/src/render_object.rs
requirements:
  - EFFECT-02
  - EFFECT-03
requirements_addressed: [EFFECT-02, EFFECT-03]
tags:
  - painter
  - style-profile
  - backend-neutral-data
must_haves:
  truths:
    - "D-05: Images and gradients are represented in backend-neutral painter/style data; no skia_safe types enter mesh-core-elements, render-object data, display-list data, or style data."
    - "D-06: Missing, inaccessible, or unsupported image assets emit diagnostics instead of silently painting nothing."
    - "D-07: Gradients start with compact linear-gradient support sufficient for shell UI backgrounds."
    - "D-08: Existing image/icon cache and profiling concepts may be reused, but cache expansion is not the goal of this phase."
    - "D-16 and D-17: Phase 31 transition polish and module install requirement resolution are not folded into Phase 55 scope."
  artifacts:
    - path: "crates/core/ui/elements/src/style/types.rs"
      provides: "Backend-neutral background image and linear-gradient style data"
      contains: "BackgroundPaint"
    - path: "crates/core/ui/elements/src/style.rs"
      provides: "Style profile tests for background image/gradient support and diagnostics"
      contains: "style_background"
---

<objective>
Add backend-neutral style/profile data for bounded background images and linear gradients, with diagnostics for unsupported values.
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 55-01-01: Add backend-neutral background paint style structs</name>
  <files>crates/core/ui/elements/src/style/types.rs crates/core/ui/elements/src/style/parse.rs crates/core/ui/elements/src/style/resolve.rs crates/core/frontend/render/src/render_object.rs</files>
  <read_first>
    - .planning/phases/55-effects-layers-shadows-blur-images-and-gradients/55-CONTEXT.md
    - .planning/phases/55-effects-layers-shadows-blur-images-and-gradients/55-RESEARCH.md
    - .planning/phases/55-effects-layers-shadows-blur-images-and-gradients/55-PATTERNS.md
    - crates/core/ui/elements/src/style/types.rs
    - crates/core/ui/elements/src/style/parse.rs
    - crates/core/ui/elements/src/style/resolve.rs
    - crates/core/frontend/render/src/render_object.rs
  </read_first>
  <behavior>
    - Add a backend-neutral `BackgroundPaint` enum with variants `None`, `Image(StyleImageSource)`, and `LinearGradient(StyleLinearGradient)`.
    - Add `StyleImageSource { path: String }`.
    - Add `StyleLinearGradient { from: Color, to: Color }`.
    - Add `background_paint: BackgroundPaint` to `ComputedStyle`, defaulting to `BackgroundPaint::None`.
    - Include `background_paint` in render-object material dirty hashing so retained paint rebuilds when image/gradient style changes.
  </behavior>
  <action>
    Extend the style data model without introducing Skia types. Implement `Default` for all new structs/enums. In `parse.rs`, add `parse_background_image(value: &str) -> BackgroundPaint` supporting exactly `none`, `url("relative/path.png")`, `url('relative/path.png')`, `url(relative/path.png)`, `linear-gradient(#112233, #445566)`, and `linear-gradient(to bottom, #112233, #445566)`. In `resolve.rs`, add a `background-image` property arm that resolves variables first, then calls `parse_background_image`. In `render_object.rs`, hash `BackgroundPaint::None`, image path strings, and gradient `from`/`to` colors in the material slot.
  </action>
  <verify>
    cargo test -p mesh-core-elements style_background -- --nocapture
    cargo test -p mesh-core-render render_object_tree_marks -- --nocapture
  </verify>
  <acceptance_criteria>
    - `rg "pub enum BackgroundPaint" crates/core/ui/elements/src/style/types.rs` finds the enum.
    - `rg "pub struct StyleImageSource" crates/core/ui/elements/src/style/types.rs` finds the struct.
    - `rg "pub struct StyleLinearGradient" crates/core/ui/elements/src/style/types.rs` finds the struct.
    - `rg "background_paint: BackgroundPaint" crates/core/ui/elements/src/style/types.rs` finds the `ComputedStyle` field.
    - `rg "fn parse_background_image" crates/core/ui/elements/src/style/parse.rs` finds the parser.
    - `rg "\"background-image\"" crates/core/ui/elements/src/style/resolve.rs` finds the resolver arm.
    - `rg "background_paint" crates/core/frontend/render/src/render_object.rs` finds retained material hashing.
  </acceptance_criteria>
  <done>ComputedStyle can carry backend-neutral background image and linear-gradient data into retained rendering.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 55-01-02: Add bounded style diagnostics and parser tests</name>
  <files>crates/core/ui/elements/src/style.rs crates/core/ui/elements/src/style/types.rs crates/core/ui/elements/src/style/parse.rs crates/core/ui/elements/src/style/resolve.rs</files>
  <read_first>
    - .planning/phases/55-effects-layers-shadows-blur-images-and-gradients/55-CONTEXT.md
    - .planning/REQUIREMENTS.md
    - crates/core/ui/elements/src/style.rs
    - crates/core/ui/elements/src/style/types.rs
    - crates/core/ui/elements/src/style/parse.rs
    - crates/core/ui/elements/src/style/resolve.rs
  </read_first>
  <behavior>
    - Add tests named `style_background_image_url_resolves_backend_neutral_source`, `style_background_linear_gradient_resolves_two_colors`, and `style_background_image_unsupported_value_records_diagnostic`.
    - Unsupported `background-image` values must produce a diagnostic on property `background-image` containing `unsupported background-image`.
    - Keep `linear-gradient` profile metadata diagnostic-only or implemented consistently with the new supported parser behavior.
  </behavior>
  <action>
    Extend style diagnostics so unsupported `background-image` values like `radial-gradient(#000, #fff)` and `url(http://example.test/image.png)` do not silently become `BackgroundPaint::None`. The resolver should record a diagnostic with property `background-image` and message containing exactly `unsupported background-image`. The parser should accept only relative `url(...)` paths and the two-color/top-to-bottom linear gradient forms listed in Task 55-01-01.
  </action>
  <verify>
    cargo test -p mesh-core-elements style_background -- --nocapture
  </verify>
  <acceptance_criteria>
    - `rg "style_background_image_url_resolves_backend_neutral_source" crates/core/ui/elements/src/style.rs` finds the test.
    - `rg "style_background_linear_gradient_resolves_two_colors" crates/core/ui/elements/src/style.rs` finds the test.
    - `rg "style_background_image_unsupported_value_records_diagnostic" crates/core/ui/elements/src/style.rs` finds the test.
    - `rg "unsupported background-image" crates/core/ui/elements/src/style.rs crates/core/ui/elements/src/style/resolve.rs` finds the diagnostic string.
    - `cargo test -p mesh-core-elements style_background -- --nocapture` passes.
  </acceptance_criteria>
  <done>Bounded image/gradient style syntax is tested and unsupported browser-like values diagnose.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Mitigation Plan |
|---|---|---|---|
| T-55-01 | Tampering | style/render data boundary | Keep all new image/gradient structs backend-neutral and add backend-neutrality grep in final verification. |
| T-55-02 | Repudiation | unsupported author CSS | Add named diagnostics for unsupported `background-image` values so missing visuals are inspectable. |
</threat_model>

<verification>
```bash
cargo test -p mesh-core-elements style_background -- --nocapture
cargo test -p mesh-core-render render_object_tree_marks -- --nocapture
```
</verification>

<success_criteria>
Plan 01 is complete when backend-neutral background image/gradient style data exists, unsupported values diagnose, and retained render-object material hashing observes the new style data.
</success_criteria>
