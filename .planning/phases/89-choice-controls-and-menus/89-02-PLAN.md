---
phase: 89-choice-controls-and-menus
plan: 02
title: Shell Choice And Menu Behavior
type: execute
wave: 2
depends_on: [89-01]
files_modified:
  - crates/core/shell/src/shell/component/runtime_tree.rs
  - crates/core/shell/src/shell/component/input/mod.rs
  - crates/core/shell/src/shell/component/input/focus.rs
  - crates/core/shell/src/shell/component/input/widgets.rs
autonomous: true
requirements: [ELEMCHOICE-02, ELEMCHOICE-03, ELEMMENU-02, ELEMMENU-03]
requirements_addressed: [ELEMCHOICE-02, ELEMCHOICE-03, ELEMMENU-02, ELEMMENU-03]
---

<objective>
Add source-tag-aware runtime behavior for choice values, checkable choices, radio groups, select option activation, and menu roving focus.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Add source-aware interaction helpers</name>
  <files>crates/core/shell/src/shell/component/runtime_tree.rs, crates/core/shell/src/shell/component/input/*.rs</files>
  <action>Replace runtime-tag-only checks with helper predicates using `data-mesh-element` where appropriate for focusable, text input, checkable, radio, select, option, menu, and menu item behavior.</action>
  <verify>nix develop -c cargo test -p mesh-core-shell phase89</verify>
</task>

<task type="auto">
  <name>Task 2: Implement selection and activation</name>
  <files>crates/core/shell/src/shell/component/input/mod.rs, crates/core/shell/src/shell/component/input/widgets.rs</files>
  <action>Dispatch `change` with option values for select/radio choices, maintain checked state for checkboxes/switches/radios, and dispatch `activate`/`click` for enabled menu items.</action>
  <verify>nix develop -c cargo test -p mesh-core-shell phase89</verify>
</task>

<task type="auto">
  <name>Task 3: Implement keyboard behavior</name>
  <files>crates/core/shell/src/shell/component/input/mod.rs, crates/core/shell/src/shell/component/input/focus.rs</files>
  <action>Add ArrowUp/ArrowDown roving focus for select/options and menu/menu-item groups, Enter/Space activation for options and menu items, and disabled-item skipping.</action>
  <verify>nix develop -c cargo test -p mesh-core-shell phase89</verify>
</task>

</tasks>
