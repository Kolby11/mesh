---
phase: 41-shipped-module-proof-and-documentation
plan: 02
title: Shell Runtime Proof
type: execute
wave: 2
depends_on: [41-01]
files_modified:
  - crates/core/shell/src/shell/discovery.rs
  - crates/core/shell/src/shell/tests.rs
  - crates/core/shell/src/shell/component/tests/interaction/navigation.rs
autonomous: true
requirements: [PROOF-01]
requirements_addressed: [PROOF-01]
user_setup: []
must_haves:
  truths:
    - "D-01: Shell proof uses the shipped audio/navigation path, not a toy module."
    - "D-03: The proof must not add service-specific Rust APIs."
    - "D-04: Automated proof includes real proof-module behavior."
    - "D-06: Runtime proof shows root graph entry, active provider, frontend interface consumption, contribution data, and diagnostics."
    - "D-07: Shell verification uses nix develop -c when native dependencies are required."
  artifacts:
    - path: "crates/core/shell/src/shell/tests.rs"
      provides: "Shell runtime proof for graph-driven provider and frontend choices"
      contains: "installed_module_graph"
    - path: "crates/core/shell/src/shell/discovery.rs"
      provides: "Generic graph-to-shell registration bridge"
      contains: "register_interfaces_from_graph"
  key_links:
    - "InstalledModuleGraph provider records -> InterfaceRegistry registration"
    - "InstalledModuleGraph frontend modules -> shell frontend filtering"
    - "navigation-bar component keybind metadata -> shipped behavior proof"
---

<objective>
Prove the shipped graph is used by shell runtime paths without adding direct audio-specific Rust branches: graph-derived providers register generically, the active provider drives backend launch candidates, enabled frontend filtering keeps the shipped navigation surface, and navigation behavior remains manifest/interface driven.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-shipped-module-proof-and-documentation/41-CONTEXT.md
@.planning/phases/41-shipped-module-proof-and-documentation/41-RESEARCH.md
@.planning/phases/41-shipped-module-proof-and-documentation/41-VALIDATION.md
@.planning/phases/41-shipped-module-proof-and-documentation/41-PATTERNS.md
@config/module.json
@modules/frontend/navigation-bar/module.json
@modules/frontend/navigation-bar/src/components/volume-button.mesh
@modules/interfaces/audio.toml
@crates/core/extension/module/src/package/installed_graph.rs
@crates/core/shell/src/shell/discovery.rs
@crates/core/shell/src/shell/tests.rs
@crates/core/shell/src/shell/component/tests/interaction/navigation.rs
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| T-41-02-01: shell runtime proof reintroduces direct `mesh.audio`, `pipewire`, or `pulseaudio` branches | high | Keep production code generic by interface id/provider record and add grep-style review in task acceptance criteria. |
| T-41-02-02: graph proof stops at package tests and never proves shell startup consumption | high | Add shell tests around `register_interfaces_from_graph`, frontend filtering, or backend candidate construction using the real installed graph. |
| T-41-02-03: shell tests fail outside Nix due native libraries and produce a false failure | medium | Use `nix develop -c cargo test ...` for shell verification commands. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Strengthen graph-driven shell provider and frontend tests</name>
  <files>crates/core/shell/src/shell/discovery.rs, crates/core/shell/src/shell/tests.rs</files>
  <read_first>crates/core/shell/src/shell/discovery.rs, crates/core/shell/src/shell/tests.rs, crates/core/extension/module/src/package/installed_graph.rs, config/module.json, modules/backend/pipewire-audio/module.json, modules/backend/pulseaudio-audio/module.json</read_first>
  <action>Per D-01, D-03, D-04, D-06, and D-07, extend shell tests named with `installed_module_graph` so they prove the real graph registers `mesh.audio` providers through `register_interfaces_from_graph`, selects only `@mesh/pipewire-audio` for backend launch candidates when the root graph marks it active, preserves `@mesh/pulseaudio-audio` as a provider record without launching it as active, and keeps `@mesh/navigation-bar` in enabled frontend filtering while excluding disabled `@mesh/text-selection-proof`. If production code needs a small generic helper to make this testable, add it to `discovery.rs` with interface/provider-neutral naming. Do not add direct Rust match arms or branches for `pipewire`, `pulseaudio`, or audio-specific methods.</action>
  <verify>nix develop -c cargo test -p mesh-core-shell shell::tests installed_module_graph</verify>
  <acceptance_criteria>
    - `crates/core/shell/src/shell/tests.rs` contains `installed_module_graph`.
    - `crates/core/shell/src/shell/tests.rs` contains `@mesh/navigation-bar`.
    - `crates/core/shell/src/shell/tests.rs` contains `@mesh/pipewire-audio`.
    - `crates/core/shell/src/shell/tests.rs` contains `@mesh/pulseaudio-audio`.
    - `crates/core/shell/src/shell/tests.rs` contains `mesh.audio`.
    - `crates/core/shell/src/shell/discovery.rs` does not add new audio-specific dispatch behavior; any touched helper names are interface/provider generic.
    - `nix develop -c cargo test -p mesh-core-shell shell::tests installed_module_graph` exits 0.
  </acceptance_criteria>
  <done>Shell tests prove the real shipped graph drives provider registration, active provider selection, and frontend filtering through generic installed-graph records.</done>
</task>

<task type="auto">
  <name>Task 2: Preserve shipped navigation interface/keybind behavior in focused tests</name>
  <files>crates/core/shell/src/shell/component/tests/interaction/navigation.rs</files>
  <read_first>crates/core/shell/src/shell/component/tests/interaction/navigation.rs, modules/frontend/navigation-bar/module.json, modules/frontend/navigation-bar/src/components/volume-button.mesh, crates/core/shell/src/shell/component/input/keyboard.rs</read_first>
  <action>Per D-01, D-03, D-04, D-06, and D-07, inspect the existing navigation interaction tests before editing. If current tests already prove the shipped volume button binds the manifest keybind id `mute`, dispatches through the shell keybind path, and respects interface-driven audio availability, leave production code unchanged and add only missing assertions. If coverage is missing, add a focused navigation test that mounts or annotates the real navigation component metadata, asserts the `keybind` attribute resolves to the manifest action id `mute`, asserts the accessibility `keyboard_shortcut` metadata reflects the manifest trigger, and asserts no backend module id such as `@mesh/pipewire-audio` is imported by the frontend component. Because this file has pre-existing unrelated dirty hunks, stage only Phase 41 hunks when committing.</action>
  <verify>nix develop -c cargo test -p mesh-core-shell shell::component::tests::interaction::navigation</verify>
  <acceptance_criteria>
    - `crates/core/shell/src/shell/component/tests/interaction/navigation.rs` contains a Phase 41-relevant assertion for `mute` or existing tests are explicitly referenced in the plan summary.
    - `modules/frontend/navigation-bar/src/components/volume-button.mesh` continues to contain `require, "mesh.audio@>=1.0"` and does not contain `@mesh/pipewire-audio`.
    - Navigation interaction tests pass with `nix develop -c`.
    - Unrelated pre-existing dirty hunks in `navigation.rs` are not reverted.
  </acceptance_criteria>
  <done>Shipped navigation behavior remains driven by interface imports and manifest keybind metadata, not backend module IDs.</done>
</task>

</tasks>

<verification>
- Run `nix develop -c cargo test -p mesh-core-shell shell::tests installed_module_graph`.
- Run `nix develop -c cargo test -p mesh-core-shell shell::component::tests::interaction::navigation`.
- Run `rg -n "pipewire|pulseaudio|mesh.audio" crates/core/shell/src/shell/discovery.rs crates/core/shell/src/shell/tests.rs` and inspect that new production behavior is generic rather than service-specific.
</verification>

<success_criteria>
- Shell runtime tests prove graph-derived interface/provider metadata and frontend filtering use the real shipped path.
- Active provider selection remains explicit from the root graph.
- No service-specific Rust API or direct backend-module import is added.
- Shell verification uses `nix develop -c` for native dependency reliability.
</success_criteria>

