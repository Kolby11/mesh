---
phase: 41-shipped-module-proof-and-documentation
plan: 01
title: Real Shipped Graph Proof
type: execute
wave: 1
depends_on: []
files_modified:
  - config/module.json
  - modules/interfaces/audio/module.json
  - modules/interfaces/audio/interface.toml
  - modules/icon-packs/default/module.json
  - crates/core/extension/module/src/package/tests.rs
autonomous: true
requirements: [PROOF-01]
requirements_addressed: [PROOF-01]
user_setup: []
must_haves:
  truths:
    - "D-01: The primary proof path is @mesh/navigation-bar, @mesh/audio-interface, @mesh/pipewire-audio, and @mesh/pulseaudio-audio."
    - "D-02: The real shipped path is the proof; fixture-only cases are limited to brittle isolated diagnostics."
    - "D-04: Automated proof covers manifest normalization, contribution indexing, diagnostics, and real proof-module behavior."
    - "D-05: Tests use real manifests and graph fixtures rather than mocked manifest parsing or mocked interface/provider resolution."
    - "D-06: Runtime proof starts from root graph entry, canonical manifests, active provider, frontend requirement/interface consumption, contribution data, and diagnostics."
  artifacts:
    - path: "config/module.json"
      provides: "Root installed-module graph for the shipped proof path"
      contains: "\"providers\": {"
    - path: "modules/interfaces/audio/module.json"
      provides: "Canonical interface module manifest for mesh.audio"
      contains: "\"name\": \"@mesh/audio-interface\""
    - path: "modules/icon-packs/default/module.json"
      provides: "Canonical default icon-pack manifest used by navigation icon requirements"
      contains: "\"name\": \"@mesh/icons-default\""
    - path: "crates/core/extension/module/src/package/tests.rs"
      provides: "Real shipped graph proof tests"
      contains: "shipped_module"
  key_links:
    - "config/module.json -> load_installed_module_graph -> InstalledModuleGraph typed getters"
    - "modules/frontend/navigation-bar/module.json -> frontend requirements/settings/keybind/icon contribution records"
    - "modules/backend/pipewire-audio/module.json and modules/backend/pulseaudio-audio/module.json -> mesh.audio provider records"
---

<objective>
Prove the canonical module model against the real bundled audio/navigation path by loading the root installed graph, checking shipped `module.json` manifests, asserting typed contributions and provider records, and preserving visible diagnostics for missing or incompatible proof-path data.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-shipped-module-proof-and-documentation/41-CONTEXT.md
@.planning/phases/41-shipped-module-proof-and-documentation/41-RESEARCH.md
@.planning/phases/41-shipped-module-proof-and-documentation/41-VALIDATION.md
@.planning/phases/41-shipped-module-proof-and-documentation/41-PATTERNS.md
@config/module.json
@modules/frontend/navigation-bar/module.json
@modules/interfaces/audio.toml
@modules/interfaces/audio/module.json
@modules/icon-packs/default/module.json
@modules/backend/pipewire-audio/module.json
@modules/backend/pulseaudio-audio/module.json
@crates/core/extension/module/src/package/installed_graph.rs
@crates/core/extension/module/src/package/tests.rs
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| T-41-01-01: proof uses synthetic fixtures and misses drift in shipped manifests | high | Load `config/module.json` and shipped module directories in the primary proof test; only use synthetic fixtures for isolated diagnostics that real manifests cannot exercise safely. |
| T-41-01-02: canonical path silently omits the interface/resource modules needed to make the proof credible | high | Add canonical `@mesh/audio-interface` and `@mesh/icons-default` entries to the shipped graph and assert they resolve as typed graph records. |
| T-41-01-03: contribution records exist but are not connected to the proof path | high | Assert typed graph getters for layout, frontend entrypoints, settings schemas, keybind actions, icon requirements, providers, active provider, and diagnostics from the shipped path. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Install canonical proof-path interface and icon modules</name>
  <files>config/module.json, modules/interfaces/audio/module.json, modules/interfaces/audio/interface.toml, modules/icon-packs/default/module.json</files>
  <read_first>config/module.json, modules/frontend/navigation-bar/module.json, modules/interfaces/audio.toml, modules/icon-packs/default/module.json, modules/backend/pipewire-audio/module.json, modules/backend/pulseaudio-audio/module.json, crates/core/extension/module/src/package/installed_graph.rs</read_first>
  <action>Per D-01, D-04, D-05, and D-06, make the real proof path resolvable through canonical installed-graph records. Create `modules/interfaces/audio/module.json` with top-level `name: "@mesh/audio-interface"`, `version: "1.0.0"`, and `mesh: { "apiVersion": "0.1", "kind": "interface", "interface": { "name": "mesh.audio", "version": "1.0", "file": "interface.toml", "domain": "audio", "relationship": "base" } }`. Copy the current `modules/interfaces/audio.toml` contract content into `modules/interfaces/audio/interface.toml` so graph contract loading has a file inside the module directory; leave the original `modules/interfaces/audio.toml` in place for existing references unless execution proves it is unused and updates all references in the same commit. Convert `modules/icon-packs/default/module.json` from legacy `id/type/api_version/icon_pack` shape to canonical top-level `name: "@mesh/icons-default"`, `version: "0.1.0"`, `description`, and `mesh: { "apiVersion": "0.1", "kind": "icon-pack", "icon_pack": { ...existing id, axes, mappings... } }`. Update `config/module.json` to include enabled entries for `@mesh/audio-interface` with `kind: "interface"` and `path: "interfaces/audio"`, and `@mesh/icons-default` with `kind: "icon-pack"` and `path: "icon-packs/default"`. Keep the existing active provider mapping `"mesh.audio": "@mesh/pipewire-audio"` and layout entrypoint `"@mesh/navigation-bar:main"` unchanged.</action>
  <verify>nix develop -c cargo test -p mesh-core-module package::tests shipped_module</verify>
  <acceptance_criteria>
    - `config/module.json` contains `@mesh/audio-interface`.
    - `config/module.json` contains `@mesh/icons-default`.
    - `modules/interfaces/audio/module.json` contains `"name": "@mesh/audio-interface"`.
    - `modules/interfaces/audio/module.json` contains `"kind": "interface"`.
    - `modules/interfaces/audio/module.json` contains `"name": "mesh.audio"`.
    - `modules/interfaces/audio/interface.toml` contains `name = "available"`.
    - `modules/icon-packs/default/module.json` contains `"name": "@mesh/icons-default"`.
    - `modules/icon-packs/default/module.json` contains `"kind": "icon-pack"`.
  </acceptance_criteria>
  <done>The shipped proof path includes canonical interface and icon-pack modules in the root installed graph.</done>
</task>

<task type="auto">
  <name>Task 2: Broaden the real shipped graph package proof</name>
  <files>crates/core/extension/module/src/package/tests.rs</files>
  <read_first>config/module.json, modules/frontend/navigation-bar/module.json, modules/interfaces/audio/module.json, modules/interfaces/audio/interface.toml, modules/backend/pipewire-audio/module.json, modules/backend/pulseaudio-audio/module.json, modules/icon-packs/default/module.json, crates/core/extension/module/src/package/installed_graph.rs, crates/core/extension/module/src/package/tests.rs</read_first>
  <action>Per D-01, D-04, D-05, and D-06, update the existing package graph proof or add a new test named with `shipped_module` in `crates/core/extension/module/src/package/tests.rs`. The test must call `load_installed_module_graph(&workspace_root.join("config/module.json"))`, assert `graph.module("@mesh/navigation-bar").unwrap().manifest_source == ModuleManifestSource::CanonicalModuleJson`, assert `graph.module("@mesh/audio-interface").unwrap().manifest_source == ModuleManifestSource::CanonicalModuleJson`, assert `graph.module("@mesh/icons-default").unwrap().manifest_source == ModuleManifestSource::CanonicalModuleJson`, assert `graph.layout_entrypoint().unwrap().module_id == "@mesh/navigation-bar"`, assert `graph.layout_entrypoint().unwrap().entrypoint_id == "main"`, assert `graph.declared_interface("mesh.audio").unwrap().module_id == "@mesh/audio-interface"`, assert `graph.backend_providers_for_interface("mesh.audio").len() == 2`, assert `graph.active_provider("mesh.audio").unwrap().module_id == "@mesh/pipewire-audio"`, assert a provider record exists for `@mesh/pulseaudio-audio`, assert `graph.frontend_entrypoints()` contains the navigation bar `main` entrypoint with path `src/main.mesh`, assert `graph.settings_schemas()` contains namespace `@mesh/navigation-bar`, assert `graph.keybind_actions()` contains action id `mute`, assert `graph.icon_requirements()` contains `audio-volume-high`, and assert `graph.icon_pack_contributions()` contains module id `@mesh/icons-default` with id `default`.</action>
  <verify>nix develop -c cargo test -p mesh-core-module package::tests shipped_module</verify>
  <acceptance_criteria>
    - `crates/core/extension/module/src/package/tests.rs` contains `shipped_module`.
    - `crates/core/extension/module/src/package/tests.rs` contains `load_installed_module_graph(&workspace_root.join("config/module.json"))`.
    - `crates/core/extension/module/src/package/tests.rs` contains `@mesh/navigation-bar`.
    - `crates/core/extension/module/src/package/tests.rs` contains `@mesh/audio-interface`.
    - `crates/core/extension/module/src/package/tests.rs` contains `@mesh/icons-default`.
    - `crates/core/extension/module/src/package/tests.rs` contains `@mesh/pipewire-audio`.
    - `crates/core/extension/module/src/package/tests.rs` contains `@mesh/pulseaudio-audio`.
    - `crates/core/extension/module/src/package/tests.rs` contains `audio-volume-high`.
    - `nix develop -c cargo test -p mesh-core-module package::tests shipped_module` exits 0.
  </acceptance_criteria>
  <done>The package test suite proves the real shipped audio/navigation path through canonical manifests and typed installed-graph records.</done>
</task>

<task type="auto">
  <name>Task 3: Add proof-path diagnostics coverage</name>
  <files>crates/core/extension/module/src/package/tests.rs</files>
  <read_first>crates/core/extension/module/src/package/tests.rs, crates/core/extension/module/src/package/installed_graph.rs, modules/frontend/navigation-bar/module.json, modules/icon-packs/material-symbols/module.json, modules/icon-packs/default/module.json, .planning/todos/pending/2026-05-15-define-module-install-requirement-resolution.md</read_first>
  <action>Per D-02, D-04, D-05, D-06, and the folded install-resolution todo, add or extend a focused package test named with `shipped_module_diagnostics` that starts from the real navigation-bar manifest semantics and proves missing or incompatible contribution data is visible through `graph.diagnostics()`. Use a small fixture only if mutating the real shipped manifests would be brittle. The assertions must include diagnostic `status`, `module_id == "@mesh/navigation-bar"` or the fixture's navigation-equivalent frontend id, and when available a `contribution_id` containing the missing semantic icon name. The test must keep diagnostics non-fatal for missing optional resource/provider data, and it must not implement installer policy, provider conflict UI, resource-pack suggestions, settings materialization, or a cascade resolver.</action>
  <verify>nix develop -c cargo test -p mesh-core-module package::tests shipped_module_diagnostics</verify>
  <acceptance_criteria>
    - `crates/core/extension/module/src/package/tests.rs` contains `shipped_module_diagnostics`.
    - `crates/core/extension/module/src/package/tests.rs` contains `graph.diagnostics()`.
    - `crates/core/extension/module/src/package/tests.rs` contains `missing_required_icon` or another exact graph diagnostic status tied to the proof path.
    - `crates/core/extension/module/src/package/tests.rs` contains `@mesh/navigation-bar` or a fixture frontend id explicitly named as a navigation proof equivalent.
    - `nix develop -c cargo test -p mesh-core-module package::tests shipped_module_diagnostics` exits 0.
  </acceptance_criteria>
  <done>The installed graph exposes proof-path diagnostics for missing or incompatible resource/contribution data without adding installer policy.</done>
</task>

</tasks>

<verification>
- Run `nix develop -c cargo test -p mesh-core-module package::tests shipped_module`.
- Run `nix develop -c cargo test -p mesh-core-module package::tests shipped_module_diagnostics`.
- Run `nix develop -c cargo test -p mesh-core-module package::tests`.
</verification>

<success_criteria>
- A real bundled audio/navigation path loads through canonical `module.json` manifests from `config/module.json`.
- The graph proves active provider selection, provider multiplicity, layout entrypoint, frontend requirements, settings/keybind/icon contribution data, and visible diagnostics.
- The proof does not rely on service-specific Rust APIs or a toy module as the primary evidence.
</success_criteria>
