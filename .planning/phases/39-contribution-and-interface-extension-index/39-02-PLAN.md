---
phase: 39-contribution-and-interface-extension-index
plan: 02
title: Provider Dependency Capability Split
type: execute
wave: 2
depends_on: [39-01]
files_modified:
  - crates/core/extension/module/src/package/installed_graph.rs
  - crates/core/extension/module/src/package/module_manifest.rs
  - crates/core/extension/module/src/package/tests.rs
  - crates/core/shell/src/shell/backend/candidates.rs
  - crates/core/shell/src/shell/tests.rs
autonomous: true
requirements: [EXT-02, EXT-04]
requirements_addressed: [EXT-02, EXT-04]
user_setup: []
must_haves:
  truths:
    - "D-39-03: Provider declarations, interface declarations, and frontend dependencies are separate graph concepts."
    - "D-39-04: Host capabilities are explicit module permissions, never inferred from provider identity."
    - "D-39-05: Backend provider diagnostics distinguish missing provider, missing active provider, missing contract, and missing host capability."
  artifacts:
    - path: "crates/core/extension/module/src/package/installed_graph.rs"
      provides: "Backend provider records and frontend requirement checks"
      contains: "BackendProviderNode"
    - path: "crates/core/shell/src/shell/backend/candidates.rs"
      provides: "Backend launch candidate diagnostics"
      contains: "backend_launch_candidates_from_graph"
---

<objective>
Keep backend providers, frontend interface requirements, and host capability requests separate in graph data and shell diagnostics so provider identity never grants host powers.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-contribution-and-interface-extension-index/39-RESEARCH.md
@.planning/phases/39-contribution-and-interface-extension-index/39-VALIDATION.md
@.planning/phases/39-contribution-and-interface-extension-index/39-PATTERNS.md
@crates/core/extension/module/src/package/installed_graph.rs
@crates/core/extension/module/src/package/module_manifest.rs
@crates/core/shell/src/shell/backend/candidates.rs
@crates/core/shell/src/shell/tests.rs
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| T-39-02-01: a provider module id or interface name is treated as proof of host capability | high | Add tests and diagnostics proving capabilities only come from `mesh.capabilities` and are not synthesized from `mesh.implements` or frontend dependencies. |
| T-39-02-02: shell diagnostics collapse missing provider, no active provider, and contract mismatch into one vague invalid state | medium | Keep distinct `BackendLifecycleStatusRecord.status` values and add focused shell tests. |
| T-39-02-03: active provider validation routes through service-specific branches | high | Use generic graph/provider/interface data and grep for new concrete service names in touched Rust files. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Extend provider records without merging concepts</name>
  <files>crates/core/extension/module/src/package/installed_graph.rs, crates/core/extension/module/src/package/tests.rs</files>
  <read_first>crates/core/extension/module/src/package/installed_graph.rs, crates/core/extension/module/src/package/module_manifest.rs, .planning/phases/39-contribution-and-interface-extension-index/39-RESEARCH.md</read_first>
  <action>Extend `BackendProviderNode` to retain provider declaration metadata needed by tools: `version`, `base_module`, `provider`, `label`, `priority`, and a capability summary copied from the backend module's explicit `mesh.capabilities`. Do not add any field that treats frontend dependency data as provider data. Update sorting and active provider lookup unchanged. Add package tests that a provider node exposes declared metadata and explicit capabilities only.</action>
  <verify>cargo test -p mesh-core-module package::tests provider_capability</verify>
  <acceptance_criteria>
    - `BackendProviderNode` records provider version and base module when declared.
    - `BackendProviderNode` capability data comes only from the backend module's `mesh.capabilities`.
    - A frontend `mesh.dependencies.backend` entry does not alter provider capability data.
    - Existing active provider tests still pass.
    - `cargo test -p mesh-core-module package::tests provider_capability` exits 0.
  </acceptance_criteria>
  <done>Provider records carry useful metadata without inferring permissions.</done>
</task>

<task type="auto">
  <name>Task 2: Add distinct provider/dependency/capability diagnostics</name>
  <files>crates/core/extension/module/src/package/installed_graph.rs, crates/core/shell/src/shell/backend/candidates.rs, crates/core/shell/src/shell/tests.rs</files>
  <read_first>crates/core/shell/src/shell/backend/candidates.rs, crates/core/shell/src/shell/tests.rs, crates/core/extension/module/src/package/installed_graph.rs</read_first>
  <action>Keep `unmet_backend_requirement` for no enabled provider, keep `no_active_provider` for installed providers without a selected active provider, keep `invalid_manifest` or a more specific status for missing contract/provider registration mismatch, and add a separate status for missing required host capability if an installed graph provider is selected but its backend module lacks a capability expected by an explicit validation helper. The helper must not hard-code audio/network/service names; it should compare declared capability strings only when a requirement is explicitly available.</action>
  <verify>cargo test -p mesh-core-shell shell::tests backend</verify>
  <acceptance_criteria>
    - Shell tests distinguish no provider from no active provider.
    - Shell tests distinguish missing interface contract/provider registration from provider availability.
    - Capability diagnostics mention explicit capability ids, not inferred service names.
    - No new concrete service-specific branch is added to `backend/candidates.rs`.
    - `cargo test -p mesh-core-shell shell::tests backend` exits 0.
  </acceptance_criteria>
  <done>Provider status reporting preserves separate failure causes.</done>
</task>

<task type="auto">
  <name>Task 3: Prove service-specific Rust branches are unnecessary</name>
  <files>crates/core/extension/module/src/package/tests.rs, crates/core/shell/src/shell/tests.rs</files>
  <read_first>crates/core/extension/module/src/package/tests.rs, crates/core/shell/src/shell/tests.rs, docs/module-system.md</read_first>
  <action>Add tests using generic interface ids such as `mesh.example` and `mesh.example.alt` to prove backend launch candidate selection, active provider validation, and frontend requirement diagnostics work without interface-specific Rust branches. Include a grep-style acceptance check in the plan summary or test comments only if useful; do not add runtime code that special-cases `audio`.</action>
  <verify>cargo test -p mesh-core-module package::tests installed_module_graph && cargo test -p mesh-core-shell shell::tests backend</verify>
  <acceptance_criteria>
    - At least one package-level test uses a non-audio interface id and passes.
    - At least one shell backend test uses a non-audio interface id and passes.
    - Existing audio provider tests still pass as data-driven cases.
    - `rg -n "mesh\\.audio|audio" crates/core/shell/src/shell/backend/candidates.rs` shows no new service-specific logic beyond existing generic tests or messages.
  </acceptance_criteria>
  <done>Provider behavior is data-driven by manifests, not service-specific Rust code.</done>
</task>

</tasks>

<verification>
- Run `cargo test -p mesh-core-module package::tests provider_capability`.
- Run `cargo test -p mesh-core-shell shell::tests backend`.
- Run `rg -n "mesh\\.audio|audio" crates/core/shell/src/shell/backend/candidates.rs` and confirm no service-specific branch was added.
</verification>

<success_criteria>
- Provider declarations, frontend dependencies, and host capability requests are separately represented.
- Provider identity does not imply service read/control permission.
- Backend diagnostics distinguish the important compatibility failure modes.
- New behavior remains generic across interface ids.
</success_criteria>
