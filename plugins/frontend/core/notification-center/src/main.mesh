<template>
  <row class="notifications">
    <column class="main">
      <NotificationFeed/>
      <slot name="content"/>
    </column>
    <column class="rail">
      <NotificationSidebar/>
      <slot name="sidebar"/>
    </column>
  </row>
</template>

<style>
.notifications {
    background: token(color.surface);
    color: token(color.on-surface);
    padding: token(spacing.lg);
    gap: token(spacing.md);
    align-items: start;
}

.main {
    flex-grow: 1;
}

.rail {
    width: 140px;
    gap: token(spacing.sm);
}
</style>
