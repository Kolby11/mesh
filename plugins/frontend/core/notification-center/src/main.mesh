<template>
  <column class="notifications">
    <text class="title">Notifications</text>
    <column class="list">
      <box class="item">
        <text class="summary">System ready</text>
        <text class="body">MESH notification center initialized.</text>
      </box>
    </column>
  </column>
</template>

<style>
.notifications {
    background: token(color.surface);
    color: token(color.on-surface);
    padding: token(spacing.lg);
    gap: token(spacing.md);
}

.title {
    font-size: token(typography.size.lg);
    font-weight: 700;
}

.list {
    gap: token(spacing.sm);
}

.item {
    padding: token(spacing.md);
    border-radius: token(radius.md);
    background: token(color.surface-variant);
}
</style>

<meta>
name = "MESH Notification Center"
description = "Default notification center surface"
role = "region"
label = "Notification center"
</meta>
