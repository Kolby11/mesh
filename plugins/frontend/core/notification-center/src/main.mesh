<template>
  <column class="notifications">
    <box class="main">
      <NotificationFeed/>
      <slot name="content"/>
    </box>
    <column class="rail">
      <NotificationSidebar/>
      <slot name="sidebar"/>
    </column>
  </column>
</template>

<script lang="luau">
import NotificationFeed from "@mesh/notification-feed"
import NotificationSidebar from "@mesh/notification-sidebar"
</script>

<style>
.notifications {
    background: token(color.surface);
    color: token(color.on-surface);
    padding: token(spacing.lg);
    gap: token(spacing.md);
    align-items: start;
}

.main {
    flex-grow: 1;
}

.rail {
    width: 140px;
    gap: token(spacing.sm);
}
</style>
