<template>
  <aside class="sidebar">
    <span class="title">Sidebar Pane</span>
    <span class="copy">A second imported widget that gives the host surface a stable right rail.</span>

    <div class="card">
      <span class="label">Pinned tools</span>
      <span>Notifications</span>
      <span>Quick settings</span>
      <span>Scratchpad</span>
    </div>

    <div class="card">
      <span class="label">Composition notes</span>
      <span>Direct imports use exported component tags.</span>
      <span>Slots can layer extra widgets on top later.</span>
      <span>Surface placement comes from config/settings.json.</span>
    </div>
  </aside>
</template>

<style>
.sidebar {
    background: token(color.surface-container-high);
    border-radius: token(radius.lg);
    padding: token(spacing.lg);
    gap: token(spacing.md);
}

.title {
    font-size: token(typography.size.lg);
    font-weight: 700;
}

.copy {
    color: token(color.on-surface-variant);
}

.card {
    background: token(color.surface-container);
    border-radius: token(radius.md);
    padding: token(spacing.md);
    gap: token(spacing.xs);
}

.label {
    color: token(color.on-surface-variant);
    font-size: token(typography.size.sm);
    font-weight: 700;
}
</style>
