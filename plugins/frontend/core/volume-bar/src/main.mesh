<template>
  <section class="volume-bar">
    <icon class="glyph" name="{audio.icon_name}" size="18" />
    <slider
      class="track"
      orient="vertical"
      min="0"
      max="100"
      value="{audio.percent}"
      mesh-action="audio-volume"
      title="{audio.tooltip}"
      aria-label="Volume"
    />
    <span class="label">{audio.label}</span>
  </section>
</template>

<script lang="luau">
function onToggleMute()
    mesh.events.publish("mesh.audio.toggle-mute", {})
end
</script>

<style>
.volume-bar {
    width: 100%;
    height: 100%;
    flex-direction: column;
    align-items: center;
    padding-block: token(spacing.sm);
    gap: token(spacing.xs);
    background: token(color.surface);
    color: token(color.on-surface);
    border-radius: token(radius.sm);
}

.glyph {
    width: 28px;
    height: 26px;
    flex-shrink: 0;
    color: token(color.on-secondary-container);
}

.track {
    width: 28px;
    flex-grow: 1;
    color: token(color.primary);
}

.label {
    flex-shrink: 0;
    font-size: token(typography.size.xs);
    font-weight: 700;
    color: token(color.on-surface-variant);
}
</style>
