<template>
  <aside class="sidebar">
    <span class="heading">Quick actions</span>
    <button>Mark all read</button>
    <button>Pause notifications</button>
    <button>Open settings</button>
  </aside>
</template>

<style>
.sidebar {
    gap: token(spacing.sm);
}

.heading {
    font-size: token(typography.size.md);
    font-weight: 700;
}
</style>
