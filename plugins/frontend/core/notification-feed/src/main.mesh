<template>
  <section class="notifications">
    <span class="title">Notifications</span>
    <div class="list">
      <article class="item">
        <span class="summary">System ready</span>
        <span class="body">MESH notification center initialized.</span>
      </article>
      <article class="item">
        <span class="summary">Plugin composition enabled</span>
        <span class="body">Notification center content now comes from widget plugins.</span>
      </article>
    </div>
  </section>
</template>

<style>
.notifications {
    gap: token(spacing.md);
}

.title {
    font-size: token(typography.size.lg);
    font-weight: 700;
}

.list {
    gap: token(spacing.sm);
}

.item {
    padding: token(spacing.md);
    border-radius: token(radius.md);
    background: token(color.surface-variant);
}
</style>
