<template>
  <section class="launcher">
    <span class="title">Launcher Pane</span>
    <span class="copy">Use this widget as the primary imported component inside a host surface.</span>
    <input class="search" type="text" placeholder="Search apps, actions, files" />

    <div class="chips">
      <button class="chip">Terminal</button>
      <button class="chip">Browser</button>
      <button class="chip">Editor</button>
    </div>

    <div class="stack">
      <button class="item">Open workspace dashboard</button>
      <button class="item">Jump to recent project</button>
      <button class="item">Create scratch note</button>
      <button class="item">Launch system monitor</button>
    </div>
  </section>
</template>

<style>
.launcher {
    background: token(color.surface-container);
    border-radius: token(radius.lg);
    padding: token(spacing.lg);
    gap: token(spacing.md);
}

.title {
    font-size: token(typography.size.lg);
    font-weight: 700;
}

.copy {
    color: token(color.on-surface-variant);
}

.search {
    width: 100%;
}

.chips {
    gap: token(spacing.sm);
}

.chip {
    justify-content: start;
}

.stack {
    gap: token(spacing.sm);
}

.item {
    justify-content: start;
}
</style>
