<script lang="luau">
    local t = require("@mesh/i18n").t
</script>


<template>
  <section class="launcher">
    <span class="title">{headline}</span>
    <input class="search" type="text" placeholder="Search apps" />
    <span class="status">{statusMessage}</span>
    <div class="results">
      <button class="result" onclick={onLaunchTerminal}>Terminal</button>
      <button class="result" onclick={onLaunchFiles}>{t(results.files)}</button>
      <button class="result" onclick={onLaunchBrowser}>{t(results.browser)}</button>
      <button class="result" onclick={onLaunchEditor}>Editor</button>
      <button class="result" onclick={onLaunchMonitor}>System Monitor</button>
      <button class="result" onclick={onLaunchSettings}>Settings</button>
      <button class="result" onclick={onLaunchMusic}>Music</button>
      <button class="result" onclick={onLaunchMessages}>Messages</button>
    </div>
  </section>
</template>

<style>
.title {
  color: token(color.on-surface)
}

.launcher {
    width: 360px;
    border-radius: token(radius.lg);
    background: token(color.surface);
    color: token(color.on-surface);
    padding: token(spacing.lg);
    gap: token(spacing.md);
}

.title {
    font-size: token(typography.size.lg);
    font-weight: 700;
}

.status {
    color: token(color.on-surface-variant);
    font-size: token(typography.size.sm);
}

.results {
    gap: token(spacing.sm);
    height: 240px;
}

.result {
    justify-content: start;
}
</style>
