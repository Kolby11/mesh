<template>
  <column class="launcher">
    <text class="title">Applications</text>
    <input class="search" placeholder="Search apps" />
    <column class="results">
      <button class="result">Terminal</button>
      <button class="result">Files</button>
      <button class="result">Browser</button>
    </column>
  </column>
</template>

<style>
.launcher {
    background: token(color.surface);
    color: token(color.on-surface);
    padding: token(spacing.lg);
    gap: token(spacing.md);
}

.title {
    font-size: token(typography.size.lg);
    font-weight: 700;
}

.results {
    gap: token(spacing.sm);
}
</style>

<meta>
name = "MESH Launcher"
description = "Default application launcher"
role = "dialog"
label = "Application launcher"
</meta>
