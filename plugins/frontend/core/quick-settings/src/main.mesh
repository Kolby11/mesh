<template>
  <section class="quick-settings">
    <span class="title">Quick Settings</span>
    <div class="toggles">
      <button class="chip">Wi-Fi</button>
      <button class="chip">Bluetooth</button>
      <button class="chip">Do Not Disturb</button>
    </div>
    <div class="audio">
      <span class="label">Volume</span>
      <input class="volume" type="range" min="0" max="100" value="42" />
    </div>
  </section>
</template>

<style>
.quick-settings {
    background: token(color.surface);
    color: token(color.on-surface);
    padding: token(spacing.lg);
    gap: token(spacing.md);
}

.title {
    font-size: token(typography.size.lg);
    font-weight: 700;
}

.toggles {
    gap: token(spacing.sm);
}

.audio {
    gap: token(spacing.sm);
}
</style>
