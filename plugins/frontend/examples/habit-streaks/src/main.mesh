<template>
  <div class="streaks">
    <span class="headline">{headline}</span>
    <div class="chip">
      <span class="chip-text">{first}</span>
    </div>
    <div class="chip">
      <span class="chip-text">{second}</span>
    </div>
    <div class="chip">
      <span class="chip-text">{third}</span>
    </div>
  </div>
</template>

<style>
.streaks {
    gap: token(spacing.sm);
    align-items: center;
}

.headline {
    color: token(color.on-surface-variant);
    font-size: token(typography.size.sm);
}

.chip {
    padding: token(spacing.sm);
    border-radius: token(radius.full);
    background: token(color.secondary-container);
}

.chip-text {
    color: token(color.on-secondary-container);
    font-size: token(typography.size.sm);
}
</style>
