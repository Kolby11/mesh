<template>
  <section class="timer">
    <span class="eyebrow">Focus session</span>
    <span class="label">{label}</span>
    <span class="remaining">{remaining}</span>
    <div class="meta">
      <span class="phase">{phase}</span>
      <span class="streak">Streak 4</span>
    </div>
    <button class="button">Pause timer</button>
  </section>
</template>

<style>
.timer {
    gap: token(spacing.sm);
    padding: token(spacing.md);
    border-radius: token(radius.lg);
    background: token(color.primary-container);
}

.eyebrow {
    color: token(color.on-primary-container);
    font-size: token(typography.size.sm);
}

.label {
    font-size: token(typography.size.lg);
    font-weight: 700;
    color: token(color.on-primary-container);
}

.remaining {
    font-size: token(typography.size.2xl);
    font-weight: 700;
    color: token(color.on-primary-container);
}

.meta {
    justify-content: space-between;
}

.phase {
    color: token(color.on-primary-container);
}

.streak {
    color: token(color.on-primary-container);
    font-size: token(typography.size.sm);
}

.button {
    background: token(color.surface);
    color: token(color.on-surface);
}
</style>
