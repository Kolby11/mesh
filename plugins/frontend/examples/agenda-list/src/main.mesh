<template>
  <column class="agenda">
    <box class="header">
      <text class="heading">{heading}</text>
      <text class="count">3 items</text>
    </box>
    <box class="items">
      <column class="item emphasis">
        <text class="title">{first}</text>
        <text class="meta">North room · 45 min</text>
      </column>
      <column class="item">
        <text class="title">{second}</text>
        <text class="meta">Remote · notes required</text>
      </column>
      <column class="item">
        <text class="title">{third}</text>
        <text class="meta">Board deck draft</text>
      </column>
    </box>
  </column>
</template>

<style>
.agenda {
    gap: token(spacing.md);
    padding: token(spacing.md);
    border-radius: token(radius.lg);
    background: token(color.surface-container);
}

.header {
    justify-content: space-between;
    align-items: center;
}

.heading {
    font-size: token(typography.size.lg);
    font-weight: 700;
}

.count {
    color: token(color.on-surface-variant);
    font-size: token(typography.size.sm);
}

.items {
    gap: token(spacing.sm);
}

.item {
    gap: token(spacing.xs);
    padding: token(spacing.sm);
    border-radius: token(radius.md);
    background: token(color.surface-container-high);
}

.emphasis {
    background: token(color.tertiary-container);
}

.title {
    font-weight: 600;
}

.meta {
    color: token(color.on-surface-variant);
    font-size: token(typography.size.sm);
}
</style>
