<template>
  <section class="agenda">
    <div class="header">
      <span class="heading">{heading}</span>
      <span class="count">3 items</span>
    </div>
    <div class="items">
      <article class="item emphasis">
        <span class="title">{first}</span>
        <span class="meta">North room · 45 min</span>
      </article>
      <article class="item">
        <span class="title">{second}</span>
        <span class="meta">Remote · notes required</span>
      </article>
      <article class="item">
        <span class="title">{third}</span>
        <span class="meta">Board deck draft</span>
      </article>
    </div>
  </section>
</template>

<style>
.agenda {
    gap: token(spacing.md);
    padding: token(spacing.md);
    border-radius: token(radius.lg);
    background: token(color.surface-container);
}

.header {
    justify-content: space-between;
    align-items: center;
}

.heading {
    font-size: token(typography.size.lg);
    font-weight: 700;
}

.count {
    color: token(color.on-surface-variant);
    font-size: token(typography.size.sm);
}

.items {
    gap: token(spacing.sm);
}

.item {
    gap: token(spacing.xs);
    padding: token(spacing.sm);
    border-radius: token(radius.md);
    background: token(color.surface-container-high);
}

.emphasis {
    background: token(color.tertiary-container);
}

.title {
    font-weight: 600;
}

.meta {
    color: token(color.on-surface-variant);
    font-size: token(typography.size.sm);
}
</style>
