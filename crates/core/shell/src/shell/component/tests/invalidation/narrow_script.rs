use super::*;

#[test]
#[ignore = "wave 0 stub, populated by tasks 2-4"]
fn script_narrow_flag_does_not_trigger_tree_rebuild() {
    unimplemented!("INV-01 narrow routing")
}

#[test]
#[ignore = "wave 0 stub, populated by tasks 2-4"]
fn narrow_path_dirties_ancestor_chain() {
    unimplemented!("INV-01 ancestor expansion")
}

#[test]
#[ignore = "wave 0 stub, populated by tasks 2-4"]
fn structural_change_falls_back_to_tree_rebuild() {
    unimplemented!("INV-01 structural fallback")
}

#[test]
#[ignore = "wave 0 stub, populated by tasks 2-4"]
fn threshold_fallback_exceeds_half() {
    unimplemented!("INV-03 threshold")
}

#[test]
#[ignore = "wave 0 stub, populated by tasks 2-4"]
fn threshold_narrow_below_half() {
    unimplemented!("INV-03 threshold")
}
