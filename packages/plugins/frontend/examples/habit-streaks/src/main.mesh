<template>
  <box class="streaks">
    <text class="headline">{headline}</text>
    <box class="chip">
      <text class="chip-text">{first}</text>
    </box>
    <box class="chip">
      <text class="chip-text">{second}</text>
    </box>
    <box class="chip">
      <text class="chip-text">{third}</text>
    </box>
  </box>
</template>

<style>
.streaks {
    gap: token(spacing.sm);
    align-items: center;
}

.headline {
    color: token(color.on-surface-variant);
    font-size: token(typography.size.sm);
}

.chip {
    padding: token(spacing.sm);
    border-radius: token(radius.full);
    background: token(color.secondary-container);
}

.chip-text {
    color: token(color.on-secondary-container);
    font-size: token(typography.size.sm);
}
</style>
